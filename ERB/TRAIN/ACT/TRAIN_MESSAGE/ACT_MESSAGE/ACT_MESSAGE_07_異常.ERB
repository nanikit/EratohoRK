;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;		이상계 메세지

;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;070, 피스트퍽
;────────────────────────────────────
@TRAIN_MESSAGE_70
PRINTFORML %CALLNAME:TARGET%는%CALLNAME:MASTER%의 질에 주먹으로 침입하기 시작했다…

;────────────────────────────────────
;071, 애널피스트
;────────────────────────────────────
@TRAIN_MESSAGE_71
PRINTFORML %CALLNAME:TARGET%는%CALLNAME:MASTER%의 아누스에 주먹으로 침입하기 시작했다…

;────────────────────────────────────
;072, 양구멍피스트
;────────────────────────────────────
@TRAIN_MESSAGE_72
PRINTFORML %CALLNAME:TARGET%는%CALLNAME:MASTER%의 질과 아누스에 주먹으로 침입하기 시작했다…

;────────────────────────────────────
;073, 방치플레이
;────────────────────────────────────
;★리액션전★
@TRAIN_MESSAGE_73
PRINTFORML %CALLNAME:TARGET%는%CALLNAME:MASTER%를 그 자리에 방치했다…

;☆리액션 후☆
@TRAIN_MESSAGE2_73
PRINTFORML %CALLNAME:TARGET%는%CALLNAME:MASTER%를 방치한 채로, 아무것도 하려고는 하지 않는다…
;REACTION_MESSAGE를 처리하면 정합성에 문제가 나오기 때문에(위해), RESULT 1을 돌려주어 완결시킨다@/L
SIF IS_NOWCOMNAME("하는대로")
	RETURN 1

;────────────────────────────────────
;074, 공중육변기플레이
;────────────────────────────────────
;★리액션전★
@TRAIN_MESSAGE_74
PRINTFORML %CALLNAME:TARGET%는 밖에서 속박되었다%CALLNAME:MASTER%의 배에 매직으로 「↓공중변소」라고 써,
PRINTFORML 그 자리에 두어 혼자서 돌아가려고 했다…

;☆리액션 후☆
@TRAIN_MESSAGE2_74
PRINTL
IF TFLAG:ACT파생 == 1                   ;ACT파생가 구속을 풀어 함께 조교 방으로 돌아갔다(애원)
	PRINTFORML %CALLNAME:MASTER%의 애원을 들어주어,%CALLNAME:TARGET%는%CALLNAME:MASTER%의 구속을 풀어 함께 조교 방으로 돌아갔다…
ELSE               ;방치를 했다
	PRINTFORML %CALLNAME:TARGET%는%CALLNAME:MASTER%를 야외에 방치해 혼자서 돌아갔다…
	IF TFLAG:ACT파생 == 0                ;ACT파생가 끝까지 사람은 오지 않았다(무사)
		PRINTFORML 혼란하고 있었다%CALLNAME:MASTER%였지만, 끝까지 사람은 오지 않았다
		PRINTFORML 겨우%CALLNAME:TARGET%에 회수되어%CALLNAME:MASTER%는 안심했다…
	ELSE            ;발견되었다
		PRINTFORML 잠시 후에, 지나감의 사람에게 발견되어 버렸다.
		IF TFLAG:ACT파생 == 2           ;ACT파생가 마음도 몸도 너덜너덜(능욕)
			PRINTFORML 그리고의%CALLNAME:MASTER%의 운명을 상상하는 것은 어렵지 않을 것이다…
			PRINTFORML %CALLNAME:TARGET%에 회수되었다%CALLNAME:MASTER%는 마음도 몸도 너덜너덜이었다…
		ELSE                            ;ACT파생가 아직 어딘지 부족한 모습(흥분)
			PRINTFORML 인이 모여,%CALLNAME:MASTER%피부응이다 흥분해 그 상황을 어딘가 즐기고 있었다
			PRINTFORML %CALLNAME:TARGET%에 회수되었다%CALLNAME:MASTER%는 아직 어딘지 부족한 모습이었다…
		ENDIF
	ENDIF
ENDIF
PRINTL

;────────────────────────────────────
;075, 페니스밴드장착
;────────────────────────────────────
;★리액션전★
@TRAIN_MESSAGE_75
IF TFLAG:ACT파생 == 0         ;장착
	IF TALENT:처녀
		PRINTFORML %CALLNAME:TARGET%는%TARGET_DESCRIPTION("개개")%검은 윤기 하는 쌍두 페니스밴드를 꺼내면,
		PRINTFORML 파과의 고통에 얼굴을 찡그리면서,%CALLNAME:MASTER%의 눈앞에서 그것을 몸에 걸쳤다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%는%TARGET_DESCRIPTION("")%검은 윤기 하는 쌍두 페니스밴드를 몸에 대어,
		PRINTFORML 그 첨단을%CALLNAME:MASTER%에 과시했다…
	ENDIF
ELSE                          ;해제
	PRINTFORML %CALLNAME:TARGET%는 몸에 대고 있던 페니스밴드를 떼어냈다…
ENDIF
