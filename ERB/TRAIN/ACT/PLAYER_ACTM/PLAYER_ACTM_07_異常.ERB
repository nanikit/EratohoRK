;─
;이상(Fist70/AFi71/양Fi72/방치 73/공중 74/P장 75/비웃는다 9/다리로 18/족지 55/목마 67)
;─
@ACT_M7(ARG = 0)
;1이상이라면 판정을 실시하지 않는다
#DIM LCOUNT, 1

;함수 초기화
CALL ACT_M_INIT(7)

;처리 개시

LOCALS = ACT_ACTM7

CALL CHA_CRI_VAR, "기준치"
CALLFORM %LOCALS%_REFERENCE

CALL CHA_CRI_VAR, "조교자의 소질"
CALLFORM %LOCALS%_TALENT_T

CALL CHA_CRI_VAR, "조교 대상의 소질"
CALLFORM %LOCALS%_TALENT_M

CALL CHA_CRI_VAR, "조교 대상의 피스트 적성"
CALLFORM %LOCALS%_FIST

CALL CHA_CRI_VAR, "조교자의 능력"
CALLFORM %LOCALS%_ABL_T

CALL CHA_CRI_VAR, "조교 대상의 능력"
CALLFORM %LOCALS%_ABL_M

CALL CHA_CRI_VAR, "조교자의 ALI"
CALLFORM %LOCALS%_ALI_T

CALL CHA_CRI_VAR, "조교자의 기력"
CALLFORM %LOCALS%_MENTAL_T

CALL CHA_CRI_VAR, "조교자의 이성"
CALLFORM %LOCALS%_REASON_T

CALL CHA_CRI_VAR, "조교자의 흥미"
CALLFORM %LOCALS%_INTEREST_T

CALL CHA_CRI_VAR, "조교 대상의 체력"
CALLFORM %LOCALS%_PHYSICAL_M

CALL CHA_CRI_VAR, @"조교자 상태(%CONDITION(TCVAR:상태변화)%)"
CALLFORM %LOCALS%_CONDITION_T

CALL CHA_CRI_VAR, "미약"
CALLFORM %LOCALS%_DRAG

CALL CHA_CRI_VAR, "줄"
CALLFORM %LOCALS%_ROPE

CALL CHA_CRI_VAR, "아이마스크"
CALLFORM %LOCALS%_EYEMASK

CALL CHA_CRI_VAR, "볼개그"
CALLFORM %LOCALS%_GAG

CALL CHA_CRI_VAR, "삼각목마"
CALLFORM %LOCALS%_HORSE

CALL CHA_CRI_VAR, "야외플레이"
CALLFORM %LOCALS%_OUT

CALL CHA_CRI_VAR, "도구를 떼었다"
CALLFORM %LOCALS%_TAKE_OFF_TOOLS

CALL CHA_CRI_VAR, "조교자 자위중"
CALLFORM %LOCALS%_MASTURBATION_T

CALL CHA_CRI_VAR, "조교 대상의 의복"
CALLFORM %LOCALS%_CLOTH_M

CALL CHA_CRI_VAR, "더하드하게"
CALLFORM %LOCALS%_PLEASE_HARD

CALL CHA_CRI_VAR, "용서를빈다"
CALLFORM %LOCALS%_PLEASE_HELP

CALL CHA_CRI_VAR, "벌"
CALLFORM %LOCALS%_OSHIOKI

CALL CHA_CRI_VAR, "연속행동 보정"
CALLFORM %LOCALS%_PREV

CALL CHA_CRI_VAR, "구상 보정치"
CALL ACT_ACTM_KOJO_CORRECTION_T, 7

CALL CHA_CRI_VAR, "ACT 보정치"
CALLFORM %LOCALS%_CORRECTION(ARG)

;ARG가 1이상이라면 처리 중단
SIF ARG
	RETURN RESULT

CALL CHA_CRI_VAR, "실행 판정"
CALLFORM %LOCALS%_ABLE

;─
;최종 판정
;─
CALL DEF_CRI_VAR("최대 전자")
LOCAL = GET_CRI_VAR("결정")
IF LOCAL == -1
	SIF FLAG:디버그
		PRINTL (이상 ACT분류에 실행 가능한 ACT가 존재하지 않습니다)
	TFLAG:ACT = -1
	CALL ACT_MENU_RESULT(2, "이상")
ELSE
	TFLAG:ACT = GET_ACTNUM(RESULTS)
ENDIF
CALL ANA_CRI_VAR()

;-------------------------------------------------------------------------
;기준치
;-------------------------------------------------------------------------
@ACT_ACTM7_REFERENCE

;필요하지 않을지도라고 생각했으므로 시험삼아 무효화@이것 보고
[SKIPSTART]
CALL ADD_CRI_VAR("피스트퍽", 10)
CALL ADD_CRI_VAR("애널피스트", 10)
CALL ADD_CRI_VAR("양구멍피스트", 10)
CALL ADD_CRI_VAR("방치플레이", 10)
CALL ADD_CRI_VAR("공중육변기플레이", 10)
CALL ADD_CRI_VAR("기분나쁘게웃는다", 8)
CALL ADD_CRI_VAR("풋잡한다", 5)
CALL ADD_CRI_VAR("다리빨고강제", 5)
CALL ADD_CRI_VAR("삼각목마", 5)
[SKIPEND]

;-------------------------------------------------------------------------
;조교자의 소질을 참조
;-------------------------------------------------------------------------
@ACT_ACTM7_TALENT_T

;조교자가 겁쟁이/기장
IF TALENT:겁쟁이
	CALL ADD_CRI_VAR("공중육변기플레이", -5)
ELSEIF TALENT:기장
	CALL ADD_CRI_VAR("공중육변기플레이", , 5)
ENDIF

;조교자가 얌전하다/거만함
IF TALENT:얌전함
	CALL ADD_CRI_VAR("피스트퍽", -5)
	CALL ADD_CRI_VAR("애널피스트", -6)
	CALL ADD_CRI_VAR("양구멍피스트", -7)
ELSEIF TALENT:거만함
	CALL ADD_CRI_VAR("방치플레이", , 4)
	CALL ADD_CRI_VAR("기분나쁘게웃는다", , 6)
	CALL ADD_CRI_VAR("풋잡한다", 2, 2)
ENDIF

;조교자가 프라이드높음/낮다
IF TALENT:프라이드높음
	CALL ADD_CRI_VAR("다리빨고강제", , 5)
ELSEIF TALENT:프라이드낮음
	CALL ADD_CRI_VAR("다리빨고강제", , -5)
ENDIF

;조교자가 자제적/충동적
IF TALENT:자제적
	CALL ADD_CRI_VAR("방치플레이", , 4)
	CALL ADD_CRI_VAR("공중육변기플레이", -5)
ELSEIF TALENT:충동적
	CALL ADD_CRI_VAR("공중육변기플레이", , 5)
	CALL ADD_CRI_VAR("삼각목마", 4)
ENDIF

;조교자가 무관심/호기심
IF TALENT:무관심
	CALL ADD_CRI_VAR("공중육변기플레이", 5)
ELSEIF TALENT:호기심
	CALL ADD_CRI_VAR("풋잡한다", , 6)
ENDIF

;조교자가 감정결핍/감정 풍부한
IF TALENT:감정결핍
	CALL ADD_CRI_VAR("공중육변기플레이", 3)
ELSEIF TALENT:감수성
	CALL ADD_CRI_VAR("기분나쁘게웃는다", , 6)
ENDIF

;조교자가튀고싶어함
IF TALENT:튀고싶어함
	CALL ADD_CRI_VAR("풋잡한다", 5)
	CALL ADD_CRI_VAR("다리빨고강제", 3)
ENDIF

;조교자가 억압/해방
IF TALENT:억압
	CALL ADD_CRI_VAR("공중육변기플레이", -5)
	CALL ADD_CRI_VAR("기분나쁘게웃는다", -5)
	CALL ADD_CRI_VAR("풋잡한다", -5)
	CALL ADD_CRI_VAR("다리빨고강제", -5)
ELSEIF TALENT:해방
	CALL ADD_CRI_VAR("공중육변기플레이", , 6)
	CALL ADD_CRI_VAR("풋잡한다", , 4)
ENDIF

;조교자가 수줍음/수치심옅음
IF TALENT:수줍음
	CALL ADD_CRI_VAR("공중육변기플레이", -5)
ELSEIF TALENT:수치심옅음
	CALL ADD_CRI_VAR("공중육변기플레이", 5)
ENDIF

;조교자가 헌신적/수동
IF TALENT:헌신적
	CALL ADD_CRI_VAR("풋잡한다", 5)
ELSEIF TALENT:수동
	CALL ADD_CRI_VAR("다리빨고강제", 5)
ENDIF

;조교자가 남성혐오로 조교 대상이 남자. 진전도에 응해 완화
SIF TALENT:남성혐오 && TALENT:MASTER:남자
	CALL ADD_CRI_VAR("풋잡한다", CFLAG:M호의 + CFLAG:호의 >= 10000? -4 # -7)

;조교자가 새디즘
IF TALENT:새디즘
	CALL ADD_CRI_VAR("다리빨고강제", , 6)
	CALL ADD_CRI_VAR("삼각목마", 3, 4)
ENDIF

;조교자가 신중/조급함
IF TALENT:신중
	CALL ADD_CRI_VAR("공중육변기플레이", -5)
ELSEIF TALENT:조급함
	CALL ADD_CRI_VAR("공중육변기플레이", 5)
ENDIF

;조교자가 심술쟁이/상냥함
IF TALENT:심술쟁이
	CALL ADD_CRI_VAR("기분나쁘게웃는다", 3)
	CALL ADD_CRI_VAR("풋잡한다", 3)
ELSEIF TALENT:상냥함
	CALL ADD_CRI_VAR("공중육변기플레이", -5)
	CALL ADD_CRI_VAR("삼각목마", -5)
ENDIF

;조교자가 광기
IF TALENT:광기
	CALL ADD_CRI_VAR("피스트퍽", , 4)
	CALL ADD_CRI_VAR("애널피스트", , 4)
	CALL ADD_CRI_VAR("양구멍피스트", , 4)
	CALL ADD_CRI_VAR("방치플레이", , 4)
	CALL ADD_CRI_VAR("공중육변기플레이", , 4)
	CALL ADD_CRI_VAR("기분나쁘게웃는다", , 7)
	CALL ADD_CRI_VAR("풋잡한다", , 4)
	CALL ADD_CRI_VAR("다리빨고강제", , 4)
	CALL ADD_CRI_VAR("삼각목마", , 4)
ENDIF

;조교자가 C민감
SIF TALENT:C민감
	CALL ADD_CRI_VAR("풋잡한다", 2, 4)

;----------------------------------------------------------------------
;피스트 적성
;경험과 쌍방의 소질이 관련되므로 소질로부터 독립
;
;다만, 작은체형 장신에 의해 곱셈을 걸치는 의의가 그다지 느껴지지 않고
;각각 경험과 소질로, 보통으로 가감(상태)하면 좋다고 생각하는 것과
;경험이 없을 때, 마이나스가 된다 라고 하는 것을 채용한다면 기초치의 개념 사용해
;----------------------------------------------------------------------
@ACT_ACTM7_FIST
;작은체형 체형/거구/VA경험/확장 경험/음계 소질 따위로 피스트의 처리를 해 본다
VARSET LOCAL

;V경험/확장 경험/음계 소질에 의해 증감
IF EXP:MASTER:V경험 < 10
	LOCAL:0 = -6
ELSEIF EXP:MASTER:V경험 < 20 && EXP:MASTER:V확장경험 < 1
	LOCAL:0 = -4
ELSEIF EXP:MASTER:V경험 < 40 && EXP:MASTER:V확장경험 < 3
	LOCAL:0 = -2
ENDIF
SIF TALENT:MASTER:음단지
	LOCAL:0 += 4

;A경험/확장 경험/음계 소질에 의해 증감
IF EXP:MASTER:A경험 < 10
	LOCAL:1 = -6
ELSEIF EXP:MASTER:A경험 < 20 && EXP:MASTER:A확장경험 < 1
	LOCAL:1 = -4
ELSEIF EXP:MASTER:A경험 < 40 && EXP:MASTER:A확장경험 < 3
	LOCAL:1 = -2
ENDIF
SIF TALENT:MASTER:음고
	LOCAL:1 += 4

;작은체형 체형/장신에 의해 증감
IF TALENT:MASTER:작은체형 || TALENT:장신
	LOCAL:0 = LOCAL:0 * 3 / 4
	LOCAL:1 = LOCAL:1 * 3 / 4
ELSEIF TALENT:MASTER:장신 || TALENT:작은체형
	LOCAL:0 = LOCAL:0 * 4 / 3
	LOCAL:1 = LOCAL:1 * 4 / 3
ENDIF

;가산 처리
CALL ADD_CRI_VAR("피스트퍽", LOCAL:0)
CALL ADD_CRI_VAR("애널피스트", LOCAL:1)
CALL ADD_CRI_VAR("양구멍피스트", LOCAL:0 + LOCAL:1)

;-------------------------------------------------------------------------
;조교 대상의 소질을 참조
;-------------------------------------------------------------------------
@ACT_ACTM7_TALENT_M

;조교 대상이 겁쟁이/기장
IF TALENT:MASTER:겁쟁이
	CALL ADD_CRI_VAR("기분나쁘게웃는다", 3)
ELSEIF TALENT:MASTER:기장
	CALL ADD_CRI_VAR("공중육변기플레이", 3)
ENDIF

;조교 대상이 반항적/순수
IF TALENT:MASTER:반항적
	CALL ADD_CRI_VAR("공중육변기플레이", 4)
	CALL ADD_CRI_VAR("삼각목마", 3)
ELSEIF TALENT:MASTER:순수
	CALL ADD_CRI_VAR("다리빨고강제", 3)
ENDIF

;조교 대상이 수줍음/수치심옅음
IF TALENT:MASTER:수줍음
	CALL ADD_CRI_VAR("방치플레이", 3)
ELSEIF TALENT:MASTER:수치심옅음
	CALL ADD_CRI_VAR("방치플레이", -3)
ENDIF

;조교 대상이 아픔에약함/아픔에강함
IF TALENT:MASTER:아픔에약함
	CALL ADD_CRI_VAR("삼각목마", 5)
ELSEIF TALENT:MASTER:아픔에강함
	CALL ADD_CRI_VAR("삼각목마", -5)
ENDIF

;조교 대상이 음경
SIF TALENT:MASTER:음핵／음경
	CALL ADD_CRI_VAR("풋잡한다", , 9)

;조교 대상이 C민감/둔감
IF TALENT:MASTER:C민감
	CALL ADD_CRI_VAR("풋잡한다", 1, 6)
ELSEIF TALENT:MASTER:C둔감
	CALL ADD_CRI_VAR("풋잡한다", -1, -6)
ENDIF

;조교 대상이 V민감/둔감
IF TALENT:MASTER:V민감
	CALL ADD_CRI_VAR("페니스밴드장착", TEQUIP:조교자페니스밴드 ? 0 # 3, TEQUIP:조교자페니스밴드 ? 0 # 1)
ELSEIF TALENT:MASTER:V둔감
	CALL ADD_CRI_VAR("페니스밴드장착", TEQUIP:페니스밴드 ? 0 # -3, TEQUIP:조교자페니스밴드 ? 0 # -1)
ENDIF

;조교자가 V민감/둔감
IF TALENT:TARGET:V민감
	CALL ADD_CRI_VAR("페니스밴드장착", TEQUIP:조교자페니스밴드 ? 0 # 3, TEQUIP:조교자페니스밴드 ? 0 # 1)
ELSEIF TALENT:TARGET:V둔감
	CALL ADD_CRI_VAR("페니스밴드장착", TEQUIP:조교자페니스밴드 ? 0 # -3, TEQUIP:조교자페니스밴드 ? 0 # -1)
ENDIF

;조교자가 처녀
SIF TALENT:TARGET:처녀
	CALL ADD_CRI_VAR("페니스밴드장착", -10)

;조교 대상이 호라이인
SIF TALENT:불사
	CALL ADD_CRI_VAR("공중육변기플레이", , 9)

;조교 대상이 후타나리
SIF TALENT:MASTER:후타나리
	CALL ADD_CRI_VAR("풋잡한다", TALENT:호기심 * 3 - TALENT:무관심, 4)

;조교 대상이 처녀
IF TALENT:MASTER:처녀
	CALL ADD_CRI_VAR("피스트퍽", -10)
	CALL ADD_CRI_VAR("양구멍피스트", -10)
	CALL ADD_CRI_VAR("공중육변기플레이", -10)
ENDIF

;조교 대상이 동정
SIF TALENT:MASTER:동정
	CALL ADD_CRI_VAR("공중육변기플레이", -5)

;조교 대상이 남자
SIF TALENT:MASTER:남자
	CALL ADD_CRI_VAR("공중육변기플레이", -3, -4)

;─
;능력, 파라미터에 의한 변동
;─
;-------------------------------------------------------------------------
;조교자의 능력을 참조
;-------------------------------------------------------------------------
@ACT_ACTM7_ABL_T

;조교자의 회화가[0→0, 5→0～5]를 가산(0～5)
CALL ADD_CRI_VAR("기분나쁘게웃는다", , LINE_CALC(ABL:회화, "0→0, 5→5, LIMIT/0～5"))

;조교자의 애무가[0→0, 5→2～4]를 가산(0～4)
CALL ADD_CRI_VAR("풋잡한다", LINE_CALC(ABL:애무, "0→0, 5→2, LIMIT/0～2"), LINE_CALC(ABL:애무, "0→0, 5→2, LIMIT/0～2"))

;조교자의 봉사가[0→0, 5→3～5]를 가산(0～5)
CALL ADD_CRI_VAR("다리빨고강제", LINE_CALC(ABL:봉사, "0→0, 5→3, LIMIT/0～3"), LINE_CALC(ABL:봉사, "0→0, 5→2, LIMIT/0～2"))

;-------------------------------------------------------------------------
;조교 대상의 능력을 참조
;-------------------------------------------------------------------------
@ACT_ACTM7_ABL_M

;조교 대상의 V감각이[0→0, 5→3]을 가산(0～3)
CALL ADD_CRI_VAR("피스트퍽", LINE_CALC(ABL:MASTER:V감각, "0→0, 5→3, LIMIT/0～3"))
;조교 대상의 V감각이[0→0, 5→2]를 가산(0～2)
CALL ADD_CRI_VAR("양구멍피스트", LINE_CALC(ABL:MASTER:V감각, "0→0, 5→2, LIMIT/0～2"))

;조교 대상의 A감각이[0→0, 5→3]을 가산(0～3)
CALL ADD_CRI_VAR("애널피스트", LINE_CALC(ABL:MASTER:A감각, "0→0, 5→3, LIMIT/0～3"))
;조교 대상의 A감각이[0→0, 5→2]를 가산(0～2)
CALL ADD_CRI_VAR("양구멍피스트", LINE_CALC(ABL:MASTER:A감각, "0→0, 5→2, LIMIT/0～2"))

;-------------------------------------
;조교자의 ALI
;-------------------------------------
@ACT_ACTM7_ALI_T

;아라이먼트가[-50→3～8, 50→-8～-3]을 가산(-8～8)
CALL ADD_CRI_VAR("공중육변기플레이", LINE_CALC(CFLAG:아라이먼트, "-50→3, 50→-3, LIMIT/-3～3"), LINE_CALC(CFLAG:아라이먼트, "-50→5, 50→-5, LIMIT/-5～5"))
;아라이먼트가[-50→-8～-3, 50→3～8]을 가산(-8～8)
CALL ADD_CRI_VAR("풋잡한다", LINE_CALC(CFLAG:아라이먼트, "-50→-3, 50→3, LIMIT/-3～3"), LINE_CALC(CFLAG:아라이먼트, "-50→-5, 50→5, LIMIT/-5～5"))
;아라이먼트가[-30→0, 0→3, 30→0]을 가산(0～3)
CALL ADD_CRI_VAR("다리빨고강제", LINE_CALC(CFLAG:아라이먼트, "-30→0, 0→3, 30→0, LIMIT/0～3"))
;아라이먼트가[-20→0, 0→2, 20→0]을 가산(0～2)
CALL ADD_CRI_VAR("피스트퍽", LINE_CALC(CFLAG:아라이먼트, "-20→0, 0→2, 20→0, LIMIT/0～2"))

;─
;게이지나 상태에 의한 변동
;─
;-------------------------------------
;조교자의 기력
;-------------------------------------
@ACT_ACTM7_MENTAL_T

;조교자의 기력이[0→3+α*3, 750→0, 1000→-3]을 가산(-3～3+α*3)(α=조교자소모도)
CALL ADD_CRI_VAR("방치플레이", LINE_CALC(BASE:기력, "0→3, 750→0, 1000→-3, LIMIT/-3～3") + TFLAG:조교자소모도 * LINE_CALC(BASE:기력, "0→3, 750→0, LIMIT/0～3"))
CALL ADD_CRI_VAR("공중육변기플레이", LINE_CALC(BASE:기력, "0→3, 750→0, 1000→-3, LIMIT/-3～3") + TFLAG:조교자소모도 * LINE_CALC(BASE:기력, "0→3, 750→0, LIMIT/0～3"))

;-------------------------------------
;조교자의 이성
;-------------------------------------
@ACT_ACTM7_REASON_T

;조교자의 이성이[0→2～7, 750→0]을 가산(0～7)
CALL ADD_CRI_VAR("기분나쁘게웃는다", LINE_CALC(BASE:이성, "0→2, 750→0, LIMIT/0～2"), LINE_CALC(BASE:이성, "0→5, 750→0, LIMIT/0～5"))
;조교자의 이성이[500→0, 1000→3]을 가산(0～3)
CALL ADD_CRI_VAR("풋잡한다", LINE_CALC(BASE:이성, "500→0, 1000→3, LIMIT/0～3"))
;조교자의 이성이[500→0, 1000→2～5]를 가산(0～5)
CALL ADD_CRI_VAR("다리빨고강제", LINE_CALC(BASE:이성, "500→0, 1000→2, LIMIT/0～2"), LINE_CALC(BASE:이성, "500→0, 1000→5, LIMIT/0～5"))

;-------------------------------------
;조교자의 흥미
;-------------------------------------
@ACT_ACTM7_INTEREST_T

;조교자의 흥미가[0→-3, 550→0]을 가산(-3～0)
CALL ADD_CRI_VAR("피스트퍽", LINE_CALC(BASE:흥미, "0→-3, 550→0, LIMIT/-3～0"))
CALL ADD_CRI_VAR("애널피스트", LINE_CALC(BASE:흥미, "0→-3, 550→0, LIMIT/-3～0"))
CALL ADD_CRI_VAR("양구멍피스트", LINE_CALC(BASE:흥미, "0→-3, 550→0, LIMIT/-3～0"))
;조교자의 흥미가[0→3, 700→-3]을 가산(-3～3)
CALL ADD_CRI_VAR("방치플레이", LINE_CALC(BASE:흥미, "0→3, 700→-3, LIMIT/-3～3"))
;조교자의 흥미가[0→5, 700→-5]를 가산(-5～5)
CALL ADD_CRI_VAR("공중육변기플레이", LINE_CALC(BASE:흥미, "0→5, 700→-5, LIMIT/-5～5"))
;조교자의 흥미가[0→-5, 700→0]을 가산(-5～0)
CALL ADD_CRI_VAR("풋잡한다", LINE_CALC(BASE:흥미, "0→-5, 700→0, LIMIT/-5～0"))
CALL ADD_CRI_VAR("다리빨고강제", LINE_CALC(BASE:흥미, "0→-5, 700→0, LIMIT/-5～0"))

;-------------------------------------
;조교 대상의 체력
;-------------------------------------
@ACT_ACTM7_PHYSICAL_M

;조교 대상의 체력이[0→-1, 500→0]을 가산(-1～0)
CALL ADD_CRI_VAR("피스트퍽", LINE_CALC(BASE:MASTER:체력, "0→-1, 500→0, LIMIT/-1～0"))
;조교 대상의 체력이[0→-3, 1000→0]을 가산(-3～0)
CALL ADD_CRI_VAR("애널피스트", LINE_CALC(BASE:MASTER:체력, "0→-3, 1000→0, LIMIT/-3～0"))
;조교 대상의 체력이[0→-4, 1000→0]을 가산(-4～0)
CALL ADD_CRI_VAR("양구멍피스트", LINE_CALC(BASE:MASTER:체력, "0→-4, 1000→0, LIMIT/-4～0"))
;※캔슬시는 정의 보정
;조교 대상의 체력이[0→9/-9, 1500→0]을 가산(-9～9)
LOCAL = TEQUIP:삼각목마 ? 1 # -1
CALL ADD_CRI_VAR("삼각목마", LOCAL * LINE_CALC(BASE:MASTER:체력, "0→9, 1500→0, LIMIT/0～9"))

;-------------------------------------
;조교자 상태
;-------------------------------------
@ACT_ACTM7_CONDITION_T

;조교자 상태(0=통상/1=피폐/2=쇠약/3=무기력/4=몽롱/5=정욕/6=분노/7=싫증/8=울적)
SELECTCASE GET_CONDITION(TARGET)
	CASE "피폐", "쇠약"
		CALL ADD_CRI_VAR("피스트퍽", -TFLAG:조교자소모도)
		CALL ADD_CRI_VAR("애널피스트", -TFLAG:조교자소모도)
		CALL ADD_CRI_VAR("양구멍피스트", -TFLAG:조교자소모도 - 3)
		CALL ADD_CRI_VAR("풋잡한다", -TFLAG:조교자소모도 * 2)
		CALL ADD_CRI_VAR("다리빨고강제", -TFLAG:조교자소모도 / 2)
		CALL ADD_CRI_VAR("삼각목마", -TFLAG:조교자소모도, -5)
	CASE "지루함"
		CALL ADD_CRI_VAR("방치플레이", 5)
		CALL ADD_CRI_VAR("공중육변기플레이", 5)
ENDSELECT

;─
;전회의 행동이나 상황에 의한 변동
;─
;-------------------------------------
;미약
;-------------------------------------
@ACT_ACTM7_DRAG
SIF TEQUIP:미약
	CALL ADD_CRI_VAR("방치플레이", TALENT:심술쟁이 * 3 - TALENT:상냥함 * 3, 4)

;-------------------------------------
;줄
;-------------------------------------
@ACT_ACTM7_ROPE
SIF TEQUIP:줄
	CALL ADD_CRI_VAR("방치플레이", 5)

;-------------------------------------
;아이마스크
;-------------------------------------
@ACT_ACTM7_EYEMASK
SIF TEQUIP:아이마스크
	CALL ADD_CRI_VAR("방치플레이", 3)

;-------------------------------------
;볼개그
;-------------------------------------
@ACT_ACTM7_GAG
SIF TEQUIP:볼개그
	CALL ADD_CRI_VAR("방치플레이", , 3)

;-------------------------------------
;삼각목마
;-------------------------------------
@ACT_ACTM7_HORSE
SIF TEQUIP:삼각목마
	CALL ADD_CRI_VAR("방치플레이", , 7)

;-------------------------------------
;야외플레이
;-------------------------------------
@ACT_ACTM7_OUT
IF TEQUIP:야외플레이
	CALL MUL_CRI_VAR("방치플레이", 150)
	CALL MUL_CRI_VAR("공중육변기플레이", 125)
ENDIF

;-------------------------------------
;요구되어 도구를 떼었다
;-------------------------------------
@ACT_ACTM7_TAKE_OFF_TOOLS
;캔슬에 응해 장착을 주저
SIF !TEQUIP:삼각목마
	CALL ADD_CRI_VAR("삼각목마", -CANCEL_TEQUIP_COUNT("삼각목마") / 20)

;-------------------------------------
;조교자 자위중
;-------------------------------------
@ACT_ACTM7_MASTURBATION_T
;조교자만 자위중
SIF TEQUIP:자위중 == 2
	CALL ADD_CRI_VAR("방치플레이", , 9)

;-------------------------------------
;조교 대상의 의복
;-------------------------------------
@ACT_ACTM7_CLOTH_M
;조교 대상의 하반신속옷이 있을 때
SIF CLOTHESTYPE(1, 0)
	CALL ADD_CRI_VAR("삼각목마", -2)

;조교 대상의 하반신윗도리에 방해될 때
IF CLOTHESTYPE(1, 1) == 2
	;조교자에게 벽통과가 없다
	IF !TALENT:벽통과
		CALL ADD_CRI_VAR("피스트퍽", -5)
		CALL ADD_CRI_VAR("애널피스트", -5)
		CALL ADD_CRI_VAR("양구멍피스트", -5)
	ENDIF
	CALL ADD_CRI_VAR("삼각목마", -5)
ENDIF

;-------------------------------------
;더하드하게
;-------------------------------------
@ACT_ACTM7_PLEASE_HARD
IF IS_NOWCOMNAME("더하드하게")
	CALL ADD_CRI_VAR("방치플레이", -10)
	CALL ADD_CRI_VAR("공중육변기플레이", 2)
	CALL ADD_CRI_VAR("기분나쁘게웃는다", -3)
	CALL ADD_CRI_VAR("풋잡한다", -3)
ENDIF

;-------------------------------------
;용서를빈다
;-------------------------------------
@ACT_ACTM7_PLEASE_HELP
;허가를 청했는지 개, 조교자 상태가 울적 이외
SIF IS_NOWCOMNAME("용서를빈다") && TCVAR:상태변화 < 8
	CALL ADD_CRI_VAR("방치플레이", 5)

;-------------------------------------
;벌중
;-------------------------------------
@ACT_ACTM7_OSHIOKI
IF TFLAG:벌플래그
	CALL ADD_CRI_VAR("공중육변기플레이", TFLAG:벌플래그 / 2)
	CALL ADD_CRI_VAR("삼각목마", TFLAG:벌플래그 / 2)
ENDIF

;─
;같은 행동 연속 실행의 확률을 다운합니다
;─
@ACT_ACTM7_PREV
SELECTCASE TFLAG:전ACT
	CASE 9, 18, 55, 67, 70 TO 74
		CALL ADD_CRI_VAR(GET_ACTNAME(TFLAG:전ACT), -3 * TFLAG:동일행동보너스, -4 * TFLAG:동일행동보너스)
ENDSELECT

;─
;보정치 계산
;─
@ACT_ACTM7_CORRECTION(ARG)
#DIM LCOUNT

;ARG가 0이외라면 보정치를 계산해, 이후의 처리를 실시하지 않는다
IF ARG
	;9, 18, 55, 67에 관해서는 여기서 계산하지 않는다
	FOR LCOUNT, 70, 75
		CALL ACT_M_RES_CALC(LCOUNT, GET_CRI_VAR(GET_ACTNAME(LCOUNT) + "누계"))
	NEXT
ELSE
	;보정치를 판정치에 가산한다
	CALL ACT_ACTM_CORRECTION(7)
ENDIF

;─
;그 외 변동이나 실행 불가능의 판정
;─
@ACT_ACTM7_ABLE
#DIM LCOUNT

;불가능 판정
CALL ACT_ACTM_ABLE(7)

