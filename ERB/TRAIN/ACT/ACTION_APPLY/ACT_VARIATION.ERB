;구상을 위해서(때문에) 조교자 행동의 파생을 결정합니다

;로. 어째서 파생 조건 코멘트로 되어 있지 않습니까? 있어도 좋지요?
;나 자위의 파생과 같은 플래그를 갈 것 같다던가 연속 실행이라든지에게 주고 있는 것은 곤란해 버립니다
;그것 구상매우 쓰기 어렵기 때문에, 단호히 숙청하도록 해 받습니다.

;성교계의 파생에 대해서는 별로 나쁘지 않습니다만, 하는 김이고 지금 숙청으로

@TRAIN_VARIATION

;TFLAG:90마다 함수 나누고 해 버립니다
;훨씬 훗날, 파생 데이터를 각 ACT의 파일로 관리시켜도 괜찮으니까
;RR 같아 보인 다다 긴 IF네스트 싫은도 있어. 자꾸자꾸 새긴다!

;인수도 있어도 괜찮지만, 응?
TRYCCALLFORM TRAIN_VARIATION_ACT{TFLAG:90}
CATCH
	;COMBI 파생계는 없으면 파생앞을 보러 간다
	TRYCALLFORM TRAIN_VARIATION_ACT{GET_COMBIACTNUM(TFLAG:ACT)}
ENDCATCH


;옷을 벗겨지고와 명한다(옷을 입으라고 명한다)
@TRAIN_VARIATION_ACT5
;성교중은 아니다
IF !GETINSERT()
	;입고 있는 것이 2매 이하 때, 옷을 입어라가 될 수 있다
	VARSET LOCAL
	FOR LOCAL, 0, 10
		SIF TEQUIP:MASTER:LOCAL
			LOCAL:1 ++
	NEXT
	SIF LOCAL:1 <= MAX(0,RAND:5-2)
		TFLAG:ACT파생 = 1
;성교중, 벗겨지는 부위를 붙이고 있으면 벗겨져에. 붙이지 않으면 입어라에.
;코스프레는 ACTABLE로 연주하고 있으므로 고려외
ELSEIF !(TEQUIP:MASTER:상반신속옷 || TEQUIP:MASTER:상반신윗도리 || TEQUIP:MASTER:전신윗도리 || TEQUIP:MASTER:악세사리 || TEQUIP:MASTER:양말)
	TFLAG:ACT파생 = 1
ENDIF

;애무(손가락으로 애무)
@TRAIN_VARIATION_ACT10
;조교 대상으로 페니스가 있으면, 반드시 자위가 된다
SIF PENIS(MASTER)
	TFLAG:ACT파생 = 1
	;비고:페니스가 있으면 통상의 애무는 일어나지 않는다
;조교 대상이 페니스밴드장착 안은, 페니스밴드 자위가 된다
SIF TEQUIP:페니스밴드
	TFLAG:ACT파생 = 2



;가슴애무
@TRAIN_VARIATION_ACT11
;조교 대상이 남자 //조건 나름으로 유두 들이마셔. 그 외의 경우는 유두 고문. 몸싸움에는 파생하지 않는다
IF TALENT:MASTER:남자
	;오늘의방침가 "귀여워해"의 경우, 옷으로 방해되지 않으면 유두 들이마셔
	IF IS_POLICY("오늘", "귀여워해") && !BRACHECK(1, 1) && !GROUPMATCH(TEQUIP:성교봉사중, 3, 4)
		TFLAG:ACT파생 = 1
	;조교자의 호의가 일정 이상 혹은 조교자가 혀사용 소유 또한 옷으로 방해되지 않는 경우는 2/3의 확률로 유두 들이마셔
	ELSEIF !IS_POLICY("오늘", "귀여워해") && (CFLAG:호의 >= 2000 || TALENT:혀사용) && !BRACHECK(1, 1) && !GROUPMATCH(TEQUIP:성교봉사중, 3, 4) && !RAND:3
		TFLAG:ACT파생 = 1
	;그 이외는 유두 고문
	ELSE
		TFLAG:ACT파생 = 3
	ENDIF
	;////////////////////////////////////////////////////////////////////////////////////////////////////////
	;남자의 경우 유두 고문 밖에 발생하지 않는 것도 저것으로, 그 나름대로 진전한 상황의 경우는 유두 들이마셔 파생하도록(듯이)
	;////////////////////////////////////////////////////////////////////////////////////////////////////////

	;반드시 유두 고문이 된다
	;비고:남자에는 이것 밖에 발생하지 않기 때문에, 방해성의 상반신옷을 입고 있으면 가슴애무 자체 실시하지 않는다
	;(다만 보통 윗도리는 옷자락을 걸어 애무할 수 있다. 원피스·도사옷·브래지어는 남자는 장비 하지 않기 때문에 관계없을지도)
	;유두 고문 자체는 여자 아이에게도 발생하도록(듯이)해 둡니다

;조교 대상이 여자 아이
ELSE
	;오늘의방침가 "귀여워해"의 경우, 옷으로 방해되지 않으면 유두 들이마셔
	IF IS_POLICY("오늘", "귀여워해") && !BRACHECK(1, 1) && !GROUPMATCH(TEQUIP:성교봉사중, 3, 4)
		TFLAG:ACT파생 = 1
	;전회가 가슴애무라면 유두계
	;(다만 벽통과가 없으면 상반신옷에 방해된다)
	ELSEIF !IS_POLICY("오늘", "귀여워해") && IS_PREVACTNAME("가슴애무") && (TALENT:벽통과|| !BRACHECK(1, 1)) && !GROUPMATCH(TEQUIP:성교봉사중, 3, 4)
		;조교자에게 유치한가 혀사용이 있으면 3/4, 없으면 1/4으로 유두 들이마셔
		IF TALENT:유치 || TALENT:혀사용 ? RAND:4 # !RAND:4
			TFLAG:ACT파생 = 1
		;그 이외는 유두 고문
		ELSE
			TFLAG:ACT파생 = 3
		ENDIF
	;단독 실행이라면 비비어 계
	ELSE
		;조교자의 욕망+B감각 3이상이라면,1/2그리고 가슴의 몸싸움
		SIF ABL:욕망 + ABL:B감각 > 2 && RAND:2
			TFLAG:ACT파생 = 2
		;몸싸움이 되지 않으면 가슴애무인 채
	ENDIF
ENDIF
;역강간, 대면좌위시킨다는 ACT_ABLE를 빠져 오므로 ACT파생 1(유두 들이마셔)의 개소에서 규제한다
;체제적으로 조금 무리가 있으므로


;쿤닐링구스(혀로 애무)
@TRAIN_VARIATION_ACT12
;조교 대상으로 페니스가 있으면, 반드시 페라가 된다
SIF PENIS(MASTER)
	TFLAG:ACT파생 = 1
	;비고:페니스가 있으면 통상의 쿤닐링구스는 일어나지 않는다

;페니스가 없으면 쿤닐링구스인 채



;애널애무
@TRAIN_VARIATION_ACT13
;조교 대상이 남자
IF TALENT:MASTER:남자
	;애널바이브 장비중은, 애널바이브 출납
	IF TEQUIP:애널바이브
		TFLAG:ACT파생 = 2
	;하반신윗도리에 방해되지 않고, 하반신속옷이 없다
	ELSEIF LOWERJACKET(1) < 2 && !LOWERUNDER(1)
		;연속 실행의 경우, 전립선자극
		SIF IS_PREVACTNAME("애널애무")
			TFLAG:ACT파생 = 1
		;행그렇게 or쾌C가 5000이상의 경우, 조교자의 애무 스킬에 응해 전립선자극
		SIF (TCVAR:MASTER:갈것같다 || PALAM:MASTER:쾌C >= 5000) && PERCENT(ABL:애무*10)
			TFLAG:ACT파생 = 1
		;발기도가 낮은 경우, 조교자의 애무 스킬에 응해 전립선자극
		SIF TCVAR:MASTER:발기도 < 1000 && PERCENT(ABL:애무*10)
			TFLAG:ACT파생 = 1
		;요점강제발기의 경우, 전립선자극
		SIF TCVAR:MASTER:요점강제발기
			TFLAG:ACT파생 = 1
	ENDIF
	;비고:후타나리의 전립선은 요도, 즉 질 위에 있다고 해, G스팟 자극의 항에 이동
	;정력 0으로 페니스가 쇠약해지고 있을 필요가 있었지만 생략한다. 실행되지 않는다고 말하는 것 그런 것!
ENDIF



;애널빨고
@TRAIN_VARIATION_ACT14
;전립선자극에의 분기가 있었지만, 숙청한다
;그런 곳까지 혀가 닿을 이유 없지 않은가www



;짝 맞추기 놀이(성기를 서로 비빈다)
@TRAIN_VARIATION_ACT16
;조교 대상으로 페니스가 있으면, 반드시 가랑이 하는게 된다
SIF PENIS(MASTER)
	TFLAG:ACT파생 = 1
	;비고:페니스가 있으면 통상의 짝 맞추기 놀이는 일어나지 않는다
;조교 대상이 페니스밴드장착 안은, 페니스밴드 가랑이가 된다
SIF TEQUIP:페니스밴드
	TFLAG:ACT파생 = 2

;페니스가 없으면 짝 맞추기 놀이인 채
;또, 조교자에게 페니스가 있으면 실행 할 수 없다(대신에 가랑이 강제가 된다)

;손가락삽입해라
@TRAIN_VARIATION_ACT19
;조교 대상이 처녀에서는 안돼
IF !TALENT:MASTER:처녀
	;바이브 장비중은, 바이브 출납
	IF TEQUIP:바이브
		TFLAG:ACT파생 = 2
	ELSE
		;연속 실행의 경우, G스팟 자극
		SIF IS_PREVACTNAME("손가락삽입해라")
			TFLAG:ACT파생 = 1
		;행그렇게 or쾌C가 5000이상의 경우, 조교자의 애무 스킬에 응해 G스팟 자극
		SIF (TCVAR:MASTER:갈것같다 || PALAM:MASTER:쾌C >= 5000) && PERCENT(ABL:애무*10)
			TFLAG:ACT파생 = 1
		;후타나리 한정
		IF TALENT:MASTER:후타나리
			;발기도가 낮은 경우, 조교자의 애무 스킬에 응해 G스팟 자극
			SIF TCVAR:MASTER:발기도 < 1000 && PERCENT(ABL:애무*10)
				TFLAG:ACT파생 = 1
			;요점강제발기의 경우, G스팟 자극
			SIF TCVAR:MASTER:요점강제발기
				TFLAG:ACT파생 = 1
		ENDIF
	ENDIF
ENDIF


;애무 강제(손가락으로 애무를 강제)
@TRAIN_VARIATION_ACT50
;조교자에게 페니스가 있으면, 반드시 자위 강제가 된다
SIF PENIS(TARGET)
	TFLAG:ACT파생 = 1
	;비고:페니스가 있으면 통상의 애무 강제는 일어나지 않는다
;조교자가 페니스밴드장착 안은, 페니스밴드 자위 강제가 된다
SIF TEQUIP:조교자페니스밴드
	TFLAG:ACT파생 = 2

;페니스가 없으면 애무 강제인 채



;쿤닐링구스 강제(입으로애무를강제)
@TRAIN_VARIATION_ACT51
;조교자가 남자라고, 반드시 페라 강제가 된다
IF TALENT:남자
	TFLAG:ACT파생 = 1

;조교자가 후타나리라고, 조교 대상이 위로돌리고라면 1/2으로 안면승마, 그렇지 않으면 페라 강제가 된다
ELSEIF TALENT:후타나리
	IF RAND:2 && TFLAG:위로돌리고
		TFLAG:ACT파생 = 2
	ELSE
		TFLAG:ACT파생 = 1
	ENDIF
	;비고:페니스가 있으면 통상의 애무 강제는 일어나지 않는다(후타나리라도 페라인가 안면승마가 된다)

;조교자에게 페니스가 없고, 조교 대상이 위로돌리고라면 안면승마가 된다
ELSEIF TFLAG:위로돌리고
	TFLAG:ACT파생 = 2
;그 이외라면 통상의 쿤닐링구스
ENDIF

;안면승마가 선택되고 있었을 때, 안면승마애널에 변화하는 일이 있다
SIF TFLAG:ACT파생 == 2 && RAND:(GET_ABL(TARGET, "A감각") + 100) > RAND:200
	TFLAG:ACT파생 = 3

;가랑이 강제
@TRAIN_VARIATION_ACT53
;조교자가 페니스밴드장착 안은, 페니스밴드 가랑이가 된다
SIF TEQUIP:조교자페니스밴드
	TFLAG:ACT파생 = 1

;가슴애무강제
@TRAIN_VARIATION_ACT57
;볼개그가 없으면,1/2의 확률로 입으로애무가 된다
;주인이 애무 계속중도 자세에 무리가 있으므로 연주하지만, 여자 아이에 대한 손으로애무만은 허가
SIF (RAND:2 == 0 && !TEQUIP:볼개그 && (!TEQUIP:조교대상C사용 || (TEQUIP:조교대상C사용 == 1 && !PENIS(MASTER)))) || TEQUIP:줄
	TFLAG:ACT파생 = 2

;비고:줄로 속박되고 있으면 반드시 입으로애무가 된다. 볼개그와 양쪽 모두 대고 있으면 실행되지 않는다
;TFLAG:93 == 1은 본래 「파후파후」(이었)였지만, 조교자의 행동으로서 수수께끼 지나므로 숙청되었다




;바이브
@TRAIN_VARIATION_ACT21
;이미 장비 하고 있는 경우, 바이브 해제가 된다
SIF TEQUIP:바이브
	TFLAG:ACT파생 = 1



;애널바이브
@TRAIN_VARIATION_ACT22
;이미 장비 하고 있는 경우, 애널바이브 해제가 된다
SIF TEQUIP:애널바이브
	TFLAG:ACT파생 = 1


;애널비즈
@TRAIN_VARIATION_ACT23
;구슬이 5개 들어가 있는 경우, 애널비즈 해제가 된다
IF TEQUIP:애널비즈 > 4
	TFLAG:ACT파생 = 1
;이미 장비 하고 있는 경우, 애널비즈 추가가 된다
ELSEIF TEQUIP:애널비즈
	TFLAG:ACT파생 = 2
ENDIF



;클리캡(C애무도구)
@TRAIN_VARIATION_ACT24
;조교 대상으로 페니스가 있다
IF PENIS(MASTER)
	;이미 장비 하고 있는 경우, 오나홀 해제가 된다
	IF TEQUIP:오나홀
		TFLAG:ACT파생 = 3
	;그렇지 않은 경우, 오나홀이 된다
	ELSE
		TFLAG:ACT파생 = 2
	ENDIF
;조교 대상으로 페니스가 없다
ELSE
	;이미 장비 하고 있는 경우, 클리캡 해제가 된다
	SIF TEQUIP:클리캡
		TFLAG:ACT파생 = 1
ENDIF



;니플캡
@TRAIN_VARIATION_ACT25
;이미 장비 하고 있는 경우, 니플캡 해제가 된다
SIF TEQUIP:니플캡
	TFLAG:ACT파생 = 1

;페니스밴드
@TRAIN_VARIATION_ACT26
;이미 장비 하고 있는 경우, 페니스밴드 해제가 된다
SIF TEQUIP:페니스밴드
	TFLAG:ACT파생 = 1



;밀어넘어뜨리기
@TRAIN_VARIATION_ACT27
;이미 밀어넘어뜨리기라고 있는 경우, 해방이 된다
SIF TEQUIP:밀어넘어뜨리기중
	TFLAG:ACT파생 = 1



;정상위
@TRAIN_VARIATION_ACT30
;다른 체위로 성교하고 있는 경우, 체위 변경이 된다
IF TEQUIP:성교중 != 1 && TEQUIP:성교중 < 6 && TEQUIP:성교중
	TFLAG:ACT파생 = 2
;애널섹스 안의 경우, 체위 변경(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교중 == 6
	TFLAG:ACT파생 = 3
;역삽입중이었던 경우, 역전이 된다
ELSEIF TEQUIP:성교봉사중 != 6 && TEQUIP:성교봉사중 != 0 && TEQUIP:성교봉사중
	TFLAG:ACT파생 = 4
;애널역성교중이었던 경우, 역전(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교봉사중 == 6
	TFLAG:ACT파생 = 5
;연속 실행의 경우, 허리를 사용하는이 된다
ELSEIF TEQUIP:성교중 == 1
	TFLAG:ACT파생 = 1
ENDIF



;후배위
@TRAIN_VARIATION_ACT31
;다른 체위로 성교하고 있는 경우, 체위 변경이 된다
IF TEQUIP:성교중 != 2 && TEQUIP:성교중 < 6 && TEQUIP:성교중
	TFLAG:ACT파생 = 2
;애널섹스 안의 경우, 체위 변경(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교중 == 6
	TFLAG:ACT파생 = 3
;역성교중이었던 경우, 역전이 된다
ELSEIF TEQUIP:성교봉사중 != 6 && TEQUIP:성교봉사중 != 0 && TEQUIP:성교봉사중
	TFLAG:ACT파생 = 4
;애널역성교중이었던 경우, 역전(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교봉사중 == 6
	TFLAG:ACT파생 = 5
;연속 실행의 경우, 허리를 사용하는이 된다
ELSEIF TEQUIP:성교중 == 2
	TFLAG:ACT파생 = 1
ENDIF



;승마위
@TRAIN_VARIATION_ACT32
;다른 체위로 성교하고 있는 경우, 체위 변경이 된다
IF TEQUIP:성교중 != 3 && TEQUIP:성교중 < 6 && TEQUIP:성교중
	TFLAG:ACT파생 = 2
;애널섹스 안의 경우, 체위 변경(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교중 == 6
	TFLAG:ACT파생 = 3
;역성교중이었던 경우, 역전이 된다
ELSEIF TEQUIP:성교봉사중 != 6 && TEQUIP:성교봉사중 != 0 && TEQUIP:성교봉사중
	TFLAG:ACT파생 = 4
;애널역성교중이었던 경우, 역전(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교봉사중 == 6
	TFLAG:ACT파생 = 5
;연속 실행의 경우, 허리를 사용하는이 된다
ELSEIF TEQUIP:성교중 == 3
	TFLAG:ACT파생 = 1
ENDIF



;대면좌위
@TRAIN_VARIATION_ACT33
;다른 체위로 성교하고 있는 경우, 체위 변경이 된다
IF TEQUIP:성교중 != 4 && TEQUIP:성교중 < 6 && TEQUIP:성교중
	TFLAG:ACT파생 = 2
;애널섹스 안의 경우, 체위 변경(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교중 == 6
	TFLAG:ACT파생 = 3
;역성교중이었던 경우, 역전이 된다
ELSEIF TEQUIP:성교봉사중 != 6 && TEQUIP:성교봉사중 != 0 && TEQUIP:성교봉사중
	TFLAG:ACT파생 = 4
;애널역성교중이었던 경우, 역전(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교봉사중 == 6
	TFLAG:ACT파생 = 5
;연속 실행의 경우, 허리를 사용하는이 된다
ELSEIF TEQUIP:성교중 == 4
	TFLAG:ACT파생 = 1
ENDIF



;배면좌위
@TRAIN_VARIATION_ACT34
;다른 체위로 성교하고 있는 경우, 체위 변경이 된다
IF TEQUIP:성교중 != 5 && TEQUIP:성교중 < 6 && TEQUIP:성교중
	TFLAG:ACT파생 = 2
;애널섹스 안의 경우, 체위 변경(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교중 == 6
	TFLAG:ACT파생 = 3
;역성교중이었던 경우, 역전이 된다
ELSEIF TEQUIP:성교봉사중 != 6 && TEQUIP:성교봉사중 != 0 && TEQUIP:성교봉사중
	TFLAG:ACT파생 = 4
;애널역성교중이었던 경우, 역전(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교봉사중 == 6
	TFLAG:ACT파생 = 5
;연속 실행의 경우, 허리를 사용하는이 된다
ELSEIF TEQUIP:성교중 == 5
	TFLAG:ACT파생 = 1
ENDIF




;애널섹스
@TRAIN_VARIATION_ACT35
;다른 체위로 성교하고 있는 경우, 체위 변경(애널에)(이)가 된다
IF TEQUIP:성교중 < 6 && TEQUIP:성교중
	TFLAG:ACT파생 = 3
;역성교중이었던 경우, 역전(애널에)(이)가 된다
ELSEIF TEQUIP:성교봉사중 != 6 && TEQUIP:성교봉사중 != 0 && TEQUIP:성교봉사중
	TFLAG:ACT파생 = 4
;애널역성교중이었던 경우, 역전(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교봉사중 == 6
	TFLAG:ACT파생 = 5
;연속 실행의 경우, 허리를 사용하는이 된다
ELSEIF TEQUIP:성교중 == 6
	TFLAG:ACT파생 = 1
ENDIF



;역강간
@TRAIN_VARIATION_ACT103
;다른 체위로 역성교하고 있는 경우, 체위 변경이 된다
IF TEQUIP:성교봉사중 && TEQUIP:성교봉사중 != 3 && TEQUIP:성교봉사중 < 6
	TFLAG:ACT파생 = 2
;애널섹스 안의 경우, 역전(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교중 == 6
	TFLAG:ACT파생 = 3
;성교중이었던 경우, 역전이 된다
ELSEIF TEQUIP:성교중 < 6 && TEQUIP:성교중
	TFLAG:ACT파생 = 4
;애널역성교중이었던 경우, 체위 변경(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교봉사중 == 6
	TFLAG:ACT파생 = 5
;연속 실행의 경우, 허리를 사용하는이 된다
ELSEIF TEQUIP:성교봉사중 == 3
	TFLAG:ACT파생 = 1
ENDIF



;수치플레이
@TRAIN_VARIATION_ACT43
;이미 수치플레이 안이라면, 수치플레이 종료가 된다
SIF TEQUIP:대거울
	TFLAG:ACT파생 = 1



;야외플레이
@TRAIN_VARIATION_ACT44
;이미 야외플레이 안이라면, 야외플레이 종료가 된다
SIF TEQUIP:야외플레이
	TFLAG:ACT파생 = 1


;줄
@TRAIN_VARIATION_ACT63
;이미 줄로 구속중이라면, 줄해제가 된다
SIF TEQUIP:줄
	TFLAG:ACT파생 = 1



;아이마스크
@TRAIN_VARIATION_ACT64
;이미 장비 하고 있으면, 아이마스크 해제가 된다
SIF TEQUIP:아이마스크
	TFLAG:ACT파생 = 1



;볼개그
@TRAIN_VARIATION_ACT65
;이미 장비 하고 있으면, 볼개그 해제가 된다
SIF TEQUIP:볼개그
	TFLAG:ACT파생 = 1



;삼각목마
@TRAIN_VARIATION_ACT67
;이미 장비 하고 있으면, 삼각목마 해제가 된다
SIF TEQUIP:삼각목마
	TFLAG:ACT파생 = 1



;관장세트
@TRAIN_VARIATION_ACT68
;관장액이 5병 들어가 있는 경우, 관장 플러그 해제가 된다
IF TEQUIP:관장기＋플러그 > 4
	TFLAG:ACT파생 = 1
;이미 장비 하고 있는 경우, 관장액추가가 된다
ELSEIF TEQUIP:관장기＋플러그
	TFLAG:ACT파생 = 2
ENDIF

;페니스밴드장착
@TRAIN_VARIATION_ACT75
;이미 장비 하고 있는 경우, 해제
SIF TEQUIP:조교자페니스밴드
	TFLAG:ACT파생 = 1


;정상위시킨다
@TRAIN_VARIATION_ACT95
;다른 체위로 역성교하고 있는 경우, 체위 변경이 된다
IF TEQUIP:성교봉사중 && TEQUIP:성교봉사중 != 1 && TEQUIP:성교봉사중 < 6
	TFLAG:ACT파생 = 2
;애널섹스 안의 경우, 역전(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교중 == 6
	TFLAG:ACT파생 = 3
;성교중이었던 경우, 역전이 된다
ELSEIF TEQUIP:성교중 < 6 && TEQUIP:성교중
	TFLAG:ACT파생 = 4
;애널역성교중이었던 경우, 체위 변경(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교봉사중 == 6
	TFLAG:ACT파생 = 5
;연속 실행의 경우, 허리를 사용하는이 된다
ELSEIF TEQUIP:성교봉사중 == 1
	TFLAG:ACT파생 = 1
ENDIF



;후배위시킨다
@TRAIN_VARIATION_ACT96
;다른 체위로 역성교하고 있는 경우, 체위 변경이 된다
IF TEQUIP:성교봉사중 && TEQUIP:성교봉사중 != 2 && TEQUIP:성교봉사중 < 6
	TFLAG:ACT파생 = 2
;애널섹스 안의 경우, 역전(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교중 == 6
	TFLAG:ACT파생 = 3
;성교중이었던 경우, 역전이 된다
ELSEIF TEQUIP:성교중 < 6 && TEQUIP:성교중
	TFLAG:ACT파생 = 4
;애널역성교중이었던 경우, 체위 변경(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교봉사중 == 6
	TFLAG:ACT파생 = 5
;연속 실행의 경우, 허리를 사용하는이 된다
ELSEIF TEQUIP:성교봉사중 == 2
	TFLAG:ACT파생 = 1
ENDIF



;대면좌위시킨다
@TRAIN_VARIATION_ACT97
;다른 체위로 역성교하고 있는 경우, 체위 변경이 된다
IF TEQUIP:성교봉사중 && TEQUIP:성교봉사중 != 4 && TEQUIP:성교봉사중 < 6
	TFLAG:ACT파생 = 2
;애널섹스 안의 경우, 역전(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교중 == 6
	TFLAG:ACT파생 = 3
;성교중이었던 경우, 역전이 된다
ELSEIF TEQUIP:성교중 < 6 && TEQUIP:성교중
	TFLAG:ACT파생 = 4
;애널역성교중이었던 경우, 체위 변경(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교봉사중 == 6
	TFLAG:ACT파생 = 5
;연속 실행의 경우, 허리를 사용하는이 된다
ELSEIF TEQUIP:성교봉사중 == 4
	TFLAG:ACT파생 = 1
ENDIF



;배면좌위시킨다
@TRAIN_VARIATION_ACT98
;다른 체위로 역성교하고 있는 경우, 체위 변경이 된다
IF TEQUIP:성교봉사중 && TEQUIP:성교봉사중 != 5 && TEQUIP:성교봉사중 < 6
	TFLAG:ACT파생 = 2
;애널섹스 안의 경우, 역전(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교중 == 6
	TFLAG:ACT파생 = 3
;성교중이었던 경우, 역전이 된다
ELSEIF TEQUIP:성교중 < 6 && TEQUIP:성교중
	TFLAG:ACT파생 = 4
;애널역성교중이었던 경우, 체위 변경(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교봉사중 == 6
	TFLAG:ACT파생 = 5
;연속 실행의 경우, 허리를 사용하는이 된다
ELSEIF TEQUIP:성교봉사중 == 5
	TFLAG:ACT파생 = 1
ENDIF



;애널섹스시킨다
@TRAIN_VARIATION_ACT99
;다른 체위로 역성교하고 있는 경우, 체위 변경(애널에)(이)가 된다
IF V_SEX(MASTER)
	TFLAG:ACT파생 = 3
;애널섹스 안의 경우, 역전(애널로부터)(이)가 된다
ELSEIF TEQUIP:성교중 == 6
	TFLAG:ACT파생 = 2
;성교중이었던 경우, 역전(애널에)(이)가 된다
ELSEIF TEQUIP:성교중 < 6 && TEQUIP:성교중
	TFLAG:ACT파생 = 4
;연속 실행의 경우, 허리를 사용하는이 된다
ELSEIF TEQUIP:성교봉사중 == 6
	TFLAG:ACT파생 = 1
ENDIF
