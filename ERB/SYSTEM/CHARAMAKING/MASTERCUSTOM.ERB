;─
;주인공 커스텀 메뉴
;─
@MASTER_CUSTOM
;-------------------------------------------------
;MASTER_CUSTOM2 관련의 추가 처리
PRINTFORML 최초부터%CALLNAME:MASTER%의 스테이터스를 커스텀 할 수 있습니다.
PRINTL 변경하고 싶은 항목을 선택해 주세요. (극단적인 커스텀에 의해,
PRINTL 난이도가 크게 바뀌는 경우도 있기 때문에 조심해 주세요)
PRINTL 
PRINTL 설정 모드를 선택해 주세요
PRINTL [0] 간이판　　[1] 상세판
$INPUT_LOOP2
INPUT
IF RESULT == 1
	JUMP MASTER_CUSTOM2
ELSEIF RESULT
	CLEARLINE 1
	GOTO INPUT_LOOP2
ENDIF
;-------------------------------------------------


$INPUT_LOOP1
CALL SHOW_CUSTOM

INPUT

IF RESULT == 0
	TFLAG:999 = 0
	RETURN 0
ELSEIF RESULT == 1
	IF TALENT:MASTER:남자
		PRINTFORMW %CALLNAME:MASTER%는 후타나리가 되었습니다
		TALENT:MASTER:남자 = 0
		TALENT:MASTER:후타나리 = 1
		SIF TALENT:MASTER:동정
			TALENT:MASTER:처녀 = 1
		IF ABL:MASTER:욕망 > 0
			EXP:MASTER:V경험 = 10
			EXP:MASTER:B경험 = 10
		ELSEIF TALENT:MASTER:처녀 == 0
			EXP:MASTER:V경험 = 2
			EXP:MASTER:B경험 = 3
		ENDIF
	ELSEIF TALENT:MASTER:후타나리
		PRINTFORMW %CALLNAME:MASTER%는 여자 아이가 되었습니다
		SIF TALENT:MASTER:동정
			TALENT:MASTER:동정 = 0
		TALENT:MASTER:후타나리 = 0
		TALENT:MASTER:조루 = 0
		TALENT:MASTER:지루 = 0
		EXP:MASTER:사정경험 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 남자가 되었습니다
		TALENT:MASTER:남자 = 1
		TALENT:MASTER:V민감 = 0
		TALENT:MASTER:V둔감 = 0
		TALENT:MASTER:빈유 = 0
		TALENT:MASTER:거유 = 0
		EXP:MASTER:V경험 = 0
		EXP:MASTER:B경험 = 0
		IF TALENT:MASTER:처녀
			TALENT:MASTER:처녀 = 0
			TALENT:MASTER:동정 = 1
		ELSEIF ABL:MASTER:욕망 > 0
			EXP:MASTER:사정경험 = 12 + TALENT:MASTER:자위쉬움
		ELSE
			EXP:MASTER:사정경험 = 3 + TALENT:MASTER:자위쉬움
		ENDIF
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 2
	IF TALENT:MASTER:처녀 || TALENT:MASTER:동정
		PRINTFORMW %CALLNAME:MASTER%는 약간 성교의 경험이 있습니다
		TALENT:MASTER:처녀 = 0
		TALENT:MASTER:동정 = 0
		EXP:MASTER:성교경험 = 1
		EXP:MASTER:C경험 = 4
		EXP:MASTER:성지식 = 5 + MASTER_CUSTOMV(-1)
		IF TALENT:MASTER:남자 == 0
			EXP:MASTER:V경험 = 2
			EXP:MASTER:B경험 = 3
		ENDIF
		SIF TALENT:MASTER:남자 || TALENT:MASTER:후타나리
			EXP:MASTER:사정경험 = 3 + TALENT:MASTER:자위쉬움
	ELSEIF ABL:MASTER:욕망 > 0
		PRINTFORMW %CALLNAME:MASTER%는 순진 무구합니다
		ABL:MASTER:욕망 = 0
		EXP:MASTER:성교경험 = 0
		EXP:MASTER:C경험 = 0
		EXP:MASTER:성지식 = MASTER_CUSTOMV(-1)
		IF TALENT:MASTER:남자 == 0
			TALENT:MASTER:처녀 = 1
			EXP:MASTER:V경험 = 0
			EXP:MASTER:B경험 = 0
		ELSEIF TALENT:MASTER:남자 || TALENT:MASTER:후타나리
			TALENT:MASTER:동정 = 1
			EXP:MASTER:사정경험 = TALENT:MASTER:자위쉬움
		ENDIF
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 몇번이나 성교를 했던 적이 있습니다
		ABL:MASTER:욕망 = 1
		EXP:MASTER:성교경험 = 3
		EXP:MASTER:C경험 = 10
		EXP:MASTER:성지식 = 10 + MASTER_CUSTOMV(-1)
		IF TALENT:MASTER:남자 == 0
			EXP:MASTER:V경험 = 10
			EXP:MASTER:B경험 = 10
		ENDIF
		SIF (TALENT:MASTER:남자 || TALENT:MASTER:후타나리) 
			EXP:MASTER:사정경험 = 12 + TALENT:MASTER:자위쉬움
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 3
	IF MASTER_CUSTOMV(-1) == 0
		EXP:MASTER:성지식 += 5
		PRINTFORMW %CALLNAME:MASTER%는 엣치한 일로 조금 흥미가 있습니다
		CALLF MASTER_CUSTOMV(1)
	ELSEIF MASTER_CUSTOMV(-1) == 1
		EXP:MASTER:성지식 += 5
		PRINTFORMW %CALLNAME:MASTER%는 엣치한 일로 흥미진진입니다
		CALLF MASTER_CUSTOMV(2)
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 엣치한 일에 대해 아무것도 모릅니다
		EXP:MASTER:성지식 -= 10
		CALLF MASTER_CUSTOMV(0)
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 4
	IF TALENT:MASTER:겁쟁이
		PRINTFORMW %CALLNAME:MASTER%는 다부지게 되었습니다
		TALENT:MASTER:겁쟁이 = 0
		TALENT:MASTER:기장 = 1
	ELSEIF TALENT:MASTER:기장
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:기장 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 겁쟁이로 되었습니다
		TALENT:MASTER:겁쟁이 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 5
	IF TALENT:MASTER:반항적
		PRINTFORMW %CALLNAME:MASTER%는 솔직해졌습니다
		TALENT:MASTER:반항적 = 0
		TALENT:MASTER:순수 = 1
	ELSEIF TALENT:MASTER:순수
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:순수 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 반항적으로 되었습니다
		TALENT:MASTER:반항적 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 6
	IF TALENT:MASTER:얌전함
		PRINTFORMW %CALLNAME:MASTER%는 건방지게 되었습니다
		TALENT:MASTER:얌전함 = 0
		TALENT:MASTER:거만함 = 1
	ELSEIF TALENT:MASTER:거만함
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:거만함 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 얌전해졌습니다
		TALENT:MASTER:얌전함 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 7
	IF TALENT:MASTER:프라이드높음
		PRINTFORMW %CALLNAME:MASTER%의 프라이드는 낮아졌습니다
		TALENT:MASTER:프라이드높음 = 0
		TALENT:MASTER:프라이드낮음 = 1
	ELSEIF TALENT:MASTER:프라이드낮음
		PRINTFORMW %CALLNAME:MASTER%의 프라이드는 보통으로 되었습니다
		TALENT:MASTER:프라이드낮음 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%의 프라이드는 높아졌습니다
		TALENT:MASTER:프라이드높음 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 8
	IF TALENT:MASTER:자제적
		PRINTFORMW %CALLNAME:MASTER%는 충동적이 되었습니다
		TALENT:MASTER:자제적 = 0
		TALENT:MASTER:충동적 = 1
	ELSEIF TALENT:MASTER:충동적
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:충동적 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 자제적으로 되었습니다
		TALENT:MASTER:자제적 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 9
	IF TALENT:MASTER:무관심
		PRINTFORMW %CALLNAME:MASTER%는 호기심이 되었습니다
		TALENT:MASTER:무관심 = 0
		TALENT:MASTER:호기심 = 1
	ELSEIF TALENT:MASTER:호기심
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:호기심 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 무관심하게 되었습니다
		TALENT:MASTER:무관심 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 10
	IF TALENT:MASTER:감정결핍
		PRINTFORMW %CALLNAME:MASTER%는 감정 풍부하게 되었습니다
		TALENT:MASTER:감정결핍 = 0
		TALENT:MASTER:감수성 = 1
	ELSEIF TALENT:MASTER:감수성
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:감수성 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 감정이 부족해졌습니다
		TALENT:MASTER:감정결핍 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 11
	IF TALENT:MASTER:낙관적
		PRINTFORMW %CALLNAME:MASTER%는 비관적으로 되었습니다
		TALENT:MASTER:낙관적 = 0
		TALENT:MASTER:비관적 = 1
	ELSEIF TALENT:MASTER:비관적
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:비관적 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 낙관적으로 되었습니다
		TALENT:MASTER:낙관적 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 12
	IF TALENT:MASTER:선을그음
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:선을그음 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 일선 넘지 않게 되었습니다
		TALENT:MASTER:선을그음 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 13
	IF TALENT:MASTER:튀고싶어함
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:튀고싶어함 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는튀고싶어함가 되었습니다
		TALENT:MASTER:튀고싶어함 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 14
	IF TALENT:MASTER:정조관념
		PRINTFORMW %CALLNAME:MASTER%는 정조관념없음이 되었습니다
		TALENT:MASTER:정조관념 = 0
		TALENT:MASTER:정조관념없음 = 1
	ELSEIF TALENT:MASTER:정조관념없음
		PRINTFORMW %CALLNAME:MASTER%의 정조관념은 남들 수준이 되었습니다
		TALENT:MASTER:정조관념없음 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 정조관념은 강해졌습니다
		TALENT:MASTER:정조관념 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 15
	IF TALENT:MASTER:억압
		PRINTFORMW %CALLNAME:MASTER%는 해방이 되었습니다
		TALENT:MASTER:억압 = 0
		TALENT:MASTER:해방 = 1
	ELSEIF TALENT:MASTER:해방
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:해방 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 억압이 되었습니다
		TALENT:MASTER:억압 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 16
	IF TALENT:MASTER:수줍음
		PRINTFORMW %CALLNAME:MASTER%는 수치심이 적어졌습니다
		TALENT:MASTER:수줍음 = 0
		TALENT:MASTER:수치심옅음 = 1
	ELSEIF TALENT:MASTER:수치심옅음
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:수치심옅음 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 부끄럽게 여기기가 되었습니다
		TALENT:MASTER:수줍음 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 17
	IF TALENT:MASTER:아픔에약함
		PRINTFORMW %CALLNAME:MASTER%는 아픔에 강해졌습니다
		TALENT:MASTER:아픔에약함 = 0
		TALENT:MASTER:아픔에강함 = 1
	ELSEIF TALENT:MASTER:아픔에강함
		PRINTFORMW %CALLNAME:MASTER%의 강함은 보통으로 되었습니다
		TALENT:MASTER:아픔에강함 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 아픔에 약해졌습니다
		TALENT:MASTER:아픔에약함 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 18
	IF TALENT:MASTER:젖기쉬움
		PRINTFORMW %CALLNAME:MASTER%는 젖기 어려워졌습니다
		TALENT:MASTER:젖기쉬움 = 0
		TALENT:MASTER:젖기어려움 = 1
	ELSEIF TALENT:MASTER:젖기어려움
		PRINTFORMW %CALLNAME:MASTER%의 애액의 분비량은 보통으로 되었습니다
		TALENT:MASTER:젖기어려움 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 젖기 쉬워졌습니다
		TALENT:MASTER:젖기쉬움 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 19
	IF TALENT:MASTER:습득빠름
		PRINTFORMW %CALLNAME:MASTER%는 기억이 나빠졌습니다
		TALENT:MASTER:습득빠름 = 0
		TALENT:MASTER:습득느림 = 1
	ELSEIF TALENT:MASTER:습득느림
		PRINTFORMW %CALLNAME:MASTER%의 학습 효율은 보통으로 되었습니다
		TALENT:MASTER:습득느림 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 기억이 좋아졌습니다
		TALENT:MASTER:습득빠름 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 20
	IF TALENT:MASTER:자위쉬움
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:자위쉬움 = 0
		EXP:MASTER:자위경험 = 0
		SIF TALENT:MASTER:남자 || TALENT:MASTER:후타나리
			EXP:MASTER:사정경험 -= 1
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 자위하기 쉬워졌습니다
		TALENT:MASTER:자위쉬움 = 1
		EXP:MASTER:자위경험 = 10
		SIF TALENT:MASTER:남자 || TALENT:MASTER:후타나리
			EXP:MASTER:사정경험 += 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 21
	IF TALENT:MASTER:악취둔감
		PRINTFORMW %CALLNAME:MASTER%는 악취민감이 되었습니다
		TALENT:MASTER:악취둔감 = 0
		TALENT:MASTER:악취민감 = 1
	ELSEIF TALENT:MASTER:악취민감
		PRINTFORMW %CALLNAME:MASTER%의 후각은 보통으로 되었습니다
		TALENT:MASTER:악취민감 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 악취둔감이 되었습니다
		TALENT:MASTER:악취둔감 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 22
	IF TALENT:MASTER:헌신적
		PRINTFORMW %CALLNAME:MASTER%는 수동이 되었습니다
		TALENT:MASTER:헌신적 = 0
		TALENT:MASTER:수동 = 1
	ELSEIF TALENT:MASTER:수동
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:수동 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 헌신적으로 되었습니다
		TALENT:MASTER:헌신적 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 23
	IF TALENT:MASTER:쾌감에순수
		PRINTFORMW %CALLNAME:MASTER%는 쾌감을 받아들이지 않게 되었습니다
		TALENT:MASTER:쾌감에순수 = 0
		TALENT:MASTER:쾌감을부정 = 1
	ELSEIF TALENT:MASTER:쾌감을부정
		PRINTFORMW %CALLNAME:MASTER%는 보통으로 되었습니다
		TALENT:MASTER:쾌감을부정 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 쾌감에순수가 되었습니다
		TALENT:MASTER:쾌감에순수 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 24
	IF TALENT:MASTER:C민감
		PRINTFORMW %CALLNAME:MASTER%는 C 둔하게 되었습니다
		TALENT:MASTER:C민감 = 0
		TALENT:MASTER:C둔감 = 1
	ELSEIF TALENT:MASTER:C둔감
		IF TALENT:MASTER:남자 == 0
			PRINTFORMW %CALLNAME:MASTER%는 V 민감하게 되었습니다
			TALENT:MASTER:C둔감 = 0
			TALENT:MASTER:V민감 = 1
		ELSE
			PRINTFORMW %CALLNAME:MASTER%는 A 민감하게 되었습니다
			TALENT:MASTER:C둔감 = 0
			TALENT:MASTER:A민감 = 1
		ENDIF
	ELSEIF TALENT:MASTER:V민감
		PRINTFORMW %CALLNAME:MASTER%는 V 둔하게 되었습니다
		TALENT:MASTER:V민감 = 0
		TALENT:MASTER:V둔감 = 1
	ELSEIF TALENT:MASTER:V둔감
		PRINTFORMW %CALLNAME:MASTER%는 A 민감하게 되었습니다
		TALENT:MASTER:V둔감 = 0
		TALENT:MASTER:A민감 = 1
	ELSEIF TALENT:MASTER:A민감
		PRINTFORMW %CALLNAME:MASTER%는 A 둔하게 되었습니다
		TALENT:MASTER:A민감 = 0
		TALENT:MASTER:A둔감 = 1
	ELSEIF TALENT:MASTER:A둔감
		PRINTFORMW %CALLNAME:MASTER%는 B 민감하게 되었습니다
		TALENT:MASTER:A둔감 = 0
		TALENT:MASTER:B민감 = 1
	ELSEIF TALENT:MASTER:B민감
		PRINTFORMW %CALLNAME:MASTER%는 B 둔하게 되었습니다
		TALENT:MASTER:B민감 = 0
		TALENT:MASTER:B둔감 = 1
	ELSEIF TALENT:MASTER:B둔감
		PRINTFORMW %CALLNAME:MASTER%의 감도는 보통으로 되었습니다
		TALENT:MASTER:B둔감 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 C 민감하게 되었습니다
		TALENT:MASTER:C민감 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 25
	IF TALENT:MASTER:빈유
		PRINTFORMW %CALLNAME:MASTER%는 거유가 되었습니다
		TALENT:MASTER:빈유 = 0
		TALENT:MASTER:거유 = 1
	ELSEIF TALENT:MASTER:거유
		PRINTFORMW %CALLNAME:MASTER%의 가슴의 사이즈는 보통으로 되었습니다
		TALENT:MASTER:거유 = 0
	ELSE
		IF TALENT:MASTER:남자
			PRINTW 남자군요?
		ELSE
			PRINTFORMW %CALLNAME:MASTER%는 빈유가 되었습니다
			TALENT:MASTER:빈유 = 1
		ENDIF
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 26
	IF TALENT:MASTER:작은체형
		PRINTFORMW %CALLNAME:MASTER%는 거구가 되었습니다
		TALENT:MASTER:작은체형 = 0
		TALENT:MASTER:장신 = 1
	ELSEIF TALENT:MASTER:장신
		PRINTFORMW %CALLNAME:MASTER%의 체형은 보통으로 되었습니다
		TALENT:MASTER:장신 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 작은체형 체형이 되었습니다
		TALENT:MASTER:작은체형 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 27
	IF TALENT:MASTER:회복빠름
		PRINTFORMW %CALLNAME:MASTER%는 회복 늦어졌습니다
		TALENT:MASTER:회복빠름 = 0
		TALENT:MASTER:회복느림 = 1
	ELSEIF TALENT:MASTER:회복느림
		PRINTFORMW %CALLNAME:MASTER%의 회복 스피드는 보통으로 되었습니다
		TALENT:MASTER:회복느림 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 회복 빨리 되었습니다
		TALENT:MASTER:회복빠름 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 28 & (TALENT:MASTER:남자 || TALENT:MASTER:후타나리)
	IF TALENT:MASTER:조루
		PRINTFORMW %CALLNAME:MASTER%는 지루가 되었습니다
		TALENT:MASTER:조루 = 0
		TALENT:MASTER:지루 = 1
	ELSEIF TALENT:MASTER:지루
		PRINTFORMW %CALLNAME:MASTER%의 사정 스피드는 보통으로 되었습니다
		TALENT:MASTER:지루 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 조루에 걸렸습니다
		TALENT:MASTER:조루 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 29
	IF TALENT:MASTER:정력절륜
		PRINTFORMW %CALLNAME:MASTER%는 정력박약이 되었습니다
		TALENT:MASTER:정력절륜 = 0
		TALENT:MASTER:정력박약 = 1
	ELSEIF TALENT:MASTER:정력박약
		PRINTFORMW %CALLNAME:MASTER%의 스태미너는 보통으로 되었습니다
		TALENT:MASTER:정력박약 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 정력절륜이 되었습니다
		TALENT:MASTER:정력절륜 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 30
	IF TALENT:MASTER:소변버릇
		PRINTFORMW %CALLNAME:MASTER%는 소변욕을 참을 수 있습니다
		TALENT:MASTER:소변버릇 = 0
	ELSE
		PRINTFORMW %CALLNAME:MASTER%는 소변욕을 참을 수 없습니다
		TALENT:MASTER:소변버릇 = 1
	ENDIF
	PRINTL 　
	GOTO INPUT_LOOP1
ELSEIF RESULT == 101
	SIF TFLAG:999 == 1
		TFLAG:999 = 0
	GOTO INPUT_LOOP1
ELSEIF RESULT == 109
	SIF TFLAG:999 == 0
		TFLAG:999 = 1
	GOTO INPUT_LOOP1
ELSEIF RESULT == 200
	RESTART
ELSE
	PRINTL 올바른 값을 입력해 주세요
	PRINTL 　
	GOTO INPUT_LOOP1
ENDIF

;─
;주인공 커스텀 메뉴의 표시
;─
@SHOW_CUSTOM
DRAWLINE
IF TFLAG:999 == 0
	PRINTL [0] 커스텀 완료, 다음에 진행됩니다
	PRINTFORM [1] %CALLNAME:MASTER%의 성별:
	IF TALENT:MASTER:남자
		PRINTL 남자
	ELSEIF TALENT:MASTER:후타나리
		PRINTL 후타나리
	ELSE
		PRINTL 여자 아이
	ENDIF
	PRINTFORM [2] %CALLNAME:MASTER%의 성 경험:
	IF TALENT:MASTER:처녀 || TALENT:MASTER:동정
		PRINTL 없음
	ELSEIF ABL:MASTER:욕망 > 0
		PRINTL 많은
	ELSE
		PRINTL 조금
	ENDIF
	PRINTFORM [3] %CALLNAME:MASTER%의 성 지식:
	IF MASTER_CUSTOMV(-1) == 0
		PRINTL 전무
	ELSEIF MASTER_CUSTOMV(-1) == 1
		PRINTL 조금
	ELSE
		PRINTL 풍부
	ENDIF
	PRINTFORM [4] %CALLNAME:MASTER%의 성격은:
	IF TALENT:MASTER:겁쟁이
		PRINTL 겁쟁이  (기장/보통)
	ELSEIF TALENT:MASTER:기장
		PRINTL 기장  (보통/겁쟁이)
	ELSE
		PRINTL 보통  (겁쟁이/기장)
	ENDIF
	PRINTFORM [5] %CALLNAME:MASTER%의 성격은:
	IF TALENT:MASTER:반항적
		PRINTL 반항적  (순수/보통)
	ELSEIF TALENT:MASTER:순수
		PRINTL 순수  (보통/반항적)
	ELSE
		PRINTL 보통  (반항적/순수)
	ENDIF
	PRINTFORM [6] %CALLNAME:MASTER%의 성격은:
	IF TALENT:MASTER:얌전함
		PRINTL 얌전함  (거만함/보통)
	ELSEIF TALENT:MASTER:거만함
		PRINTL 거만함  (보통/얌전함)
	ELSE
		PRINTL 보통  (얌전하다/거만함)
	ENDIF
	PRINTFORM [7] %CALLNAME:MASTER%의 프라이드는:
	IF TALENT:MASTER:프라이드높음
		PRINTL 높다
	ELSEIF TALENT:MASTER:프라이드낮음
		PRINTL 낮다
	ELSE
		PRINTL 보통
	ENDIF
	PRINTFORM [8] %CALLNAME:MASTER%의 정신은:
	IF TALENT:MASTER:자제적
		PRINTL 자제적  (충동적/보통)
	ELSEIF TALENT:MASTER:충동적
		PRINTL 충동적  (보통/자제적)
	ELSE
		PRINTL 보통  (자제적/충동적)
	ENDIF
	PRINTFORM [9] %CALLNAME:MASTER%는 주위에는:
	IF TALENT:MASTER:무관심
		PRINTL 무관심
	ELSEIF TALENT:MASTER:호기심
		PRINTL 호기심
	ELSE
		PRINTL 남들 수준에 흥미가 있다
	ENDIF
	PRINTFORM [10]%CALLNAME:MASTER%의 감정의 풍부함은:
	IF TALENT:MASTER:감정결핍
		PRINTL 감정결핍
	ELSEIF TALENT:MASTER:감수성
		PRINTL 감정 풍부
	ELSE
		PRINTL 보통
	ENDIF
	PRINTFORM [11]%CALLNAME:MASTER%의 인생관은:
	IF TALENT:MASTER:낙관적
		PRINTL 낙관적  (비관적/보통)
	ELSEIF TALENT:MASTER:비관적
		PRINTL 비관적  (보통/낙관적)
	ELSE
		PRINTL 보통  (낙관적/비관적)
	ENDIF
	PRINTFORM [12]%CALLNAME:MASTER%의 교우 관계는:
	IF TALENT:MASTER:선을그음
		PRINTL 선을그음  (보통)
	ELSE
		PRINTL 보통  (선을그음)
	ENDIF
	PRINTFORM [13]%CALLNAME:MASTER%의 표현욕구는:
	IF TALENT:MASTER:튀고싶어함
		PRINTL 튀고싶어함  (보통)
	ELSE
		PRINTL 보통  (튀고싶어함)
	ENDIF
	PRINTFORM [14]%CALLNAME:MASTER%의 정조관념:
	IF TALENT:MASTER:정조관념
		PRINTL 강함
	ELSEIF TALENT:MASTER:정조관념없음
		PRINTL 그다지 없다
	ELSE
		PRINTL 남들 수준 정도
	ENDIF
ELSE
	PRINTL [0] 커스텀 완료, 다음에 진행됩니다
	PRINTFORM [15]%CALLNAME:MASTER%의 언동은:
	IF TALENT:MASTER:억압
		PRINTL 억압  (해방/보통)
	ELSEIF TALENT:MASTER:해방
		PRINTL 해방  (보통/억압)
	ELSE
		PRINTL 보통  (억압/해방)
	ENDIF
	PRINTFORM [16]%CALLNAME:MASTER%의 수치심은:
	IF TALENT:MASTER:수줍음
		PRINTL 수줍음
	ELSEIF TALENT:MASTER:수치심옅음
		PRINTL 부끄러워하고 얇다
	ELSE
		PRINTL 보통
	ENDIF
	PRINTFORM [17]%CALLNAME:MASTER%의 몸은:
	IF TALENT:MASTER:아픔에약함
		PRINTL 아픔에약함  (아픔에강함/보통)
	ELSEIF TALENT:MASTER:아픔에강함
		PRINTL 아픔에강함  (보통/아픔에약함)
	ELSE
		PRINTL 보통  (아픔에약함/아픔에강함)
	ENDIF
	PRINTFORM [18]%CALLNAME:MASTER%의 몸은:
	IF TALENT:MASTER:젖기쉬움
		PRINTL 젖기쉬움  (젖기어려움/보통)
	ELSEIF TALENT:MASTER:젖기어려움
		PRINTL 젖기어려움  (보통/젖기쉬움)
	ELSE
		PRINTL 보통  (젖기쉬움/젖기어려움)
	ENDIF
	PRINTFORM [19]%CALLNAME:MASTER%의 학습 효율은:
	IF TALENT:MASTER:습득빠름
		PRINTL 습득빠름
	ELSEIF TALENT:MASTER:습득느림
		PRINTL 습득느림
	ELSE
		PRINTL 보통
	ENDIF
	PRINTFORM [20]%CALLNAME:MASTER%는
	IF TALENT:MASTER:자위쉬움
		PRINTL 자주(잘) 자신을 위로하고 있습니다
	ELSE
		PRINTL 스스로는 그다지 하지 않습니다
	ENDIF
	PRINTFORM [21]%CALLNAME:MASTER%의 후각은:
	IF TALENT:MASTER:악취둔감
		PRINTL 악취둔감
	ELSEIF TALENT:MASTER:악취민감
		PRINTL 악취민감
	ELSE
		PRINTL 보통
	ENDIF
	PRINTFORM [22]%CALLNAME:MASTER%의 주동성은:
	IF TALENT:MASTER:헌신적
		PRINTL 헌신적
	ELSEIF TALENT:MASTER:수동
		PRINTL 수동
	ELSE
		PRINTL 보통
	ENDIF
	PRINTFORM [23]%CALLNAME:MASTER%는 쾌감을 솔직하게 받아들입니까:
	IF TALENT:MASTER:쾌감에순수
		PRINTL 쾌감에순수
	ELSEIF TALENT:MASTER:쾌감을부정
		PRINTL 쾌감을부정
	ELSE
		PRINTL 보통
	ENDIF
	PRINTFORM [24]%CALLNAME:MASTER%는 특히 민감이나 둔한 곳이 있습니까:
	IF TALENT:MASTER:C민감
		PRINTL C민감
	ELSEIF TALENT:MASTER:C둔감
		PRINTL C둔감
	ELSEIF TALENT:MASTER:V민감
		PRINTL V민감
	ELSEIF TALENT:MASTER:V둔감
		PRINTL V둔감
	ELSEIF TALENT:MASTER:A민감
		PRINTL A민감
	ELSEIF TALENT:MASTER:A둔감
		PRINTL A둔감
	ELSEIF TALENT:MASTER:B민감
		PRINTL B민감
	ELSEIF TALENT:MASTER:B둔감
		PRINTL B둔감
	ELSE
		PRINTL 특별히 이루어
	ENDIF
	PRINTFORM [25]%CALLNAME:MASTER%의 가슴의 사이즈는:
	IF TALENT:MASTER:빈유
		PRINTL 빈유
	ELSEIF TALENT:MASTER:거유
		PRINTL 거유
	ELSE
		IF TALENT:MASTER:남자
			PRINTL 남자
		ELSE
			PRINTL 보통
		ENDIF
	ENDIF
	PRINTFORM [26]%CALLNAME:MASTER%의 체형은:
	IF TALENT:MASTER:작은체형
		PRINTL 작은체형 체형
	ELSEIF TALENT:MASTER:장신
		PRINTL 거구
	ELSE
		PRINTL 보통
	ENDIF
	PRINTFORM [27]%CALLNAME:MASTER%의 회복 스피드는:
	IF TALENT:MASTER:회복빠름
		PRINTL 회복빠름
	ELSEIF TALENT:MASTER:회복느림
		PRINTL 회복느림
	ELSE
		PRINTL 보통
	ENDIF
	IF TALENT:MASTER:남자 || TALENT:MASTER:후타나리
		PRINTFORM [28]%CALLNAME:MASTER%의 사정 스피드는:
		IF TALENT:MASTER:조루
			PRINTL 조루
		ELSEIF TALENT:MASTER:지루
			PRINTL 지루
		ELSE
			PRINTL 보통
		ENDIF
	ENDIF
	PRINTFORM [29]%CALLNAME:MASTER%의 스태미너는:
	IF TALENT:MASTER:정력절륜
		PRINTL 정력절륜
	ELSEIF TALENT:MASTER:정력박약
		PRINTL 정력박약
	ELSE
		PRINTL 보통
	ENDIF
	PRINTFORM [30]%CALLNAME:MASTER%의 요도 괄약근은:
	IF TALENT:MASTER:소변버릇
		PRINTL 느슨한
	ELSE
		PRINTL 보통
	ENDIF
ENDIF
DRAWLINE
PRINTFORML   page {TFLAG:999+1} / 2 
PRINTL [101]전의 페이지      [109]차의 페이지     [200] - 설정 모드 변경

;─
;주인공의 게이지의 셋업
;─
@BASE_MASTER_SETUP
;주인공의 사정/모유/소변욕 게이지를 셋업
CALL BASE_SETUP_PENIS(MASTER)
MAXBASE:MASTER:모유 = 0
MAXBASE:MASTER:소변욕 = 0
SIF TALENT:MASTER:모유체질
	MAXBASE:MASTER:모유 = 10000
SIF TALENT:MASTER:소변버릇
	MAXBASE:MASTER:소변욕 = 10000

;주인공의 체력/기력/이성 게이지를 셋업
IF NO:MASTER == 0
	;디폴트
	MAXBASE:MASTER:체력 = 2000
	MAXBASE:MASTER:기력 = 1200
	;아픔에강함/아픔에약함
	IF TALENT:MASTER:아픔에약함
		MAXBASE:MASTER:체력 -= 50
		MAXBASE:MASTER:기력 -= 25
	ELSEIF TALENT:MASTER:아픔에강함
		MAXBASE:MASTER:체력 += 50
		MAXBASE:MASTER:기력 += 25
	ENDIF
	;작은체형 체형/거구
	IF TALENT:MASTER:작은체형
		MAXBASE:MASTER:체력 -= 100
	ELSEIF TALENT:MASTER:장신
		MAXBASE:MASTER:체력 += 100
	ENDIF
	;정력절륜/정력박약
	IF TALENT:MASTER:정력절륜
		MAXBASE:MASTER:체력 += 25
		MAXBASE:MASTER:기력 += 75
	ELSEIF TALENT:MASTER:정력박약
		MAXBASE:MASTER:체력 -= 25
		MAXBASE:MASTER:기력 -= 75
	ENDIF
	;겁쟁이/기장
	IF TALENT:MASTER:겁쟁이
		MAXBASE:MASTER:기력 -= 50
	ELSEIF TALENT:MASTER:기장
		MAXBASE:MASTER:기력 += 50
	ENDIF
	;감정결핍/감정 풍부
	IF TALENT:MASTER:감정결핍
		MAXBASE:MASTER:기력 -= 25
	ELSEIF TALENT:MASTER:감수성
		MAXBASE:MASTER:기력 += 25
	ENDIF
	;낙관적/비관적
	IF TALENT:MASTER:비관적
		MAXBASE:MASTER:기력 -= 25
	ELSEIF TALENT:MASTER:낙관적
		MAXBASE:MASTER:기력 += 25
	ENDIF
	;억압/해방
	IF TALENT:MASTER:억압
		MAXBASE:MASTER:기력 -= 50
	ELSEIF TALENT:MASTER:해방
		MAXBASE:MASTER:기력 += 50
	ENDIF
	;호라이인
	SIF TALENT:MASTER:불사
		MAXBASE:MASTER:체력 += 100
	
	;극단적인 수치를 갭
	SIF MAXBASE:MASTER:체력 < 1000
		MAXBASE:MASTER:체력 = 1000
	SIF MAXBASE:MASTER:체력 > 3000
		MAXBASE:MASTER:체력 = 3000
	SIF MAXBASE:MASTER:기력 < 600
		MAXBASE:MASTER:기력 = 600
	SIF MAXBASE:MASTER:기력 > 1800
		MAXBASE:MASTER:기력 = 1800
ENDIF

;디폴트
MAXBASE:MASTER:이성 = 1000
;겁쟁이/기장
IF TALENT:MASTER:겁쟁이
	MAXBASE:MASTER:이성 -= 50
ELSEIF TALENT:MASTER:기장
	MAXBASE:MASTER:이성 += 50
ENDIF
;프라이드높음/프라이드낮음
IF TALENT:MASTER:프라이드높음
	MAXBASE:MASTER:이성 += 50
ELSEIF TALENT:MASTER:프라이드낮음
	MAXBASE:MASTER:이성 -= 50
ENDIF
;감정결핍/감정 풍부
IF TALENT:MASTER:감정결핍
	MAXBASE:MASTER:이성 += 50
ELSEIF TALENT:MASTER:감수성
	MAXBASE:MASTER:이성 -= 50
ENDIF
;자제적/충동적
IF TALENT:MASTER:자제적
	MAXBASE:MASTER:이성 += 50
ELSEIF TALENT:MASTER:충동적
	MAXBASE:MASTER:이성 -= 50
ENDIF
;자위쉬움
SIF TALENT:MASTER:자위쉬움
	MAXBASE:MASTER:이성 -= 50
;도착적
SIF TALENT:MASTER:도착적
	MAXBASE:MASTER:이성 -= 50
;유치
SIF TALENT:MASTER:유치
	MAXBASE:MASTER:이성 -= 50
;광기
SIF TALENT:MASTER:광기
	MAXBASE:MASTER:이성 -= 200
;금단의지식
SIF TALENT:MASTER:금단의지식
	MAXBASE:MASTER:이성 += 100

;극단적인 수치를 갭
SIF MAXBASE:MASTER:이성 < 500
	MAXBASE:MASTER:이성 = 500
SIF MAXBASE:MASTER:이성 > 1500
	MAXBASE:MASTER:이성 = 1500

CFLAG:MASTER:필요경험치 = 100

;게이지를 초기화한다
BASE:MASTER:체력 = MAXBASE:MASTER:체력
BASE:MASTER:기력 = MAXBASE:MASTER:기력
BASE:MASTER:사정 = MAXBASE:MASTER:사정
BASE:MASTER:모유 = MAXBASE:MASTER:모유
BASE:MASTER:소변욕 = 0
BASE:MASTER:이성 = MAXBASE:MASTER:이성

