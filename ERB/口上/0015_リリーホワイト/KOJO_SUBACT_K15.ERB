;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[릴리 화이트]구상 탈의·추가 액션 구상 파일
;
;	조교자가 조교 지시 이외에 실시하는 행동의 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;//잠정 사양@/L

;────────────────────────────────────
;탈의 액션 구상
;────────────────────────────────────
;·조교자 액션의 앞에 강제탈의나 자주탈의가 발생했을 때에 표시
;·이 구상의 시점에서는 아직 TEQUIP는 갱신되어 있지 않다
;·첫회 분기는 「처음 보았다/보였다」란 할 수 없다.
;  숍 메뉴로 벗게 했던 적이 있을지도 모르고, 조교의 흐름으로 완전하게 벗게 하지 않고서 노출만 하고 있는 경우도 있다.
;  어쩌면 조수였던 때에 본 적이 있을지도 모른다. 그러한 의미에서는 간이적인 것이다.
;  엄밀한 의미에서의 「초견」플래그를 준비할지 어떨지는 요망하는 대로라고 하는 일로.
;-------------------------------------------------
@KOJO_DATUI_K15
;-------------------------------------------------
;관련 변수…
;강제탈의:TFLAG:102  자주탈의:TFLAG:103
;=(-1, 강제탈의에 저항, 1, 윗도리, 2, 하반신윗도리, 3, 웃도리, 4, 상반신 전부, 5, 하반신 전부,
;  6, 악세사리─, 7, 악세사리─이외 모두, 8, 전라까지, 9, 속옷만)
;-------------------------------------------------
;○릴리 화이트○
;전신윗도리	;원피스(상하별로는 않기 때문에 주의)
;상반신속옷	;브래지어
;하반신속옷	;드로워즈
;양말		;삭스
;악세사리	;－

;(삭스 탈의)
;  →원피스 탈의(브라＆팬츠 보이고)
;　　→상or속옷 탈의(그대로응or젖가슴 보이고)
;　　　→상or속옷 탈의(전라 보이고)
;　　　　→(삭스 탈의)
;원피스를 입은 채로 팬츠 탈의(그다지 없어?)
;  →(삭스 탈의)
;　　→원피스 탈의(그대로응 보이고)
;　　　→브라 탈의(전라 보이고)
;　　　　　→(삭스 탈의)

;전신윗도리 탈의(P탈의/)
;P탈의(B탈의/윗도리 착의/)
;B탈의(P탈의/)
;단번에전라에(상대가전라/)(양말 착의/)
;함께 벗는다(전라에/)
;강제탈의(성공/실패)(자신은 벗는다(전라/))
;양말 탈의(전라에/)
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의에 저항된 FIRSTDATUI(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 < 0
	;=================================================
	;자신은 벗었다
	;=================================================
	IF 구상기입체크(100) && TFLAG:자주탈의
		IF RAND:2 == 0
			PRINTFORMW 「부끄럽다%RAND_SPLIT("의/응입니까")%?」
		ELSE
			PRINTFORMW 「양복 벗지 않는다%RAND_SPLIT("의/응입니까")%?」
		ENDIF
			PRINTFORMW 「그러면, 릴리만이라도%RAND_SPLIT("탈유지/준비 7")%%RAND_SPLIT("밭이랑/있네요/왕일까")%♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		IF RAND:2 == 0
			PRINTFORMW 「부끄럽다%RAND_SPLIT("의/응입니까")%?」
		ELSE
			PRINTFORMW 「양복 벗지 않는다%RAND_SPLIT("의/응입니까")%?」
		ENDIF
		IF RAND:2 == 0
			PRINTFORMW 「그렇지만, 벗지 않으면 양복 더러워져 버려요?」
		ELSE
			PRINTFORMW 「그렇지 않으면, 입은 채로%RAND_SPLIT("를 좋아합니까/엣치하고 싶은거야")%?   후후♪」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;두 사람 모두 뭔가 벗은 FIRSTDATUI(10)
;------------------------------------------------
;·주로 섹스계 행동의 예비 동작으로 발생한다
;·원피스 탈의라면 이 시점에서 「브라·팬츠 보이고」는 죽어?
;·삭스라면 아직 가능성도
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 > 0 && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(10)
		PRINTFORMW 「………… 후훗♪」
		PRINTFORMW 「서로 무언이라고, 반대로 두근두근 해 버리네요…」
		PRINTFORMW 「이봐요, 양복 빌려 주세요?」
		PRINTFORMW 「주름이 되지 않도록, 분명하게 정리해 두네요♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가[정욕]또한전라가 되었다
	;=================================================
	IF 구상기입체크(100) && T_COND("정욕") && ZENRACHECK(0)
		;주인님 임신중!!
		IF TALENT:TARGET:임신 == 1
			PRINTFORMDW %RAND_SPLIT("일 부풀어 오른 임산부배/완만한 둥그스름을 띤 배")%에 살그머니 손을 더해, 마지막 한 장에 손가락을 건다…
			PRINTFORMW 「영차…… 에헤헷, 전부 벗어 버렸습니다…」
		;임신하고 있지 않아!
		ELSE
			PRINTFORMW 「%RAND_SPLIT("응 실마리/영차")%%RAND_SPLIT("와/")%…… 에헤헷, 전부 벗어 버렸습니다…」
		ENDIF
		IF RAND:2 == 0
			PRINTFORMW 「릴리의%RAND_SPLIT("두근두근/흥분")%, 전해질까나?」
		ELSE
			PRINTFORMW 「릴리의%RAND_SPLIT("부끄럽다/엣치한")%곳… 전부 보여 주네요♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(1000, 1000)
			IF RAND:2 == 0
				PRINTFORMW 「부끄럽지만…%RAND_SPLIT("후후/")%」
				PRINTFORMW 「무엇인가%RAND_SPLIT("두근두근/개─응")%해서는%RAND_SPLIT("있네요/밭이랑")%」
			ELSE
				PRINTFORMW 「%RAND_SPLIT("양복이/")%주름이 되지 않도록, 분명하게 정리해―…%RAND_SPLIT("좋아/응")%」
				PRINTFORMW 「%RAND_SPLIT("이봐요, 와/어떻습니까/에헤헤")%… 꽉 하면, 조금 전보다%RAND_SPLIT("두근두근 해 버린다/따뜻하다/기분이 좋다")%군요…♪」
			ENDIF
		ELSE
			IF RAND:2 == 0
				PRINTFORMW 「입은 채로도 좋지만…」
				PRINTFORMW 「역시 릴리는%RAND_SPLIT("여기의/당신의 체온이 느껴진다")%(분)편을 좋아하네요…%RAND_SPLIT("후후/에헤헤")%♪」
			ELSE
				PRINTFORMW 「준비는 좋습니까?」
				PRINTFORMW 「그러면%RAND_SPLIT("슬슬/즉시")%시작해 버릴까요」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·악세사리─마셔 FIRSTDATUI(20)
;------------------------------------------------
;·양말을 벗는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 == 6 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;전라가 되었다(벗기 전은 양말 이외 알몸이라든지 그러한 느낌)
	;=================================================
	IF 구상기입체크(100) && TFLAG:자주탈의 && ZENRACHECK(0)
		IF RAND:2 == 0
			PRINTFORMW 「양말만이라고 하는 것도 뭔가 위화감이 있는 걸」
			PRINTFORMDW 릴리가 삭스의 발끝을 타, 이끌 때에,
			PRINTFORMDW Ｍ글자에 덮은 허벅지의 안쪽에서 어린 균열이 힐끔힐끔 보일듯 말듯 하고 있다…
			PRINTFORMW 「…… 어?   혹시, 전라에 양말만의 (분)편이 흥분하는 사람입니까?」
			PRINTFORMW 「%RAND_SPLIT("후후/흐음")%… 에서도 이번은 보류예요」
			PRINTFORMW 「네, 벌써 벗어 버렸습니다♪  네에에♪」
		ELSE
			PRINTFORMW 「%RAND_SPLIT("네에에/엣또")%…… 전부, 벗어 버렸어요」
			PRINTFORMW 「…… 발가락이 예쁘다?   후후, 이상한 칭찬하는 방법♪」
			PRINTFORMDW 릴리는 양말을 접으면서, 락 해 기분에 대굴대굴웃고 있다…
			PRINTFORMW 「%CNAME(MASTER)%에 보았으면 좋은 것은, 여기♪」
			PRINTFORMW 「이봐요, 릴리의 알몸%RAND_SPLIT(", 어떻습니까?/를 봐, 분명하게 개─분해♪")%」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		IF RAND:2 == 0
			PRINTFORMW 「릴리의%RAND_SPLIT("양말/삭스")%, 갖고 싶습니까?」
			PRINTFORMW 「후훗, 주지 않아요♪」
		ELSE
			PRINTFORMW 「한 장씩… 다음은%RAND_SPLIT("양말/삭스")%를 벗네요♪」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·만p FIRSTDATUI(30)
;·드로워즈를 벗는다
;------------------------------------------------
;·이번 탈의로 성기가 보이게 되었을 경우에 표시
;·팬티 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;응 이렇게 p
	;=================================================
	IF 구상기입체크(75) && PENIS(TARGET)
		;발기도 충분히
		IF TCVAR:TARGET:발기도 >= 1000
			IF RAND:2 == 0
				PRINTFORMDW 릴리의%RAND_SPLIT("펄떡펄떡에/딱딱")%발기한 페니스가 힘차게%CALLNAME:MASTER%의 전에%RAND_SPLIT("마주 앉음/찌르기")%나온다…
			ELSE
				PRINTFORMDW 릴리의%RAND_SPLIT("융성하게 굽은 목을 쳐든다/팡팡 긴장되었다/거북한 것 같게 텐트를 치고 있었다")%페니스가 힘차게%CALLNAME:MASTER%의 전에%RAND_SPLIT("마주 앉음/찌르기")%나온다…
			ENDIF
		;발기도 불충분
		ELSE
			PRINTFORMDW %RAND_SPLIT("충분히 정액을 모아두었다/오스 냄새나는 발정향기를 발한다")%릴리의 남성기가%CALLNAME:MASTER%의 눈앞에%RAND_SPLIT("마주 앉음/찌르기")%나온다…
		ENDIF
		;후타나리는, 조교 완료(2000/2000) 정도로 붙이는 인상
		IF FAVOR(3000, 3000)
			IF RAND:2 == 0
				PRINTFORMW 「이봐요,%CNAME(MASTER)%의 정말 좋아하는 릴리의%RAND_SPLIT("후타나리 자지/자지")%예요♪」
				PRINTFORM 「분명하게 인사, 해 주세요
				CALL HEARTCB
				PRINTFORMW 」
			ELSE
				PRINTFORMW 「이봐요, 정말 좋아하는 자지에 분명하게 인사해 주세요」
				PRINTFORMW 「\@ TALENT:MASTER:남자 ? 사내 아이인데 # 여자 아이끼리가 좋아하는가라고 생각하면, 설마 \@자지가 난 여자 아이를 좋아한다 라고… 후훗♪」
				PRINTFORM 「와… 릴리의%RAND_SPLIT("후타나리 자지/왕자지")%로 충분히기분좋게해줘 주네요
				CALL HEARTCB
				PRINTFORMW 」
			ENDIF
		ELSE
			IF RAND:2 == 0
				PRINTFORMW 「뭐 놀란 얼굴 하고 있습니까, 적당 익숙해져 주세요?」
				PRINTFORMW 「이것은%CNAME(MASTER)%가 망응%RAND_SPLIT("다/그리고 길렀다")%후타나리 자지이니까… 우후훗♪」
			ELSE
				PRINTFORMW 「역시 아직 위화감이 있네요…」
				PRINTFORMW 「%CNAME(MASTER)%에 가득 질내사정하면 익숙해지는 것입니까…%RAND_SPLIT("후후/저기")%♪」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써 가슴이 보이고 있다
	;팬츠 탈의→전라가 되는 or전라삭스
	;=================================================
	IF 구상기입체크(100) && B_DATUICHECK(0) == 1
		;윤활 충분히
		IF PALAM:TARGET:윤활 >= 500
			IF RAND:2 == 0
				PRINTFORMDW %RAND_SPLIT("애액으로 흠뻑젖었다/가랑이 부분에 부끄러운 얼룩을 만들었다")%드로워즈가 『%RAND_SPLIT("//엣취")%』라고 소리를 내 릴리의 발밑에 떨어졌다…
			ELSE
				PRINTFORMDW 애액을 들이 마셔%RAND_SPLIT("묵직 무거워졌다/딱 치부에 들러붙었다")%드로워즈에 손가락을 걸어, 천천히%RAND_SPLIT("과시한다/초조하게 한다")%같게 한쪽 발씩 벗어 간다…
			ENDIF
		;윤활 부족
		ELSE
			IF RAND:2 == 0
				PRINTFORMDW 릴리%RAND_SPLIT("는 부끄러운 듯이 백자의 허벅지를 꼭 닫았지만/의 사타구니에는 희미하게 솜털이 나 있을 뿐으로")%、%RAND_SPLIT("선명하게 했다/새끼 손가락 한 개로 가릴 수 있을 정도의/세로선 한 개의 것 아직 어리다")%균열을 덮어 가리는 것은 아무것도 없다…
			ELSE
				PRINTFORMDW 릴리는 발정한 암컷의 냄새를 감돌게 하면서,%CALLNAME:MASTER%의 눈앞에%RAND_SPLIT("선명하게 했다/새끼 손가락 한 개로 가릴 수 있을 정도의/두툼한 음순에 끼워졌다")%%RAND_SPLIT("한 줄기/세로선")%를 드러냈다…
			ENDIF
		ENDIF
		IF FAVOR(1000, 1000)
			IF RAND:2 == 0
				PRINTFORMW 「마지막 한 장도…%RAND_SPLIT("응 실마리/영차")%… 와」
				PRINTFORM 「에헤헤… 릴리의 알몸,%RAND_SPLIT("어떻습니까/어떨까요")%
				CALL HEARTCB
				PRINTFORMW 」
			ELSE
				PRINTFORMW 「최후는 드로워즈도…%RAND_SPLIT("네에에/그런데/벗어 해 버리네요")%♪」
				PRINTFORM 「릴리의 부끄러운 곳…%CNAME(MASTER)%에 전부 보았으면 좋구나
				CALL HEARTCB
				PRINTFORMW 」
			ENDIF
		ELSE
			IF RAND:2 == 0
				PRINTFORMW 「그러면,%RAND_SPLIT("마지막 한 장/드로워즈")%도…」
				PRINTFORMW 「저,%RAND_SPLIT("가만히/그렇게")%볼 수 있으면 벗기 어렵습니다…%RAND_SPLIT("엣치/이제(벌써)/")%…」
			ELSE
				PRINTFORMW 「전부, 벗어 버리네요…%RAND_SPLIT("응 실마리/영차")%… 와」
				PRINTFORMW 「저, 저…… 릴리의 신체,%RAND_SPLIT("어떨까요/개─분해 주겠습니까")%…?」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;브라 착용 상태 or원피스 착용으로 속옷만 벗는다
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「드로워즈, 벗어 버리네요……%RAND_SPLIT("응 실마리/영차/")%」
		PRINTFORMW 「에헤헤……%RAND_SPLIT("어떨까/어떨까요")%, 흥분해 버렸습니까?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·말하는 p FIRSTDATUI(40)
;·브라를 벗는다
;------------------------------------------------
;·이번 탈의로 젖가슴이 보이게 되었을 경우에 표시
;·브라 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && !TALENT:거유 && FIRSTDATUI(40)
		PRINTFORMW 「\@ TALENT:MASTER:남자 ? 그… 남자는 큰 가슴을 좋아한다는 것은 (들)물었던 적이 있습니다만… # 여자 아이끼리인데, 조금 수줍어 버리네요… \@」
		PRINTFORMW 「어떨까요… 릴리의 작은 가슴에서도, 개─분해 주겠습니까?」
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써 성기가 보이고 있다
	;브라만 착의→전라
	;공수상?
	;=================================================
	IF 구상기입체크(75) && P_DATUICHECK(0) == 1
		;거유 지문
		IF TALENT:거유
			PRINTFORMDW 릴리의%RAND_SPLIT("풍부한/휘어지게 여물었다/크고 탄력 있다")%유방이%RAND_SPLIT("응/응")%와 브라로부터 흘러 떨어진다
		;빈유지문
		ELSEIF TALENT:빈유
			PRINTFORMDW 릴리의%RAND_SPLIT("완만하게 부풀어 오른 유방/완만한 쌍구의 정점으로 피는 벚꽃의 꽃봉오리가/얇게 미성숙인 유방")%가 브라아래로부터 얼굴을 들여다 보게 한다
		;보통젖지문
		ELSE
			PRINTFORMDW 릴리의%RAND_SPLIT("부풀어 오르기 시작한 찡 탄력 있다/손바닥에 꼭 들어가는 정도의/요정으로 해서는 큰/포동포동 부드러운")%미유가 브라아래로부터 얼굴을 내밀었다
		ENDIF
		IF FAVOR(1000, 1000)
			IF RAND:2 == 0
				PRINTFORMW 「%RAND_SPLIT("네에에/어, 어떨까요")%…… 릴리의 젖가슴으로, 개─분해 주겠습니까?」
				PRINTFORMW 「%RAND_SPLIT("시선이 박힙니다만/저, 만약 좋다면")%…%RAND_SPLIT("손대어/빨아/맛봐/파후파후 해")%봅니까?   후후♪」
			ELSE
				PRINTFORMW 「전부, 벗어 버리네요…%RAND_SPLIT("응 실마리/영차")%… 와」
				PRINTFORMW 「저, 저…… 릴리의 신체,%RAND_SPLIT("어떨까요/개─분해 주겠습니까")%…?」
			ENDIF
		ELSE
			IF RAND:2 == 0
				PRINTFORMW 「아직, 조금%RAND_SPLIT("수줍어 버리네요/부끄럽네요")%…」
				PRINTFORMW 「뭐라고 말할까요, 엣치한 일을 하고 있는 때와는 또 달라…」
				PRINTFORMW 「벗을 때마다,%RAND_SPLIT("당신을 요구하는 기분이 강해져 가/지금부터 엣치한 일 해 버린대/릴리의 엣치한 마음도 간파해져 가는 것 같아")%…」
				PRINTFORMW 「무엇인가…%RAND_SPLIT("개─응/두근두근")%해 버립니다… 에헤헷」
			ELSE
				PRINTFORMW 「젖가슴, 좋아합니까?   후후,%RAND_SPLIT("어쩐지 사랑스럽다/아기 보고 싶다/응석쟁이씨")%군요♪」
				PRINTFORMW 「상냥하게 해 준다면…%RAND_SPLIT("비비어/손대어/타/빨아/들이마셔/는 해/물렁물렁은 해")%봐도 괜찮아요?   후후♪」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;팬츠를 신은 채로, 브라 탈의
	;=================================================
	IF 구상기입체크(100)
		;거유 지문
		IF TALENT:거유
			PRINTFORMDW 릴리의%RAND_SPLIT("풍부한/휘어지게 여물었다/크고 탄력 있다")%유방이%RAND_SPLIT("응/응")%와 브라로부터 흘러 떨어진다
		;빈유지문
		ELSEIF TALENT:빈유
			PRINTFORMDW 릴리의%RAND_SPLIT("완만하게 부풀어 오른 유방/완만한 쌍구의 정점으로 피는 벚꽃의 꽃봉오리가/얇게 미성숙인 유방")%가 브라아래로부터 얼굴을 들여다 보게 한다
		;보통젖지문
		ELSE
			PRINTFORMDW 릴리의%RAND_SPLIT("부풀어 오르기 시작한 찡 탄력 있다/손바닥에 꼭 들어가는 정도의/요정으로 해서는 큰/포동포동 부드러운")%미유가 브라아래로부터 얼굴을 내밀었다
		ENDIF
		IF FAVOR(1000, 1000)
			IF RAND:2 == 0
				PRINTFORMW 「%RAND_SPLIT("네에에/어, 어떨까요")%…… 릴리의 젖가슴으로, 개─분해 주겠습니까?」
				PRINTFORMW 「엣또… 릴리의 것으로 좋다면,%RAND_SPLIT("손대어/빨아/맛봐/파후파후 해")%봅니까?   후후♪」
			ELSE
				PRINTFORMW 「이봐요,%CNAME(MASTER)%의 정말 좋아하는 릴리의 젖가슴이에요♪」
				PRINTFORM 「드로워즈를 벗기 전에… 우선은 젖가슴으로%RAND_SPLIT("즐거움/기분 좋아져")%짊어지는군
				CALL HEARTCB
				PRINTFORMW 」
			ENDIF
		ELSE
			IF RAND:2 == 0
				PRINTFORMW 「아직, 조금%RAND_SPLIT("수줍어 버리네요/부끄럽네요")%…」
				PRINTFORMW 「뭐라고 말할까요, 엣치한 일을 하고 있는 때와는 또 달라…」
				PRINTFORMW 「벗을 때마다,%RAND_SPLIT("당신을 요구하는 기분이 강해져 가/지금부터 엣치한 일 해 버린대/릴리의 엣치한 마음도 간파해져 가는 것 같아")%…」
				PRINTFORMW 「무엇인가…%RAND_SPLIT("개─응/두근두근")%해 버립니다… 에헤헷」
			ELSE
				PRINTFORMW 「젖가슴, 좋아합니까?   후후,%RAND_SPLIT("어쩐지 사랑스럽다/아기 보고 싶다/응석쟁이씨")%군요♪」
				PRINTFORMW 「상냥하게 해 준다면…%RAND_SPLIT("비비어/손대어/타/빨아/들이마셔/는 해/물렁물렁은 해")%봐도 괜찮아요?   후후♪」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·팬티 보이고 FIRSTDATUI(50)
;자주탈의·브라 보이고 FIRSTDATUI(60)
;------------------------------------------------
;·원피스를 벗는다
;·정리합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(60)
		RETURN 1
	ENDIF
	
	;=================================================
	;원피스를 벗어 브라＆팬츠 보이고
	;=================================================
	IF 구상기입체크(100)&& P_DATUICHECK(0) == 2
		;윤활 충분하면 팬츠
		IF PALAM:TARGET:윤활 >= 500
			IF RAND:2 == 0
				PRINTFORMDW 릴리의%RAND_SPLIT("백자의/새하얀/투명하도록(듯이) 희다/건강한")%허벅지, 그리고%RAND_SPLIT("애액으로 흠뻑젖었다/가랑이 부분에 부끄러운 얼룩을 만들었다")%드로워즈가 공공연하게 된다…
			ELSE
				PRINTFORMDW 얄팍한 드로워즈는 릴리의 애액을 들이 마셔,%RAND_SPLIT("가랑이 부분에 부끄러운 얼룩을 작/딱 치부에 들러붙어 어린 균열을 공공연하게 시키고")%라고 있다…
			ENDIF
		;윤활 부족하면 브라
		ELSE
			;거유
			IF TALENT:거유
				PRINTFORMDW 릴리의%RAND_SPLIT("풍부한/휘어지게 여물었다/크고 탄력 있다")%유방이 브라안%RAND_SPLIT("로 거북한 것 같게 들어가/에 충분히 들어가/로부터 당장 흘러 떨어져 마을 흔들려")%있다…
			;빈유
			ELSEIF TALENT:빈유
				PRINTFORMDW 브라의 사이즈가 컸던 것일까, 릴리가 움직일 때에%RAND_SPLIT("그 무방비인 가슴팍/브라의 틈새")%로부터 힐끔힐끔%RAND_SPLIT("핑크의 꽃봉오리/벚꽃의 꽃봉오리가/찡 발기한 유두")%가 얼굴을 들여다 보게 했다…
			;보통젖
			ELSE
				PRINTFORMDW 릴리는%RAND_SPLIT("순백의/레이스를 다루었다/연분홍색의/")%사랑스러운 속옷을 과시하도록(듯이),%CALLNAME:MASTER%의 앞에서 빙글 1바퀴 해 보였다…
			ENDIF
		ENDIF

		IF FAVOR(1000, 1000)
			IF RAND:2 == 0
				PRINTFORMW 「이봐요… 천천히, 한 장씩 벗어 가네요……%RAND_SPLIT("후후/후후/")%」
				PRINTFORM 「%CNAME(MASTER)%를 위해서(때문에) 사랑스러운 속옷을 입고 왔기 때문에, 분명하게%RAND_SPLIT("개─분해/두근두근 해/정령을 모아두어")%주세요
				CALL HEARTCB
				PRINTFORMW 」
			ELSE
				PRINTFORMW 「%RAND_SPLIT("저기요/응/글쎄요")%、%RAND_SPLIT("이것 새롭게 사 본/이 속옷이 지금 유행비치는 것 같은 응/일단 승부 속옷")%이지만… 어떨까요?」
				PRINTFORMW 「좀 더%RAND_SPLIT("어리다/엣치한/밝은 색의/사랑스럽다/부끄럽다")%속옷이%CNAME(MASTER)%기호였는지… 후훗♪」
			ENDIF
		ELSE
			IF RAND:3 == 0
				PRINTFORMW 「%RAND_SPLIT("어와/저, 저")%… 어떻게로%RAND_SPLIT("인가/짊어질까")%?」
				PRINTFORMW 「릴리의 속옷 모습에서도%RAND_SPLIT("두근두근/불끈불끈/개─응")%해 주면 기쁘구나♪」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「%RAND_SPLIT("/영차")%… 양복 접기 때문에, 조금 기다리고 있어 주세요?」
			ELSE
				PRINTFORMW 「조금 아이 같습니까?」
				PRINTFORMW 「%RAND_SPLIT("/이제(벌써)/그런 일 말해져도")%… 릴리는 사랑스러운 속옷을 좋아합니다 것…」
				PRINTFORMW 「불만이라면 이제(벌써) 보여 주지 않기 때문에?」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;팬츠 탈의→원피스 탈의의 패턴
	;그대로응 보여에 들이마셔져?   순번 바꿀까…
	;착의를 좋아하면 가능성도
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「%RAND_SPLIT("저기요/응/글쎄요")%、%RAND_SPLIT("이것 새롭게 사 본/이 속옷이 지금 유행비치는 것 같은 응/일단 승부 속옷")%이지만… 어떨까요?」
		PRINTFORMW 「좀 더%RAND_SPLIT("어리다/엣치한/밝은 색의/사랑스럽다/부끄럽다")%속옷이%CNAME(MASTER)%기호였는지… 후훗♪」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·전라에 FIRSTDATUI(70)
;·전라까지 벗었다
;------------------------------------------------
;·「전라까지 벗었다」만이 아니고, 「지금 벗었으므로전라가 되었다」라고 하는 의미
;·「윗도리 밖에 남지 않은 상태로 윗도리를 벗었다」등도 포함된다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && ZENRACHECK(0) && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(70)
		PRINTFORMW 
		RETURN 1
	ENDIF

	;=================================================
	;주인님 임신중!!
	;=================================================
	IF 구상기입체크(60) && TALENT:TARGET:임신 == 1
		PRINTFORMW 「%RAND_SPLIT("네에에/후후/어떨까")%… 상당히 배도 부풀어 올라 온 것이에요?」
		PRINTFORM 「이봐요, 꽉 해… 릴리의 신체가 차가워지지 않도록, 분명하게 따뜻하게 해 주세요
		CALL HEARTCB
		PRINTFORMW 」
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음(75％)
	;=================================================
	IF 구상기입체크(75)
		;발기도 충분히
		IF TCVAR:TARGET:발기도 >= 1000
			PRINTFORMDW %RAND_SPLIT("펄떡펄떡에/딱딱에/배에 닿을 것 같을 정도")%발기한 릴리의 페니스가, 수컷 수상한 페로몬을 풀어 놓으면서%CALLNAME:MASTER%의 전에%RAND_SPLIT("마주 앉음/찌르기")%나온다…
			PRINTFORMW 「전부 벗어 버렸습니다… 에헤헷♪」
			PRINTFORM 「이봐요, 전에 가르쳤군요? …… 릴리의%RAND_SPLIT("발기 자지/후타나리 자지/자지")%에, 분명하게 인사합니다
			CALL HEARTCB
			PRINTFORMW 」
			RETURN 1
		;윤활 충분하면 팬츠
		ELSEIF PALAM:TARGET:윤활 >= 500
			IF RAND:2 == 0
				PRINTFORMDW 완전히 다 녹았다%RAND_SPLIT("비밀의 틈/밀호")%로부터%RAND_SPLIT("애액/애밀")%가 투와 흘러넘쳐, 릴리 발밑에 몇 개의 부끄러운 얼룩을 만들었다…
				PRINTFORMW 「%RAND_SPLIT("응 실마리/영차")%%RAND_SPLIT("와/")%…… 에헤헷, 전부 벗어 버렸습니다…」
				PRINTFORMW 「흘림 하고 있는 것 같다… 는, 부끄러운 일 말하지 말아 주세욧!!」
				PRINTFORM 「이제(벌써)…… 각오, 해 주세요
				CALL HEARTCB
				PRINTFORMW 」
			ELSE
				;성교경험, 당신에 나 있다
				IF EXP:TARGET:성교경험 > 80 && PENIS(MASTER)
					PRINTFORMDW 완전히 유용되고%CALLNAME:MASTER%의%RAND_SPLIT("페니스/남근/웅기/음경")%의%RAND_SPLIT("모양을 기억/맛을 기억/모양에/포로와")%릴리의%RAND_SPLIT("암컷/질/미/바보")%육은,%RAND_SPLIT("갖고 싶어하는 듯이 얇게 입을 열어/환희때를 기대해/녹진녹진")%상스러운 군침을 늘어뜨리고 있다…
					PRINTFORMW 「이봐요, 릴리의 알몸… 좀 더 잘 봐 주세요……」
					PRINTFORMW 「%RAND_SPLIT("코코/또/보지/뱃속")%가……%RAND_SPLIT("두근두근/쿨쿨/꽈악/두근두근은")%해, 뜨거워서…」
					PRINTFORM 「%CNAME(MASTER)%가 아니면… 이 쑤셔, 멈추지 않는 것 같습니다……
					CALL HEARTCB
					PRINTFORMW 」
				ELSE
					PRINTFORMDW 완전히 다 녹았다%RAND_SPLIT("비밀의 틈/밀호")%로부터%RAND_SPLIT("애액/애밀")%가 투와 흘러넘쳐, 릴리 발밑에 몇 개의 부끄러운 얼룩을 만들었다…
					PRINTFORMW 「%RAND_SPLIT("응 실마리/영차")%%RAND_SPLIT("와/")%… 전부, 벗어 버렸습니다♪」
					PRINTFORMW 「%RAND_SPLIT("이렇게 젖어 버려 아픈 응이라고/이것, 마치 실금 해 버린 것 같아")%… 어쩐지 부끄럽네요… 에헤헷」
				ENDIF
			ENDIF
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;아마 조교 1발째로 여기에 떨어진다
	;=================================================
	IF 구상기입체크(100)
		;윤활 충분히, 성교경험, 당신에 나 있다
		IF EXP:TARGET:성교경험 > 80 && PENIS(MASTER) && PALAM:TARGET:윤활 >= 1000
			PRINTFORMDW 완전히 유용되고%CALLNAME:MASTER%의%RAND_SPLIT("페니스/남근/웅기/음경")%의%RAND_SPLIT("모양을 기억/맛을 기억/모양에/포로와")%릴리의%RAND_SPLIT("암컷/질/미/바보")%육은,%RAND_SPLIT("갖고 싶어하는 듯이 얇게 입을 열어/환희때를 기대해/녹진녹진")%상스러운 군침을 늘어뜨리고 있다…
			PRINTFORMW 「이봐요, 릴리의 알몸… 좀 더 잘 봐 주세요……」
			PRINTFORMW 「%RAND_SPLIT("코코/또/보지/뱃속")%가……%RAND_SPLIT("두근두근/쿨쿨/꽈악/두근두근은")%해, 뜨거워서…」
			PRINTFORM 「%CNAME(MASTER)%가 아니면… 이 쑤셔, 멈추지 않는 것 같습니다……
			CALL HEARTCB
			PRINTFORMW 」
		;윤활 충분히
		ELSEIF PALAM:TARGET:윤활 >= 1000
			IF RAND:2 == 0
				PRINTFORMDW %RAND_SPLIT("애액으로 흠뻑젖었다/가랑이 부분에 부끄러운 얼룩을 만들었다")%드로워즈가 『%RAND_SPLIT("//엣취")%』라고 소리를 내 릴리의 발밑에 떨어졌다…
			ELSE
				PRINTFORMDW 애액을 들이 마셔%RAND_SPLIT("묵직 무거워졌다/딱 치부에 들러붙었다")%드로워즈에 손가락을 걸어, 천천히%RAND_SPLIT("과시한다/초조하게 한다")%같게 한쪽 발씩 벗어 간다…
			ENDIF
		;윤활 부족
		ELSE
			SELECTCASE RAND:5
				CASE 0
					PRINTFORMDW %RAND_SPLIT("천천히/한 장씩/초조하게 하도록(듯이)")%, 릴리의 몸을 싸는 천이 파사리와 소리를 내 발밑에 떨어져 간다……
				CASE 1
					PRINTFORMDW 둥실둥실의 로브아래로부터는, 사랑스러운 브라와 드로워즈에 장식해졌다%RAND_SPLIT("투명한 것처럼 흰 피부가/미성숙인 소녀의 지체가")%……
					PRINTFORMDW 더욱 쌍구의 정점으로 피는 연분홍색의 사랑스러운 꽃봉오리도,%RAND_SPLIT("선명하게 한 예쁜/새끼 손가락 한 개로 가릴 수 있을 정도의 어리다")%세로선도 순서에 공공연하게 되어 간다……
				CASE 2
					PRINTFORMDW 릴리의 손가락끝이 마지막에 남은 흰 옷감에 걸려, 천천히%RAND_SPLIT("선명하게 했다/새끼 손가락 한 개로 가릴 수 있을 정도의/세로선 한 개의 것 아직 어리다")%균열이 공공연하게 되어 간다…
				CASE 3
					PRINTFORMDW 릴리%RAND_SPLIT("는 부끄러운 듯이 백자의 허벅지를 꼭 닫았지만/의 사타구니에는 희미하게 솜털이 나 있을 뿐으로")%、%RAND_SPLIT("선명하게 했다/새끼 손가락 한 개로 가릴 수 있을 정도의/세로선 한 개의 것 아직 어리다")%균열을 덮어 가리는 것은 아무것도 없다…
				CASE 4
					PRINTFORMDW 릴리는 발정한 암컷의 냄새를 감돌게 하면서,%CALLNAME:MASTER%의 눈앞에%RAND_SPLIT("선명하게 했다/새끼 손가락 한 개로 가릴 수 있을 정도의/두툼한 음순에 끼워졌다")%%RAND_SPLIT("한 줄기/세로선")%를 드러냈다…
			ENDSELECT
		ENDIF

		;대사
		SELECTCASE RAND:6
			CASE 0
				PRINTFORMW 「전부, 벗어 버리네요…%RAND_SPLIT("응 실마리/영차")%… 와」
				PRINTFORMW 「저, 저…… 릴리의 신체,%RAND_SPLIT("어떨까요/개─분해 주겠습니까")%…?」
			CASE 1
				PRINTFORMW 「그러면,%RAND_SPLIT("마지막 한 장/드로워즈")%도…」
				PRINTFORMW 「저,%RAND_SPLIT("가만히/그렇게")%볼 수 있으면 벗기 어렵습니다…%RAND_SPLIT("엣치/이제(벌써)/")%…」
			CASE 2
				PRINTFORMW 「아직, 조금%RAND_SPLIT("수줍어 버리네요/부끄럽네요")%…」
				PRINTFORMW 「뭐라고 말할까요, 엣치한 일을 하고 있는 때와는 또 달라…」
				PRINTFORMW 「벗을 때마다,%RAND_SPLIT("당신을 요구하는 기분이 강해져 가/지금부터 엣치한 일 해 버린대/릴리의 엣치한 마음도 간파해져 가는 것 같아")%…」
				PRINTFORMW 「무엇인가…%RAND_SPLIT("개─응/두근두근")%해 버립니다… 에헤헷」
			CASE 3
				PRINTFORMW 「젖가슴, 좋아합니까?   후후,%RAND_SPLIT("어쩐지 사랑스럽다/아기 보고 싶다/응석쟁이씨")%군요♪」
				PRINTFORMW 「상냥하게 해 준다면…%RAND_SPLIT("비비어/손대어/타/빨아/들이마셔/는 해/물렁물렁은 해")%봐도 괜찮아요?   후후♪」
			CASE 4
				PRINTFORMW 「마지막 한 장도…%RAND_SPLIT("응 실마리/영차")%… 와」
				PRINTFORM 「에헤헤… 릴리의 알몸,%RAND_SPLIT("어떻습니까/어떨까요")%
				CALL HEARTCB
				PRINTFORMW 」
			CASE 5
				PRINTFORMW 「최후는 드로워즈도…%RAND_SPLIT("네에에/그런데/벗어 해 버리네요")%♪」
				PRINTFORM 「릴리의 부끄러운 곳…%CNAME(MASTER)%에 전부 보았으면 좋구나
				CALL HEARTCB
				PRINTFORMW 」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·악세사리─마셔 FIRSTDATUI(80)
;------------------------------------------------
;·주로 양말. 그 밖에 장갑이나 소매 따위
;·벗긴 중에 접어들지 않는다! 되기 십상인 것으로 이것만은 부위별 텐프레를 준비
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 == 6
	;=================================================
	;전라가 되었다
	;=================================================
	IF 구상기입체크(100) && ZENRACHECK(1)
		SELECTCASE RAND:3
			CASE 0
				PRINTFORMW 「이봐요, 분명하게 다리 올리지 않으면 벗게 하지 않아요」
				PRINTFORMW 「전라양말 같은거 얼간이인 모습,%CNAME(MASTER)%도 싫겠지요?   후후♪」
			CASE 1
				PRINTFORMW 「이봐요, 릴리가 양말 벗게 해 주니까요?」
				PRINTFORMW 「알몸에 양말인 채는, 변태씨같고 불쌍하네요… 후훗♪」
			CASE 2
				PRINTFORMW 「이봐요, 이제(벌써) 알몸과 다름없으니까 저항하지 않고, 저기?」
				PRINTFORMW 「저항하면 무리하게 벗겨 버려요?」
				PRINTFORMW 「응～～, 네♪  이것입니다 팡팡이군요♪」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		IF RAND:2 == 0
			PRINTFORMW 「이봐요, 릴리가 양말%RAND_SPLIT("벗어 버릴까/벗게 해 버리는군/벗어 해요")%♪」
			PRINTFORMW 「갈아입음도\@ TALENT:MASTER:남자 ? 사정 # 식사도 \@… 전부 릴리가 돌봐 주니까요♪」
		ELSE
			PRINTFORMW 「예쁜%RAND_SPLIT("손가락/발")%… 먹어 버리고 싶을 정도♪」
			PRINTFORMW 「그렇다, 발바닥%RAND_SPLIT("고서 고서 해/간질임")%면서가게해 줄까요?」
			PRINTFORM 「저것, 상당히 기분 좋은 것 같아요
			CALL HEARTCB
			PRINTFORMW 」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·고추 보여라 FIRSTDATUI(90)
;------------------------------------------------
;·이번 탈의로 남성기가 보이게 되었을 경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;·전라남자·후타나리는 여기에 떨어진다. 아마 제일 빈도가 많다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && PENIS(MASTER)
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;조교 대상 발기도 파생
		SELECTCASE TCVAR:MASTER:발기도
			CASE IS > 1500
				PRINTFORMDW %RAND_SPLIT("펄떡펄떡에/딱딱에/배에 닿을 것 같을 정도")%%RAND_SPLIT("발기했다/격분했다/뒤로 젖혔다")%웅기가 철썩%RAND_SPLIT("배를/릴리의 뺨을")%쳐, 한층 더 쾌락을 추구해 흠칫 뛰었다…
			CASE IS > 1000
				PRINTFORMDW 속옷으로부터 얼굴을 내밀었다%RAND_SPLIT("음경/육봉")%가, 릴리에 인사를 하는 것처럼 흠칫 튀었다…
			CASEELSE
				PRINTFORMDW 한심하게 속옷을 벗겨져 부끄러운 부분을 모두 릴리에 보여져 버렸다…
		ENDSELECT
		SELECTCASE RAND:4
			CASE 0
				PRINTFORMW 「부끄럽습니까? … 후훗」
				PRINTFORM 「지금부터, 좀 더 부끄러운 일… 해 주니까요
				CALL HEARTCB
				PRINTFORMW 」
			CASE 1
				PRINTFORMW 「이봐요, 숨기면%RAND_SPLIT("메/안돼")%예요?」
				PRINTFORM 「라고 손은 뒤… 자지는 릴리의 얼굴의 전에, 말야
				CALL HEARTCB
				PRINTFORMW 」
			CASE 2
				PRINTFORMW 「사랑스럽지만… 그로테크스…?」
				PRINTFORMW 「몇번 봐도, 질리지 않네요…%RAND_SPLIT("후후/에헤헷")%♪」
			CASE 3
				PRINTFORM 「엣치한 모양, 거기에 매우 냄새가 나서…%RAND_SPLIT("후~/응/후하아")%…
				CALL HEARTCB
				PRINTFORMW 」
				PRINTFORM 「자지, 충분히 귀여워해 주네요
				CALL HEARTCB
				PRINTFORMW 」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·그대로응 보여라 FIRSTDATUI(100)
;------------------------------------------------
;·이번 탈의로 여성기가 보이게 되었을 경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && !TALENT:MASTER:남자
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;윤활 충분히
		IF PALAM:MASTER:윤활 >= 1000
			PRINTFORMW 「이제(벌써)%RAND_SPLIT("녹진녹진/주륵주륵/미끈미끈")%가 되어 버리고 있네요…」
			PRINTFORM 「이봐요…%RAND_SPLIT("상스럽다/엣치한/추잡함")%%RAND_SPLIT("실 당겨 버려/냄새가 나")%야… 후훗
			CALL HEARTCB
			PRINTFORMW 」
		ELSE
			PRINTFORMW 「예쁜 몸이니까, 숨기고 있어서는 아까워요?」
			PRINTFORMW 「이봐요,%RAND_SPLIT("엣치한 균열/핑크의 고기/사랑스러운 보지")%… 좀 더 잘 보여 주세요…」
		ENDIF
		PRINTFORM 「%RAND_SPLIT("괜찮아/안심해/릴리에 맡겨")%, 여자 아이의%RAND_SPLIT("엣치한 단지/느끼는 곳")%는…～응과%RAND_SPLIT("알아/알아/알아")%로부터
		CALL HEARTCB
		PRINTFORMW 」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·젖가슴 보여라 FIRSTDATUI(110)
;------------------------------------------------
;·이번 탈의로 젖가슴이 보이게 되었을 경우에 표시
;·브라 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;·조교 대상이 남자라고 표시하지 않기 때문에, 남자 한정 구상에서는 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && B_DATUICHECK(1) == 3 && !TALENT:MASTER:남자
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;조교 대상이 거유
		IF TALENT:MASTER:거유
			PRINTFORMW 「큰 젖가슴이군요～」
			PRINTFORMW 「짜면 밀크도 나와 버릴 것 같습니다♪」
		;조교 대상이 빈유
		ELSEIF TALENT:MASTER:빈유
			PRINTFORMW 「작은 젖가슴…%RAND_SPLIT("사랑스럽다/아이같네요")%」
		;보통
		ELSE
			PRINTFORMW 「꼭 릴리의 손바닥에 들어갈 정도…%RAND_SPLIT("미유라는 녀석이군요/의욕이 있어 예뻐요")%」
		ENDIF

		IF RAND:2 == 0
			PRINTFORM 「릴리가 가득, 기분 좋게 해 주니까요
			CALL HEARTCB
			PRINTFORMW 」
		ELSE
			PRINTFORM 「가득 만지작거려, 음유에 시켜 버릴까… 후훗
			CALL HEARTCB
			PRINTFORMW 」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·팬티 보여라 FIRSTDATUI(120)
;------------------------------------------------
;·이번 탈의로 하반신속옷이 보이게 되었을 경우에 표시
;·조교 대상이 하반신속옷의 없는 캐릭터 한정의 구상이면 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 2
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;조교 대상으로 페니스 있어
		IF PENIS(MASTER)
			;조교 대상 발기도 파생
			SELECTCASE TCVAR:MASTER:발기도
				CASE IS > 1500
					PRINTFORMW 「%RAND_SPLIT("후후/쿠스/아하하")%♪  텐트 쳐 버려, 정말로 사랑스러우니까♪」
				CASE IS > 1000
					PRINTFORMW 「%RAND_SPLIT("슝슝/스으… 후~/흥흥")%…%RAND_SPLIT("엣치한/농후한/오징어 냄새가 나다/미숙하다/짐승 같은")%%RAND_SPLIT("수컷의/정액의/오스의/")%냄새로 어질 어질해 버립니다♪」
				CASEELSE
					PRINTFORMW 「그리고 한 장으로%RAND_SPLIT("사랑스럽다/당신의")%자지를 볼 수 있군요… 후훗♪」
			ENDSELECT
			SELECTCASE RAND:3
				CASE 0
					PRINTFORM 「우선은 옷감 너머로… 글자～절의 부엌 애태움이라고 주네요
					CALL HEARTCB
					PRINTFORMW 」
				CASE 1
					PRINTFORM 「와… 빨리 벗고 싶어지도록(듯이), 가득%RAND_SPLIT("애태움이라고/스윽스윽 해/부비부비 해/괴롭혀")%주네요
					CALL HEARTCB
					PRINTFORMW 」
				CASE 2
					PRINTFORMW 「이봐요, 와… 팬츠 너머로 자지%RAND_SPLIT("부비부비/쫄깃쫄깃/스윽스윽/좋은 아이 좋은 아이")%해 줄테니까…」
					PRINTFORM 「%RAND_SPLIT("엣치한 국물/로리콘 정액/질척질척 태우고─해")%, 해 버려요
					CALL HEARTCB
					PRINTFORMW 」
			ENDSELECT
		;조교 대상으로 페니스 없음
		ELSE
			;윤활 충분히
			IF PALAM:MASTER:윤활 >= 1000
				PRINTFORMW 「%RAND_SPLIT("어/응/응～")%?   … 응, 가랑이의 곳에%RAND_SPLIT("엣치한/상스럽다/추잡하다/부끄럽다")%얼룩이 가능하게 되고 있어요?」
				PRINTFORMW 「기대해 버리고 있을까나… 후훗, 사랑스러우니까♪」
			ELSE
				PRINTFORMW 「이봐요, 그리고 한 장으로%CNAME(MASTER)%의 중요한 곳이 뻔히 보임이군요… 후훗♪」
			ENDIF
				IF RAND:2 == 0
					PRINTFORM 「우선은 옷감 너머로… 글자～절의 부엌 애태움이라고 주네요
					CALL HEARTCB
					PRINTFORMW 」
				ELSE
					PRINTFORM 「다음에 직접 손대어 주기 때문에… 기대해 기다리고 있어 주세요
					CALL HEARTCB
					PRINTFORMW 」
				ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·브라 보여라 FIRSTDATUI(130)
;------------------------------------------------
;·이번 탈의로 브라 상반신속옷이 보이게 되었을 경우에 표시
;·조교 대상이 남자라고 표시하지 않기 때문에, 남자 한정 구상에서는 쓸 필요가 없다
;·조교 대상이 상반신속옷의 없는 캐릭터 한정의 구상이면 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && B_DATUICHECK(1) == 2 && !TALENT:MASTER:남자
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(130)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「사랑스러운 속옷, 무엇 입어도 어울린다 라고 부럽습니다…」
		PRINTFORMW 「… 이번 함께, 코스프레 엣치해 봅니까?   후후♪」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·전라에 FIRSTDATUI(140)
;------------------------------------------------
;·「전라까지 벗겼다」만이 아니고, 「지금 벗겼으므로전라가 되었다」라고 하는 의미
;·「윗도리 밖에 남지 않은 상태로 윗도리를 벗겼다」등도 포함된다
;·「젖가슴 보여라」～「팬티 보여라」를 썼을 경우는 기입할 필요는 없다(상층의 분기에 먹혀져 여기까지 떨어져 오지 않기 때문에)
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:강제탈의 && ZENRACHECK(1)
	;=================================================
	;그 외·조건 없음
	;=================================================
;	IF 구상 기입 체크(0)
;		PRINTFORMW 
;		RETURN 1
;	ENDIF
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·뭔가 벗은 FIRSTDATUI(150)
;------------------------------------------------
;·자위·봉사계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;주인님 임신중!!
		IF TALENT:TARGET:임신 == 1
			PRINTFORMDW %RAND_SPLIT("일 부풀어 오른 임산부배/완만한 둥그스름을 띤 배")%에 살그머니 손을 더해, 천천히 의복을 벗어 간다…
			PRINTFORMW 「영차… 와  네에에, 벗어 버렸습니다…」
		;임신하고 있지 않아!
		ELSE
			PRINTFORMDW 릴리는%RAND_SPLIT("신선하고 어린 신체를 과시하도록(듯이)/당신의 시선을 권하도록(듯이)/")%%RAND_SPLIT("천천히 초조하게 하도록(듯이)/요염한 행동으로/힘차게")%의복을 벗어 간다…
			IF RAND:2 == 0
				PRINTFORMW 「부끄럽지만…%RAND_SPLIT("후후/네에에/")%…」
				PRINTFORMW 「무엇인가%RAND_SPLIT("두근두근/개─응")%해서는%RAND_SPLIT("있네요/밭이랑")%」
			ELSE
				PRINTFORMW 「%RAND_SPLIT("양복이/")%주름이 되지 않도록, 분명하게 정리해―…%RAND_SPLIT("좋아/응")%」
				PRINTFORMW 「%RAND_SPLIT("이봐요, 와/어떻습니까/에헤헤")%… 꽉 하면, 조금 전보다%RAND_SPLIT("두근두근 해 버린다/따뜻하다/기분이 좋다")%군요…♪」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·뭔가 벗긴 FIRSTDATUI(160)
;------------------------------------------------
;·애무계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의
	IF RAND:2 == 0
		PRINTFORMW 「%RAND_SPLIT("이봐요/자자")%, 그것도 벗어 버릴까요♪」
		PRINTFORM 「입은 채라면… 엣치한 국물로 더러워져 버리니까요
		CALL HEARTCB
		PRINTFORMW 」
	ELSE
		PRINTFORMW 「양복 벗어 해요♪」
		PRINTFORMW 「%RAND_SPLIT("이거 참/이봐요/네에에/이제(벌써)")%, 이야이야 해도 쓸데없기 때문에♪」
	ENDIF
	RETURN 1
ENDIF

;========================================================================
;추가 액션 구상
;========================================================================
;·조교자가 추가행동을 취했을 때에 범용적으로 표시
;·쓰는 경우, 액션 구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
;────────────────────────────────────
;EXTRA1, Ｃ애무
;────────────────────────────────────
;·추가로 애무 또는 자위를 실시한다
;·조교 대상으로 페니스가 없는 경우는 애무, 어느 경우는 자위
;-------------------------------------------------
@KOJO_ACT_EXTRA_K15_1
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_0, 애무한다
;-------------------------------------------------
;·조교 대상으로 페니스가 없는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !PENIS(MASTER)
	PRINTFORMW 추가 열리면
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_1, 자위 한다
;-------------------------------------------------
;·조교 대상으로 페니스가 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && PENIS(MASTER)
	PRINTFORMW 추가 열리면
	RETURN 1
ENDIF

;────────────────────────────────────
;EXTRA2, Ｖ애무
;────────────────────────────────────
;·추가로 바지 애무를 실시한다
;·조교 대상이 남자의 경우 발생하지 않는다
;·조교 대상이 처녀의 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K15_2
;-------------------------------------------------
IF 구상기입체크(0)
	PRINTFORMW 추가 열리면
	RETURN 1
ENDIF

;────────────────────────────────────
;EXTRA3, Ａ애무
;────────────────────────────────────
;·추가로 애널애무를 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K15_3
;-------------------------------------------------
IF 구상기입체크(0)
	PRINTFORMW 추가 열리면
	RETURN 1
ENDIF

;────────────────────────────────────
;EXTRA4, Ｂ애무
;────────────────────────────────────
;·추가로 유두 고문을 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K15_4
;-------------------------------------------------
IF 구상기입체크(0)
	PRINTFORMW 추가 열리면
	RETURN 1
ENDIF

;────────────────────────────────────
;미약등사용구상
;────────────────────────────────────
;·조교자가 미약이나 로션을 사용했을 때에 범용적으로 표시
;·「액션 구상」으로 기입 체크를=2로 했을 경우는 표시되지 않는다
;·쓰는 경우, 액션 구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
@KOJO_USE_K15
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션/미약동시 사용
;------------------------------------------------
;·주로 성교계 커맨드로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && GETBIT(TFLAG:미약등사용구상, 0) && GETBIT(TFLAG:미약등사용구상, 1)
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션 사용
;------------------------------------------------
;·완구·성교계 커맨드등으로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && GETBIT(TFLAG:미약등사용구상, 0)
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;미약사용
;------------------------------------------------
;·애무계 커맨드등으로부터 발생한다
;·키스로 발생했을 경우는 입으로 옮김이 되기 (위해)때문에, 그 케이스는 키스의 액션 구상내에서 처리하는 편이 좋다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && GETBIT(TFLAG:미약등사용구상, 1)
	PRINTFORMW 
	RETURN 1
ENDIF
