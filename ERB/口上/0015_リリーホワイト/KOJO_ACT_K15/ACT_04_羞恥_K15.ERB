;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[릴리 화이트]구상 수치계 액션 구상 파일
;
;	수치계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;수치계 ACT40～45
;========================================================================
;·TFLAG:ACT(40=자위/41=비패개장/42=자위과시하고/43=수치플레이/44=야외플레이/45=방뇨/46=코스프레(미실장))
;·TFLAG:ACT분류 == 4에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "수치")로 줍는 것이 가능
;·ACT에 따라서는 거부가 가능하고 있거나 않기도 하고
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;─
;ACT40, 자위
;─
;·자위 해 보여라라고 명령한다
;·이 단계에서 명령에 따르는지 아닌지는 미정
;·REACT분류가 소극적 따른다/태연하게 따른다/적극적 따른다/받아들이는 이외의 경우는, 모두 REACT분류 "거부"취급
;·턴방침이 "이상"이라면 발생하지 않는다
;·밀어넘어뜨리기중인 경우는 발생하지 않는다
;·벌써 자위중인 경우는 발생하지 않는다
;·TEQUIP(줄/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·이 ACT에 파생은 없다
;-------------------------------------------------
@KOJO_ACT_K15_40
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·지문에 분기는 없음
;·@TARGET_DESCRIPTION로 상황 묘사가 들어가는 경우 있어
;·조교 대상의 페니스의 유무, 페니스 없음의 경우는 페니스밴드의 유무에 의한 나누어 쓰고가 생각된다
;·조교 대상이 남자이면 쾌B는 발생하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
;텐프레로서는 상태마다 첫회 분기를 나누는 것도 어떨까라고 생각했으므로 통합해 최상위에 배치
;파생마다 첫회 판정하고 싶은 경우는 개별의 분기에 분리해, FIRSTACT(0)/FIRSTACT(1)/FIRSTACT(2)와 같이하면 좋다
;조교 대상으로 페니스가 없는 경우, 페니스밴드 분기와 그 외 분기의 양쪽 모두에 떨어질 가능성이 있으므로
;FIRSTACT의 이벤트번호를 가지런히 해 버리는지, 감히 쌍방을 살릴까는 쓰는 사람 나름
;-------------------------------------------------
IF 구상기입체크(0) && FIRSTACT(0)
	;=================================================
	;조교 대상으로 페니스 있어		TALENT:MASTER:남자 /TALENT:MASTER:후타나리등으로 구별 차지지 않은 것도 없다
	;=================================================
	IF 구상기입체크(0) && PENIS(MASTER)
		PRINTFORMW 
		RETURN 1
	ENDIF
	;=================================================
	;페니스밴드장착 안			당연한일이면서, 페니스는 없다
	;=================================================
	IF 구상기입체크(0) && TEQUIP:페니스밴드
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상의 분기를 모두 묻으면 여자 MASTER 분기에 상당
	;방치해 이 개소에만 한다면 범용적으로 기술하는 것을 추천
	;=================================================
	;그 외						녀 MASTER로 페니스밴드를 장착하고 있지 않는 경우
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상으로 페니스가 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;MASTER가 남자, 혹은 후타나리인 경우
;그 외·조건 없음을 쓴 다음, 필요에 따라서 분기를 증감해 주세요
;개소에 따라서는 TALENT:MASTER:남자든지 TALENT:MASTER:후타나리로 별라고도 좋다고 생각합니다
;-------------------------------------------------
IF 구상기입체크(0) && PENIS(MASTER)
	;=================================================
	;전COM가가게해!
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("가게해!")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 기분좋게해
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("기분좋게해")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:갈것같다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자도 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;오나홀 장착중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:오나홀
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;애널비즈 /애널바이브 장착중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:애널비즈, TEQUIP:애널바이브) > 0
		;애널비즈 사용중
		IF TEQUIP:애널비즈
			PRINTFORMW 
		;애널바이브 사용중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;만족보너스로 "하드"를 선택하고 있는 경우는 벌플래그가 선 채로 있으므로
	;IF 구상 기입 체크(25)와 같이 확률로 하는 것이 좋은 경우도 있다
	;=================================================
	;벌모드
	;=================================================
	IF 구상기입체크(0) && TFLAG:벌플래그
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 페니스밴드를 장착하고 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;필요에 따라서 이 네스트를 개방해, 분기를 더한 다음 사용해 주세요
;! PENIS(MASTER) 분기에 통합해 버리는 방법도 있습니다
;-------------------------------------------------
IF 구상기입체크(0) && TEQUIP:페니스밴드
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상으로 페니스가 없는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;필요에 따라서 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(0) && !PENIS(MASTER)
	;=================================================
	;전COM가가게해!
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("가게해!")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 기분좋게해
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("기분좋게해")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:갈것같다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자도 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;바이브/애널바이브 장착중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:바이브, TEQUIP:애널바이브) > 0
		;바이브 사용중
		IF TEQUIP:바이브
			PRINTFORMW 
		;애널바이브 사용중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;만족보너스로 "하드"를 선택하고 있는 경우는 벌플래그가 선 채로 있으므로
	;IF 구상 기입 체크(25)와 같이 확률로 하는 것이 좋은 경우도 있다
	;=================================================
	;벌모드
	;=================================================
	IF 구상기입체크(0) && TFLAG:벌플래그
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;─
;ACT41, 비패개장
;─
;·조교자가 조교 대상의 비패를 개장 한다. 소위 「쿠빠아」
;·조교 대상이 남자의 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"의 경우는 발생하지 않는다
;·TEQUIP:삼각목마 장착중은 발생하지 않는다
;·밀어넘어뜨리기중은 발생하지 않는다
;·성교중/성교봉사중은 발생하지 않는다
;·이 ACT에 파생은 없다
;·현재 리액션 후의@ACTION_APPLY2에 처리 없음
;-------------------------------------------------
@KOJO_ACT_K15_41
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·TEQUIP(안면승마/자위중)은 해제된다
;·하반신 전부 강제탈의의 처리 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT41, 비패개장
;━━━━━━━━━━━━━━━━━━━━━━━━
;TEQUIP(아이마스크/줄) 등의 분기를 마련해 주는 것도 재미있을지도 모릅니다
;필요에 따라서 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		;조교 대상이 처녀
		IF TALENT:MASTER:처녀
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 처녀
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:처녀
		;윤활 충분히
		IF PALAM:MASTER:윤활 >= 500
			PRINTFORMW 
		;윤활 부족
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;윤활 충분히
		IF PALAM:MASTER:윤활 >= 500
			PRINTFORMW 
		;윤활 부족
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT42, 자위과시하고
;─
;·조교자가 과시하도록(듯이) 자위를 시작한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·조교자가 벌써 자위중인 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K15_42
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;조교자 욕망, 기교, 조교레벨, 조교자 기교, 기묘한손가락소질에 응해 쾌C/쾌BSOURCE 증가
;바이브 소지의 경우의 처리는 0616판 시점에서는 봉인되고 있다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자에게 페니스가 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && PENIS(TARGET)
	;-------------------------------------------------
	;소분기
	;-------------------------------------------------
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 「여자 자위과시하고 첫회」
		PRINTFORMW 「」
		PRINTFORMW 「」
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;아이마스크 장착중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:아이마스크
		;조교 대상도 자위중
		IF GETBIT(TEQUIP:자위중, 0)
			PRINTFORMDW 
			PRINTL  
			PRINTFORMW 「아이마스크 상호 자위」
		;통상
		ELSE
			PRINTFORMW 「아이마스크 자위」
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;조교 대상도 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 「상호 자위」
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;구속중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:줄
		PRINTFORMW 「구속중 자위과시하고」
		PRINTFORMW 
		RETURN 1
	ENDIF


	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		IF FAVOR(0, 2000)
			IF RAND:4 == 0
				PRINTFORMW 「하아…, 응……%CNAME(MASTER, TARGET)%의 모습을 보고 있으면…… 불끈불끈 해 와 버렸다」
				PRINTFORMW 「응아…, 아아…, 후아……%CNAME(MASTER, TARGET)%도, 릴리로 흥분햇」
			ELSEIF RAND:3 == 0
				PRINTFORMW 「RAND3」
				PRINTFORMW 「」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「RAND2」
				PRINTFORMW 「」
			ELSE
				PRINTFORMW 「RAND1」
				PRINTFORMW 「」
			ENDIF
		ELSE
			IF RAND:4 == 0
				PRINTFORMW 「RAND4」
				PRINTFORMW 「」
				PRINTFORMW 「」
			ELSEIF RAND:3 == 0
				PRINTFORMW 「RAND3」
				PRINTFORMW 「」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「RAND2」
				PRINTFORMW 「」
				PRINTFORMW 「」
			ELSE
				PRINTFORMW 「조금…… 부끄럽지만」
				PRINTFORMW 「릴리의 것으로…… 흥분해 주세요」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;페니스밴드장착 안
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TEQUIP:조교자페니스밴드
	;=================================================
	;조교 대상도 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자에게 페니스가 없는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !PENIS(TARGET)
	;-------------------------------------------------
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		;상호 자위
		IF GETBIT(TEQUIP:자위중, 0)
			PRINTFORMW 「%CNAME(MASTER, TARGET)%가 보고 있으면 릴리도 하고 싶어져 버렸습니다…… 읏♪」
			PRINTFORMW 「…… 하아……%CNAME(MASTER, TARGET)%를 위해서(때문에) 가득 만지작거려 버릴테니까……」
			PRINTFORM 「릴리의 엣치한 모습을 반찬으로 해… 읏… 자위 해 주세요
			CALL HEARTCB
			PRINTFORMW 」
			PRINTL  
			PRINTFORMDW 권하는 것 같은 말과는 정반대로 릴리의 소리는 희미하게 떨려, 보여지는 일에 익숙하지 않는 것 같았다
			PRINTFORMDW 릴리는 꾸욱 허리를 내밀어, 무방비에 노출된 음순을 가느다란 손가락으로 개─와 어루만진다
			PRINTFORMDW 흘러넘친 애액을 음핵에 처발라, 중지와 약지로 원을 그리는 것처럼 해 돌기를 희롱하고 있다……
			PRINTL  
			PRINTFORMW 「응우, 창피하여 얼굴이 화끈거릴 것 같은 정도 부끄러운데, 굉장히 느껴 버립니다……」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%도 같음…… 일까?   후후♪」
			PRINTFORM 「『혼자 엣치』인데, 우쿳, 둘이서 하는 것 기분─군요
			CALL HEARTCB
			PRINTFORMW 」
		ELSE
			PRINTFORMW 「릴리의 『혼자 엣치』보고 있어 주세요…… 읏♪」
			PRINTL  
			PRINTFORMDW 릴리는 수치로 목 언저리까지 홍조시켜, 떨리는 손가락으로 미육을 펴넓힌다……
			PRINTFORMDW %CALLNAME:MASTER%가 보이기 쉽게 허리를 구불거리게 해, 음핵 포피에 휩싸여진 민감한 꽃봉오리에 손가락을 기게 한다……
			PRINTL  
			PRINTFORMW 「째씨의 가죽을 박히라고, 앗, 만지작만지작은 합니다……♪」
			PRINTFORMW 「상스러운 곳……%CNAME(MASTER, TARGET)%로 보여져 흥분, 응쿳♪」
			PRINTL  
			PRINTFORMDW 릴리좀 끈적끈적한 물소리를 세우면서, 물기를 띤 눈동자로%CALLNAME:MASTER%를 응시했다……
			PRINTL  
			PRINTFORMW 「하아, 하웃♪  당겨 버렸, 군요…… 시선이 무섭습니다……」
			PRINTFORMW 「릴리는 엣치해, 변태로, 나쁜 아이이니까……%CNAME(MASTER, TARGET)%에 미움받아 버린다……」
			PRINTFORMW 「…… 그렇지만 멈추지 않습니다…… 힛…… 우쿳……」
			PRINTL  
			PRINTFORMW 「……-를 봐……%CNAME(MASTER, TARGET)%에도, 개─분했으면 좋구나……」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;아이마스크 장착중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:아이마스크
		;조교 대상도 자위중
		IF GETBIT(TEQUIP:자위중, 0)
			;녀 조교자 x남 or후타나리
			IF TALENT:MASTER:남자 || TALENT:MASTER:후타나리
				PRINTFORMW 「아이마스크로 안보이는 만큼, 귀로 망상해…… 읏♪」
				PRINTFORM 「%CNAME(MASTER, TARGET)%의 라고 손의 움직임에 맞추어, -도 보지 만지작거리고 있어요
				CALL HEARTCB
				PRINTFORMW 」
				PRINTL  
				PRINTFORMDW 강직을 잡아당기는 소리에 조금 늦어, 『』라고 희미한 흡착음이 고막을 흔든다
				PRINTFORMDW 빨리, 늦고…… 깊고, 얕고…… 반복할 때에, 점점 2개의 추잡한 소리가 꼭 겹치기 시작한다
				PRINTFORMDW %CALLNAME:MASTER%는 릴리의 비육에 비벼넣어지는 감촉을 상상해, 과시하는 것처럼 열중해 손을 움직였다……
				PRINTL  
				PRINTFORMW 「초조하게 하지 맛, 아큣♪  빠르게, 쫄깃쫄깃 햇♪」
				PRINTFORM 「후훗♪  도─그래 섹스 햇, 함께 기분 좋아지자
				CALL HEARTCB
				PRINTFORMW 」
			;여자 아이끼리
			ELSE
				PRINTFORMW 「아이마스크로 안보이는 만큼, 귀로 망상해…… 읏♪」
				PRINTFORM 「%CNAME(MASTER, TARGET)%의 라고 손의 움직임에 맞추어, -도 보지 만지작거리고 있어요
				CALL HEARTCB
				PRINTFORMW 」
				PRINTL  
				PRINTFORMDW %CALLNAME:MASTER%가 비밀의 틈을 훑는 소리에 조금 늦어, 희미한 흡착음이 고막을 흔든다
				PRINTFORMDW 빨리, 늦고…… 깊고, 얕고…… 반복할 때에, 점점 2개의 추잡한 소리가 꼭 겹치기 시작한다
				PRINTFORMDW %CALLNAME:MASTER%는 비밀스런 장소에 흘러 들어가지는 릴리의 시선을 상상해, 열중해 손을 움직였다……
			ENDIF
		;통상
		ELSE
			PRINTFORMW 「아이마스크로 안보이는 만큼, 약간 대담하게 만지작거려 버리네요……」
			PRINTFORM 「%CNAME(MASTER, TARGET)%도 릴리의 엣치한 소리로, 가득 흥분해 주세요
			CALL HEARTCB
			PRINTFORMW 」
			PRINTL  
			PRINTFORMDW 얇은 옷스침의 소리에 이어, 『』라고 희미한 흡착음이 고막을 흔든다
			PRINTFORMDW 릴리의 달콤한 염성이 점점 높아져, 수분을 충분히 포함한 물소리가 울리기 시작한다……
			PRINTFORMDW 추잡한 소리가, 달콤한 미취가, 예민하게 되었다%CALLNAME:MASTER%의 감각 신경에 분홍색의 안개를 걸친다……
			PRINTL  
			PRINTFORMW 「응쿳, 하앗♪  이런 상스러운 모습으로……%CNAME(MASTER, TARGET)%의 앞에서 자위 해……」
			PRINTFORM 「보이고 있을 리가 없는데……%CNAME(MASTER, TARGET)%의 시선으로 가 버릴 것 같습니닷
			CALL HEARTCB
			PRINTFORMW 」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;조교 대상도 자위중
	;=================================================
	IF 구상기입체크(100) && GETBIT(TEQUIP:자위중, 0)
		IF RAND:2 == 0
			PRINTFORMW 「릴리도 자위 하고 싶어져 버렸습니다…… 읏♪」
			PRINTFORM 「이봐요,%CNAME(MASTER, TARGET)%도 보고 있을 뿐이 아니고…… 릴리를 반찬으로 해 쫄깃쫄깃 해
			CALL HEARTCB
			PRINTFORMW 」
			PRINTL  
			PRINTFORMDW 릴리는 꾸욱 허리를 내밀어, 무방비에 노출된 음순을 가느다란 손가락으로 개─와 어루만진다
			PRINTFORMDW 흘러넘친 애액을 음핵에 처발라, 중지와 약지로 원을 그리는 것처럼 해 돌기를 희롱하고 있다……
			PRINTL  
			PRINTFORMW 「수치않고 가부구응, 굉장히 느껴 있습니다……%CNAME(MASTER, TARGET)%도 개─분하고 있는 거야?」
			PRINTFORM 「아우, 우읏♪  『혼자 엣치』인데, 우쿳, 둘이서의 기분─군요
			CALL HEARTCB
			PRINTFORMW 」
		ELSE
			PRINTFORMW 「저기, 자위 서로 보여주기 할까요♪  보기 쉽게…… 영차♪」
			PRINTFORM 「%CNAME(MASTER, TARGET)%의 라고라고의 움직임에 맞추어, 릴리도 함께 자위 해 버리네요
			CALL HEARTCB
			PRINTFORMW 」
			PRINTL  
			PRINTFORMDW 강직을 잡아당기는 소리에 조금 늦어, 『츄풋츄풋』이라고 희미한 흡착음이 고막을 흔든다
			PRINTFORMDW 빨리, 늦고…… 깊고, 얕고…… 반복할 때에, 점점 2개의 추잡한 소리가 꼭 겹치기 시작한다
			PRINTFORMDW %CALLNAME:MASTER%는 릴리의 비육에 비벼넣어지는 감촉을 상상해, 열중해 손을 움직였다……
			PRINTL  
			PRINTFORMW 「아큣♪  빠른 것, 곧 가 버려요♪」
			PRINTFORM 「좀 더♪  도─그래 섹스 햇, 함께 기분 좋아지자
			CALL HEARTCB
			PRINTFORMW 」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;구속중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:줄
		PRINTFORMW 「움직일 수 없다%CNAME(MASTER, TARGET)%의 눈앞에서……」
		PRINTFORMW 「자위…… 해 버리네요♪」
		PRINTL  
		PRINTFORMDW 혀를 펴면 닿을 것 같은 거리로, 릴리가 권하도록(듯이) 부드러운 허벅지를 『하』의 글자에 열린다
		PRINTFORMDW 볼록 살집의 좋은 비밀의 틈이 얇게 입을 열어, 갖고 싶어하는 듯이 투명한 우액을 늘어뜨리고 있다……
		PRINTL  
		PRINTFORM 「아우, 하앗
		CALL HEARTCB
		PRINTFORM   시선만으로, 읏, 질내까지 범해지고 있는 것 같습니닷
		CALL HEARTCB
		PRINTFORMW 」
		PRINTFORM 「저기요…… 째씨, 괴롭히기 때문에♪  부탁, -를 보고 있어 주세요
		CALL HEARTCB
		PRINTFORMW 」
		RETURN 1
	ENDIF

	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			IF RAND:4 == 0
				PRINTFORMW 「%CNAME(MASTER, TARGET)%의 모습을 보고 있으면…… 두근두근 해 와 버렸습니다♪」
				PRINTFORM 「응쿳, 읏, 하아♪릴리의 엣치한 모습 봐,%CNAME(MASTER, TARGET)%도 두근두근 해 주세요
				CALL HEARTCB
				PRINTFORMW 」
			ELSEIF RAND:3 == 0
				PRINTFORMW 「릴리의 자위…… 보고 있어 주세요♪」
				PRINTL  
				PRINTFORMDW 혀를 펴면 닿을 것 같은 거리로, 릴리의 부드러운 허벅지가 『M』의 형태에 열린다
				PRINTFORMDW 벚꽃의 꽃봉오리가 핑 연주해져 뿜어 나온 감밀이 음순을 타 꽉 오므라진 국혈을 적신다……
				PRINTFORMDW 눈앞에서 피로 되는 단정치 못한 치태에,%CALLNAME:MASTER%는 한 눈을 팔 수가 없다……
				PRINTL  
				PRINTFORM 「아우, 하앗
				CALL HEARTCB
				PRINTFORM   시선만으로, 읏, 질내까지 범해지고 있는 것 같습니닷
				CALL HEARTCB
				PRINTFORMW 」
				PRINTFORM 「저기요…… 좀 더 안쪽도 괴롭히기 때문에♪  부탁, -를 보고 있어 주세요
				CALL HEARTCB
				PRINTFORMW 」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「%CNAME(MASTER, TARGET)%에 보고 있어 받으면 말이죠, 굉장히 흥분해……」
				PRINTFORMW 「부끄러운데, 응쿳, 굉장히 느껴 버리며……♪」
				PRINTFORM 「『혼자 엣치』인데, 우쿳, 두 명 쪽이 기분 좋아져 버렸습니다
				CALL HEARTCB
				PRINTFORMW 
			ELSE
				PRINTFORMW 「릴리의 『혼자 엣치』보고 있어 주세요…… 읏♪」
				PRINTL  
				PRINTFORMDW 릴리는 수치로 목 언저리까지 홍조시켜, 떨리는 손가락으로 미육을 펴넓힌다……
				PRINTFORMDW 추잡하게 허리를 구불거리게 해, 음핵 포피에 휩싸여진 민감한 꽃봉오리에 손가락을 기게 한다……
				PRINTL  
				PRINTFORMW 「이봐요, 째씨의 가죽을 박히라고, 앗, 만지작만지작은 합니다……♪」
				PRINTFORMW 「릴리의 상스러운 곳……%CNAME(MASTER, TARGET)%로 보여져 흥분, 응쿳♪」
			ENDIF
		ELSE
			IF RAND:4 == 0
				PRINTFORMW 「릴리의 부끄러운 곳, 전부 보았으면 좋은거야……」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%로 보여지면서…… 혼자 엣치해」
				PRINTFORMW 「엣치한 국물 늘어뜨려 가 버리는 곳까지……」
				PRINTFORMW 「전부 보고 있어 주세요♪」
			ELSEIF RAND:3 == 0
				PRINTFORMW 「릴리의 자위, 읏, 보고 있어 주세요♪」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%의 손가락을 상상해……」
				PRINTFORM 「째씨, 괴롭혀 버리네요
				CALL HEARTCB
				PRINTFORMW 」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「여자 아이의 혼자 엣치의 방법……」
				PRINTFORM 「릴리가 특별히 가르쳐 주네요
				CALL HEARTCB
				PRINTFORMW 」
				PRINTL  
				PRINTFORMW 「…… 이것이라도 상당히 부끄러워요? 후훗♪」
			ELSE
				PRINTFORMW 「조금…… 부끄럽지만…… 응우♪」
				PRINTFORMW 「릴리의 자위 봐…… 흥분해 주세요」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT43, 수치플레이
;─
;·조교 대상을 대거울의 전에 데리고 간다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:삼각목마 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·이미 수치플레이 안이라면, 수치플레이 종료가 된다
;-------------------------------------------------
@KOJO_ACT_K15_43
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·TFLAG:ACT파생(0=개시/1=종료)
;-------------------------------------------------
;·현상 본체측에서는 노출 SOURCE 증가의 처리가 있을 뿐
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT43_0, 수치플레이 개시
;━━━━━━━━━━━━━━━━━━━━━━━━
;요컨데 개시, 종료시에게만 표시되는 개소. 지문도 매정함
;간결하게 끝마쳐 버리는지, 감히 열중한 일을 할까는 쓰는 사람 나름
;TCVAR:MASTER:상태변화 등으로 나누는 것도 재미있을지도 모릅니다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;조건으로서는 레어이지만 현상, 이 가능성은 일단 고려해 두는 것에 나쁠건 없을까라고 한다
	;조교 도구가 다수 있는 경우, 한 번의 조교에 사용할 수 있는 도구의 수를 규제할까? 그렇다고 하는 이야기도 있으므로
	;장래적으로는 미묘한 서는 위치의 분기일지도 모른다
	;=================================================
	;아이마스크 장착중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:아이마스크
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT43_1, 수치플레이 종료
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT44, 야외플레이
;─
;·조교 대상으로 목걸이를 입어 밖에 데리고 나간다
;·턴방침이 "소프트", "노멀"이라면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(줄/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·이미 야외플레이 안이라면, 야외플레이 종료가 된다
;-------------------------------------------------
@KOJO_ACT_K15_44
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·TFLAG:ACT파생(0=개시/1=종료)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT44_0, 야외플레이 개시
;━━━━━━━━━━━━━━━━━━━━━━━━
;개시시에 자위중, 안면승마 안인 경우, 그것들은 해제된다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT44_1, 야외플레이 종료
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT45, 방뇨
;─
;·오줌누어 보여라라고 명령한다
;·이 단계에서는 명령에 따를지 어떨지 확정하고 있지 않다
;·REACT분류가 소극적 따른다/태연하게 따른다/적극적 따른다/받아들이는 이외의 경우는, 모두 REACT분류 "거부"취급
;·턴방침이 "소프트"라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드) 장착중에는 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·소변욕(BASE:MASTER:소변욕)이 5000 이하라면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K15_45
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && FIRSTACT(0)
	;=================================================
	;조교 대상이 남자의 경우
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:남자
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 후타나리의 경우
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:후타나리
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기서 범용적으로 기술할까 상층의 분기에서 개별적으로 나누어 쓸까는 쓰는 사람 나름
	;모두 묻는다면, 조교 대상이 여자인 경우가 됩니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 남자의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TALENT:MASTER:남자
	;=================================================
	;벌모드
	;=================================================
	IF 구상기입체크(0) && TFLAG:벌플래그
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 후타나리의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TALENT:MASTER:후타나리
	;=================================================
	;벌모드
	;=================================================
	IF 구상기입체크(0) && TFLAG:벌플래그
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 여자 아이의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;나누어 쓰고가 귀찮으면 이 개소의&& ! PENIS(MASTER)를 삭제해 여기서 보완해 버리는 방법도 있습니다
;-------------------------------------------------
IF 구상기입체크(0) && !PENIS(MASTER)
	;=================================================
	;벌모드
	;=================================================
	IF 구상기입체크(0) && TFLAG:벌플래그
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
