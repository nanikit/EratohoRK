;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[릴리 화이트]구상 콤비 네이션계 액션 구상 파일
;
;	콤비 네이션계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;콤비 네이션계 ACT300～312
;========================================================================
;·TFLAG:ACT(300=W자위/301=W페라/302=W키스/303=W가랑이/304=W파이즈리/305=W풋잡
;·306=승마두구멍공격/307=대면두구멍공격/308=변칙혼란모란/309=애널두구멍공격/310=W역강간/311=골짜기를건너감/312=2개빨고강제)
;·조수가 존재하는 경우,50%의 확률로 콤비 네이션에 파생한다
;·TFLAG:ACT가 GET_COMBIACTNUM(TFLAG:ACT)로 덧쓰기되어 함수내에서 ACT 번호가 정의된다
;·콤비 네이션시의 ASSI는 반드시(ASSI:1)된다
;·ACT_ABLE는 기본적으로 파생 바탕으로 준거
;-------------------------------------------------
;  회화 문중등에서 조수를 불러 나누고 싶을 때는%CNAME((ASSI:1), TARGET)%등으로 하는 일에 의해
;  캐릭터 CSV로 정의되고 있으면 상대에 의해 불러 나눕니다
;  EVENT111, 주인님으로서 새로운 조교자를 고용한 개소에서 CSTR:(FLAG:주인님):호칭목록에 대입하는 일에 의해
;  구상측에서 임의의 통칭을 정의하는 일도 가능
;  조수가 본래의 주인님이라고 하는 가능성도 있다
;-------------------------------------------------

;─
;ACT300, W자위
;─
;·조교자와 조수의 2명이 조교 대상으로 손 애무 한다
;·ACT10, 손으로애무로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_300
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT300, W자위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;%CALLNAME:MASTER%는 %CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%의 손가락에 성기 전체를 빠짐없이 애무되고
	;단번에 절정으로 이끌렸다…
	;=================================================
	;절정으로 이끌리고
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:초조하게해번 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 반응을 물으면서 잡은 육봉을 상하시키고
	;절정 직전에 애태우고 있다…
	;=================================================
	;초조하게 해 안
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:초조하게해번 > 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 함께 %CALLNAME:MASTER%의 남성기를 손으로 훑어 냈다…
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT301, W페라
;─
;·조교자와 조수의 2명이 조교 대상으로 펠라치오를 실시한다
;·ACT12, 입으로애무로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_301
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT301, W페라
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 귀두를 사이에 두어 혀를 얽히게 해라
	;사정을 재촉했다…
	;=================================================
	;절정으로 이끌리고
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:초조하게해번 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 귀두를 중심으로 졸졸 혀를 기 조생
	;절정 직전에 애태우고 있다…
	;=================================================
	;초조하게 해 안
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:초조하게해번 > 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 서로 경쟁하도록(듯이) %CALLNAME:MASTER%의 남성기에 혀를 기게 했다…
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT302, W키스
;─
;·조교자와 조수의 2명이 조교 대상으로 키스를 실시한다
;·ACT15, 키스로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_302
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·지문에 분기 없음. 내용은 이하와 같다
;  %CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%에 얼굴을 접근해,
;  %CALLNAME:MASTER%의 혀를 서로 빼앗았다…
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT302, W키스
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;조교자가 갈 것 같다
		IF TCVAR:갈것같다
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;초조하게 해 안
		IF TCVAR:MASTER:초조하게해번 > 1
			PRINTFORMW 
		;초조하게 해 없음
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT303, W가랑이
;─
;·조교자와 조수의 2명이 조교 대상으로 가랑이를 실시한다
;·조교 대상으로 페니스가 있는 것이 전제
;·ACT16, 성기를서로켠다로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_303
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·지문은 조교 대상이 위로돌리고지 아닌지로 분기
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT303, W가랑이
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 음순으로 %CALLNAME:MASTER%의 남성기를 끼워 넣어 켜기 시작했다…
	;=================================================
	;조교 대상이 위로돌리고
	;=================================================
	IF 구상기입체크(0) && TFLAG:위로돌리고
		;초조하게 해 안
		IF TCVAR:MASTER:초조하게해번 > 1
			PRINTFORMW 
		;초조하게 해 없음
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%상하과 겹쳐져, 밀착한 음순의 사이에
	;남성기를 삽입하도록(듯이) 명했다…
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;초조하게 해 안
		IF TCVAR:MASTER:초조하게해번 > 1
			PRINTFORMW 
		;초조하게 해 없음
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT304, W파이즈리
;─
;·조교자와 조수의 2명이 조교 대상으로 파이즈리를 실시한다
;·조교 대상으로 페니스가 있는 것이 전제
;·ACT17, 파이즈리한다로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_304
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·지문은 초조하게해번에 의해 분기. 각 분기내에서 조교자＆조수의 가슴의 사이즈에 의한 파생
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT304, W파이즈리
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;정직, 그 밖에 얼마든지 할 길은 있습니다만, 우선 텐프레로서 제시한다면 이런 느낌일까와
	;=================================================
	;절정으로 이끌리고
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:초조하게해번 == 1
		;%CALLNAME:MASTER%의 페니스는 두 명의 거유의 틈새로 구깃구깃으로 되고
		;가는 것은 이미 시간의 문제다…
		;-------------------------------------------------
		;조교자와 조수가 거유
		;-------------------------------------------------
		IF 구상기입체크(0) && TALENT:거유 && TALENT:(ASSI:1):거유
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 힘껏 가슴을 대어 자그마한 골짜기를 만들면
		;%CALLNAME:MASTER%의 페니스를 사이에 둬, 사랑스럽게 정액을 졸라대고 있다…
		;-------------------------------------------------
		;조교자와 조수가 빈유
		;-------------------------------------------------
		IF 구상기입체크(0) && TALENT:빈유 && TALENT:(ASSI:1):빈유
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 페니스를 가슴으로 사이에 두면
		;격렬하게 상반신을 흔들어 단번에 절정까지 이끌었다…
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
		;온전히 하는 것은 수고인 것으로 "그 외"만으로 끝마쳐 버리는 것도 있어
		;여기를 묻어 두면 상위의 분기를 비워도 보완됩니다
	ENDIF
	
	;=================================================
	;초조하게 해 안
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:초조하게해번 > 1
		;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 페니스를 가슴으로 완전하게 감싸면
		;보들보들 압력을 가하고 %CALLNAME:MASTER%를 초조하게 하고 있다…
		;-------------------------------------------------
		;조교자와 조수가 거유
		;-------------------------------------------------
		IF 구상기입체크(0) && TALENT:거유 && TALENT:(ASSI:1):거유
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 페니스를 가슴으로 사이에 두어 얼싸안아
		;둘이서 혀를 서로 걸고 있다…
		;-------------------------------------------------
		;조교자와 조수가 빈유
		;-------------------------------------------------
		IF 구상기입체크(0) && TALENT:빈유 && TALENT:(ASSI:1):빈유
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 페니스를 가슴으로 사이에 두면
		;%CALLNAME:MASTER%의 반응을 물으면서 초조하게 하고 있다…
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
		;온전히 하는 것은 수고인 것으로 "그 외"만으로 끝마쳐 버리는 것도 있어
		;여기를 묻어 두면 상위의 분기를 비워도 보완됩니다
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 남성기를 4개의 유방으로 끼워 넣었다…
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;발기
		IF TCVAR:MASTER:발기도 >= 1000
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT305, W풋잡
;─
;·조교자와 조수의 2명이 조교 대상으로 풋잡한다
;·조교 대상으로 페니스가 있는 것이 전제
;·ACT18, 풋잡한다로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_305
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·지문에 분기 없음. 내용은 이하와 같다
;  %CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 협력해 %CALLNAME:MASTER%의 남성기를 다리로 훑어 냈다…
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT305, W풋잡
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:갈것같다
		;초조하게해번에 의한 파생
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게 해 안
			CASE IS > 1
				PRINTFORMW 
			;초조하게 해 종료
			CASE 1
				PRINTFORMW 
			;초조하게 해 없음
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;발기
		IF TCVAR:MASTER:발기도 >= 1000
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT306, 승마 두 구멍 고문
;─
;·승마위중의 조교 대상으로 조수가 A삽입한다
;·ACT32, 승마위로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_306
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;ACT파생에의 대입은 TEQUIP를 참조해 행해지고 있다
;예를 들면 ACT파생 1등의 경우, 전회 ACT가 승마위라고는 할 수 없기 때문에 주의
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT306, 승마 두 구멍 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 질내로부터 뽑지않고에,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에 애널도 범하도록 지시했다…
	;=================================================
	;연속(승마위로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%가 삽입한 채로 체위를 승마위로 바꾸면,
	;%CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 실룩거리는 애널에 자신의 남성기를 주었다…
	;=================================================
	;체위 변경(V성교계로부터)			페니스밴드에서의 A성교중의 경우에게만 파생한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 삽입한\@ TEQUIP:조교자페니스밴드? 의욕형 # 페니스 \@를 애널로부터 뽑아 %CALLNAME:MASTER%에 승마위로 삽입하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에 애널도 범하도록 지시했다…
	;=================================================
	;A성교로부터						페니스밴드에서의 A성교중의 경우에게만 파생한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 자신에게 삽입한\@ TEQUIP:페니스밴드? 의욕형 # 페니스 \@를 뽑아, %CALLNAME:MASTER%에 승마위로 삽입하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에 애널도 범하도록 지시했다…
	;=================================================
	;V성교 봉사계로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 자신에게 삽입한\@ TEQUIP:페니스밴드? 의욕형 # 페니스 \@를 뽑아, %CALLNAME:MASTER%에 승마위로 삽입하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에 애널도 범하도록 지시했다…
	;=================================================
	;A성교 봉사로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%에 승마위로 삽입시키면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에 애널도 범하도록 지시했다…
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;─
;ACT307, 대면 두 구멍 고문
;─
;·조교자가 조교 대상의 V에 삽입한 채로, 조수가 조교 대상의 A에 삽입한다
;·ACT32, 대면좌위로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_307
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT307, 대면 두 구멍 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 질내에 삽입한 채로
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에 애널도 범하도록 지시했다…
	;=================================================
	;연속(대면좌위로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%가 삽입한 채로 체위를 대면좌위로 바꾸면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에 애널도 범하도록 지시했다…
	;=================================================
	;체위 변경(V성교계로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 삽입한\@ TEQUIP:조교자페니스밴드? 의욕형 # 페니스 \@를 애널로부터 뽑아 %CALLNAME:MASTER%에 대면좌위로 삽입하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에 애널도 범하도록 지시했다…
	;=================================================
	;A성교로부터						페니스밴드에서의 A성교중의 경우에게만 파생한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 자신에게 삽입한\@ TEQUIP:페니스밴드? 의욕형 # 페니스 \@를 뽑아 %CALLNAME:MASTER%에 대면좌위로 삽입하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에 애널도 범하도록 지시했다…
	;=================================================
	;V성교 봉사계로부터				v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 자신에게 삽입한\@ TEQUIP:페니스밴드? 의욕형 # 페니스 \@를 뽑아 %CALLNAME:MASTER%에 대면좌위로 삽입하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에 애널도 범하도록 지시했다…
	;=================================================
	;A성교 봉사로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%에 대면좌위로 삽입하면,
	;%CALLNAME:MASTER%의 엉덩이살을 열어 %CALLNAME:(ASSI:1)%에 애널도 범하도록 지시했다…
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;─
;ACT308, 변칙혼란모란
;─
;·조교 대상의 V에 배면좌위로 삽입하면서, 조수에 결합부의 상황을 실황시킨다
;·ACT33, 배면좌위로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_308
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT308, 변칙혼란모란
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 질내로부터 뽑지않고에 그대로 안으면 와
	;%CALLNAME:(ASSI:1)%에 결합부의 모습을 실황시켰다…
	;=================================================
	;연속(배면좌위로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%%CALLNAME:MASTER%를 뒤로부터 삽입한 채로 안아,
	;%CALLNAME:(ASSI:1)%에 결합부의 모습을 실황시켰다…
	;=================================================
	;체위 변경(V성교계로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 배면좌위로 관철하면 그대로 안아,
	;%CALLNAME:(ASSI:1)%에 결합부의 모습을 실황시켰다…
	;=================================================
	;A성교로부터						페니스밴드에서의 A성교중의 경우에게만 파생한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 배면좌위로 관철하면 그대로 안아,
	;%CALLNAME:(ASSI:1)%에 결합부의 모습을 실황시켰다…
	;=================================================
	;V성교 봉사계로부터				v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 배면좌위로 관철하면 그대로 안아,
	;%CALLNAME:(ASSI:1)%에 결합부의 모습을 실황시켰다…
	;=================================================
	;A성교 봉사로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 배면좌위로 관철하면 그대로 안아,
	;%CALLNAME:(ASSI:1)%에 결합부의 모습을 실황시켰다…
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT309, 애널 두 구멍 고문
;─
;·조교자가 조교 대상의 A에 삽입한 채로, 조수가 조교 대상의 V에 삽입한다
;·조교 대상이 남자의 경우는 발생하지 않는다
;·ACT33, 애널섹스로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_309
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT309, 애널 두 구멍 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음. 내용은 이하와 같다
;  %CALLNAME:TARGET%는 %CALLNAME:MASTER%를의 애널을 관철하면 그대로 안아,
;  %CALLNAME:(ASSI:1)%에 전부터도 범하도록 지시했다
;-------------------------------------------------
IF 구상기입체크(0)
	;지문은 분기 하지 않습니다만, ACT파생자체는 ACT33, 애널섹스와 같게 정의되고 있습니다
	;필요에 따라서 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT310, W역강간
;─
;·조교자와 조수로, 조교 대상의 페니스를 교대로 삽입하고 즐긴다
;·ACT103, 역강간으로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_310
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=V성교 봉사계로부터/3=A성교계로부터/4=V성교계로부터/5=A성교 봉사계로부터)
;·v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT310, W역강간
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;V성교 봉사로 질내사정 후, 계속해 실행된다고 하는 시추에이션
	;이 경우의 지문은, ACT파생 1와 같은 것이 된다
	;=================================================
	;뽑지않고에 속행
	;=================================================
	IF 구상기입체크(0) && TFLAG:뽑지않고
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:MASTER%의 위에서 격렬하게 허리를 흔들고 있던 %CALLNAME:TARGET%는 일단\@ TEQUIP:페니스밴드? 의욕형 # 페니스 \@를 해방하면
	;%CALLNAME:(ASSI:1)%에 삽입시키고 %CALLNAME:MASTER%의 남성기를 교대로 즐기기로 했다…
	;=================================================
	;연속(역강간으로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%과 연결된 채로 밀어넘어뜨린다와 %CALLNAME:(ASSI:1)%를 불러들여,
	;%CALLNAME:MASTER%의 남성기를 교대로 삽입하면서 즐겼다…
	;=================================================
	;체위 변경(V성교 봉사로부터)
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 밀어넘어뜨린다와 %CALLNAME:(ASSI:1)%를 불러들여,
	;%CALLNAME:MASTER%의 남성기를 교대로 삽입하면서 즐겼다…
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT311, 골짜기를건너감
;─
;·3 P의 일종
;·조교자와 조수가 상하과 겹쳐져, 조교 대상이 두 명의 바기나를 교대로 범한다
;·ACT95, 정상위시킨다로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_311
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=V성교 봉사계로부터/3=A성교계로부터/4=V성교계로부터/5=A성교 봉사계로부터)
;·v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT311, 골짜기를건너감
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음. 내용은 이하와 같다
;  %CALLNAME:TARGET%는 %CALLNAME:(ASSI:1)%과 상하서로 과 겹쳐져,
;  %CALLNAME:MASTER%에 두 명의 바기나를 교대로 범하도록(듯이) 명령했다…
;-------------------------------------------------
IF 구상기입체크(0)
	;V성교 봉사로 질내사정 후, 계속해 실행된다고 하는 시추에이션
	;=================================================
	;뽑지않고에 속행
	;=================================================
	IF 구상기입체크(0) && TFLAG:뽑지않고
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;지문은 분기 하지 않습니다만, ACT파생자체는 ACT95, 정상위시킨다와 같게 정의되고 있습니다
	;필요에 따라서 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT312, 2개빨고강제
;─
;·조교자와 조수의 페니스에 조교 대상이 봉사하는 플레이
;·이 단계에서는 명령만
;·ACT51, 입으로애무를강제로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K15_312
;구상 독자 내부 처리
CALL DECIDEACTCHECK_K15
;-------------------------------------------------
;·v1. 010 현재 전용의 지문이 없기 때문에, 파생원의 지문이 표시된다. 내용은 이하와 같다
;  %CALLNAME:TARGET%는 %CALLNAME:MASTER%에 자신의 남성기를 입으로 잡아당기라고 명령했다…
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT312, 2개빨고강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

