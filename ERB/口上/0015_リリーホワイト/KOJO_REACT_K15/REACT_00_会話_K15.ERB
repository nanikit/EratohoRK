;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[릴리 화이트]구상 회화계 개별 리액션 구상 파일
;
;	회화계 리액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 REACT0～9
;========================================================================
;·1231판보다 TFLAG:REACT부하가 도입되어 선택한 COM가 자연스러운 행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운 행동/1=무리가 없는 행동/2=무리를 한 행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;─
;REACT0, 지금의기분을얘기한다·개별 리액션
;─
;·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K15_0
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일 하고 싶다)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따른다 것으로, 리액션이빈정거린다 이외의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 질문에 분명히 답하는 것이 할 수 없었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 말씨 나쁘게 질문에 답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 나가려 한 말을 삼켜 대답을 흐렸다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「무─?   릴리에는…… 말하기 어려운 일입니까?」
			PRINTFORMW 「우우…… 무리하게, 라고는 말하지 않지만」
			PRINTFORMW 「역시 조금, 신경이 쓰여 버립니다」
		ELSE
			IF RAND:3 == 0
				PRINTFORMW 「왠지 불투명합니다아……」
				PRINTFORMW 「릴리는 작지만,%CNAME(MASTER)%의 기분, 분명하게 받아들여져요?」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「무슨 일입니까?   부끄러운 일, 입니까?」
				PRINTFORMW 「릴리도 무리하게는 (듣)묻지 않지만…… 무─……」
				PRINTFORMW 「%CNAME(MASTER)%는 상당한 부끄럼쟁이씨군요」
			ELSE
				PRINTFORMW 「응우, 들리지 않네요」
				PRINTFORMW 「좀 더%CNAME(MASTER)%의 솔직한 기분을 들려주세요」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게대답한다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 어려운 말을 퍼부었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 용기를 내 강하게 맞았다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 눈을 일등 해 입에서는 강하게 행동했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「아읏♪  이제(벌써), 깜짝 놀라서 끝냈어요」
			PRINTFORMW 「무─…… 이것은 릴리도 져 있을 수 없네요♪」
		ELSE
			PRINTFORMW 「끼양!   비, 깜짝 놀라 버렸습니다」
			PRINTFORMW 「이것이 『조교하는 보람이 있다』라는 것일까요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 여유의 미소로 어깨를 움츠리고 「지금의 기분은, 이라고 (들)묻어도」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 조금 생각하고 나서 「지금의 기분은, 이라고 (들)묻어도」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 간신히 야유 섞임에 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「이제(벌써), 괴롭히지 말아 주세요……」
			PRINTFORMW 「…… 혹시, 릴리의 일로부터인가 라고 있습니다?」
		ELSE
			PRINTFORMW 「후훗♪  릴리 알아 버렸습니다!」
			PRINTFORMW 「……%CNAME(MASTER)%는 좀 더  오·시·오·키  해 주었으면 하네요♪」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;-------------------------------------------------
	;소분기
	;-------------------------------------------------
	;첫회
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(200)
		;조교 대상으로부터의 호의 500이상
		IF FAVOR(500)
			PRINTFORMW 「응아우…… 릴리는 어떻게 받아 들이면 좋겠지요」
			PRINTFORMW 「그렇지만,%CNAME(MASTER)%의 기분을 (들)물을 수 있어, 릴리는 기쁩니다♪」
		;조교 대상으로부터의 호의 500 미만
		ELSE
			PRINTFORMW 「후훗♪  릴리 알아 버렸습니다!」
			PRINTFORMW 「……%CNAME(MASTER)%는 좀 더  오·시·오·키  해 주었으면 하네요♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;조교 대상으로부터의 호의 1000이상
	;첫회
	;=================================================
	IF 구상기입체크(100) && FAVOR(1000)
		;역강간으로 질내사정의 뒤
		IF TFLAG:전회질내사정 == 2
			PRINTFORMW 「앗, 하아읏♪  기쁩니다♪」
			PRINTFORMW 「%CNAME(MASTER)%의 발기 자지도, 릴리에 범해져 완전히 좋은 아이가 되었어요♪」
		;섹스로 질내사정의 뒤
		ELSEIF TFLAG:전회질내사정 == 1
			PRINTFORMW 「앗, 하아, 읏♪  에서도, 아직 멀었습니다♪」
			PRINTFORMW 「%CNAME(MASTER)%의 말로, 릴리 좀 더 흥분해 버렸으니까♪」
		;통상
		ELSE
			PRINTFORMW 「응우…… 그렇게 분명히 말해지면, 릴리도 부끄럽습니다」
			PRINTFORMW 「그렇지만, 매우 기쁩니다…… 정말이에요?   후후♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;조교 대상으로부터의 호의 500이상
	;첫회
	;=================================================
	IF 구상기입체크(100) && FAVOR(500)
		;역강간으로 질내사정의 뒤
		IF TFLAG:전회질내사정 == 2
			PRINTFORMW 「아하아♪  릴리의 보지, 기분이 좋네요♪」
			PRINTFORMW 「솔직한 자지는, 좀 더 좋은 아이 좋은 아이 해 주고 싶어져 버립니다♪」
		;섹스로 질내사정의 뒤
		ELSEIF TFLAG:전회질내사정 == 1
			PRINTFORMW 「응, 후아♪  솔직한 아이는 릴리 정말 좋아해요♪」
			PRINTFORMW 「좀 더 펄떡펄떡의 딱딱해♪  주인님에 오─이바지해 주세요♪」
		;통상
		ELSE
			PRINTFORMW 「너무 정면에서 말해지면 수줍어 버리네요…… 후훗♪」
			PRINTFORMW 「%CNAME(MASTER)%의 기분, 릴리는 매우 기쁩니다♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 함께 있어지는 것이 기쁘면 솔직하게 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 분명히 지금의 기분을 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 기분을 해치지 않게, 손질해 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			IF RAND:3 == 0
				PRINTFORMW 「…… 기쁩니다」
				PRINTFORMW 「%CNAME(MASTER)%의 말로, 릴리는……」
				PRINTFORMW 「우응, 릴리는%CNAME(MASTER)%와 함께, 둘이서 행복하게 되고 싶습니다」
				PRINTFORMW 「%CNAME(MASTER)%의 기분…… 말로, 신체로 연결되어, 좀 더 깊게 알고 싶구나」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「우우…… 뭔가 수줍어 버리네요」
				PRINTFORMW 「…… 릴리만, 간사합니다…… 읏」
				PRINTL  
				PRINTFORMDW 릴리는 부드러운 가슴으로%CALLNAME:MASTER%의 얼굴을 감싸는 것처럼 상냥하게 껴안았다……
				PRINTL  
				PRINTFORMW 「%CNAME(MASTER)%…… 좋아해요…… 너무 좋아……」
				PRINTFORMW 「…… 후훗, 귀까지 빨강……%CNAME(MASTER)%사랑스러운, 너무 좋아♪」
			ELSE
				PRINTFORMW 「%CNAME(MASTER)%가 기뻐해 주면, 릴리도 기쁩니다」
				PRINTFORMW 「좀더 좀더%CNAME(MASTER)%의 일 들려주세요」
				PRINTFORMW 「%CNAME(MASTER)%를 하고 싶은 것, 릴리로 해 주었으면 하는 것……」
				PRINTFORMW 「가득 가득, 릴리에 들려줘……」
			ENDIF
		ELSE
			PRINTFORMW 「%CNAME(MASTER)%솔직해져 주어 기쁩니다」
			PRINTFORMW 「릴리도……%CNAME(MASTER)%가싫어한 것은 하고 싶지 않으며」
			PRINTFORMW 「%CNAME(MASTER)%와 함께…… 기분 좋아지고 싶으니까♪」
			PRINTFORMW 「%CNAME(MASTER)%의 기분, 좀 더 릴리에 들려주세요♪」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 태연하게 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 4
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 그렇게 나쁘다고 생각하지는 않다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「그저 나쁘지는 않다」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 적극			시선을 피해, 나쁘지는 않아라고 중얼거렸다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 혐				힘껏의 허풍으로, 그저 나쁘지 않다고 대답했다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일 하고 싶다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들인다 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 5
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 좀 더 기분 좋아지고 싶다고 했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 기분 좋은 일 하고 싶은 기분이라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 아첨하도록(듯이) 감히 추잡한 행위를 졸랐다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------
[SKIPSTART]
통상의 경우 리액션의 지문은@TRAIN_MESSAGE2와@REACTION_MESSAGE로 처리됩니다만
이하의 부분은@TRAIN_MESSAGE2의 조건으로부터 빠지기 때문에(위해), @REACTION_MESSAGE에서만 처리됩니다
COM 분기에 있어서도 당연히 TFLAG:REACT파생는 기능하고, @REACTION_MESSAGE내에서도 REACT부하 마다 나누어 쓸 수 있습니다
SELECTCASE GET_COM_RANK()
	;자연스러운 행동
	CASE "자연스러운 행동"
		PRINTFORMW 
	;무리가 없는 행동
	CASE "무리가 없는 행동"
		PRINTFORMW 
	;무리를 한 행동
	CASE "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 싫어
		ELSE
			PRINTFORMW 
		ENDIF
ENDSELECT
상기와 같은 분기를 마련하는 일에 의해, 본체측의 변화에 대응하는 일은 가능합니다만
텐프레에 있어서 디폴트로 마련하려면 품이 많이 든다고 생각하는 까닭에, 기본적으로 REACT내의 COM 분기에는 테두리를 마련하지 않을 방침입니다
범용적인 내용으로 해도 좋아, 또 부분적으로 범용 리액션(COM)에 흘려도 좋다고 생각합니다
뭐 REACT부하 이외에도 확장하는 방법은 얼마든지 있거나 하므로, 그 근처의 판단은 각 구상 작자에게 맡기고 싶다고 생각합니다
3 BM/4 MB에서도 쓰기 때문에 어쨌든 생각되는 요소를 전부 넣어 줘! 적인 요망이 만약 있으면, 그 때는 또 생각하고 싶다와
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;지금의기분을얘기한다에서의 COM침묵한다는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어 벌포인트가 가산된다
;-------------------------------------------------
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	IF FAVOR(0, 2000)
		PRINTFORMW 「므우, 침묵은 좋지 않아요?」
		PRINTFORMW 「신경이 쓰여요……, 어떻게 하면 가르쳐 주겠습니까?」
		PRINTFORMW 「좀더 좀더,%CNAME(MASTER)%의 일 알고 싶어져 버렸습니다♪」
	ELSE
		PRINTFORMW 「릴리의 독심술의 전에서는, 입다물어도 쓸데없어요」
		PRINTFORMW 「무─, 흠흠…… 엣?   좀 더 격렬하게 괴롭혀, 입니까?   변태씨군요♪」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;벌포인트 가산 파생:「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		IF FAVOR(0, 2000)
			PRINTFORMW 「어떻게 하지요, 릴리도 곤란해 버려요……」
			PRINTFORMW 「그런 반응되면…… 흥분해 버리지 않습니까♪」
			PRINTFORMW 「%CNAME(MASTER)%도, 알고 있어 말하고 있습니다……♪」
		ELSE
			PRINTFORMW 「무─, 어떻게 할까요. 이제(벌써) 허락했으면 좋습니까?」
			PRINTFORMW 「%CNAME(MASTER)%의 기분은 알았습니다」
			PRINTFORMW 「…… 그렇지만, 릴리는 주인님이기 때문에♪  구별하고가 없는 아이에게는 벌 하지 않으면 안 되지요♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「아읏♪  야다야다는 아이같이 졸라대 버리며♪」
			PRINTFORMW 「%CNAME(MASTER)%사랑스러운…… 릴리군요, 좀 더 사랑스러운 모습 보고 싶어져 버렸습니다♪」
		ELSE
			PRINTFORMW 「허락했으면 좋습니까…… 후훗, 어떻게 하지요」
			PRINTFORMW 「왜냐하면, 그런 반응되면…… 릴리 흥분해 버리지 않습니까♪」
			PRINTFORMW 「저기, 야다야다는…… 좀 더 릴리에 졸라대 보여 주세요♪」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「그, 그렇게 무서워하지 않아도 괜찮지 않습니까」
			PRINTFORMW 「이봐요, 릴리예요?   ―…… 두렵지 않아요」
		ELSE
			PRINTFORMW 「이제(벌써) …… 릴리는 두렵지 않아요」
			PRINTFORMW 「혹시, 조교 너무 격렬했습니까?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT0, 지금의기분을얘기한다

;─
;REACT1, 자신에게로의기분을얘기한다·개별 리액션
;─
;·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K15_1
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다! /2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따른다 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 아무래도 싫다고는 말할 수 없었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「싫지 않은… 일지도 모른다…」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 본심을 숨기도록(듯이) 눈을 숙여, 「싫지 않은… 일지도 모른다…」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			IF RAND:2 == 0
				PRINTFORMW 「일지도 모르는…… 입니까」
				PRINTFORMW 「후훗,%CNAME(MASTER)%는 릴리를 기대시키는 것이 능숙하네요」
				PRINTFORMW 「저기%CNAME(MASTER)%…… 좀 더 릴리의 일 봐 주세요」
			ELSE
				PRINTFORMW 「그래, 싫지 않은…… 입니까」
				PRINTFORMW 「매우 기쁩니다…… 그렇지만, 릴리는 욕심쟁이이기 때문에……」
				PRINTFORMW 「좀더 좀더,%CNAME(MASTER)%의 기분을 갖고 싶어져 버렸습니다」
			ENDIF
		ELSE
			IF RAND:2 == 0
				PRINTFORMW 「싫지 않아…… 후훗, 릴리는 기대해도 될까요?」
				PRINTFORMW 「…… 저기%CNAME(MASTER)%」
				PRINTFORMW 「신체도 마음도…… 릴리만을 봐 주세요……」
			ELSE
				PRINTFORMW 「이제(벌써), 기분 맞추기라면 『좋아』라고 말하지 않으면 안됩니다?   후후♪」
				PRINTFORMW 「기쁩니다…… 사실이에요?」
				PRINTFORMW 「릴리…… 기대해도 좋지요?」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게대답한다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 「좋아하게 될 수 있을 이유가 없다!」라고 강하게 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「그런 일이 있을 이유 없다!」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 감정을 부정하도록(듯이) 고개를 저어 「싫다!」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「흐음…… 좋아요?   -개─에―」
			PRINTFORMW 「…… 릴리는 화나 있지 않아요?」
			PRINTFORMW 「조금, 기대하고 있었을 뿐입니다」
		ELSE
			PRINTFORMW 「므우…… 뭐 당연하네요」
			PRINTFORMW 「감금한 장본인을 좋아하게 될 수 있을 이유…… 없지요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 「좋아하게 될 수 있을 이유가 없다!」라고 강하게 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「그런 일이 있을 이유 없다!」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 감정을 부정하도록(듯이) 고개를 저어 「싫다!」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「야유를 말할 수 있다니 아직도 건강한 증거군요♪」
			PRINTFORMW 「…… 저기?   요정에 조교되고 기뻐하는 헤··타·이씨♪」
			PRINTFORMW 「앗!   그렇지 않으면, 혹시 벌의 졸라댐이었는가?」
		ELSE
			PRINTFORMW 「빈정거려도, 릴리는 따로 화내거나 하지 않아요?」
			PRINTFORMW 「…… 고집이 있는%CNAME(MASTER)%가, 좀 더 온순하게 될 수 있도록(듯이)」
			PRINTFORM 「조금, 격렬하게 시아니야도……
			CALL HEARTCB
			PRINTW 」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 적은지, REACT분류가 태연하게 따른다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 「좀 더 좋아하게 되고 싶기 때문에, 심한 것은 하지 말아줘」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「이제(벌써) 심한 일 하지 않으면, 좋아하게 될 수 있을지도 모른다」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 기분을 묻도록(듯이) 시선을 향한 후, 「심한 일만 하는 사람은 싫다」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			IF RAND:2 == 0
				PRINTFORMW 「아우…… 릴리, 너무 혹시 해 버렸습니까?」
				PRINTFORMW 「%CNAME(MASTER)%에 좋아한다고 말해 받을 수 있도록(듯이), 다음으로부터는 상냥하게 하네요」
			ELSE
				PRINTFORMW 「아우…… 릴리도 상냥하게 하고 싶지만, 가감(상태)가 어렵습니다……」
				PRINTFORMW 「왜냐하면%CNAME(MASTER)%도 참 로리콘씨의도M이고…… 므우」
				PRINTFORMW 「좀 더 상냥하게…… 릴리도 노력하네요」
			ENDIF
			ELSE
			IF RAND:2 == 0
				PRINTFORMW 「아우…… 릴리도 상냥하게 하고 싶지만, 가감(상태)가 어렵습니다……」
				PRINTFORMW 「왜냐하면%CNAME(MASTER)%도 참 로리콘씨의도M이고…… 므우」
				PRINTFORMW 「좀 더 상냥하게…… 릴리도 노력하네요」
			ELSE
				PRINTFORMW 「아우…… 미안해요  꽤 가감(상태)가 어렵습니다」
				PRINTFORMW 「상냥하게 하면, 좀 더 릴리의 일 좋아하게는 기미인가?」
				PRINTFORMW 「무무무…… 의지가 나왔습니다!   릴리, 노력하네요♪」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 1000이상
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 4
	;부하 2는 연주한 (분)편이 뭔가 형편이 좋은 것 같아 우선
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(4) && TFLAG:REACT부하 < 2
		PRINTFORMW 「좋아한다 라고…… 우우……」
		PRINTL  
		PRINTFORMDW %CALLNAME:MASTER%가 자신의 정직한 속마음을 전하면,
		PRINTFORMDW 릴리는 세키면 해, 개구한 채로 움직이지 못하게 되었다.
		PRINTL  
		;한 번 릴리의 입으로부터 좋아한다고 전하고 있다
		IF FAVOR(3000, 3000)
			PRINTFORMW 「…… 저, 저…… 엣또……」
			PRINTFORMW 「굉장히, 기뻐…… 릴리는, 매우, 매우 기쁩니다……」
			PRINTFORMW 「…… 기뻐서, 울어 버릴 정도로…… 릴리는, 매우 행복합니다」
			PRINTFORMW 「정말 좋아하는%CNAME(MASTER)%에…… 조, 좋아한다고 말해 받을 수 있어」
			PRINTL  
			PRINTFORMW 「릴리도…… 에헤헤♪  정말 좋아합니다♪」
			PRINTFORMW 「너무 좋아,%CNAME(MASTER)%……」
			PRINTL  
			PRINTFORMDW 꽃 피는 것 같은 웃는 얼굴에 쏘아 맞혀져, 시간이 멈춘다.
			PRINTFORMDW 릴리가 물기를 띤 눈동자안에, 자신의 모습이 흔들리고 있었다.
			PRINTFORMDW 그녀가 끔뻑 눈을 닫으면, 긴 속눈썹 속눈썹의 사이부터
			PRINTFORMDW 눈물이 뺨에 늘어져, 담홍색의 입술을 적셨다.
			PRINTFORMDW 그 눈물의 아토를 더듬도록(듯이), 끌어당길 수 있도록(듯이), 흰 백합의 꽃잎에 입맞춤을 했다.
			PRINTL  
			PRINTFORMDW 「읏……」
			PRINTFORMDW 「…… 후훗♪  반드시 오늘이, 릴리에 있어서의 백년째인 것이군요♪」
		;릴리는, 아직 생각을 전하지 않았다
		ELSEIF FAVOR(0, 2000)
			PRINTFORMW 「…… 저, 저…… 엣또……」
			PRINTFORMW 「굉장히, 기뻐…… 릴리는, 매우, 매우 기쁩니다……」
			PRINTFORMW 「…… 기뻐서, 울어 버릴 정도로…… 릴리는, 매우 행복합니다」
			PRINTFORMW 「정말 좋아하는%CNAME(MASTER)%에…… 조, 좋아한다고 말해 받을 수 있어」
			PRINTFORMW 「릴리도…… 에헤헤♪  정말 좋아합니다♪」
			PRINTL  
			PRINTFORMDW 아직 어림이 남는 웃는 얼굴에, 수치가 담홍색과 같이 찔러넣어
			PRINTFORMDW 릴리를 더욱 더 요염하게 빛낼 수 있다.
			PRINTL  
			PRINTFORMW 「릴리의 전부,%CNAME(MASTER)%에 주…… 네요♪」
			PRINTFORMW 「그러니까 릴리에도,%CNAME(MASTER)%를 전부 줘♪」
			PRINTFORM 「%CNAME(MASTER)%의 마음으로 정령으로…… 릴리를%CNAME(MASTER)%색에 물들여 주세요
			CALL HEARTCB
			PRINTW 」
		ELSE
			PRINTFORMW 「엣또…… 기쁩니다……♪」
			PRINTFORMW 「%CNAME(MASTER)%가 릴리의 일을…… 에헤헷♪」
			PRINTFORMW 「릴리도%CNAME(MASTER)%의 일 정말 좋아해요♪」
			PRINTL  
			PRINTFORM 「기분 좋은 봐 도미에게 라고 토정 하고 있는 때라든지, 최고로 사랑스러워서
			CALL HEARTCB
			PRINTW 」
			PRINTFORM 「이봐요 이봐요♪  다─있고 수기인 릴리가, 가득 기분 좋게 시켜 종류군요―
			CALL HEARTCB
			PRINTW 」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;역강간으로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
		PRINTFORMW 「에헤헷♪  감사합니다♪」
		PRINTFORMW 「그렇지만, 여자 아이에게 범해지면서 그런 일 말해도 설득력 없어요?」
		PRINTFORMW 「믿기를 원했으면♪  응♪  좀더 좀더 릴리를 만족시켜 주세요♪」
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;섹스로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(0) && TFLAG:전회질내사정 == 1
		PRINTFORMW 
		PRINTFORMW 
		RETURN 1
	ENDIF

	;자연스러운 행동
	;=================================================
	;REACT부하 0, 붙임성 좋게 웃어 정말 좋아한다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 분명히 좋아라고 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 억지웃음을 띄워, 좋아해라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(3000, 3000)
			IF RAND:3 == 0
				PRINTFORMW 「릴리를 좋아합니까―…… 후훗♪」
				PRINTL  
				PRINTFORMD 릴리는%CALLNAME:MASTER%의 근처에 걸터앉아,
				IF RAND:2 == 0
					PRINTFORMDW 몸을 의지해 왔다.
				ELSE
					PRINTFORMDW %CALLNAME:MASTER%의 몸을 살그머니 껴안았다.
				ENDIF
				PRINTK  
				PRINTFORMW 「그러면 릴리가 응석부려 버리거나 해도, 좋지요♪」
				PRINTL  
				PRINTFORMDW 속눈썹이 접촉할 것 같은 거리로, 릴리와 시선을 관련된다.
				PRINTFORMDW 한숨으로 입술을 간지러워질 때에, 두근두근 고동이 크게 울려 간다……
				PRINTFORMDW 접할 수 있지 않았는데, 서서히 저리는 것 같은 쾌감이 하반신을 지배한다……
				PRINTL  
				PRINTFORMW 「저항의 방법, 잊어 버렸어?   으응, 받아들여 버린 거네요♪」
				PRINTFORM 「이제(벌써) 완전히, 몸도 마음도…… 릴리의 연인씨…… 구나
				CALL HEARTCB
				PRINTW 」
			ELSEIF RAND:2 == 0
				PRINTFORM 「기쁩니다
				CALL HEARTCB
				PRINTW   릴리도 말야,%CNAME(MASTER)%를 아주 좋아해요♪」
				PRINTFORMW 「정말 좋아하기 때문에,%CNAME(MASTER)%의 전부를 갖고 싶어져 버립니다……」
				PRINTFORMW 「릴리의 저기, 매우 기분 좋았던 것이지요?」
				PRINTFORMW 「중요한 곳에서 깊게 연결되어, 움찔움찔은 해…… 릴리도 굉장히 기분 좋았어」
				PRINTFORMW 「…… 그래서 말야, 저…… 엣치한 아이는, 싫게 되지 말아 주세요?」
				PRINTL  
				PRINTFORMDW 릴리는 수치에 뺨을 물들여, 머뭇머뭇이라고 말하기 어려운 듯이 시선을 유영하게 한다……
				PRINTL  
				PRINTFORMW 「…… 좋아하기 때문에……」
				PRINTFORMW 「서로 사모함이라면…… 좀 더 기분 좋고, 될 수 있는 것일까 하고, 생각해……」
				PRINTFORMW 「에 녹아, 서로 섞여, 두 명이 1개가 될 정도로…… 좀 더%CNAME(MASTER)%와……」
			ELSE
				PRINTFORMW 「…… 기쁘구나」
				PRINTFORMW 「그렇지만…… 저, 정말로 릴리로 좋습니까? 　%CNAME(MASTER)%에 가득 심한 일 했는데?」
				PRINTL  
				PRINTFORMW 「…… 감사합니다…… 에헤헷♪」
				PRINTFORM 「%CNAME(MASTER)%의 몸도 마음도…… 좀더 좀더, 릴리에 열중하게 시켜 준다
				CALL HEARTCB
				PRINTW 」
				
			ENDIF
		ELSEIF FAVOR(0, 2000)
			IF RAND:3 == 0
				PRINTFORMW 「앗……」
				PRINTL  
				PRINTFORMW 「에헤헷…… 깜짝 놀라 버렸습니다」
				PRINTFORMW 「릴리,%CNAME(MASTER)%에 심한 일…… 가득 해 버렸기 때문에」
				PRINTFORMW 「릴리도 말야,%CNAME(MASTER)%를 좋아해……」
				PRINTFORMW 「그렇지만…… 같은 정도,%CNAME(MASTER)%가 흐트러지는 모습도 좋아해……」
				PRINTFORMW 「좋아하기 때문에, 소중히 하고 싶은데…… 기분이 멈출 수 없어서……」
				PRINTL  
				PRINTFORMW 「저기…… 계속되어, 하자?」
				PRINTFORMW 「릴리군요…… 좀더 좀더,%CNAME(MASTER)%와 연결되고 싶은거야……」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「아, 안됩니다……」
				PRINTFORMW 「릴리는,%CNAME(MASTER)%에 심한 일 가득 해……」
				PRINTFORMW 「%CNAME(MASTER)%가 기분 좋은 것은, 그러한 (분)편이 정령이 많이 받을 수 있기 때문으로……」
				PRINTFORMW 「그러니까…… 그…… 그런 릴리에서도……」
				PRINTFORMW 「%CNAME(MASTER)%의 『좋아』는 기분에…… 릴리가 응해도 괜찮을까요……」
				PRINTFORM 「…… 그렇지만, 좋아하게 되어 버렸다면…… 후훗♪  어쩔 수 없지요
				CALL HEARTCB
				PRINTW 」
			ELSE
				PRINTFORMW 「사랑은 맹목이라고 합니다만……」
				PRINTFORMW 「릴리,%CNAME(MASTER)%를 가득 손상시켜…… 지금이라도 그렇습니다」
				PRINTFORMW 「%CNAME(MASTER)%를 가두어, 엣치한 조교를 하는…… 나쁜 요정씨이랍니다?」
				PRINTFORMW 「…… 쾌락에 져 버렸다…… 라든지?」
				PRINTFORMW 「…… 후훗, 아하핫♪」
				PRINTFORMW 「미안해요, 조금 심술쟁이 해 버렸어요♪」
				PRINTFORM 「…… 기쁩니다, 릴리도 정말 좋아해요
				CALL HEARTCB
				PRINTW 」
			ENDIF
		ELSE
			PRINTFORMW 「『좋아』입니까…… 에헤헷♪」
			PRINTFORMW 「릴리에도, 마침내 봄이 와 버렸습니까?」
			PRINTFORMW 「…… 주인님과 노예의 신분 차이의 사랑……」
			PRINTFORMW 「어슴푸레한 방에서, 서로의 정령을 서로 교환할 뿐(만큼) 의…… 청순하다고는 먼, 짓무른 관계인데……」
			PRINTFORMW 「…… 만약,%CNAME(MASTER)%와 지금과는 다른 만나는 방법을 하고 있으면……」
			PRINTFORMW 「…………」
			PRINTFORMW 「…… 이라니♪  감사합니다, 포상, 주어 버리네요♪」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들인다 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 5
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(5)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;소분기
	;-------------------------------------------------
	;역강간으로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
		;주인님 임신중!!
		IF TALENT:TARGET:임신 == 1
			PRINTFORM 「팥고물♪  보테배섹스보다 과격한 일은… 후훗
			CALL HEARTCB
			PRINTFORMW 」
			PRINTFORM 「좋아요
			CALL HEARTCB
			PRINTFORMW 　%CNAME(MASTER)%의 끝없는 성욕도, 릴리가 전부 받아 들여 주네요♪」
		;임신하고 있지 않아!
		ELSE
			PRINTFORMW 「팥고물♪  도, 좀 더입니까?」
			PRINTFORM 「…… 이것 이상--되면, 릴리 어머니가 되어 버려요
			CALL HEARTCB
			PRINTW 」
			PRINTFORMW 「그렇지만,%CNAME(MASTER)%의 끝없는 성욕도, 릴리가 전부 받아 들여 줍니닷♪」
			PRINTFORMW 「%CNAME(MASTER)%가 그 기분이라면…… 임신할 때까지, 아기의 방에 사정해 봅니까?」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;섹스로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
		PRINTFORMW 「조, 좀 더 굉장한 일…… 입니까?」
		PRINTFORMW 「우─…… 릴리에 어떤 헨타이프레이를 시킬 생각입니까!」
		RETURN 1
	ENDIF
	
	;-------------------------------------------------
	;조교 대상에의 호감도 1000이상
	;=================================================
	IF 구상기입체크(60) && FAVOR(0, 1000)
		PRINTFORM 「이제(벌써),%CNAME(MASTER)%는 릴리에 지지 않을 정도 욕심쟁이씨군요
		CALL HEARTCB
		PRINTW 」
		PRINTFORMW 「……%CNAME(MASTER)%는 이제(벌써) 상냥한 엣치하지 만족할 수 없다……」
		PRINTFORMW 「…… 머리가 저릴 정도로 격렬하게 하지 않으면 만족할 수 없는거야…… 후훗♪」
		PRINTFORM 「그러면, 릴리와 좀 더 굉장한 일…… 시있어요
		CALL HEARTCB
		PRINTW 」
		RETURN 1
	ENDIF

	;자연스러운 행동
	;=================================================
	;REACT부하 0, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」라고 대답했다
	;=================================================
	IF 구상기입체크(60) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 「저기…… 굉장한 일은, 릴리에 어떤 헨타이프레이를 시킬 생각입니까?」
		PRINTFORMW 「분명하게 말해 주지 않으면, 릴리 몰라요?   후후♪」
		PRINTFORM 「이봐요♪  졸·라·댐
		CALL HEARTCB
		PRINTW   해 봐 주세요♪」
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「좀 더 좋아하게 되도록(듯이), 좀 더 귀여워했으면 좋겠다」라고 대답했다
	;=================================================
	IF 구상기입체크(60) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 「아읏♪  착정 되는 것 버릇이 되어 버렸습니까?」
		PRINTFORM 「왜냐하면 릴리가%CNAME(MASTER)%를 귀여워한다 라고 하는 것은……『그런 일』이지요
		CALL HEARTCB
		PRINTFORMW 」
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 「좋아하게 될 수 있을 정도로, 굉장한 일 해 주면 된다」라고 대답했다
	;=================================================
	IF 구상기입체크(60) && GET_COM_RANK() == "무리를 한 행동"
		IF RAND:2 == 0
			PRINTFORMW 「그렇게 『굉장한 일』하면, 릴리를 좋아하게 되기 전에 망가져 버릴지도…… 키득♪」
			PRINTFORM 「그렇지만%CNAME(MASTER)%의 부탁이라면 어쩔 수 없지요
			CALL HEARTCB
			PRINTFORMW 」
		ELSE
			PRINTFORMW 「굉장한 일은, 릴리에 어떤 헨타이프레이를 시킬 생각인 것이지요……」
			PRINTFORMW 「응─…… 표본으로 하고 싶기 때문에 릴리에 『굉장한 일』해 줍니까?」
			PRINTFORM 「후훗♪  는, 농담이에요
			CALL HEARTCB
			PRINTFORMW 」
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「아읏♪  착정 되는 것 버릇이 되어 버렸습니까?」
		PRINTFORMW 「모처럼 릴리가 상냥하게 하고 있는데 만족할 수 없다니…… 안 되는 아이군요♪」
		PRINTFORMW 　
		PRINTFORMW 「저기…… 굉장한 일은, 릴리에 어떤 헨타이프레이를 시킬 생각입니까?」
		PRINTFORMW 「분명하게 말해 주지 않으면, 릴리 몰라요?   후후♪」
		PRINTFORM 「이봐요♪  졸·라·댐
		CALL HEARTCB
		PRINTW   해 봐 주세요♪」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자신에게로의기분을얘기한다의 경우는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어 벌포인트가 가산된다
;-------------------------------------------------
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	IF FAVOR(0, 2000)
		PRINTFORMW 「…… 흠흠, 『릴리의 일을 아주 좋아하다』 『그러니까 좀 더 격렬하게 해』?」
		PRINTFORMW 「헤─,%CNAME(MASTER)%는 역시 변태씨이군요♪」
		PRINTFORMW 「…… 엣?   부끄럼쟁이인%CNAME(MASTER)%의 기분을 대변 해 준 것이에요? …… 후훗♪」
		PRINTFORMW 「이번은%CNAME(MASTER)%로부터 직접 (들)물을 수 있도록(듯이), 릴리 노력해 버리네요♪」
	ELSE
		PRINTFORMW 「여보세요?   입다물고 있으면 몰라요?」
		PRINTFORMW 「혹시…… 매우 추잡한 망상으로, 릴리를 터무니없게 범해 있거나 하는 것입니까?」
		PRINTFORMW 「후훗…… 엣치하네요♪　%CNAME(MASTER)%의 변태씨♪」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;벌포인트 가산 파생:「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1 ;의도가 알지 않기 때문에 헤맨 것입니다만, 우선 ON로 해 두었습니다@/L
		IF FAVOR(0, 2000)
			PRINTFORMW 「허락했으면 좋습니까?」
			PRINTFORMW 「이야이야궯궲 해도 안됩니다♪」
			PRINTFORMW 「이봐요, 릴리의 일을 어떻게 생각하고 있는지 말해 봐 주세요♪」
		ELSE
			PRINTFORMW 「그렇게 말하고 싶지 않습니까?」
			PRINTFORMW 「…… 몸도 마음도 릴리의 것이 되면, 지금 보다 더 행복하게 될 수 있는데……」
			PRINTFORMW 「인간은, 어려운 생물이군요……」
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「허락해는, 전혀 릴리가 괴롭히고 있는 것 같지 않습니까」
			PRINTFORMW 「『같지 않아서 괴롭히고 있지 않은가』는?」
			PRINTFORMW 「그런 말투 하면, 정말로 괴롭혀 버려요♪」
		ELSE
			PRINTFORMW 「거, 거기까지 말하고 싶지 않습니까?」
			PRINTFORMW 「우우…… 조금 쇼크입니다……」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		IF FAVOR(0, 2000)
			PRINTFORMW 「그렇게 무서워하지 말고?」
			PRINTFORMW 「그렇닷, 즐거운 일 생각해 봅시다!」
			PRINTFORMW 「%CNAME(MASTER)%가 좋아하는 일이라든지, 기분이 좋은 일……」
			PRINTFORMW 「릴리에 상냥하게 꾸짖을 수 있는 상상…… 해 봅시다?」
		ELSE
			PRINTFORMW 「우우…… 명백하게 두려워해지면…… 조금 쇼크입니다……」
			PRINTFORMW 「지금의 릴리…… 그렇게 무섭습니까?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT1, 자신에게로의기분을얘기한다

;─
;REACT2, 성에대한이야기를한다·개별 리액션
;─
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K15_2
;-------------------------------------------------
;  파생할 가능성이 있는 COM는 우선 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는 방법도 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;벌포인트 가산 파생:「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게해줘
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋게해줘")
	;=================================================
	;벌포인트 가산 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;TEQUIP에 의해 쾌SOURCE가 들어가 있는 경우에 파생
	;=================================================
	;REACT파생 1, 쾌락에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 상황에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("자위하기시작한다")
	;=================================================
	;REACT파생 1, 「마음대로 자위 하지마」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「마음대로 자위 하고 있지만 좋은가」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT2, 성에대한이야기를한다

;─
;REACT3, 상냥하게위로한다·개별 리액션
;─
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K15_3
;-------------------------------------------------
;  파생할 가능성이 있는 COM에는 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는 방법도 있습니다
;-------------------------------------------------
;  우선 회화계 COM를 모두 망라되어 있습니다만
;  10 접수대의 애무계 COM, 50 접수대 이후의 그 외 COM등도 상황에 따라서는 선택 가능하게 됩니다
;  필요에 따라서 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
	IF FAVOR(3000, 3000)
		PRINTFORMW 「입다물어 버려 무슨 일이야?   부끄러운 것일까?」
		PRINTFORMW 「그런 일 신경쓰지 않아도 괜찮은데…… 키득♪」
	ELSEIF FAVOR(0, 2000)
		PRINTFORMW 「무─, 조금 쇼크입니다……」
		PRINTFORMW 「좀 더 솔직해져…… 응석부려도 괜찮아요?」
	ELSE
		PRINTFORMW 「뭔가 말해 주세요, 외롭지 않습니까……」
		PRINTFORMW 「저기, 좀 더 솔직해집시다?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무뚝뚝하게대답한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성 있게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("붙임성좋게대답한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("강하게대답한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더하드하게
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("더하드하게")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게해줘
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋게해줘")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT3, 상냥하게위로한다

;─
;REACT4, 엄격하게위협한다·개별 리액션
;─
;·TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만
;-------------------------------------------------
@KOJO_REACT_K15_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(3)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT4, 엄격하게위협한다

;─
;REACT5, 의상의변경을요구한다·개별 리액션
;─
;·조교 대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가 "소극적 따른다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가 "적극적 따른다", "태연하게 따른다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
@KOJO_REACT_K15_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;=================================================
	;한 장 벗었다(소극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("소극적 따른다/받아들인다")
		;-------------------------------------------------
		;마지막 한 장을 벗어전라가 되었다
		;-------------------------------------------------
		IF 구상기입체크(0) && (TEQUIP:MASTER:악세사리 + TEQUIP:MASTER:양말 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신윗도리 + TEQUIP:MASTER:상반신윗도리 + TEQUIP:MASTER:전신윗도리) == 0
			;REACT분류받아들인다			전라가 되어, 기대와 흥분으로 피부가 달아오르는 것을 느꼈다
			IF IS_COMGRONAME("받아들인다")
				PRINTFORMW 
			;REACT분류소극적 따른다			어쩔 수 없이 마지막 한 장을 벗어, 전라가 되었다
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;완만한 움직임으로 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;어쩔 수 없이 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_COMGRONAME("소극적 따른다")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;상층의 분기를 모두 묻었을 경우는 불필요한 개소
		;반대로 묻지 않는 경우는 이 개소가 보완이 책임을 다합니다
		;-------------------------------------------------
		;그 외·조건 없음
		;-------------------------------------------------
		IF 구상기입체크(100)
			IF FAVOR(0, 2000)
				IF RAND:2 == 0
					PRINTFORMW 「부끄러운 것 같은%CNAME(MASTER)%의 표정…… 오슬오슬 해 버립니다♪」
					PRINTFORM 「……%CNAME(MASTER)%의 엣치한 신체, 릴리에 좀 더 잘 보여 주세요
					CALL HEARTCB
					PRINTFORMW 」
				ELSE
					PRINTFORMW 「릴리에 알몸을 보여지는 것이 부끄럽습니까?」
					PRINTFORMW 「…… 다른군요?」
					PRINTFORMW 「『보여지는 것』이 아니고 『보여지고 느껴 버리는 것』이 부끄러운거야」
					PRINTFORM 「노출광의 재능까지 있다니 정말로 변태씨이군요
					CALL HEARTCB
					PRINTFORMW 」
				ENDIF
			ELSE
				IF RAND:2 == 0
					PRINTFORMW 「그렇게 초조해 해지면, 릴리도 흥분해 버립니다♪」
					PRINTFORMW 「이봐요, 계속해 주세요…… 부끄러운 곳, 릴리가 전부 봐 줄테니까」
				ELSE
					PRINTFORMW 「얼굴 빨강인가군요…… 후훗♪」
					PRINTFORMW 「좀 더 부끄러운 것 같은 얼굴, 릴리에 보였으면 좋구나」
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;2매 벗었다(적극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("적극적 따른다/태연하게 따른다")
		;-------------------------------------------------
		;전라가 되었다
		;-------------------------------------------------
		IF 구상기입체크(0) && (TEQUIP:MASTER:악세사리 + TEQUIP:MASTER:양말 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신윗도리 + TEQUIP:MASTER:상반신윗도리 + TEQUIP:MASTER:전신윗도리) == 0
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;적극적으로 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_COMGRONAME("적극적 따른다")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;솜씨 좋게 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_COMGRONAME("태연하게 따른다")
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;상층의 분기를 모두 묻었을 경우는 불필요한 개소
		;반대로 묻지 않는 경우는 이 개소가 보완이 책임을 다합니다
		;-------------------------------------------------
		;그 외·조건 없음
		;-------------------------------------------------
		IF 구상기입체크(100)
			IF FAVOR(0, 2000)
				IF RAND:2 == 0
					PRINTFORMW 「또 스으스으 합니까?   후후♪」
					PRINTFORMW 「와♪  릴리가 따뜻하게 해 줍니다♪」
				ELSE
					PRINTFORMW 「힘차게 벗어 버려, 완전히 릴리의 말하는대로군요♪」
					PRINTFORMW 「좀 더 수줍음이 있던 (분)편이 흥분합니다만…… 후훗♪」
				ENDIF
			ELSE
				PRINTFORMW 「후훗♪  그렇게 초조해 해 벗지 않아도 괜찮아요」
				PRINTFORMW 「좀 더 천천히, 두 명의 시간을 즐깁시다♪」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;벗지 않았다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("거부")
		;부끄러워서 벗겨지지 않았다
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
			IF FAVOR(0, 2000)
				PRINTFORMW 「입은 채로 엣치하고 싶습니까?」
				PRINTFORMW 「그렇지 않으면, 릴리의 손으로 벗게 했으면 좋은…… 이라든지?」
			ELSE
				PRINTFORMW 「릴리가 돕지 않으면, 갈아입음도 혼자서 할 수 없습니까?」
				PRINTFORMW 「그 쪽이, 상당히 부끄럽다고 생각합니다만……」
			ENDIF
		;싫어해 벗지 않았다
		ELSE
			IF FAVOR(0, 2000)
				PRINTFORMW 「릴리에 무리하게 벗겨지고 싶은 것인지?   후후♪」
				PRINTFORMW 「좋아요♪  에서도, 분명하게 저항해, 부끄러워해 주세요?」
				PRINTFORM 「그 쪽이…… 흥분해 버릴테니까
				CALL HEARTCB
				PRINTFORMW 」
			ELSE
				IF RAND:2 == 0
					PRINTFORMW 「얼굴 새빨갛게 해 버려, 사랑스럽닷♪」
					PRINTFORMW 「%CNAME(MASTER)%의부끄러워한다 얼굴, 좀 더 보고 싶어져 버렸습니다」
				ELSE
					PRINTFORMW 「무─, 부끄러워서 벗겨지지 않습니까……」
					PRINTFORMW 「그렇지만, 이대로 하면 양복주름이 되어 버려요?」
				ENDIF
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;한 장 입었다(소극적 반응)
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("소극적 따른다/받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;2매 입었다(적극적 반응)
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적 따른다/태연하게 따른다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;지문에서는 "싫어해"라고 하는 취급
	;=================================================
	;입지 않았다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("거부")
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT5, 의상의변경을요구한다

;─
;REACT6, 합의를요구한다·개별 리액션
;─
;·조교 대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
@KOJO_REACT_K15_6
;-------------------------------------------------
;수고를 싫어하지 않는 것이면, TALENT:MASTER:남자등으로 내부에서 나누는 것도 있을지도 모릅니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따른다 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("소극적 따른다")
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 각오를 결정한 것처럼, 작은 소리로 승낙했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 0, 작은 소리로 승낙했다	
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 적극			부끄러움에 스러질 것 같은 소리로 승낙했다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 혐				분함에 얼굴을 비뚤어지게 하면서도 승낙했다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 마음대로 하면 좋다고 대답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 태연하게 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("태연하게 따른다")
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 별로 싫지 않다고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 마음대로 하면 좋다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 적극			하고 싶으면 좋다고 말했다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 혐				마음대로 하면 좋다고 대답했다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("적극적 따른다")
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 처음을 줄 수 있어 기쁘다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 뜻을 정해 바쳐도 괜찮다고 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 이제 지킬 수 없다면 적어도 상냥하게 여겨지도록(듯이)와 동의 했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게해줘 주는 것이라면 처음을 주어도 괜찮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들인다 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 처음을 바치기 때문에기분좋게해줘 갖고 싶다고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 기분좋게해줘 주는 것이라면 처음을 주어도 괜찮다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 처음을 바치라고 한다면, 괴로움을 잊을 수 있을 정도로기분좋게해줘라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서 "거부"에 세트 된다)
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("거부")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(4)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT6, 합의를요구한다

;─
;REACT7, 벌이라고선언한다·개별 리액션
;─
;·선언해도 벌이 발생한다고는 할 수 없다
;-------------------------------------------------
@KOJO_REACT_K15_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM"여리게대답한다"는 일정 조건으로 REACT분류"용서를빈다"에 파생한다
;-------------------------------------------------
IF 구상기입체크(0) && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더하드하게
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("더하드하게")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다") 
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(7)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다") && TFLAG:REACT부하 < 2
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;기입 체크
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT7, 벌이라고선언한다

;─
;REACT8, 고함친다·개별 리액션
;─
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K15_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT8, 고함친다

;─
;REACT9, 기분나쁘게웃는다·개별 리액션
;─
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K15_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT9, 기분나쁘게웃는다


