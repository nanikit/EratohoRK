;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	에이린 구상 조교자 행동 전이벤트 구상 파일
;
;	조교자 행동 전이벤트 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV12, 조교자 행동 서두
;========================================================================
;·TRAIN_PROCCES.ERB내, @TRAIN_CANCEL로부터 불려 간다
;·구상주의 파라미터 표시의 직후에 표시. 조교 지시는 이미 결정되고 있다
;·조교중지선언의 교환이나, 조교 턴 개시전에 주인님으로부터 한 마디, 와 같이 쓰는 일도 가능
;·오전, 오후의 조교 개시는 아니고, 조교 턴의 개시전에는 매회 불려 가는 개소
;·그 때문에도 있어, 조교중지시와 경과시간 0이외의 개소는 IF 구상 기입 체크(25) 등으로서 확률 표시하는 일을 추천
;·조교중지시에게만 지문이 존재한다
;·문장 표시 후에 본체측의 DRAWLINE 처리 있어
;-------------------------------------------------
;FIRSTEVENT(0)～나, FIRSTEVENT(10)～등으로 해서 테두리마다 할당하고 있습니다만
;FIRSTEVENT/ONCEEVENT/RANDEVENT/ORDEREVENT는 사실상 무한하게 이벤트번호를 늘릴 수 있으므로
;필요에 따라서 임의에 할당해 주세요
;-------------------------------------------------
@KOJO_EVENT_K29_12(ARG)
;-------------------------------------------------
;인수 내역…
;ARG(0=조교가능/1=구상주의 피로로 중지/2=조교 대상의 피로로 중지/3= 포상에 의한 중지)
;-------------------------------------------------

;─
;조교중지 ARG:1 ARG:2 ARG:3 FIRSTEVENT(0)～
;─
;·피로, 혹은 포상에 의한 조교중지의 경우의 구상
;·기입 체크=1의 경우는 구상 표시 후 본체측에서 PRINTL의 공행이 들어간다
;·기입 체크를=2로 하면, 지문을 표시하지 않는다
;·이라고 할까 RESULT > 2로 판정되므로 2이상이라면 같은 거동이 됩니다만 일단 비추천이라고 하는 일로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;포상에 의한 중지
;RESULT치(1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 3
	;기입 체크를 2 이하로 했을 경우는 이하의 지문이 표시된다. 표시 타이밍은 구상 표시의 뒤
	;%CALLNAME:TARGET%는 오늘의 조교를 중지해,
	;%CALLNAME:MASTER%멈추거나로 한 시간을 보냈다…
	PRINTFORMW 
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자의 피로에 의한 중지
;RESULT치(1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && ARG == 1
	;기입 체크를 2 이하로 했을 경우는 이하의 지문이 표시된다. 표시 타이밍은 구상 표시의 뒤
	;조교의 시간이 왔지만, 들어 온 %CALLNAME:TARGET%는 어쩐지 피곤한 모습으로,
	;이번은 휴일이라고 말해 나갔다…
	PRINTFORMW 「오늘은 조금 지쳐 있어」
	PRINTFORMW 「나도 아직도 갈 수 있다고 생각했지만…… 나이일까」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상의 피로에 의한 중지
;RESULT치(1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 2
	;기입 체크를 2 이하로 했을 경우는 이하의 지문이 표시된다. 표시 타이밍은 구상 표시의 뒤
	;조교의 시간이 왔지만, %CALLNAME:TARGET%는 심한 모습의 %CALLNAME:MASTER%를 봐 당분간 생각하면,
	;이번은 쉬어도 좋다고 말해 나갔다…
	PRINTFORMW 
	RETURN 2
ENDIF

;-------------------------------------------------
;·이하, ARG:0(조교가능) 상당
;·TRAIN_PROCCES.ERB내, @BEFORE_ACT로부터 불려 간다
;·이 시점에서 ACT는 벌써 결정하고 있지만 ACT파생의 결정은 이루어지지 않았다
;·NOWEX의 리셋트는 되지 않았다
;·이것들의 개소를 빠진 뒤로 ACT 개시가 된다
;·분기의 증감은 자유롭게 할 수 있습니다만, 우선도에는 주의합시다
;-------------------------------------------------

;─
;조교 개시 직후 FIRSTEVENT(10)～
;─
;·경과시간이 0인 경우에 표시된다
;·그 회의 조교 개시 직후에 한 번만으로, 이후 그 조교중에는 표시되지 않는다
;·이 개소를 빠진 뒤로 ACT 개시가 된다
;-------------------------------------------------
;  여기서 IS_POLICY("오늘", "벌") 등으로오늘의방침를 말하게 하는 일도 가능했다거나 합니다만
;  EVENT17, 오늘의 방침 구상과 입을 가능성이 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 개시 직후
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && !TFLAG:경과시간
	;=================================================
	;조교자가 조금 피곤함
	;=================================================
	IF 구상기입체크() && CFLAG:소모 > 2
		PRINTFORMW 「하아…… 자, 해요……」
		RETURN 1
	ENDIF
	
	;ARG:3의 분기를 썼을 경우는 불필요한 개소
	;=================================================
	;포상에 의한 조교중지의 경우
	;=================================================
	IF 구상기입체크(0) && IS_POLICY("오늘", "휴게")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ARG:1의 분기를 썼을 경우는 불필요한 개소
	;=================================================
	;조교 대상의 피로에 의한 중지
	;=================================================
	IF 구상기입체크(0) && CFLAG:MASTER:소모 > 5
		PRINTFORMW 「피로가 보여요. 오늘은 쉬세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 조금 피곤함
	;=================================================
	IF 구상기입체크() && CFLAG:MASTER:소모 > 2
		PRINTFORMW 「조금 피곤한 것 같지만…… 괜찮아요」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상에의 호감도 1000이상
	;=================================================
	IF 구상기입체크(0) && AFFECTION("권애")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「건강한 것 같구나. 시작합시다」
		RETURN 1
	ENDIF
ENDIF

;─
;벌FIRSTEVENT(20)～
;─
;·TFLAG:벌플래그가 서 있는 경우에 표시되는 개소
;·벌플래그가 서 있어도, 가학계 이외의 ACT가 되는 경우가 있다
;-------------------------------------------------
;가학계 ACT, 파생하는 일에 의해 가학계에 포함될 가능성이 있는 이라마치오
;의상의변경을요구한다/기분나쁘게웃는다/애널바이브/후배위/애널섹스/자위/야외플레이/방뇨/다리빨고강제
;이상계 ACT/성교 봉사계 ACT
;상기의 ACT 이외는 TFLAG:벌플래그에 의해 ACT_ABLE로 규제된다(0616판 시점)
;-------------------------------------------------
;-------------------------------------------------
;·1회의 조교로 1회만 표시된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;벌개시 
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:벌플래그 && ONCEEVENT(20)
	;=================================================
	;전회 ACT가 벌선언
	;=================================================
	IF 구상기입체크(0) && IS_PREVACTNAME("벌이라고선언한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;벌개시 이외를 처리하는 개소
;━━━━━━━━━━━━━━━━━━━━━━━━
;벌중
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:벌플래그
	;=================================================
	;전회 ACT가 벌선언 한편 이번 ACT가 가학계
	;=================================================
	IF 구상기입체크(0) && MENUMATCH(TFLAG:ACT, "가학") && IS_PREVACTNAME("벌이라고선언한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전회 ACT가 벌선언
	;=================================================
	IF 구상기입체크(0) && IS_PREVACTNAME("벌이라고선언한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;질내, 장내 사정 직후 FIRSTEVENT(30)～
;─
;NOWEX:사정내지 NOWEX:MASTER:사정으로 사정종의 참조도 가능했다거나 합니다만
;EVENT20, 절정 구상에 동종의 분기와 입을 가능성도 있습니다
;-------------------------------------------------
;관련 변수…
;
;TFLAG:전회질내사정(1=V성교/2=V성교 봉사/3=V성교(하늘 쏘아 맞히고)/4=V성교 봉사(하늘 쏘아 맞히고)/5=A성교/6=A성교 봉사/7=A성교(하늘 쏘아 맞히고)/8=A성교 봉사(하늘 쏘아 맞히고)
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정 직후(조교 대상→조교자)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && GROUPMATCH(TFLAG:전회질내사정, 2, 4)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크() && TFLAG:전회질내사정 == 4
		PRINTFORMW 「나오지 않네요……」
		PRINTFORMW 「전부 다 내 버렸을까」
		RETURN 1
	ENDIF
	
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「이봐요 이봐요, 좀 더 나오겠죠?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내 사정 직후(조교 대상→조교자)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && GROUPMATCH(TFLAG:전회질내사정, 6, 8)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크() && TFLAG:전회질내사정 == 8
		PRINTFORMW 「움찔움찔 하고 있는데 나오지 않는다니……」
		PRINTFORMW 「조금 너무 짜내 버렸을까?」
		RETURN 1
	ENDIF
		
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「대단히 기세 좋게 나오는 거네」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정 직후(조교자→조교 대상)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && GROUPMATCH(TFLAG:전회질내사정, 1, 3)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크() && TFLAG:전회질내사정 == 3
		PRINTFORMW 「안돼…… 이제(벌써) 나오지 않아요」
		RETURN 1
	ENDIF
	
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「이렇게 내면, 임신해 버릴지도 몰라」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내 사정 직후(조교자→조교 대상)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && GROUPMATCH(TFLAG:전회질내사정, 5, 7)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크() && TFLAG:전회질내사정 == 7
		PRINTFORMW 「웃…… 이제(벌써) 더 이상은 나오지 않아요」
		RETURN 1
	ENDIF
	
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「몸중에서 퍼지는 느낌이지요?」
		RETURN 1
	ENDIF
ENDIF

;─
;성교중·성교봉사중 FIRSTEVENT(40)～
;─
;관련 변수…
;TEQUIP:성교중(조교자가 조교 대상으로 삽입중) (1=정상위/2=후배위/3=승마위/4=대면좌위/5=배면좌위/6=애널섹스)
;TEQUIP:성교봉사중(조교 대상이 조교자에게 삽입중) 내용은 상기의 것과 같음
;
;관련 함수…
;V_SEX (=TARGET, TEQUIP:성교중 < 6/=MASTER, TEQUIP:성교봉사중 < 6)와 동의
;A_SEX (=TARGET, TEQUIP:성교중 == 6/MASTER, TEQUIP:성교봉사중 == 6)와 동의
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;성교중(조교자→조교 대상)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(50) && TEQUIP:성교중
	;=================================================
	;페니스밴드 사용
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(!PENIS(TARGET), TEQUIP:조교자페니스밴드) > 0
		;애널섹스중
		IF A_SEX()
			PRINTFORMW 
		;그 외의 성교계
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;애널섹스(A성교)
	;=================================================
	IF 구상기입체크() && A_SEX()
		SELECTCASE RANDEVENT(2, 0)
			CASE 0
				PRINTFORMW 「후후, 질과 달리 얼마든지 안쪽까지 찌를 수 있는 것이 좋은 곳이군요……」
				PRINTFORMW 「이봐요! 좀 더 좋은 소리로 허덕이세요!」
			CASE 1
				;페니반파탄은 신경쓰지 않는다
				PRINTFORMW 「에 단단히 조이고 있어요……」
				PRINTFORMW 「사랑스러운 소리 내 버려! 이 변태!」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외의 성교계(V성교)
	;=================================================
	IF 구상기입체크() && V_SEX()
		IF PENIS(TARGET)
			SELECTCASE RANDEVENT(4, 1)
				CASE 0
					PRINTFORMW 「어때? 나의 자지는」
					PRINTFORMW 「좀더 좀더 찔러 주어요」
				CASE 1
					PRINTFORMW 「나의 자지로 찔린다니 상상도 할 수 없었던 것이지요?」
					PRINTFORMW 「후후후…… 당신이 갈 때까지 찔러 주어요」
				CASE 2
					PRINTFORMW 「굉장하네요……」
					PRINTFORMW 「당신의 질내…… 매우 기분이 좋아요……」
				CASE 3
					PRINTFORMW 「하아…… 굉장햇……!」
					PRINTFORMW 「질벽이…… 휘감겨……!」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교봉사중(조교 대상→조교자)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TEQUIP:성교봉사중
	;=================================================
	;페니스밴드 사용
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(!PENIS(MASTER), TEQUIP:페니스밴드) > 0
		;애널섹스중
		IF A_SEX(MASTER)
			PRINTFORMW 
		;그 외 성교 봉사계
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;애널섹스(A성교 봉사)
	;=================================================
	IF 구상기입체크(0) && A_SEX(MASTER)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외 성교 봉사계(V성교 봉사)
	;=================================================
	IF 구상기입체크(0) && V_SEX(MASTER)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;절정 한 직후 FIRSTEVENT(50)～
;─
;·조교자, 조교 대상이 절정 했을 경우에 표시되는 구상
;·개별의 부위별 절정을 줍고 싶으면, IF 구상 기입 체크(0) && NOWEX:C절정등의 분기를 추가해 주세요
;·사정종은 NOWEX:사정 /NOWEX:MASTER:사정으로 참조하는 것이 가능합니다
;-------------------------------------------------
;관련 변수…
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;
;관련 함수…
;TRUECHECK 실로 되는 것이 몇개 있을까를 돌려준다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 절정 한 직후
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TRUECHECK(NOWEX:C절정, NOWEX:V절정, NOWEX:A절정, NOWEX:B절정) > 0
	;=================================================
	;조교자 사정
	;=================================================
	IF 구상기입체크(20) && NOWEX:사정
		PRINTFORMW 「아아…… 나와 버렸다」
		RETURN 1
	ENDIF
	
	;각 부위의 절정이 1개이상의 경우에 실로 되는 개소
	;=================================================
	;다중절정
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(NOWEX:C절정, NOWEX:V절정, NOWEX:A절정, NOWEX:B절정) > 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 절정 한 직후
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TRUECHECK(NOWEX:MASTER:C절정, NOWEX:MASTER:V절정, NOWEX:MASTER:A절정, NOWEX:MASTER:B절정) > 0
	;=================================================
	;사정
	;=================================================
	IF 구상기입체크(20) && NOWEX:MASTER:사정
		SELECTCASE RANDEVENT(2, 3)
			CASE 0
				PRINTFORMW 「많이 나왔지 않은…… 그렇게 모여 있었을까?」
			CASE 1
				PRINTFORMW 「아직 떨리고 있는 안에 남아 있지 않은 것일까?」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;각 부위의 절정이 1개이상의 경우에 실로 되는 개소
	;=================================================
	;다중절정
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(NOWEX:MASTER:C절정, NOWEX:MASTER:V절정, NOWEX:MASTER:A절정, NOWEX:MASTER:B절정) > 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		RETURN 1
	ENDIF
ENDIF

;─
;조교 방침 변경 FIRSTEVENT(60)
;─
;·조교 방침이 바뀌었을 때에 표시한다
;·어디까지나 방침인 것으로 행동을 세세하게 결정하는 것은 아니다
;-------------------------------------------------
;관련 변수…
;TFLAG:턴방침		이 턴의 조교 방침(1=휴게/2=소프트/3=노멀/4=하드/5=이상)
;TFLAG:전턴방침		전턴의 조교 방침(내용은 턴방침과 같음)
;
;관련 함수…
;IS_POLICY	조교 방침 명칭과 합치하면 진을 돌려준다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 방침 변경
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:턴방침 != TFLAG:전턴방침
	;=================================================
	;턴방침 1:휴게(확실히 체력 회복 행동이 된다)
	;=================================================
	IF 구상기입체크() && IS_POLICY("지금", "휴게")
		PRINTFORMW 「지쳤군요, 조금 쉬어요」
		PRINTFORMW 「당신도 쉬세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 2:소프트
	;=================================================
	IF 구상기입체크() && IS_POLICY("지금", "소프트")
		PRINTFORMW 「가끔 씩은 상냥하게 해 주지 않으면」
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 3:노멀
	;=================================================
	IF 구상기입체크() && IS_POLICY("지금", "노멀")
		PRINTFORMW 「그런데, 조교 조교」
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 4:하드
	;=================================================
	IF 구상기입체크() && IS_POLICY("지금", "하드")
		PRINTFORMW 「후후, 다음은……」
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 5:이상
	;=================================================
	IF 구상기입체크() && IS_POLICY("지금", "이상")
		PRINTFORMW 「그런데, 인간의 몸이라고 하는 것은 어느 정도까지 견딜 수 있을까요」
		RETURN 1
	ENDIF
ENDIF

;─
;조건 없음 FIRSTEVENT(70)～
;─
;·여기까지의 조건에 히트 하지 않았던 경우에 표시되는 개소
;·BASE로 소분기를 마련해 있습니다만, 임의의 조건을 추가하는 일도 할 수 있습니다
;------------------------------------------------
;관련 변수…
;BASE		(0, 체력/1, 기력/2, 사정/3, 모유/4, 소변욕/5, 이성/6, 흥미/7, 초조/8, 만족)
;
;관련 함수…
;TENSION	인수로 지정한 BASE치를 나타낸다(50%이상은 높은,25%이상은 일반,25% 미만은 낮다)
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조건 없음
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크()
	;=================================================
	;만족 50％이상
	;=================================================
	IF 구상기입체크() && TENSION(TARGET, 8) == "높다"
		PRINTFORMW 「꽤 해요」
		RETURN 1
	ENDIF
	
	;=================================================
	;이성 25％이하
	;=================================================
	IF 구상기입체크() && TENSION(TARGET, 5) == "낮다"
		PRINTFORMW 「인내 할 수 없어요」
		RETURN 1
	ENDIF
	
	;=================================================
	;흥미 25％이하
	;=================================================
	IF 구상기입체크() && TENSION(TARGET, 6) == "낮다"
		PRINTFORMW 「지루하구나」
		RETURN 1
	ENDIF
	
	;=================================================
	;초조 50％이상
	;=================================================
	IF 구상기입체크() && TENSION(TARGET, 7) == "높다"
		PRINTFORMW 「사랑스러움 남아 미움 백배, 는」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 다음은 무엇으로 할까?
		RETURN 1
	ENDIF
ENDIF
