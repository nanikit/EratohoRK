;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	영림구상 가학계 액션 구상 파일
;
;	가학계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;가학계 ACT60～68
;========================================================================
;·TFLAG:ACT(60=스팽킹/61=채찍/62=바늘/63=줄/64=아이마스크/65=볼개그/66=매도/67=삼각목마/68=관장세트)
;·TFLAG:ACT분류 == 6에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "가학")로 참조하는 것이 가능
;·TEQUIP계는 기본, ACT 개시시점으로써 장착된다
;·ACT에 따라서는 조교 대상의 반응하기 나름으로 캔슬되는 것이 있다
;·줄/아이마스크/볼개그/삼각목마는 TEQUIP로 관리되고 해당 ACT/REACT 이외에도 TEQUIP:줄등으로 참조할 수 있다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;─
;ACT60, 스팽킹
;─
;·조교자가 조교 대상의 엉덩이를 손바닥에서 두드린다
;·ACT 개시시점으로써 벌써 두드리고 있다
;·성교봉사중에는 발생하지 않는다
;·안면승마 계는 해제된다
;·조교자가 자위중인 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K29_60
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT60, 스팽킹
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;·@TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	[SKIPSTART]
	각 분기내에 ABL:MASTER:마조끼나 ABL:MASTER:고통중독 등으로 파생을 마련하는 것도 좋다고 생각합니다
	디폴트로 준비하면 무리하게 묻어에 걸리는 예가 비교적 좋게 보여지므로, 텐프레로서는 굳이 마련하지 않습니다
	필요에 따라서 확장해 주세요
	
	SELECTCASE ABL:MASTER:마조끼
		;마조끼 6이상
		CASE IS > 6
			PRINTFORMW 
		;마조끼 이상
		CASE IS > 4
			PRINTFORMW 
		;그 외
		CASEELSE
			PRINTFORMW 
	ENDSELECT
	
	라든지, 이런 느낌으로. 확률도 관련되어 나눈다면…
	IF ELSEIF로 ABL:MASTER:마조끼 > 6 && ! RAND:2같은 방식이라도 좋을까
	단지 파생시킬 뿐(만큼)이라면 IF ELSEIF보다 SELECTCASE의 사용이 바람직합니다
	[SKIPEND]
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
		PRINTFORMW 「말하는 일을 (듣)묻지 않는다면 실력 행사군요」
		RETURN 1
	ENDIF
	
	;오늘의방침가 벌의 경우는 여기에 밖에 떨어지지 않게 되므로
	;구상 기입 체크(20)등으로 해서 확률을 짜는 것을 추천
	;=================================================
	;벌발동중
	;=================================================
	IF 구상기입체크(0) && TFLAG:벌플래그
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;TEQUIP:조교대상C사용 == 2(입에 의한 애무)는 고려하지 않는다
	;=================================================
	;C애무 계속중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:조교대상C사용 == 1
		;자위 계속중
		IF PENIS(MASTER)
			PRINTFORMW 
		;애무 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「벌의 시간이야」
		PRINTFORMW 「겨우 참는거네」
		RETURN 1
	ENDIF
ENDIF

;─
;ACT61, 채찍
;─
;·조교자가 채찍을 크게 치켜들고 마음껏 찍어내린다
;·ACT 개시시점으로써 벌써 두드리고 있다
;·턴방침이 "소프트"라면 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K29_61
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT61, 채찍
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;아이마스크 장착
	;=================================================
	IF 구상기입체크(0) && TEQUIP:아이마스크
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;구속중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:줄
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT62, 바늘
;─
;·유두와 성기에 바늘을 찌른다
;·ACT 개시시점으로써 벌써 찌르고 있다
;·조교 대상으로 페니스가 있는 경우는 페니스에, 없는 경우는 클리토리스에 찌른다
;·가슴과 성기(페니스/바기나)로부터 출혈해, 혈액 더러움이 추가된다
;·턴방침이 "소프트", "노멀"이라면 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마 계, 자위계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K29_62
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT62, 바늘
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 페니스의 유무로 분기. 분기내에 파생 없음
;-------------------------------------------------
;IF 구상 기입 체크(0) && PENIS(MASTER) 등으로 나누는 방법도 있습니다. 필요에 따라서 분기를 증감해 주세요
;비교적 쓰기 어려운 개소이며, 온전히 하는 것은 수고인 것로\@ PENIS(MASTER) ? 페니스 # 클리토리스 \@와 같이 3항을 사용하면 락 할 수 있거나 합니다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;아이마스크 장착중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:아이마스크
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT63, 줄
;─
;·조교 대상을 줄로 묶으려고 한다
;·지문은"묶으려고 했다…"되고 있지만 처리적으로는 ACT 개시시점으로써 속박되고 있다
;·TEQUIP:줄로 묶기의 강도를 참조할 수 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미 장착 끝나, 한편 TFLAG:벌플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:줄자체는 REACT분류가 "허가를청한다"의 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을 확실히 참조하는 수단 없음. 본체측의 개선이 요구된다
;·(REACT63 이외에도 캔슬 처리가 되는 경우가 있기 때문에(위해))
;·성교중/성교봉사중에는 발생하지 않는다
;·구속시에 자위나 안면승마, 조교 대상의 페니스 사용중은 해제된다
;-------------------------------------------------
@KOJO_ACT_K29_63
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT63_0, 줄장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·반항각인, 조교자 조교레벨, 기교, 결박능숙 소질에 응해 구속 SOURCE 증가
;·구속 SOURCE에 응해 강도 상승
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;묶기 강도는 통상이라면 1이나 2 정도라고 생각됩니다
	;그 이상의 묶기 강도는 반항각인이나 조교자의 결박능숙 소질+일정 이상의 기교가 요구될까하고
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		SELECTCASE TEQUIP:줄
			;묶기 강도 3, 혹은 그 이상
			CASE IS >= 3
				PRINTFORMW 
			;묶기 강도 3
			CASE 2
				PRINTFORMW 
			;그 외
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT63_1, 줄해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;줄해제에 파생한 시점에서, "TEQUIP:줄 = 0"이 되므로 해제시에 묶어 강도는 참조 할 수 없다
;장착과 강도로 같은 TEQUIP를 재사용하고 있기 때문에(위해), 이와 같이 되어 버린다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;ABL:MASTER:구속중독 되어 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 따라서 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT64, 아이마스크
;─
;·조교자가 아이마스크를 대게 하려고 한다
;·지문에서는"대게 하려고 했다…"되고 있지만 처리상은 ACT 개시시점으로써 장착되고 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미 장착 끝나, 한편 TFLAG:벌플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:아이마스크는 REACT분류가 "허가를청한다"(이었)였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을 확실히 참조하는 수단 없음. 본체측의 개선이 요구된다
;·(REACT64 이외에도 캔슬 처리가 되는 경우가 있기 때문에(위해))
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마 계는 해제된다
;·조교자가 자위중이었던 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K29_64
;-------------------------------------------------
;·TFLAG:ACT파성(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT64_0, 아이마스크 장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·조교 대상으로 페니스가 있는 경우, 마조끼에 응해 발기도 상승
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ABL:MASTER:구속중독 되어 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 따라서 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT64_1, 아이마스크 해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT65, 볼개그
;─
;·조교자가 볼개그를 끼우게 하려고 한다
;·지문에서는"끼우게 하려고 했다…"되고 있지만 처리상은 ACT 개시시점으로써 장착되고 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미 장착 끝나, 한편 TFLAG:벌플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:볼개그는 REACT분류가 "허가를청한다"(이었)였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을※확실히 참조하는 수단 없음. 본체측의 개선이 요구된다
;·※REACT64 이외에도 캔슬 처리가 되는 경우가 있기 때문에(위해)
;·턴방침이 "소프트", "노멀"의 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·안면승마 계는 해제된다
;·조교자가 자위중이었던 경우는 해제된다
;-------------------------------------------------
@KOJO_ACT_K29_65
;-------------------------------------------------
;·TFLAG:ACT파성(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT65_0, 볼개그 장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·조교 대상으로 페니스가 있는 경우, 마조끼에 응해 발기도 상승
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ABL:MASTER:구속중독 되어 ABL:MASTER:마조끼로 파생을 설치해도 좋을까 생각합니다
	;@RANDACT로 랜덤 처리하는 것도 좋을 것입니다
	;필요에 따라서 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT65_1, 볼개그 해제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT66, 매도
;─
;·도발하도록(듯이) 조교 대상의 치태를 소리에 낸다
;·회화계로부터 발생하는 경우도 있다
;·턴방침이 "소프트"라면 발생하지 않는다
;·회화계로부터 파생하는 경우도 있다
;-------------------------------------------------
@KOJO_ACT_K29_66
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT66, 매도
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;성교중/성교봉사중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:성교중, TEQUIP:성교봉사중) > 0
		;성교중
		IF TEQUIP:성교중
			PRINTFORMW
		;성교봉사중
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;자위중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(GETBIT(TEQUIP:자위중, 0), GETBIT(TEQUIP:자위중, 1)) > 0
		;조교 대상이 자위중
		IF GETBIT(TEQUIP:자위중, 0)
			PRINTFORMW 
		;조교자가 자위중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;C애무 계속중
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상C사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상C사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT67, 삼각목마
;─
;·조교 대상을 목마에 실으려고 한다
;·지문은"실으려고 했다…"되고 있지만 처리상은 ACT 개시시점으로써 실려지고 있다
;· 실은 쾌C는 아니고 쾌V가 들어간다(나있는 목마!?)
;·MASTER가 처녀라면, 처녀상실이 일어난다(@ACTION_APPLY2로 처리. REACT분류가 허가를청한다의 경우는 회피된다)
;·조교 대상이 남자의 경우는 발생하지 않는다
;·턴방침이 "소프트", "노멀"때에는 발생하지 않는다
;·TEQUIP(바이브/애널바이브/애널비즈/관장기＋플러그/페니스밴드) 장착시에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·야외플레이 때에는 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·이상계로부터 발생하는 경우도 있다
;·이미 장비 하고 있었을 경우는 해제가 된다
;·이미 장착 끝나, 한편 TFLAG:벌플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
;·TEQUIP:삼각목마는 REACT분류가 "허가를청한다"(이었)였던 경우에 해제된다
;·0616판 시점의 사양으로 이 거동을※확실히 참조하는 수단 없음. 본체측의 개선이 요구된다
;·※REACT67 이외에도 캔슬 처리가 되는 경우가 있기 때문에(위해)
;-------------------------------------------------
@KOJO_ACT_K29_67
;-------------------------------------------------
;TFLAG:ACT파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT67_0, 삼각목마 장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생로 분기. 파생내에서는 분기 없음
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 목마에 실으려고 했다…
	
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT67_1, 삼각목마 해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;TEQUIP 캔슬 시에는 여기서 주울 수 있는 것이 아닌 것에 주의
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 목마로부터 내려 주었다…
	
	;첫회라고는 말해도, 과거에 TEQUIP 캔슬이나 해제되지 않는 채 조교 종료로 있던 가능성도 있다
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT68, 관장세트
;─
;·관장액의 주입/추가/배출
;·최초의 주입시(ACT파생=0)는 양 1～3. 추가시(ACT파생=2)는 양 1～4. 각각 랜덤으로 처리된다
;·;이미 장비 하고 있는 경우, 관장액추가가 된다
;·관장액이 5병 들어가 있는 경우, 관장 플러그 해제가 된다
;·관장액의 분량은 TEQUIP:관장기＋플러그로 참조할 수 있다
;·TEQUIP(애널바이브/애널비즈/페니스밴드/삼각목마) 장착시에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·턴방침이 "소프트"라면 발생하지 않는다
;·도구계로부터 파생하는 경우도 있다
;-------------------------------------------------
@KOJO_ACT_K29_68
;-------------------------------------------------
;·TFLAG:ACT파생(0=장착/1=해제/2=관장액추가)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_0, 관장기＋플러그 장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 ACT파생에 의해 분기. 파생내에서는 분기 없음
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	
	;지문↓
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("개개, ") %%CALLNAME:MASTER%의 국혈에
	;관장액을 흘려 넣어, 마개를 했다…
	
	;ACT 개시시점으로써 액은 주입되고 있어 TEQUIP:관장기＋플러그로 액량을 참조하는 것이 가능
	
	;=================================================
	;첫회 「주입」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;사양이 바뀔 가능성을 고려해 ==는 아니고>=로 해둔다
	;=================================================
	;액량 3
	;=================================================
	IF 구상기입체크(0) && TEQUIP:관장기＋플러그 >= 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;액량 2
	;=================================================
	IF 구상기입체크(0) && TEQUIP:관장기＋플러그 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;액량의 분기를 모두 묻었을 경우는 TEQUIP:관장기＋플러그 == 1에 상당
	;개별적으로 나누지 않고 SELECTCASE TEQUIP:관장기＋플러그로 정리해 버리는 일도 가능
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_1, 관장기＋플러그 해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;여기에 파생하는 시점에서 액량은 5가 되어 있다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	
	;지문↓
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("면서, ") %%CALLNAME:MASTER%로부터
	;항문마개를 뽑아, 장내의 오물을 항문으로부터 흩뿌리게 했다…
	
	;=================================================
	;첫회 「배출」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT68_2, 관장액추가
;━━━━━━━━━━━━━━━━━━━━━━━━
;이미 주입 끝난 경우에 이 개소에 파생
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	
	;지문↓
	;%CALLNAME:TARGET%는%TARGET_DESCRIPTION("면서, ") %%CALLNAME:MASTER%의 애널에
	;한층 더 관장액을 주입하려고 했다…
	
	[SKIPSTART]
	액량으로 나누어 쓴다면 comment out를 해제
	하층의 PRINTFORMW를 삭제해 주세요
	;관장액분량 파생
	SELECTCASE TEQUIP:관장기＋플러그
		;액량 5
		CASE 5
			PRINTFORMW 
		;액량 4
		CASE 4
			PRINTFORMW 
		;액량 3
		CASE 3
			PRINTFORMW 
		;그 외
		CASEELSE
			PRINTFORMW 
	ENDSELECT
	[SKIPEND]
	;나누어 쓰지 않는 것이면 여기서
	PRINTFORMW 
	RETURN 1
ENDIF
