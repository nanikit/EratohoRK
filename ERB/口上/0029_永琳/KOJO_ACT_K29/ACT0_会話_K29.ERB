;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	에이린 구상 회화계 액션 구상 파일
;
;	회화계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 ACT0～9
;========================================================================
;·TFLAG:ACT(0=지금의기분을얘기한다/1=자신에게로의기분을얘기한다/2=성에대한이야기를한다/3=상냥하게위로한다/4=엄격하게위협한다/5=의상의변경을요구한다/6=합의를요구한다/7=벌이라고선언한다/8=고함친다/9=기분나쁘게웃는다)
;·TFLAG:ACT분류 == 0에 해당
;·ACT9, 기분나쁘게웃는다는 이상계 ACT분류로부터 파생하는 경우가 있다
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------
;────────────────────────────────────
;ACT0, 지금의기분을얘기한다
;────────────────────────────────────
;·지금의 기분에 대해 들어 온다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K29_0
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 지금의기분을얘기한다
;거의 (듣)묻지 않는 애널은 취미
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크()
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
		PRINTFORMW 「그렇구나, 지금의 기분을 들려주어 받을까」
		PRINTFORMW 「이것도 조교의 일환이야. 말에는 조심하는거네」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;조교 대상의Ｃ에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상Ｃ사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상Ｃ사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상Ｃ사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크() && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			[SKIPSTART]
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			[SKIPEND]
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 「하아…… 어때? 그런 나리 해 아무것도 하지 못하고 범해져 버리는 기분은」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「어떤 기분일까?」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT1, 자신에게로의기분을얘기한다
;────────────────────────────────────
;·자신의 일을 어떻게 생각할까 얘기한다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일 조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K29_1
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 자신에게로의기분을얘기한다
;의외로 (듣)묻거나 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
		PRINTFORMW 「나에게로의 기분을 들려주어 받을까」
		PRINTFORMW 「후후후, 부끄러워해서는 안 돼요」
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;조교 대상의Ｃ에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상Ｃ사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상Ｃ사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상Ｃ사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(2, 0)
			CASE 0
				PRINTFORMW 「나에게로의 기분을 들려주어 받을까」
			CASE 1
				PRINTFORMW 「나의 일, 어떻게 생각해?」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT2, 성에대한이야기를한다
;────────────────────────────────────
;·조교자가 성에 대한 지식을 말하기 시작한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·조교자에게 성지식이 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K29_2
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT2, 성에대한이야기를한다
;가끔 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크()
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
		IF TALENT:MASTER:동정 || TALENT:MASTER:처녀
			PRINTFORMW 「당신, 이런 종류의 이야기에는 저항이 있을 듯 하지만……」
			PRINTFORMW 「혹시, 이성을 모르는 것일까?」
			PRINTFORMW 「순진무구, 아니, 그런 일에 인연이 없었던 것이군요. 불쌍해」
			RETURN 1
		ENDIF
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;조교 대상의Ｃ에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상Ｃ사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상Ｃ사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상Ｃ사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「당신은 스스로의 처리가 되어 있을까」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT3, 상냥하게위로한다
;────────────────────────────────────
;·조교자가 상냥한 말로 위로해 온다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K29_3
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 상냥하게위로한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;조교 대상의Ｃ에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상Ｃ사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상Ｃ사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상Ｃ사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마중
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「녹초가 되고 있는 거야?」
		PRINTFORMW 「확실히 하세요. 앞일이 걱정되어요」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT4, 엄격하게위협한다
;────────────────────────────────────
;·어려운 말로 위협한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K29_4
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화할 가능성이 있다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 엄격하게위협한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크()
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
		PRINTFORMW 「자신의 상황을 생각하면 어떻게 해야할 것인가는 알아요?」
		PRINTFORMW 「그럴 기분이 들면 당신에 좀 더 심한 일을 할 수 있어」
		PRINTFORMW 「분명하게 나의 말하는 일에 따르는거네」
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;조교 대상의Ｃ에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상Ｃ사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상Ｃ사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상Ｃ사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		SELECTCASE RANDACT(3, 0)
			CASE 0
				PRINTFORMW 「나에게 반항하면…… 어떻게 될까 알까?」
			CASE 1
				PRINTFORMW 「나에게는, 당신을 어떻게라도 할 수 있다는 것을 잊지 말아줘」
				PRINTFORMW 「구워먹든 삶아먹든…… 좋아하게」
			CASE 2
				PRINTFORMW 「알고 있어? 당신의 운명은 나의 손안에」
				PRINTFORMW 「따르는 편이 현명해요」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT5, 의상의변경을요구한다
;────────────────────────────────────
;·조교자가 의상의 변경을 요구해 온다
;·조교 대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:쓰러트리기중에는 발생하지 않는다
;·TEQUIP:줄이 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K29_5
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%에 옷을 벗어라고 명령했다…"되고 있어 이것이 변화하는 일은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
		PRINTFORMW 「그러면, 옷을 벗어 줘」 
		PRINTFORMW 「부끄러워할 것은 없어요…… 기본인 것이니까」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크() && ONCEACT(0)
		PRINTFORMW 「네네, 그러면 오늘도 옷을 벗어요」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상으로 페니스 있어
	;=================================================
	IF 구상기입체크() && PENIS(MASTER)
		;발기하고 있다
		IF TCVAR:MASTER:발기도 >= 1000
			PRINTFORMW 「아라아라, 옷을 밀어올리고 있어요? 후후후」
			PRINTFORMW 「어떻게 되어 있을까…… 벗지 않으면 모르네요」
		;그 외
		ELSE
			PRINTFORMW 「무슨 일이야?」
			PRINTFORMW 「빨리 벗어, 자지 보이기를 원해요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상으로 페니스 없음
	;=================================================
	IF 구상기입체크(0) && !PENIS(MASTER)
		;젖고 있다
		IF PALAM:MASTER:윤활 > 500
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「응, 그러면 옷을 벗어 받을까」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT5_1, 옷을 입도록(듯이) 명령
;────────────────────────────────────
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:ACT파생 == 1
	PRINTFORMW 「옷, 입어도 괜찮아요. 입으세요」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT6, 합의를요구한다
;────────────────────────────────────
;·처음을 받아도 괜찮을까 얘기한다
;·처음=처녀 or동정(양쪽 모두의 경우도 있다)
;·이미 합의를 얻고 있는 경우는 발생하지 않는다
;·조교 대상이 처녀도 동정도 아닌 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K29_6
;-------------------------------------------------
;함수@TARGET_DESCRIPTION로 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;후는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 합의를요구한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크()
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 남자의 경우
	;=================================================
	IF 구상기입체크() && TALENT:MASTER:남자
		PRINTFORMW 「당신…… 동정이지요?」
		PRINTFORMW 「나에게………… 그것을 바칠 생각은 없을까?」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 후타나리의 경우
	;=================================================
	IF 구상기입체크() && TALENT:MASTER:후타나리
		PRINTFORMW 「당신, 동정이던가?」
		PRINTFORMW 「여자 아이 쪽도 처음이야?」
		PRINTFORMW 「사랑스러운…… 어느쪽이나, 내가 받아 줄까?」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 여자 아이의 경우
	;=================================================
	IF 구상기입체크() && !PENIS(MASTER)
		PRINTFORMW 「당신이 소중히 해 온 순결…… 내가 가라앉혀 주어요」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT7, 벌이라고선언한다
;────────────────────────────────────
;·지금부터 벌 한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:벌플래그는 서 있다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP:쓰러트리기중시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K29_7
;-------------------------------------------------
;여기에서는 지문의 변화는 없다. 분기의 증감은 기호로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, 벌이라고선언한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「무뚝뚝하게대답한다/강하게대답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;=================================================
	IF 구상기입체크() && IS_PREVCOMNAME("무뚝뚝하게대답한다/강하게대답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 「좀 더 착실한 반응은 할 수 없는거야?」
		PRINTFORMW 「화나요」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「기분좋게해줘」(이 철면피!)
	;=================================================
	IF 구상기입체크() && IS_PREVCOMNAME("기분좋게해줘")
		PRINTFORMW 「이 변태……♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「더하드하게」(이 돼지째!)
	;=================================================
	IF 구상기입체크() && IS_PREVCOMNAME("더하드하게")
		PRINTFORMW 「마조히스트에는 마조히스트든지의 벌을 해 주지 않으면♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「용서를빈다」(허락해 받을 수 없었습니다)
	;=================================================
	IF 구상기입체크() && IS_PREVCOMNAME("용서를빈다")
		PRINTFORMW 「유감이지만, 허락할 생각은 없어요」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「자위하기시작한다」(사람의 이야기를 들어라!)
	;=================================================
	IF 구상기입체크() && IS_PREVCOMNAME("자위하기시작한다")
		PRINTFORMW 「재미있지만, 명령을 거역한 벌이 필요하구나」
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		SELECTCASE RANDACT(2, 0)
			CASE 0
				PRINTFORMW 「당신 같은 애물단지에게는」
				PRINTFORMW 「힘으로 말하는 일을 들려줄 수 밖에 없는 것 같네」
			CASE 1
				PRINTFORMW 「입으로 말해도 쓸데없는 같구나」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT8, 고함친다
;────────────────────────────────────
;·큰 소리로고함친다
;·턴방침이 "소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:벌플래그에 관련되지 않고, 발생할 가능성이 있다(벌중에 한층 더고함친다가능성등)
;-------------------------------------------------
@KOJO_ACT_K29_8
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 큰 소리로 고함쳤다…"만으로 파생은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 고함친다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「무뚝뚝하게대답한다/강하게대답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("무뚝뚝하게대답한다/강하게대답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT9, 기분나쁘게웃는다
;────────────────────────────────────
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·화제 『비웃는다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K29_9
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 기분나쁘게웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;조교 대상의Ｃ에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상Ｃ사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상Ｃ사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상Ｃ사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「후후후……」
		RETURN 1
	ENDIF
ENDIF
