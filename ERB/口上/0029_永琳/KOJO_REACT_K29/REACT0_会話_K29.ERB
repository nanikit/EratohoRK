;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	에이린 구상 회화계 개별 리액션 구상 파일
;
;	회화계 리액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 REACT0～9
;========================================================================
;·1231판보다 TFLAG:REACT부하가 도입되어 선택한 COM가 자연스러운 행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운 행동/1=무리가 없는 행동/2=무리를 한 행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;─
;REACT0, 지금의기분을얘기한다·개별 리액션
;─
;·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K29_0
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일 하고 싶다)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따르는 것으로, 리액션이빈정거린다 이외의 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 0
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 질문에 분명히 답하는 것이 할 수 없었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 말씨 나쁘게 질문에 답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 나가려 한 말을 삼켜 대답을 흐렸다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「좀 더 분명하게 대답하세요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게대답한다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 1
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 어려운 말을 퍼부었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 용기를 내 강하게 맞았다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 눈을 일등 해 입에서는 강하게 행동했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「좋은 담력 하고 있지 않아」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 2
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 여유의 미소로 어깨를 움츠리고 「지금의 기분은, 이라고 (들)묻어도」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 조금 생각하고 나서 「지금의 기분은, 이라고 (들)묻어도」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 간신히 야유 섞임에 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「질문의 내용이 나빴을까?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 3
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 함께 있어지는 것이 기쁘면 솔직하게 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 분명히 지금의 기분을 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 기분을 해치지 않게, 손질해 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「센스 있은 것 하지 않아」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 태연하게 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 4
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 그렇게 나쁘다고 생각하지는 않다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「그저 나쁘지는 않다」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 적극			시선을 피해, 나쁘지는 않아라고 중얼거렸다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 혐				힘껏의 허풍으로, 그저 나쁘지 않다고 대답했다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「우유부단이군요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일 하고 싶다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 5
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 좀 더 기분 좋아지고 싶다고 했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 기분 좋은 일 하고 싶은 기분이라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 아첨하도록(듯이) 감히 추잡한 행위를 졸랐다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「자신에게 정직한거네. 나쁘지는 않지만, 여기에서는 단점이군요」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------
[SKIPSTART]
통상의 경우 리액션의 지문은@TRAIN_MESSAGE2와@REACTION_MESSAGE로 처리됩니다만
이하의 부분은@TRAIN_MESSAGE2의 조건으로부터 빠지기 때문에(위해), @REACTION_MESSAGE에서만 처리됩니다
COM 분기에 있어서도 당연히 TFLAG:REACT파생는 기능하고, @REACTION_MESSAGE내에서도 REACT부하 마다 나누어 쓸 수 있습니다
SELECTCASE GET_COM_RANK()
	;자연스러운 행동
	CASE "자연스러운 행동"
		PRINTFORMW 
	;무리가 없는 행동
	CASE "무리가 없는 행동"
		PRINTFORMW 
	;무리를 한 행동
	CASE "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 싫어
		ELSE
			PRINTFORMW 
		ENDIF
ENDSELECT
상기와 같은 분기를 마련하는 일에 의해, 본체측의 변화에 대응하는 일은 가능합니다만
텐프레에 있어서 디폴트로 마련하려면 품이 많이 든다고 생각하는 까닭에, 기본적으로 REACT내의 COM 분기에는 테두리를 마련하지 않을 방침입니다
범용적인 내용으로 해도 좋아, 또 부분적으로 범용 리액션(COM)에 흘려도 좋다고 생각합니다
뭐 REACT부하 이외에도 확장하는 방법은 얼마든지 있거나 하므로, 그 근처의 판단은 각 구상 작자에게 맡기고 싶다고 생각합니다
3 BM/4 MB에서도 쓰기 때문에 어쨌든 생각되는 요소를 전부 넣어 줘! 적인 요망이 만약 있으면, 그 때는 또 생각하고 싶다와
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;지금의기분을얘기한다에서의 COM침묵한다는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어 벌포인트가 가산된다
;-------------------------------------------------
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「입다물어도 상황은 변함없어요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;벌포인트 가산 파생:「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「안되지 않아. 그런 간단하게 약한소리를 해서는」
		PRINTFORMW 「근성을 다시 단련할 필요가 있을 듯 하구나」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「어쩔 수 없네요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「그렇게 내가 무서운 것일까?」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「무서워해 버려, 사랑스럽다」
		RETURN 1
	ENDIF
ENDIF
;REACT0, 지금의기분을얘기한다

;─
;REACT1, 자신에게로의기분을얘기한다·개별 리액션
;─
;·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K29_1
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다! /2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 0
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 아무래도 싫다고는 말할 수 없었다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「싫지 않은… 일지도 모른다…」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 본심을 숨기도록(듯이) 눈을 숙여, 「싫지 않은… 일지도 모른다…」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「아라, 뜻밖의 일 하는 거네」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게대답한다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 1
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 「좋아하게 될 수 있을 이유가 없다!」라고 강하게 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「그런 일이 있을 이유 없다!」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 감정을 부정하도록(듯이) 고개를 저어 「싫다!」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「그것은 그래요, 후후」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 2
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 「좋아하게 될 수 있을 이유가 없다!」라고 강하게 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「그런 일이 있을 이유 없다!」라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 감정을 부정하도록(듯이) 고개를 저어 「싫다!」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「여유인 것 같지 않아. 그 여유도 언제까지 계속될까요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 적은지, REACT분류가 태연하게 따른다
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 3
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 「좀 더 좋아하게 되고 싶기 때문에, 심한 것은 하지 말아줘」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「이제(벌써) 심한 일 하지 않으면, 좋아하게 될 수 있을지도 모른다」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 기분을 묻도록(듯이) 시선을 향한 후, 「심한 일만 하는 사람은 싫다」라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「그런 일 말해지면 조금 생각해 버린다…… 랄까」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 1000이상
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 4
	;부하 2는 연주한 (분)편이 뭔가 형편이 좋은 것 같아 우선
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(4) && TFLAG:REACT부하 < 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 붙임성 좋게 웃어 정말 좋아한다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 분명히 좋아라고 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 억지웃음을 띄워, 좋아해라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「납치 감금된 다음 좋아한다 라고, 당신도 호기심이군요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크() && TFLAG:ACT파생 == 5
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(5)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 붙임성 좋게 웃어 정말 좋아한다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 분명히 좋아라고 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 억지웃음을 띄워, 좋아해라고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「아라, 재미있는 일 말하지 않아. 자신의 말에는 책임을 가져?」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자신에게로의기분을얘기한다의 경우는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어 벌포인트가 가산된다
;-------------------------------------------------
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「대답할 수 없는 이유이기도 할까?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;벌포인트 가산 파생:「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「헤에, 허락했으면 좋다」
		PRINTFORMW 「나는 참회 되는 것 같은 기억은 없지만, 당신에게는 꺼림칙한 것이 있는 거네」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「공기를 읽을 수 없는 사람은 좋아하지 않아요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「유감이구나. 두려워해져 버리면 나도 실망이예요」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「상당히 무서워해지고 있는 것 같구나, 후후후」
		RETURN 1
	ENDIF
ENDIF
;REACT1, 자신에게로의기분을얘기한다

;─
;REACT2, 성에대한이야기를한다·개별 리액션
;─
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K29_2
;-------------------------------------------------
;  파생할 가능성이 있는 COM는 우선 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는 방법도 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「나는 상당히 진지해요?」
	PRINTFORMW 「그렇지 않으면 커뮤니케이션은 서툴러?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「무정하네요, 이제(벌써)」
		RETURN 1
	ENDIF
	
	;REACT분류"소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
		PRINTFORMW 「화제로서는 부족했을까」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;벌포인트 가산 파생:「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「뭐야, 모처럼 온화한 기분으로 화제를 꺼내 주었는데」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「아라, 이런 것은 골칫거리?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게해줘
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("기분좋게해줘")
	;=================================================
	;벌포인트 가산 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「이런 이야기로 인내 할 수 없게 되어 버리는 것은 사랑스럽지만, 곧바로 요구해 버리는 것은 감점이군요」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「아라, 흥분해 버렸어?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("부끄러워한다")
	;TEQUIP에 의해 쾌SOURCE가 들어가 있는 경우에 파생
	;=================================================
	;REACT파생 1, 쾌락에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 상황에부끄러워한다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
		PRINTFORMW 「당신의 몸의 일이기도 한거야. 부끄러워한다 것은 없어요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「무엇을 상상했어?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("자위하기시작한다")
	;=================================================
	;REACT파생 1, 「마음대로 자위 하지마」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「이봣, 안되지 않는 마음대로 그런 일 시작해는」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「마음대로 자위 하고 있지만 좋은가」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
		PRINTFORMW 「완전히, 백문이 불여일견인 거네, 당신은」
		RETURN 1
	ENDIF
ENDIF
;REACT2, 성에대한이야기를한다

;─
;REACT3, 상냥하게위로한다·개별 리액션
;─
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K29_3
;-------------------------------------------------
;  파생할 가능성이 있는 COM에는 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는 방법도 있습니다
;-------------------------------------------------
;  우선 회화계 COM를 모두 망라되어 있습니다만
;  10 접수대의 애무계 COM, 50 접수대 이후의 그 외 COM등도 상황에 따라서는 선택 가능하게 됩니다
;  필요에 따라서 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「아라, 마음에 드시지 않았을까?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「타인의 호의는 제대로 받아 들이세요」
		RETURN 1
	ENDIF
	
	;REACT분류"소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
		PRINTFORMW 「무기력인 것은 간단하게는 낫지 않는 것 같네」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무뚝뚝하게대답한다")
	PRINTFORMW 「앞은 길어질 것 같구나」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성 있게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("붙임성좋게대답한다")
	PRINTFORMW 「솔직한 곳은 호감이 가질 수 있어요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("강하게대답한다")
	PRINTFORMW 「어이쿠…… 조금 예상외군요」
	PRINTFORMW 「위로해 주고 있기 때문에 솔직하게 받아 들이면?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 「솔직하지 않은거네」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더하드하게
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("더하드하게")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「어쩔 수 없는 아이, 당신은」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
		PRINTFORMW 「생각해 두어요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「무엇을 허락했으면 좋은 것일까?」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「잘 모르네요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게해줘
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("기분좋게해줘")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「좀 더 다른 의미로 위로해 받고 싶은거네」
		PRINTFORMW 「싫어요. 기분 나쁘다」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「입으로 말해지는 것보다 행동으로 나타나고 싶은거네」
		RETURN 1
	ENDIF
ENDIF
;REACT3, 상냥하게위로한다

;─
;REACT4, 엄격하게위협한다·개별 리액션
;─
;·TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만
;-------------------------------------------------
@KOJO_REACT_K29_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「후후후, 아무것도 말할 수 없게 되어 버렸을까」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「이제 그만두어? 어느 입이 그것을 말하고 있을까?」
		RETURN 1
	ENDIF
	
	;REACT분류"소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
		PRINTFORMW 「위협해졌다면 그 정도의 반응이 타당하구나」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 「아라, 아직 당신에 그런 여유가 남아 있었어?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(3)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「허락하기를 원하는거네. 어쩔 수 없어요……」
		PRINTFORMW 「라고도 말한다고 생각했어? 그렇게 간단하게 허락해 줄 이유 없잖아」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「허락하기를 원하는거네. 어쩔 수 없어요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("비명을올린다")
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「아라아라, 망가져 버렸을까? 후후후」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「좋아요, 좀 더 무서워하세요」
		RETURN 1
	ENDIF
ENDIF
;REACT4, 엄격하게위협한다

;─
;REACT5, 의상의변경을요구한다·개별 리액션
;─
;·조교 대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가"소극적 따른다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가"적극적 따른다", "태연하게 따른다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
@KOJO_REACT_K29_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:ACT파생 == 0
	;=================================================
	;한 장 벗었다(소극적 반응)
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("소극적 따른다/받아들인다")
		;-------------------------------------------------
		;마지막 한 장을 벗어전라가 되었다
		;-------------------------------------------------
		IF 구상기입체크() && (TEQUIP:MASTER:악세사리 + TEQUIP:MASTER:양말 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신윗도리 + TEQUIP:MASTER:상반신윗도리 + TEQUIP:MASTER:전신윗도리) == 0
			;REACT분류받아들인다			전라가 되어, 기대와 흥분으로 피부가 달아오르는 것을 느꼈다
			IF IS_COMGRONAME("받아들인다")
				PRINTFORMW 「아라아라, 붉어져 버려, 사랑스러워요」
			;REACT분류소극적 따른다			어쩔 수 없이 마지막 한 장을 벗어, 전라가 되었다
			ELSE
				PRINTFORMW 「좋은 몸이군요」
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;완만한 움직임으로 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크() && IS_COMGRONAME("받아들인다")
			PRINTFORMW 「빨리 벗으세요」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;어쩔 수 없이 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크() && IS_COMGRONAME("소극적 따른다")
			PRINTFORMW 「뭐 좋아요」
			RETURN 1
		ENDIF
		
		;상층의 분기를 모두 묻었을 경우는 불필요한 개소
		;반대로 묻지 않는 경우는 이 개소가 보완이 책임을 다합니다
		;-------------------------------------------------
		;그 외·조건 없음
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;2매 벗었다(적극적 반응)
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("적극적 따른다/태연하게 따른다")
		;-------------------------------------------------
		;전라가 되었다
		;-------------------------------------------------
		IF 구상기입체크() && (TEQUIP:MASTER:악세사리 + TEQUIP:MASTER:양말 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신윗도리 + TEQUIP:MASTER:상반신윗도리 + TEQUIP:MASTER:전신윗도리) == 0
			PRINTFORMW 「남의 앞에서 분명하게 벗겨지는 아이는 시간이 들지 않아도 괜찮아요」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;적극적으로 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크() && IS_COMGRONAME("적극적 따른다")
			PRINTFORMW 「꽤 좋지 않아」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;솜씨 좋게 벗어 던졌다
		;-------------------------------------------------
		IF 구상기입체크() && IS_COMGRONAME("태연하게 따른다")
			PRINTFORMW 「대단히 익숙해진 느낌이군요」
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;벗지 않았다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("거부")
		;부끄러워서 벗겨지지 않았다
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
			PRINTFORMW 「아라, 부끄럼쟁이씨네」
			PRINTFORMW 「내가 벗겨 줄까?」
		;싫어해 벗지 않았다
		ELSE
			PRINTFORMW 「착의 플레이를 좋아해?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:ACT파생 == 1
	;=================================================
	;한 장 입었다(소극적 반응)
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("소극적 따른다/받아들인다")
		PRINTFORMW 「붙임성 나쁘네요」
		RETURN 1
	ENDIF
	
	;=================================================
	;2매 입었다(적극적 반응)
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("적극적 따른다/태연하게 따른다")
		PRINTFORMW 「이봐요 이봐요, 입었다 입었다」
		RETURN 1
	ENDIF
	
	;지문에서는 "싫어해"라고 하는 취급
	;=================================================
	;입지 않았다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("거부")
		PRINTFORMW 「입는 것이 싫어?」
		RETURN 1
	ENDIF
ENDIF
;REACT5, 의상의변경을요구한다

;─
;REACT6, 합의를요구한다·개별 리액션
;─
;·조교 대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
@KOJO_REACT_K29_6
;-------------------------------------------------
;수고를 싫어하지 않는 것이면, TALENT:MASTER:남자등으로 내부에서 나누는 것도 있을지도 모릅니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("소극적 따른다")
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 각오를 결정한 것처럼, 작은 소리로 승낙했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 0, 작은 소리로 승낙했다	
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 적극			부끄러움에 스러질 것 같은 소리로 승낙했다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 혐				분함에 얼굴을 비뚤어지게 하면서도 승낙했다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 마음대로 하면 좋다고 대답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 태연하게 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("태연하게 따른다")
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 별로 싫지 않다고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 마음대로 하면 좋다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 적극			하고 싶으면 좋다고 말했다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 혐				마음대로 하면 좋다고 대답했다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("적극적 따른다")
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 처음을 줄 수 있어 기쁘다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 뜻을 정해 바쳐도 괜찮다고 고했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 이제 지킬 수 없다면 적어도 상냥하게 여겨지도록(듯이)와 동의 했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게해줘 주는 것이라면 처음을 주어도 괜찮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("받아들인다")
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 처음을 바치기 때문에기분좋게해줘 갖고 싶다고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 기분좋게해줘 주는 것이라면 처음을 주어도 괜찮다고 대답했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 처음을 바치라고 한다면, 괴로움을 잊을 수 있을 정도로기분좋게해줘라고 말했다
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서 "거부"에 세트 된다)
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("거부")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(4)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT6, 합의를요구한다

;─
;REACT7, 벌이라고선언한다·개별 리액션
;─
;·선언해도 벌이 발생한다고는 할 수 없다
;-------------------------------------------------
@KOJO_REACT_K29_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「아라, 겁먹었어?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM"여리게대답한다"는 일정 조건으로 REACT분류"용서를빈다"에 파생한다
;-------------------------------------------------
IF 구상기입체크() && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「그런 일 말해지면, 그만두고 싶지 않게 되어 버린다」
		RETURN 1
	ENDIF
	
	;REACT분류"소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
		PRINTFORMW 「들리지 않네요, 후후후」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 「그 억지도 언제까지 계속될까」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더하드하게
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("더하드하게")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「좋지 않은…… 이제(벌써) 뒤에는 물러날 수 없어요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
		PRINTFORMW 「아직 어딘지 부족한 것일까?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("용서를빈다") 
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTREACT(7)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「그런 일 말해지면…… 좀 더 괴롭히고 싶어져 버린다」
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「안돼, 허락해 주지 않기 때문에」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명을올린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("비명을올린다") && TFLAG:REACT부하 < 2
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 0
		PRINTFORMW 「좋은 소리군요…… 좀 더 우세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORMW 「견딜 수 없어요……」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;무서워한다
;기입 체크
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORMW 「그렇게 무서워하고 있으면, 나의 가학심을 일으킬 수 있어요」
		PRINTFORMW 「당신도 불행한 존재군요」
		RETURN 1
	ENDIF
ENDIF
;REACT7, 벌이라고선언한다

;─
;REACT8, 고함친다·개별 리액션
;─
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K29_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT8, 고함친다

;─
;REACT9, 기분나쁘게웃는다·개별 리액션
;─
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K29_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT분류"소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT9, 기분나쁘게웃는다
