;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	아큐 구상 회화계 액션 구상 파일
;
;	회화계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;회화계
;────────────────────────────────────
;TFLAG:ACT분류 == 0에 해당
;ACT9, 기분나쁘게웃는다는 이상계 ACT분류로부터 파생하는 경우가 있다
;범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;*************************************************
;ACT0, 지금의기분을얘기한다
;*************************************************
;·지금의 기분에 대해 들어 온다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_0
;=================================================
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 지금의기분을얘기한다
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && ONCEACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;조교 대상의Ｃ에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상Ｃ사용
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GROUPMATCH(TEQUIP:조교대상Ｃ사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상Ｃ사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 「아, 아아…… 당신님……!」
				PRINTFORMW 「아공의 것솔솔은…… 맛있습니까……?」
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 「아, 아아…… 당신님……!」
				PRINTFORMW 「아공의 것솔솔은…… 맛있습니까……?」
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 「허리를 비틀어 미끈미끈의 벽에 비비는 것이 좋아?」
				PRINTFORMW 「상하에 움직여, 톡톡 질구에 키스 하는 (분)편이 좋아합니까?」
				PRINTFORMW 「그렇지 않으면…… 양쪽 모두 한번에, 맛보고 싶은, 이라든지♪」
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 「아, 아아…… 당신님……!」
				PRINTFORMW 「아공의 것솔솔은…… 맛있습니까……?」
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 「아, 아아…… 당신님……!」
				PRINTFORMW 「아공의 것솔솔은…… 맛있습니까……?」
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 「엉덩이…… 힘들다아……」
				PRINTFORMW 「너무 아프거나 하지 않습니까아……?」
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;안면승마중
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TEQUIP:안면승마 || TEQUIP:안면승마애널
		IF CFLAG:153 == 0
			PRINTFORMW 「여자의 엉덩이에 깔린다, 라고 말하는 것은…… 어떤 기분입니까?」
			PRINTFORMW 「솔직하게 대답해 주어도, 당신님의 일을 싫어해 따위에 되지 않기 때문에……」
		ELSE
			PRINTFORMW 「…… 얼굴을 엉덩이부 쉬어 부적마다」
			PRINTFORMW 「마치 돼지인 것 같네요. 이봐요 브, 브」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 0)
		IF CFLAG:153 == 0
			PRINTFORMDW 자위하고 있는 장소에 토 숨결이 닿을 것 같을 정도에 얼굴을 접근해, 아큐는 홍조 한 얼굴로 관찰하고 있다.
			PRINTFORMW 「거기가, 당신님의 기분이 좋은 곳……」
			PRINTFORMW 「에에, 제대로 기억해요」
		ELSE
			PRINTFORMW 「…… 무엇인가, 업신여겨지는 일로 한층 더 흥분하고 있지 않습니까?」
			PRINTFORMW 「아니오, 알아요. 나에게는」
			PRINTFORMW 「이 변태」
			PRINTFORMDW 아큐는%CALLNAME:MASTER%가 자위하는 모양을 차가운 시선으로 내려다 보았다.
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF CFLAG:153 == 0
			PRINTFORMW 「가감(상태)는 어떠할는지요?」
			PRINTFORMW 「빨리?   느슨하고?   상냥하고?   그렇지 않으면……」
		ELSE
			PRINTFORMW 「분명하게 반성하고 있습니까?」
			PRINTFORMW 「뇌의…… 창의네, 어차피 페니스로 물건을 생각하고 있기 때문에」
			PRINTFORMW 「불알의 안쪽까지 제대로 공포를 새겨 붙여 주세요」
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT1, 자신에게로의기분을얘기한다
;*************************************************
;·자신의 일을 어떻게 생각할까 얘기한다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일 조교중에서는 억제된다
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_1
;=================================================
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 자신에게로의기분을얘기한다
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;조교 대상의Ｃ에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상Ｃ사용
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GROUPMATCH(TEQUIP:조교대상Ｃ사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상Ｃ사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;안면승마중
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TEQUIP:안면승마 || TEQUIP:안면승마애널
		IF CFLAG:153 == 0
			PRINTFORMW 「어떻습니까?   당신님 전용의 보지예요……」
			PRINTFORMW 「부드럽고, 따뜻해서, 냄새는…… 읏, 부끄럽습니다♪」
		ELSE
			PRINTFORMW 「자, 가축인것 같게 나의 엉덩이에 키스를 하면서 대답해 주세요」
			PRINTFORMW 「『당신은, 나의, 무엇일까요?』」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF CFLAG:153 == 0
			PRINTFORMW 「가슴이나 엉덩이도 만족에 없는, 어린 아이의 몸에서는 있습니다만……」
			PRINTFORMW 「사람을 "기분 좋고"할 방법에 대해서는, 잘 알고 있을 생각이지만」
			PRINTFORMW 「…… 어떻습니까?   나의 일은, 사랑해 받을 수 있습니까……?」
		ELSE
			PRINTFORMW 「얼마나 나의 신체가 작고, 약해도」
			PRINTFORMW 「당신은 그것 이하의 생물로 밖에 없는 것이에요?」
			PRINTFORMW 「자, 비참하게 허가를 청하는이라고 보면 어떻습니까?」
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT2, 성에대한이야기를한다
;*************************************************
;·조교자가 성에 대한 지식을 말하기 시작한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·조교자에게 성지식이 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_2
;=================================================
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT2, 성에대한이야기를한다
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;조교 대상의Ｃ에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상Ｃ사용
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GROUPMATCH(TEQUIP:조교대상Ｃ사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상Ｃ사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;안면승마중
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TEQUIP:안면승마 || TEQUIP:안면승마애널
		PRINTFORMW 「애무의 작법을 모릅니까?」
		PRINTFORMW 「손가락으로 딱 닫은 나의 째의 개를 넓혀, 혀로 정중하게 빨아 주세요」
		PRINTFORMW 「이따금, 와 달라붙거나 뭔가 해…… 후훗」
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「여기가 정소 상체…… 압니까?」
		PRINTFORMW 「말랑말랑 하고 있어, 뭐라고도 만지는 느낌이 좋아요」
		PRINTFORMDW 아큐는 옥대의 가죽을 이끌거나 늘리거나 하면서, 즐거운 듯이 그렇게 고했다.
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT3, 상냥하게위로한다
;*************************************************
;·조교자가 상냥한 말로 위로해 온다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_3
;=================================================
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 상냥하게위로한다
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;조교 대상의Ｃ에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상Ｃ사용
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GROUPMATCH(TEQUIP:조교대상Ｃ사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상Ｃ사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;안면승마중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:안면승마 || TEQUIP:안면승마애널
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT4, 엄격하게위협한다
;*************************************************
;·어려운 말로 위협한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_4
;=================================================
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화할 가능성이 있다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 엄격하게위협한다
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;조교 대상의Ｃ에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상Ｃ사용
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GROUPMATCH(TEQUIP:조교대상Ｃ사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상Ｃ사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT5, 의상의변경을요구한다
;*************************************************
;·조교자가 의상의 변경을 요구해 온다
;·조교 대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:쓰러트리기중에는 발생하지 않는다
;·TEQUIP:줄이 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_5
;=================================================
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%에 옷을 벗어라고 명령했다…"되고 있어 이것이 변화하는 일은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 옷을 벗도록(듯이) 명령
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && ONCEACT(0)
		PRINTFORMW 「자, 오늘도 시작합시다?」
		PRINTFORMW 「부부가 부모님이 될 때까지, 차분히, 충분히……」
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상으로 페니스 있어
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PENIS(MASTER)
		;발기하고 있다
		IF TCVAR:MASTER:발기도 >= 1000
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상으로 페니스 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && !PENIS(MASTER)
		;젖고 있다
		IF PALAM:MASTER:윤활 > 500
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「좀 더…… 당신님의 피부를 보여 주세요」
		PRINTFORMW 「딱 거듭해 아울러, 용해되고 싶습니다……」
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT5_1, 옷을 입도록(듯이) 명령
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 옷을 입도록(듯이) 명령
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	PRINTFORMW 
	RETURN LOCAL:1
ENDIF

;*************************************************
;ACT6, 합의를요구한다
;*************************************************
;-------------------------------------------------
;·처음을 받아도 괜찮을까 얘기한다
;·처음=처녀 or동정(양쪽 모두의 경우도 있다)
;·이미 합의를 얻고 있는 경우는 발생하지 않는다
;·조교 대상이 처녀도 동정도 아닌 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_6
;=================================================
;함수@TARGET_DESCRIPTION로 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;그리고는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 합의를요구한다
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상이 남자의 경우
	;기입 체크
	LOCAL = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:남자
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상이 후타나리의 경우
	;기입 체크
	LOCAL = 0
	;=================================================
	IF LOCAL && TALENT:MASTER:후타나리
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상이 여자 아이의 경우
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && !PENIS(MASTER)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;성별 마다의 나누어 쓰고를 하지 않으면, 여기만 써 둡시다
	;남자, 후타나리, 여자 아이 분기를 모두 묻었을 경우는 불필요합니다. 삭제합시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT7, 벌이라고선언한다
;*************************************************
;·지금부터 벌 한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:벌플래그는 서 있다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP:쓰러트리기중시에는 발생하지 않는다
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_7
;=================================================
;여기에서는 지문의 변화는 없다. 분기의 증감은 기호로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, 벌이라고선언한다
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;조교내 첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && ONCEACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;전COM가 「무뚝뚝하게대답한다/강하게대답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("무뚝뚝하게대답한다/강하게대답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;전COM가 「기분좋게해줘」（이 철면피! ）
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("기분좋게해줘")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;전COM가 「더하드하게」（이 돼지째! ）
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("더하드하게")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;전COM가 「용서를빈다」（허락해 받을 수 없었습니다）
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("용서를빈다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;전COM가 「자위하기시작한다」（인의 이야기를 들어라! ）
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("자위하기시작한다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT8, 고함친다
;*************************************************
;·큰 소리로고함친다
;·턴방침이 "소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:벌플래그에 관련되지 않고, 발생할 가능성이 있다（벌중에 한층 더고함친다가능성등 ）
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_8
;=================================================
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 큰 소리로 고함쳤다…"만으로 파생은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 고함친다
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && ONCEACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;전COM가 「무뚝뚝하게대답한다/강하게대답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("무뚝뚝하게대답한다/강하게대답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT9, 기분나쁘게웃는다
;*************************************************
;-------------------------------------------------
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·화제 『비웃는다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_9
;=================================================
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 기분나쁘게웃는다
;기입 체크（=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET)/TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;기승위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;조교 대상의Ｃ에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상Ｃ사용
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GROUPMATCH(TEQUIP:조교대상Ｃ사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상Ｃ사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER)/TEQUIP:페니스밴드등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
