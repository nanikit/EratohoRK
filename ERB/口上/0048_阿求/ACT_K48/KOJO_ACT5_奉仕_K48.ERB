;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	아구구상 봉사계 액션 구상 파일
;
;	봉사계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;─
;봉사계
;─
;·TFLAG:ACT(50=손으로 애무를 강제/51=입으로 애무를 강제/52=파이즈리/53=가랑이/55=다리 빨고 강제/56=이라마치오/57=가슴 애무 강제)
;·TFLAG:ACT 분류 == 5에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "봉사")로 줍는 것이 가능
;·조교 대상으로, 조교자의 신체를 애무하도록(듯이) 명령한다
;·기본적으로는 명령인 것으로, 이 단계에서는 따를지 어떨지 확정하고 있지 않다(ACT56, 이라마치오는 거부 할 수 없다)
;-------------------------------------------------
;·ACT54, 풋잡은 12/04/08판으로 숙청
;-------------------------------------------------

;*************************************************
;ACT50, 손으로 애무를 강제
;*************************************************
;·애무 강제/자위 강제/페니스 밴드 자위 강제
;·조교 대상(MASTER)으로, 손에 의한 애무를 강제한다
;·조교자에게 페니스가 있으면 반드시 자위 강제가 된다(페니스가 있으면 통상의 애무 강제는 일어나지 않는다)
;·조교자가 페니스 밴드 장착중은 페니스 밴드 자위 강제가 된다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "하드", "이상"이라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(줄/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교 대상이 따르는지 아닌지는 이 시점에서는 미정
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
;TFLAG:ACT 파생(0=애무 강제/1=자위 강제/2=페니스 밴드 자위 강제)
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_50
;=================================================
;·지문은 ACT 파생마다 분기. 각 ACT 파생내에서는 분기 없음
;·ACT 파생 0, 애무때만 쾌B/쾌ASOURCE 증가
;·그 외의 경우는 쾌C한정
;-------------------------------------------------

;이 구조로 첫회 분기에 떨어지면 파생에 관련되지 않고 한 번 뿐인 표시
;파생마다 첫회 분기를 마련하고 싶으면 각 ACT 파생내에서 처리하는 일을 추천합니다
;FIRSTACT(0)/FIRSTACT(1)/FIRSTACT(2)와 같이 나누어 주면 파생마다 첫회를 참조할 수 있습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50, 첫회
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;ACT 파생 1, 자위 강제
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;ACT 파생 2, 페니스 밴드 자위 강제
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 2
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;상층의 분기를 묻었을 경우는 TFLAG:ACT 파생 == 0으로 동의가 된다
	;여기에서만 끝마친다면, 범용적으로 사용할 수 있도록(듯이) 유의해야 할 개소
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_0, 애무 강제 「자신의 성기를 손으로 애무하라고 명령한다」
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;조교자가 갈 것 같다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_1, 자위 강제 「자신의 남성기를 손으로 잡아당기라고 명령한다」
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;조교자가 갈 것 같다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;페니스 밴드에의 애무는 쾌CSOURCE가 감소해, 쾌VSOURCE가 증가한다
;장착하고 있는 시점에서 대상이 처녀일 가능성은 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_2, 페니스 밴드 자위 강제 「페니스 밴드를 손으로 잡아 움직이라고 명령한다」
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;조교자가 갈 것 같다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT51, 입으로 애무를 강제
;*************************************************
;·쿤닐링구스/펠라치오/안면 승마/안면 승마 애널/
;·조교 대상으로, 입에 의한 애무를 강제한다
;·조교자가 후타나리의 경우, 조교 대상이 위로 돌리고라면 1/2으로 안면 승마, 그렇지 않으면 페라 강제가 된다
;·조교자에게 페니스가 없고, 조교 대상이 위로 돌리고라면 안면 승마가 된다
;·안면 승마가 선택되고 있었을 때, 안면 승마 애널에 변화하는 일이 있다
;·모든 조건으로부터 빠졌을 경우는 통상의 쿤닐링구스가 된다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각 목마) 장착중은 발생하지 않는다
;·조교자가 페니스 밴드를 장착하고 있는 경우는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
;TFLAG:ACT 파생(0=쿤닐링구스/1=페라/2=안면 승마/3=안면 승마 애널)
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_51
;=================================================

;우선 FIRSTACT(0)로 최상층에 첫회를 배치했습니다만, 각 ACT 파생내에서 처리하는 것도 좋을 것입니다
;FIRSTACT의 이벤트 번호를 입지 않도록 하면, 파생마다 첫회 판정을 할 수 있습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51, 첫회
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;ACT 파생이 페라 강제
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;ACT 파생이 안면 승마
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 2
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;ACT 파생이 안면 승마 애널
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 분기를 모두 묻었을 경우는 쿤닐링구스 강제 취급이 된다
	;파생마다 나누어 쓰지 않는 것이면, 이 개소를 범용적으로 사용할 수 있는 내용으로 한 다음 불필요한 분기를 삭제하면 좋을 것입니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;조교 대상의 C를 꾸짖으면서 안면 승마가 되는 시추에이션
;안면 승마가 되는 경우는 조교자의 페니스, 페니스 밴드등을 고려하는 필요없음
;필요에 따라서
;IF TEQUIP:페니스 밴드
;ELSEIF PENIS(MASTER)
;ELSE
;등의 분기를 마련해 사용해 주세요
;TEQUIP:조교 대상 C사용 == 1은 손으로 애무중
;TEQUIP:조교 대상 C사용 == 2는 입으로 애무중이 됩니다
;일단 입과 손 각각의 분기를 마련했습니다만
;조교자의 소질에 따라서는 입으로 애무가 대부분 나오지 않았다거나 하므로, 그 근처를 지켜본 다음 분기를 증감해 주세요
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상을 C를 꾸짖어지고 있다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TEQUIP:조교대상C사용 == 1 || TEQUIP:조교대상C사용 == 2
	;=================================================
	;질내 사정 후 3 턴 이내		질구로부터는 조금 전 충분히 짜낸 정액이 걸죽 넘쳐 나오고 있다…
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && INRANGE(TCVAR:MASTER:질내사정후경과턴, 1, 2)
		;구에서 애무중
		IF TEQUIP:조교대상C사용 == 2
			PRINTFORMW 
		;손으로 애무중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;안면 승마 애널 파생			눈앞에 실룩거리는 국문이 드러내지고 있다……
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 3
		;구에서 애무중
		IF TEQUIP:조교대상C사용 == 2
			PRINTFORMW 
		;손으로 애무중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;통상의 안면 승마를 상정@VAGINA_DESCRIPTION는 묘사계 함수
	;=================================================
	;그 외·조건 없음			무방비인%VAGINA_DESCRIPTION(TARGET)%가 드러내지고 있다……
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;구에서 애무중
		IF TEQUIP:조교대상C사용 == 2
			PRINTFORMW 
		;손으로 애무중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_0, 쿤닐링구스 강제
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;조교자가 갈 것 같다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;연속 실행
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「이 신체는 아직 숫처녀인 것으로…… 단단히풀어 주세요」
		PRINTFORMW 「그렇지 않으면, 곤란한 것은 나만이 아닙니다…… 서방님?」
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_1, 펠라치오
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;조교자가 갈 것 같다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;연속 실행
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_2, 안면 승마
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;조교자가 갈 것 같다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:갈것같다
		PRINTFORMW 「응읏…… 빨리, 빨리 빨아 주세요……」
		PRINTFORMW 「당신의 얼굴에 칠해…… 나……!」
		PRINTFORMDL
		PRINTFORMDW 아구는 크게 다리를 열면,%CALLNAME:MASTER%의 머리를 붙잡기 동글동글 클리토리스를 코 끝에 강압한다……
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;연속 실행
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;질내 사정 후 3 턴 이내
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && INRANGE(TCVAR:MASTER:질내사정후경과턴, 1, 2)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「귀하만 즐기고 있어…… 간사합니다」
			PRINTFORMW 「나의 것솔솔에도, 자비를 주시지 않습니까?」
			PRINTFORMDL
			PRINTFORMDW 그렇게 말하면 아구는, 다리를 크게 벌어져 위로 향해 되었다%CALLNAME:MASTER%에 과시한다.
			PRINTFORMDW 말랑 한 배로부터, 정도 좋게 부풀어 오른 스지까지가 뻔히 보임이 된다……
			PRINTFORMDW 아구는 그대로 입 위에 앉으면, 머뭇머뭇몸을 비틀었다.
			PRINTFORMDL
			PRINTFORMW 「아, 후…… 이것은, 꽤」
			PRINTFORMW 「배덕적이어, 조금, 즐거울지도……?」
			KFLAG:151 += 1
		ELSE
			PRINTFORMW 「완전히 둔해 빠지다. 적어도 자지가 쓰레기라면 입으로 즐겁게 하자, 라고 위 생각이 잘 미치지 않습니까?」
			PRINTFORMDL
			PRINTFORMDW 소, 라고 스스로의 성기를 강압하는 아구.
			PRINTFORMDW 체중은 생각했던 것보다도 가볍고, 살집이 얇은 일을 방문하게 한다.
			PRINTFORMDW 이지만 그 이상으로, 콧구멍에까지 비집고 들어간 달콤한 냄새가%CALLNAME:MASTER%의 머리의 심지를 구워 간다……
			PRINTFORMDL
			PRINTFORMW 「자, 조금은 성의를 보여 주세요」
			PRINTFORMW 「돼지는 돼지인것 같고, 날름날름 빨아 봉사합니다?」
			KFLAG:151 += 1
		ENDIF
	RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_3, 안면 승마 애널
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 3
	;쾌C 이외는 TCVAR:갈 것 같은 것은 주울 수 없기 때문에 PALAM로
	;=================================================
	;조교자의 쾌A가 일정 이상 「갈 것 같다」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:쾌A >= 8500
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;연속 실행
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「후훗, 조금 곤란하게 해 버립니다」
			PRINTFORMW 「여기는, 아이를 만드는데는 사용할 수 없습니다만……」
			PRINTFORMW 「귀하를 기쁘게 하는데는, 사용할 수 있기 때문에?」
			PRINTFORMDL
			PRINTFORMDW 아구는 앉으면서도, 머뭇머뭇허리를 움직여 엉덩이살에 얼굴을 묻게 한다.
			PRINTFORMDW 그 모습으로부터는, 다 숨길 수 있지 않는 흥미가 배어 나오고 있다……
			KFLAG:151 += 1
		ELSE
			PRINTFORMDW %CALLNAME:MASTER%의 입에 애널을 강압해, 아구는 기학적인 미소를 띄웠다.
			PRINTFORMDL
			PRINTFORMW 「그렇게 심한 냄새는 나지 않을 것입니다?   고기라든지 그다지 먹지않고」
			PRINTFORMW 「자, 마음껏 봉사 하셔 주세요, 아이의 엉덩이를 아주 좋아하는 마조돼지씨?」
			PRINTFORMW 「달라?   그런 일은 없을 것입니다. 육봉으로부터 내뿜는 기쁨이 다 숨겨져 있지 않아요?」
			PRINTFORMDL
			PRINTFORMDW 더욱 아구는 허리를 몇번이나 상하에 흔들어, 무리하게 봉사를 강요해 온다……
			KFLAG:151 += 1
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT52, 파이즈리
;*************************************************
;·조교 대상으로 파이즈리에서의 봉사를 강제한다
;·조교 대상이 남자의 경우는 발생하지 않는다
;·조교자에게 페니스가 없는 경우는 발생하지 않는다
;·턴 방침이 "이상"의 경우는 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(줄/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_52
;=================================================
;지문에 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT52, 파이즈리
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;첫회
	;기입 체크
	LOCAL = 0
	;=================================================
	IF LOCAL && FIRSTACT(0)
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자가 갈 것 같다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;조교 대상의 B절정이 가깝다고 하는 시추에이션
	;=================================================
	;조교 대상의 쾌B가 일정 이상
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:MASTER:쾌B >= 8500
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상이 거유
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:거유
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상이 빈유
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:빈유
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT53, 가랑이
;*************************************************
;·조교 대상으로 페니스 밴드, 혹은 페니스를 가랑이에 끼워 스치고라고 명령한다
;·조교자에게 봉이 없다(페니스/페니스 밴드) 경우는 발생하지 않는다
;·조교 대상으로 페니스가 있는 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "이상"의 경우는 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:삼각 목마 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교 대상으로 다리가 없으면 발생하지 않는다(현재 상태로서는도자고가 조교 대상인 경우만이라고 생각된다)
;·REACT 분류가 거부/날뛴다/도망치는인 경우는 거절 취급
;-------------------------------------------------
;TFLAG:ACT 파생(0=가랑이/1=페니스 밴드 가랑이)
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_53
;=================================================
;·안면 승마계, 자위계는 해제된다
;·지문은 ACT 파생으로 분기. 파생내에서는 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------

;우선 최상층에 배치했습니다만, ACT 파생내에서 처리해도 된다고 생각합니다
;FIRSTACT의 인수를 가지런히 하는지, FIRSTACT(0), FIRSTACT(1)로서 개별적으로 첫회 판정을 주울까는 쓰는 사람 나름
;━━━━━━━━━━━━━━━━━━━━━━━━
;첫회
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;페니스 밴드 가랑이 강제
	;기입 체크
	LOCAL = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;TFLAG:ACT 파생 == 1의 분기를 썼을 경우는 ACT 파생 == 0(가랑이 강제) 취급이 된다
	;상층의 분기를 방치해 여기에만 한다면, 범용적으로 사용할 수 있도록(듯이) 유의해야 할 개소
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;조교자에게 페니스가 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_0, 가랑이 강제
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;갈 것 같다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:갈것같다 || TCVAR:MASTER:갈것같다
		;조교자(TARGET)가 갈 것 같다
		IF TCVAR:TARGET:갈것같다
			PRINTFORMW 
		;조교 대상(MASTER)이 갈 것 같다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;윤활 충분히
		IF PALAM:MASTER:윤활 >= 500
			PRINTFORMW 
		;윤활 부족
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;조교자가 페니스 밴드 장착중의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_1, 페니스 밴드 가랑이 강제
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;갈 것 같다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:갈것같다 || TCVAR:MASTER:갈것같다
		;조교자(TARGET)가 갈 것 같다
		IF TCVAR:TARGET:갈것같다
			PRINTFORMW 
		;조교 대상(MASTER)이 갈 것 같다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;윤활 충분히
		IF PALAM:MASTER:윤활 >= 500
			PRINTFORMW 
		;윤활 부족
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT55, 다리 빨고 강제
;*************************************************
;·조교자가 다리를 빨라고 명령해 온다
;·이상계로부터 발생하는 경우 있어
;·턴 방침이 "소프트"라면 발생하지 않는다
;·오늘의 방침이 "러브러브"라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교자에게 다리가 없는 경우는 발생하지 않는다
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_55
;=================================================
;·지문에 분기 없음@TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT55, 다리 빨고 강제
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;전회 COM가 허가를 청했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("허가를 청한다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;벌모드
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:벌플래그
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상이 위로 돌리고
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:위로돌리고
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자가 양말을 신고 있다
	;(디폴트로 양말 신지 않은 캐릭터는 표시되지 않는다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:양말
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT56, 이라마치오
;*************************************************
;·명령하는 것은 아니고, 억지로 페니스를 입에 돌진한다
;·조교 대상의 반응은 미정이지만, 거절하는 일은 할 수 없다
;·가학계로부터 발생하는 경우 있어
;·턴 방침이 "노멀", "소프트"라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교자에게 페니스가 없는 경우는 발생하지 않는다(페니스 밴드에 의한 이라마치오는 없다)
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_56
;=================================================
;·자위중이면 해제된다
;·지문에 분기 없음. 지문은↓
;·%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 머리를 잡아, 난폭하게 %CALLNAME:MASTER%의 목까지 찔러 넣었다…
;-------------------------------------------------
;·조교자의 발기 정도는 TCVAR:발기로 참조할 수 있다
;·TCVAR:발기(0= 아직 부드럽다/1=서서히 경도를 더하고 있다/2=발기한/2이상=뒤로 젖혔다)적인 느낌으로
;-------------------------------------------------

;필요에 따라서 분기를 증감해 주세요
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT56, 이라마치오
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자가 갈 것 같다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상이 위로 돌리고
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:위로돌리고
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;*************************************************
;ACT57, 가슴 애무 강제
;*************************************************
;·조교 대상으로, 조교자의 가슴을 애무하도록(듯이) 강제한다
;·조교자가 남자의 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "이상"이라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:줄장착중은 입으로 가슴 애무 강제에 파생한다. 다만 볼개그를 장착중의 경우는 튕겨진다
;·볼개그가 없으면,1/2의 확률로 입으로 애무가 된다
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급
;·PALAM가 치정선행이나 반항 선행인가로"부끄러워서 봉사 할 수 없었다/싫어해 요구에 응하지 않았다"에 파생한다
;-------------------------------------------------
;TFLAG:ACT 파생(0=손으로 가슴 애무 강제/2=입으로 가슴 애무 강제)
;-------------------------------------------------
;=================================================
@KOJO_ACT_K48_57
;=================================================
;·지문은 ACT 파생으로 분기. 파생내에서는 분기 없음
;·@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;·안면 승마는 해제된다
;·REACT에 대해, 위로 돌리고가 아니고, ACT 파생이 입으로 애무가 아닌 경우,1/2그리고 뒤로부터 애무에 파생한다
;-------------------------------------------------

;우선 최상층에 배치했습니다만, ACT 파생내에서 처리해도 된다고 생각합니다
;FIRSTACT의 인수를 가지런히 하는지, FIRSTACT(0), FIRSTACT(1)로서 개별적으로 첫회 판정을 주울까는 쓰는 사람 나름
;━━━━━━━━━━━━━━━━━━━━━━━━
;첫회
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;ACT 파생 2, 입으로 가슴 애무를 강제
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT파생 == 2
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;TFLAG:ACT 파생 == 2 분기를 묻었을 경우는! TFLAG:ACT 파생 분기로서 기능
	;여기에서만 끝마친다면, 어디라도 사용할 수 있는 내용을
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_0, 손으로 가슴 애무를 강제
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;이전에는 체위마다야는은 규제되고 있었지만, 현재는 모든 체위로 발생한다
	;경우에 따라서는 엉뚱한 체제가 되지만, 이것은 현재 상태로서는 사양이라고 하는 일로
	;=================================================
	;조교자 삽입중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교중
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상 삽입중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교봉사중
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자가 갈 것 같다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:쾌B > 8500
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_2, 입으로 가슴 애무를 강제
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;이전에는 체위마다야는은 규제되고 있었지만, 현재는 모든 체위로 발생한다
	;경우에 따라서는 엉뚱한 체제가 되지만, 이것은 현재 상태로서는 사양이라고 하는 일로
	;=================================================
	;조교자 삽입중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교중
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상 삽입중
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:성교봉사중
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교자가 갈 것 같다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:쾌B > 8500
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
