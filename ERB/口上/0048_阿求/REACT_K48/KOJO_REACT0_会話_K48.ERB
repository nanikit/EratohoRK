;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	아구구상 회화계 개별 리액션 구상 파일
;
;	회화계 리액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;─
;회화계
;─
;·1231판보다 TFLAG:REACT부하가 도입되어 선택한 COM가 자연스러운 행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운 행동/1=무리가 없는 행동/2=무리를 한 행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;*************************************************
;REACT0, 지금의기분을얘기한다·개별 리액션
;*************************************************
;·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일 하고 싶다)
;-------------------------------------------------

;=================================================
@KOJO_REACT_K48_0
;=================================================

;REACT분류가 소극적 따르는 것으로, 리액션이 빈정거리는 이외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 0
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 질문에 분명히 답하는 것이 할 수 없었다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 말씨 나쁘게 질문에 답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 나가려 한 말을 삼켜 대답을 흐렸다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;리액션이 강하게응답한다의 경우
;REACT분류는 "거부"에 세트 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 어려운 말을 퍼부었다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 용기를 내 강하게 맞았다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 눈을 일등 해 입에서는 강하게 행동했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;리액션이 빈정거리는 것 경우
;REACT분류는 "거부"에 세트 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 여유의 미소로 어깨를 움츠리고 「지금의 기분은, 이라고 (들)묻어도」라고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 조금 생각하고 나서 「지금의 기분은, 이라고 (들)묻어도」라고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 간신히 야유 섞임에 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT분류가 적극적 따르는 것 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 3
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 함께 있어지는 것이 기쁘면 솔직하게 말했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 분명히 지금의 기분을 고했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 기분을 해치지 않게, 손질해 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT분류가 태연하게 따르는 것 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 4
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 그렇게 나쁘다고 생각하지는 않다고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「그저 나쁘지는 않다」라고 말했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 적극			시선을 피해, 나쁘지는 않아라고 중얼거렸다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 혐				힘껏의 허풍으로, 그저 나쁘지 않다고 대답했다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT분류가 받아들이는 것 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일 하고 싶다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 5
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 좀 더 기분 좋아지고 싶다고 했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 기분 좋은 일 하고 싶은 기분이라고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 아첨하도록(듯이) 감히 추잡한 행위를 졸랐다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------
[SKIPSTART]
통상의 경우 리액션의 지문은@TRAIN_MESSAGE2와@REACTION_MESSAGE로 처리됩니다만
이하의 부분은@TRAIN_MESSAGE2의 조건으로부터 빠지기 때문에(위해), @REACTION_MESSAGE에서만 처리됩니다
COM 분기에 있어서도 당연히 TFLAG:REACT파생는 기능하고, @REACTION_MESSAGE내에서도 REACT부하 마다 나누어 쓸 수 있습니다
SELECTCASE GET_COM_RANK()
	;자연스러운 행동
	CASE "자연스러운 행동"
		PRINTFORMW 
	;무리가 없는 행동
	CASE "무리가 없는 행동"
		PRINTFORMW 
	;무리를 한 행동
	CASE "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 싫어
		ELSE
			PRINTFORMW 
		ENDIF
ENDSELECT
상기와 같은 분기를 마련하는 일에 의해, 본체측의 변화에 대응하는 일은 가능합니다만
텐프레에 있어서 디폴트로 마련하려면 품이 많이 든다고 생각하는 까닭에, 기본적으로 REACT내의 COM 분기에는 테두리를 마련하지 않을 방침입니다
범용적인 내용으로 해도 좋아, 또 부분적으로 범용 리액션(COM)에 흘려도 좋다고 생각합니다
뭐 REACT부하 이외에도 확장하는 방법은 얼마든지 있거나 하므로, 그 근처의 판단은 각 구상 작자에게 맡기고 싶다고 생각합니다
3 BM/4 MB에서도 쓰기 때문에 어쨌든 생각되는 요소를 전부 넣어 줘! 적인 요망이 만약 있으면, 그 때는 또 생각하고 싶다와
[SKIPEND]

;지금의기분을얘기한다에서의 COM 침묵하고는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어 벌포인트가 가산된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 허가를청한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("허가를청한다") && TFLAG:REACT부하 < 2
	;=================================================
	;벌포인트 가산 파생:「의견을 내다니 좋은 담력이구나!」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("무서워한다")
	;무서워하는 것으로 벌포인트 가산이라고 하는 것도 이상한 생각이 듭니다만, 현상은 이것이 사양이라고 하는 일로
	;덧붙여서@COM7와@COM33는 내용이 완전히 같았다거나 합니다
	;=================================================
	;벌포인트 가산
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT0, 지금의기분을얘기한다


;*************************************************
;REACT1, 자신에게로의기분을얘기한다·개별 리액션
;*************************************************
;·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다! /2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_1
;=================================================

;REACT분류가 소극적 따르는 것 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 0
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 아무래도 싫다고는 말할 수 없었다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「싫지 않은… 일지도 모른다…」라고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 본심을 숨기도록(듯이) 눈을 숙여, 「싫지 않은… 일지도 모른다…」라고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;리액션이 강하게응답한다의 경우
;REACT분류는 "거부"에 세트 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 「좋아하게 될 수 있을 이유가 없다!」라고 강하게 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「그런 일이 있을 이유 없다!」라고 말했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 감정을 부정하도록(듯이) 고개를 저어 「싫다!」라고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;리액션이 빈정거리는 것 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 「좋아하게 될 수 있을 이유가 없다!」라고 강하게 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「그런 일이 있을 이유 없다!」라고 말했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 감정을 부정하도록(듯이) 고개를 저어 「싫다!」라고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 적은지, REACT분류가 태연하게 따른다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 3
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 「좀 더 좋아하게 되고 싶기 때문에, 심한 것은 하지 말아줘」라고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 「이제(벌써) 심한 일 하지 않으면, 좋아하게 될 수 있을지도 모른다」라고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 기분을 묻도록(듯이) 시선을 향한 후, 「심한 일만 하는 사람은 싫다」라고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 1000이상
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 4
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTREACT(4)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 붙임성 좋게 웃어 정말 좋아한다고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 분명히 좋아라고 고했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 억지웃음을 띄워, 좋아해라고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT분류가 받아들이는 것 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 5
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTREACT(5)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;자연스러운 행동
	;=================================================
	;REACT부하 0, 붙임성 좋게 웃어 정말 좋아한다고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리가 없는 행동
	;=================================================
	;REACT부하 1, 분명히 좋아라고 고했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;무리를 한 행동
	;=================================================
	;REACT부하 2, 억지웃음을 띄워, 좋아해라고 대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT부하분기를 모두 묻으면 불필요한 개소
	;반대로 상층의 분기를 쓰지 않는 것이면 여기서 보완시킵시다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, 상기의 COM, REACT분류에 속하지 않는 COM 마다의 분기
;-------------------------------------------------

;자신에게로의기분을얘기한다의 경우는 TFLAG:REACT파생 == 1(질문에 답하지 않는다) 취급이 되어 벌포인트가 가산된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 허가를청한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("허가를청한다")
	;=================================================
	;벌포인트 가산 파생:「의견을 내다니 좋은 담력이구나!」
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("무서워한다")
	;무서워하는 것으로 벌포인트 가산이라고 하는 것도 이상한 생각이 듭니다만, 현상은 이것이 사양이라고 하는 일로
	;덧붙여서@COM7와@COM33는 내용이 완전히 같았다거나 합니다
	;=================================================
	;벌포인트 가산
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT1, 자신에게로의기분을얘기한다


;*************************************************
;REACT2, 성에대한이야기를한다·개별 리액션
;*************************************************
;=================================================
@KOJO_REACT_K48_2
;=================================================
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
;  파생할 가능성이 있는 COM는 우선 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는 방법도 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 심약구응답한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("심약구응답한다")
	;REACT분류 "허가를청한다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 허가를청한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("허가를청한다")
	;=================================================
	;벌포인트 가산 파생:「의견을 내다니 좋은 담력이구나!」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게해
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분좋게해")
	;=================================================
	;벌포인트 가산 「이 철면피의 돼지째!」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("부끄러워한다")
	;TEQUIP에 의해 쾌SOURCE가 들어가 있는 경우에 파생
	;=================================================
	;REACT파생 1, 쾌락에 부끄러워한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT파생 0, 상황에 부끄러워한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("무서워한다")
	;무서워하는 것으로 벌포인트 가산이라고 하는 것도 이상한 생각이 듭니다만, 현상은 이것이 사양이라고 하는 일로
	;덧붙여서@COM7와@COM33는 내용이 완전히 같았다거나 합니다
	;=================================================
	;벌포인트 가산
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("자위하기시작한다")
	;=================================================
	;REACT파생 1, 「마음대로 자위 하지마」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「마음대로 자위 하고 있지만 좋은가」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT2, 성에대한이야기를한다

;*************************************************
;REACT3, 상냥하게위로한다·개별 리액션
;*************************************************
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
;  파생할 가능성이 있는 COM에는 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는 방법도 있습니다
;-------------------------------------------------
;  우선 회화계 COM를 모두 망라되어 있습니다만
;  10 접수대의 쾌락계 COM, 50 접수대 이후의 그 외 COM등도 상황에 따라서는 선택 가능하게 됩니다
;  필요에 따라서 분기를 증감해 주세요
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_3
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 심약구응답한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("심약구응답한다")
	;REACT분류 "허가를청한다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게응답한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("무뚝뚝하게응답한다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성 있게 응답한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("붙임성좋게응답한다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게응답한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("강하게응답한다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 좀더하드하게해
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("좀더하드하게해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 허가를청한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("허가를청한다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게해
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분좋게해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT3, 상냥하게위로한다

;*************************************************
;REACT4, 엄격하게위협한다·개별 리액션
;*************************************************
;=================================================
@KOJO_REACT_K48_4
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 심약구응답한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("심약구응답한다")
	;REACT분류 "허가를청한다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 허가를청한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("허가를청한다")
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTREACT(3)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("비명을올린다")
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;무서워하는 것으로 벌포인트 가산이라고 하는 것도 이상한 생각이 듭니다만, 현상은 이것이 사양이라고 하는 일로
	;덧붙여서@COM7와@COM33는 내용이 완전히 같았다거나 합니다
	;=================================================
	;벌포인트 가산
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT4, 엄격하게위협한다

;*************************************************
;REACT5, 의상의변경을요구한다·개별 리액션
;*************************************************
;·조교 대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가 "소극적 따른다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가 "적극적 따른다", "태연하게 따른다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_5
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 0
	;=================================================
	;한 장 벗었다(소극적 반응)
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("소극적 따른다/받아들인다")
		;-------------------------------------------------
		;마지막 한 장을 벗어 전라가 되었다
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && (TEQUIP:MASTER:악세사리 + TEQUIP:MASTER:양말 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신윗도리 + TEQUIP:MASTER:상반신윗도리 + TEQUIP:MASTER:전신윗도리) == 0
			;REACT분류받아들인다			전라가 되어, 기대와 흥분으로 피부가 달아오르는 것을 느꼈다
			IF IS_COMGRONAME("받아들인다")
				PRINTFORMW 
			;REACT분류소극적 따른다			어쩔 수 없이 마지막 한 장을 벗어, 전라가 되었다
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;완만한 움직임으로 옷을 한 장 벗어 던졌다
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && IS_COMGRONAME("받아들인다")
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		;-------------------------------------------------
		;어쩔 수 없이 옷을 한 장 벗어 던졌다
		;기입 체크
		LOCAL:2 = 1
		;-------------------------------------------------
		IF LOCAL:2 && IS_COMGRONAME("소극적 따른다")
			PRINTFORMW 「……?」
			PRINTFORMW 「절제 계속이라고, 좀 더 짐승과 같이 된다고 들었습니다만」
			PRINTFORMW 「의외로, 신사이군요?」
			PRINTFORMDW 아구는 미소지으면서 고개를 갸웃했다……
			RETURN LOCAL:2
		ENDIF
		
		;상층의 분기를 모두 묻었을 경우는 불필요한 개소
		;반대로 묻지 않는 경우는 이 개소가 보완이 책임을 다합니다
		;-------------------------------------------------
		;그 외·조건 없음
		;기입 체크
		LOCAL:2 = 1
		;-------------------------------------------------
		IF LOCAL:2
			PRINTFORMW 「초조하게 하도록(듯이), 라고 하는 것도 나쁘지 않네요」
			PRINTFORMW 「평상시, 초조하게함이라고 있을 뿐이기 때문에……」
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;2매 벗었다(적극적 반응)
	;기입 체크
	LOCAL = 1
	;=================================================
	IF LOCAL && IS_COMGRONAME("적극적 따른다/태연하게 따른다")
		;-------------------------------------------------
		;전라가 되었다
		;기입 체크
		LOCAL:2 = 1
		;-------------------------------------------------
		IF LOCAL:2 && (TEQUIP:MASTER:악세사리 + TEQUIP:MASTER:양말 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + TEQUIP:MASTER:하반신윗도리 + TEQUIP:MASTER:상반신윗도리 + TEQUIP:MASTER:전신윗도리) == 0
			PRINTFORMW 「뭐……♪」
			PRINTFORMW 「건강의 좋은 일……」
			PRINTFORMDW 아구는%CALLNAME:MASTER%의 사타구니의 근처를 응시하면서, 킥킥웃고 있다.
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;적극적으로 벗어 던졌다
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && IS_COMGRONAME("적극적 따른다")
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;솜씨 좋게 벗어 던졌다
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && IS_COMGRONAME("태연하게 따른다")
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;상층의 분기를 모두 묻었을 경우는 불필요한 개소
		;반대로 묻지 않는 경우는 이 개소가 보완이 책임을 다합니다
		;-------------------------------------------------
		;그 외·조건 없음
		;기입 체크
		LOCAL:2 = 1
		;-------------------------------------------------
		IF LOCAL:2
			PRINTFORMW 「후후, 빨리 개방해 받고 싶습니까?」
			PRINTFORMW 「그리고 아주 조금의 인내예요……」
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;벗지 않았다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("거부")
		;부끄러워서 벗겨지지 않았다
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
			PRINTFORMW 
		;싫어해 벗지 않았다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;한 장 입었다(소극적 반응)
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL && IS_COMGRONAME("소극적 따른다/받아들인다")
		PRINTFORMW 「에에, 좋다.」
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;2매 입었다(적극적 반응)
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("적극적 따른다/태연하게 따른다")
		PRINTFORMW 「에에, 좋다.」
		RETURN LOCAL:1
	ENDIF
	
	;지문에서는 "싫어해"라고 하는 취급
	;=================================================
	;입지 않았다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("거부")
		PRINTFORMW 「벌거벗은 채로로 있고 싶습니까……?」
		PRINTFORMW 「머지않아 부친이 될 때에, 그러면 조금 곤란해 버립니다만……」
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT5, 의상의변경을요구한다

;*************************************************
;REACT6, 합의를요구한다·개별 리액션
;*************************************************
;·조교 대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
;  수고를 싫어하지 않는 것이면, TALENT:MASTER:남자등으로 내부에서 나누는 것도 있을지도 모릅니다
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_6
;=================================================

;REACT분류가 소극적 따르는 것 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("소극적 따른다")
	;=================================================
	;각오를 결정한 것처럼, 작은 소리로 승낙했다(자연스러운 행동)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;작은 소리로 승낙했다(무리가 없는 행동)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 적극			부끄러움에 스러질 것 같은 소리로 승낙했다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 혐				분함에 얼굴을 비뚤어지게 하면서도 승낙했다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT분류가 태연하게 따르는 것 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 마음대로 하면 좋다고 대답했다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("태연하게 따른다")
	;=================================================
	;별로 싫지 않다고 말했다(자연스러운 행동)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;마음대로 하면 좋다고 대답했다(무리가 없는 행동)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 적극			하고 싶으면 좋다고 말했다
		IF COMCOR_POSI()
			PRINTFORMW 
		;실은 혐				마음대로 하면 좋다고 대답했다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT분류가 적극적 따르는 것 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("적극적 따른다")
	;=================================================
	;처음을 줄 수 있어 기쁘다고 대답했다(자연스러운 행동)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;뜻을 정해 바쳐도 괜찮다고 고했다(무리가 없는 행동)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;이제 지킬 수 없다면 적어도 상냥하게 여겨지도록(듯이)와 동의 했다(무리를 한 행동)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT분류가 받아들이는 것 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게해 주는 것이라면 처음을 주어도 괜찮다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("받아들인다")
	;=================================================
	;처음을 바치기 때문에 기분좋게해 갖고 싶다고 말했다(자연스러운 행동)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;기분좋게해 주는 것이라면 처음을 주어도 괜찮다고 대답했다(무리가 없는 행동)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;처음을 바치라고 한다면, 괴로움을 잊을 수 있을 정도로 기분좋게해라고 말했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서 "거부"에 세트 된다)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의하지 않는다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("거부")
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTREACT(4)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT6, 합의를요구한다

;*************************************************
;REACT7, 벌이라고선언한다·개별 리액션
;*************************************************
;·선언해도 벌이 발생한다고는 할 수 없다
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_7
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;COM"심약구응답한다"는 일정 조건으로 REACT분류 "허가를청한다"에 파생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 심약구응답한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("심약구응답한다") && TFLAG:REACT부하 < 2
	;REACT분류 "허가를청한다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("빈정거린다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 좀더하드하게해
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("좀더하드하게해")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 허가를청한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("허가를청한다") 
	;=================================================
	;첫회
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && FIRSTREACT(7)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명을올린다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("비명을올린다") && TFLAG:REACT부하 < 2
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;무서워한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;무서워하는 것으로 벌포인트 가산이라고 하는 것도 이상한 생각이 듭니다만, 현상은 이것이 사양이라고 하는 일로
	;덧붙여서@COM7와@COM33는 내용이 완전히 같았다거나 합니다
	;=================================================
	;벌포인트 가산
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT7, 벌이라고선언한다

;*************************************************
;REACT8, 고함치는·개별 리액션
;*************************************************
;=================================================
@KOJO_REACT_K48_8
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 심약구응답한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("심약구응답한다")
	;REACT분류 "허가를청한다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 허가를청한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("허가를청한다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("비명을올린다") && TFLAG:REACT부하 < 2
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;무서워하는 것으로 벌포인트 가산이라고 하는 것도 이상한 생각이 듭니다만, 현상은 이것이 사양이라고 하는 일로
	;덧붙여서@COM7와@COM33는 내용이 완전히 같았다거나 합니다
	;=================================================
	;벌포인트 가산
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT8, 고함친다

;*************************************************
;REACT9, 기분나쁘게웃는다·개별 리액션
;*************************************************
;=================================================
@KOJO_REACT_K48_9
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 심약구응답한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("심약구응답한다")
	;REACT분류 "허가를청한다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 허가를청한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("허가를청한다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("비명을올린다") && TFLAG:REACT부하 < 2
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("무서워한다") && TFLAG:REACT부하 < 2
	;무서워하는 것으로 벌포인트 가산이라고 하는 것도 이상한 생각이 듭니다만, 현상은 이것이 사양이라고 하는 일로
	;덧붙여서@COM7와@COM33는 내용이 완전히 같았다거나 합니다
	;=================================================
	;벌포인트 가산
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT9, 기분나쁘게웃는다
