;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	아큐 구상 애무계 개별 리액션 구상 파일
;
;	애무계 리액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;─
;애무계
;─
;· 각 REACT내의 나누는 방법으로서 TFLAG:REACT분류(IS_COMGRONAME)나 SELECTCOM(IS_NOWCOMNAME)가 생각됩니다
;·다만, 범위의 넓은 조건으로 주워 버리는 것은 기본적으로 추천하지 않습니다
;·ACT분류:애무계의 경우, 기본적으로 애무계 COM(COM10~COM16)가 선택지가 됩니다만
;·상황에 따라서는 그 외의 것이 선택지에 포함될 가능성도 있습니다
;·리액션(REACT)으로 처리되지 않았던 것은 범용 리액션(COM)으로 보완되므로
;·무리하게 모두 묻을 필요는 없습니다
;·필요라고 생각하는 것을 좁혀, COM와의 제휴를 생각해 효율 좋게 구축해야 합니다
;-------------------------------------------------
;· 각 REACT 및 ACT파생내는,&& IS_NOWCOMNAME든지,&& IS_COMGRONAME등의 조건으로 결정하고 쳐 합시다
;·여기에 조건 없음 분기가 있으면, 리액션에 관련되지 않고, 모두 주워 버려 범용 리액션에 흐르지 않습니다
;·요컨데, 상정된 시스템의 파괴에 연결됩니다
;-------------------------------------------------
;  기본적으로 지문은 TFLAG:REACT분류(받아들인다/적극적 따른다/태연하게 따른다/용서를빈다/소극적 따른다)로 분기 해,
;  각 REACT분류내에서 TFLAG:REACT부하(자연스러운 행동/무리가 없는 행동/무리를 한 행동)으로 파생
;  ACT_MESSAGE.ERB를 참조하면서의 작업을 추천합니다
;-------------------------------------------------
;  우선 ACT파생 마다 테두리를 분리해 있습니다만, 1개에 모은 다음 범용적인 기술로 수고를 줄인다고 하는 방법도 있습니다
;  정직, 방식은 얼마든지 있으므로, 이하에 나타내는 방법은 하나의 예라고 생각해 주세요
;-------------------------------------------------

;*************************************************
;REACT10, 손으로애무·개별 리액션
;*************************************************
;·TFLAG:ACT파생(0=애무/1=자위/2=페니스밴드 자위)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_10
;=================================================

;ACT파생 0에 대해서는 참고로서 쾌락계 COM를 모두 망라되어 있습니다
;필요에 따라서 분기를 증감해 주세요
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 애무
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적 따른다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")
		;쾌감에부끄러워한다
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;상황에부끄러워한다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("싫어한다")
		;싫어해 방해했다			REACT분류"날뛴다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;싫어했다					REACT분류"소극적 따른다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고매도한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("서투르다고매도한다")
		;강한척 해					REACT분류"소극적 따른다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 따른다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 따른다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적 따른다"
		ELSE
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분좋다！")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;ACT파생 1는 파생 0으로 같은 종류의 것을 다른 형태로 제시해 보겠습니다
;아무튼 이런 사용 방법도 있다고 하는 일로
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 자위
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적 따른다")
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")

		;=================================================
		;사정 직전
		;기입 체크
		LOCAL:1 = 1
		;=================================================
		IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
			PRINTFORMW 「…… 안됏, 행 얼굴짱과 보여 주지 않으면 싫습니닷」
			PRINTFORMW 「심술쟁이 하는 아이는, 도중에 멈추어 버려요?」
			PRINTFORMW 「--할 때도, 아큐에 얼굴 좀 보여주세요♪」
		ENDIF

		IF CFLAG:153 == 0		;CFLAG:153은 대입을 하지 않기 때문에 0밖에 되지 않습니다. ELSE 이후는 공수상이 됩니다@/L
			PRINTFORMW 「조금, 자극이 너무 강했습니까?」
			PRINTFORMW 「그렇지 않으면…… 후후, 권하고 계신다든가?」
		ELSE
			PRINTFORMW 「그 대로, 조금 반성하세요」
			PRINTFORMW 「남자의 존엄하게 관계되는 일인 것이기 때문에, 이 조루」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고매도한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("서투르다고매도한다")

		;=================================================
		;사정 직전
		;기입 체크
		LOCAL:1 = 1
		;=================================================
		IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
			PRINTFORMW 「
		ENDIF

		;강한척 해					REACT분류"소극적 따른다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 따른다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;수고를 싫어하지 않는 것이면, IF든지 3항이든지를 사용해 내부에서 나누어 쓰는 것도 있어
	;=================================================
	;REACT분류 4, 받아들인다＆COM, 쾌감을받아들인다/기분좋다！
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("받아들인다") && IS_NOWCOMNAME("쾌감을받아들인다/기분좋다！")
	
		;=================================================
		;사정 직전
		;기입 체크
		LOCAL:1 = 1
		;=================================================
		IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
			PRINTFORMW 「에에, 나에게 모두 맡겨 주세요」
			PRINTFORMW 「이 손가락과 말랑말랑의 손바닥에서…… 당신님의 특농정액, 빈틈없이 짜내 드립니다」
		ENDIF

		;소비 기력에 의한 파생
		IF CFLAG:153 == 0
			SELECTCASE GET_COM_RANK()
				;자연스러운 행동
				CASE "자연스러운 행동"
					PRINTFORMW 「뭐…… 칭찬의 말 감사합니다, 당신님」
					PRINTFORMW 「이대로, 아큐의 기술에 녹아 주세요♪」
				;무리가 없는 행동
				CASE "무리가 없는 행동"
					PRINTFORMW 「뭐…… 칭찬의 말 감사합니다, 당신님」
					PRINTFORMW 「이대로, 아큐의 기술에 녹아 주세요♪」
				;무리를 한 행동
				CASE "무리를 한 행동"
					PRINTFORMW 「조금, 솔직해져 받을 수 있던 것 같아…… 기쁩니다♪」
					PRINTFORMW 「물론, 앞으로도 쇼지를 세울 생각은 없지만」
			ENDSELECT
		ELSE
			PRINTFORMW 「…… 끔찍하게 허덕이고 있는 경우입니까?」
			PRINTFORMDW 아큐는 눈썹을 찡그려, 페니스를 훑는 손가락을 강하게 꽉 쥐었다……
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT분류 2, 태연하게 따른다＆COM, 겁없게웃는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("태연하게 따른다") && IS_NOWCOMNAME("겁없게웃는다")
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;COM가 방해/애원에 파생하면 COM에 흘린다
	;&& TFLAG:REACT파생 < 1 자체는 없어도 REACT분류의 결정 집에서 같은 결과를 얻을 수 있습니다만, 사양의 가시화라고 하는 일로
	;=================================================
	;REACT분류 2, 소극적 따른다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("소극적 따른다") && IS_NOWCOMNAME("싫어한다/쾌감을참는다") && TFLAG:REACT파생 < 1
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;ACT파생 0/1을 참고에 확장하든지, 통합하든지
;그런 (뜻)이유로 여기는 최소한으로 해 둡니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 페니스밴드 자위
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;COM12, 서투르다고매도한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("서투르다고매도한다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT10, 손으로애무

;*************************************************
;REACT11, 가슴애무·개별 리액션
;*************************************************
;·0203판 현재, 지문은 ACT파생 2, 몸싸움과 그 외로 분기
;·ACT파생 2, 몸싸움에 대해 REACT분류(소극적 따른다/태연하게 따른다/적극적 따른다/받아들인다) 이외는 "거부"취급
;-------------------------------------------------
;  파생마다 온전히 하면 수고가 심상치 않기 때문에, ACT파생분기의 상층에서 범용 처리하거나
;  각 ACT파생내의 COM 분기등은 일부의 것에 좁혀 버리는 것이 좋다고 생각합니다
;  또 ACT파생로 단락짓지 않고, 상당히기로 정리해 버리는 방법도 있습니다만, 공이 많이 든 일을 하기에는 적합하지 않다라고 생각합니다
;-------------------------------------------------
;·TFLAG:ACT파생(0=가슴애무/1=유두 들이마셔/2=몸싸움/3=유두 고문)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_11
;=================================================
;정해진 사용 방법 따위는 없기 때문에, 이하에 나타내는 것은 어디까지나 하나의 예라고 하는 일로
;분기의 증감, COM에 흘리는지 아닌지의 판단은 작자의 판단에 맡깁니다
;-------------------------------------------------
;-------------------------------------------------
;COM 단체의 분기 ACT파생의 상층에서 처리
;-------------------------------------------------

;ACT파생 2는 거동이 다르므로 조건으로 연주한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("부끄러워한다") && TFLAG:ACT파생 !=2
	;=================================================
	;쾌감에 대해서부끄러워한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1를 썼을 경우는 REACT파생 0, 상황에 대해서부끄러워한다취급
	;파생 1을 방치했을 경우는 여기서 보완해,&& TFLAG:REACT파생 < 1등으로 해서 COM에 흘려 버리는 것도 있어
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("싫어한다")
	;ACT파생 2의 경우는 ACT파생 2 분기내의 IS_COMGRONAME("거부")로 처리
	;=================================================
	;싫어해 방해했다(REACT분류 6, 날뛴다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1 && TFLAG:ACT파생 !=2
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1를 썼을 경우는 REACT파생 0, 싫어한(REACT분류 1, 소극적 따른다) 취급
	;파생 1을 방치했을 경우는 여기서 보완해,&& TFLAG:REACT파생 < 1등으로 해서 COM에 흘려 버리는 것도 있어
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;=================================================
;COM12, 서투르다고매도한다
;기입 체크
LOCAL = 0
;=================================================
IF LOCAL && IS_NOWCOMNAME("서투르다고매도한다")
	;=================================================
	;강한척 해(REACT분류:소극적 따른다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1를 썼을 경우는 REACT파생 0, 미숙한 테크닉에 대해서(REACT분류:태연하게 따른다) 취급
	;파생 1을 방치했을 경우는 여기서 보완해,&& TFLAG:REACT파생 < 1등으로 해서 COM에 흘려 버리는 것도 있어
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;-------------------------------------------------
;이하, ACT파생 마다의 분기
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 가슴애무
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 따른다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적 따른다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분좋다！")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;조교자에게 소질(유치/혀사용)이 없는 경우는 발생하는 확립은 낮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 유두 들이마셔
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 따른다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적 따른다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분좋다！")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;봉사계 COM가 선택지가 되는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 몸싸움
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;요구에 응하지 않았다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("거부")
		;부끄러워해
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
			PRINTFORMW 
		;싫어해
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;-----------------------------------------------------------------
	;이하, COM를 병렬화
	;수고를 싫어하지 않는 것이면, IF든지 3항이든지를 사용해 내부에서 나누어 쓰는 것도 있어
	;-----------------------------------------------------------------
	
	;=================================================
	;REACT분류 4, 받아들인다＆COM, 쾌감을받아들인다/기분좋다！
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("받아들인다") && IS_NOWCOMNAME("쾌감을받아들인다/기분좋다！")
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	;=================================================
	;REACT분류 2, 태연하게 따른다＆COM, 겁없게웃는다/태연하게한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("태연하게 따른다") && IS_NOWCOMNAME("겁없게웃는다/태연하게한다")
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT분류 2, 적극적 따른다＆COM, 적극적한다/애정을담아준다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("적극적 따른다") && IS_NOWCOMNAME("적극적한다/애정을담아준다")
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;COM 싫어해 방해/애원에 파생하면 COM에 흘린다
	;&& TFLAG:REACT파생 < 1 자체는 없어도 REACT분류의 결정 집에서 같은 결과를 얻을 수 있습니다만, 사양의 가시화라고 하는 일로
	;=================================================
	;REACT분류 2, 소극적 따른다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("소극적 따른다") && IS_NOWCOMNAME("싫어한다/쾌감을참는다") && TFLAG:REACT파생 < 1
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;조교자에게 소질(유치/혀사용)이 없는 경우는 발생하는 확립이 높다
;조교 대상이 남자의 경우는 여기에 밖에 떨어지지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 3, 유두 고문
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 3
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 따른다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적 따른다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분좋다！")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT11, 가슴애무

;*************************************************
;REACT12, 입으로애무·개별 리액션
;*************************************************
;  파생마다 단락지어 있습니다만, 예를 들면 ACT파생 1만 써 뒤는 범용에 던진다고 하는 일도 할 수 있습니다
;-------------------------------------------------
;·TFLAG:ACT파생(0=쿤닐링구스/1=페라)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_12
;=================================================

;ACT파생 0에 대해서는 참고로서 쾌락계 COM를 모두 망라되어 있습니다
;필요에 따라서 분기를 증감, 없고 구조를 개변해 사용해 주세요
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 쿤닐링구스
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적 따른다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")
		;쾌감에부끄러워한다
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;상황에부끄러워한다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("싫어한다")
		;싫어해 방해했다			REACT분류"날뛴다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;싫어했다					REACT분류"소극적 따른다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고매도한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("서투르다고매도한다")
		;강한척 해					REACT분류"소극적 따른다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 따른다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 따른다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적 따른다"
		ELSE
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분좋다！")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;ACT파생 1는 파생 0으로 같은 종류의 것을 다른 형태로 제시해 보겠습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 페라
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적 따른다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")
		;쾌감에부끄러워한다
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;상황에부끄러워한다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고매도한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("서투르다고매도한다")
		;강한척 해					REACT분류"소극적 따른다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 따른다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;수고를 싫어하지 않는 것이면, IF든지 3항이든지를 사용해 내부에서 나누어 쓰는 것도 있어
	;=================================================
	;REACT분류 4, 받아들인다＆COM, 쾌감을받아들인다/기분좋다！
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("받아들인다") && IS_NOWCOMNAME("쾌감을받아들인다/기분좋다！")
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT분류 2, 태연하게 따른다＆COM, 겁없게웃는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("태연하게 따른다") && IS_NOWCOMNAME("겁없게웃는다")
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
	
	;COM가 방해/애원에 파생하면 COM에 흘린다
	;&& TFLAG:REACT파생 < 1 자체는 없어도 REACT분류의 결정 집에서 같은 결과를 얻을 수 있습니다만, 사양의 가시화라고 하는 일로
	;=================================================
	;REACT분류 2, 소극적 따른다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("소극적 따른다") && IS_NOWCOMNAME("싫어한다/쾌감을참는다") && TFLAG:REACT파생 < 1
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT12, 입으로애무

;*************************************************
;REACT13, 애널애무·개별 리액션
;*************************************************
;·지문은 ACT파생로 분기. ACT파생 1, 2 분기내의 파생 없음
;·ACT파생 0, 애널애무만 REACT분류로 파생 처리된다
;-------------------------------------------------
;·TFLAG:ACT파생(0=애널애무/1=전립선자극/2=애널바이브 출납)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_13
;=================================================

;ACT파생 0에 대해서는 참고로서 쾌락계 COM를 모두 망라되어 있습니다
;필요에 따라서 분기를 증감, 없고 구조를 개변해 사용해 주세요
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 애널애무
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적 따른다")
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("부끄러워한다")

		;=================================================
		;사정 직전
		;기입 체크
		LOCAL:1 = 1
		;=================================================
		IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
			PRINTFORMW 「괜찮아, 엉덩이로 가 버리는 일은 전혀 부끄러운 일이 아니에요……♪」
			PRINTFORMW 「왜냐하면 히에다의 기술이 너무 기분 좋은 것이 나쁩니다 것이군요?」
			PRINTFORMW 「당신님은 조금도 나쁘지 않으니까, 부끄러워하지 않고 기분 좋아져……」
		ENDIF

		IF CFLAG:153 == 0
			PRINTFORMW 「역시, 엉덩이를 누군가에게 보여지는 것은 부끄럽지요……」
			PRINTFORMW 「그렇지만, 여자 아이는 모두 하고 있는 일이랍니다?」
			PRINTFORMW 「그러니까, 이렇게 해 만지작거려지는 것도…… 점점, 기분 좋게 되어 갈테니까♪」
		ELSE
			PRINTFORMW 「눈물지을 만큼 부끄럽다면, 어째서 페니스가 발기하고 있습니까?」
			PRINTFORMW 「결국, 당신과 같은게 히에다에 반항한다니 할 수 있을 이유가 없어요」
			PRINTFORMW 「계집아이 한사람에게 엉덩이 구멍 후비어져, 일어서는 것조차 할 수 없는 걸요」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("싫어한다")
		;싫어해 방해했다			REACT분류"날뛴다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;싫어했다					REACT분류"소극적 따른다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고매도한다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("서투르다고매도한다")
		;강한척 해					REACT분류"소극적 따른다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 따른다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 따른다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적 따른다"
		ELSE
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다/기분좋다！")

		;=================================================
		;사정 직전
		;기입 체크
		LOCAL:1 = 1
		;=================================================
		IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
			PRINTFORMW 「에에, 자지의 뒤편으로부터 려 하기 시작해지는 쾌감은, 있고 와 팔짱 짊어져?」
			PRINTFORMW 「히에다의 기술로 우무 사정하는 것을 기억해 버려서는, 버릇이 되어도 이상하지 않아요……?」
			PRINTFORMW 「엉덩이를 후비어지지 않은 날은 어딘지 부족해 근질근질 해, 끝에는 자신으로부터 엉덩이에 손가락을 넣고이고라고……」
			PRINTFORMW 「만약 그렇게 되어도, 아큐는 당신님의 일을 사랑하고 있으니까요?」
		ENDIF

		IF CFLAG:153 == 0
			PRINTFORMW 「후후…… 당신님도 완전히, 히에다의 손가락기술의 포로가 되어 받을 수 있던 것 같아……」
			PRINTFORMW 「전립선에, 아큐가 새긴 시르시……」
			PRINTFORMW 「이것으로, 만약 어디엔가 가 버리는 일이 있어도, 분명하게 나의 곁으로 돌아올 수 있네요♪」
		ELSE
			PRINTFORMW 「결국 저항하는 일도 하지 않게 되었습니까……」
			PRINTFORMW 「아니오, 상관하지 않아요. 아무쪼록, 주어지는 대로 탐내 하셔 주세요」
			PRINTFORMW 「이쪽도, 그럴 생각으로 예의 범절 하도록 해 받습니다」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("기분좋다！")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;조교 대상이 남자의 경우에만 파생
;ACT파생 0를 참고에 확장하든지, 통합하든지
;그런 (뜻)이유로 여기는 최소한으로 해 둡니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 전립선자극
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;COM14, 쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적 따른다"
		ELSE
			PRINTFORMW
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다/COM16, 기분좋다！
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다/기분좋다！")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;ACT파생 0를 참고에 확장하든지, 통합하든지
;그런 (뜻)이유로 여기는 최소한으로 해 둡니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 애널바이브 출납
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;COM14, 쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적 따른다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다/COM16, 기분좋다！
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다/기분좋다！")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT13, 애널애무

;*************************************************
;REACT14, 애널빨고·개별 리액션
;*************************************************
;·지문은 REACT분류로 분기
;·ACT의 파생 없음
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_14
;=================================================
;ACT파생는 없기 때문에 COM 분기에서 완전하게 단락짓습니다
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류"소극적 따른다")
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("부끄러워한다")
	;쾌감에부끄러워한다
	IF TFLAG:REACT파생 == 1
		PRINTFORMW 
	;상황에부끄러워한다
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("싫어한다")
	;싫어해 방해했다			REACT분류"날뛴다"
	IF TFLAG:REACT파생 == 1
		PRINTFORMW 
	;싫어했다					REACT분류"소극적 따른다"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고매도한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("서투르다고매도한다")
	;강한척 해					REACT분류"소극적 따른다"
	IF TFLAG:REACT파생 == 1
		PRINTFORMW 
	;미숙한 테크닉에 대해서	REACT분류"태연하게 따른다"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다(REACT분류"태연하게 따른다")
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("겁없게웃는다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을참는다")
	;파생:애원					REACT분류"용서를빈다"
	IF TFLAG:REACT파생 == 1
		PRINTFORMW 
	;파생:내구					REACT분류"소극적 따른다"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분좋다！")
	PRINTFORMW 
	RETURN LOCAL
ENDIF
;REACT14, 애널빨고

;*************************************************
;REACT15, 키스·개별 리액션
;*************************************************
;·지문은 REACT분류로 분기
;·ACT의 파생 없음
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_15
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을참는다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을참는다")
	;=================================================
	;REACT파생 1, 애원(REACT분류"용서를빈다")
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT파생 1 분기를 썼을 경우는 REACT파생 0(파생:내구) 취급
	;IF LOCAL:1 && TFLAG:REACT파생 < 1등으로 해서, REACT의 파생 1을 삭제해 COM로 처리하는 것도 있어
	;=================================================
	;그 외·REACT부하파생
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN LOCAL:1
	ENDIF
ENDIF

;1개로 정리한 다음 && TFLAG:REACT부하 < 1등으로 해서
;특정의 파생은 COM로 처리라든지, 그런 일도 가능합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을받아들인다")
	;소비 기력에 의한 파생
	SELECTCASE GET_COM_RANK()
		;자연스러운 행동
		CASE "자연스러운 행동"
			PRINTFORMW 
		;무리가 없는 행동
		CASE "무리가 없는 행동"
			PRINTFORMW 
		;무리를 한 행동
		CASE "무리를 한 행동"
			PRINTFORMW 
	ENDSELECT
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("기분좋다！")
	;소비 기력에 의한 파생
	SELECTCASE GET_COM_RANK()
		;자연스러운 행동
		CASE "자연스러운 행동"
			PRINTFORMW 
		;무리가 없는 행동
		CASE "무리가 없는 행동"
			PRINTFORMW 
		;무리를 한 행동
		CASE "무리를 한 행동"
			PRINTFORMW 
	ENDSELECT
	RETURN LOCAL
ENDIF
;REACT15, 키스

;*************************************************
;REACT16, 성기를서로켠다·개별 리액션
;*************************************************
;TFLAG:ACT파생(=0, 조개 우려라/=1, 가랑이/=2, 페니스밴드 가랑이)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_16
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 조개 우려라
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 가랑이
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 페니스밴드 가랑이
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT16, 성기를서로켠다

;*************************************************
;REACT17, 파이즈리한다·개별 리액션
;*************************************************
;·지문은 REACT분류로 분기
;·ACT의 파생 없음
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_17
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을참는다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL&& IS_NOWCOMNAME("쾌감을참는다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을받아들인다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF
;REACT17, 파이즈리한다

;*************************************************
;REACT18, 풋잡한다·개별 리액션 FIRSTTIME(218)
;*************************************************
;=================================================
@KOJO_REACT_K48_18
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을참는다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을참는다")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌감을받아들인다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("쾌감을받아들인다")

		;=================================================
		;사정 직전
		;기입 체크
		LOCAL:1 = 1
		;=================================================
		IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
			PRINTFORMW 「이런이런…… 좋습니까?   이대로라면, 당신님에게 정액이 닥쳐 버려요?」
			PRINTFORMW 「양 다리의, 10개의 손가락의 사이에 질척질척 휘저어져…… 사정해, 좋네요?」
			PRINTFORMW 「알았습니다…… 아무쪼록, 건강한 자리 짤 수 있는 것를 하늘 높게 날아 오르게 해 주세요」
			PRINTFORMW 「아큐에 교배할 때의 기세를, 관찰시키며……♪」
		ENDIF

		IF CFLAG:153 == 0
			PRINTFORMW 「완전히 버릇이 되어 버렸어요, 어린이 발에 밟혀」
			PRINTFORMW 「그다지 밖에 돌아 다니지 않기 때문에, 아큐의 발바닥은 말랑말랑해 부드러울 것입니다?」
			PRINTFORMW 「마치 이렇게 해, 자지에 칠하기 위해서(때문에) 되어있을까의 모양……♪」
		ELSE
			PRINTFORMW 「옥대를 유녀(어린)의 다리로 좋을대로 만지작거려져, 저항할 생각도 없습니까?」
			PRINTFORMW 「그렇지 않으면, 내가 실제로 심한 일을 하지 않는다고, 빨고 잘라 있는 것일까요」
			PRINTFORMW 「확실히, 무너져 곤란한 것은 나도이지만…… 이렇게도 무포상이라고, 먼가에 씌인다 라고 있군요……?   킥킥」
		ENDIF 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명을 지른다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("비명을올린다")
		IF CFLAG:153 == 0
			PRINTFORMW 「…… 안됩니다, 그렇게 사랑스러운 소리를 지르면……」
			PRINTFORMW 「그렇게 할 생각 같은거 없는데, 좀 더 괴롭히고 싶어져 버리지 않습니까……♪」
		ELSE
			PRINTFORMW 「도움은 오지 않아요?   집안의 사람이라면, 내가 무엇을 하고 있을까 파악하고 있고」
			PRINTFORMW 「…… 아 그렇다, 이번 어느 정도 조교가 진행되었는지 집의 사람에게 보이게 해 도는 것도 나쁘지 않네요……」
		ENDIF
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;날뛴다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("날뛴다") && !GROUPMATCH(TFLAG:REACT파생, 0, 1)
	PRINTFORMW 
	RETURN LOCAL
ENDIF
;REACT18, 풋잡한다

;*************************************************
;REACT19, 손가락삽입해라
;*************************************************
;TFLAG:ACT파생(0=손가락삽입해라/1=G스팟 자극/2=바이브 출납)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_19
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 손가락삽입해라
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, G스팟 자극
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 바이브 출납
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 2
	;=================================================
	;조교 대상이 후타나리
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:후타나리
		;-------------------------------------------------
		;쾌감을받아들인다
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
			PRINTFORMW
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;쾌감을참는다
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && IS_NOWCOMNAME("쾌감을참는다")
			PRINTFORMW
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;조교 대상으로 페니스 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && !PENIS(MASTER)
		;-------------------------------------------------
		;쾌감을받아들인다
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
			PRINTFORMW
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;쾌감을참는다
		;기입 체크
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && IS_NOWCOMNAME("쾌감을참는다")
			PRINTFORMW
			RETURN LOCAL:2
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT19_1, G스팟 자극
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0 ;off
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
	;=================================================
	;조교 대상이 후타나리
	;기입 체크
	LOCAL:1 = 0 ;off
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:후타나리
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;조교 대상으로 페니스 없음
	;기입 체크
	LOCAL:1 = 0 ;off
	;=================================================
	IF LOCAL:1 && !PENIS(MASTER)
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT19_0, 손가락삽입해라
;기입 체크(=0, 비표시, 1, 표시, 2, 표시해 미약등 구상을 스킵)
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;쾌감을받아들인다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;쾌감을참는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_NOWCOMNAME("쾌감을참는다")
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;*************************************************
;REACT27, 밀어넘어뜨린다·개별 리액션
;*************************************************
;TFLAG:ACT파생(=0, 밀어넘어뜨린다/=1, 밀어넘어뜨리기 종료)
;-------------------------------------------------
;=================================================
@KOJO_REACT_K48_27
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 밀어넘어뜨린다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT파생
	;=================================================
	;REACT분류받아들인다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("받아들인다")
		PRINTFORMW 「…… 사랑하고 있습니다, 당신님……」
		RETURN LOCAL:1
	ENDIF
	;=================================================
	;REACT분류적극적 따르는/태연하게 따른다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("적극적 따른다/태연하게 따른다")
		PRINTFORMW 「이렇게 해, 저승에까지 붙어 와 받을 수 있으면……」
		PRINTFORMW 「정말, 과연 사치가 지나는군요」
		RETURN LOCAL:1
	ENDIF

	;=================================================
	;REACT분류용서를빈다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("용서를빈다")
		IF CFLAG:153 == 0
			PRINTFORMW 「그런…… 싫습니까?」
			PRINTFORMW 「요괴가 아닙니다 해, 취해 먹을 생각은 없는데……」
		ELSE
			PRINTFORMW 「이것은 벌이에요?   종마를 도움이 될 수 없게 되면, 도살장에 보내는 것은 당연하겠지요?」
			PRINTFORMW 「그것이 싫으면…… 알고 있네요?」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT분류소극적 따른다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("소극적 따른다")
		IF CFLAG:153 == 0
			PRINTFORMW 「아직 조금, 모두 받아들여 받기에는 딱딱할까……」
			PRINTFORMW 「그런데도…… 천천히, 사랑을 서로 확인합시다, 당신님」
		ELSE
			PRINTFORMW 「지금부터, 당신의 자지에 연속적으로 자극을 더합니다」
			PRINTFORMW 「이번은 제대로, 인내 할 수 있는 곳을 보여 주세요, 종마씨」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 밀어넘어뜨리기 종료
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT파생 == 1
ENDIF
