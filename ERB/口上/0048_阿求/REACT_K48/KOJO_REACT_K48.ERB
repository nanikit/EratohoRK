;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;		개별 리액션 구상
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;*************************************************
;개별 리액션 구상
;*************************************************
;·조교 대상이 커맨드를 결정한 뒤로 표시. 소분기의 증감은 자유롭게
;·조교자 액션별로 준비하는 고정의 리액션 구상
;·「@KOJO_REACT_KX1_(행동 번호)」로 행동마다의 함수를 작성하고 쓴다. 함수마다 코피페 해 개변을 추천
;=================================================
;반환값 관련
;·RETURN 1으로 구상을 표시한 취급이 되어, 그 이후를 부르지 않는다. 반환값에 주의하는 것
;·개별 리액션을 표시하지 않았던 경우는 「범용 리액션 구상」이 불린다
;·표시했는가 어떤가는 RETURN의 반환값을 보므로, 범용에 흘리는 경우는 RETURN 0이 되도록(듯이)
;=================================================
;
;간이 플로우
;    표시 타이밍·조교 대상의 리액션(커맨드·반응)
;        액션 번호별의, 개별 리액션 구상(지금 여기)
;            표시하지 않았던 경우, 커맨드 번호별의 범용 리액션 구상
;                표시하지 않았던 경우, 통괄 범용 리액션 구상
;
;여기서 RETURN 1하면 범용을 일절 부르지 않기 때문에, 특히 신중한 운용이 필요.
;범용 리액션을 충실시키면, 회화계 이외는 일절 쓰지 않아도 별로 좋다.
;=================================================
