;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;		[아구]구상 범용 리액션 구상 파일
;
;	개별 리액션(REACT)으로부터 빠져 온 것을 해당 COM로 처리하는 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;=====================================================================
;범용 리액션 구상
;=====================================================================
;·개별 리액션 구상을 표시하지 않았던 경우에 표시
;·KOJO_REACT_KX1_Y1의 RESULT가 0이었을 경우, 범용 리액션이 불려 간다
;·REACT로 표시한지 아닌지는 RESULT로 판정되므로, REACT로 표시해 RETURN 0으로 한층 더 범용도 표시시키는 일도 가능한 것이긴 함
;·KOJO_COM_KX1_Y1로부터 RESULT 0이 돌아가면 통괄 범용 리액션(KOJO_REACT_KX)이 불려 간다
;---------------------------------------------------------------------
;간이 플로우
;    표시 타이밍·조교 대상의 리액션(커맨드·반응)
;        액션 번호별의, 개별 리액션 구상
;            표시하지 않았던 경우, 커맨드 번호별의 범용 리액션 구상←(지금 여기)
;                표시하지 않았던 경우, 통괄 범용 리액션 구상
;---------------------------------------------------------------------
;관련 변수/함수류…
;
;ACT분류계
;TFLAG:ACT분류 이 턴의 조교 메뉴(0=회화/1=애무/2=도구/3=성교/4=수치/5=봉사/6=가학/7=이상/8=사역/9=휴게/10=성교 봉사)
;GET_ACTM ACT 번호로부터 ACT분류번호를 취득하는 함수
;MENUMATCH 지정한 ACT분류에 포함되어 있으면 진을 돌려준다(회화/애무/도구/성교/수치/봉사/가학/이상/사역/휴게/성적봉사)
;
;COM계
;TFLAG:REACT파생 각 SELECTCOM의 파생. COM마다 파생 내용은 다르다
;TFLAG:REACT부하 선택한 COM의 기력 소비를 판별한다(0=자연스러운 행동/2=무리가 없는 행동/무리를 한 행동)
;COMCOR_POSI 주로 소극적 따르는 계 COM의 기력 소비가 큰 때(TFLAG:REACT부하 == 2인 경우)에, COM 보정치가 호의적인가 어떤가를 판단
;
;그 외
;TFLAG:REACT인상 각 SELECTCOM의 상대에게로의 심상(0=영향없음/1=좋은인상/2=약간좋은인상/3=나쁜인상/4=약간나쁜인상/5=0에서멀어짐/6=0에가까워짐)
;TFLAG:벌포인트( 100모이면 벌개시)
;FIRSTCOM  첫회 범용 리액션 판정 함수  SELECTCOM를 자동으로 참조하므로, 현재 선택한 COM 마다 번호가 독립해 보존된다
;ONCECOM  조교내 첫회 판정용 함수. FIRSTTIME계와 달라 조교전에 초기화된다. 기본적인 사양은 FIRSTCOM와 같이
;RANDCOM(AUTORAND계)  RAND 처리로 이벤트 종류마다의 번호를 돌려주지만, 전종 볼 때까지 같은 숫자는 돌려주지 않는다. 모두 표시했을 때에 초기화.
;
;---------------------------------------------------------------------

;=====================================================================
;※ACT분류와 그 파생에 대해
;
;COM에 따라서는 발생하는 ACT분류가 복수에 걸칩니다
;각 COM내를 TFLAG:ACT분류로 나누어 주는 일에 의해, 보다 정확하게 상황을 참조하는 것이 가능합니다
;다만, ACT분류자체도 파생하는 것이 있어, 이 경우, TFLAG:ACT분류에서는 파생 상황을 참조 할 수 있지 못하고, 약간 이상한 일이 됩니다
;
;예를 들면…, ACT55, 다리빨고강제는 본래 ACT분류:봉사계입니다만, 이상계로부터 발생하는 경우가 있어,
;TFLAG:ACT분류의 단락에서는, 상정 그대로의 구상이 표시되지 않는다고 말하는 것이 생각됩니다
;
;거기서 TFLAG:ACT분류는 아니고, @MENUMATCH든지@GET_ACTM로 ACT분류 마다의 분기를 마련해 주면, 파생처를 참조하는 것이 가능하게 되어
;실제로 행해지고 있는 ACT분류로 처리되게 됩니다
;IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "봉사")와 같은 테두리를 마련해 주면, 위에 올린 것 같은 상황에도 대응이 가능합니다
;MENUMATCH의 분류 마다의 정의에 대해서는 이 파일의 28행째를 참조해 주세요
;
;ACT분류파생 일람…
;(각 수치는 ACT 번호)
;회화(기분 0/호의 1/지식 2/농락해라 3/위협한다 4/의상 5/합의 6/처분 7/고함친다 8/비웃는다 9/매도 66)
;애무(손으로 10/가슴을 11/입으로 12/A손 13/A구 14/키스 15/C찰 16/B찰 17/다리로 18/로터 20/압도 27)
;도구(로터 20/바이 21/A바이 22/A비 23/C사랑 24/니풀 25/관장 68)
;성교(정상 30/후배 31/승마 32/대면 33/배면 34/AS35)
;수치(자위 40/개장 41/보이고 42/대거울 43/야외 44/방뇨 45/코스(미실장)46/방치 73)
;봉사(손으로 50/입으로 51/가슴으로 52/가랑이 53/족지 55/이라 56/승마 32/가슴을 57)
;가학(손바닥 60/회초리 61/바늘 62/줄 63/눈가림 64/입마개 65/매도 66/목마 67/관장 68/이라 56)
;이상(Fist70/AFi71/양Fi72/방치 73/공중 74/비웃는다 9/다리로 18/족지 55/목마 67)
;사역(미실장)
;휴게(휴일 90/복약 91/치료 92)
;성교(봉사) (정상 95/후배 96/승마 103/대면 97/배면 98/AS99)
;
;=====================================================================
[SKIPSTART]
MENUMATCH소분기 견본…

다른 분기와의 우선도에 주의해, 필요에 따라서 각 COM에 추가해 사용해 주세요
	;=================================================
	;ACT분류, 회화
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "회화")
	ENDIF
	;=================================================
	;ACT분류, 애무
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "애무")
	ENDIF
	;=================================================
	;ACT분류, 도구
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "도구")
	ENDIF
	;=================================================
	;ACT분류, 성교
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "성교")
	ENDIF
	;=================================================
	;ACT분류, 수치
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "수치")
	ENDIF
	;=================================================
	;ACT분류, 봉사
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "봉사")
	ENDIF
	;=================================================
	;ACT분류, 가학
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "가학")
	ENDIF
	;=================================================
	;ACT분류, 이상
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "이상")
	ENDIF
	;=================================================
	;ACT분류, 휴게
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "휴게")
	ENDIF
	;=================================================
	;ACT분류, 성적봉사
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "성적봉사")
	ENDIF
[SKIPEND]
;---------------------------------------------------------------------

;※각 분기에는 대응하는 지문을 제시되어 있습니다만, 최종적으로 어떠한 형태로 할까 아직 뭐라고도 말할 수 없는 곳

;=====================================================================
;회화계 반응 COM0 접수대
;=====================================================================
;·주로 ACT분류:회화계/휴게계 시에 발생한다
;·발생은 트리거 이벤트제. 특정의 ACT가 트리거가 된다
;·ACT분류:회화에 대해서는 REACT로 완결시키는 것이 바람직함
;-------------------------------------------------

;─
;COM0, 침묵한다
;─
;·따랐던 것에는 안 된다
;·ACT1, 지금의기분을얘기한다/자신에게로의기분을얘기한다의 경우는 REACT파생 1, "질문에 답하지 않는다"되어 벌포인트 가산
;·REACT인상:나쁜인상
;·의존도 변화(-1, -1)
;-------------------------------------------------
;·ACT가 지금의기분을얘기한다/자신에게로의기분을얘기한다의 경우는 파생 1, 질문에 답하지 않는 취급
;·그 외의 경우는 파생 0, 단순한 침묵
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류 0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류 2:도구
;	ACT:줄/아이마스크/볼개그/삼각목마
;ACT분류 4:수치
;	ACT:수치플레이/야외플레이
;ACT분류 6:가학
;	ACT:매도
;ACT분류 7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류 9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;--------------------------------------------------
;·TFLAG:REACT파생(0=단순한 침묵/1=질문에 답하지 않는다)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_0
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;ACT, 지금의기분을얘기한다/자신에게로의기분을얘기한다의 경우에 파생한다
	;=================================================
	;REACT파생 1, 질문에 답하지 않는다
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		IF KFLAG:153 == 0
			PRINTFORMW 「대답해 받을 수 없습니까?」
			PRINTFORMW 「모처럼인 것이기 때문에, 부부 사이 좋게 해 나가고 싶습니다만…… 안됩니까?」
		ELSE
			PRINTFORMW 「…… 침묵, 입니까」
			PRINTFORMW 「뭐, 변명 하는 것보다는 보기 흉하지 않은 것인지도 모르네요」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 무심코 뭔가 이야기를 시작했지만, 부끄러운 내용이었으므로
			;말을 삼켜, 입다물고 있었다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 기분이 안좋은 것 같게 침묵을 지킨 채였다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 아무것도 말하지 않았다…
		IF KFLAG:153 == 0
			PRINTFORMW 「」
		ELSE
			PRINTFORMW 「」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%의 모습을 엿보는 것 같은 기색을 보였지만,
		;아무것도 말하지 못하고 입다물고 있었다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「…… 너무 조용하게 되시면, 조금 지내기가 불편합니다」
			PRINTFORMW 「저…… 나, 가능한 한 능숙하게 할테니까」
		ELSE
			PRINTFORMW 「……」
			PRINTFORMDW 아구도 또한, 소매치기로 한 얼굴로 이쪽을 내려다 보고 있다.
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM1, 심약구응답한다
;─
;·REACT분류는 파생에 의해 변화
;·ACT("엄격하게위협한다/벌이라고선언한다/고함친다/밀어넘어뜨린다")로 일정 조건을 채우면 REACT파생 1, 애원에 파생
;·REACT인상:약간좋은인상
;·의존도 변화(0, +1)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다/의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;-------------------------------------------------
;·TFLAG:REACT파생(0=심약구응답한다/1=애원 한다)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_1
;=================================================
;·REACT파생로 내용이 다르므로 파생마다 나누어 버립니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 애원 한다(REACT분류:허가를청한다)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신이 응석부린 기분을 뿌리쳐,
			;허약하면서, 벌써 그만두고라고 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 기분을 해치지 않게 가능한 한 허약하고,
			;이제(벌써) 이런 일 그만두고라고 말했다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 심약구, 이제(벌써) 이런 일 그만두고라고 말했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 심약구 작은 소리로, 기분을 엿보도록(듯이)하면서
		;이제(벌써) 이런 일 그만두고라고 말했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「마음에 드시지 않습니까?」
			PRINTFORMW 「아구후더솔솔, 에 휘저어……」
			PRINTFORMW 「귀하의 탱탱 태우고 말할 수 있고 와, 질…… 라고 밝혀 받지 않으면, 곤란해 버립니다」
			PRINTFORMW 「저기?   좀 더, 힘낼 수 있습니다지요……?」
		ELSE
			PRINTFORMW 「무엇을 바보 같은 일을……」
			PRINTFORMW 「그렇게까지 한심하게 간원 한다면, 다음은 좀 더 참고 보이는 것이군요」
			PRINTFORMW 「이것으로는 단순한 자업자득이지요?」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 심약구응답한다(REACT분류:소극적 따른다)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신의 기분에 희미하게 깨달으면서도,
			;마음이 약하게 거리를 취하는 것 같은 대답을 했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 기분을 해치지 않게 가능한 한 허약하게 대답했다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 심약구대답했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 심약구 작은 소리로, 기분을 엿보도록(듯이)하면서 대답했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF TCVAR:MASTER:초조하게해번
			IF KFLAG:153 == 0
				PRINTFORMW 「신체를 움찔움찔 뒤로 젖혀지면서라도 반응해 주다니……」
				PRINTFORMW 「아구, 기쁩니다. 좀 더 큰 소리, 내 주세요……」
			ELSE
				PRINTFORMW 「자지 만지작거려져 아헤 표정하면서 대답하는 것은, 그 정도가 한계입니까」
				PRINTFORMW 「역시, 좀 더 어렵게 조교해 올릴 필요가 있네요」
			ENDIF
			KFLAG:151 += 1
		ELSE
			IF KFLAG:153 == 0
				PRINTFORMW 「그처럼 사양 하시지 않아도 좋아요?」
				PRINTFORMW 「우리들은 이미 부부인 것이기 때문에…… 좀 더 친한 듯이 불러 주세요.」
			ELSE
				PRINTFORMW 「시시한 대답이군요. 좀 더 재미있는 울음 소리는 없습니까?」
				PRINTFORMW 「뭐, 좋을 것입니다. 그 한심한 상판떼기만은 일생 잊지 않고 있어 줍니다」
			ENDIF
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM2, 무뚝뚝하게응답한다
;─
;·REACT분류:없음
;·따랐던 것에는 안 된다
;·벌포인트가 가산된다
;·REACT인상:나쁜인상
;·의존도 변화(-2, -2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다/의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
;=================================================
@KOJO_COM_K48_2
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게응답한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 무심코 뭔가 이야기를 시작했지만, 부끄러운 내용이었으므로
			;아무것도 아니면 무뚝뚝하게 손질했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 힘껏의 저항을 담아 무뚝뚝하게 응했다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 무뚝뚝하게\@ IS_NOWACTNAME("밀어넘어뜨린다") ? 응답 # 대답 \@했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 심지의 강한 눈동자로%CALLNAME%를 응시해 무뚝뚝하게 응했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「…… 뭐, 이것으로부터군요?   군요?」
			PRINTFORMDW 아구는 자신을 격려하도록(듯이) 중얼거렸다.
		ELSE
			PRINTFORMW 「……」
			PRINTFORMDW 아구는 차가운 눈으로%CALLNAME:MASTER%의 일을 쏘아보고 있다…….
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM3, 붙임성좋게응답한다
;─
;·REACT분류:적극적 따른다
;·REACT인상:좋은인상
;·의존도 변화(+3, +1)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
;=================================================
@KOJO_COM_K48_3
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성좋게응답한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는 잘 비위를 맞추려고 만들기 웃는 얼굴로 응했다…
		PRINTFORMW 「응…… 뭐, 기분만 받아 두네요」
		PRINTFORMW 「좀 더, 자연히(에) 하고 계셔 주세요」
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 붙임성 좋게\@ IS_NOWACTNAME("밀어넘어뜨린다") ? 응답 # 대답 \@했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 기쁜듯이%CALLNAME%에 미소지어,
		;붙임성 좋게 대답했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF TCVAR:MASTER:초조하게해번
			IF KFLAG:153 == 0
				PRINTFORMW 「기분이 좋습니까?   후후, 기쁩니다.」
				PRINTFORMW 「좀더 좀더 솔직하게, 울어 받을 수 있도록, 한층 단련해 보이네요」
			ELSE
				PRINTFORMW 「에에, 좋은 아이예요 강아지. 말하는 대로 『기다려』도 (들)물을 수 있네요?」
				PRINTFORMW 「그렇지 않으면 가축어가 아니라고 모릅니까……?」
				PRINTFORMW 「이봐요, 원원워」
			ENDIF
			KFLAG:151 += 1
		ELSE
			IF KFLAG:153 == 0
				PRINTFORMDW 아구는 화끈 얼굴을 붉힌 후, 뺨을 손으로 가리면서 기대었다.
				PRINTFORMW 「싫다, 부끄러워요, 귀하……」	
				PRINTFORMW 「하, 한번 더 말해 받을 수 없습니까……?」
			ELSE
				PRINTFORMW 「…… 대답만은 좋군요.」
				PRINTFORMW 「뭐 좋습니다. 구별하고가 없는 남근은 나의 손으로 단련해 주면 좋은 것뿐이기 때문에」
			ENDIF
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM4, 강하게응답한다
;─
;·REACT분류:없음
;·따랐던 것에는 안 된다
;·ACT, 밀어넘어뜨린다의 경우는 REACT파생 1, "밀어넘어뜨리기에 굽히지 않는다"에 파생
;·REACT인상:나쁜인상
;·의존도 변화(-1, -3)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다/의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
;=================================================
@KOJO_COM_K48_4
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게응답한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;ACT, 밀어넘어뜨린다의 경우 파생
	;=================================================
	;REACT파생 1, 밀어넘어뜨리기에 굽히지 않는다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		;%CALLNAME:MASTER%는 이제 그만두라고 강하게 말했다…
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 무심코 뭔가 이야기를 시작했지만, 부끄러운 내용이었으므로
			;%CALLNAME%에는 관계없으면 강하게 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 강한척 해 응했지만, 소리는 조금 떨고 있다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 강해 대답했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%를 노려봐, 강해 응했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMDW 아구는 마음 탓인지 멍하니 하고 있다.
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM5, 빈정거린다
;─
;·ACT, 지금의기분을얘기한다/자신에게로의기분을얘기한다의 경우는 REACT분류:거부
;·그 이외의 경우는 REACT분류:태연하게 따르는이 된다
;·벌포인트 가산
;·REACT인상:나쁜인상
;·의존도 변화(-3, -1)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/합의를요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:줄/아이마스크/볼개그/삼각목마
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
;=================================================
@KOJO_COM_K48_5
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 솔직한 기분을 말하려고 했지만,
			;왠지 부끄러워져, 야유 섞임에 응했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 작은 악의를 말에 혼합해 따끔 돌려주었다…
			;말 있고는 했지만, 기분을 해치지 않은가 끊임없이 신경쓰고 있다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 빈정거리면서도, 말하는 일에는 솔직하게 따라서 보였다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 여유를 보이는 것 같은 태도로 홀연히 따르면서,
		;야유 섞임에 응답했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「뭐, 후후후.」
			PRINTFORMW 「농담을 말할 수 있으려면, 막역할 수 있던 것 같고 최상입니다」
			PRINTFORMW 「회화가 없는 것 보다는 상당히 좋기 때문에」
		ELSE
			PRINTFORMW 「…… 호우?   구에서 이 히에다 아가씨서로 라고 말할 생각 입니까?」
			PRINTFORMW 「좋을 것입니다. 충분히…… 인 체하며 줍니다」
			PRINTFORMDW 사악한 미소를 띄운 아구에, 심하게 설파해져 버렸다……
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM6, 좀더하드하게해
;─
;·REACT분류:받아들인다
;·조교자의 소질, BASE:초조해 등에 의해 REACT파생 1, 「이 철면피의 암퇘지놈!」(이)가 된다
;·REACT파생 1의 경우는 벌포인트 가산
;·REACT인상:0에서멀어짐
;·의존도 변화(0, +3)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/상냥하게위로한다
;		벌이라고선언한다
;ACT분류:1:애무
;	ACT:손으로애무/가슴애무/입으로애무/키스/성기를서로켠다/파이즈리한다
;ACT분류:2:도구
;	ACT:C애무도구/니플캡/줄/아이마스크/볼개그/삼각목마
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:스팽킹
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=이 철면피의 암퇘지놈!)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_6
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 「이 철면피의 암퇘지놈!」
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	PRINTFORMW 「…… 좀 더 격렬하게 했으면 좋은, 입니까?」
	PRINTFORMDL
	PRINTFORMDW 아구가 감기고 있던 분위기가, 어딘가 가칠가칠 한 것으로 바뀐다.
	PRINTFORMDL 
	PRINTFORMW 「귀하가 그렇게 말씀하시므로 있으면 상관하지 않지만……」
	PRINTFORMW 「무너지지 말아 주세요, 새끼 돼지씨?」
	PRINTFORMDL
	PRINTFORMDW 정숙한 분위기를 버리고 가, 가학적에 비웃는 아구에%CALLNAME:MASTER%는 등골을 얼릴 수 있었다……
	KFLAG:153 = 1
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌SOURCE가 일정 이상
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && SOURCE:MASTER:쾌C + SOURCE:MASTER:쾌V + SOURCE:MASTER:쾌A + SOURCE:MASTER:쾌B > 10
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 기분을 누르면서, 좀 더 하고라고 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 반 자포자기가 되어, 좀 더 하드하게 하면 좋다고 말했다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 좀 더 굉장한 것 여겨지고 싶다고 했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감에 빠져, 좀 더 굉장한 일 하고라고 말했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;쾌SOURCE가 일정 이상의 네스트를 개방하지 않는 경우는, 상황에 관련되지 않고 이 개소가 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 여러가지 기분을 정리하면서 말을 선택해,
			;%CALLNAME%로 된다면 좀 더 하드해도 좋다고 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 자포자기가 되어, 좀 더 하드하게 하면 좋다고 말했다…
			;말 있고는 했지만, 입술을 딱 닫아 어깨를 진동시키고 있다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 흥미 반으로, 좀더하드하게해도 좋다고 말해 보았다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 좀 더 기분이 좋은 일을 해 주었으면 해져,
		;무심코 좀더하드하게해라고 말해 버렸다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「그…… 좀 더 괴롭혔으면 좋은, 이라고 하는 일, 입니까?」
			PRINTFORMW 「귀하의 말하는 일이라면 선처 합니다만…… 조금, 변하네요」
		ELSE
			PRINTFORMW 「하아…… 반성은 커녕, 버릇이 되어 버린다고는」
			PRINTFORMW 「실로 실망인 휴지군요, 불면 나는 것 같은 모양인 것은?
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM7, 허가를청한다
;─
;·REACT분류:허가를청한다. 파생했을 경우는 REACT분류:없음
;·조교자의 소질, BASE:이성, 아라이먼트에 의해 REACT파생 1, 「의견을 내다니 좋은 담력이군요!」(이)가 된다
;·턴방침이 이상의 경우는 파생 1이 되기 쉽다
;·파생 1의 경우는 벌포인트 가산
;·REACT인상:0에가까워짐
;·의존도 변화(0, +2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류:1:애무
;	ACT:애널애무/애널빨고/밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:애널바이브/애널비즈/관장세트/페니스밴드
;ACT분류:3:성교
;	ACT:후배위/배면좌위/애널섹스
;ACT분류:4:수치
;	ACT:자위/비패개장/수치플레이/야외플레이/방뇨
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:스팽킹/채찍/바늘
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착/피스트퍽/애널피스트/양구멍피스트/공중육변기플레이
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:역강간
;
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=의견을 내다니 좋은 담력이군요!)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_7
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 허가를청한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;ACT, 공중육변기플레이에서는 파생하지 않는다
	;=================================================
	;REACT파생 1, 의견을 내다니 좋은 담력이군요!
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		IF KFLAG:153 == 0
			PRINTFORMW 「…… 앗…… 안타깝고, 되어 버렸습니까?」
			PRINTFORMW 「그렇지만, 안됩니다. 그런 푸념뿐이면, 몇시까지 지나도 나를 임신하게 되어지지 않아요?」
			PRINTFORMW 「자, 스윽스윽 해 올리기 때문에 노력해 주세요」
		ELSE
			PRINTFORMW 「아우성치는 것보다도 행동으로 가리키면 어떻습니까?」
			PRINTFORMW 「이, 쓰레기를, 조금은 소용에 되도록(듯이)해 주세요, 라고 말하고 있습니다?」
			PRINTFORMDW 아구는 말의 리듬을 타 피시피시와 손가락으로 국소를 연주했다.
			PRINTFORMDW %CALLNAME:MASTER%의 신체에 전격과 같은 자극이 달린다……
			PRINTFORMW 「…… 후훗, 좀 더 벌이 필요한 같네요」
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신이 이상해지고 있는 것을 자각해,
			;더 이상 이상하게 되는 것은 무섭다고 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 아첨하도록(듯이)%CALLNAME%를 올려보면, 이제(벌써) 허락해와 바랐다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 이제(벌써) 허락하고라고 말하기 시작했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 눈물을 머금어%CALLNAME%에 이제(벌써) 허락해 주세요간원 했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF TCVAR:MASTER:초조하게해번
			IF KFLAG:153 == 0
				PRINTFORMW 「후후…… 토정 하고 싶군요……?」
				PRINTFORMW 「상냥하고, 상냥하게 이끌도록 해 받을테니까……」
				PRINTFORMW 「최고의 타이밍, 는 해 버리는 편이 기분이 좋아요……?」
			ELSE
				PRINTFORMW 「허가를청한다 정도에는 학습했다, 라고 하는 일입니까」
				PRINTFORMW 「그런데…… 그러나, 예의 범절은 엿과 채찍이 중요하기 때문에……」
			ENDIF
			KFLAG:151 += 1
			RETURN LOCAL:1
		ELSE
			IF KFLAG:153 == 0
				PRINTFORMW 「그렇게 심한 일을 한 기억은 없습니다만……」
				PRINTFORMW 「그, 나와 함께 보내는 일, 싫습니까……?」
				PRINTFORMDW 아구는 눈에 눈물을 모아%CALLNAME:MASTER%를 올려보고 있다……
			ELSE
				PRINTFORMW 「찰나적이라고 알고 있어 상 야비하게 열락에 잠겨 두면서, 이번은 허가를 바란다고는」
				PRINTFORMW 「완전히, 이것이니까 가축을 기르는 것은 질리지 않네요」
			ENDIF
			RETURN LOCAL:1
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM8, 기분좋게해
;─
;·REACT분류:받아들인다
;·조교자의 소질, BASE:이성, 아라이먼트에 의해 REACT파생 1, 「이 철면피의 암퇘지놈!」(이)가 된다
;·파생 1의 경우는 벌포인트 가산
;·REACT인상:0에서멀어짐
;·의존도 변화(0, +2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다/합의를요구한다
;ACT분류:1:애무
;	ACT:애널애무/애널빨고/밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:페니스밴드
;ACT분류:3:성교
;	ACT:
;ACT분류:4:수치
;	ACT:자위과시하고/방뇨
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:역강간
;
;TEQUIP:볼개그는 튕겨진다
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=이 철면피의 암퇘지놈!)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_8
;=================================================
;·REACT파생 0는 사실상 COM 명칭 그대로 해석 해 줄 수 있으면 좋을까
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 이 철면피의 암퇘지놈!
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	PRINTFORMW 
	RETURN LOCAL:1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌SOURCE가 일정 이상
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && SOURCE:MASTER:쾌C + SOURCE:MASTER:쾌V + SOURCE:MASTER:쾌A + SOURCE:MASTER:쾌B > 10
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 기분을 누르면서, 좀 더 하고라고 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 몹시 꾸짖을 수 있는 것보다는 좋다고 생각해, 이대로 하고라고 말했다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 좀 더 기분좋게해계속 이라고 말했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감에 빠져, 좀 더 자주(잘) 해와 졸랐다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「에에, 물론…… 기대에 따라 보여요」
		PRINTFORMDW 입가를 소매로 숨겨, 아구는 얇게 웃었다……
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;쾌SOURCE가 일정 이상의 네스트를 개방하지 않는 경우는, 상황에 관련되지 않고 이 개소가 보완합니다
;본체측의 처리를 나누어지고 있으므로, 텐프레측도 일단 나누어 보았습니다만
;현시점에서는 내용에 그다지 차이가 없기 때문에, 이 개소만이라도 좋은 생각은 듭니다
;다만 이 바리안트, 지문이 단번에 증가하거나 바뀌거나 하므로 뭐라고도 말할 수 없는 곳
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 기분을 누르면서, 욕망에 정직하게
			;좀 더 기분 좋은 일을 되고 싶다고 했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 몹시 꾸짖을 수 있는 것보다는 좋다고 생각해,
			;어차피라면 기분 좋아지고 싶다고 했다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 기분좋게해라고 말하기 시작했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 정욕의 포로가 된 것처럼, 좀 더 기분 좋은 일 해라고 호소했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「에에, 물론…… 기대에 따라 보여요」
		PRINTFORMDW 입가를 소매로 숨겨, 아구는 얇게 웃었다……
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;쾌락계 반응
;=====================================================================
;·쾌SOURCE나 특정의 PALAM가 트리거가 되어 발생한다
;·COM에 따라서는 ACT분류가 트리거가 된다
;-------------------------------------------------

;─
;COM10, 부끄러워한다
;─
;·REACT분류:소극적 따른다
;·벌포인트 가산
;·쾌SOURCE가 일정 이상의 경우는 파생 1, 쾌감에 부끄러워하는이 된다
;·의존도 변화(+1, 0)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:성에대한이야기를한다/의상의변경을요구한다
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:수치
;	ACT:자위/비패개장/자위과시하고/수치플레이/야외플레이/방뇨
;ACT분류:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/가슴애무강제
;ACT분류:이상
;	ACT:방치플레이
;
;※SOURCE:쾌C/쾌V/쾌A/쾌B, PALAM:치정이 트리거가 된다
;-------------------------------------------------
;·TFLAG:REACT파생(0=상황에 부끄러워한다/1=쾌감에 부끄러워한다)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_10
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 쾌감에 대해서 부끄러워한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 좀 더 기분 좋아지고 싶다고 생각하고 있는 자신을 의식해 버려,
			;뺨을 물들여 부끄러워했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 쾌감이 주어지는 일에 강한 수치를 느껴
			;몸을 움츠러들게 해 부끄러워했다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 복받치는 쾌감을 숨기지 못하고, 부끄러워해 얼굴을 돌렸다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감으로 소리를 흘려, 깜짝 놀란 것처럼 숨을 삼키면
		;시선을 피해 부끄러워했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「
			PRINTFORMW 「
		ELSE
			PRINTFORMW 「
			PRINTFORMW 「
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;REACT파생 1, 쾌감에 부끄러워하는 것을 썼을 경우는 REACT파생 0, 상황에 부끄러워하는 취급
;파생 1을 쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;이하에 나타내는 지문은 REACT파생 0의 것
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 모습을 볼 수 있는 것으로 이상한 기분이 되어 버려,
			;더욱 더 뺨을 물들여 부끄러워했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 자신을 욕보이는 시선에 견딜 수 없게 되어,
			;몸을 안도록(듯이)해 부끄러워했다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 욕보여지고 있는 상황에 견딜 수 있지 못하고, 수치의 너무 얼굴을 돌렸다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%가 빠는 것 같은 시선을 느껴
		;무심코 시선을 피해 부끄러워했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「얼굴, 외면하면 싫습니다……」
			PRINTFORMW 「나의 눈, 가만히 보고 있어 주…… 세요?」
		ELSE
			PRINTFORMW 「밀크 탱크에도 부끄러워할 정도의 지능은 있는 것이군요」
			PRINTFORMW 「그런 물건보다 먼저, 판을 붙여야 할텐데」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM11, 싫어한다
;─
;·REACT분류:소극적 따른다
;·REACT파생 1는 ACT 방해가 되어 REACT분류:날뛰는 취급
;·의존도 변화(-1, -1)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:의상의변경을요구한다
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/풋잡
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;
;※SOURCE:쾌C/쾌V/쾌A/쾌B, PALAM:치정이 트리거가 된다
;-------------------------------------------------
;·TFLAG:REACT파생(0=싫어한다/1=싫어해 ACT를 방해)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_11
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 싫어해 방해한다(REACT분류:날뛴다)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 순간에 싫어하는 손을 대어 버렸지만,
			;%CALLNAME%에 미안한 것 같은 표정을 보였다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 힘껏의 기력을 쥐어짜, 싫어해%CALLNAME%로부터 피했다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 싫어해 몸을 비틀어,%CALLNAME%의 방해를 했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 싫어해%CALLNAME%를 밀쳤다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「꺗……」
		PRINTFORMDW 저항된 아구는, 반목이 되어%CALLNAME:MASTER%를 응시하고 있다……
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;파생 1을 썼을 경우는 REACT파생 0, 싫어하지만 ACT는 방해하지 않고 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 내심에서는 그렇게 그렇지만도 않다고 생각하면서도,
			;부끄럽기 때문에 싫어해 보였다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 힘껏의 기력을 쥐어짜, 떨리는 소리로 싫다고 말했다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 고개를 저어 싫어했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 그런 것 싫다고 말했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「사실은 그런 얼굴, 하기를 바라지 않습니다만……」
		PRINTFORMW 「뭐…… 그 사이 알아주면, 믿고 있습니다」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM12, 서투르다고매도한다
;─
;·REACT분류:태연하게 따른다
;·REACT파생 1(강한척 한다)는 REACT분류:소극적 따르는 취급
;·벌포인트 가산
;·파생 0, 새겨 내린다(REACT인상:약간나쁜인상)
;·파생 1, 강한척 한다(REACT인상:나쁜인상)
;·의존도 변화(-2, -3)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:성교
;	ACT:정상위/후배위/대면좌위/배면좌위
;
;※TFLAG:ACT분류(1=애무/3=성교)가 트리거
;-------------------------------------------------
;·TFLAG:REACT파생(0=새겨 내린다/1=강한척 한다)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_12
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 강한척 해(쾌감을 느끼면서)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는 허덕이는 소리 섞임의 뻔히 보임의 허세로,%CALLNAME%에 서투르다고 말했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;착실하게 쾌감을 느끼면서도 %CALLNAME:MASTER%는 서투르다고 우겼다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감에 얼굴을 비뚤어지게 하면서도, 이런 것은 느끼지 않는다고 말했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF TCVAR:MASTER:초조하게해번
			PRINTFORMW 「아라…… 의외로 여유가 있는 것이군요」
			PRINTFORMW 「이렇게 움찔움찔 떨고 있으면서, 좀 더 강하게 했으면 좋습니까?」
			PRINTFORMW 「그렇다면…… 좀 더 인내 할 수 있군요?」
			KFLAG:151 -= 1
		ELSE
			PRINTFORMW 「그렇게는 말하면서도, 여기가 좋을 것입니다?」
			PRINTFORMW 「이봐요…… 눈이 몽롱 해 왔어요……」
			PRINTFORMW 「후후, 귀하가 띄우는 표정은 모두 알고 있을테니까」
			PRINTFORMW 「그러므로, 사랑스럽게 춤추어 주세요……?」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;파생 1을 썼을 경우는 REACT파생 0, 새겨 내리는(실제로 서투른 경우) 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는 힘껏의 허풍으로,%CALLNAME%에 서투르다고 말했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:TARGET%의 미숙한 테크닉에, %CALLNAME:MASTER%는 서투르다고 말했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 이것이라면 자신이 능숙하다고 말했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「기분 좋게는…… 없는 것 같네요」
		PRINTFORMW 「무무, 실제로 해 보면 다른 거네……」
		PRINTFORMDW 마음 탓인지, 아구는 시무룩 하고 있다.
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM13, 겁없게웃는다
;─
;·REACT분류:태연하게 따른다
;·벌포인트 가산
;·REACT인상:나쁜인상
;·의존도 변화(-2, -3)
;-------------------------------------------------
;·트리거가 되는 행동, 소스, 파라미터
;·SOURCE:쾌C/쾌V/쾌A/쾌B, SOURCE:아픔/구속, ACT분류:가학
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그/매도/삼각목마/관장세트
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_13
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;PRINTFORML %CALLNAME:MASTER%는 자신은 아직 제정신이라고 확인하도록(듯이) 웃어 보였다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 들러붙은 것 같은 미소를 돌려주었지만, 눈이 헤엄치고 있다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 일부러 %CALLNAME:TARGET%를 도발하도록(듯이) 웃었다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%가 조교에 고심하고 있는 것을 조소했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM14, 쾌감을참는다
;─
;·REACT분류:소극적 따른다
;·일정 조건으로 REACT파생 1 「애원(REACT분류:허가를청한다)」취급
;·파생 1의 경우 REACT인상는 0에가까워짐
;·의존도 변화(+1, -1)
;-------------------------------------------------
;※트리거가 되는 행동, 소스, 파라미터
;·SOURCE:쾌C/쾌V/쾌A/쾌B
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무, 가슴애무, 입으로애무, 애널애무, 애널빨고, 키스, 성기를서로켠다
;		파이즈리한다, 풋잡한다
;ACT분류:도구
;	ACT:로터, 바이브, 애널바이브, 애널비즈, C애무도구, 니플캡
;ACT분류:성교
;	ACT:정상위, 후배위, 승마위, 대면좌위, 배면좌위, 애널섹스
;ACT분류:수치
;	ACT:자위, 비패개장
;ACT분류:봉사
;	ACT:파이즈리, 가랑이
;ACT분류:이상
;	ACT:피스트퍽, 애널피스트, 양구멍피스트
;-------------------------------------------------
;·TFLAG:REACT파생(0=쾌감을참는다/1=애원 한다)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_14
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 애원 한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 쾌감으로 제정신을 잃는 일에 무서워하고 있다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 쾌감을 받아들이려고 하지 않고, 그만두어와 간원 했다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 강한 쾌감을 참으면서, 이제(벌써) 이런 일 그만두어와 간원 했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감을 참으면서 허가를 바라고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;파생 1을 썼을 경우는 REACT파생 0, 쾌감을 인내 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;사정 직전
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && NOWEX:MASTER:사정 && KFLAG:152 == 1
	SELECTCASE TFLAG:ACT
		CASE 18
			PRINTFORMW 「다리는 역시 불만입니까?」
			PRINTFORMW 「자지는, 별로 그렇게 말하지 않은 느낌이지만요～」
			PRINTFORMW 「이봐요, 양 다리로 타마타마─에……」
			PRINTFORMW 「키득, 사정이 허가되면, 빠르게 사정하는 것도 훈련중이에요?」
		CASE 13
			PRINTFORMW 「참아도, 쓸데없습니다」
			PRINTFORMW 「귀하의 약한 장소는 알고 있을테니까……」
			PRINTFORMW 「엉덩이의 구멍 후비어져, 사정해 버릴 수 밖에 없어요?」
		CASEELSE
			PRINTFORMW 「이제 참을 필요는 없다고 말하는데……」
			PRINTFORMW 「혹시, 아직 계속하고 싶었던 것입니까?」
			PRINTFORMW 「그렇지만, 계속 너무 참아, 펀…… 은 망가져 버려도 곤란할테니까」
			PRINTFORMW 「나에게 관리되어, 지금은 토정 하셔 주세요」
	ENDSELECT
	RETURN LOCAL:1
	ENDIF
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 욕망을 뿌리쳐, 쾌감을 견디려고 했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 쾌감을 받아들이려고 하지 않고 참고 있다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 필사적으로 쾌감을 참았다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 분명한 쾌감을 느끼면서도, 그것을 참으려고 하고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF TCVAR:MASTER:초조하게해번
			IF KFLAG:153 == 0
				IF (KFLAG:150 + KFLAG:151) >= 9
					SELECTCASE TFLAG:ACT
						CASE 18
							PRINTFORMW 「자지를 다리로 짓밟을 수 있어, 침을 늘어뜨리면서 이를 악물어……」
							PRINTFORMW 「내가 아니면, 환멸 하고 있는 그림일지도 모르네요?」
							PRINTFORMW 「괜찮아요, 나는 귀하의 일을 사랑하고 있을테니까♪」
							PRINTFORMW 「제대로, 특농 "자리 짤 수 있는"조차 만들어 받을 수 있으면…… 말야」
						CASE 13
							PRINTFORMW 「가려운 곳은 없습니까―?」
							PRINTFORMW 「…… 에에, 이, 조금 분위기를 살린 개소군요♪」
							PRINTFORMW 「좀 더, 좀 더예요♪」
							PRINTFORMW 「눈 짝짝 시키면서, 조금만 더 참으면, 사정해도 좋아요♪」
							PRINTFORMW 「―……4…… 씨…… 에게―……」
						CASEELSE
							PRINTFORMW 「그런데, 귀하가 너무나 사랑스러운 얼굴을 하는 것이니까, 나의 꿀도 흘러넘쳐 와 버려」
							PRINTFORMW 「압니까?   아직 털도 나지 않은, 새하얀 줄기 보지가……」
							PRINTFORMW 「귀하의 자지를 무는 일을 상상해, 엣치하게 실을 당기고 있습니다……♪」
							PRINTFORMW 「저기, 상상해?   바둥바둥 녹진녹진의, 어린 아이 보지……」
							PRINTFORMW 「아이인데 어머니가 되고 싶어하고 있어, 안쪽―, 라고 교배해 주었으면 한거야……」
							PRINTFORMW 「…… 전혀 기분이 잊혀지지 않아?   어머나, 미안해요, 나도 참♪」
					ENDSELECT
				ELSEIF (KFLAG:150 + KFLAG:151) >= 7
					SELECTCASE TFLAG:ACT
						CASE 18
							PRINTFORMW 「아아…… 아구의 발가락이, 귀하의 자지에 착 달라붙어……」
							PRINTFORMW 「허리에 불필요한 힘을 써서는 안됩니다?」
							PRINTFORMW 「손 정도 요령 있게 힘 조절을 할 수 없기 때문에…… 똑하고, 말해 버릴지도……」
						CASE 13
							PRINTFORMW 「아라?   귀하, 고기구멍이 자극되어…… 꿀이 뚝뚝 떨어져 내리고 있지 않습니까?」
							PRINTFORMW 「안됩니다?   나는 아직, 허가를 내고 있지 않아요?」
							PRINTFORMW 「이봐요, 확실히 무릎을 세워 두지 않으면. 손가락이 엉덩이 중(안)에서 날뛰어 버려요」
							PRINTFORMW 「힘내―, 인내, 인내……」
						CASEELSE
							PRINTFORMW 「…… 머릿속이 흰색구는 라고 온 것이지요?」
							PRINTFORMW 「-구신체를 훑어지는 것만으로…… 찌릿찌릿은 반응해 버립니다」
							PRINTFORMW 「뭔가 다른 일을 생각하는 편이, 기분이 잊혀질지도 모르네요」
					ENDSELECT
				ELSEIF (KFLAG:150 + KFLAG:151) >= 3
					PRINTFORMW 「그렇습니다, 숨을 들이마셔…… 토해……」
					PRINTFORMW 「천천히 호흡하면서, 다리의 힘을 꿰매어라고 말해 주세요」
					PRINTFORMW 「신체의 여분의 힘을 빼…… 자지에만 의식을 집중시켜?」
					PRINTFORMW 「향기인가─구 들이 마시면, 머리가 뿌옇게 해 올까요……?」
				ELSE
					PRINTFORMW 「에에, 그렇게. 인내예요…… 분명하게, 참아 주세요」
					PRINTFORMW 「어금니를 확실히 씹어…… 구, 구, 는 복받쳐 오는 정액을, 눌러 둡니다」
					PRINTFORMW 「말하는 대로 할 수 있으면, 포상을 올리니…… 까요?」
				ENDIF
			ELSE
				IF (KFLAG:150 + KFLAG:151) >= 9
					SELECTCASE TFLAG:ACT
						CASE 18
							PRINTFORMW 「헤에, 꽤 노력하지 않습니까. 틀림없이, 다리로 남자의 존엄을 짓밟아지는 것을 좋아하는 변태라고 생각하고 있었습니다만」
							PRINTFORMW 「최대한 옥대를 다리로 려 하기 시작해져, 비참하게 사정하는 것은 아닐까……」
							PRINTFORMW 「에에!   이렇게 해!   타마를!   짓밟아 부숴져!」
							PRINTFORMW 「…… 후응, 견딜 수 있습니까?   구로부터 거품 불고 있습니다만」
						CASE 13
							PRINTFORMW 「이봐, 울음 소리는 어떻게 한 것입니까?   분명하게 모모 울어 주세요」
							PRINTFORMW 「…… 좀 더 큰 소리로. 그렇지 않으면, 손가락 전부 돌진해요?」
							PRINTFORMW 「그렇습니다. 이를 악물어 참자는, 달콤하니까」
							PRINTFORMW 「그렇지만, 일생 잊을 수 없는 아픔과 쾌감이라든지…… 나와 갖춤이군요…… 키득」
						CASEELSE
							PRINTFORMW 「이제(벌써), 무엇 되고 있을지도 잘 모른다는 얼굴이군요」
							PRINTFORMW 「사람의 존엄 따위 있던 것이 아닙니다만…… 뭐, 하반신으로 물건을 생각하는 생물은 원래 사람이 아니기도 하구요」
							PRINTFORMW 「잘 할 수 있었어요, 고기 바이브씨. 인간님으로부터 포상을 드립니다」
					ENDSELECT
				ELSEIF (KFLAG:150 + KFLAG:151) >= 6
					PRINTFORMW 「…… 꽤 참네요. 아무래도 반성하고 있는 것은 확실한 것 같습니다」
					PRINTFORMW 「뭐, 가축은 채찍만이라도 자라지 않기 때문에, 본의가 아닙니다만 먹이를 올립시다」
					PRINTFORMW 「힘내라―, 자지, 힘내라―, 자지……」
				ELSEIF (KFLAG:150 + KFLAG:151) >= 3
					PRINTFORMW 「이런이런, 이 정도의 단계에서, 이렇게 쿠퍼액을 불기 시작하고 있어 괜찮은 것입니까?」
					PRINTFORMW 「이렇게 끈적끈적하고 있으면, 여분 미끈미끈으로서 기분 좋아져 버려요?」
					PRINTFORMW 「알면 수도꼭지를 빈틈없이 잡아 쿠퍼액의 누출을 막아 주세요」
					PRINTFORMW 「그렇지 않으면, 요도까지 "훈련"하지 않으면 안됩니까?」
				ELSE
					PRINTFORMW 「쓰레기 자지에서도, 과연 학습 기능정도는 갖춰지고 있습니까」
					PRINTFORMW 「에에, 그것으로 좋습니다. 그다지 나의 손을 번거롭게 할 수 없어 주세요」
					PRINTFORMW 「너무나 사용할 수 없다고 한다면, 그 안 눌러꺾어 버려요?」
				ENDIF
			ENDIF
			KFLAG:151 += 1
		ELSE
			IF KFLAG:153 == 0
				IF RAND:3 == 0
					PRINTFORMW 「마음에 드시지 않았습니까?   귀하」
					PRINTFORMW 「그렇지 않으면, 동녀에게 만지작거려지게 되면…… 프라이드가 허락하지 않습니까?」
				ELSEIF RAND:2 == 0
					PRINTFORMW 「그렇게 뺨을 물들이는 모습, 사랑스러워요」
					PRINTFORMW 「그림에 남겨 두고 싶을 정도…… 후훗♪」
				ELSE
					PRINTFORMW 「에에, 알고 말고요…… 남아인 것, 프라이드가 있습니다인 거네요」
					PRINTFORMW 「그것을 꽃과 같이 집어내는 것도, 여자의 의무이기 때문에……」
				ENDIF
			ELSE
				PRINTFORMW 「돼지는 돼지 나름대로 생각하는 곳이 있는 것 같네요」
				PRINTFORMW 「뭐, 정직 쓸데없는 저항도 좋은 곳이지만. 싫지 않아요?」
			ENDIF
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM15, 쾌감을받아들인다
;─
;·COM16, 기분좋다！와 거의 같음. 차이는 의존도의 변화만
;·조교 대상이 의존하기 쉽다
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도 변화(+1, +5)
;-------------------------------------------------
;※트리거가 되는 행동, 소스, 파라미터
;·SOURCE:쾌C/쾌V/쾌A/쾌B
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위, 비패개장
;ACT분류:봉사
;	ACT:파이즈리, 가랑이
;ACT분류:이상
;	ACT:피스트퍽, 애널피스트, 양구멍피스트
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_15
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;쾌감에 방심해 버리면, %CALLNAME:MASTER%의 마음은 순식간에 녹기 시작했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 달콤한 교성을 주면서 쾌감을 받아들였다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 자신이 기분 좋아지는 것인 만큼 필사적으로 쾌감을 탐내고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF TCVAR:MASTER:초조하게해번
			IF KFLAG:153 == 0
				PRINTFORMW 「이봐요, 늠름한 얼굴이 녹진녹진이 되어 버리고 있어요?」
				PRINTFORMW 「아무것도 생각할 수 없다는 얼굴 해…… 분명하게 참고 있습니까?」
			ELSE
				PRINTFORMW 「허리를 흔들어도 좋으면 허락한 기억은 없는데요?」
				PRINTFORMW 「이 정도의 자극으로 낑낑거리며가는……」
				PRINTFORMW 「완전히, 얕고 얇다고는 이 일이군요」
				PRINTFORMW 「조금 조금 강하게, 두드리거나 하는 것이 좋습니까?」
			ENDIF
			KFLAG:151 -= 1
		ELSE
			IF KFLAG:153 == 0
				PRINTFORMW 「능숙 받아지고 있습니까?」
				PRINTFORMW 「나는 이제(벌써), 귀하의 물건인 것이기 때문에……」
				PRINTFORMW 「사양 하시지 않고, 뭐든지 분부해 주십시오?」
				PRINTFORMW 「이 유녀(어린)의 신체로 할 수 있는 일이라면, 어디까지나……」
			ELSE
				PRINTFORMW 「야무지지 못하게 혀를 내밀어…… 마치 개군요」
				PRINTFORMW 「부정하고 싶습니까?   라면, 이 딱딱의 물건을 조금은 진정시켜 봐서는?」
				PRINTFORMW 「나에게 관리되는 것이 행복하게 완성되도록, 조교해 드립니다……」
			ENDIF
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM16, 기분좋다！
;─
;·COM15, 쾌감을받아들인다와와 거의 같음. 차이는 의존도의 변화만
;·주인님의 의존을 권하기 쉽다
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도 변화(+4, +1)
;-------------------------------------------------
;※트리거가 되는 행동, 소스, 파라미터
;·SOURCE:쾌C/쾌V/쾌A/쾌B
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위, 비패개장
;ACT분류:봉사
;	ACT:파이즈리, 가랑이
;ACT분류:이상
;	ACT:피스트퍽, 애널피스트, 양구멍피스트
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_16
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는 아첨하도록(듯이) 기분 좋은 것 같은 소리로 허덕이고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 쾌락을 얻을 수 있는 행복을 감사하는것 같이 소리에 냈다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 달콤한 소리로 쾌감과 기쁨을%CALLNAME%에게 전했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;성교계 반응 COM20 접수대
;=====================================================================
;·ACT분류:성교/성적봉사, ACT:파이즈리한다/성기를서로켠다 등이 트리거가 되어 발생
;-------------------------------------------------

;─
;COM20, 허리를흔드는
;─
;·주도권(SEX_INITIATIVE)에 의해 내용이 다른 COM
;-------------------------------------------------
;·ACT분류가 성교 봉사, ACT:승마위의 경우는 주도권은 조교 대상(SEX_INITIATIVE(MASTER))
;·REACT분류:적극적 따른다
;·의존도 변화(+5, +3)
;-------------------------------------------------
;·ACT분류가 성교, ACT:역강간/성기를서로켠다/파이즈리한다의 경우는 주도권은 조교자
;·※성기를서로켠다/파이즈리한다는 SEX_INITIATIVE(TARGET)에서는 참조 할 수 없다
;·REACT분류:받아들인다
;·의존도 변화(+4, +8)
;-------------------------------------------------
;·REACT인상:좋은인상
;-------------------------------------------------
;·초조하게 해 안은 실행 불가
;·ACT:파이즈리한다의 경우, 조교자가 빈유라고 튕겨진다
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:성기를서로켠다/파이즈리한다
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_20
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;주도권은 조교 대상(ACT분류가 성교 봉사 /ACT:승마위의 경우)
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && SEX_INITIATIVE(MASTER)
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME%의 환심을 얻기 위해, %CALLNAME:MASTER%는 힘껏 허리를 사용했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는%CALLNAME%를 기쁘게 하려고 허리를 사용하고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 허리사용으로 상대를 느끼게 하는 일에 기쁨을 찾아내고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;SEX_INITIATIVE(MASTER)를 쓰지 않았던 경우는 여기서 보완됩니다만 범위가 넓기 때문에
;최저한, TEQUIP:성교중/성교봉사중, IS_COMGRONAME/IS_NOWACTNAME든지로 나누어 쓰지 않으면 정합성은 무너지는 일이 될까하고
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우(ACT분류가 성교 /ACT:역강간/성기를서로켠다/파이즈리한다의 경우)
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%가 허리를 아무리 털어도, 자신의 쾌감이 강해질 뿐이었다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME%의 신체에 빠지도록(듯이), %CALLNAME:MASTER%의 허리는 마음대로 움직여 쾌감을 탐낸다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 자신이 기분이 좋게 여차저차허리를 흔들고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「그런 식으로, 칠해서는…… 읏♪」
			PRINTFORMW 「그다지 운동하고 있지 않기 때문에…… 상당히 고기가 붙어 있는 것이, 부끄럽습니다……」
		ELSE
			PRINTFORMW 「무엇을 굽실굽실 허리를 흔들고 있습니다?」
			PRINTFORMW 「나의 포로와 완성되는 것은 좋습니다만, 분명하게 인내 할 수 없으면 또 심한 꼴을 당하게 해요?」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM21, 안에내！
;─
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·조교 대상이 남자의 경우는 튕겨진다
;·의존도 변화(+10, +10)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_21
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 안에내！
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 범해져 쾌락으로 마음을 어지럽혀지면서
		;자포자기가 된 것처럼, 이제(벌써) 안으로 좋은, 안에 내고라고 말했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;정상위, 대면좌위의 경우는 지문이 변화
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;정상위/대면좌위의 경우	%CALLNAME:MASTER%는 %CALLNAME:TARGET%의 허리를 양 다리로 단단히안아, 안에 내와 속삭였다…
		;그 외의 경우			%CALLNAME:MASTER%는 관철해지는 쾌감에 빠져, 안에내！라고 외쳤다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 범해지는 쾌감과 정욕에 지배되어
		;황홀한 기분의 표정으로 어떻게 되어도 괜찮은, 안에 갖고 싶다고 외쳤다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM22, 안에내지마！
;─
;·REACT분류:허가를청한다
;·REACT파생 1(안에 낸다)의 경우는 벌포인트 추가. REACT분류:없음
;·REACT인상:나쁜인상
;·의존도 변화(0, -10)
;·인내 플래그는 소멸한다
;·조교 대상이 남자의 경우는 튕겨진다
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;-------------------------------------------------
;·TFLAG:REACT파생(0=소원을 (들)물은/1=안에 낸다)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_22
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 안에내지마！
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;0203판 현재, 파생해도 지문은 공통
	;=================================================
	;REACT파생 1, 안에 낸다
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 수치도 세상소문도 없고%CALLNAME%를 요구하고 싶은 감정을 열심히 뿌리쳐,
			;중만은 안돼, 밖에 내라고 외쳤다…
			PRINTFORMW 
		ELSE
			;%CALLNAME:MASTER%는%CALLNAME%의 분노를 사는 것을 무서워하면서도,
			;교배되는 것을 무서워해, 안은 그만두어, 내서는 안돼와 비통하게 외쳤다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 관철해지는 쾌감을 참으면서, 안에내지마！라고 외쳤다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 힘 없고%CALLNAME%에 범해져 축 늘어지면서도
		;중만은 그만두어, 허락해와 애원 했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;고통계 반응
;=====================================================================
;·가학계 행동이나 삽입을 수반하는 각종 행동으로 볼 수 있다
;·0203판 현재, 고통계에 의존도 변화의 처리 없음
;-------------------------------------------------

;─
;COM30, 아픔을참는다
;─
;·REACT분류:소극적 따른다
;·REACT인상:약간좋은인상
;·SOURCE:아픔이 없는 경우는 튕겨진다
;-------------------------------------------------
;·트리거가 되는 행동, 소스, 파라미터
;·SOURCE:아픔/구속
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:가슴애무/입으로애무/애널애무/애널빨고/키스/파이즈리한다
;ACT분류:도구
;	ACT:바이브/애널바이브/애널비즈
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:봉사
;	ACT:이마라치오
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_30
;=================================================
;·일정 이상의 쾌SOURCE가 있는 경우 지문이 변화한다
;※REACT파생에 대입되어 있지 않기 때문에, TFLAG:REACT파생에서는 참조 할 수 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌SOURCE가 일정 이상
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && SOURCE:MASTER:쾌C + SOURCE:MASTER:쾌V + SOURCE:MASTER:쾌A + SOURCE:MASTER:쾌B > 10
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 여러가지 감정을 삼켜, 쾌감과 고통을 동시에 참았다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 아픔안에 쾌감을 의식해, 무서워하면서 양쪽 모두를 참았다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;쾌감과 고통이 동시에 덮쳐, %CALLNAME:MASTER%는 흐려진 소리를 흘렸다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 아픔을 감추기 (위해)때문에, 동시에 복받치는 쾌감에 달라붙었다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「무엇인가……」
		PRINTFORMW 「소리에 『색』이 섞여 오고 있지 않습니까……?」
		PRINTFORMDW 필사적으로 고개를 젓는다%CALLNAME:MASTER%를, 아구는 눈을 치켜 뜨고 봄에 응시했다.
		PRINTFORMW 「이것은 이것은…… 후후, 즐거움이군요?」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 고통을 견디면서,%CALLNAME%의 행위를 받아들였다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 고민의 신음을 흘리면서 아픔을 참았다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 아픔을 참았다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 이빨을 이를 악물어 고통을 참았다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「아프지요?   슬프지요……」
		PRINTFORMW 「그렇지만, 아프게 여겨지는 것 같은 일을 했기 때문에이랍니다……?」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM31, 비명을올린다
;─
;·REACT분류:소극적 따른다
;·REACT인상:0에서멀어짐
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류:애무
;	ACT:가슴애무/입으로애무/애널애무/애널빨고/키스/파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:바이브/애널바이브/애널비즈
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:봉사
;	ACT:이마라치오
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그/매도/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트/공중육변기플레이
;-------------------------------------------------
;·TFLAG:REACT파생(0=아픔에 의한 비명/1=공포에 의한 비명)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_31
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 공포에 의한 비명
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 아첨하는 것 같은 비명을 올렸다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 무서워한 것처럼 비명을 올려 보였다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 공포를 느껴 비명을 올렸다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 이상한 공포를 느껴 비명을 올렸다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;REACT파생 1를 썼을 경우는 REACT파생 0, 아픔에 의한 비명 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는%CALLNAME%가 좋아하는 고통의 비명을 자연히(에) 소리에 냈다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 비명을 올려 고통에 몸부림쳐 보였다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 아픔을 견딜 수 있지 못하고 비명을 올렸다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 아픔의 너무 비통한 외침을 올렸다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「아아, 어째서지요……」
		PRINTFORMW 「그 소리를 듣고 있으면, 뭔가 오슬오슬 돋우는 것이 있습니다……」
		PRINTFORMDW %CALLNAME:MASTER%의 비명을 (들)물으면서, 아구는 황홀의 미소를 띄웠다……
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM32, 날뛴다
;─
;·파생에 의해 날뛰고 성공(REACT분류:날뛴다), 날뛰고 실패(REACT분류:소극적 따른다)로 나누어진다
;·날뛰고가 통과하면 도구계/성교계/성교 봉사계 커맨드를 방해한 취급이 된다
;·벌포인트 추가
;·REACT인상:약간나쁜인상
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스
;		성기를서로켠다/파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그/관장세트
;ACT분류:휴게
;	ACT:치료한다
;ACT분류:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다
;-------------------------------------------------
;·TFLAG:REACT파생(0=날뛰고 성공/1=줄구속에 의한 날뛰고 실패/2=제지에 의한 날뛰고 실패)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_32
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT파생 2, 제지에 의한 날뛰고 실패(REACT분류:소극적 따른다)
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 2
		;%CALLNAME:MASTER%는 날뛰어 저항하려고 했지만, %CALLNAME:TARGET%에 억제 당했다…
		PRINTFORMW 「조, 조금…… 너무 강하게 저항하지 말아 주세요!」
		PRINTFORMW 「난폭은 좋지 않…… 네요?」
		PRINTFORMDW 급소를 오돌오돌 잡으면서 생긋 웃는 아구에,%CALLNAME:MASTER%는 수긍할 수 밖에 없었다……
		KFLAG:151 -= 1
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT파생 1, 줄구속에 의한 날뛰고 실패(REACT분류:소극적 따른다)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		;속박된 %CALLNAME:MASTER%는 날뛰어 저항하려고 했지만, 줄이 한층 더 먹혀들어 다만 아픈 것뿐이다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;파생 1으로 2를 썼을 경우는 파생 0, 날뛰고 성공(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
	;※파생 마다의 내용의 차이가 비교적 크기 때문에, 나누어 쓰는 것이 무난하다라고 생각합니다
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		;%CALLNAME:MASTER%는 날뛰어 %CALLNAME:TARGET%의 행동을 방해했다. %CALLNAME:TARGET%는 꽤 화나 있는 것 같다…
		PRINTFORMDL
		PRINTFORMW 「꺗……」
		PRINTFORMDL
		PRINTFORMDW 가녀린 신체를 튕겨날려진 아구가, 다다미 위를 누웠다.
		PRINTFORMDW 스쳤을 것인가, 그 손의 갑은 붉게 물들고 있다……
		PRINTFORMDW 당분간 정신나가고 있던 아구로 있었지만, 손등의 피를 없는 취하면,%CALLNAME:MASTER%의 사타구니를 용서 없게 찼다.
		PRINTFORMDW 아픔에 굴러 몸부림친다%CALLNAME:MASTER%를, 아구는 차갑게 내려다 보고 있다……
		PRINTFORMDL
		PRINTFORMW 「아무리 종마라고 해도, 날뛴다면 거세할 수 밖에 없어요?   알고 있습니까?」
		PRINTFORMW 「…… 좋다. 그렇지만, 부부답게 있기 전에…… 한 번 분명히 상하 관계를 철저히 가르친 (분)편이, 서로의 때문인 것 같네요?」
		KFLAG:153 = 1
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM33, 무서워한다
;─
;·REACT분류:허가를청한다
;·일정 조건으로 분노를 사, 벌포인트 추가
;-------------------------------------------------
;·트리거 이벤트제
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류:1:애무
;	ACT:풋잡한다/밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:줄/아이마스크/볼개그/관장세트/애널바이브/애널비즈
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/공중육변기플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;-------------------------------------------------
;·TFLAG:REACT파생(0=허가를청한다/1=의견을 내다니 좋은 담력이군요!)
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_33
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;ACT, 공중육변기플레이에서는 파생하지 않는다
	;=================================================
	;REACT파생 1, 벌포인트 추가
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는%CALLNAME%보다 오히려 이상하게 될 것 같은 자신에게 무서워하고 있는 것 같다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 분한 생각을 삼켜, 얌전하게 공포의 시선을 향했다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 공포를 느끼고 무서워하고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 작은 동물과 같이 무서워하고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「무서운, 입니까……?」
		PRINTFORMW 「괜찮아, 힘을 빼…… 내가 반드시, 행복하게 해 드릴테니까……」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;봉사계 반응 COM40 접수대
;=====================================================================
;·조교자에게로의 애무를 포함한 행동으로 볼 수 있다
;·ACT분류:봉사+트리거 이벤트제
;-------------------------------------------------

;─
;COM40, 거부한다
;─
;·REACT분류:거부
;·REACT인상:약간나쁜인상
;·벌포인트 추가
;·의존도 변화(-2, -4)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/가슴애무강제
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:영양제를먹인다
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_40
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM40, 거부한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 미혹을 보이면서도 요구에는 응하지 않았다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 압도되면서도 요구는 삼키지 않았다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%의 요구를 거부했다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 강한 의지로 요구를 퇴짜놓았다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「도 없음, 입니까」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM41, 소극적한다
;─
;·REACT분류:소극적 따른다
;·REACT파생 1는 "소극적인의가 마음에 들지 않는다"되어 벌포인트 추가
;·REACT인상:약간좋은인상
;-------------------------------------------------
;·TFLAG:REACT파생 == 0, 소극적이지만 좋은가
;·의존도 변화(-3, +2)
;-------------------------------------------------
;·TFLAG:REACT파생 == 1, 소극적인의가 마음에 들지 않는다
;·의존도 변화(-5, 0)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:의상의변경을요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/애널섹스시킨다
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_41
;=================================================
;·파생에 관련되지 않고 지문은 공통
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;본체 측에 전용의 처리 없음
	;=================================================
	;파생 1, 소극적인의가 마음에 들지 않는다(벌포인트 가산)
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신으로부터 행동하는 용기가 없는 것 같다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 분한 듯이 하면서도 거역하려고는 하지 않는다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 어떻게든%CALLNAME%가 말하는 대로 하고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 역네 없다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「간지럽습니다. 좀 더 격렬하고……」
			PRINTFORMW 「나의 취향도, 확실히 기억해 주세요……」
		ELSE
			PRINTFORMW 「완전히, 이것이라면 버터개가 아직 사용으로가 있다고 하는 것」
			PRINTFORMW 「압니까?   동물 이하예요, 동물 이하」
			PRINTFORMW 「다만조차 성적 기능이 뒤떨어지고 있는데, 더 이상 봉사 능력까지 뒤떨어지고 있으면…… 밥이 될 수 밖에 노우가 없어요?」
		ENDIF
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM42, 적극적한다
;─
;·REACT분류:적극적 따른다
;·REACT인상:좋은인상
;·인내 플래그 해소
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:의상의변경을요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/애널섹스시킨다
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_42
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자기보다%CALLNAME%를 우선하려고 하고 있다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 의식해 기특하게 행동하려고 하고 있는 것 같다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 진행되어%CALLNAME%에 힘쓰고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 어쨌든%CALLNAME%를 기쁘게 하고 싶은 것 같다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		IF KFLAG:153 == 0
			PRINTFORMW 「응, 앗……♪  손대어지고 있는 곳으로부터, 사랑이 전해져 옵니다……♪」
			PRINTFORMW 「거기, 거기!   좀 더 상냥하게 해 주세요……」
			PRINTFORMDW 허리를 여차저차 흔들어, 아구는%CALLNAME:MASTER%의 봉사에 참고 있다……
		ELSE
			PRINTFORMW 「응…… 돼지치고는, 훌륭해요……♪」
			PRINTFORMW 「엉덩이의 구멍에 이를 때까지, 확실히 빨게 한 보람이 있었어요」
			PRINTFORMW 「지금은 자신으로부터 진행되어, 나의 엉덩이에 얼굴을 묻으러 오니까……♪」
		ENDIF
		KFLAG:151 += 1
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM43, 애정을담아준다
;─
;·일정 이상의 호감도, 조교레벨이 필요
;·REACT분류:적극적 따른다
;·REACT인상:좋은인상
;·인내 플래그 해소
;·반발 각인이 있으면 튕겨진다
;·의존도 변화(+6, +10)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/애널섹스시킨다
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_43
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을담아준다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는 평정을 가장하고 있지만, 기쁜 듯한 표정을 숨길 수 없다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 넋을 잃은 얼굴로 정중하게 명령을 구사할 수 있었다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%의 태도는 사랑스러운 사람에 힘쓸 때의 그것 그 자체였다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「아훗, 귀하……♪」
		PRINTFORMW 「나도, 사랑하고 있어요……♪」
		PRINTFORMW 「자, 오늘 밤도 확실히 종을 쳐박을 수 있도록(듯이), 의욕에 넘친다고 합시다?」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM44, 태연하게한다
;─
;·REACT분류:태연하게 따른다
;·REACT인상:약간좋은인상
;·의존도 변화(+2, -2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:의상의변경을요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/애널섹스시킨다
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_44
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 평상심을 유지하려 하고 있다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 무리를 하면서도 평정을 가장하고 있다…
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는%CALLNAME%를 기쁘게 해 환심을 권하려고 하고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%에는%CALLNAME%의 모습을 살필 뿐(만큼)의 여유가 있는 것 같다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;그 외의 반응 COM50 접수대
;=====================================================================
;·특정의 장면에서 발생하는 이상한 행동
;-------------------------------------------------

;─
;COM50, 자위하기시작한다
;─
;·REACT분류:받아들인다
;·파생 1, 마음대로 자위 한데의 경우는 벌포인트 추가
;·REACT인상:나쁜인상
;·인내 플래그 해소
;-------------------------------------------------
;·트리거 이벤트제
;-------------------------------------------------
;·줄로 속박되고 있으면 안돼
;·벌써 자위하고 있다, 성교중, 성교봉사중은 튕겨진다
;-------------------------------------------------
;TFLAG:REACT파생 == 0, 마음대로 자위 하고 있지만 좋은가, 의존도 변화(-1, 0)
;TFLAG:REACT파생 == 1, 마음대로 자위 하지마, 의존도 변화(-3, +2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다
;ACT분류:1:애무
;	ACT:키스
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:
;ACT분류:4:수치
;	ACT:자위과시하고/수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/다리빨고강제/가슴애무강제
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이
;ACT분류:9:휴게
;	ACT:쉬게한다
;ACT분류:10:성교 봉사
;	ACT:
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_50
;=================================================
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;본체 측에 전용의 처리 없음
	;=================================================
	;REACT파생 1, 마음대로 자위 하지마
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT파생 == 1
		PRINTFORMW 「…… 무엇을 하고 계십니까?」
		PRINTFORMDL
		PRINTFORMDW 아구의 극한의 눈동자가%CALLNAME:MASTER%를 관철한다.
		PRINTFORMDW 그 두 눈동자에는 숨기지 못할 실망까지도가 스며 나오고 있다……
		PRINTFORMDL
		PRINTFORMW 「참아라, 라고 말하고 있는데 자신으로부터 무엇을 새기기 시작하다니」
		PRINTFORMW 「하아…… 알았습니다. 한 번, 돼지와 같이 신체에 철저히 가르치는 것이 좋은 것 같네요」
		KFLAG:153 = 1
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는 자위를 하는 것으로 쾌감에 빠져 버리려고 하고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 참지 못하고 마음대로 자신을 위로하고 있다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%의 손은 무의식 중에 자신의 성기를 만지작거리고 있는 것 같다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「자위 해라, 라고 하는 기억은 없습니다만……」
		PRINTFORMW 「뭐, 사정까지 도달하지 않으면 상관하지 않지만도,」
		PRINTFORMW 「그렇지만 그 거, 자신이 괴로워질 뿐(만큼)인 것은?」
		RETURN LOCAL:1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM51, 도구를제외한다
;─
;·REACT분류:날뛴다
;·TEQUIP:(바이브/애널바이브/애널비즈/관장기＋플러그/클리캡/오나홀/니플캡/아이마스크/볼개그/페니스밴드)를 장착하고 있는 것이 조건
;·조교 대상이 자위중인 경우는 중단이 된다
;·REACT인상:약간나쁜인상
;·의존도 변화(-3, -10)
;-------------------------------------------------
;·제외하는 도구는 한 번에 1개까지
;※우선도는 이하와 같다
;(아이마스크>관장기＋플러그>애널바이브>바이브> 오나홀>클리캡>볼개그>니플캡> 애널비즈)
;-------------------------------------------------
;선택 가능한 조건…
;·TEQUIP:(바이브/애널바이브/애널비즈/관장기＋플러그/클리캡/오나홀/니플캡/아이마스크/볼개그/페니스밴드)를 장착하고 있는 것이 조건
;·TEQUIP:줄/성교중은 실행 불가
;-------------------------------------------------
;·제외한 도구의 TEQUIP 번호가 TFLAG:REACT파생에 들어간다
;·0203판 현재, 이 COM가 선택지에 나오지 않는 불편 있어?
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_51
;=================================================
;·지문은 범용 처리가 있을 뿐
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM51, 도구를제외한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;REACT파생분기를 추가하는 경우는 이 개소의 상층에서
	;=================================================
	;그 외·조건 없음
	;기입 체크
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;%CALLNAME:MASTER%는 장착된 도구를 마음대로 제외해, %CALLNAME:TARGET%는 매우 화나 있는 것 같다…
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;─
;COM52, 하는대로
;─
;·효과 없음. 실행할 수 있는 반응이 한 개도 없을 때의 구제
;·조교자에게 좋을대로 희롱해지고 한편, 하라는 대로 되는 목표 상황
;·REACT파생 마다 REACT분류, 의존도 변화가 바뀐다
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_52
;=================================================

;------------------------------------------------
;REACT분류:받아들인다/의존도 변화(+4, +10)
;성교계 ACT·역강간
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 「무저항에 범해진다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;PRINTFORML %CALLNAME:MASTER%는 힘이 들어가지 않는 신체를 무저항에 범해져 이송되는 쾌락에 허덕이고 있다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;REACT분류:받아들인다/의존도 변화(+10, +10)
;성교계 봉사계 ACT·승마위
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 2, 「빠진 것처럼허리를흔드는」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 2
	;%CALLNAME:MASTER%는%CALLNAME%의 신체에 빠진 것처럼, 숨을 어지럽혀 오로지 허리를 흔들고 있다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;ACT(의상 변경/자위/방뇨/개장/이라마치오)
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 3, 「말해지는 대로 천천히 따른다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 3
	;%CALLNAME:MASTER%는 다만%CALLNAME%에 말해지는 대로 따랐다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;봉사계 ACT
;REACT분류:받아들인다/의존도 변화(+8, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 4, 「매료되어진 것처럼 봉사를 반복한다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 4
	;마치 매료되어진 것처럼, %CALLNAME:MASTER%는 무심하게%CALLNAME%에 봉사했다…
	;심 여기에 있지 않고라고 하는 모습으로, 그 손놀림은 이상한 것이다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;ACT(위협한다/벌과 선언/고함친다/기분나쁘게웃는다/매도)
;반항적인 소질이 있어, 굴복각인 5 이하의 경우
;REACT분류:소극적 따른다/의존도 변화(0, -2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 5, 「분한 듯이 노려본다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 5
	;%CALLNAME:MASTER%는 분한 듯이 입술을 깨물어,%CALLNAME%를 올려보았다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;ACT(위협한다/벌과 선언/고함친다/기분나쁘게웃는다/매도)
;REACT분류:소극적 따른다/의존도 변화(+2, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 6, 「무서워한 것처럼 응시한다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 6
	;%CALLNAME:MASTER%는 무서워한 것처럼%CALLNAME%를 응시했다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;가학계 ACT·고통이 큰 경우
;REACT분류:소극적 따른다/의존도 변화(+8, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 7, 「소리가 되지 않는 비명을 올려 몸부림쳐 뒹군다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 7
	;%CALLNAME:MASTER%는 아픔의 그다지 소리가 되지 않는 비명을 올려 몸부림쳐 뒹굴었다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT 또한 자위중인 경우
;REACT분류:받아들이는 /REACT인상:나쁜인상/의존도 변화(-2, +2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 8, 「오나니─에 열중해 듣지 않았다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 8
	;%CALLNAME:MASTER%는 자위에 몰두해 온전히 대답을 할 수 없었다…
	;%CALLNAME%는 불만인 것 같다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT
;REACT분류:받아들인다/의존도 변화(+4, +2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 9, 「기분 좋아서 선대답」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 9
	;%CALLNAME:MASTER%는 쾌감에 몸부림 하면서 수긍할 수 밖에 없었다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT 이외의 경우
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 10, 「머리 진흰색 황홀 상태」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 10
	;%CALLNAME:MASTER%는 주어지는 쾌감에 희롱해져 황홀이 되고 있다…
	;녹초가된 머릿속은 새하얗게 물들어, 아무것도 생각하는 것이 할 수 없다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;회화계 ACT
;REACT분류:받아들인다/의존도 변화(+8, +4)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 11, 「스스로도 모르는 채 부끄러운 것을 말해버린다」
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 11
	;%CALLNAME:MASTER%는 어딘가 망연히 한 채로 대답 했다…
	;스스로도 잘 모르는 채로 부끄러운 것을 말해버리고 있는 생각이 든다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;대만족 보너스로 쉬게 해를 선택했을 경우
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;포상에 의한 한번휴식의 경우
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:한번휴식 && IS_POLICY("오늘", "휴게")
	;%CALLNAME:MASTER%는 안심한 것처럼 몸이 힘을 뺐다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;조교자, 혹은 조교 대상 상태에 의해 한번휴식이 되었을 경우
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;한번휴식의 경우
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:한번휴식
	;%CALLNAME:MASTER%는 안심한 것처럼 몸이 힘을 뺐다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;------------------------------------------------
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;%CALLNAME:MASTER%는 신체를 내던져, 되는 대로 되어 있다…
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;=====================================================================
;특수한 반응 COM70～
;=====================================================================

;─
;COM70, 가게해!
;─
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도 변화(+10, +6)
;-------------------------------------------------
;선택 가능한 조건…
;·초조하게 해 안은 아니다
;·TCVAR:MASTER:갈것같다가 서 있다
;·애무계 ACT(키스/밀어넘어뜨리기 이외) 한편 각 부위의 PALAM가 5000이상이다
;※상기 조건에 들어맞는 경우는 가능
;-------------------------------------------------
;·TFLAG:REACT파생(0=들은체 만체 한다/1=가게해 준다/2=가게해 주지 않는다)
;-------------------------------------------------
;ACT에 있어서 TCVAR:MASTER:초조하게해번 == 1(가게 한다)에서 만나도 COM70의 선택으로 TCVAR:MASTER:초조하게해번 > 1(초조하게 한다)(이)가 되는 경우 있어
;현재의 사양에서는 이 손의 변화를 참조할 방법은 없다. 본체측의 대응이 바람직한다!
;-------------------------------------------------
;=================================================
@KOJO_COM_K48_70
;=================================================
;우선 최소한의 분기를 놓아둡니다만
;PENIS(MASTER) 등으로 페니스의 유무를 참조하는 것이 무난할지도 모릅니다
;-------------------------------------------------

;%CALLNAME:TARGET%는 심술궂은 미소를 띄우면 %CALLNAME:MASTER%를 초조하게 하기 시작했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 2, 가게해 주지 않는다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 2
	;=================================================
	;ACT분류, 애무
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "애무")
		PRINTFORMW 「흠칫, 흠칫 신체가 떨고 있네요……」
		PRINTFORMW 「군침을 늘어뜨리면서, 동녀에게 향해 간원 하는만큼 절정 하고 싶습니까?」
		PRINTFORMDW 낄낄 웃으면서, 아구는 애무를 계속한다.
		PRINTFORMDW 나 개미의 문이동까지 정중하게 희롱해져%CALLNAME:MASTER%는 비명을 올렸다……
		PRINTFORMW 「이봐요, 좀 더 많이 부탁해 주세요」
		PRINTFORMW 「작은 아이에게 만져져, 필사적으로 졸라댑니다?」
		KFLAG:151 += 1
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;성교봉사중
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "성적봉사") || TEQUIP:성교봉사중
		IF V_SEX(MASTER)
			PRINTFORMW 「아직…… 아직 안돼, 입니다!」
			PRINTFORMW 「한계까지, 모아 둬, 모아 둬……」
			PRINTFORMW 「, 그리고, 나의 사잉키라고 주지 않으면 싫습니닷!」
			PRINTFORMDW 끈적거린 물소리를 세우면서, 작은 신체로 아구는 열심히 허리를 흔들고 있다……
		ELSE
			PRINTFORMW 「응웃…… 안돼, 예요……」
			PRINTFORMW 「엉덩이에는 내 버리면, 모처럼 모아둔 정액님……」
			PRINTFORMW 「전부, 쓸데없게 되어 버립니다……?」
			PRINTFORMW 「그렇지 않으면, 파파는 보지보다…… 엉덩이의 구멍이 비어나오고 인가……?」
			PRINTFORMDW 나이 다 녹은 여자의 얼굴로, 아구는 느슨느슨%CALLNAME:MASTER%의 것을 물고 있다……
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「느긋한 자극이 안타깝네요……」
		PRINTFORMW 「그렇지만, 그 얼굴…… 좀 더 확실히, 기억하게 해 주세요」
		PRINTFORMW 「분명하게 참지 않았으면…… 또, 심한 눈 봐 버려요?」
		RETURN LOCAL:1
	ENDIF
ENDIF

;%CALLNAME:TARGET%는 상냥하게 미소지으면서 애무를 계속했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 1, 가게해 준다
;기입 체크
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 1
	;=================================================
	;ACT분류, 애무
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "애무")
		PRINTFORMW 「에에, 분명하게 참아 받을 수 있다면」
		PRINTFORMW 「얼마든지, 더 없이 행복한 절정을 맛보게 해 드려요」
		PRINTFORMW 「그러니까…… 나의 말하는 일을 잘 들…… 어?」
		PRINTFORMDW 끈적끈적으로 한 애무로, 아구는%CALLNAME:MASTER%의 성감을 높여 간다……
		PRINTFORMDW 녹은 웃는 얼굴로 미소짓는 동녀에게,%CALLNAME:MASTER%는 신체를 진동시켰다.
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;성교봉사중
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && MENUMATCH(TFLAG:ACT, "성적봉사") || TEQUIP:성교봉사중
		IF V_SEX(MASTER)
			PRINTFORMW 「아구의, 상태는, 어떻습니까……?」
			PRINTFORMW 「아웃…… 칭찬 받을 수 있는 것, 가지 끝, 주위입니다♪  구멍 더해♪」
			PRINTFORMW 「아공, 노력해 버리네요♪」
			PRINTFORMDW 아구는 생긋 미소지어, 허리를 강탈 세우고%CALLNAME:MASTER%의 질내사정을 재촉하고 있다……
			PRINTFORMDW 큐우큐우 밭두둑질이, 훌쩍거려 붐비는 것처럼 남근을 몰아세운다……
		ELSE
			PRINTFORMW 「믿을 수 없습니다…… 모처럼의 정액, 엉덩이에 내 버리다니이……」
			PRINTFORMW 「(이)든지, 그렇지만, 기분이 좋습니다……♪」
			PRINTFORMW 「자지로, 엉덩이의 구멍, ♪  는 퍼져 버리고 있습니다아♪」
			PRINTFORMDW 항학의 쾌락에 열중한 것인가, 아구의 응답은 부족하다.
			PRINTFORMDW 때때로, 천할 정도에 소리가 나오는 허리 흔들기로, 아구는%CALLNAME:MASTER%의 주도권을 잡았다.
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;그 외
	;기입 체크
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1
		PRINTFORMW 「좋아요…… 자, 나에게 신체를 맡겨……?」
		PRINTFORMW 「힘을 빼…… 숨을 들이마셔…… 아구의 소리만, (듣)묻고 있어 주세요……」
		RETURN LOCAL:1
	ENDIF
ENDIF

;파생 0의 경우의 처리 없음
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 0, 요구를 들은체 만체 한다
;기입 체크
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:REACT파생 == 0
	SELECTCASE RANDCOM(3, 0)
		CASE 0
			PRINTFORMW 
		CASE 1
			PRINTFORMW 
		CASE 2
			PRINTFORMW 
	ENDSELECT
	RETURN LOCAL
ENDIF
