;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	모코우 구상 조교 이벤트 구상 파일
;
;	조교 턴중 이벤트, 절정 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV20, 절정 구상
;========================================================================
;·조교자나 조교 대상이 절정에 이를 때 표시
;· 각 절정은 개별의 호출을 가지지 않고, 모두 파생으로 처리된다
;-------------------------------------------------
;=================================================
;관련 변수…
;
;TFLAG:전회질내사정, (1=V성교/2=V성교 봉사/3=V성교(하늘 쏘아 맞히고)/4=V성교 봉사(하늘 쏘아 맞히고)/5=A성교/6=A성교 봉사/7=A성교(하늘 쏘아 맞히고)/8=A성교 봉사(하늘 쏘아 맞히고))
;TFLAG:REACT분류, (0=없음/1=소극적 따른다/2=적극적 따른다/3=허가를청한다/4=날뛴다/5=요구에 응하지 않는, 거부/6=도망치는, 발버둥 친다)
;NOWEX:사정, (1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;TEQUIP:자위중, (bit0=조교 대상/bit1=조교자)
;
;=================================================
;함수류…
;
;MASTER_EX
;인  수:ARG:0… 애무자→1. 조교 대상 자신/2. 조교자/4. 조수 1/8. 조수 2/16. 조수 3
;                      /64. 도구/128. 변소 이용자  생략 하면 애무자를 묻지 않는다
;              비트화에서의 지정이 가능
;       ARG:1…(생략가능) 절정의 종별→1. C절정/2. V절정/3. A절정/4. B절정
;                                  /11. 분유/12. 사정/13. 방뇨
;ARG:0(bit0 =조교 대상 자신 /bit1 =조교자 /bit2 =조수 1/bit3 =조수 2/bit4 =조수 3/bit6 =도구 /bit7 =변소 이용자)
;-------------------------------------------------
;TARGET_EX
;인  수:ARG:0… 애무자→1. 조교 대상/2. 조교자 자신/4. 조수 1/8. 조수 2/16. 조수 3
;              비트화에서의 지정이 가능. 생략 하면 애무자를 묻지 않는다
;       ARG:1…(생략가능) 절정의 종별→1. c절정/2. v절정/3. a절정/4. b절정
;                                  /11. 분유/12. 사정
;ARG:0(bit0 =조교 대상 /bit1 =조교자 자신 /bit2 =조수 1/bit3 =조수 2/bit4 =조수 3)
;-------------------------------------------------
@KOJO_EVENT_K31_20(ARG)
;-------------------------------------------------
;TFLAG:전회질내사정;(1=V성교/2=V성교 봉사/3=V성교(하늘 쏘아 맞히고)/4=V성교 봉사(하늘 쏘아 맞히고)/5=A성교/6=A성교 봉사/7=A성교(하늘 쏘아 맞히고)/8=A성교 봉사(하늘 쏘아 맞히고))
;-------------------------------------------------

;─
;질내사정(MASTER→TARGET)
;─
;·V성교 봉사계로 질내사정 하는 경우에 표시
;·TFLAG:106은 뽑지 않는 판정 마다+1된다
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;-------------------------------------------------

;TFLAG:전회질내사정 == 4(하늘 쏘아 맞히고)는 연주해 둡니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정(조교 대상→조교자)
;━━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
	;=================================================
	;가[연모]
	;=================================================
	IF 구상기입체크(100) && KFLAG:601 == 1
		PRINTFORML 「후아, 가득, 가득 나오고 있다」
		PRINTFORMW 「나의 안에서 기분 좋아져 준 것이구나」
		RETURN 2
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「후후, 가득 나왔군요」
		PRINTFORMW 「사실, 바보같이 허리 흔들어 버려, 그렇게 나중 기분 좋았어?」
	;아라이먼트부
	ELSE
		PRINTFORML 「후후, 가득 나왔군요」
		PRINTFORMW 「사실, 바보같이 허리 흔들어 버려, 그렇게 나중 기분 좋았어?」
	ENDIF
	RETURN 2
ENDIF

;─
;애널내 사정(MASTER→TARGET)
;─
;·A성교 봉사계로 장내 사정하는 경우에 표시
;·TFLAG:106, 뽑지않고는 뽑지 않는 판정 마다+1된다
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;애널내 사정(조교 대상→조교자)
;━━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && GROUPMATCH(TFLAG:전회질내사정, 6, 8)
;ENDIF

;─
;질내사정(TARGET→MASTER)
;─
;·V성교계로 질내사정 하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;·현재의 사양에서는 조교자가 드라이사정 하는 것은 매우 희(거의 없어?)
;-------------------------------------------------

;;TFLAG:전회질내사정 == 3(하늘 쏘아 맞히고)은 연주해 둡니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정(조교자→조교 대상)
;━━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
	;=================================================
	;COM가안에내！
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("안에내！")
		;아라이먼트정
		IF ALI(0)
			PRINTFORML 「응하아. 가득 내 버렸다」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%안, 정말로 기분 좋았어요」
		;아라이먼트부
		ELSE
			PRINTFORML 「응하아. 가득 내 버렸다」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%안, 정말로 기분 좋았어요」
		ENDIF
		RETURN 2
	ENDIF
	
	;=================================================
	;COM가안에내지마！
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("안에내지마！")
		;아라이먼트정
		IF ALI(0)
			PRINTFORML 「응하아. 가득 내 버렸다」
			PRINTFORML 「응, 심하다는? 그렇지만 기분 좋았던 것이겠지, 그렇게 허리 흔들어 버려」
			PRINTFORMW 「그래서, 안은 싫다고 말해져도 말야」
		;아라이먼트부
		ELSE
			PRINTFORML 「응하아. 가득 내 버렸다」
			PRINTFORML 「응, 심하다는? 그렇지만 기분 좋았던 것이겠지, 그렇게 허리 흔들어 버려」
			PRINTFORMW 「그래서, 안은 싫다고 말해져도 말야」
		ENDIF
		RETURN 2
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「응하아. 가득 내 버렸다」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%안, 정말로 기분 좋았어요」
	;아라이먼트부
	ELSE
		PRINTFORML 「응하아. 가득 내 버렸다」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%안, 정말로 기분 좋았어요」
	ENDIF
	RETURN 2
ENDIF

;─
;애널내 사정(TARGET→MASTER)
;─
;·A성교계로 장내 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;·현재의 사양에서는 조교자가 드라이사정 하는 것은 매우 희(거의 없어?)
;-------------------------------------------------
;IF 구상 기입 체크(0) && GROUPMATCH(TFLAG:전회질내사정, 5, 7)
;ENDIF

;─
;동시절정
;─
;·조교자와 조교 대상이 함께 절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(100) && (NOWEX:MASTER:C절정 || NOWEX:MASTER:V절정 || NOWEX:MASTER:A절정 || NOWEX:MASTER:B절정) && (NOWEX:C절정 || NOWEX:V절정 || NOWEX:A절정 || NOWEX:B절정)
	;=================================================
	;조교자가 사정
	;=================================================
	IF 구상기입체크(100) && NOWEX:사정
		;가[연모]
		IF KFLAG:601 == 1
			PRINTFORML 「하아, 가득 걸어 버렸다.」
			PRINTFORML 「괜찮아? 곧바로 예쁘게 하니까요…」
			PRINTFORMW 그렇게 말하면 모코우는 스스로의 정액을 빨아내,%CALLNAME:MASTER%의 몸을 맑은 시작했다….
			RETURN 2
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「나의 정액으로 새하얗게 되면서 쾌감에 떨리면서%CNAME(MASTER, TARGET)%, 정말로 멋져」 
		;아라이먼트부
		ELSE
			PRINTFORML 「나의 정액으로 새하얗게 되면서 쾌감에 떨리면서%CNAME(MASTER, TARGET)%, 정말로 멋져」
			PRINTFORMW 「이 정복감, 버릇이 될 것 같다」
		ENDIF
		RETURN 2
	ENDIF
	
	;=================================================
	;조교 대상이 사정
	;=================================================
	IF 구상기입체크(100) && NOWEX:MASTER:사정
		;가[연모]
		IF KFLAG:601 == 1
			PRINTFORML 「하아, 가득 나왔군요」
			PRINTFORML 「응, 맛있어」
			PRINTFORMDW 모코우는%CALLNAME:MASTER%의 정액을 건져 올려, 사랑스러운 듯이 입에 넣었다.
			RETURN 2
		ENDIF
		
		LOCALS = \@ TALENT:MASTER:남자 ? 녀 # 나 \@
		;아라이먼트정
		IF ALI(0)
			PRINTFORML 「후후, 가득 걸쳐져 버렸다」
			PRINTFORMDW 「%CNAME(MASTER, TARGET)%도 상당히%LOCALS%를 기쁘게 할 방법을 알려져 왔지 않아?」
		;아라이먼트부
		ELSE
			PRINTFORML 「후후, 가득 걸쳐져 버렸다」
			PRINTFORMDW 「%CNAME(MASTER, TARGET)%도 상당히%LOCALS%를 기쁘게 할 방법을 알려져 왔지 않아?」
		ENDIF
		RETURN 2
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「하아, 하아…. 좋다, 이것」
		PRINTFORMW 「좀더 좀더 기분 좋아지자?」
	;아라이먼트부
	ELSE
		PRINTFORML 「하아, 하아…. 좋다, 이것」
		PRINTFORMW 「좀더 좀더 기분 좋아지자?」
	ENDIF
	RETURN 2
ENDIF


;─
;조교 대상 사정
;─
;·조교 대상이 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 가감(상태)해 사용하는 것이 바람직함
;-------------------------------------------------
IF 구상기입체크(100) && NOWEX:MASTER:사정
	;=================================================
	;가[연모]
	;=================================================
	IF 구상기입체크(100) && KFLAG:601 == 1
		PRINTFORML 「하아, 가득 나왔군요」
		PRINTFORML 「응, 맛있어」
		PRINTFORMDW 모코우는%CALLNAME:MASTER%의 정액을 건져 올려, 사랑스러운 듯이 입에 넣었다.
		RETURN 2
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「어때? 기분 좋았어?」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%는 정말로 쾌락의 노예네」
	;아라이먼트부
	ELSE
		PRINTFORML 「보기 흉하게 흘려 보내 버려 아무튼」
		PRINTFORMW 「정말로 동물 이하군요」
	ENDIF
	RETURN 2
ENDIF

;─
;조교자 사정
;─
;·조교자가 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;·현재의 사양에서는 조교자가 드라이사정 하는 것은 매우 희(거의 없어?)
;-------------------------------------------------
IF 구상기입체크(100) && NOWEX:사정
	;=================================================
	;가[연모]
	;=================================================
	IF 구상기입체크(100) && KFLAG:601 == 1
		PRINTFORML 「하아, 가득 걸어 버렸다.」
		PRINTFORML 「괜찮아? 곧바로 예쁘게 하니까요…」
		PRINTFORMDW 그렇게 말하면 모코우는 스스로의 정액을 빨아내,%CALLNAME:MASTER%의 몸을 맑은 시작했다….
		RETURN 2
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「%CNAME(MASTER, TARGET)%새하얗구나」
		PRINTFORMW 「앞으로도, 좀더 좀더 나로 물들여 준다」
	;아라이먼트부
	ELSE
		PRINTFORML 「%CNAME(MASTER, TARGET)%새하얗구나」
		PRINTFORMW 「앞으로도, 좀더 좀더 나로 물들여 준다」
	ENDIF
	RETURN 2
ENDIF

;─
;조교 대상 절정
;─
;·조교 대상이 절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(100) && (NOWEX:MASTER:C절정 || NOWEX:MASTER:V절정 || NOWEX:MASTER:A절정 || NOWEX:MASTER:B절정)
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「좋아, 그 얼굴. 좀더 좀더 보였으면 좋구나」
	;아라이먼트부
	ELSE
		PRINTFORMW 「야무지지 못하고 쾌락 탐내고 있어요, 정말로 이 욕구돼지는」
	ENDIF
	RETURN 2
ENDIF

;─
;조교자 절정
;─
;·조교자가 절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(100) && (NOWEX:C절정 || NOWEX:V절정 || NOWEX:A절정 || NOWEX:B절정)
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「히양, 하아우, 응아아아아아」
		PRINTFORML 「하아, 하아. 가게 되어 버렸다」
		PRINTFORMW 「응 굉장히 좋았다. 이것은 포상 올리지 않으면 안 될까」
	;아라이먼트부
	ELSE
		PRINTFORML 「히양, 하아우, 응아아아아아」
		PRINTFORML 「하아, 하아. 가게 되어 버렸다」
		PRINTFORMW 「응 굉장히 좋았다. 이것은 포상(벌) 해 올리지 않으면 안 될까」
	ENDIF
	RETURN 2
ENDIF

;─
;조교 대상 C절정
;─
;·조교 대상이 C절정 하는 경우에 표시
;·여기에서는 「여자 아이의 C절정」을 상정하고 있다
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:MASTER:C절정
;ENDIF

;─
;조교 대상 V절정
;─
;·조교 대상이 V절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:MASTER:V절정
;ENDIF

;─
;조교 대상 A절정
;─
;·조교 대상이 A절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:MASTER:A절정
;ENDIF

;─
;조교 대상 B절정
;─
;·조교 대상이 B절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:MASTER:B절정
;ENDIF

;─
;조교자 C절정
;─
;·조교자가 C절정 하는 경우에 표시
;·여기에서는 「여자 아이의 C절정」을 상정하고 있다
;·사정은 사정 구상의 분기에서 쓰는 것이 바람직함
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:C절정
;ENDIF

;─
;조교자 V절정
;─
;·조교자가 V절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:V절정
;ENDIF

;─
;조교자 A절정
;─
;·조교자가 A절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:A절정
;ENDIF

;─
;조교자 B절정
;─
;·조교자가 B절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:B절정
;ENDIF

;─
;조교 대상 물총
;─
;·조교 대상이 물총의 경우에 표시
;·현상, ACT손가락삽입해라시에 G스팟 고문에 파생했을 경우에게만 일어난다
;·주인님의 물총은 없다
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:MASTER:물총
;ENDIF

;─
;조교 대상 실금
;─
;·조교 대상이 실금 하는 경우에 표시
;·조교 대상으로 소질:소변버릇이 없으면 발생하지 않는다
;·C절정없이 실금만이라고 하는 것은 드물게 있는 케이스
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:MASTER:방뇨
;ENDIF