;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	모코우 구상 조교 종료 이벤트 구상 파일
;
;	조교 종료 이벤트 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 조교 종료 구상
;========================================================================
;·@EVENTCOMEND로부터 불려 간다
;·구상주가 조교 종료하기 직전에 표시
;·ARG:1, ARG:2의 경우는 지문 표시 후에 PRINTL로 공행. 그 뒤로 구상을 표시
;·ARG:0의 경우는 고유의 지문 없음. 구상 표시전에 PRINTL로 공행이 들어간다
;·RESULT 0이상을 돌려주면 구상 표시 후에 PRINTL로 공행이 들어간다
;-------------------------------------------------
;관련 변수…
;FLAG:일상제어 조교 종료시 상황(1=통상/2=정력 0/3=한번휴식/4=실신했다)
;-------------------------------------------------
@KOJO_EVENT_K31_13(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 보통으로 종료, 1, 조교 대상이 피곤한, 2, 조교 대상이 실신했다)
;-------------------------------------------------

;─
;ARG:1, ARG:2, 피로·실신에 의한 조교 종료 FIRSTEVENT(0)～
;─
;·조교 대상 소모도에 응해 피로/실신으로 나누어진다
;·호출 타이밍은 지문의 표시→PRINTL→구상의 표시
;·EVENT201, 간호(일상 이벤트)의 개소를 쓰는 경우는, 능숙하게 제휴할 수 있도록(듯이)하면 좋을 것입니다
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 피곤함
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 실신했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 2
	IF KFLAG:601 == 1
		IF KFLAG:610 == 0
			PRINTFORML 「깨어났어?」
			PRINTFORMDL 여기는…. 그런가 정신을 잃고 있었는가….
			PRINTFORMDL 상으로부터 들여다 보는 얼굴과 후두부에 느끼는 감촉으로 무릎 베개해 받고 있다고 알아, 일어나려고 한다.
			PRINTFORML 「아아, 좋아요, 좀 더 자도」
			PRINTFORMDL 그런%CALLNAME:MASTER%를 억제해, 모코우는 머리카락을 사랑스러운 듯이 어루만졌다.
			PRINTFORML 「미안해요, 무심코 우쭐해져%CNAME(MASTER, TARGET)%의 일 생각하지 않았었다」
			PRINTFORMW 「다음으로부터는 좀 더 잘 하기 때문에…」
			KFLAG:610 = 1
		ELSE
			PRINTFORML 「깨어난 것 같구나」
			PRINTFORML 「사양하지 않아도 괜찮아요, 내가 무릎 베개 해 주고 싶은 것뿐이니까」
			PRINTFORMW 「그러니까 조금만 더 이대로 있게 해…」
		ENDIF
	ELSE
		;아라이먼트정
		IF ALI(0)
			PRINTFORML 「저것, 조금 너무 격렬하게 했는지」
			PRINTFORML 「… 기분 좋은 것 같은 얼굴 해 버려」
			PRINTFORMW 「… 이 뒤처리 어떻게 하는거야…」
		;아라이먼트부
		ELSE
			PRINTFORML 「저것, 이제 한계?」
			PRINTFORML 「뭐, 그렇다면 그걸로 좋지만」
			PRINTFORMW 「일어나면 스스로 정리하세요」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;─
;ARG:0보통으로 종료 FIRSTEVENT(10)～
;─
;·통상의 조교 종료시의 구상
;·지문 없음. PRINTL에 의한 공행의 뒤로 구상 표시
;·테두리마다 이벤트번호를 털어 있습니다만, 임의로 설정하는 일은 가능합니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;대만족 보너스를 얻고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:이번에대만족
	IF CFLAG:601 == 1
		IF CFLAG:611 == 0
			PRINTFORML 「고마워요」
			PRINTFORMDL 조교가 끝나, 방을 나오려고 한 곳에서 되돌아 보면, 모코우는 그렇게 말했다.
			PRINTFORML 「나, 지금 매우 충실해」
			PRINTFORML 「이런 생활%CNAME(MASTER, TARGET)%에는 폐인만이겠지만」
			PRINTFORML 「그렇지만 미안. 이제(벌써)%CNAME(MASTER, TARGET)%없이는 생각할 수 없으니까」
			PRINTFORML 「생활의 걱정은 하지 않아도 괜찮은거야. 그러니까」
			PRINTFORMW 「그러니까%CNAME(MASTER, TARGET)%는 나를 기분 좋게 시키는 일만 생각해,?」
			CFLAG:611 = 1
		ELSE
			PRINTFORML 「오늘도 굉장히 좋았어요. … 그렇다」
			PRINTFORML 「포상에 오늘은%CNAME(MASTER, TARGET)%가 좋아하는 것 만들어 준다. 기대하고 있어」
			PRINTFORMDW 모코우는 그렇게 말하면 만면의 미소로 방을 나갔다….
		ENDIF
	ELSE
		;아라이먼트정
		IF ALI(0)
			PRINTFORML 「응, 굉장히 매력적으로 좋았다」
			PRINTFORMW 「또 즐겁게 해」
		;아라이먼트부
		ELSE
			PRINTFORML 「이번은 여기까지」
			PRINTFORML 「뭐, 좀 더 괴롭혔으면 좋은거야?」
			PRINTFORML 「안돼. 이제(벌써) 소리 시들어 와 있지 않아」
			PRINTFORMW 「그러면 모처럼의 좋은 소리를 들을 수 없어요」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;*************************************************
;이하, 호감도에 의한 파생
;*************************************************

;━━━━━━━━━━━━━━━━━━━━━━━━
;(조건 없음)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	IF KFLAG:601 ==1
		IF KFLAG:612 == 0
			PRINTFORML 「… 어째서? 어째서 나를 만족시켜 주지 않는거야?」
			PRINTFORMW 「내가 싫어? 이 생활이 불만?」
			PRINTFORML 「너… 어떤 분의 생각이야」
			PRINTFORMW 「너는 나의 노예야! 나를 기분 좋게 시키는 것만 생각하고 있으면 좋은거야!!」
			PRINTW
			PRINTFORML 「나는 필요없는 것, 그 밖에 아무것도! 나는 아무것도 필요하지 않은거야!」
			PRINTFORML 「그러니까… 이니까….」
			PRINTFORMW 「적어도%CNAME(MASTER, TARGET)%만은, 나만의 것으로 있어요…」
			KFLAG:612 = 1
		ELSE
			PRINTFORML 「…. 미안 오늘은 여기까지」
			PRINTFORML 「밥은 적당하게 내 두기 때문에 혼자서 먹어」
			PRINTFORMDW 모코우는 그렇게 말하면 어두운 얼굴로 방을 나갔다….
		ENDIF
	ELSE
		PRINTFORML 「뭔가 식어 와 버렸군…」
		PRINTFORMW 「이번은 여기까지」
	ENDIF
	RETURN 1
ENDIF