;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	妹紅口上 調教終了イベント口上ファイル
;
;	調教終了イベント口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 調教終了口上
;========================================================================
;・@EVENTCOMENDから呼び出される
;・口上主が調教終了する直前に表示
;・ARG:1, ARG:2の場合は地の文表示後にPRINTLで空行。その後に口上を表示
;・ARG:0の場合は固有の地の文なし。口上表示の前にPRINTLで空行が入る
;・RESULT 0以上を返すと口上表示後にPRINTLで空行が入る
;-------------------------------------------------
;関連変数…
;FLAG:日常制御	調教終了時状況(1=通常/2=精力0/3=一回休み/4=失神した)
;-------------------------------------------------
@KOJO_EVENT_K31_13(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 普通に終了、1, 調教対象が疲れている、2, 調教対象が失神した)
;-------------------------------------------------

;────────────────────────────────────
;ARG:1, ARG:2, 疲労・失神による調教終了 FIRSTEVENT(0)～
;────────────────────────────────────
;・調教対象消耗度に応じて疲労/失神に分かれる
;・呼び出しタイミングは地の文の表示→PRINTL→口上の表示
;・EVENT201, 介抱(日常イベント)の箇所を書く場合は、上手く連携できるようにすると良いでしょう
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が疲れている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && ARG == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が失神した
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 2
	IF KFLAG:601 == 1
		IF KFLAG:610 == 0
			PRINTFORML 「目が覚めた？」
			PRINTFORMDL ここは・・・。そうか気を失っていたのか・・・。
			PRINTFORMDL 上から覗き込む顔と後頭部に感じる感触で膝枕してもらっていると分かり、起き上がろうとする。
			PRINTFORML 「ああ、いいわよ、もうちょっと寝てても」
			PRINTFORMDL そんな%CALLNAME:MASTER%を制し、妹紅は髪を愛しそうに撫でた。
			PRINTFORML 「ごめんね、ついつい調子に乗って%CNAME(MASTER, TARGET)%の事考えてなかった」
			PRINTFORMW 「次からはもっとうまくやるから・・・」
			KFLAG:610 = 1
		ELSE
			PRINTFORML 「目が覚めたみたいね」
			PRINTFORML 「遠慮しないでいいわよ、私が膝枕してあげたいだけなんだから」
			PRINTFORMW 「だからもう少しだけこのままで居させて・・・」
		ENDIF
	ELSE
		;アライメント正
		IF ALI(0)
			PRINTFORML 「あれ、少し激しくしすぎたかな」
			PRINTFORML 「・・・気持ちよさそうな顔しちゃって」
			PRINTFORMW 「・・・この後始末どうするのよ・・・」
		;アライメント負
		ELSE
			PRINTFORML 「あれ、もう限界？」
			PRINTFORML 「まあ、それならそれでいいけど」
			PRINTFORMW 「起きたら自分で片付けなさいよ」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ARG:0 普通に終了 FIRSTEVENT(10)～
;────────────────────────────────────
;・通常の調教終了時の口上
;・地の文なし。PRINTLによる空行の後に口上表示
;・枠毎にイベント番号を振ってありますが、任意に設定する事は可能です
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;大満足ボーナスを得ている
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:今回で大満足
	IF CFLAG:601 == 1
		IF CFLAG:611 == 0
			PRINTFORML 「ありがとうね」
			PRINTFORMDL 調教が終り、部屋を出ようとしたところで振り返ると、妹紅はそう言った。
			PRINTFORML 「私、今とっても充実してるの」
			PRINTFORML 「こんな生活%CNAME(MASTER, TARGET)%には迷惑なだけだろうけれど」
			PRINTFORML 「でもごめん。もう%CNAME(MASTER, TARGET)%無しでは考えられないから」
			PRINTFORML 「生活の心配はしなくてもいいの。だから」
			PRINTFORMW 「だから%CNAME(MASTER, TARGET)%は私を気持ちよくさせる事だけ考えて、ね？」
			CFLAG:611 = 1
		ELSE
			PRINTFORML 「今日も凄く良かったよ。・・・そうだ」
			PRINTFORML 「ご褒美に今日は%CNAME(MASTER, TARGET)%の好きな物作ってあげる。楽しみにしていてね」
			PRINTFORMDW 妹紅はそういうと満面の笑みで部屋を出て行った・・・。
		ENDIF
	ELSE
		;アライメント正
		IF ALI(0)
			PRINTFORML 「うん、凄く魅力的で良かった」
			PRINTFORMW 「また楽しませてね」
		;アライメント負
		ELSE
			PRINTFORML 「今回は此処までね」
			PRINTFORML 「なに、もっといじめて欲しいの？」
			PRINTFORML 「だめ。もう声枯れて来てるじゃない」
			PRINTFORMW 「それじゃ折角の良い声が聞けないわ」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;*************************************************
;以下、好感度による派生
;*************************************************

;━━━━━━━━━━━━━━━━━━━━━━━━
;（条件なし）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	IF KFLAG:601 ==1
		IF KFLAG:612 == 0
			PRINTFORML 「・・・なんで？なんで私を満足させてくれないの？」
			PRINTFORMW 「私が嫌い？この生活が不満？」
			PRINTFORML 「アンタ・・・何様のつもりよ」
			PRINTFORMW 「アンタは私の奴隷なの！私を気持ちよくさせることだけ考えてればいいの！！」
			PRINTW
			PRINTFORML 「私はいらないのよ、他に何も！私は何も要らないの！」
			PRINTFORML 「だから・・・だから・・・。」
			PRINTFORMW 「せめて%CNAME(MASTER, TARGET)%だけは、私だけのものでいてよ・・・」
			KFLAG:612 = 1
		ELSE
			PRINTFORML 「・・・。ごめん今日は此処まで」
			PRINTFORML 「ご飯は適当に出しておくから一人で食べて」
			PRINTFORMDW 妹紅はそういうと暗い顔で部屋を出て行った・・・。
		ENDIF
	ELSE
		PRINTFORML 「なんか冷めてきちゃったな・・・」
		PRINTFORMW 「今回は此処までね」
	ENDIF
	RETURN 1
ENDIF