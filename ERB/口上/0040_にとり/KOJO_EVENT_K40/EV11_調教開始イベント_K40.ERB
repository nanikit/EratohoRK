;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	에 있어 구상 조교 개시 이벤트 구상 파일
;
;	조교 개시 이벤트 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV11, 조교 개시 구상
;========================================================================
@KOJO_EVENT_K40_11(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 통상/1, 조교자의 피로에 의한 중지/2, 조교 대상의 피로에 의한 중지)
;-------------------------------------------------

;─
;조교 첫회·10회째
;─
;·조교 첫회, 및 10회째에 표시되는 구상
;·CFLAG:조교회수로 회수는 어떻게라도 참조할 수 있습니다만, 너무 하면 다른 조건을 탕진할 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 첫회·구상주가 주인님
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && CFLAG:조교회수 == 1 && TALENT:TARGET:주인님
	PRINTFORMDW 짤각, 이라고 문이 열려 새가%CALLNAME:MASTER%의 전에 모습을 보였다.
	PRINTFORMW 「그러면, 오늘부터%CNAME(MASTER, TARGET)%는 나의 실험대라고 하는 일로」
	;BGM 컷 인  아쿠타가와 류노스케의 갓빠  ～ Candid Friend
	CALL BGM_CUTIN(CSTR:곡1)
	PRINTFORMW 「협력적이라면 기쁘구나」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 첫회·구상주가 주인님이 아닌 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && CFLAG:조교회수 == 1
	;중복 부분이 많기 때문에 조금 만지작거립니다@/L
	PRINTFORMDW 짤각, 이라고 문이 열려 소녀가%CALLNAME:MASTER%의 전에 모습을 보였다.
	PRINTFORMW 「에…… 와 처음 뵙겠습니다」
	;BGM 컷 인  아쿠타가와 류노스케의 갓빠  ～ Candid Friend
	CALL BGM_CUTIN(CSTR:곡1)
	IF (MARK:반항각인 >= 5 || MARK:트라우마 >= 5 || MARK:쾌락각인 >= 5)
		IF 0
		;반항각인 Lv5 이상
		ELSEIF MARK:반항각인 >= 5
			PRINTFORMW 「이것은, 귀찮은 것 같다……」
		;트라우마 Lv5 이상
		ELSEIF MARK:트라우마 >= 5
			PRINTFORMW 「그런, 무서워하지 않아도……」
		;쾌락각인 Lv7 이상
		ELSEIF MARK:쾌락각인 >= 5
			PRINTFORMW 「좀, 시선이 무섭다……」
		ENDIF
		PRINTFORMW 「나는 하성에 있어. 잘 부탁드립니다―」
		RETURN 1
	ELSE
		;에 새가 주인님이 아닌 경우, 이치가 맞지 않기 때문에 comment out@/L
		;PRINTFORMW 「실험대가 되어 주다니」
		PRINTFORMW 「나는 하성에 있어. 잘 부탁드립니다―」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 10회째
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && CFLAG:조교회수 == 10 && !ARG
	PRINTFORMDW 짤각, 이라고 문이 열려 새가%CALLNAME:MASTER%의 전에 모습을 보였다.
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「슬슬, 익숙해져 주었는지」
		PRINTFORMW 「기분 좋아질 수 있도록(듯이), 노력하고 있지만」
	;아라이먼트부
	ELSE
		PRINTFORMW 「슬슬, 익숙해졌으면 좋구나」
		PRINTFORMW 「너무 아픈 것은, 하고 싶지 않지만」
	ENDIF
	PRINTFORMW 「그러면, 시작할까!」
	RETURN 1
ENDIF

;─
;조교 중지 ARG:1 ARG:2
;─
;CFLAG:소모 > 5혹은 CFLAG:MASTER:소모 > 5를 조건으로서 불려 가는 개소
;이 조건으로부터 빠졌을 경우는 통상의 조교 개시 구상이 불린다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;피로로 조교가 중지가 되는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG > 0
	PRINTFORMDW 짤각, 이라고 문이 열려 새가%CALLNAME:MASTER%의 전에 모습을 보였다.
	;=================================================
	;피곤한 것은 구상주(TARGET)
	;=================================================
	IF 구상기입체크(100) && ARG == 1
		PRINTFORMW 「좀, 수면 부족으로 말야……」
		PRINTFORMW 「나쁘지만, 휴일이라는 것으로」
		PRINTFORMDW 에 새는 휘청거리면서, 방에서 나갔다……
		RETURN 1
	ENDIF
	
	;=================================================
	;피곤한 것은 조교 대상(MASTER)
	;=================================================
	IF 구상기입체크(100) && ARG == 2
		PRINTFORMW 「저것, 지치고 있는 것 같다 누나」
		PRINTFORMW 「그러면, 휴일이라는 것으로」
		PRINTFORMDW 에 새는 조금 어깨를 떨어뜨려, 방에서 나갔다……
		RETURN 1
	ENDIF
ENDIF

;─
;그 외·호감도등에 의한 분기
;─
;·현재의 ACTABLE나 COMABLE의 사양적으로, 진전도를 측정하려면 (사실상) 호감도를 기준으로 할 수 밖에 없다고 생각된다
;·의존도는@COM로 대입되고 참조할 수 있을 뿐으로, 본체측의 거동에는 아무런 영향을 주지 않았다
;·향후의 손질 여하에 따라서는 용도도 있을지도? 그렇다고 하는 곳
;-------------------------------------------------

;-------------------------------------------------
;·서로 서로 좋아하고 있다
;-------------------------------------------------
;원래는 ED가 실장되어 있지 않은 시대에, 가짜의 ED 범위로서 사용되는 일이 많았던 개소
;현상에서도 이 단계에 이르려면, 그만한 일수와 진전이 필요하게 된다고 생각됩니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;상사상애(호의 3000이상·호감도 3000이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("상사상애")
	;=================================================
	;첫회·구상주가 주인님
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:주인님 && FIRSTEVENT(0)
		PRINTFORMDW , 충분하고…… 라고 하는 것에 새의 발소리를 들었다%CALLNAME:MASTER%는, 에 새를 맞이할 준비를 갖추었다.
		PRINTFORMDW 발소리가 문의 앞에서 멈추어, 당분간 지난 후, 파닥파닥 문에서 멀어지도록(듯이) 소리가 울린다.
		PRINTFORMDW 맥 빠짐 했다%CALLNAME:MASTER%는, 자신에게 뭔가 실수이기도 했을 것인가, 라고 괴로워한다.
		PRINTFORMDW 그 때, 문을 열려고 하는 것에 새는, 복도에서 머리를 움켜 쥐어 어떻게 잘라야할 것인가, 고민하고 있었다.
		PRINTL
		PRINTFORMDW 약 1시간의 뒤……
		PRINTFORMDW 철컥, 라고 문이 열려 흠칫흠칫에 새가%CALLNAME:MASTER%의 전에 모습을 보였다.
		PRINTL 
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	;IF 구상 기입 체크(0)
	;ENDIF
ENDIF

;-------------------------------------------------
;·조교 대상이 구상 주로 이제(벌써) 헤롱헤롱
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 완료(호의 2000이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("조교 완료")
	PRINTFORMDW , 충분하고…… 와에 새의 발소리를 들었다%CALLNAME:MASTER%는, 가슴을 크게 울리게 해 새를 마중했다.
	;=================================================
	;첫회·구상주가 주인님
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:주인님 && FIRSTEVENT(10)
		PRINTFORMW 「응?   무슨 일이야?」
		PRINTFORMW 「그렇게 응시할 수 있으면, 수줍어 버리지만 응」
		RETURN 1
	ENDIF
	
	;원의 것은 TALENT:주인님으로 나누어 있었습니다만, 내용이 완전히 같은 것으로 정리합니다
	;이하, 다른 개소에서도 같은 처치를 실시합니다. 헛됨은 극력 깎고 싶은 의향@/L
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(100)
		;어찌 되었든 K:1 = RAND:3같은 것을 죽이고 싶었다. 1문자 변수 살인대@/L
		SELECTCASE RANDEVENT(3, 10)
			CASE 0
				PRINTFORMW 「최근, 굉장히 솔직해졌네요」
			CASE 1
				PRINTFORMW 「무슨 일이야, 갑자기 히쭉거려 버려……」
			CASE 2
				PRINTFORMW 「좀, 너무 접근하는 것이 아닐까」
		ENDSELECT
		PRINTFORMW 「그러면, 시작할까?」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·조교자가 조교 대상에 빠져 버려, 깊기도 하고 도착 한 사랑을 따르고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;몹시 사랑함(호감도 2000이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("몹시 사랑함")
	PRINTFORMDW 파닥파닥, 라고 밖이 소란스러워졌는지라고 생각하면, 에 새가 기세 좋게 방에 뛰어들어 왔다.
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(20)
		PRINTFORMW 「갑자기, 만나고 싶어져……」
		PRINTFORMW 「폐라든지였거나, 하지 않을까……?」
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDEVENT(3, 20)
			CASE 0
				PRINTFORMW 「어제 자,%CNAME(MASTER, TARGET)%의 꿈, 봐 버려」
			CASE 1
				PRINTFORMW 「가득, 기분 좋게 해 주네요?」
			CASE 2
				PRINTFORMW 「%CNAME(MASTER, TARGET)%는, 어떤 것이 기호인가」
		ENDSELECT
		PRINTFORMW 「시간 아깝고, 시작할까」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·조교 대상이 구상 주로 조교되는 것을 행복하게 느끼고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교가 끝난 상태(호의 1000이상
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("조교가 끝난 상태")
	PRINTFORMDW 짤각, 이라고 문으로부터 소리가 나기 전에,%CALLNAME:MASTER%는 자세를 바로잡아 새를 기다리고 있었다.
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(30)
		PRINTFORMW 「저것……, 뭐 하고 있는 것,%CNAME(MASTER, TARGET)%?」
		PRINTFORMW 「이봐요 이봐요, 차가워져 버려?」
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDEVENT(3, 30)
			CASE 0
				PRINTFORMW 「좀, 딱딱하지 않아?」
			CASE 1
				PRINTFORMW 「그렇게, 손꼽아 기다리고 있던 것이다아」
			CASE 2
				PRINTFORMW 「이번은 어떤 것이 좋아?   가능한 한 희망은 (들)물어?」
		ENDSELECT
		PRINTFORMW 「그러면, 할까?」
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;-------------------------------------------------
;·조교자에 대해 혐오감을 안고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;혐오(호의-100 미만)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && AFFECTION("혐오")
ENDIF

;-------------------------------------------------
;·마음에 상처를 만들어져 주인님의 모습을 보는 것만으로 무서워해 버린다
;·트라우마의 종은 「%ACTSTR(CFLAG:MASTER:8)%」로 표시할 수 있다
;  예) PRINTFORMW 「이제(벌써)%ACTSTR(CFLAG:MASTER:트라우마)%는 싫습니다 라고?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;공포(호의 200 미만·트라우마 Lv3 이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && AFFECTION("공포")
ENDIF

;-------------------------------------------------
;·마음을 열지 않는 노예에게 초조해지는 주인님
;━━━━━━━━━━━━━━━━━━━━━━━━
;반항(호의 0 이하·반항각인 Lv3 이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && AFFECTION("반항")
ENDIF
[SKIPEND]

;-------------------------------------------------
;·조교되는 것도 오히려 좋아하게 되었는지도 모른다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;눈을 뜸(호의 500이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("눈을 뜸")
	PRINTFORMDW 짤각, 이라고 문이 열려 새가%CALLNAME:MASTER%의 전에 모습을 보였다.
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(70)
		PRINTFORMW 「아, 기다렸어?   라니」
		PRINTFORMW 「솔직하게 말해 어때?   여기의 생활?」
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDEVENT(3, 70)
			CASE 0
				PRINTFORMW 「어때?   외롭지 않았어?」
			CASE 1
				PRINTFORMW 「다음, 어떤 일 되고 싶어?」
			CASE 2
				PRINTFORMW 「%CNAME(MASTER, TARGET)%, 상당히 익숙해져 온 같지요」
		ENDSELECT
		PRINTFORMW 「그러면, 시작합니까」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·정중하게 조교를 진행되어, 생각했던 것보다 싫지 않을지도…
;━━━━━━━━━━━━━━━━━━━━━━━━
;순응(호의 100이상·호감도 100이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("순응")
	PRINTFORMDW 짤각, 이라고 문이 열려 새가%CALLNAME:MASTER%의 전에 모습을 보였다.
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(80)
		PRINTFORMW 「어때?   조금은 익숙해졌지 않아?」
		PRINTFORMW 「조금, 분위기가 바뀐 것 같아」
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDEVENT(3, 80)
			CASE 0
				PRINTFORMW 「이번은 조금, 굉장해?」
			CASE 1
				PRINTFORMW 「앞 한 녀석이지만 말야, 저것 어땠어?」
			CASE 2
				PRINTFORMW 「최근, 조금 반응이 재미있어졌군요」
		ENDSELECT
		PRINTFORMW 「그러면, 시작해?」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·요컨데, 다른 조건에 히트 하지 않았던 경우에 떨어지는 개소
;·만일 다른 분기를 모두 방치했을 경우, 이 개소만 써 두면 뭔가는 표시된다든가, 그런 장소입니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;초반(조건 없음)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTFORMDW 짤각, 이라고 문이 열려 새가%CALLNAME:MASTER%의 전에 모습을 보였다.
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(90)
		PRINTFORMW 「기분은 어때?   빠른 동안에 익숙했으면 좋다」
		PRINTFORMW 「그러면, 시작할까」
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDEVENT(3, 90)
			CASE 0
				PRINTFORMW 「컨디션은 어때? …… 피험체의 건강 관리는 큰 일이니까」
			CASE 1
				PRINTFORMW 「슬슬, 익숙해져 오지 않아? …… 하아, 아직인가」
			CASE 2
				PRINTFORMW 「이번은 어떻게 하지? …… 우웃, 무시하지 않고……」
		ENDSELECT
		PRINTFORMW 「그러면, 시작할까」
		RETURN 1
	ENDIF
ENDIF