;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	니토리 구상 회화계 액션 구상 파일
;
;	회화계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 ACT0～9
;========================================================================
;·TFLAG:ACT(0=지금의기분을얘기한다/1=자신에게로의기분을얘기한다/2=성에대한이야기를한다/3=상냥하게위로한다/4=엄격하게위협한다/5=의상의변경을요구한다/6=합의를요구한다/7=벌이라고선언한다/8=고함친다/9=기분나쁘게웃는다)
;·TFLAG:ACT분류 == 0에 해당
;·ACT9, 기분나쁘게웃는다는 이상계 ACT분류로부터 파생하는 경우가 있다
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------
;────────────────────────────────────
;ACT0, 지금의기분을얘기한다
;────────────────────────────────────
;·지금의 기분에 대해 들어 온다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K40_0
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 지금의기분을얘기한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「우선은%CNAME(MASTER, TARGET)%의 일, 모르면」
		PRINTFORMW 「이런 상황으로 지금, 어떤 기분일까?」
		RETURN 1
	ENDIF
	
	;=================================================
	;성교 봉사계로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
		;아라이먼트정
		IF ALI(0)
			SELECTCASE RANDACT(3, 0)
				CASE 0
					PRINTFORMW 「가득, 냈군요……」
					PRINTFORMW 「나의 몸, 어땠어?」
				CASE 1
					PRINTFORMW 「굉장히, 따뜻해……」
					PRINTFORMW 「%CNAME(MASTER, TARGET)%는, 어때?」
				CASE 2
					PRINTFORMW 「아…… 아직, 어딘지 부족한거야?」
					PRINTFORMW 「후후…… 정직하게, 말해?」
			ENDSELECT
		;아라이먼트부
		ELSE
			SELECTCASE RANDACT(3, 1)
				CASE 0
					PRINTFORMW 「아─……」
					PRINTFORMW 「낼 수 있으면, 뭐든지 좋은 걸까나」
				CASE 1
					PRINTFORMW 「어이쿠, 나와 버렸군요」
					PRINTFORMW 「한계는, 어디에 있을 것이다?」
				CASE 2
					PRINTFORMW 「이런 일 당해, 어때?」
					PRINTFORMW 「분해?   잔혹한?」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;성교계로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
		;아라이먼트정
		IF ALI(0)
			SELECTCASE RANDACT(3, 2)
				CASE 0
					PRINTFORMW 「아…… 기분, 좋았다……」
					PRINTFORMW 「%CNAME(MASTER, TARGET)%는, 어땠어……?」
				CASE 1
					PRINTFORMW 「안에, 내 버린……」
					PRINTFORMW 「미안해요, 나, 인내 할 수 없어서……」
				CASE 2
					PRINTFORMW 「굉장히, 따뜻해……」
					PRINTFORMW 「느껴 주면, 기쁘구나」
			ENDSELECT
		;아라이먼트부
		;응 어떤 기분? 질내사정되어 어떤 기분?
		ELSE
			SELECTCASE RANDACT(3, 3)
				CASE 0
					PRINTFORMW 「아, 나와 버렸다」
					PRINTFORMW 「뭐, 문제 없지요」
				CASE 1
					PRINTFORMW 「무엇?   좀 더, 나오고 싶은거야?」
					PRINTFORMW 「정직하게 대답했으면 좋구나」
				CASE 2
					PRINTFORMW 「아……, 기분, 좋았다……」
					PRINTFORMW 「좋았지요? 　%CNAME(MASTER, TARGET)%에도, 용도가 있어」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			SELECTCASE RANDACT(3, 4)
				CASE 0
					PRINTFORMW 「후우……, 조금 잠시 쉴까」
					PRINTFORMW 「그런데, 여기의 생활에는 벌써 익숙해졌어?」
				CASE 1
					PRINTFORMW 「일단, 휴식─」
					PRINTFORMW 「그래서, 조금 전의, 어땠어?」
				CASE 2
					PRINTFORMW 「싫은 일이라든지, 있으면 말해?」
					PRINTFORMW 「이런 일 하고 있는 내가 말하는 것도 뭐 하지만 말야」
			ENDSELECT
		;아라이먼트부
		ELSE
			SELECTCASE RANDACT(3, 5)
				CASE 0
					PRINTFORMW 「조금 전의, 어때?」
					PRINTFORMW 「아팠어?   그렇지 않으면 괴로왔어?」
				CASE 1
					PRINTFORMW 「지금은, 어떤 느낌?」
					PRINTFORMW 「망가지는 한계도, 알아 두지 않으면」
				CASE 2
					PRINTFORMW 「기분은 어때?」
					PRINTFORMW 「재미있는 반응이라고, 기쁘구나」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT1, 자신에게로의기분을얘기한다
;────────────────────────────────────
;·자신의 일을 어떻게 생각할까 얘기한다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일 조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K40_1
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 자신에게로의기분을얘기한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「대답은 예상되어지고 있지만, 일단이네」
		PRINTFORMW 「나의 일, 어떻게 생각해?」
		RETURN 1
	ENDIF
	
	;=================================================
	;성교 봉사계로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
		;아라이먼트정
		IF ALI(0)
			SELECTCASE RANDACT(3, 0)
				CASE 0
					PRINTFORMW 「그렇게, 나의 몸이 좋았던 것이다아……」
					PRINTFORMW 「말에서도, 가르쳐 주셨으면 하는데」
				CASE 1
					PRINTFORMW 「아프고, 없었어?」
					PRINTFORMW 「나, 기분 좋아서, 무심코……」
				CASE 2
					PRINTFORMW 「나의 일, 좋아?」
					PRINTFORMW 「하하, 농담이야……」
			ENDSELECT
		;아라이먼트부
		ELSE
			SELECTCASE RANDACT(3, 1)
				CASE 0
					PRINTFORMW 「내게 해 주었지만, 어때?」
					PRINTFORMW 「무엇인가, 말하는 일 있는 것이 아니야?」
				CASE 1
					PRINTFORMW 「나의 몸은 기분 좋았어?」
					PRINTFORMW 「굉장히, 보기 흉한 얼굴, 하고 있었어?」
				CASE 2
					PRINTFORMW 「그렇게 나를 임신시키고 싶은거야?」
					PRINTFORMW 「%CALLNAME:MASTER%에 그런 생활력 있다고는 생각되지 않지만, 저기」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;성교계로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
		;아라이먼트정
		IF ALI(0)
			SELECTCASE RANDACT(3, 2)
				CASE 0
					PRINTFORMW 「미안해요, 출 가신……!」
					PRINTFORMW 「나의 일, 좋아하고 있어 줄래……?」
				CASE 1
					PRINTFORMW 「아아…… 기분, 좋았어요……」
					PRINTFORMW 「%CALLNAME:MASTER%는, 어땠어?」
				CASE 2
					PRINTFORMW 「책임은, 취하기 때문에……」
					PRINTFORMW 「앞으로도,%CALLNAME:MASTER%와 함께 있어도 괜찮을까……」
			ENDSELECT
		;아라이먼트부
		ELSE
			SELECTCASE RANDACT(3, 3)
				CASE 0
					PRINTFORMW 「나와 버렸지만, 좋지요」
					PRINTFORMW 「%CALLNAME:MASTER%는 이것, 좋아하는 걸 응?」
				CASE 1
					PRINTFORMW 「어때?   이런 일 당해?」
					PRINTFORMW 「기분 좋았던 것이겠지? 　%CALLNAME:MASTER%, 변태니까」
				CASE 2
					PRINTFORMW 「기분 좋은 것 같은 얼굴, 해 버리고 있네요」
					PRINTFORMW 「어떻게 하시고 싶은거야?   나에게 좀 더 해 주었으면 해?」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 조교의 최초의 액션
	;=================================================
	IF 구상기입체크(100) && TFLAG:경과시간 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「슬슬, 이런 생활에도 익숙해져 왔는지?」
			PRINTFORMW 「어때?   나와 살아 봐」
		;아라이먼트부
		ELSE
			PRINTFORMW 「적당, 협력적이 되어도 괜찮은 것이 아니야?」
			PRINTFORMW 「이런 생활로, 만족?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;호감도 1000이상
	;=================================================
	IF 구상기입체크(100) && AFFECTION("권애")
		SELECTCASE RANDACT(3, 4)
			CASE 0
				PRINTFORMW 「엣또, 자……」
				PRINTFORMW 「%CALLNAME:MASTER%는, 나의 일, 어떻게 생각하고 있는 거야?」
			CASE 1
				PRINTFORMW 「최근, 조금 신경이 쓰이는 일이 있는거네요」
				PRINTFORMW 「나의 일, 어떻게 생각해……?」
			CASE 2
				PRINTFORMW 「여러가지, 해 버렸지만 말야,%CALLNAME:MASTER%의 일, 싫지 않아」
				PRINTFORMW 「%CALLNAME:MASTER%는, 어떤가……」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			SELECTCASE RANDACT(3, 5)
				CASE 0
					PRINTFORMW 「후우, 조금 쉴까」
					PRINTFORMW 「신경쓰고 있기 때문에, 감사해 주었으면 하구나」
				CASE 1
					PRINTFORMW 「상당히 격렬하게 해 버렸지만, 괜찮아?」
					PRINTFORMW 「좀 더, 느슨한 한편이 좋을까?」
				CASE 2
					PRINTFORMW 「나의 일, 어떻게 생각해?」
					PRINTFORMW 「아, 아니, 묘한 의미가 아니야」
			ENDSELECT
		;아라이먼트부
		ELSE
			SELECTCASE RANDACT(3, 6)
				CASE 0
					PRINTFORMW 「완고하구나……」
					PRINTFORMW 「좀 더 나에게 협력해 주어도, 좋은 것이 아닐까」
				CASE 1
					PRINTFORMW 「좀 더, 격렬하게 하려고 생각하지만」
					PRINTFORMW 「어때?   아직 견딜 수 있을 것 같은가」
				CASE 2
					PRINTFORMW 「나의 일, 어떻게 생각하고 있는 거야?」
					PRINTFORMW 「극심하게 다루어지면 어떻게 되는지, 흥미 있어」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT2, 성에대한이야기를한다
;────────────────────────────────────
;·조교자가 성에 대한 지식을 말하기 시작한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·조교자에게 성지식이 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K40_2
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT2, 성에대한이야기를한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT3, 상냥하게위로한다
;────────────────────────────────────
;·조교자가 상냥한 말로 위로해 온다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K40_3
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 상냥하게위로한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT4, 엄격하게위협한다
;────────────────────────────────────
;·어려운 말로 위협한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K40_4
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화할 가능성이 있다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 엄격하게위협한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT5, 의상의변경을요구한다
;────────────────────────────────────
;·조교자가 의상의 변경을 요구해 온다
;·조교 대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:쓰러트리기중에는 발생하지 않는다
;·TEQUIP:줄이 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K40_5
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%에 옷을 벗어라고 명령했다…"되고 있어 이것이 변화하는 일은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「우선, 옷 벗어 받을까……」
			PRINTFORMW 「거부하지 않아 받을 수 있으면, 기쁘구나」
		;아라이먼트부
		ELSE
			PRINTFORMW 「그러면, 옷 벗어 줄까나」
			PRINTFORMW 「지금부터 그런 일, 하는거야?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 조교의 최초의 액션
	;=================================================
	IF 구상기입체크(100) && TFLAG:경과시간 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「천천히, 쉬게 했어?」
			PRINTFORMW 「그러면 옷 벗어, 시작할까」
		;아라이먼트부
		ELSE
			PRINTFORMW 「자, 시작할까」
			PRINTFORMW 「우선은, 그 옷을 벗어 줄까나?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			SELECTCASE RANDACT(3, 0)
				CASE 0
					PRINTFORMW 「옷, 벗었으면 좋구나」
					PRINTFORMW 「갑자기로, 미안해요?」
				CASE 1
					PRINTFORMW 「네, 옷 벗어―」
					PRINTFORMW 「기분이 좋은 일, 해 줄테니까」
				CASE 2
					PRINTFORMW 「맨살을 쬐어도, 좋아?」
					PRINTFORMW 「괜찮다면, 나 돕지만……」
			ENDSELECT
		;아라이먼트부
		ELSE
			SELECTCASE RANDACT(3, 1)
				CASE 0
					PRINTFORMW 「이봐요, 빨리 옷 벗어?」
					PRINTFORMW 「별로 몸에 흥미가 있는 것이 아니지만 말야」
				CASE 1
					PRINTFORMW 「%CALLNAME:MASTER%의 알몸은, 보고 싶지도 않지만 말야」
					PRINTFORMW 「할 수 있을 것의 폭이 좁아져 버리기 때문에……」
				CASE 2
					PRINTFORMW 「무엇?   벗게 했으면 좋은거야?」
					PRINTFORMW 「응석부릴 수 있어도 곤란하기 때문에, 빨리 벗어?」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;아라이먼트정
	IF ALI(0)
		SELECTCASE RANDACT(3, 10)
			CASE 0
				PRINTFORMW 「슬슬, 차가워져 버릴까나」
				PRINTFORMW 「이제(벌써) 옷, 입어도 괜찮아」
			CASE 1
				PRINTFORMW 「옷 입게 해 줄까?」
				PRINTFORMW 「농담이야……」
			CASE 2
				PRINTFORMW 「어떻게 할까나」
				PRINTFORMW 「우선, 옷 입어 받을까」
		ENDSELECT
	;아라이먼트부
	ELSE
		SELECTCASE RANDACT(3, 11)
			CASE 0
				PRINTFORMW 「빨리, 옷 입어요」
				PRINTFORMW 「행동이 늦구나」
			CASE 1
				PRINTFORMW 「조금 시큰둥한가」
				PRINTFORMW 「옷, 입었으면 좋겠다 응」
			CASE 2
				PRINTFORMW 「지금부터, 어떻게 하지?」
				PRINTFORMW 「우선, 옷은 입어 둬」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT6, 합의를요구한다
;────────────────────────────────────
;·처음을 받아도 괜찮을까 얘기한다
;·처음=처녀 or동정(양쪽 모두의 경우도 있다)
;·이미 합의를 얻고 있는 경우는 발생하지 않는다
;·조교 대상이 처녀도 동정도 아닌 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K40_6
;-------------------------------------------------
;함수@TARGET_DESCRIPTION로 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;후는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 합의를요구한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT7, 벌이라고선언한다
;────────────────────────────────────
;·지금부터 벌 한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:벌플래그는 서 있다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP:쓰러트리기중시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K40_7
;-------------------------------------------------
;여기에서는 지문의 변화는 없다. 분기의 증감은 기호로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, 벌이라고선언한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT8, 고함친다
;────────────────────────────────────
;·큰 소리로고함친다
;·턴방침이 "소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:벌플래그에 관련되지 않고, 발생할 가능성이 있다(벌중에 한층 더고함친다가능성등)
;-------------------------------------------------
@KOJO_ACT_K40_8
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 큰 소리로 고함쳤다…"만으로 파생은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 고함친다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT9, 기분나쁘게웃는다
;────────────────────────────────────
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·화제 『비웃는다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K40_9
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 기분나쁘게웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF