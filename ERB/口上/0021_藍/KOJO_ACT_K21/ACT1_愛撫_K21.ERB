;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	야쿠모쪽구상 애무계 액션 구상 파일
;
;	애무계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;애무계 ACT10～19 & 27
;========================================================================
;·TFLAG:ACT(10=손으로애무/11=가슴애무/12=입으로애무/13=애널애무/14=애널빨고/15=키스/16=성기를서로켠다/17=파이즈리한다/18=풋잡한다/19=손가락삽입해라/27=밀어넘어뜨린다)
;·TFLAG:ACT분류 == 1에 해당
;·ACT18, 풋잡은 이상계로부터 파생하는 경우 있어
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "애무")로 줍는 것이 가능
;·조교자가 조교 대상을 애무한다. 이 구상의 시점에서 조교자는 벌써 손을 대고 있다
;·미약을 사용하는 케이스가 있다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;─
;ACT10, 손으로애무(ACT10_0, 애무하는/ACT10_1, 자위 하는/ACT10_2, 페니스밴드 자위)
;─
;·C 중심으로 애무한다
;·조교 대상으로 페니스가 있는 경우, 및 페니스밴드장착 때에는 파생 커맨드가 된다
;·조교 대상이 자위중의 경우는 해제된다
;·조교자의 손에 더러움이 있는 경우 불쾌 SOURCE가 변동한다
;·조교자 기교, 애무, 기묘한손가락소질에 응해 발기도 상승
;-------------------------------------------------
@KOJO_ACT_K21_10
;-------------------------------------------------
;·TFLAG:ACT파생(0=애무/1=자위/2=페니스밴드 자위)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_1, 자위 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·남성기에의 애무를 실시한다, 즉 손 애무
;·다른 파생과 비교해 SOURCE:MASTER:쾌C는+200%
;·조교자 기교, 애무, 기묘한손가락소질에 응해 발기도 상승
;-------------------------------------------------
;필요에 따라서, TEQUIP:오나홀이나 TCVAR:MASTER:발기도
;PANTSCHECK(1) 등으로 분기를 마련하는 일도 할 수 있습니다.
;온전히 하면 수고가 심상치 않아서, 효율 좋게 기술하는 일을 추천합니다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「역시 여기가 기분이 좋을 것이다?」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(30) && ONCEACT(0)
		;하반신 복장이 있는 경우
		IF PANTSCHECK(1)
			PRINTFORMW 「고구마벌레같이 꼼질꼼질 하고 있구나」
		;그 외
		ELSE
			SELECTCASE RANDACT(2, 0)
				CASE 0
					PRINTFORMW 「후흥…… 좆밥 투성이의 더러운 자지째」
				CASE 1
					PRINTFORMW 「여기에 오고 나서 혼자서는 하고 있지 않는 것인지?」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(50) && TCVAR:MASTER:갈것같다
		;-------------------------------------------------
		;전부터 애무
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:MASTER:위치전후 == 1
			;초조하게해번에 의해 분기
			SELECTCASE TCVAR:MASTER:초조하게해번
				;초조하게함 안
				CASE IS > 1
					PRINTFORMW 「이제 내고 싶다는? 어떻게 할까나」
				;초조하게함 종료
				CASE 1
					PRINTFORMW 「어쩔 수 없는 녀석이다. 1회 내 버려라」
				;초조하게함 없음
				CASEELSE
					PRINTFORMW 「보기 흉한 얼굴 해 버려. 빨리 사정해 편해져」
			ENDSELECT
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;뒤로부터 애무
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:MASTER:위치전후 == 2
			;초조하게해번에 의해 분기
			SELECTCASE TCVAR:MASTER:초조하게해번
				;초조하게함 안
				CASE IS > 1
					PRINTFORMDW 쪽은 당신의 목덜미나 귓전에 자극을 주면서 초조하게함이라고 있다……
				;초조하게함 종료
				CASE 1
					PRINTFORMW 「슬슬 내게 해 주자. 얼마나 멀리 날까 볼만하다」
				;초조하게함 없음
				CASEELSE
					PRINTFORMW 「너무 허리 굽히고 있으면 배가 더러워져 버리겠어?」
			ENDSELECT
			RETURN 1
		ENDIF
		
		;위치전후 분기를 묻는다면 필요없음. 묻지 않으면 여기서 보완시킵시다
		;불필요한 분기는 삭제, 혹은 comment out 하는 것으로 부하는 가벼워집니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		;IF 구상 기입 체크(0)
		;	;초조하게해번에 의해 분기
		;	SELECTCASE TCVAR:MASTER:초조하게해번
		;		;초조하게함 안
		;		CASE IS > 1
		;			PRINTFORMW 
		;		;초조하게함 종료
		;		CASE 1
		;			PRINTFORMW 
		;		;초조하게함 없음
		;		CASEELSE
		;			PRINTFORMW 
		;	ENDSELECT
		;	RETURN 1
		;ENDIF
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마 안
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
			;전부터 애무
			IF TCVAR:MASTER:위치전후 == 1
				PRINTFORMW 
			;뒤로부터 애무
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;전부터 애무
			IF TCVAR:MASTER:위치전후 == 1
				PRINTFORMW 
			;뒤로부터 애무
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE TCVAR:MASTER:발기도
			CASE IS > 1490
				SELECTCASE TCVAR:MASTER:초조하게해번
					CASE IS > 2
						PRINTFORMW 「후후후, 『기다려』다.」
						PRINTFORMDW 쪽은 보들보들 손바닥으로 페니스를 자극하면서도, 손가락으로 요도를 잡아 사정을 두고 있다……
					CASE 1
						PRINTFORMW 「나의 손가락에, 나머지 어느 정도 견딜 수 있을까나」
					CASE 0
						PRINTFORMW 「쿠쿳, 에 발기하고 있지 않은가」
				ENDSELECT
			CASE IS > 1300
				PRINTFORMW 「쿠퍼가 질질 흘러넘치고 있겠어, 이제 한계인가……?」
			CASE IS > 1000
				PRINTFORMW 「이렇게 크게 해…… 이것을 어디에, 어떻게 하고 싶다?」
			CASE IS > 300
				PRINTFORMW 「너의, 페니스…」
				PRINTFORMW 「맥박칠 때마다 커져 와 있겠어… 기분이 좋은 것인지?」
			CASEELSE
				PRINTFORMW 「후훗, 아직 부드럽고 슥슥의 페니스도 좋아하는 것이다」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;ACT10_1, 자위 한다

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_2, 페니스밴드 자위
;━━━━━━━━━━━━━━━━━━━━━━━━
;·조교 대상이 페니스밴드장착 때에 발생
;·쾌CSOURCE적으로는 TFLAG:ACT파생 == 1인가 아닌가로 밖에 보지 않은 같기 때문에, 사실상 자위는 아니고 애무와 동등?
;·쾌V도 보지 않는 것 같은 느낌. 그리고 부위의 더러움도 참조 할 수 없는 것이 아닐까와
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(1)
		;하반신 복장이 있는 경우
		IF PANTSCHECK(1)
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마 안
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
			;전부터 애무
			IF TCVAR:MASTER:위치전후 == 1
				PRINTFORMW 
			;뒤로부터 애무
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;전부터 애무
			IF TCVAR:MASTER:위치전후 == 1
				PRINTFORMW 
			;뒤로부터 애무
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;전부터 애무
		IF TCVAR:MASTER:위치전후 == 1
			PRINTFORMW 
		;뒤로부터 애무
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;ACT10_2, 페니스밴드 자위

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_0, 애무한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·여성기에의 애무를 실시한다
;·SOURCE적으로는 쾌B와 쾌C는 동등
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(2)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(2)
		;하반신 복장이 있는 경우
		IF PANTSCHECK(1)
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:갈것같다
		;-------------------------------------------------
		;전부터 애무
		;-------------------------------------------------
		IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 1
			;초조하게해번에 의해 분기
			SELECTCASE TCVAR:MASTER:초조하게해번
				;초조하게함 안
				CASE IS > 1
					PRINTFORMW 
				;초조하게함 종료
				CASE 1
					PRINTFORMW 
				;초조하게함 없음
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;뒤로부터 애무
		;-------------------------------------------------
		IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 2
			;초조하게해번에 의해 분기
			SELECTCASE TCVAR:MASTER:초조하게해번
				;초조하게함 안
				CASE IS > 1
					PRINTFORMW 
				;초조하게함 종료
				CASE 1
					PRINTFORMW 
				;초조하게함 없음
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
		
		;위치전후 분기를 묻는다면 필요없음. 묻지 않으면 여기서 보완시킵시다
		;불필요한 분기는 삭제, 혹은 comment out 하는 것으로 부하는 가벼워집니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;초조하게해번에 의해 분기
			SELECTCASE TCVAR:MASTER:초조하게해번
				;초조하게함 안
				CASE IS > 1
					PRINTFORMW 
				;초조하게함 종료
				CASE 1
					PRINTFORMW 
				;초조하게함 없음
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마 안
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
			;전부터 애무
			IF TCVAR:MASTER:위치전후 == 1
				PRINTFORMW 
			;뒤로부터 애무
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;전부터 애무
			IF TCVAR:MASTER:위치전후 == 1
				PRINTFORMW 
			;뒤로부터 애무
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;전부터 애무
		IF TCVAR:MASTER:위치전후 == 1
			PRINTFORMW 
		;뒤로부터 애무
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;ACT10_0, 애무한다

;─
;ACT11, 가슴애무
;─
;·가슴애무/유두 들이마셔/젖의 몸싸움/유두 고문
;·특정의 조건으로 「유두 들이마셔」 「젖의 몸싸움」 「유두 고문」에 파생한다
;·조교 대상이 남자의 경우는 반드시 「유두 고문」(이)가 되어, 그 밖에 파생하는 일은 없다
;-------------------------------------------------
@KOJO_ACT_K21_11
;-------------------------------------------------
;·TFLAG:ACT파생(0=가슴애무/1=유두 들이마셔/2=몸싸움/3=유두 고문)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_0, 가슴애무
;━━━━━━━━━━━━━━━━━━━━━━━━
;·모든 파생 조건으로부터 빠지면 통상의 가슴애무가 된다
;·조교자의 욕망+B감각이 3이상의 경우 1/2의 확률로 몸싸움에 파생
;·그리고 새었을 경우에게만, 통상의 가슴애무된다
;-------------------------------------------------
IF 구상기입체크(50) && !TFLAG:ACT파생
	;전부터 애무
	IF TCVAR:MASTER:위치전후 == 1
		PRINTFORMW 「부드러운 가슴이다……」
	;뒤로부터 애무
	ELSE
		PRINTFORMW 「이봐요 이봐요, 가슴이 무방비야?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_1, 유두 들이마셔
;━━━━━━━━━━━━━━━━━━━━━━━━
;·전회 ACT가 가슴애무의 경우 유두계가 된다
;·그 때, 조교자에게 유치한가 혀사용이 있으면 3/4, 없으면 1/4으로 유두흡실 된다
;-------------------------------------------------
IF 구상기입체크(50) && TFLAG:ACT파생 == 1
	;상반신 복장이 요점 밀치고			옷을
	IF BRACHECK(1, 1)
		PRINTFORMW 「이봐요, 분명하게 옷을 잡아 두어라」
		PRINTFORMW 「응─, 츄…… 하아, 유두 대굴대굴」
	;그 외
	ELSE
		PRINTFORMW 「날름, 츄, 응츄…」
		PRINTFORMW 「이봐요, 유두도 서 왔어」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_2, 젖의 몸싸움
;━━━━━━━━━━━━━━━━━━━━━━━━
;·가슴의 몸싸움을 요구한다. 이 시점에서의 조교 대상의 반응은 미정
;·유두계의 조건으로부터 빠졌을 경우, 조교자의 욕망+B감각 3이상이라면,1/2그리고 가슴의 몸싸움이 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;상반신 복장이 방해			옷 위로부터
	;=================================================
	IF 구상기입체크(0) && BRACHECK(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;상반신윗도리가 요점 밀치고		윗도리를 걸어 인상
	;=================================================
	IF 구상기입체크(0) && UPPERJACKET(1) == 1
		;속옷도 걸었다
		IF UPPERUNDER(1) == 1
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;상반신윗도리가 요점 밀치고		속옷의 옷자락으로부터 손을 잠입시킴
	;=================================================
	IF 구상기입체크(0) && UPPERUNDER(1) == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_3, 유두 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
;·전회 ACT가 가슴애무의 경우 유두계가 된다
;·그 때, 조교자에게 유치한가 혀사용이 있으면 1/4, 없으면 3/4으로 유두 고문이 된다
;·조교 대상이 남자의 경우는 유두 꾸짖어 밖에 되지 않는다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;=================================================
	;조교 대상이 남자
	;=================================================
	IF 구상기입체크(80) && TALENT:MASTER:남자
		;전부터 애무
		IF TCVAR:MASTER:위치전후 == 1
			PRINTFORMW 「가슴이 좋은, 이라고 하는 남자도 있는 것 같으니까」
		;뒤로부터 애무
		ELSE
			PRINTFORMDW 쪽은 나중에%CALLNAME:MASTER%의 유두를 엄지로 비비고 있다……
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;전부터 애무
		IF TCVAR:MASTER:위치전후 == 1
			PRINTFORMW 
		;뒤로부터 애무
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT12, 입으로애무
;─
;·쿤닐링구스한다/펠라치오 한다
;·하반신의 의상이 남아 있어도 발생하는 일이 있다. 조교자에게 TALENT:벽통과가 없는 경우는 비켜 놓아 실행한다
;·턴방침이 "이상"이라면 발생하지 않는다
;·TEQUIP(바이브/오나홀/페니스밴드/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교 대상으로 페니스가 있으면, 반드시 페라가 된다
;-------------------------------------------------
@KOJO_ACT_K21_12
;-------------------------------------------------
;·TFLAG:ACT파생(0=쿤닐링구스/1=페라)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT12_0, 쿤닐링구스한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;안면승마, 아라이먼트가 부의 경우, TALENT:새디즘등으로 지문이 분기 하는 경우 있어
	;필요에 따라서 확장하면 좋을 것입니다
	;=================================================
	;조교 대상이갈것같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:갈것같다
		;초조하게해번 파생 처리
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게함 안
			CASE IS > 1
				PRINTFORMW 
			;초조하게함 종료
			CASE 1
				PRINTFORMW 
			;초조하게함 없음
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마 안
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;연속 실행의 네스트를 개방한다면 여기는 반드시 써 둡시다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;LOWERJACKET(1) == 1 「」LOWERUNDER(1) == 1 「비켜 놓아」등으로 확장하는 것도 좋을 것입니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT12_1, 펠라치오 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;안면승마, 아라이먼트가 부의 경우, TALENT:새디즘등으로 지문이 분기 하는 경우 있어
	;필요에 따라서 확장하면 좋을 것입니다
	;=================================================
	;조교 대상이갈것같다
	;=================================================
	IF 구상기입체크(80) && TCVAR:MASTER:갈것같다
		;초조하게해번 파생 처리
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게함 안
			CASE IS > 1
				PRINTFORMDW 당신이 사정에 이를 것 같은 한심한 소리를 내면, 쪽은 당신의 눈을 응시하고 목을 작게 옆에 흔들었다
			;초조하게함 종료
			CASE 1
				PRINTFORMDW 드디어 인내도 한계에 이르러 몸을 비틀게 하면, 그것을 헤아렸는지 그녀는 깊게까지 음경을 물었다……
			;초조하게함 없음
			CASEELSE
				PRINTFORMDW 그녀가 입에 익숙한 구음에, 당신은 그 안쪽에 자식을 흘릴 것 같게 되었다
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(80) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마 안
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;연속 실행의 네스트를 개방한다면 여기는 반드시 써 둡시다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(100)
			PRINTFORMDW 쪽은 먹이를 획득한 짐승과 같이 당신의 에모노를 물어 떼어 놓지 않는다……
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;LOWERJACKET(1) == 1 「」LOWERUNDER(1) == 1 「비켜 놓아」등으로 확장하는 것도 좋을 것입니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMDW 쪽은 당신의 페니스를 입에 넣어 핥아대고 있다
		RETURN 1
	ENDIF
ENDIF

;─
;ACT13, 애널애무
;─
;·애널애무/전립선자극/애널바이브 출납
;·애널바이브 장비중은, 애널바이브 출납에 파생
;·연속 실행의 경우, 전립선자극에 파생
;·갈것같다or쾌C가 5000이상의 경우, 조교자의 애무 스킬에 응해 전립선자극에 파생
;·발기도가 낮은 경우, 조교자의 애무 스킬에 응해 전립선자극에 파생
;·요점강부디기의 경우, 전립선자극에 파생
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드/애널비즈) 장착중에는 발생하지 않는다
;·성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K21_13
;-------------------------------------------------
;·TFLAG:ACT파생(0=애널애무/1=전립선자극/2=애널바이브 출납)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_0, 애널애무
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;안면승마 안
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 「그러면 나는 여기에 방해키라고 받을까」
		RETURN 1
	ENDIF
	
	;=================================================
	;하반신 복장이 요점 밀치고
	;=================================================
	IF 구상기입체크(0) && PANTSCHECK(1, 1)
		;하반신윗도리를 걷고
		IF LOWERJACKET(1) == 1
			PRINTFORMW 
		;속옷 너머로
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		IF ABL:MASTER:A감각 > 3
			PRINTFORMW 「후후, 손대는 전부터 갖고 싶어하는 듯이 빠끔빠끔 시키고 있지 않은가」
			IF TALENT:MASTER:남자
				PRINTFORMW 「자지에서도 돌진했으면 좋은 것인지?」
			ELSE
				PRINTFORMW 「이것은 벌이 필요한가」
			ENDIF
		ELSE
			PRINTFORMW 「너의 항문, 힘든데」
			PRINTFORMW 「내가 느슨느슨해 줄거니까…」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_1, 전립선자극(애널애무)
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 남자의 경우에만 파생
;후타나리의 전립선은 요도, 즉 질 위에 있다고 해, G스팟 자극에
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	IF TEQUIP:성교봉사중
		;연속 실행
		IF TFLAG:ACT == TFLAG:전ACT
			SELECTCASE RANDACT(2, 0)
				CASE 0
					PRINTFORMW 「うりうりうり 팔아, 아직 나는 만족하지 않아」
				CASE 1
					PRINTFORMW 「시들 때까지 내 받지 않으면. 모인 정액 전부 나의 것」
			ENDSELECT
		;통상
		ELSE
			SELECTCASE RANDACT(4 ,1)
				CASE 0
					PRINTFORMW 「제일 기분이 좋은 곳, 많이 찔러 줄까……」
					;PRINTFORMW 「이봐요, 의지 스윗치를 눌러 주자」
				CASE 1
					PRINTFORMW 「너의 민감한 곳, 그리고 나의 민감한 곳」
					PRINTFORMW 「이렇게 해 맞대고 비비면…… 굉장히 기분이 좋다고 생각하지 않는가?」
				CASE 2
					PRINTFORMW 「엉덩이안까지 전부 성감대가 되어 받자」
				CASE 3
					PRINTFORMW 「뿌리로부터 끝 쪽까지, 전부 나의 전용 자지」
					PRINTFORMW 「나의 몸으로 한 방울 남김없이 내 받지 않으면」
			ENDSELECT
		ENDIF
	ELSE
		IF TFLAG:ACT == TFLAG:전ACT
			IF TALENT:MASTER:남자
				SELECTCASE RANDACT(3, 2)
					CASE 0
						PRINTFORMW 「너의 추잡한 분혈이…」
						PRINTFORMW 「구츄 , 구츄는… 군침을 흘려, 기뻐하고 있는 것이 알겠어……」
					CASE 1
						PRINTFORMW 「오─들 이봐요, 좀 더 좋아해 미쳐 보여 줘」
						PRINTFORMW 「몸중에서 나에게 손대어 받을 수 있다니 행운아째」
					CASE 2
						PRINTFORMW 「브레이크는 효과가 없을 정도, 전부가 흘러나와 버리도록(듯이) 망가져 버려라」
				ENDSELECT
			ENDIF
		;통상
		ELSE
			IF PALAM:MASTER:쾌A > 8000
				PRINTFORMW 「어이쿠, 자지의 앞으로부터 맛있을 것 같은 이슬이 늘어져 왔어……」
			ELSEIF ABL:MASTER:A감각 > 2
				PRINTFORMW 「좋아 좋아, 여기를 나에게 만져 받고 싶었던 것이구나」
			ELSE
				PRINTFORMW 「어때?   여기가 전립선이다」
				PRINTFORMW 「후훗, 느끼고 있는 것 같구나. 페니스도 펄떡펄떡이 되어 있겠어」
			ENDIF
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_2, 애널바이브 출납
;━━━━━━━━━━━━━━━━━━━━━━━━
;·애널바이브 장비중은, 애널바이브 출납이 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;조교 대상이 남자
	IF TALENT:MASTER:남자
		PRINTFORMW 「우와아, 땅땅 찌를 때마다 자지 뛰어 버려…… 전문가가 들어오면 어떻게 되어 버릴 것이다?」
	;그 외
	ELSE
		PRINTFORMW 「전보다도 뒤 쪽이 기호로?」
	ENDIF
	RETURN 1
ENDIF

;─
;ACT14, 애널빨고
;─
;·애널빨고
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP(애널바이브/애널비즈/관장기＋플러그/페니스밴드/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·TCVAR:MASTER:초조하게해번이 실로 되어 있는 경우는 발생하지 않는다
;·혀에 의한 전립선자극에의 파생은 「그런 곳까지 혀가 닿을 이유 없지 않은가www」라고 하는 일로 숙청되고 있습니다
;·현시점에서 ACT가 파생하는 일은 없지만, ACT 개변의 가능성에 대비해, TFLAG:ACT파생 == 0으로 지정
;-------------------------------------------------
@KOJO_ACT_K21_14
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT14_0, 애널빨고
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「더럽다는? 너로부터 하면 그럴지도 모르는구나」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(100) && ONCEACT(0)
		SELECTCASE RANDACT(2, 0)
			CASE 0
				PRINTFORMW 「더러운 것을 빨려지고 싶지 않으면, 분명하게 스스로 씻는구나」
			CASE 1
				PRINTFORMW 「킁킁…… 이번은 씻어 온 것 같다. 정중한 일로 안까지」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(50) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		;IF TEQUIP:조교자페니스밴드
		;	PRINTFORMW 
		;그 외
		;ELSE
			PRINTFORMW (이 냄새나…… 견딜 수 없어요)
		;ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마 안
	;=================================================
	IF 구상기입체크(50) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 「그런데, 나도 빨아 줄까…… 여기를 말야」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;=================================================
	IF 구상기입체크(50) && GETBIT(TEQUIP:자위중, 0)
		IF PENIS(MASTER)
			PRINTFORMW 「여기로부터도 밀어 내 주자」
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;하반신 복장이 요점 밀치고
	;=================================================
	IF 구상기입체크(0) && PANTSCHECK(1, 1)
		;하반신윗도리를 걷고
		IF LOWERJACKET(1) == 1
			PRINTFORMW 
		;속옷 너머로
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(2, 1)
			CASE 0
				PRINTFORMW 「낯간지러운가? 혹시 기분이 좋은 것이 아닌가?」
			CASE 1
				PRINTFORMW 「언제 봐도 예쁜 색…… 여기는 언제까지나 아이같다」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;─
;ACT15, 키스
;─
;·키스/미약키스
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·조교자에게 TALENT:벽통과가 없는 경우, TEQUIP:볼개그 장착중은 튕겨진다
;-------------------------------------------------
@KOJO_ACT_K21_15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15, 키스 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && FIRSTACT(0)
	;=================================================
	;서로 처음
	;=================================================
	IF 구상기입체크(0) && EXP:MASTER:키스경험 == 0 && EXP:TARGET:키스경험 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 처음
	;=================================================
	IF 구상기입체크(0) && EXP:MASTER:키스경험 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 처음
	;=================================================
	IF 구상기입체크(0) && EXP:TARGET:키스경험 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;"첫회"의 네스트를 개방해, 각 조건의 분기를 방치한다면 여기서 보완합시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMDW 갑자기 겹친 그녀의 입은, 어딘지 모르게 피와 같은 냄새가 났다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15_0, 키스
;━━━━━━━━━━━━━━━━━━━━━━━━
;! GETBIT(TFLAG:미약등사용, 1) 등의 조건을 삭제해! TFLAG:ACT파생로 정리해 버려도 좋다고 생각합니다
;무리하게 모든 개소를 묻는 일은 없기 때문에, 필요에 따라서 구분하여 사용해 주세요
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생 && !GETBIT(TFLAG:미약등사용, 1)
	;=================================================
	;성교중(조교자 삽입)
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위를 파생 처리
		SELECTCASE TEQUIP:성교중
			;정상위					두를 잡아 정열적인 키스를 했다…
			CASE 1
				PRINTFORMW 
			;후배위					뒤돌아 보게 한 입술을 빼앗았다…
			CASE 2
				PRINTFORMW 
			;승마위					하로부터 밀어올리면서 정열적으로 입맞춤했다…
			CASE 3
				PRINTFORMW 
			;대면좌위				꽉 껴안으면 그대로 입술을 빼앗았다…
			CASE 4
				PRINTFORMW 
			;배면좌위				뒤돌아 보게 한 그대로 입술을 빼앗았다…
			CASE 5
				PRINTFORMW 
			;애널섹스			아누스를 범하면서 키스 해 왔다…
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중(조교 대상 삽입)
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;초조하게해번에 의한 파생			TCVAR:MASTER:갈것같다로 둘러싸지는 않기 때문에, CASEELSE는 통상 상태를 의미한다
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게함 안
			CASE IS > 1
				PRINTFORMW 
			;초조하게함 종료
			CASE 1
				PRINTFORMW 
			;초조하게함 없음
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMDW 두 명의 타액이 서로 섞여, 입 끝으로부터 더럽게 늘어지고 나와 있다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15_1, 미약키스
;━━━━━━━━━━━━━━━━━━━━━━━━
;·조교 대상으로 입으로 옮김으로 미약을 먹인다
;-------------------------------------------------
;RESULT치를 2로서@KOJO_USE_KX의 미약등사용구상을 호출하지 않는 설정을 디폴트로 해둡니다
;필요에 따라서 반환값을 설정해 주세요
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생 && GETBIT(TFLAG:미약등사용, 1)
	;미약사용 구상 처리필 체크(미약사용의 지문 컷 할 때는 TFLAG:미약등사용지문스킵의 코멘트를 해제)
	CLEARBIT TFLAG:미약등사용구상, 1
	;CLEARBIT TFLAG:미약등사용지문스킵, 1
	;=================================================
	;성교중(조교자 삽입)
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위를 파생 처리
		SELECTCASE TEQUIP:성교중
			;정상위					두를 잡아 정열적인 키스를 했다…
			CASE 1
				PRINTFORMW 
			;후배위					뒤돌아 보게 한 입술을 빼앗았다…
			CASE 2
				PRINTFORMW 
			;승마위					하로부터 밀어올리면서 정열적으로 입맞춤했다…
			CASE 3
				PRINTFORMW 
			;대면좌위				꽉 껴안으면 그대로 입술을 빼앗았다…
			CASE 4
				PRINTFORMW 
			;배면좌위				뒤돌아 보게 한 그대로 입술을 빼앗았다…
			CASE 5
				PRINTFORMW 
			;애널섹스			아누스를 범하면서 키스 해 왔다…
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 2
	ENDIF
	
	;=================================================
	;성교봉사중(조교 대상 삽입)
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;초조하게해번에 의한 파생			TCVAR:MASTER:갈것같다로 둘러싸지는 않기 때문에, CASEELSE는 통상 상태를 의미한다
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게함 안
			CASE IS > 1
				PRINTFORMW 
			;초조하게함 종료
			CASE 1
				PRINTFORMW 
			;초조하게함 없음
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 2
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 2
	ENDIF
ENDIF

;─
;ACT16, 성기를서로켠다
;─
;·짝 맞추기 놀이/가랑이 한다
;·조교 대상으로 페니스가 있는 경우는 ACT파생 == 1 「가랑이 한다」에 파생한다
;·조교 대상이 페니스밴드장착 안은, ACT파생 == 2 「페니스밴드 가랑이」가 된다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·조교 대상이 여자 아이로 바이브 장착중이라면 발생하지 않는다
;·TEQUIP(오나홀/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교자에게 페니스가 있으면 발생하지 않는다(봉사계의 가랑이 강제가 된다)
;-------------------------------------------------
@KOJO_ACT_K21_16
;-------------------------------------------------
;·TFLAG:ACT파생(=0, 조개 우려라/=1, 가랑이/=2, 페니스밴드 가랑이)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_0, 짝 맞추기 놀이
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;조교 대상이갈것같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:갈것같다
		;초조하게해번 파생
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게함 안
			CASE IS > 1
				PRINTFORMW 
			;초조하게함 종료
			CASE 1
				PRINTFORMW 
			;초조하게함 없음
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;위로돌리고
		IF TFLAG:위로돌리고
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;지문은 조교 대상의 하반신 복장 마다의 분기 있어. 필요에 따라서 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;위로돌리고
		IF TFLAG:위로돌리고
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_1, 가랑이 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·여성기와 넓적다리로 남성기를 사이에 두어 애무한다
;·조교 대상으로 페니스가 있으면, 반드시 가랑이 하는게 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;조교 대상이갈것같다
	;=================================================
	IF 구상기입체크(50) && TCVAR:MASTER:갈것같다
		;초조하게해번 파생
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게함 안
			CASE IS > 1
				PRINTFORMW 「뭐? 아직 내도 좋을 이유 없을 것이다?」
			;초조하게함 종료
			CASE 1
				PRINTFORMW 「어쩔 수 없구나, 내도 좋아」
				PRINTFORMW (나의 가슴까지 닿을까나?)
			;초조하게함 없음
			CASEELSE
				PRINTFORMW 「언제까지 견딜 수 있을까나?」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;위로돌리고
		IF TFLAG:위로돌리고
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;지문은 조교 대상의 하반신 복장 마다의 분기 있어. 필요에 따라서 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;위로돌리고
		;IF TFLAG:위로돌리고
		;	PRINTFORMW 
		;그 외
		;ELSE
			PRINTFORMW 「후훗, 너의 사타구니가 나의 것으로 질척질척이다」
		;ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_2, 페니스밴드 가랑이
;━━━━━━━━━━━━━━━━━━━━━━━━
;·조교 대상이 페니스밴드장착 안은, 페니스밴드 가랑이가 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;지문은 조교 대상의 하반신 복장 마다의 분기 있어. 필요에 따라서 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;위로돌리고
		IF TFLAG:위로돌리고
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT17, 파이즈리한다
;─
;·TFLAG:벌플래그가 끊고 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"의 경우는 발생하지 않는다
;·TEQUIP(오나홀/삼각목마) 장착중에는 발생하지 않는다
;·조교 대상으로 페니스가 없는 경우는 발생하지 않는다
;·조교자가 남자의 경우는 발생하지 않는다
;·현재 ACT파생는 존재하지 않지만 장래의 개변에 대비해, ACT파생 == 0으로 한다
;-------------------------------------------------
@KOJO_ACT_K21_17
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT17_0, 파이즈리한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;지문은 조교자의 가슴의 사이즈로 분기 합니다.
	;소질을 변경하는 것이 가능한 것으로, 필요에 따라서 확장하는 것도 좋을 것입니다
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;초조하게해번에 의해 분기
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게함 안
			CASE IS > 1
				PRINTFORMW 「나의 가슴에 내고 싶다는?」
				PRINTFORMW 「응, 어떻게 할까나?」
				PRINTFORMDW 쪽은 장난인 듯한 표정으로 당신을 보고 있다……
			;초조하게함 종료
			CASE 1
				PRINTFORMDW 골짜기로 할 수 있던 앞달리기의 작은 웅덩이를 유방에 쳐발라, 페이스를 올려 가슴을 상하시켰다
			;초조하게함 없음
			CASEELSE
				PRINTFORMW 「이대로, 이대로 유중에 내 봐라, 너의 정령을 받아 들여 준다」
				PRINTFORMDW 쪽은 그\@ TALENT:TARGET:거유 ? 큰# \@유방으로, 페니스를 완전하게 감싸 마찰을 걸쳤다…
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 「마음에 들었는지?   나의 가슴」
		PRINTFORMW 「이봐요, 자꾸자꾸 쿠퍼액이 나온다」
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(50)
		[SKIPSTART]
		;---------------------------------------------------
		;지문에는 착의 상태에 의한 분기가 존재합니다만, 나누어 쓰고도 귀찮은 것으로 필요에 따라서 comment out를 제외하든지
		;바라는 형태에 개변하든지 해 사용해 주세요
		;---------------------------------------------------
		;간섭하는 의복이 존재할 때
		IF TRUECHECK(TEQUIP:상반신속옷, TEQUIP:상반신윗도리, TEQUIP:전신윗도리) > 0 || (TEQUIP:MASTER:하반신속옷 && ! TALENT:벽통과) || CFLAG:의상 == 3
			;노출 상황에 의해 분기
			SELECTCASE TEQUIP:착의노출 & 6
				;간섭 개소전노출시
				CASE 6
					PRINTFORMW 
				;조교 대상 성기 노출시
				CASE 4
					PRINTFORMW 
				;조교자 가슴 노출시
				CASE 2
					PRINTFORMW 
				;간섭 개소비노출시
				CASEELSE
					PRINTFORMW 
			ENDSELECT
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		[SKIPEND]
		;그 밖에 분기를 마련하지 않는다면, 여기는 써 둡시다
		;분기를 마련한다면 여기의 PRINT는 공행을 낳으므로 삭제합시다
		PRINTFORMW 「예쁘겠지?   나의 가슴은」
		PRINTFORMW 「이것으로 비비어 받을 수 있다. 행복하게 생각하는구나」
		RETURN 1
	ENDIF
ENDIF

;─
;ACT18, 풋잡한다
;─
;·풋잡한다
;·조교 대상으로 페니스가 없는 경우는 발생하지 않는다
;·이상계로부터 파생하는 경우도 있다
;·TFLAG:벌플래그가 끊고 있으면 발생하지 않는다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·TEQUIP(오나홀/삼각목마) 장착중은 발생하지 않는다
;·성교중/성교봉사중은 발생하지 않는다
;·조교자에게 다리가 없으면 발생하지 않는다
;·조교 대상이 여자 아이라면 발생하지 않는다
;·현재 ACT파생는 존재하지 않지만 장래의 개변에 대비해, ACT파생 == 0으로 한다
;-------------------------------------------------
@KOJO_ACT_K21_18
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT18, 풋잡한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;지문에 TCVAR:MASTER:초조하게해번 등의 분기는 존재하지 않는 개소
	;=================================================
	;조교 대상이 갈 것 같다				양 다리로 %CALLNAME:MASTER%의 페니스를 몹시 거칠게 훑어 왔다…
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		PRINTFORMW 「이봐요 이봐요, 다리에 끼워져 많이 날려 버려랏」
		RETURN 1
	ENDIF
	
	;풋잡은 작자의 취미는 아닌 것 같아, 지문은 비교적 부실이었거나
	;반대로 말하면, 다양하게 해도 지문과의 정합성이 무너질 걱정은 그다지 없기 때문에, 좋을대로 하면 좋을 것입니다
	;=================================================
	;연속 실행						유쾌한 것 같게 %CALLNAME:MASTER%를 내려다 보면서 페니스를 동글동글 짓밟고 있다…
	;=================================================
	IF 구상기입체크(80) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 「나의 다리로 짓밟을 수 있어 흥분해…」
		PRINTFORMW 「후훗, 기분이 좋을 것이다?   알겠어」
		RETURN 1
	ENDIF
	
	;지문에 파생은 마련해 있지 않습니다만, 이것은 비교적 필요라고 생각되므로
	;=================================================
	;양말 신은 채로 풋잡(디폴트로 양말 신지 않은 캐릭터는 표시되지 않는다)
	;=================================================
	IF 구상기입체크(0) && TEQUIP:양말
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;조교자에게 TALENT:벽통과가 없는 경우, 옷감 너머로 된다
	;=================================================
	;하반신 복장이 방해
	;=================================================
	IF 구상기입체크(80) && PANTSCHECK(1)
		PRINTFORMW 「참지 않으면 옷이 더러워져 버리겠어―?
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「다리로 비비어 주자」
		PRINTFORMW 「후훗, 조금 커졌다」
		RETURN 1
	ENDIF
ENDIF

;─
;ACT19, 손가락삽입해라(ACT19_0, 손가락삽입해라/ACT19_1, G스팟 자극/ACT19_2, 바이브 출납)
;─
;·조교 대상이 후타나리 혹은 여자의 경우에 발생
;·조교 대상이 처녀의 경우 G스팟 자극등에 파생하지 않는＆쾌V에 마이너스 보정
;·바이브 장비중은 바이브 출납에 파생
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교 대상이 남자라면 발생하지 않는다
;·조교 대상이 후타나리의 경우, 초조하게함 안에는 발생하지 않는다
;-------------------------------------------------
;※조교 대상이 처녀는 아니다
;※연속 실행의 경우
;※갈것같다or쾌C가 5000이상의 경우, 조교자의 애무 스킬에 응해
;※발기도가 낮은 경우, 조교자의 애무 스킬에 응해
;※요점강부디기의 경우
;·상기 조건의 경우 G스팟 자극에 파생한다
;-------------------------------------------------
@KOJO_ACT_K21_19
;-------------------------------------------------
;·TFLAG:ACT파생(0=손가락삽입해라/1=G스팟 자극/2=바이브 출납)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_0, 손가락삽입해라					질구에 손가락끝을 주어, 살그머니 안쪽에 밀어넣었다…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;안면승마 안
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;위치전후 분기는 필요에 따라서
	;어디선가 통합해 버리는 것도 좋다고 생각합니다
	;=================================================
	;뒤로부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_1, G스팟 자극					손가락을 진입시키고 G스팟을 직접 자극했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;갈것같다or쾌C가 5000이상의 경우, 조교자의 애무 스킬에 응해 G스팟 자극
;G스팟 고문에 파생하면 전립선자극 취급이 된다
;-------------------------------------------------
;조교 대상이 후타나리의 경우에서도 발생하므로, 필요에 따라서 PENIS(MASTER) 되어! PENIS(MASTER)로 분기를 마련하면 좋다고 생각합니다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;뒤로부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_2, 바이브 출납					G스팟을 후벼파도록(듯이) 격렬하게 움직였다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;지문 자체에 파생은 없습니다만, @TARGET_DESCRIPTION에서의 상황 묘사가 들어갈 가능성이 있습니다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	PRINTFORMW 
	RETURN 1
ENDIF

;─
;ACT27, 밀어넘어뜨린다
;─
;·밀어넘어뜨린다/밀어넘어뜨리기 종료
;·이미 밀어넘어뜨리기라고 있는 경우, 해방이 된다
;·TFLAG:벌플래그가 끊고 있으면 발생하지 않는다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·TEQUIP:페니스밴드장착 안에는 발생하지 않는다
;·초조하게함 안에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K21_27
;-------------------------------------------------
;TFLAG:ACT파생(=0, 밀어넘어뜨린다/=1, 밀어넘어뜨리기 종료)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT27_0, 밀어넘어뜨린다			%CALLNAME:MASTER%를 밀어넘어뜨리기…
;━━━━━━━━━━━━━━━━━━━━━━━━
;지문에 파생은 없습니다만, @TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성이 있습니다
;-------------------------------------------------
IF 구상기입체크(50) && !TFLAG:ACT파생
	SELECTCASE RANDACT(2, 0)
		CASE 0
			PRINTFORMW 「자, 주인님에 배를 드러내 봐」
		CASE 1
			PRINTFORMW 「이것으로 너도 이루는 대로」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT27_1, 밀어넘어뜨리기 종료		%CALLNAME:MASTER%를 자유롭게 했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	PRINTFORMW 
	RETURN 1
ENDIF
