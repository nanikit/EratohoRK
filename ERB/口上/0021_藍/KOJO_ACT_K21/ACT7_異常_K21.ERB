;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	야쿠모쪽구상 이상계 액션 구상 파일
;
;	이상계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;이상계 ACT70~75
;========================================================================
;·TFLAG:ACT(70=피스트 성교/71=애널 피스트/72= 양구멍 피스트/73=방치 플레이/74=공중 육변기 플레이/75=페니스 밴드 장착)
;·TFLAG:ACT 분류 == 7에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "이상")로 참조하는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;─
;ACT70, 피스트 성교
;─
;·조교 대상의 질에 주먹을 넣는다
;·ACT 개시시점으로써 손을 대고 있다
;·조교 대상이 처녀인 경우는 처녀 상실이 된다(@ACTION_APPLY2_70으로 처리된다)
;·조교 대상이 남자인 경우는 발생하지 않는다
;·턴 방침이 "소프트", "노멀"의 경우는 발생하지 않는다
;·오늘의 방침이 "러브러브"의 경우는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·안면 승마계, 자위계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K21_70
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT70, 피스트 성교
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(100)
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 질에 주먹으로 침입하기 시작했다…
	
	;첫회 판정중 한쪽이 소비되었을 경우, 이후, 그 만큼기는 기독이 되어 표시되는 일은 없습니다
	;=================================================
	;첫회 「조교 대상이 처녀」
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:처녀 && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 「조교 대상이 처녀는 아니다」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(50)
		SELECTCASE ABL:MASTER:마조끼
			;마조끼 5이상
			CASE IS >= 5
				PRINTFORMW 「주먹이 들어가 있다고 하는데, 상당히 젖어 오고 있구나」
				PRINTFORMW 「느끼고 있구나」
			;마조끼 3이상
			CASE IS >= 3
				PRINTFORMW 「아파?   그렇지 않으면, 기분이 좋아?」
				PRINTFORMW 「뭐, 나도 경험이 없을 것도 아니지만…… 꽤 기분 좋은 것 같다」
			CASEELSE
				PRINTFORMW 「아프다고 생각하지만, 이것도 조교 가운데다」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;─
;ACT71, 애널 피스트
;─
;·조교 대상의 아누스에 주먹을 넣는다
;·ACT 개시시점으로써 손을 대고 있다
;·조교 대상이 남자인 경우는 발생하지 않는다
;·턴 방침이 "소프트", "노멀"의 경우는 발생하지 않는다
;·오늘의 방침이 "러브러브"의 경우는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·안면 승마계, 자위계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K21_71
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT71, 애널 피스트
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(100)
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 애널에 주먹으로 침입하기 시작했다…
	
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(50)
		SELECTCASE ABL:MASTER:마조끼
			;마조끼 5이상
			CASE IS >= 5
				PRINTFORMW 「옷… 상당히 간단하게 들어갔군」
				PRINTFORMW 「개발되고 있구나, 후훗」
			;마조끼 3이상
			CASE IS >= 3
				PRINTFORMW 「어때?   배설구에 넣을 수 있는 느낌은」
				PRINTFORMW 「배가 따뜻해지겠지?」
			CASEELSE
				PRINTFORMW 「과연 이것은 위험해?」
				PRINTFORMW 「힘을 빼,?」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;─
;ACT72, 양구멍 피스트
;─
;·조교 대상의 질과 아누스에 주먹을 넣는다
;·ACT 개시시점으로써 손을 대고 있다
;·조교 대상이 처녀인 경우는 처녀 상실이 된다(@ACTION_APPLY2_72로 처리된다)
;·조교 대상이 남자인 경우는 발생하지 않는다
;·턴 방침이 "소프트", "노멀", "하드"의 경우는 발생하지 않는다
;·오늘의 방침이 "러브러브"의 경우는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·안면 승마계, 자위계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K21_72
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT72, 양구멍 피스트
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(0)
	
	;지문↓
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 질과 애널에 주먹으로 침입하기 시작했다…
	
	;첫회 판정중 한쪽이 소비되었을 경우, 이후, 그 만큼기는 기독이 되어 표시되는 일은 없습니다
	;=================================================
	;첫회 「조교 대상이 처녀」
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:처녀 && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF

	;=================================================
	;첫회 「조교 대상이 처녀는 아니다」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT73, 방치 플레이
;─
;·조교자가 조교 대상을 방치한다
;·턴 방침이 "소프트", "노멀", "이상"이라면 발생하지 않는다
;·(이상계 ACT로 턴 방침 "이상"으로 연주하고 있는 것은 미스라고 생각되고)
;·오늘의 방침이 "러브러브"의 경우는 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·수치계로부터 발생하는 경우도 있다(0503판 현재, 수치계로부터 파생했을 경우에게만 발생이라고 생각되고)
;·안면 승마계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K21_73
;-------------------------------------------------
;·ACT의 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT73, 방치 플레이
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(0)
	
	;지문↓
	;%CALLNAME:TARGET%는 다만 %CALLNAME:MASTER%를 방치해 아무것도 하지 않는다…
	
	;=================================================
	;조교자 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교 대상도 자위중
		IF GETBIT(TEQUIP:자위중, 0)
			PRINTFORMW 
		;조교자만 자위중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;TEQUIP계 분기는 아이마스크 이외에도 바이브, 애널 바이브, 애널 비즈등이 생각됩니다
	;필요에 따라서 확장해 주세요
	;=================================================
	;아이마스크 장착중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:아이마스크
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT74, 공중 육변기 플레이
;─
;·속박된 조교 대상을 밖에 방치한다
;·방치되어 발견되었을 경우 조교 대상이 처녀이면 처녀 상실. 동정의 경우는 동정 상실이 된다
;·줄과 목걸이가 갖추어지지 않았다고 발생하지 않는다
;·턴 방침이 "소프트", "노멀", "하드"의 경우는 발생하지 않는다
;·오늘의 방침이 "러브러브"라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·REACT 분류가 "허가를 청한다"(이었)였던 경우는 캔슬된다
;-------------------------------------------------
@KOJO_ACT_K21_74
;-------------------------------------------------
;·TFLAG:ACT 파생(0=무사/1=애원/2=능욕/3=흥분)
;·조교 대상의 반응과 확률로 REACT 이후(ACTION_APPLY2)에 파생한다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT73, 공중 육변기 플레이
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문에 분기 없음
;-------------------------------------------------
IF 구상기입체크(0)
	
	;지문↓
	;%CALLNAME:TARGET%는 밖에서 속박된 %CALLNAME:MASTER%의 배에 매직으로 「↓공중 변소」라고 써,
	;그 자리에 두어 혼자서 돌아가려고 했다…
	
	;=================================================
	;조교 대상이 처녀
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:처녀
		;첫회
		IF FIRSTACT(0)
			PRINTFORMW 
		;첫회시에 애원에 의해 캔슬되었을 경우, 혹은 무사했던 경우
		;이 구조에서는, 처녀를 잃을 때까지 이"ELSE"의 개소에서 처리된다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 동정
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:동정
		;첫회
		IF FIRSTACT(0)
			PRINTFORMW 
		;첫회시에 애원에 의해 캔슬되었을 경우, 혹은 무사했던 경우
		;이 구조에서는, 동정을 잃을 때까지 이"ELSE"의 개소에서 처리된다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;처녀, 동정 이외의 경우의 첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT75, 페니스 밴드 장착
;─
;·조교자가 쌍두 페니스 밴드를 몸에 대어, 조교 대상으로 과시한다
;·조교자가 처녀인 경우 처녀 상실이 된다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교자에게 페니스가 있는 경우에는 발생하지 않는다
;·이미 장착하고 있었을 경우는 해제가 된다
;·안면 승마계는 해제된다
;-------------------------------------------------
@KOJO_ACT_K21_75
;-------------------------------------------------
;·TFLAG:ACT 파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT75_0, 페니스 밴드 장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·페니스 밴드 장비중은 쾌C가 쾌V에 변환
;·변환율… 쾌C를 20%로 해, 나머지의 값에 V감각+1/C감각+1을 걸어 2로 나누어 쾌V에 가산한다
;·지문은 ACT 파생, 및 처녀인지 아닌지로 분기 한다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;조교자의 초기설정이 처녀가 아닌 경우는(기본적으로) 불필요한 개소입니다
	;FIRSTACT의 이벤트 번호를 가지런히 해, 처녀 이외의 경우와 중복 하지 않게 되어 있습니다만
	;이 개소가 필요하지 않은 경우는 삭제해 버리는 것이 좋다고 생각합니다
	;=================================================
	;첫회 「조교자가 처녀」
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:처녀 && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;조교자가 처녀가 아닌 경우는 여기서 첫회 판정을 합니다
	;=================================================
	;첫회 「조교자가 처녀가 아닌 경우」
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT75_1, 페니스 밴드 해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·몸에 대고 있는 페니스 밴드를 떼어낸다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
