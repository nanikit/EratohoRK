;*************************************************
;자세 변경
;*************************************************
;우선 테두리만
;쓸지 어떨지는 뭐라고도 말할 수 없는 곳
;-------------------------------------------------
[SKIPSTART]
;=================================================
@KOJO_POSE_K64(ARG, ARG:1, ARG:2)
;=================================================
;ARG =자세
;ARG:1 =위치전후
;ARG:2 =위치상하
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;위치상하(자유)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG:2 == 0
	;=================================================
	;자세 변화 있어
	;=================================================
	IF 구상 기입 체크(0) && ARG >= 0
		;=================================================
		;자유
		;=================================================
		IF 구상 기입 체크(0) && ARG == 0
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;=================================================
		;위로돌리고
		;=================================================
		IF 구상 기입 체크(0) && ARG == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;=================================================
		;안정도
		;=================================================
		IF 구상 기입 체크(0) && ARG == 2
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;=================================================
		;네발로 엎드림
		;=================================================
		IF 구상 기입 체크(0) && ARG == 3
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;=================================================
		;기립
		;=================================================
		IF 구상 기입 체크(0) && ARG == 4
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;=================================================
		;무릎서기
		;=================================================
		IF 구상 기입 체크(0) && ARG == 10
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;자세 변화 없음
	;=================================================
	IF 구상 기입 체크(0) && ARG < 0
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;위치상하(위를 탔다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG:2 == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;위치상하(위에 실었다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG:2 == 2
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;위치상하(상하 변화 없음)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG:2 < 0
	;=================================================
	;위로돌리고
	;=================================================
	IF 구상 기입 체크(0) && ARG == 1
		SELECTCASE ARG:1	;위치전후
			CASE 0		;응시한다
				PRINTFORMW 
			CASE 1		;대면
				PRINTFORMW 
			CASE 2		;MASTER의 배후
				PRINTFORMW 
			CASE 3		;MASTER가 배후
				PRINTFORMW 
			CASE IS < 0	;전후 변화 없음
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;안정도
	;=================================================
	IF 구상 기입 체크(0) && ARG == 2
		SELECTCASE ARG:1	;위치전후
			CASE 0		;응시한다
				PRINTFORMW 
			CASE 1		;대면
				PRINTFORMW 
			CASE 2		;MASTER의 배후
				PRINTFORMW 
			CASE 3		;MASTER가 배후
				PRINTFORMW 
			CASE IS < 0	;전후 변화 없음
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;네발로 엎드림
	;=================================================
	IF 구상 기입 체크(0) && ARG == 3
		SELECTCASE ARG:1	;위치전후
			CASE 0		;응시한다
				PRINTFORMW 
			CASE 1		;대면
				PRINTFORMW 
			CASE 2		;MASTER의 배후
				PRINTFORMW 
			CASE 3		;MASTER가 배후
				PRINTFORMW 
			CASE IS < 0	;전후 변화 없음
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;기립
	;=================================================
	IF 구상 기입 체크(0) && ARG == 4
		SELECTCASE ARG:1	;위치전후
			CASE 0		;응시한다
				PRINTFORMW 
			CASE 1		;대면
				PRINTFORMW 
			CASE 2		;MASTER의 배후
				PRINTFORMW 
			CASE 3		;MASTER가 배후
				PRINTFORMW 
			CASE IS < 0	;전후 변화 없음
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;무릎서기
	;=================================================
	IF 구상 기입 체크(0) && ARG == 10
		SELECTCASE ARG:1	;위치전후
			CASE 0		;응시한다
				PRINTFORMW 
			CASE 1		;대면
				PRINTFORMW 
			CASE 2		;MASTER의 배후
				PRINTFORMW 
			CASE 3		;MASTER가 배후
				PRINTFORMW 
			CASE IS < 0	;전후 변화 없음
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;자유
	;=================================================
	IF 구상 기입 체크(0) && ARG == 0
		SELECTCASE ARG:1	;위치전후
			CASE 0		;응시한다
				PRINTFORMW 
			CASE 1		;대면
				PRINTFORMW 
			CASE 2		;MASTER의 배후
				PRINTFORMW 
			CASE 3		;MASTER가 배후
				PRINTFORMW 
			CASE IS < 0	;전후 변화 없음
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
[SKIPEND]
