;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	진하고 구상 도구계 액션 구상 파일
;
;	도구계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;도구계 ACT20~26
;========================================================================
;·TFLAG:ACT(20=로터/21=바이브/22=애널 바이브/23=애널 비즈/24=C애무 도구/25=니플캡/26=페니스 밴드)
;·TFLAG:ACT 분류 == 2에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "도구")로 줍는 것이 가능
;·조교자가 조교 대상으로 도구를 사용한다. 이 구상의 시점에서 조교자는 벌써 손을 대고 있다
;·미약·로션을 사용하는 케이스가 있다
;·도구류의 장착, 해제등은 TFLAG:ACT 파생으로 정의된다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;─
;ACT20, 로터
;─
;·C 중심으로 애무한다
;·애무계로부터 발생하는 경우도 있다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "하드", "이상"이라면 발생하지 않는다
;·TEQUIP(클리캡/오나홀/페니스 밴드/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교 대상으로 페니스가 있는 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K64_20
;-------------------------------------------------
;·ACT 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT20, 로터
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;TCVAR:MASTER:위치 전후와 TEQUIP:안면 승마/안면 승마 애널로 지문이 변화합니다
	;필요에 따라서 분기를 증감해 주세요
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:갈것같다
		SELECTCASE TCVAR:MASTER:초조하게해번	;초조하게 해 번에 의해 분기
			CASE IS > 1	;초조하게 해 안
				PRINTFORMW 
			CASEELSE	;초조하게 해 없음·초조하게 해 종료
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;하반신 복장이 요점 밀치고
	;=================================================
	IF 구상기입체크(0) && PANTSCHECK(1, 1)
		IF PANTSCHECK(1)	;옷 너머로
			PRINTFORMW 
		ELSE				;그 외	LOWERJACKET(1) == 1으로 옷자락으로부터 손을 넣어 LOWERUNDER(1) == 1으로 속옷을 비켜 놓는 상황. 양쪽 모두 성립하고 있는 가능성도 있다
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;위치 전후를 분리한다면, TCVAR:MASTER:위치 전후 == 2로 뒤로부터가 됩니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		IF TCVAR:MASTER:위치전후 == 1	;전부터 애무
			PRINTFORMW 
		ELSE							;뒤로부터 애무
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT21, 바이브
;─
;·조교 대상이 남자의 경우는 발생하지 않는다
;·TFLAG:벌플래그가 끊고 있으면 발생하지 않는다
;·턴 방침이 "이상"이라면 발생하지 않는다
;·TEQUIP(삼각 목마/페니스 밴드) 장착중이라면 발생하지 않는다
;·성교중에는 발생하지 않는다
;·조교 대상이 처녀라도 발생할 가능성이 있다
;-------------------------------------------------
@KOJO_ACT_K64_21
;-------------------------------------------------
;·TFLAG:ACT 파생(0=바이브 장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT21_0, 바이브 장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 후타나리의 경우, 전립선 자극 취급이 되어 발기도가 상승한다
;지문은 파생하지 않기 때문에, ABL:MASTER:V감각이든지 TEQUIP든지로 분기를 늘려도 괜찮다고 생각합니다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;조교 대상이 처녀의 경우, 처녀 상실이 일어난다. REACT로 참조하는 경우는 TALENT:MASTER:처녀는 아니고, TCVAR:MASTER:처녀 상실로
	;=================================================
	;처녀
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:처녀
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 후타나리
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:후타나리
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT21_1, 바이브 해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·이미 장비 하고 있는 경우, 바이브 해제가 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT22, 애널 바이브
;─
;·턴 방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP(애널 비즈/관장기＋플러그/삼각 목마/페니스 밴드) 장착중은 발생하지 않는다
;·애널 섹스중(TEQUIP:성교중 == 6, 혹은 A_SEX())에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K64_22
;-------------------------------------------------
;TFLAG:ACT 파생(0=애널 바이브 장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT22_0, 애널 바이브 장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 남자의 경우, 전립선 자극 취급이 되어 발기도가 상승
;지문은 파생하지 않기 때문에, ABL:MASTER:A감각이든지 TEQUIP든지로 분기를 늘려도 괜찮다고 생각합니다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;바이브 장착중(두 구멍 고문)
	;=================================================
	IF 구상기입체크(0) && TEQUIP:TARGET:바이브
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT22_1, 애널 바이브 해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;이미 장비 하고 있는 경우, 애널 바이브 해제가 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT23, 애널 비즈
;─
;·애널 비즈 장착/해제/추가
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "이상"이라면 발생하지 않는다
;·TEQUIP(애널 바이브/관장기＋플러그/삼각 목마/페니스 밴드) 장착중에는 발생하지 않는다
;·A성교중(TEQUIP:성교중 == 6, 혹은 A_SEX()) 시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K64_23
;-------------------------------------------------
;TFLAG:ACT 파생(0=장착/1=해제/2=추가)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT23_0, 애널 비즈 장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;·장착시에 1~3개의 사이에 랜덤으로 정해진다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT_23_1, 애널 비즈 해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·구슬이 5개 들어가 있는 경우, 애널 비즈 해제가 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT23_2, 애널 비즈 추가
;━━━━━━━━━━━━━━━━━━━━━━━━
;·비즈의 추가는 1~3의 사이에 랜덤으로 정해진다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		SELECTCASE TEQUIP:TARGET:애널비즈	;비즈의 개수에 의한 파생
			CASE IS >= 4	;4개목, 혹은 5개째. 사양상, 5개 이상이 되는 일은 없다
				PRINTFORMW 
			CASE 3			;3개목
				PRINTFORMW 
			CASEELSE		;그 외(1개째/2개목)
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;─
;ACT24, C애무 도구
;─
;·클리캡/오나홀의 장착, 해제
;·오나홀을 소지 또한 조교 대상으로 페니스가 있는 경우는 「오나홀」에 파생한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "하드", "이상"의 경우는 발생하지 않는다
;·TEQUIP(삼각 목마/페니스 밴드) 장착중에는 발생하지 않는다
;·오나홀 장착은 발기도가 1000 이하(TCVAR:MASTER:발기도 < 1000)의 경우에 연주해져 발생하지 않는다
;·성교 봉사중에는 발생하지 않는다
;-------------------------------------------------
;·어느쪽이나 비전동 사양. 마음대로 움직이지 않아!
;-------------------------------------------------
@KOJO_ACT_K64_24
;-------------------------------------------------
;·TFLAG:ACT 파생(0=클리캡 장착/1=클리캡 해제/2=오나홀 장착/3=오나홀 해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT24_0, 클리캡 장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT24_1, 클리캡 해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·이미 장비 하고 있는 경우, 클리캡 해제가 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT24_2, 오나홀 장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
		IF TALENT:MASTER:동정	;조교 대상이 동정
			PRINTFORMW
		ELSE					;그 외
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT24_3, 오나홀 해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·이미 장비 하고 있는 경우, 오나홀 해제가 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 3
	;=================================================
	;정액 서로 더러워져
	;=================================================
	IF 구상기입체크(0) && GET_STAIN("페니스", "정액", MASTER)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT25, 니플캡
;─
;·니플캡 장착/해제
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "하드", "이상"이라면 발생하지 않는다
;·니플캡이라고 말해도, 지문에서는 클립 로터라고 하는 취급
;-------------------------------------------------
;·실태는 클립 로터인 것으로 움직인다. 동력이 전기인가 어떤가는 차치하고
;-------------------------------------------------
@KOJO_ACT_K64_25
;-------------------------------------------------
;TFLAG:ACT 파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT25_0, 니플캡 장착
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT25_1, 니플캡 해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;·이미 장비 하고 있는 경우, 니플캡 해제가 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT26, 페니스 밴드
;─
;·페니스 밴드 장착/해제
;·조교자가 조교 대상으로 쌍두 페니스 밴드 장착 시키려고 하는 장면
;·조교 대상이 처녀라면, 처녀 상실이 일어난다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·TEQUIP:(애널 비즈/애널 바이브/관장기＋플러그/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교 대상으로 페니스가 있으면 발생하지 않는다
;·TEQUIP(바이브/클리캡)은 양립하지 않는 것으로서 해제된다
;-------------------------------------------------
@KOJO_ACT_K64_26
;-------------------------------------------------
;·TFLAG:ACT 파생(0=장착/1=해제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT26_0, 페니스 밴드를 장착시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		IF TALENT:MASTER:처녀	;조교 대상이 처녀
			PRINTFORMW 
		ELSE					;그 외
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT26_1, 페니스 밴드를 해제시킨다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
