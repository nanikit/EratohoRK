;-------------------------------------------------
;こいし口上 汎用アクション口上ファイル
;-------------------------------------------------
;個別の調教者アクションが表示されない場合に
;呼び出される口上をまとめたファイル（仮）
;あくまで個人用なのでコメントは最小限
;他のキャラに適用する場合は@KOJO_ACT_KXXを任意の番号に変更
;現在は将来用って感じ
;-------------------------------------------------
;・例によって例の如くアリスさんからお借りしてきた
;-------------------------------------------------
;*************************************************
;汎用アクション口上
;*************************************************
[SKIPSTART]
;=================================================
;@TFLAG:80の各項目で分岐
;=================================================
;・個別の調教者アクションが表示されなかった際に表示
;・ACTはUSEのスキップがあるから念のため戻り値に注意
;・戻り値 (0.非表示/1.表示/2.表示して媚薬等口上をスキップ)
;-------------------------------------------------
@KOJO_ACT_K64
;-------------------------------------------------

;-------------------------------------------------
;愛撫、奉仕、性交奉仕、性交等@TFLAG:80で分岐
;-------------------------------------------------
IF 口上記入チェック(0)
	;=================================================
	;会話
	;=================================================
	IF 口上記入チェック(0) && MENUMATCH(TFLAG:ACT, "会話")
		;-------------------------------------------------
		;相思相愛
		;-------------------------------------------------
		IF 口上記入チェック(0) && AFFECTION("相思相愛")
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;愛撫
	;=================================================
	IF 口上記入チェック(0) && MENUMATCH(TFLAG:ACT, "愛撫")
		;-------------------------------------------------
		;相思相愛
		;-------------------------------------------------
		IF 口上記入チェック(0) && AFFECTION("相思相愛")
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;道具
	;=================================================
	IF 口上記入チェック(0) && MENUMATCH(TFLAG:ACT, "道具")
		;-------------------------------------------------
		;相思相愛
		;-------------------------------------------------
		IF 口上記入チェック(0) && AFFECTION("相思相愛")
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;性交
	;=================================================
	IF 口上記入チェック(0) && MENUMATCH(TFLAG:ACT, "性交")
		;-------------------------------------------------
		;相思相愛
		;-------------------------------------------------
		IF 口上記入チェック(0) && AFFECTION("相思相愛")
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;羞恥
	;=================================================
	IF 口上記入チェック(0) && MENUMATCH(TFLAG:ACT, "羞恥")
		;-------------------------------------------------
		;相思相愛
		;-------------------------------------------------
		IF 口上記入チェック(0) && AFFECTION("相思相愛")
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;奉仕
	;=================================================
	IF 口上記入チェック(0) && MENUMATCH(TFLAG:ACT, "奉仕")
		;-------------------------------------------------
		;相思相愛
		;-------------------------------------------------
		IF 口上記入チェック(0) && AFFECTION("相思相愛")
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;加虐
	;=================================================
	IF 口上記入チェック(0) && MENUMATCH(TFLAG:ACT, "加虐")
		;-------------------------------------------------
		;相思相愛
		;-------------------------------------------------
		IF 口上記入チェック(0) && AFFECTION("相思相愛")
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;異常
	;=================================================
	IF 口上記入チェック(0) && MENUMATCH(TFLAG:ACT, "異常")
		;-------------------------------------------------
		;相思相愛
		;-------------------------------------------------
		IF 口上記入チェック(0) && AFFECTION("相思相愛")
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;使役
	;=================================================
	IF 口上記入チェック(0) && GET_ACTM(TFLAG:ACT) == 8
		;-------------------------------------------------
		;相思相愛
		;-------------------------------------------------
		IF 口上記入チェック(0) && AFFECTION("相思相愛")
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;ご休憩
	;=================================================
	IF 口上記入チェック(0) && MENUMATCH(TFLAG:ACT, "休憩")
		;-------------------------------------------------
		;相思相愛
		;-------------------------------------------------
		IF 口上記入チェック(0) && AFFECTION("相思相愛")
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;性交奉仕
	;=================================================
	IF 口上記入チェック(0) && MENUMATCH(TFLAG:ACT, "性奉")
		;-------------------------------------------------
		;相思相愛
		;-------------------------------------------------
		IF 口上記入チェック(0) && AFFECTION("相思相愛")
			PRINTFORMW
			RETURN 1
		ELSE
		
		;-------------------------
		;その他
		;-------------------------
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF
[SKIPEND]
