;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	진하고 구상 추가 액션 구상 파일
;
;	조교자가 조교 지시 이외에 실시하는 행동의 파일
;
;Version		Reason
;0. 01			앨리스 구상 추가 액션 구상 파일 Version1. 67을 베이스로 재구축
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;*************************************************
;조교 액션·커맨드 구상
;*************************************************

;=================================================
;탈의 액션 구상
;=================================================
;·조교자 액션의 앞에 강제탈의나 자주탈의가 발생했을 때에 표시
;·이 구상의 시점에서는 아직 TEQUIP는 갱신되어 있지 않다
;·첫회 분기는 「처음 보았다/보였다」란 할 수 없다.
;  숍 메뉴로 벗게 했던 적이 있을지도 모르고, 조교의 흐름으로 완전하게 벗게 하지 않고서 노출만 하고 있는 경우도 있다.
;  어쩌면 조수였던 때에 본 적이 있을지도 모른다. 그러한 의미에서는 간이적인 것이다.
;  엄밀한 의미에서의 「초견」플래그를 준비할지 어떨지는 요망하는 대로라고 하는 일로.
;-------------------------------------------------
;memo
;·기재가 사라지고 있었기 때문에 사양 변경된 것이라고 생각하고 있으면, 아무래도 코드적으로는 조수가[주인님]의 경우의 특수 처리가 남아 있는 분위기
;·실제로 움직여 확인은 하고 있지 않지만, 변함없으면 이하와 같이 움직이가 될 것
;·조교자가 벗게 하는 경우→조수가[주인님]라면 조교자의 탈의 서포트 구상을 부른다. 그렇지 않으면 조교자의 탈의 구상(여기)를 부른다
;·조수가 벗게 하는 경우→조수가[주인님]라면 조수의 탈의 구상(여기)를 부른다. 그렇지 않으면 조수의 탈의 서포트 구상을 부른다
;·이 특수 처리때는, 일시적으로 TARGET와 ASSI가 바뀌므로, 이 구상의 구상주는 항상 TARGET인 것이 보증된다
;·즉 이 구상은 조교자의 강제/자주탈의시 뿐만이 아니라, 조수의 탈의 서포트에서도 불리는 일이 있다. 좋다?
;·그리고 조수 구상은 기본적으로 오른쪽 대고로 표시되므로, 이 구상도 오른쪽 공격이 되는 경우가 있다. 여기가 포인트
;· 오른쪽 대고 위, 대사나 지문의 즉시가 발생한다 따위 해, 레이아웃적으로 무너지는 부분이 나오지 않는가 주의 할 것. 테스트 중점인
;·그렇지 않아도 즉시가 발생하는 것 같은 장문은… 적당한 개행이… 그… 어때?
;·귀찮으면 입장 한정으로 회피해 버리는 것도 손일지도?
;=================================================
@KOJO_DATUI_K64
;=================================================
;-------------------------------------------------
;관련 변수…
;강제탈의:TFLAG:102  자주탈의:TFLAG:103
;=(-1, 강제탈의에 저항, 1, 윗도리, 2, 하반신윗도리, 3, 웃도리, 4, 상반신 전부, 5, 하반신 전부,
;  6, 악세사리─, 7, 악세사리─이외 모두, 8, 전라까지, 9, 속옷만)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의에 저항된 FIRSTDATUI(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:강제탈의 < 0
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크() && FIRSTDATUI(0)
		PRINTFORMW 「저것,%K64_CNAME()%어째서 방해 하는 거야?」
		IF TALENT:MASTER:남자
			PRINTFORMW 「보여져 부끄러운 것 같은 허술한 것이라도 매달려 있는 거야?」
			PRINTFORMW 「…%K64_CNAME()%는 애완동물인 것이니까, 너무 멋대로를 말해서는 안 돼요」
		ELSE	;후타나리/여자 아이
			IF ASSISTALENT(GETNUM(TALENT, "남자")) >= 0	;조수에 남자가 있다. 이 사양은 현재 사라지고 있다
				PRINTFORMW 「아,%K64_CNAME(ASSISTALENT(GETNUM(TALENT, "남자")))%가 보고 있기 때문에 부끄러운거야?」
				PRINTFORMW 「… 그런가… 우선은 거기로부터 예의범절을 가르치지 않으면 안되는거네」
			ELSE
				PRINTFORMW 「별로 여자 아이끼리인 것이니까, 부끄러워한다일 없는데…」
				PRINTFORMW 「하물며,%K64_CNAME()%는 애완동물인 것이니까…」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;자신은 벗었다
	;=================================================
	IF 구상기입체크() && TFLAG:자주탈의
		PRINTFORMW 「좀, 나만 벗어%K64_CNAME()%는 벗지 않는다는건 어떻게 말하는 일이야?」
		PRINTFORMW 「그 거 간사하지 않아?」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE RANDDATUI(2, 0)
		CASE 0
			PRINTFORMW 「응, 옷을 벗기는 것만으로 그렇게 저항하다니…」
			PRINTFORMW 「이래서야 앞일이 걱정되는구나…」
		CASEELSE
			PRINTFORMW 「또 그렇게 방해 한다～!」
			PRINTFORMW 「주인님에 피부를 쬐는 것이 그렇게 싫어?」
			PRINTFORMW 「……… 뭐, 그건 그걸로 즐길 수 있을 것 같지만 말야♪」
	ENDSELECT
	RETURN 1
ENDIF

;·주로 섹스계 행동의 예비 동작으로 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;두 사람 모두 뭔가 벗은 FIRSTDATUI(1)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 > 0 && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:주인님 && FIRSTDATUI(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가[정욕]또한전라가 되었다
	;=================================================
	IF 구상기입체크(0) && T_COND("정욕") && ZENRACHECK(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 조교 대상을 몹시 사랑함
	;=================================================
	IF 구상기입체크(0) && AFFECTION("몹시 사랑함")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 
	RETURN 1
ENDIF

;·주로 양말. 그 밖에 장갑이나 소매 따위
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·악세사리─마셔 FIRSTDATUI(2)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 == 6 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(2)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전라가 되었다(벗기 전은 양말 이외 알몸이라든지 그러한 느낌)
	;=================================================
	IF 구상기입체크(0) && ZENRACHECK(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번 탈의로 성기가 보이게 되었을 경우에 표시
;·팬티 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·만p FIRSTDATUI(3)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && P_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(3)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써 가슴이 보이고 있다
	;=================================================
	IF 구상기입체크(0) && B_DATUICHECK(0) == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;응 이렇게 p
	;=================================================
	IF 구상기입체크(0) && PENIS(TARGET)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번 탈의로 젖가슴이 보이게 되었을 경우에 표시
;·브라 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·말하는 p FIRSTDATUI(4)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && B_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(4)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써 성기가 보이고 있다
	;=================================================
	IF 구상기입체크(0) && P_DATUICHECK(0) == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번 탈의로 팬티(캐릭터에 의해 드로워즈·트렁크스)가 보이게 되었을 경우에 표시
;·하반신속옷이 없는 캐릭터는 쓰는 필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·팬티 보이고 FIRSTDATUI(5)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && P_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(5)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번 탈의로 브라(캐릭터에 의해 스포트브라·사라시)가 보이게 되었을 경우에 표시
;·상반신속옷이 없는 캐릭터는 쓰는 필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·브라 보이고 FIRSTDATUI(6)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && B_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(6)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 
	RETURN 1
ENDIF

;·「전라까지 벗었다」만이 아니고, 「지금 벗었으므로전라가 되었다」라고 하는 의미
;·「윗도리 밖에 남지 않은 상태로 윗도리를 벗었다」등도 포함된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·전라에 FIRSTDATUI(7)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && ZENRACHECK(0) && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(7)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 
	RETURN 1
ENDIF

;·주로 양말. 그 밖에 장갑이나 소매 따위
;·벗긴 중에 접어들지 않는다! 되기 십상인 것으로 이것만은 부위별 텐프레를 준비
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·악세사리─마셔 FIRSTDATUI(8)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 == 6
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(8)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전라가 되었다
	;=================================================
	IF 구상기입체크(0) && ZENRACHECK(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번 탈의로 남성기가 보이게 되었을 경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·고추 보여라 FIRSTDATUI(9)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && PENIS(MASTER)
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번 탈의로 여성기가 보이게 되었을 경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·그대로응 보여라 FIRSTDATUI(10)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && !TALENT:MASTER:남자
	IF PENIS(TARGET)	;조교자에게 페니스 있어
		PRINTFORMW 
	ELSE				;조교자에게 페니스 없음
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;·이번 탈의로 젖가슴이 보이게 되었을 경우에 표시
;·브라 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;·조교 대상이 남자라고 표시하지 않기 때문에, 남자 한정 구상에서는 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·젖가슴 보여라 FIRSTDATUI(11)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 3 && !TALENT:MASTER:남자
	PRINTFORMW 
	RETURN 1
ENDIF

;·이번 탈의로 하반신속옷이 보이게 되었을 경우에 표시
;·조교 대상이 하반신속옷의 없는 캐릭터 한정의 구상이면 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·팬티 보여라 FIRSTDATUI(12)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 2
	IF TALENT:MASTER:남자
		PRINTFORMW 
	ELSEIF TALENT:MASTER:후타나리
		PRINTFORMW 
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;·이번 탈의로 브라 상반신속옷이 보이게 되었을 경우에 표시
;·조교 대상이 남자라고 표시하지 않기 때문에, 남자 한정 구상에서는 쓸 필요가 없다
;·조교 대상이 상반신속옷의 없는 캐릭터 한정의 구상이면 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·브라 보여라 FIRSTDATUI(13)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 2 && !TALENT:MASTER:남자
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(13)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 
	RETURN 1
ENDIF

;·「전라까지 벗겼다」만이 아니고, 「지금 벗겼으므로전라가 되었다」라고 하는 의미
;·「윗도리 밖에 남지 않은 상태로 윗도리를 벗겼다」등도 포함된다
;·「젖가슴 보여라」～「팬티 보여라」를 썼을 경우는 기입할 필요는 없다(상층의 분기에 먹혀져 여기까지 떨어져 오지 않기 때문에)
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·전라에 FIRSTDATUI(14)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의 && ZENRACHECK(1)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(14)
		IF TALENT:MASTER:남자
			PRINTFORMW 
		ELSEIF TALENT:MASTER:후타나리
			PRINTFORMW 
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF TALENT:MASTER:남자
		PRINTFORMW 
	ELSEIF TALENT:MASTER:후타나리
		PRINTFORMW 
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;·자위·봉사계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·뭔가 벗은 FIRSTDATUI(15)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(ASSI, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(15)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 
	RETURN 1
ENDIF

;·애무계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·뭔가 벗긴 FIRSTDATUI(16)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:강제탈의
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTDATUI(16)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 
	RETURN 1
ENDIF


;=================================================
;추가 액션 구상
;=================================================
;·조교자가 추가행동을 취했을 때에 범용적으로 표시
;·쓰는 경우, 액션 구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------

;-------------------------------------------------
;EXTRA1, C애무
;-------------------------------------------------
;·추가로 애무 또는 자위를 실시한다
;·조교 대상으로 페니스가 없는 경우는 애무, 어느 경우는 자위
;-------------------------------------------------
@KOJO_ACT_EXTRA_K64_1
;-------------------------------------------------
;EXTRA1_0, 애무한다
;-------------------------------------------------
;·조교 대상으로 페니스가 없는 경우
;-------------------------------------------------
IF 구상기입체크(0) && !PENIS(MASTER)
	PRINTFORMW 
	RETURN 1
ENDIF

;-------------------------------------------------
;EXTRA1_1, 자위 한다
;-------------------------------------------------
;·조교 대상으로 페니스가 있는 경우
;-------------------------------------------------
IF 구상기입체크(0) && PENIS(MASTER)
	PRINTFORMW 
	RETURN 1
ENDIF

;-------------------------------------------------
;EXTRA2, V애무
;-------------------------------------------------
;·추가로 바지 애무를 실시한다
;·조교 대상이 남자의 경우 발생하지 않는다
;·조교 대상이 처녀의 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K64_2
IF 구상기입체크(0)
	PRINTFORMW 
	RETURN 1
ENDIF

;-------------------------------------------------
;EXTRA3, A애무
;-------------------------------------------------
;·추가로 애널애무를 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K64_3
IF 구상기입체크(0)
	PRINTFORMW 
	RETURN 1
ENDIF

;-------------------------------------------------
;EXTRA4, B애무
;-------------------------------------------------
;·추가로 유두 고문을 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K64_4
IF 구상기입체크(0)
	PRINTFORMW 
	RETURN 1
ENDIF


;=================================================
;미약등사용구상
;=================================================
;·조교자가 미약이나 로션을 사용했을 때에 범용적으로 표시
;·「액션 구상」으로 기입 체크를=2로 했을 경우는 표시되지 않는다
;·쓰는 경우, 액션 구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
@KOJO_USE_K64
;-------------------------------------------------
;로션/미약동시 사용
;-------------------------------------------------
;·주로 성교계 커맨드로부터 발생한다
;-------------------------------------------------
IF 구상기입체크(0) && (TFLAG:미약등사용구상 & 3) == 3
	PRINTFORMW 
	RETURN 1
ENDIF

;-------------------------------------------------
;로션 사용
;-------------------------------------------------
;·완구·성교계 커맨드등으로부터 발생한다
;-------------------------------------------------
IF 구상기입체크() && (TFLAG:미약등사용구상 & 1)
	PRINTFORMW 「그렇다, 이것도 사용해 준다」
	PRINTFORMW 「네, 미끈미끈～♪」
	RETURN 1
ENDIF

;-------------------------------------------------
;미약사용
;-------------------------------------------------
;·애무계 커맨드등으로부터 발생한다
;·키스로 발생했을 경우는 입으로 옮김이 되기 (위해)때문에, 그 케이스는 키스의 액션 구상내에서 처리하는 편이 좋다
;-------------------------------------------------
IF 구상기입체크(0) && (TFLAG:미약등사용구상 & 2)
	PRINTFORMW 
	RETURN 1
ENDIF
