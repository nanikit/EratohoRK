;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	첸구상 조교자 행동 전이벤트 구상 파일
;
;	조교자 행동 전이벤트 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV12, 조교자 행동 서두
;========================================================================
;·TRAIN_PROCCES.ERB내, @TRAIN_CANCEL로부터 불려 간다
;·구상주의 파라미터 표시의 직후에 표시. 조교 지시는 이미 결정되고 있다
;·조교 중지 선언의 교환이나, 조교 턴 개시전에 주인님으로부터 한 마디, 와 같이 쓰는 일도 가능
;·오전, 오후의 조교 개시는 아니고, 조교 턴의 개시전에는 매회 불려 가는 개소
;·그 때문에도 있어, 조교 중지시와 경과시간 0이외의 개소는 IF 구상 기입 체크(25) 등으로서 확률 표시하는 일을 추천
;·조교 중지시에게만 지문이 존재한다
;·문장 표시 후에 본체측의 DRAWLINE 처리 있어
;-------------------------------------------------
;FIRSTEVENT(0)～나, FIRSTEVENT(10)～등으로 해서 테두리마다 할당하고 있습니다만
;FIRSTEVENT/ONCEEVENT/RANDEVENT/ORDEREVENT는 사실상 무한하게 이벤트번호를 늘릴 수 있으므로
;필요에 따라서 임의에 할당해 주세요
;-------------------------------------------------
@KOJO_EVENT_K13_12(ARG)
;-------------------------------------------------
;인수 내역…
;ARG(0=조교가능/1=구상주의 피로로 중지/2=조교 대상의 피로로 중지/3= 포상에 의한 중지)
;-------------------------------------------------

;변수 초기화
VAR_K13:13 = 0

;자위절정의 판정용
IF VAR_K13:17 >= 1
	VAR_K13:17 ++
ENDIF

;꼬리 플래그
IF TCVAR:MASTER:초조하게해번 <= 0
	VAR_K13:30 = 0
ENDIF

;─
;조교 중지 ARG:1 ARG:2 ARG:3 FIRSTEVENT(0)～
;─
;·피로, 혹은 포상에 의한 조교 중지의 경우의 구상
;·기입 체크=1의 경우는 구상 표시 후 본체측에서 PRINTL의 공행이 들어간다
;·기입 체크를=2로 하면, 지문을 표시하지 않는다
;·이라고 할까 RESULT > 2로 판정되므로 2이상이라면 같은 거동이 됩니다만 일단 비추천이라고 하는 일로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;포상에 의한 중지
;RESULT치(1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 3
	;기입 체크를 2 이하로 했을 경우는 이하의 지문이 표시된다. 표시 타이밍은 구상 표시의 뒤
	;%CALLNAME:TARGET%는 오늘의 조교를 중지해,
	;%CALLNAME:MASTER%멈추거나로 한 시간을 보냈다…
	PRINTFORMW 
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자의 피로에 의한 중지
;RESULT치(1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 1
	;기입 체크를 2 이하로 했을 경우는 이하의 지문이 표시된다. 표시 타이밍은 구상 표시의 뒤
	;조교의 시간이 왔지만, 들어 온 %CALLNAME:TARGET%는 어쩐지 피곤한 모습으로,
	;이번은 휴일이라고 말해 나갔다…
	PRINTFORMW 
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상의 피로에 의한 중지
;RESULT치(1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 2
	;기입 체크를 2 이하로 했을 경우는 이하의 지문이 표시된다. 표시 타이밍은 구상 표시의 뒤
	;조교의 시간이 왔지만, %CALLNAME:TARGET%는 심한 모습의 %CALLNAME:MASTER%를 봐 당분간 생각하면,
	;이번은 쉬어도 좋다고 말해 나갔다…
	PRINTFORMW 
	RETURN 2
ENDIF

;-------------------------------------------------
;·이하, ARG:0(조교가능) 상당
;·TRAIN_PROCCES.ERB내, @BEFORE_ACT로부터 불려 간다
;·이 시점에서 ACT는 벌써 결정하고 있지만 ACT파생의 결정은 이루어지지 않았다
;·NOWEX의 리셋트는 되지 않았다
;·이것들의 개소를 빠진 뒤로 ACT 개시가 된다
;·분기의 증감은 자유롭게 할 수 있습니다만, 우선도에는 주의합시다
;-------------------------------------------------

;─
;조교 개시 직후 FIRSTEVENT(10)～
;─
;·경과시간이 0인 경우에 표시된다
;·그 회의 조교 개시 직후에 한 번만으로, 이후 그 조교중에는 표시되지 않는다
;·이 개소를 빠진 뒤로 ACT 개시가 된다
;-------------------------------------------------
;  여기서 IS_POLICY("오늘", "벌") 등으로오늘의방침를 말하게 하는 일도 가능했다거나 합니다만
;  EVENT17, 오늘의 방침 구상과 입을 가능성이 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 개시 직후
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:경과시간
	;=================================================
	;조교자가 조금 피곤함
	;=================================================
	IF 구상기입체크(0) && CFLAG:소모 > 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ARG:3의 분기를 썼을 경우는 불필요한 개소
	;=================================================
	;포상에 의한 조교 중지의 경우
	;=================================================
	IF IS_POLICY("오늘", "휴게")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ARG:1의 분기를 썼을 경우는 불필요한 개소
	;=================================================
	;조교 대상의 피로에 의한 중지
	;=================================================
	IF 구상기입체크(0) && CFLAG:MASTER:소모 > 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 조금 피곤함
	;=================================================
	IF 구상기입체크(0) && CFLAG:MASTER:소모 > 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상에의 호감도 1000이상
	;=================================================
	IF 구상기입체크(0) && AFFECTION("권애")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;─
;절정 한 직후 FIRSTEVENT(50)～
;─
;·조교자, 조교 대상이 절정 했을 경우에 표시되는 구상
;·개별의 부위별 절정을 줍고 싶으면, IF 구상 기입 체크(0) && NOWEX:C절정등의 분기를 추가해 주세요
;·사정종은 NOWEX:사정 /NOWEX:MASTER:사정으로 참조하는 것이 가능합니다
;-------------------------------------------------
;관련 변수…
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;
;관련 함수…
;TRUECHECK 실로 되는 것이 몇개 있을까를 돌려준다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 절정 한 직후
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TRUECHECK(NOWEX:C절정, NOWEX:V절정, NOWEX:A절정, NOWEX:B절정) > 0
	;=================================================
	;조교자 사정
	;=================================================
	IF 구상기입체크(0) && NOWEX:사정
		PRINTFORMW 
	ENDIF
	
	;=================================================
	;★삼중 절정 이상
	;=================================================
	IF 구상기입체크() && TRUECHECK(NOWEX:C절정, NOWEX:V절정, NOWEX:A절정, NOWEX:B절정) > 2
		;연속 삼중 절정(2회째만)
		IF VAR_K13:12 == 1
			SELECTCASE RAND:2
				CASE 0
					PRINTFORML 「냐, 냐웃, 냐우우우읏……??」
					PRINTFORML 연속의 절정에,%CALLNAME%는 완전히 얼굴을 느슨해지게 해 달콤한 소리를 내 떨고 있다
					PRINTFORMW 「보, 보지 말아요…… 저, 절대 얼간이인 얼굴이 되어 버리고 있기 때문에……?」
				CASE 1
					PRINTFORML 「하힛, 하히이……??」
					PRINTFORML 「이, 가는 것 멈추지 않아……?   이것, 굉장해……??」
					PRINTFORMW 격렬한 절정의 연속으로,%CALLNAME%는 완전히 쾌락의 포로가 된 것처럼 달콤한 소리를 높이고 있다
			ENDSELECT
			RETURN 1
		ELSE
			SELECTCASE RAND:3
				CASE 0
					PRINTFORMW 「아, 히이, 응……?   조금 의식 톤이었다……?」
				CASE 1
					PRINTFORMW 「아, 후아아……?   개, 허리가…… 빠져 버린다아……??」
				CASE 2
					PRINTFORMW 「아후웃……?   있고, 가득 가 버렸다아……?」
			ENDSELECT
		ENDIF
		VAR_K13:13 = 1
		;연속 삼중 절정을 카운트
		VAR_K13:12 ++

	;=================================================
	;그 외·조건 없음
	;=================================================
	;자위절정
	ELSEIF VAR_K13:17 == 2
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「우후후…… 오나니하면서 가 버렸어?」
				PRINTFORMW 「내가 가는 곳, 분명하게 보고 있어 주었네요?」
			CASE 1
				PRINTFORMW 「아핫……?   역시 자위는 기분이 좋네요?」
				PRINTFORMW 「%FUNC_K13_CALLNAME()%도, 기분 좋아지고 싶어?   후후?」
		ENDSELECT
	ELSE
		SELECTCASE RAND:3
			CASE 0
				PRINTFORMW 「하아……?   기분 좋았다?」
			CASE 1
				PRINTFORMW 「후아……?   아직 가고 있는?」
			CASE 2
				PRINTFORMW 「앗, 굉장해?   체가 움찔움찔 해 버려……?」
		ENDSELECT
		VAR_K13:13 = 1
		VAR_K13:12 = 0
	ENDIF
ELSE
	VAR_K13:12 = 0
ENDIF

;─
;벌FIRSTEVENT(20)～
;─
;·TFLAG:벌플래그가 서 있는 경우에 표시되는 개소
;·벌플래그가 서 있어도, 가학계 이외의 ACT가 되는 경우가 있다
;-------------------------------------------------
;가학계 ACT, 파생하는 일에 의해 가학계에 포함될 가능성이 있는 이라마치오
;의상의변경을요구한다/기분나쁘게웃는다/애널바이브/후배위/애널섹스/자위/야외플레이/방뇨/다리빨고강제
;이상계 ACT/성교 봉사계 ACT
;상기의 ACT 이외는 TFLAG:벌플래그에 의해 ACT_ABLE로 규제된다(0616판 시점)
;-------------------------------------------------
;-------------------------------------------------
;·1회의 조교로 1회만 표시된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;벌개시 
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:벌플래그 && ONCEEVENT(20)
	;=================================================
	;전회 ACT가 벌선언
	;=================================================
	IF 구상기입체크(0) && IS_PREVACTNAME("벌이라고선언한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;벌개시 이외를 처리하는 개소
;━━━━━━━━━━━━━━━━━━━━━━━━
;벌중
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:벌플래그
	;=================================================
	;전회 ACT가 벌선언 한편 이번 ACT가 가학계
	;=================================================
	IF 구상기입체크(0) && MENUMATCH(TFLAG:ACT, "가학") && IS_PREVACTNAME("벌이라고선언한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전회 ACT가 벌선언
	;=================================================
	IF 구상기입체크(0) && IS_PREVACTNAME("벌이라고선언한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;질내, 장내 사정 직후 FIRSTEVENT(30)～
;─
;NOWEX:사정내지 NOWEX:MASTER:사정으로 사정종의 참조도 가능했다거나 합니다만
;EVENT20, 절정 구상에 동종의 분기와 입을 가능성도 있습니다
;-------------------------------------------------
;관련 변수…
;
;TFLAG:전회질내사정(1=V성교/2=V성교 봉사/3=V성교(하늘 쏘아 맞히고)/4=V성교 봉사(하늘 쏘아 맞히고)/5=A성교/6=A성교 봉사/7=A성교(하늘 쏘아 맞히고)/8=A성교 봉사(하늘 쏘아 맞히고)
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정 직후(조교 대상→조교자)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && GROUPMATCH(TFLAG:전회질내사정, 2, 4)
	;=================================================
	;뽑지않고의 N발목
	;=================================================

	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크() && TFLAG:전회질내사정 == 4
		;뽑지않고의 N발목
		IF TFLAG:뽑지않고
			IF VAR_K13:13
				PRINTL 
				PRINTFORML 「………… 조금, 정액 나와 있지 않잖아!」
				PRINTFORML 「모처럼 질내사정으로, 좀 더 기분 좋게 갈 수 있다고 생각했는데…… 이제(벌써)!」
			ENDIF
			PRINTFORML 「이봐요 이봐요!   정액을 낼 수 없으면, 그 만큼 정기를 많이 빨아 올려 버려요?」
			PRINTFORML %CALLNAME%는 느슨느슨 허리를 흔들어, 하늘 공격해 움찔움찔 경련한다%CALLNAME:MASTER%의 페니스를 조른다
			PRINTFORML %CALLNAME:MASTER%는 강렬한 쾌감과 동시에, 전신의 힘이%CALLNAME%의 질로 들이마셔져 가는 것을 느꼈다
			PRINTFORMW 「우후후…… 너무 만약 뽑아 죽여 버리면, 그 때는 미안해요?」
		ELSE
			SELECTCASE RAND:3
				CASE 0
					CALL K13_ORGASM_BRANK()
					PRINTFORML 「아라, 이제(벌써) 나오지 않는거야?   그렇지 않지요?」
					PRINTFORML 「도와 주기 때문에, 사양하지 않고 흰 것 가득 내도 좋아요?」
					PRINTFORML %CALLNAME%는 정액을 짜내는 것 같은 움직임으로 가차 없이허리를 비튼다
					PRINTFORML 간 직후의 음경에 강렬한 쾌락 자극이 주어져%CALLNAME%는 비명을 올려 번민 미쳤다
					PRINTFORMW 「이만큼 해도 나오지 않으면, 정말로 텅텅같다?   킥킥……?」
				CASE 1
					CALL K13_ORGASM_BRANK()
					PRINTFORML 「이제(벌써) 최후야?   사용할 수 없는 애완동물……」
					PRINTFORMW 「주인님에 내라고 말해지면, 무리여도 내는 것이 애완동물의 근무겠지?」
				CASEELSE
					CALL K13_ORGASM_BRANK()
					PRINTFORML 「모처럼 주인님이 보지 빌려 주었는데 최후는, 어떤 생각일까?」
					PRINTFORML %CALLNAME%는, 완전히 쇠약해지고 잘라 질로부터 빠진 페니스에 손을 뻗으면, 강한 힘으로 꾸욱 잡았다
					PRINTFORML 하늘 공격해 직후의 민감 페니스에 강렬한 아픔과 쾌감이 달려,%CALLNAME:MASTER%는 주위의 시선에도 아랑곳 없는 비명을 올렸다
					PRINTFORMW 「저기?   정신나가지 말고, 조금은 미안한 것 같이 하면 어때?」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크()
		;조루사정
		IF NOWEX:MASTER:사정 == 4
			CALL K13_ORGASM_BRANK()
			IF RAND:2
				PRINTFORML 「완전히, 누가 내도 좋다고 말했을까?」
				IF TFLAG:뽑지않고
					PRINTFORMW 「벌로서 이대로 계속하게 해 받아요. 이봐요, 빨리 자지 단단하게 하세요!」
				ELSE
					PRINTFORMW 「이것은 예의 범절이 필요한 것 같구나?」
				ENDIF
			ELSE
				PRINTFORML 「이렇게 빨리 내 버리다니 사용할 수 없는 조루 자지군요」
				IF TFLAG:뽑지않고
					PRINTFORMW 「벌로서 이대로 계속하게 해 받아요. 이봐요, 빨리 자지 단단하게 하세요!」
				ELSE
					PRINTFORMW 「이것은 예의 범절이 필요할까?」
				ENDIF
			ENDIF
		ELSEIF TFLAG:뽑지않고 >= 3 && RAND:3 == 0
			CALL K13_ORGASM_BRANK()
			PRINTFORML 「하앙……?   이것으로 몇 회째던가?」
			IF AFFECTION("조교 완료")
				PRINTFORML 「뭐 좋아. 한계까지 정령을 짜내 주어?」
				PRINTFORMW 「%FUNC_K13_CALLNAME()%도, 그 쪽이 기쁘지요?」
			ELSE
				PRINTFORMW 「뭐 좋아. 한계까지 정령을 짜내 주어?」
			ENDIF
		;뽑지않고의 N발목
		ELSEIF TFLAG:뽑지않고
			IF RAND:2
				CALL K13_ORGASM_BRANK()
				PRINTFORML 「후후, 맛있었어요? 　%FUNC_K13_CALLNAME()%의 정기?」
				PRINTFORMW 「…… 그러면, 이대로 다시 한번 부탁이군요?」
			ELSE
				CALL K13_ORGASM_BRANK()
				PRINTFORML 「아핫……%FUNC_K13_CALLNAME()%의 정령이 흘러들어 와, 기분이 좋은?」
				PRINTFORMW 「좀 더 갖고 싶어져 버렸다. 물론, 아직 할 수 있네요?   후후……?」
			ENDIF
		ELSE
			IF RAND:2
				CALL K13_ORGASM_BRANK()
				PRINTFORML 「아는?   가득 내 버렸군요?」
				PRINTFORMW 「이봐요…… 안으로부터 이렇게 흘러넘쳐 와 버리고 있어?」
			ELSE
				CALL K13_ORGASM_BRANK()
				PRINTFORML 「후후……? 　%FUNC_K13_CALLNAME()%의 정령, 만끽되어 받았어요」
				PRINTFORMW 「%FUNC_K13_CALLNAME()%도, 주인님의 안에 사정할 수 있어 기분 좋았던 것이겠지?」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내 사정 직후(조교 대상→조교자)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && GROUPMATCH(TFLAG:전회질내사정, 6, 8)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크(0) && TFLAG:전회질내사정 == 8
		PRINTFORMW 
		RETURN 1
	ENDIF
		
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정 직후(조교자→조교 대상)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && GROUPMATCH(TFLAG:전회질내사정, 1, 3)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크(0) && TFLAG:전회질내사정 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내 사정 직후(조교자→조교 대상)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && GROUPMATCH(TFLAG:전회질내사정, 5, 7)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크(0) && TFLAG:전회질내사정 == 7
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;성교중·성교봉사중 FIRSTEVENT(40)～
;─
;관련 변수…
;TEQUIP:성교중(조교자가 조교 대상으로 삽입중) (1=정상위/2=후배위/3=승마위/4=대면좌위/5=배면좌위/6=애널섹스)
;TEQUIP:성교봉사중(조교 대상이 조교자에게 삽입중) 내용은 상기의 것과 같음
;
;관련 함수…
;V_SEX (=TARGET, TEQUIP:성교중 < 6/=MASTER, TEQUIP:성교봉사중 < 6)와 동의
;A_SEX (=TARGET, TEQUIP:성교중 == 6/MASTER, TEQUIP:성교봉사중 == 6)와 동의
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;성교중(조교자→조교 대상)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TEQUIP:성교중
	;=================================================
	;페니스밴드 사용
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(!PENIS(TARGET), TEQUIP:조교자페니스밴드) > 0
		;애널섹스 안
		IF A_SEX(TARGET)
			PRINTFORMW 
		;그 외의 성교계
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;애널섹스(A성교)
	;=================================================
	IF 구상기입체크(0) && A_SEX(TARGET)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외의 성교계(V성교)
	;=================================================
	IF 구상기입체크(0) && V_SEX(TARGET)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교봉사중(조교 대상→조교자)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TEQUIP:성교봉사중
	;=================================================
	;페니스밴드 사용
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(!PENIS(MASTER), TEQUIP:페니스밴드) > 0
		;애널섹스 안
		IF A_SEX(MASTER)
			PRINTFORMW 
		;그 외 성교 봉사계
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;애널섹스(A성교 봉사)
	;=================================================
	IF 구상기입체크(0) && A_SEX(MASTER)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외 성교 봉사계(V성교 봉사)
	;=================================================
	IF 구상기입체크(0) && V_SEX(MASTER)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 절정 한 직후
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
;=================================================
;사정
;=================================================
IF 구상기입체크() && NOWEX:MASTER:사정
	;드라이사정
	IF NOWEX:MASTER:사정 == 1
		IF RAND:2
			CALL K13_ORGASM_BRANK()
			PRINTFORML 「아라?   지금, 갔다고 생각했지만, 기분탓이었을까?」
			PRINTFORMW 「분명하게 사정할 수 있을 때까지 괴롭혀 주지 않으면?   키득……」
		ELSE
			CALL K13_ORGASM_BRANK()
			PRINTFORML 「아는?   움찔움찔체 진동시켜 버려, 사랑스럽네요」
			PRINTFORMW 「그러면, 빨리 세우세요?   주인님을 만족시키지 않고 끝나다니 허락하지 않아요?」
		ENDIF
	;사정
	ELSEIF NOWEX:MASTER:사정 == 2
		IF RAND:2
			CALL K13_ORGASM_BRANK()
			PRINTFORML 「후후…… 기분 좋았어?」
			PRINTFORMW 「%FUNC_K13_CALLNAME()%의 정령, 변함 없이 맛있네요?」
		ELSE
			CALL K13_ORGASM_BRANK()
			PRINTFORML 「후후, 사랑스러운 가는 얼굴이었어요?   만끽되어 받았어요?」
			PRINTFORMW 「다음은 어떻게 예의범절을 가르쳐 줄까?」
		ENDIF
	;대량사정
	ELSEIF NOWEX:MASTER:사정 == 3
		IF RAND:2
			CALL K13_ORGASM_BRANK()
			PRINTFORML 「후후…… 상당히 많이 냈군요?」
			PRINTFORMW 「가득 내는 것은 상관없지만, 간단하게 고정시키가 되면 허락하지 않아요?」
		ELSE
			CALL K13_ORGASM_BRANK()
			PRINTFORML 「이렇게 가득 내 버려…… 마치 진정한 흘림이군요」
			PRINTFORMW 「주인님에 조교되는 것이 그렇게 기뻤어?   후후, 사랑스러운 애완동물이예요?」
		ENDIF
	;사정(조루)
	ELSEIF NOWEX:MASTER:사정 == 4
		CALL K13_ORGASM_BRANK()
		PRINTFORML 「완전히…… 누가 내도 좋다고 말했을까?」
		PRINTFORMW 「인내력이 없는 애완동물에게는, 좀 더 어려운 조교가 필요하구나」
	;A책사정
	ELSEIF NOWEX:MASTER:사정 == 5
		IF RAND:2
			CALL K13_ORGASM_BRANK()
			PRINTFORML 「우후후, 엉덩이 실금, 매우 기분 좋음 그랬어?」
			PRINTFORMW 「그렇다 치더라도 엉덩이만으로 내 버리다니 마개의 느슨한 자지군요」
		ELSE
			CALL K13_ORGASM_BRANK()
			PRINTFORML 「엉덩이를 꾸짖을 수 있었을 뿐으로 내 버리다니%FUNC_K13_SEXSELECT("수컷으로서는 완전하게 실격이군요", "부끄러워서 밖 걸을 수 없네요♪")%」
			PRINTFORMW 「이런 덜 떨어짐의%FUNC_K13_SEXSELECT("수컷", "자")%는, 고양이에 길러지고 있는 것이 조화예요♪」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;=================================================
;사정을 수반하지 않는 절정
;=================================================
IF 구상기입체크() && TRUECHECK(NOWEX:MASTER:C절정, NOWEX:MASTER:V절정, NOWEX:MASTER:A절정, NOWEX:MASTER:B절정) > 0
	;각 부위의 절정이 1개이상의 경우에 실로 되는 개소
	;=================================================
	;다중절정
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(NOWEX:MASTER:C절정, NOWEX:MASTER:V절정, NOWEX:MASTER:A절정, NOWEX:MASTER:B절정) > 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		SELECTCASE RAND:3
			CASE 0
				CALL K13_ORGASM_BRANK()
				PRINTFORML 「후후…… 움찔움찔 해 버려, 사랑스럽네요」
				PRINTFORMW 「그 상태로, 주인님을 가득 즐겁게 하세요?」
			CASE 1
				CALL K13_ORGASM_BRANK()
				PRINTFORML 「후후, 사랑스러운 가는 얼굴이었어요?   만끽되어 받았어요?」
				PRINTFORMW 「다음은 어떻게 예의범절을 가르쳐 줄까?」
			CASE 2
				CALL K13_ORGASM_BRANK()
				PRINTFORML 「팥고물? 　%FUNC_K13_CALLNAME()%의 정기, 맛있어?」
				PRINTFORMW 「후후…… 나, 한 그릇 더를 갖고 싶어져 와 버렸군……」
		ENDSELECt
		RETURN 1
	ENDIF
ENDIF

;─
;조교 방침 변경 FIRSTEVENT(60)
;─
;·조교 방침이 바뀌었을 때에 표시한다
;·어디까지나 방침인 것으로 행동을 세세하게 결정하는 것은 아니다
;-------------------------------------------------
;관련 변수…
;TFLAG:턴방침		이 턴의 조교 방침(1=휴게/2=소프트/3=노멀/4=하드/5=이상)
;TFLAG:전턴방침		전턴의 조교 방침(내용은 턴방침과 같음)
;
;관련 함수…
;IS_POLICY	조교 방침 명칭과 합치하면 진을 돌려준다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 방침 변경
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:턴방침 != TFLAG:전턴방침
	;=================================================
	;턴방침 1:휴게(확실히 체력 회복 행동이 된다)
	;=================================================
	IF 구상기입체크(0) && IS_POLICY("지금", "휴게")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 2:소프트
	;=================================================
	IF 구상기입체크(0) && IS_POLICY("지금", "소프트")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 3:노멀
	;=================================================
	IF 구상기입체크(0) && IS_POLICY("지금", "노멀")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 4:하드
	;=================================================
	IF 구상기입체크(0) && IS_POLICY("지금", "하드")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 5:이상
	;=================================================
	IF 구상기입체크(0) && IS_POLICY("지금", "이상")
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;조건 없음 FIRSTEVENT(70)～
;─
;·여기까지의 조건에 히트 하지 않았던 경우에 표시되는 개소
;·BASE로 소분기를 마련해 있습니다만, 임의의 조건을 추가하는 일도 할 수 있습니다
;------------------------------------------------
;관련 변수…
;BASE		(0, 체력/1, 기력/2, 사정/3, 모유/4, 소변욕/5, 이성/6, 흥미/7, 초조해/8, 만족)
;
;관련 함수…
;TENSION	인수로 지정한 BASE치를 나타낸다(50%이상은 높은,25%이상은 일반,25% 미만은 낮다)
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조건 없음
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;만족 50％이상
	;=================================================
	IF 구상기입체크(0) && TENSION(TARGET, 8) == "높다"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;이성 25％이하
	;=================================================
	IF 구상기입체크(0) && TENSION(TARGET, 5) == "낮다"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;흥미 25％이하
	;=================================================
	IF 구상기입체크(0) && TENSION(TARGET, 6) == "낮다"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;초조 50％이상
	;=================================================
	IF 구상기입체크(0) && TENSION(TARGET, 7) == "높다"
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

@K13_ORGASM_BRANK(ARGS:0 = "…………")
IF VAR_K13:13
	PRINTL 
	PRINTFORML 「%ARGS:0%」
ENDIF
