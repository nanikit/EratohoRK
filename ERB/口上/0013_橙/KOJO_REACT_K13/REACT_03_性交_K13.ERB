;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	첸구상 성교계 개별 리액션 구상 파일
;
;	성교계 리액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;성교계 REACT30～35
;========================================================================
;· 각 REACT내의 나누는 방법으로서 TFLAG:REACT분류(IS_COMGRONAME)나 SELECTCOM(IS_NOWCOMNAME)가 생각됩니다
;·다만, 범위의 넓은 조건으로 주워 버리는 것은 기본적으로 추천하지 않습니다
;·ACT분류:성교계의 경우, 기본적으로 애무계 COM(COM10~COM16), 성교계 COM(COM20~COM22), 고통계 COM(COM30~COM33)가 선택지가 됩니다만
;·상황에 따라서는 그 외의 것이 선택지에 포함될 가능성도 있습니다
;·리액션(REACT)으로 처리되지 않았던 것은 범용 리액션(COM)으로 보완되므로
;·무리하게 모두 묻을 필요는 없습니다
;·필요라고 생각하는 것을 좁혀, COM와의 제휴를 생각해 효율 좋게 구축해야 합니다
;-------------------------------------------------
;· 각 REACT내는,&& IS_NOWCOMNAME든지,&& IS_COMGRONAME등의 조건으로 결정하고 쳐 합시다
;·여기에 조건 없음 분기가 있으면, 리액션에 관련되지 않고, 모두 주워 버려 범용 리액션에 흐르지 않습니다
;·조교 대상의 반응에 관련되지 않고, 같은 리액션 밖에 하지 않게 되어 버립니다
;·요컨데 이것은, 상정된 시스템의 파괴에 연결됩니다
;-------------------------------------------------
;  REACT의 지문은 ACT_MESSAGE.ERB의 TRAIN_MESSAGE2_{TFLAG:ACT}로 처리됩니다
;  기본적으로 지문은 TFLAG:REACT분류(받아들인다/적극적 따른다/태연하게 따른다/용서를빈다/소극적 따른다)로 분기 해,
;  각 REACT분류내에서 TFLAG:REACT부하(자연스러운 행동/무리가 없는 행동/무리를 한 행동)으로 파생
;  고유의 TRAIN_MESSAGE2가 없는 경우는, REACTION_MESSAGE로 COM마다 범용 처리됩니다
;  ACT_MESSAGE.ERB REACTION_MESSAGE.ERB를 참조하면서의 작업을 추천합니다
;-------------------------------------------------
;각 REACT에서는 상정되는 반응을 망라되어 있습니다. 필요에 따라서 분기를 증감해 사용해 주세요
;COM에 따라서는 REACT파생로 REACT분류가 변화하는 일에 주의
;각 COM의 파생은 기본적으로 REACT파생 1를 조건 결정 우리 소분기로 해, REACT파생 0는 그 외 취급으로 조건 없다고 보고 있습니다
;수고를 걸고 싶지 않은 경우는 그 외만을 써, 파생에 관련되지 않고 해당 개소를 보완할 수가 있습니다
;또 IF 구상 기입 체크(0) && TFLAG:REACT파생 < 1으로 하는 일로 파생했을 경우는 범용 리액션에 흘린다 따위라고 하는 일도 가능합니다
;파생은 장래 증가할 가능성도 있으므로 != 1등으로 하는 것보다는< 1으로 하는 것이 무난하다라고 생각합니다
;-------------------------------------------------
;성교계의 ACT파생는 ACT 개시시의 시추에이션의 차이를 나타내는 것이어
;내용이 크게 다른 것은 아니다. 그렇다고 하는 것으로 REACT에 있어서는 ACT파생로 나누지 않을 방침
;라고는 해도, 파생으로 나누어 보는 것도 시도로서는 재미있을지도 모릅니다
;-------------------------------------------------

;─
;REACT30, 정상위·개별 리액션
;─
;·TFLAG:ACT파생(0=통상/1=연속 실행/2=다른 V성교로부터/3=A성교로부터/4=V성교 봉사로부터/5=A성교 봉사로부터)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:더하드하게
;애무계 반응
;	COM:싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;성교계 반응
;	COM:허리를흔드는/안에내！/안에내지마！
;고통계 반응
;	COM:날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_30
;-------------------------------------------------
;·지문은 REACT분류(받아들인다/적극적 따른다. 태연하게 따른다/용서를빈다/소극적 따른다)로 분기
;·캔슬 처리는 ACT_MESSAGE_OTHERS에서 범용 처리
;·주도권은 조교자(SEX_INITIATIVE(TARGET))
;-------------------------------------------------

;MASTER가 처녀라면, 처녀상실이 일어난다. 이 처리는@ACTION_APPLY2_30에서 행해진다
;REACT분류가날뛴다로 캔슬 처리가 불려 갔을 때는 처녀상실, 동정상실은 회피
;━━━━━━━━━━━━━━━━━━━━━━━━
;처녀상실/동정상실
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TRUECHECK(TCVAR:MASTER:처녀상실, TCVAR:동정상실) > 0
	;=================================================
	;조교 대상이 처녀상실＆조교자가 동정상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:처녀상실 && TCVAR:동정상실
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET) /TEQUIP:조교자페니스밴드 따위로 나누어 써도 좋을 것입니다
	;=================================================
	;조교 대상이 처녀상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:처녀상실
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 동정상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:동정상실
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더하드하게(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("더하드하게")
	;=================================================
	;「이 철면피의 돼지째!」(벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(조교 대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 1의 경우는 REACT분류:날뛴다취급
;-------------------------------------------------
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고매도한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("서투르다고매도한다")
	;=================================================
	;쾌감을 느끼면서(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「실제로 서투른 경우」(REACT분류:태연하게 따른다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다(REACT분류:태연하게 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다(소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 1의 경우는 REACT분류:용서를빈다취급
;-------------------------------------------------
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
	;=================================================
	;애원(REACT분류:용서를빈다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를흔드는(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
;주도권에 의해 REACT분류가 변화하는 일에 주의
;-------------------------------------------------
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를흔드는")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 안에내！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("안에내！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 안에내지마！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("안에내지마！")
	;=================================================
	;중에 낸다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「안에 내지 않는다」(REACT분류:용서를빈다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 아픔을참는다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("아픔을참는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「아픔에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 1. 파생 2의 경우는 REACT분류:소극적 따르는 취급
;REACT분류가날뛴다였을 경우는 성교는 캔슬이 된다
;-------------------------------------------------
IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;날뛰고 실패(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
		;줄구속에 의한 날뛰고 실패
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;제지에 의한 날뛰고 실패
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;수고를 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT30, 정상위

;─
;REACT31, 후배위·개별 리액션
;─
;·TFLAG:ACT파생(0=통상/1=연속 실행/2=다른 V성교로부터/3=A성교로부터/4=V성교 봉사로부터/5=A성교 봉사로부터)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:용서를빈다
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;성교계 반응
;	COM:허리를흔드는/안에내！/안에내지마！
;고통계 반응
;	COM:날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_31
;-------------------------------------------------
;·지문은 REACT분류(받아들인다/적극적 따른다, 태연하게 따른다/용서를빈다\소극적 따른다)로 분기
;·캔슬 처리는 ACT_MESSAGE_OTHERS에서 범용 처리
;·주도권은 조교자(SEX_INITIATIVE(TARGET))
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;처녀상실/동정상실
;━━━━━━━━━━━━━━━━━━━━━━━━
;MASTER가 처녀라면, 처녀상실이 일어난다. 이 처리는@ACTION_APPLY2_30에서 행해진다
;REACT분류가날뛴다로 캔슬 처리가 불려 갔을 때는 처녀상실, 동정상실은 회피
;-------------------------------------------------
IF 구상기입체크(0) && TRUECHECK(TCVAR:MASTER:처녀상실, TCVAR:동정상실) > 0
	;=================================================
	;조교 대상이 처녀상실＆조교자가 동정상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:처녀상실 && TCVAR:동정상실
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET) /TEQUIP:조교자페니스밴드 따위로 나누어 써도 좋을 것입니다
	;=================================================
	;조교 대상이 처녀상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:처녀상실
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 동정상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:동정상실
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다(REACT분류:용서를빈다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다")
	;파생 1의 경우는 REACT분류:없음
	;=================================================
	;의견을 내다니 좋은 담력이구나! (벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(조교 대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1의 경우는 REACT분류:날뛴다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고매도한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("서투르다고매도한다")
	;=================================================
	;쾌감을 느끼면서(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「실제로 서투른 경우」(REACT분류:태연하게 따른다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다(REACT분류:태연하게 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를빈다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다(소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
	;=================================================
	;애원(REACT분류:용서를빈다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가 변화하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를흔드는(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를흔드는")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 안에내！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("안에내！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 안에내지마！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("안에내지마！")
	;=================================================
	;중에 낸다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「안에 내지 않는다」(REACT분류:용서를빈다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 아픔을참는다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("아픔을참는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「아픔에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1. 파생 2의 경우는 REACT분류:소극적 따르는 취급
;REACT분류가날뛴다였을 경우는 성교는 캔슬이 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;날뛰고 실패(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
		;줄구속에 의한 날뛰고 실패
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;제지에 의한 날뛰고 실패
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;수고를 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT31, 후배위

;─
;REACT32, 승마위·개별 리액션
;─
;·TFLAG:ACT파생(0=통상/1=연속 실행/2=다른 V성교로부터/3=A성교로부터/4=V성교 봉사로부터/5=A성교 봉사로부터)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:싫어한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;성교계 반응
;	COM:허리를흔드는/안에내！/안에내지마！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;봉사계 반응
;	COM:거부한다/소극적한다/적극적한다/애정을담아준다/태연하게한다
;-------------------------------------------------
@KOJO_REACT_K13_32
;-------------------------------------------------
;·지문은 REACT분류(날뛴다, 도망친다/거부/받아들인다/적극적 따른다, 태연하게 따른다/용서를빈다/소극적 따른다)로 분기
;·캔슬 처리는 ACT_MESSAGE_OTHERS에서 범용 처리
;·다른 성교계와는 달라, 봉사계 반응(COM40 접수대)도 나오는 일에 주의
;·캔슬이 성립하는 것은 다른 성교계와 달리, REACT분류가날뛴다/거부의 경우이다
;·주도권은 조교 대상(SEX_INITIATIVE(MASTER))
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;처녀상실/동정상실
;━━━━━━━━━━━━━━━━━━━━━━━━
;MASTER가 처녀라면, 처녀상실이 일어난다. 이 처리는@ACTION_APPLY2_30에서 행해진다
;REACT분류가날뛴다로 캔슬 처리가 불려 갔을 때는 처녀상실, 동정상실은 회피
;-------------------------------------------------
IF 구상기입체크(0) && TRUECHECK(TCVAR:MASTER:처녀상실, TCVAR:동정상실) > 0
	;=================================================
	;조교 대상이 처녀상실＆조교자가 동정상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:처녀상실 && TCVAR:동정상실
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET) /TEQUIP:조교자페니스밴드 따위로 나누어 써도 좋을 것입니다
	;=================================================
	;조교 대상이 처녀상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:처녀상실
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 동정상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:동정상실
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;파생 1의 경우는 REACT분류:날뛴다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고매도한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("서투르다고매도한다")
	;=================================================
	;쾌감을 느끼면서(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「실제로 서투른 경우」(REACT분류:태연하게 따른다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다(REACT분류:태연하게 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를빈다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다(소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
	;=================================================
	;애원(REACT분류:용서를빈다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를흔드는(REACT분류:적극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT 승마위의 경우, REACT분류는 적극적 따르는이 된다
;-------------------------------------------------
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를흔드는")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 안에내！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("안에내！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 안에내지마！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("안에내지마！")
	;=================================================
	;중에 낸다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「안에 내지 않는다」(REACT분류:용서를빈다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 아픔을참는다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("아픔을참는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「아픔에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1. 파생 2의 경우는 REACT분류:소극적 따르는 취급
;REACT분류가날뛴다였을 경우는 성교는 캔슬이 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;날뛰고 실패(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
		;줄구속에 의한 날뛰고 실패
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;제지에 의한 날뛰고 실패
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;수고를 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM40, 거부한다(REACT분류:거부)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("거부한다")
	PRINTFORMW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적한다(REACT분류:적극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("적극적한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을담아준다(REACT분류:적극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("애정을담아준다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게한다(REACT분류:태연하게 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게한다")
	PRINTFORMW 
	RETURN 1
ENDIF
;REACT32, 승마위

;─
;REACT33, 대면좌위·개별 리액션
;─
;·TFLAG:ACT파생(0=통상/1=연속 실행/2=다른 V성교로부터/3=A성교로부터/4=V성교 봉사로부터/5=A성교 봉사로부터)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;성교계 반응
;	COM:허리를흔드는/안에내！/안에내지마！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_33
;-------------------------------------------------
;·지문은 REACT분류(받아들인다/적극적 따른다. 태연하게 따른다/용서를빈다/소극적 따른다)로 분기
;·캔슬 처리는 ACT_MESSAGE_OTHERS에서 범용 처리
;·주도권은 조교자(SEX_INITIATIVE(TARGET))
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;처녀상실/동정상실
;━━━━━━━━━━━━━━━━━━━━━━━━
;MASTER가 처녀라면, 처녀상실이 일어난다. 이 처리는@ACTION_APPLY2_30에서 행해진다
;REACT분류가날뛴다로 캔슬 처리가 불려 갔을 때는 처녀상실, 동정상실은 회피
;-------------------------------------------------
IF 구상기입체크(0) && TRUECHECK(TCVAR:MASTER:처녀상실, TCVAR:동정상실) > 0
	;=================================================
	;조교 대상이 처녀상실＆조교자가 동정상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:처녀상실 && TCVAR:동정상실
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET) /TEQUIP:조교자페니스밴드 따위로 나누어 써도 좋을 것입니다
	;=================================================
	;조교 대상이 처녀상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:처녀상실
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 동정상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:동정상실
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;파생 1의 경우는 REACT분류:날뛴다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고매도한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("서투르다고매도한다")
	;=================================================
	;쾌감을 느끼면서(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「실제로 서투른 경우」(REACT분류:태연하게 따른다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다(REACT분류:태연하게 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를빈다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다(소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
	;=================================================
	;애원(REACT분류:용서를빈다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가 변화하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를흔드는(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를흔드는")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 안에내！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("안에내！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 안에내지마！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("안에내지마！")
	;=================================================
	;중에 낸다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「안에 내지 않는다」(REACT분류:용서를빈다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 아픔을참는다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("아픔을참는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「아픔에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1. 파생 2의 경우는 REACT분류:소극적 따르는 취급
;REACT분류가날뛴다였을 경우는 성교는 캔슬이 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;날뛰고 실패(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
		;줄구속에 의한 날뛰고 실패
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;제지에 의한 날뛰고 실패
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;수고를 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT33, 대면좌위

;─
;REACT34, 배면좌위·개별 리액션
;─
;·TFLAG:ACT파생(0=통상/1=연속 실행/2=다른 V성교로부터/3=A성교로부터/4=V성교 봉사로부터/5=A성교 봉사로부터)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:용서를빈다
;애무계 반응
;	COM:싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;성교계 반응
;	COM:허리를흔드는/안에내！/안에내지마！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_34
;-------------------------------------------------
;·지문은 REACT분류(받아들인다/적극적 따른다, 태연하게 따른다/용서를빈다/소극적 따른다)로 분기
;·캔슬 처리는 ACT_MESSAGE_OTHERS에서 범용 처리
;·주도권은 조교자(SEX_INITIATIVE(TARGET))
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;처녀상실/동정상실
;━━━━━━━━━━━━━━━━━━━━━━━━
;MASTER가 처녀라면, 처녀상실이 일어난다. 이 처리는@ACTION_APPLY2_30에서 행해진다
;REACT분류가날뛴다로 캔슬 처리가 불려 갔을 때는 처녀상실, 동정상실은 회피
;-------------------------------------------------
IF 구상기입체크(0) && TRUECHECK(TCVAR:MASTER:처녀상실, TCVAR:동정상실) > 0
	;=================================================
	;조교 대상이 처녀상실＆조교자가 동정상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:처녀상실 && TCVAR:동정상실
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET) /TEQUIP:조교자페니스밴드 따위로 나누어 써도 좋을 것입니다
	;=================================================
	;조교 대상이 처녀상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:처녀상실
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 동정상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:동정상실
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다(REACT분류:용서를빈다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다")
	;파생 1의 경우는 REACT분류:없음
	;=================================================
	;의견을 내다니 좋은 담력이구나! (벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(조교 대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1의 경우는 REACT분류:날뛴다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고매도한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("서투르다고매도한다")
	;=================================================
	;쾌감을 느끼면서(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「실제로 서투른 경우」(REACT분류:태연하게 따른다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다(REACT분류:태연하게 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를빈다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다(소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
	;=================================================
	;애원(REACT분류:용서를빈다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가 변화하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를흔드는(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를흔드는")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 안에내！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("안에내！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 안에내지마！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("안에내지마！")
	;=================================================
	;중에 낸다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「안에 내지 않는다」(REACT분류:용서를빈다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 아픔을참는다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("아픔을참는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「아픔에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1. 파생 2의 경우는 REACT분류:소극적 따르는 취급
;REACT분류가날뛴다였을 경우는 성교는 캔슬이 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;날뛰고 실패(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
		;줄구속에 의한 날뛰고 실패
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;제지에 의한 날뛰고 실패
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;수고를 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT34, 배면좌위

;─
;REACT35, 애널섹스·개별 리액션
;─
;·TFLAG:ACT파생(0=통상/1=연속 실행/3=V성교로부터/4=V성교 봉사로부터/5=A성교 봉사로부터)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:용서를빈다
;애무계 반응
;	COM:싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;성교계 반응
;	COM:허리를흔드는/안에내！/안에내지마！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_35
;-------------------------------------------------
;·지문은 REACT분류(받아들인다/적극적 따른다, 태연하게 따른다/용서를빈다/소극적 따른다)로 분기
;·캔슬 처리는 ACT_MESSAGE_OTHERS에서 범용 처리
;·주도권은 조교자(SEX_INITIATIVE(TARGET))
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;처녀상실/동정상실
;━━━━━━━━━━━━━━━━━━━━━━━━
;MASTER가 처녀라면, 처녀상실이 일어난다. 이 처리는@ACTION_APPLY2_30에서 행해진다
;REACT분류가날뛴다로 캔슬 처리가 불려 갔을 때는 처녀상실, 동정상실은 회피
;-------------------------------------------------
IF 구상기입체크(0) && TRUECHECK(TCVAR:MASTER:처녀상실, TCVAR:동정상실) > 0
	;=================================================
	;조교 대상이 처녀상실＆조교자가 동정상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:처녀상실 && TCVAR:동정상실
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET) /TEQUIP:조교자페니스밴드 따위로 나누어 써도 좋을 것입니다
	;=================================================
	;조교 대상이 처녀상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:처녀상실
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 동정상실
	;=================================================
	IF 구상기입체크(0) && TCVAR:동정상실
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다(REACT분류:용서를빈다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다")
	;파생 1의 경우는 REACT분류:없음
	;=================================================
	;의견을 내다니 좋은 담력이구나! (벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(조교 대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1의 경우는 REACT분류:날뛴다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고매도한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("서투르다고매도한다")
	;=================================================
	;쾌감을 느끼면서(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「실제로 서투른 경우」(REACT분류:태연하게 따른다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다(REACT분류:태연하게 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를빈다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다(소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
	;=================================================
	;애원(REACT분류:용서를빈다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
	PRINTFORMW 
	RETURN 1
ENDIF

;주도권에 의해 REACT분류가 변화하는 일에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를흔드는(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를흔드는")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 안에내！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("안에내！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 안에내지마！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("안에내지마！")
	;=================================================
	;중에 낸다(REACT분류:없음)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「안에 내지 않는다」(REACT분류:용서를빈다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 아픔을참는다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("아픔을참는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「아픔에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1. 파생 2의 경우는 REACT분류:소극적 따르는 취급
;REACT분류가날뛴다였을 경우는 성교는 캔슬이 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;날뛰고 실패(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
		;줄구속에 의한 날뛰고 실패
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;제지에 의한 날뛰고 실패
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;수고를 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT35, 애널섹스