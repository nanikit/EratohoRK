;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	첸구상 애무계 개별 리액션 구상 파일
;
;	애무계 리액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;애무계 REACT10～19 & REACT27
;========================================================================
;· 각 REACT내의 나누는 방법으로서 TFLAG:REACT분류(IS_COMGRONAME)나 SELECTCOM(IS_NOWCOMNAME)가 생각됩니다
;·다만, 범위의 넓은 조건으로 주워 버리는 것은 기본적으로 추천하지 않습니다
;·ACT분류:애무계의 경우, 기본적으로 애무계 COM(COM10~COM16)가 선택지가 됩니다만
;·상황에 따라서는 그 회화계, 고통계, 그 외의 것이 선택지에 포함될 가능성도 있습니다
;·리액션(REACT)으로 처리되지 않았던 것은 범용 리액션(COM)으로 보완되므로
;·무리하게 모두 묻을 필요는 없습니다
;·필요라고 생각하는 것을 좁혀, COM와의 제휴를 생각해 효율 좋게 구축해야 합니다
;-------------------------------------------------
;· 각 REACT 및 ACT파생내는,&& IS_NOWCOMNAME든지,&& IS_COMGRONAME등의 조건으로 결정하고 쳐 합시다
;·여기에 조건 없음 분기가 있으면, 리액션에 관련되지 않고, 모두 주워 버려 범용 리액션에 흐르지 않습니다
;·요컨데, 상정된 시스템의 파괴에 연결됩니다
;-------------------------------------------------
;  기본적으로 지문은 TFLAG:REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를빈다/소극적으로 한다)로 분기 해,
;  각 REACT분류내에서 TFLAG:REACT부하(자연스러운 행동/무리가 없는 행동/무리를 한 행동)으로 파생
;  ACT_MESSAGE.ERB를 참조하면서의 작업을 추천합니다
;-------------------------------------------------
;  우선 ACT파생 마다 테두리를 분리해 있습니다만, 1개에 모은 다음 범용적인 기술로 수고를 줄인다고 하는 방법도 있습니다
;  정직, 방식은 얼마든지 있으므로, 이하에 나타내는 방법은 하나의 예라고 생각해 주세요
;-------------------------------------------------

;────────────────────────────────────
;REACT10, 손으로애무·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=애무/1=자위/2=페니스밴드 자위)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:더하드하게
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_10
;-------------------------------------------------
;·ACTTION_APPLY2의 처리 없음
;·지문은 ACT파생 마다 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를빈다/소극적으로 한다)로 분기
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 애무
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0에 대해서는 가능성이 있는 COM를 대충 망라해 둡니다
;필요에 따라서 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("부끄러워한다")
		;쾌감에부끄러워한다
		IF TFLAG:REACT파생 == 1
			PRINTFORML 「느끼고 있는거죠?   라면 좀 더 솔직해지세요」
			PRINTFORMW 「분명하게 왈가닥 왈가닥 울어, 주인님을 즐겁게 하는거야」
		;상황에부끄러워한다
		ELSE
			PRINTFORML 「아라. 애완동물의 주제에, 일인분에 부끄러워해 버려」
			PRINTFORMW 「좀 더 바보씨가 될 수 있도록(듯이), 내가 책임을 가져 개발 해 주지 않으면」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
		;싫어해 방해했다			REACT분류"날뛴다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;싫어했다					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고매도한다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("서투르다고매도한다")
		;강한척 해					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("쾌감을받아들인다")
		IF RAND:2
			PRINTFORML 「후후, 보지 만지작거려진 것 뿐으로, 그렇게 야무지지 못한 얼굴 해 버려……」
			PRINTFORMW 「상당히 애완동물다워졌지 않아. 좀 더, 좋아할 뿐(만큼) 한심하게 울어도 좋은거야?」
		ELSE
			PRINTFORML 「아핫, 사랑스러운 소리♪  완전히 솔직해져 버렸군요」
			PRINTFORMW 「보지 만지작거려지는 것 기분 좋지요?   분명하게 주인님에 감사하는거야?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("기분좋다！")
		IF RAND:2
			PRINTFORML 「조금…… 그런 소리 나오면, 나까지 이상한 기분이 되지 않아」
			PRINTFORMW 「완전히, 건방진 애완동물. 좀 더 괴롭혀 줄까?」
		ELSE
			PRINTFORML 「우후후, 이제(벌써) 기분 좋은 일 이외, 아무것도 생각할 수 없다는 느낌이군요?」
			PRINTFORML 「…… 그렇지만, 주인님을 무시해 기분 좋아지는 것은 허락하지 않아요?」
			PRINTFORML %CALLNAME%는%CALLNAME:MASTER%의 클리토리스, 라고 강하게 꼬집었다
			PRINTFORML 강렬한 아픔과 쾌감에, 무심코%CALLNAME:MASTER%의 몸이 뛴다
			PRINTFORMW 「이봐요, 분명하게 나의 눈을 보세요?   갈 때는 나에게 감사하면서 가는거야?」
		ENDIF
		RETURN 1
	ENDIF
	
	;파생 1. 파생 2의 경우는 REACT분류:소극적 따르는 취급
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
		;-------------------------------------------------
		;날뛰고 실패(REACT분류:소극적으로 한다)
		;-------------------------------------------------
		IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
			[SKIPSTART]
			※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
			;줄구속에 의한 날뛰고 실패
			IF TFLAG:REACT파생 == 1
				PRINTFORMW 
			;제지에 의한 날뛰고 실패
			ELSE
				PRINTFORMW
			ENDIF
			RETURN 1
			[SKIPEND]
			
			;수고를 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 자위
;ACT파생 2, 페니스밴드 자위
;━━━━━━━━━━━━━━━━━━━━━━━━
;-------------------------------------------------
IF 구상기입체크() && GROUPMATCH(TFLAG:ACT파생, 1, 2)
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("부끄러워한다")
		;쾌감에부끄러워한다
		IF TFLAG:REACT파생 == 1
			PRINTFORML 「느끼고 있는거죠?   라면 좀 더 솔직해지세요」
			PRINTFORMW 「분명하게 앙앙 허덕여, 주인님을 즐겁게 하는거야」
		;상황에부끄러워한다
		ELSE
			PRINTFORML 「자지 만지작거려지는 것이, 그렇게 부끄러운거야?」
			PRINTFORMW 「애완동물의 주제에, 쓸데없게 프라이드가 높은거네. 건방지구나」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("싫어한다")
		;싫어해 방해했다			REACT분류"날뛴다"
		IF TFLAG:REACT파생 == 1
			PRINTFORML 「이봐!   얌전하게 하세요!!   건방지구나!」
			PRINTFORMW 「모처럼 주인님이 자지 훑어 준다 라고 하고 있는데, 무엇이 불만인 것이야!?」
		;싫어했다					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORML 「주인님이 일부러, 자지기분좋게해줘 하고 있는거야?」
			PRINTFORMW 「감사한다면 어쨌든, 실례인 태도라고 생각하지 않는 것일까?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고매도한다
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("서투르다고매도한다")
		;강한척 해					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
			PRINTFORML 「후응?   나의 손으로 훑어진 정도는, 반응하지 않는다고 말하는거야?」
			PRINTFORMW 「그것치고는, 나의 손안으로 커져 와 있어요. …… 어떻게 말하는 일일까?」
		;미숙한 테크닉에 대해서	REACT분류"태연하게 한다"
		ELSE
			PRINTFORML 「애완동물의 주제에, 하나 하나 번거롭네요!」
			PRINTFORMW 「너무 거만함 말하면, 이대로%FUNC_K13_CALLNAME()%남을 수 있는 것을 잡아요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORML 「아라?   모처럼 내가 손으로 훑어 주고 있는데, 건방진 태도군요」
		PRINTFORMW 「애완동물이라면, 솔직하게 느끼는 얼굴을 보여, 나를 즐겁게 하세요!」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		;IF TFLAG:REACT파생 == 1
		;파생:내구					REACT분류"소극적으로 한다"
		;ELSE
		;ENDIF
		IF RAND:2
			PRINTFORML 「이봐요, 솔직하게 기분 좋아져 버리세요」
			PRINTFORMW 「고집을 부려도, 좋은 일은 한 개도 없어?」
		ELSE
			PRINTFORML 「주인님이 일부러, 자지 훑어 주고 있는거야?」
			PRINTFORMW 「좀 더 기쁜 듯한 소리를 높이면 어때?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("쾌감을받아들인다")
		IF RAND:2
			PRINTFORML 「후후, 야무지지 못한 얼굴 해 버려……」
			PRINTFORMW 「이대로, 좀 더 쫄깃쫄깃 해 준다. 분명하게 주인님에 감사하는거야?」
		ELSE
			PRINTFORML 「후후, 나의 손안으로 자지가 사랑스럽게 움찔움찔 하고 있어요?」
			PRINTFORMW 「그대로 기분 좋아져도 좋으니까, 좀 더 나에게 느끼고 있는 얼굴을 보이세요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("기분좋다！")
		IF RAND:2
			PRINTFORML 「조금…… 그런 소리 나오면, 나까지 이상한 기분이 되지 않아」
			PRINTFORMW 「완전히, 건방진 애완동물. 좀 더 괴롭혀 줄까?」
		ELSE
			PRINTFORML 「우후후, 이제(벌써) 기분 좋은 일 이외, 아무것도 생각할 수 없다는 느낌이군요?」
			PRINTFORMW 「그렇지만, 주인님을 무시해 기분 좋아지면 안 돼요?   이봐요, 분명하게 나의 눈을 보세요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM32, 날뛴다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("날뛴다")
		;날뛰고 실패(REACT분류:소극적으로 한다)
		IF GROUPMATCH(TFLAG:REACT파생, 1, 2)
			PRINTFORML 「이봐!   저항한다니 허락하지 않아요!」
			PRINTFORMW 「%FUNC_K13_CALLNAME()%는 얌전하고, 자지 훑어져 히히말해 주세요!」
		;날뛰고 성공
		ELSE
			PRINTFORML 「이봐!   얌전하게 하세요!!   건방지구나!」
			PRINTFORMW 「모처럼 주인님이 자지 훑어 준다 라고 하고 있는데, 무엇이 불만인 것이야!?」
		ENDIF
		RETURN 1
	ENDIF
	
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT10, 손으로애무

;────────────────────────────────────
;REACT11, 가슴애무·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=가슴애무/1=유두 들이마셔/2=몸싸움/3=유두 고문)
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:더하드하게
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
;·지문은 ACT파생 2, 몸싸움과 그 외로 분기
;·ACTION_APPLY2에 있어서, ACT파생 2의 경우는 수용, 거부의 판정 처리 있어
;·ACT파생 2, 몸싸움에 대해 REACT분류(소극적으로 한다/태연하게 한다/적극적으로 한다/받아들인다) 이외는 "거부"취급
;-------------------------------------------------
;  파생마다 온전히 하면 수고가 심상치 않기 때문에, ACT파생분기의 상층에서 범용 처리하거나
;  각 ACT파생내의 COM 분기등은 일부의 것에 좁혀 버리는 것이 좋다고 생각합니다
;  또 ACT파생로 단락짓지 않고, 상당히기로 정리해 버리는 방법도 있습니다만, 공이 많이 든 일을 하기에는 적합하지 않다라고 생각합니다
;-------------------------------------------------
@KOJO_REACT_K13_11
;-------------------------------------------------
;정해진 사용 방법 따위는 없기 때문에, 이하에 나타내는 것은 어디까지나 하나의 예라고 하는 일로
;분기의 증감, COM에 흘리는지 아닌지의 판단은 작자의 판단에 맡깁니다
;-------------------------------------------------

;*************************************************
;COM 단체의 분기 ACT파생의 상층에서 처리
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더하드하게(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("더하드하게")
	;=================================================
	;「이 철면피의 돼지째!」(벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(조교 대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2는 거동이 다르므로 조건으로 연주한다
;-------------------------------------------------
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다") && TFLAG:ACT파생 !=2
	;=================================================
	;쾌감에 대해서부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1를 썼을 경우는 REACT파생 0, 상황에 대해서부끄러워한다취급
	;파생 1을 방치했을 경우는 여기서 보완해,&& TFLAG:REACT파생 < 1등으로 해서 COM에 흘려 버리는 것도 있어
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
	;ACT파생 2의 경우는 ACT파생 2 분기내의 IS_COMGRONAME("거부")로 처리
	;=================================================
	;싫어해 방해했다(REACT분류 6, 날뛴다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1 && TFLAG:ACT파생 !=2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1를 썼을 경우는 REACT파생 0, 싫어한(REACT분류 1, 소극적으로 한다) 취급
	;파생 1을 방치했을 경우는 여기서 보완해,&& TFLAG:REACT파생 < 1등으로 해서 COM에 흘려 버리는 것도 있어
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고매도한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("서투르다고매도한다")
	;=================================================
	;강한척 해(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1를 썼을 경우는 REACT파생 0, 미숙한 테크닉에 대해서(REACT분류:태연하게 한다) 취급
	;파생 1을 방치했을 경우는 여기서 보완해,&& TFLAG:REACT파생 < 1등으로 해서 COM에 흘려 버리는 것도 있어
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;*************************************************
;이하, ACT파생 마다의 분기
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 가슴애무
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 유두 들이마셔
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자에게 소질(유치/혀사용)이 없는 경우는 발생하는 확립은 낮다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 몸싸움
;━━━━━━━━━━━━━━━━━━━━━━━━
;봉사계 COM가 선택지가 되는 경우 있어
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;요구에 응하지 않았다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("거부")
		;부끄러워해
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
			PRINTFORMW 
		;싫어해
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;-----------------------------------------------------------------
	;이하, COM를 병렬화
	;수고를 싫어하지 않는 것이면, IF든지 3항이든지를 사용해 내부에서 나누어 쓰는 것도 있어
	;-----------------------------------------------------------------
	
	;=================================================
	;REACT분류 4, 받아들인다＆COM, 쾌감을받아들인다/기분좋다！
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다") && IS_NOWCOMNAME("쾌감을받아들인다/기분좋다！")
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	;=================================================
	;REACT분류 2, 태연하게 한다＆COM, 겁없게웃는다/태연하게한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("태연하게 한다") && IS_NOWCOMNAME("겁없게웃는다/태연하게한다")
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류 2, 적극적으로 한다＆COM, 적극적한다/애정을담아준다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적으로 한다") && IS_NOWCOMNAME("적극적한다/애정을담아준다")
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;COM 싫어해 방해/애원에 파생하면 COM에 흘린다
	;&& TFLAG:REACT파생 < 1 자체는 없어도 REACT분류의 결정 집에서 같은 결과를 얻을 수 있습니다만, 사양의 가시화라고 하는 일로
	;=================================================
	;REACT분류 2, 소극적으로 한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("소극적으로 한다") && IS_NOWCOMNAME("싫어한다/쾌감을참는다") && TFLAG:REACT파생 < 1
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 3, 유두 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자에게 소질(유치/혀사용)이 없는 경우는 발생하는 확립이 높다
;조교 대상이 남자의 경우는 여기에 밖에 떨어지지 않는다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 3
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT11, 가슴애무

;────────────────────────────────────
;REACT12, 입으로애무·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생(0=쿤닐링구스/1=페라)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:더하드하게
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_12
;-------------------------------------------------
;·지문은 ACT파생 마다 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를빈다/소극적으로 한다)로 분기
;·파생마다 단락지어 있습니다만, 예를 들면 ACT파생 1만 써 뒤는 범용에 던진다고 하는 일도 할 수 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 쿤닐링구스
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0에 대해서는 참고로서 가능성이 있는 COM를 망라해 둡니다
;필요에 따라서 분기를 증감, 없고 구조를 개변해 사용해 주세요
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;COM6, 더하드하게(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("더하드하게")
		;-------------------------------------------------
		;「이 철면피의 돼지째!」(벌포인트 가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(조교 대상의 의견을 묵살) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
		;쾌감에부끄러워한다
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;상황에부끄러워한다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
		;싫어해 방해했다			REACT분류"날뛴다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;싫어했다					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고매도한다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("서투르다고매도한다")
		;강한척 해					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 페라
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1는 파생 0으로 같은 종류의 것을 다른 형태로 제시해 보겠습니다
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
		;쾌감에부끄러워한다
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;상황에부끄러워한다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고매도한다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("서투르다고매도한다")
		;강한척 해					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;미숙한 테크닉에 대해서	REACT분류"태연하게 한다"
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;수고를 싫어하지 않는 것이면, IF든지 3항이든지를 사용해 내부에서 나누어 쓰는 것도 있어
	;=================================================
	;REACT분류 4, 받아들인다＆COM, 더하드하게/쾌감을받아들인다/기분좋다！
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다") && IS_NOWCOMNAME("더하드하게/쾌감을받아들인다/기분좋다！")
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류 2, 태연하게 한다＆COM, 겁없게웃는다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("태연하게 한다") && IS_NOWCOMNAME("겁없게웃는다")
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;COM가 방해/애원에 파생하면 COM에 흘린다
	;&& TFLAG:REACT파생 < 1 자체는 없어도 REACT분류의 결정 집에서 같은 결과를 얻을 수 있습니다만, 사양의 가시화라고 하는 일로
	;=================================================
	;REACT분류 2, 소극적으로 한다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("소극적으로 한다") && IS_NOWCOMNAME("싫어한다/쾌감을참는다") && TFLAG:REACT파생 < 1
		;소비 기력에 의한 파생
		SELECTCASE GET_COM_RANK()
			;자연스러운 행동
			CASE "자연스러운 행동"
				PRINTFORMW 
			;무리가 없는 행동
			CASE "무리가 없는 행동"
				PRINTFORMW 
			;무리를 한 행동
			CASE "무리를 한 행동"
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT12, 입으로애무

;────────────────────────────────────
;REACT13, 애널애무·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생（0=애널애무/1=전립선자극/2=애널바이브 출납）
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:용서를빈다
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_13
;-------------------------------------------------
;·지문은 ACT파생로 분기. ACT파생 1, 2 분기내의 파생 없음
;·ACT파생 0, 애널애무만 REACT분류(받아들인다/적극적으로 한다, 태연하게 한다/용서를빈다/소극적으로 한다)로 파생 처리된다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 애널애무
;ACT파생 1, 전립선자극(남자 한정)
;ACT파생 2, 애널바이브 출납(남자로 지문 분기)
;※우선 공통의 문장을 표시해, 일부 필요에 따라서 분기 시킬 방침으로
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0에 대해서는 가능성이 있는 COM를 망라해 둡니다
;필요에 따라서 분기를 증감, 없고 구조를 개변해 사용해 주세요
;-------------------------------------------------
IF 구상기입체크()
	;=================================================
	;COM7, 용서를빈다(REACT분류:용서를빈다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다")
		;파생 1의 경우는 REACT분류:없음
		;-------------------------------------------------
		;의견을 내다니 좋은 담력이구나! (벌포인트 가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(조교 대상의 의견을 묵살) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("부끄러워한다")
		;쾌감에부끄러워한다
		IF TFLAG:REACT파생 == 1
			PRINTFORML 「아라?   엉덩이로 느껴 버리는 것은 부끄러워?」
			PRINTFORMW 「후후. 지금에, 부끄럽다든가 생각할 여유도 없어질 정도로, 개발 해 준다♪」
		;상황에부끄러워한다
		ELSE
			PRINTFORML 「후후. 엉덩이를 만지작거려지는데는, 아직 저항이 있는 것 같구나?」
			PRINTFORMW 「그렇지만,%FUNC_K13_CALLNAME()%는 나의 애완동물인 것이니까, 내가 하는 것은 뭐든지 받아들이지 않으면 안 돼요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM11, 싫어한다
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("싫어한다")
		;싫어해 방해했다			REACT분류"날뛴다"
		IF TFLAG:REACT파생 == 1
			PRINTFORML 「좀, 날뛰지 말아요!   능숙하게 만지작거릴 수 없지요!?」
			IF PENIS(MASTER)
				PRINTFORMW 「너무 멋대로 말하고 있으면, 그 추접스러운 구슬을 쥐어뜯어요!!」
			ELSE
				PRINTFORMW 「너무 멋대로 말하고 있으면, 그 예쁜 엉덩이를 찢어요!!」
			ENDIF
		;싫어했다					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORML 「이봐요 이봐요, 날뛰지 않는거야!」
			PRINTFORMW 「서투르게 날뛸 수 있으면 손이 빗나가,%FUNC_K13_CALLNAME()%의 똥구멍, 피투성이가 되어 버려요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM12, 서투르다고매도한다
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("서투르다고매도한다")
		;강한척 해					REACT분류"소극적으로 한다"
		IF TFLAG:REACT파생 == 1
			PRINTFORML 「후응, 반항적이군요……」
			PRINTFORMW 「그것치고,%FUNC_K13_CALLNAME()%의 똥구멍, 큥큥 꼭 죄여 사랑스럽게 반응하고 있어요?」
		;미숙한 테크닉에 대해서	REACT분류"태연하게 한다"
		ELSE
			PRINTFORML 「그래, 나의 서투른 애무는 느껴지지 않는다고 말하는 거네?」
			PRINTFORMW 「…… 너무 건방진 말을 하고 있으면, 이대로%FUNC_K13_CALLNAME()%의 똥구멍, 갈기갈기 찢어요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM13, 겁없게웃는다(REACT분류"태연하게 한다")
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORML 「헤에…… 상당히 여유가 아니다」
		PRINTFORML 「그렇다면, 그런 여유도 없어질 정도로, 철저하게 개발해 좋아하게 해 주어요」
		PRINTFORMW 「기대해 주세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM14, 쾌감을참는다
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("쾌감을참는다")
		;파생:애원					REACT분류"용서를빈다"
		IF TFLAG:REACT파생 == 1
			PRINTFORML 「아라, 그렇게 엉덩이로 느끼는 것은 싫어?」
			PRINTFORMW 「주인님이, 엉덩이로 좋아하는 모습을 보여라라고 말하고 있는거야?   분명하게 솔직하게 따르세요!」
		;파생:내구					REACT분류"소극적으로 한다"
		ELSE
			PRINTFORML 「이봐요, 좀 더 힘을 빼세요!」
			PRINTFORMW 「너무 고집을 부려도,%FUNC_K13_CALLNAME()%가 아픈 생각을 할 뿐(만큼)이야?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORML 「엉덩이로 이렇게 느껴 버리다니 부끄러운 애완동물」
		PRINTFORML 「그렇지만, 나는%FUNC_K13_CALLNAME()%의 보기 흉한 모습, 좋아해요♪」
		PRINTFORMW 「후후…… 아직도, 좀 더, 부끄러운 아이가 되어요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("기분좋다！")
		PRINTFORML 「후후, 그렇게 기분이 좋은거야?」
		PRINTFORMW 「한심한 모습으로 엉덩이 만지작거려지고 있는데, 굉장히 행복한 얼굴 하고 있어요……?」
		RETURN 1
	ENDIF

	;=================================================
	;COM30, 아픔을참는다
	;=================================================
	IF 구상기입체크() && IS_NOWCOMNAME("아픔을참는다")
		PRINTFORML 「후후, 아직 아플까?」
		PRINTFORML 「안심하세요, 그 중 기분 좋아져 오기 때문에」
		PRINTFORMW 「…… 아마」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT13, 애널애무

;────────────────────────────────────
;REACT14, 애널핥기·개별 리액션
;────────────────────────────────────
;·ACT파생없음. ACTION_APPLY2의 처리 없음
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:용서를빈다
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_14
;-------------------------------------------------
;·지문은 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를빈다/소극적으로 한다)로 분기
;·ACT파생는 없기 때문에 COM 분기에서 완전하게 단락짓습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류"소극적으로 한다")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;쾌감에부끄러워한다
	IF TFLAG:REACT파생 == 1
		PRINTFORML 「푸핫……. 우후후, 좀 더 솔직하게 느껴 버리세요」
		PRINTFORMW 「엉덩이를 빨 수 있고 느껴 버리는 부끄러운 애완동물이라도, 나는 대환영이니까?」
	;상황에부끄러워한다
	ELSE
		PRINTFORML 「뭐?   부끄러운거야?   참아 주세요」
		PRINTFORMW 「나는 주인님인 것이니까,%FUNC_K13_CALLNAME()%의 부끄러운 곳의 냄새도 맛도, 전부 다 조사하지 않으면 안 되는거야♪」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
	;싫어해 방해했다			REACT분류"날뛴다"
	IF TFLAG:REACT파생 == 1
		PRINTFORMW 
	;싫어했다					REACT분류"소극적으로 한다"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고매도한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("서투르다고매도한다")
	;강한척 해					REACT분류"소극적으로 한다"
	IF TFLAG:REACT파생 == 1
		PRINTFORMW 
	;미숙한 테크닉에 대해서	REACT분류"태연하게 한다"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다(REACT분류"태연하게 한다")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
	;파생:애원					REACT분류"용서를빈다"
	IF TFLAG:REACT파생 == 1
		PRINTFORMW 
	;파생:내구					REACT분류"소극적으로 한다"
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다(REACT분류"받아들인다")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORML 「아읏…… 그렇게 좋은 것 같이 되면, 나도 기합이 들어가 버려요」
	IF RAND:2
		PRINTFORML 「이봐요, 좀 더 서비스 해 준다…… 날름, 날름,?」
		PRINTFORMW %CALLNAME%의 혀사용이 격렬해져,%CALLNAME:MASTER%는 넋을 잃을 것 같은 감각에 습격당했다
	ELSE
		PRINTFORML 「이봐요, 좀 더 좋아할 뿐(만큼)가는 좋은거야?   …… 응츗, 날름, 응붓,?」
		PRINTFORMW %CALLNAME%의 혀로 엉덩이 구멍의 안쪽까지 빨아져%CALLNAME:MASTER%는 소리가 되지 않는 소리를 높여 몸부림쳤다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！(REACT분류"받아들인다")
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
	PRINTFORML 「푸핫……?   후후, 완전히 엉덩이 꾸짖을 수 있는 것이 버릇이 되어 버렸군요?」
	IF RAND:2
		PRINTFORML 「이봐요, 좀 더 서비스 해 준다…… 날름, 날름,?」
		PRINTFORMW %CALLNAME%의 혀사용이 격렬해져,%CALLNAME:MASTER%는 넋을 잃을 것 같은 감각에 습격당했다
	ELSE
		PRINTFORML 「이봐요, 좀 더 좋아할 뿐(만큼)가는 좋은거야?   …… 응츗, 날름, 응붓,?」
		PRINTFORMW %CALLNAME%의 혀로 엉덩이 구멍의 안쪽까지 빨아져%CALLNAME:MASTER%는 소리가 되지 않는 소리를 높여 몸부림쳤다
	ENDIF
	RETURN 1
ENDIF
;REACT14, 애널핥기

;────────────────────────────────────
;REACT15, 키스·개별 리액션
;────────────────────────────────────
;·ACT파생없음. ACTION_APPLY2의 처리 없음
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:더하드하게
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;그 외 반응
;	COM:자위하기시작한다
;-------------------------------------------------
@KOJO_REACT_K13_15
;-------------------------------------------------
;·지문은 REACT분류(적극적으로 한다/받아들인다/태연하게 한다/용서를빈다/소극적으로 한다)로 분기
;·TEQUIP:성교중은 체위마다, TEQUIP:성교봉사중은 TCVAR:MASTER:애태움수로 파생한다
;·가능성이 있는 것을 대충 놓아둡니다. 필요에 따라서 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더하드하게(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("더하드하게")
	;=================================================
	;「이 철면피의 돼지째!」(벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(조교 대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1의 경우는 REACT분류:날뛴다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고매도한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	;=================================================
	;쾌감을 느끼면서(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「실제로 서투른 경우」(REACT분류:태연하게 한다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를빈다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
	;=================================================
	;애원(REACT분류:용서를빈다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
	PRINTFORMW 
	RETURN 1
ENDIF
;REACT15, 키스

;────────────────────────────────────
;REACT16, 성기를서로켠다·개별 리액션
;────────────────────────────────────
;·TFLAG:ACT파생（=0, 조개 우려라/=1, 가랑이/=2, 페니스밴드 가랑이）
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:더하드하게
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_16
;-------------------------------------------------
;·지문은 ACT파생 마다 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를빈다, 소극적으로 한다)로 분기
;·필요에 따라서 분기를 증감해 사용해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 조개 우려라
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;쾌감을참는다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌감을받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 가랑이
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;쾌감을참는다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌감을받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 페니스밴드 가랑이
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;쾌감을참는다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌감을받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT16, 성기를서로켠다

;────────────────────────────────────
;REACT17, 파이즈리한다·개별 리액션
;────────────────────────────────────
;·ACT파생없음
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:더하드하게
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_17
;-------------------------------------------------
;·지문은 COM:허리를흔드는, REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를빈다/소극적으로 한다)로 분기
;·애무계 COM를 망라. 필요에 따라서 분기를 증감해 사용해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더하드하게(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("더하드하게")
	;=================================================
	;「이 철면피의 돼지째!」(벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(조교 대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고매도한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	;=================================================
	;쾌감을 느끼면서(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「실제로 서투른 경우」(REACT분류:태연하게 한다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
	PRINTFORMW 
	RETURN 1
ENDIF

;애태움시에는 선택 불가
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM20, 허리를흔드는
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("허리를흔드는")
	;=================================================
	;갈것같다상태로 애태움 종료/애태움 없음
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:갈것같다 && TCVAR:MASTER:애태움수 < 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 아픔을참는다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("아픔을참는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「아픔에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1. 파생 2의 경우는 REACT분류:소극적 따르는 취급
;REACT분류가날뛴다였을 경우는 성교는 캔슬이 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;날뛰고 실패(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
		;줄구속에 의한 날뛰고 실패
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;제지에 의한 날뛰고 실패
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;수고를 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT17, 파이즈리한다

;────────────────────────────────────
;REACT18, 풋잡한다·개별 리액션
;────────────────────────────────────
;·ACT파생없음
;-------------------------------------------------
;상정되는 COM
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_18
;-------------------------------------------------
;·지문은 REACT분류(받아들인다/적극적으로 한다, 태연하게 한다/용서를빈다/소극적으로 한다)로 분기
;·필요에 따라서 분기를 증감해 사용해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1의 경우는 REACT분류:날뛴다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM12, 서투르다고매도한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	;=================================================
	;쾌감을 느끼면서(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「실제로 서투른 경우」(REACT분류:태연하게 한다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다(REACT분류:태연하게 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;파생 1의 경우는 REACT분류:용서를빈다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다(소극적으로 한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("쾌감을참는다")
	;=================================================
	;애원(REACT분류:용서를빈다)
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORML 「모처럼 주인님이 기분 좋게 해 주고 있는데, 상당히 실례인 태도군요?」
		PRINTFORMW 「너무 깔보는 태도를 취하고 있는 것 같으면, 이대로 차 잡아요?」
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORML 「무엇을 참고 있는거야」
		PRINTFORMW 「밟히고 느끼는 것이 분한거야?   그런 프라이드, 나의 애완동물에게는 필요없어요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("쾌감을받아들인다")
	IF RAND:2
		PRINTFORML 「밟혀 그런 소리 내다니 부끄러운 애완동물……」
		PRINTFORMW 「이봐요 이봐요, 좀 더 주인님의 아래에서 한심하고가는 보이세요!」
	ELSE
		PRINTFORML 「그렇게 다리로 되는 것이 기분이 좋은거야?」
		PRINTFORMW 「조금 전부터 얼굴이 느슨해지고 있을 뿐으로, 재미있게 되어 있어요?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("기분좋다！")
	IF RAND:2
		PRINTFORML 「이봐!   아무리 뭐라해도 기쁨 지났다!」
		PRINTFORML %CALLNAME%는 돌연 훨씬 다리에 거는 힘을 강하게 하면, 페니스를 찌부러뜨리는것 같이 강하게 밟아 붙여 왔다
		PRINTFORMW 「이래서야, 내가%FUNC_K13_CALLNAME()%에 봉사하고 있는 것 같지 않아!」
		PRINTFORMW 「어느 쪽이 주인님일까, 안 것이 아니에요!」
	ELSE
		PRINTFORML 「조금 밟힌 것 뿐으로 거기까지 좋아하다니 터무니 없는 변태 자지군요」
		PRINTFORMW 「완전히 이제(벌써)…… 나까지 느껴 와 버리지 않아?   어떻게 해 주는거야!」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORML 「아라?   혹시 짓밟아 부숴진다고 생각해 버렸어?   그런 일 하지 않아요♪」
		PRINTFORMW 「…… 정말로 짓밟아 부수면 어떤 반응해 줄까?」
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「아픔에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크()
		PRINTFORML 「아라?   아팠어?   미안해요」
		PRINTFORMW 「안심하세요. 분명하게, 아파도 기분 좋아질 수 있도록(듯이) 조교 해 주기 때문에♪」
		RETURN 1
	ENDIF
ENDIF

;파생 1. 파생 2의 경우는 REACT분류:소극적 따르는 취급
;REACT분류가날뛴다였을 경우는 성교는 캔슬이 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;날뛰고 실패(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
		;줄구속에 의한 날뛰고 실패
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;제지에 의한 날뛰고 실패
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;수고를 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다(REACT분류:용서를빈다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크() && TFLAG:REACT파생 == 1
		PRINTFORML 「모처럼 주인님이 다리로 기분 좋게 해 주고 있는데, 상당히 실례인 태도군요」
		PRINTFORMW 「너는 앙앙 허덕이고 있으면 좋은거야. 너무 나를 화나게 하는 것 같으면, 진심으로 짓밟아 부숴요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		PRINTFORML 「후후, 싫어하고 있는 것 치고는 기분 좋은 것 같구나」
		PRINTFORMW 「솔직하게 부탁해도 돼……『좀 더 밟아 주세요』는?」
		RETURN 1
	ENDIF
ENDIF

;REACT18, 풋잡한다

;────────────────────────────────────
;REACT19, 손가락삽입해라
;────────────────────────────────────
;·TFLAG:ACT파생(0=손가락삽입해라/1=Ｇ스팟 자극/2=바이브 출납)
;-------------------------------------------------
;상정되는 COM
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:날뛴다
;-------------------------------------------------
@KOJO_REACT_K13_19
;-------------------------------------------------
;지문은 ACT파생 0만 REACT분류로 분기
;그 외는 TALENT:MASTER:후타나리인지 아닌지로 파생할 뿐
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 손가락삽입해라
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 REACT분류(받아들인다/적극적으로 한다/태연하게 한다/용서를빈다, 소극적으로 한다)로 분기
;·애무계 COM를 망라. 필요에 따라서 분기를 증감해 사용해 주세요
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;COM10, 부끄러워한다(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
		;-------------------------------------------------
		;쾌감에부끄러워한다
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;파생 1의 경우는 REACT분류:날뛴다취급
	;=================================================
	;COM11, 싫어한다(REACT분류:소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
		;-------------------------------------------------
		;싫어해 방해했다(REACT분류:날뛴다)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM12, 서투르다고매도한다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
		;-------------------------------------------------
		;쾌감을 느끼면서(REACT분류:소극적으로 한다)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「실제로 서투른 경우」(REACT분류:태연하게 한다) 취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM13, 겁없게웃는다(REACT분류:태연하게 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("겁없게웃는다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1의 경우는 REACT분류:용서를빈다취급
	;=================================================
	;COM14, 쾌감을참는다(소극적으로 한다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
		;-------------------------------------------------
		;애원(REACT분류:용서를빈다)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM15, 쾌감을받아들인다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM16, 기분좋다！(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, Ｇ스팟 자극
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 조교 대상 캔버스 선반 리카 아닌가로 분기
;·REACT분류에 의한 분기는 없고, 내용은 범용적인 것
;-------------------------------------------------
;우선 최소한의 구성으로 해 둡니다. 필요에 따라서 분기를 추가해 주세요
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;질내로부터 전립선을 반죽해 돌려지는 감각에 %CALLNAME:MASTER%의 남성기는 팽창해,
	;소용돌이치는 것 같은 강렬한 사정감이 복받쳐 온다…
	;=================================================
	;조교 대상이 후타나리
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:후타나리
		;-------------------------------------------------
		;쾌감을받아들인다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;쾌감을참는다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
	
	;%CALLNAME:MASTER%는Ｇ스팟을 손가락으로 후벼파지는 감각에
	;머리카락을 흩뜨려 몸부림쳤다…
	;=================================================
	;조교 대상으로 페니스 없음
	;=================================================
	IF 구상기입체크(0) && !PENIS(MASTER)
		;-------------------------------------------------
		;쾌감을받아들인다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;쾌감을참는다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 바이브 출납
;━━━━━━━━━━━━━━━━━━━━━━━━
;·지문은 조교 대상 캔버스 선반 리카 아닌가로 분기
;·REACT분류에 의한 분기는 없고, 내용은 범용적인 것
;-------------------------------------------------
;우선 최소한의 구성으로 해 둡니다. 필요에 따라서 분기를 추가해 주세요
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;질내로부터 전립선을 반죽해 돌려지는 감각에 %CALLNAME:MASTER%의 남성기는 팽창해,
	;소용돌이치는 것 같은 강렬한 사정감이 복받쳐 온다…
	;=================================================
	;조교 대상이 후타나리
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:후타나리
		;-------------------------------------------------
		;쾌감을받아들인다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;쾌감을참는다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
	
	;%CALLNAME:MASTER%는Ｇ스팟을 바이브에 후벼파지는 감각에
	;머리카락을 흩뜨려 몸부림쳤다…
	;=================================================
	;조교 대상으로 페니스 없음
	;=================================================
	IF 구상기입체크(0) && !PENIS(MASTER)
		;-------------------------------------------------
		;쾌감을받아들인다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
			PRINTFORMW
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;쾌감을참는다
		;-------------------------------------------------
		IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
			PRINTFORMW
			RETURN 1
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;────────────────────────────────────
;REACT27, 쓰러트린다·개별 리액션
;────────────────────────────────────
;TFLAG:ACT파생（=0, 쓰러트린다/=1, 쓰러트리기 종료）
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:여리게대답한다/무뚝뚝하게대답한다/붙임성좋게대답한다/강하게대답한다/용서를빈다/기분좋게해줘
;고통계 반응
;	COM:무서워한다
;-------------------------------------------------
@KOJO_REACT_K13_27
;-------------------------------------------------
;지문은 REACT분류(받아들인다/적극적으로 한다, 태연하게 한다/용서를빈다/소극적으로 한다)로 분기
;ACT파생 마다의 분기는 없음 한편, 내용적으로는 ACT파생 0의 것
;ACT파생 1의 경우는 정합성이 무너진다고 생각된다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 쓰러트린다
;━━━━━━━━━━━━━━━━━━━━━━━━
;이 개소는 개별의 COM마다 기술하려면 적합하지 않는다고 생각되므로
;REACT분류로 나누어 둡니다
;-------------------------------------------------
IF 구상기입체크() && !TFLAG:ACT파생
	;=================================================
	;REACT분류받아들인다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("받아들인다")
		IF FUNC_K13_ISLOVER()
			IF RAND:2
				PRINTFORML 「기쁜 듯한 얼굴 해 버려…… 내가 예의범절을 가르칠 수 있는 것이 버릇이 되어 버렸을까?」
				PRINTFORMW 「소망 대로, 가득 사랑해, 가득 울릴 수 있어 주어?」
			ELSE
				PRINTFORML 「아라?   깔아눌러지고 있는데, 그렇게 사랑스러운 얼굴 해 버리군요?」
				PRINTFORMW 「…… 뭔가 나까지 흥분해 와 버린♪  터무니없게 괴롭혀 버리니까요?」
			ENDIF
		ELSE
			PRINTFORML 「깔아눌러지고 기뻐해 버리다니 부끄러운 아이네♪」
			PRINTFORMW 「좋아요…… 후회할 정도로, 터무니없게 희롱해 준다♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류적극적 따르는/태연하게 한다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("적극적으로 한다/태연하게 한다")
		IF FUNC_K13_ISLOVER()
			IF RAND:2
				PRINTFORML %CALLNAME:MASTER%를 쓰러트리기%CALLNAME%는,%CALLNAME:MASTER%의 목덜미를 거슬거슬의 혀로 훑도록(듯이) 빨았다
				PRINTFORMW 「응후…… 맛있어?   이대로, 전부 다 먹어 주어요?」
			ELSE
				PRINTFORML 「우후후…… 억눌러 버린♪  이제 도망칠 수 없네요?」
				PRINTFORMW 「그러면, 가득 사랑해 주기 때문에, 좋은 소리로 울어요?」
			ENDIF
		ELSE
			PRINTFORML 「후후…… 저항하지 않는다는 것은, 내가 마음대로 해도 좋은거네?」
			PRINTFORMW 「소망 대로, 충분히 예의범절을 가르쳐 준다♪」
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;REACT분류용서를빈다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("용서를빈다")
		IF BASE:MASTER:체력 <= 500
			IF FUNC_K13_ISLOVER()
				PRINTFORML 「후후, 이제(벌써) 완전히 지치고 지치고인걸. …… 쉬게 하기를 원하네요?」
				PRINTFORMW 「물론 각하야♪  억지로 가게 되었을 때의, 한심한 소리, 들려주세요……?」
			ELSE
				PRINTFORML 「아라, 벌써 지치게 되었어?   허락하기를 원하는거야?」
				PRINTFORMW 「안되게 정해져 있겠죠. 내가 만족할 때까지, 무리하게라도 교제하게 해 준다♪」
			ENDIF
		ELSE
			IF FUNC_K13_ISLOVER()
				PRINTFORML 「그렇게 싫어하지 않아도 좋지요?   다쳐 버리지 않아!」
				PRINTFORMW 「어느 쪽이든, 나의 좋을대로 시켜 받아요!」
			ELSE
				PRINTFORML 「후후, 그렇게 무서워하지 않아도 괜찮은거야?   거기까지 심한 것은 하지 않을 예정이니까」
				PRINTFORMW 「…… 뭐, 기분 나름으로 예정은 바뀌지만 말야♪」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류소극적 따른다
	;=================================================
	IF 구상기입체크() && IS_COMGRONAME("소극적으로 한다")
		IF FUNC_K13_ISLOVER()
			PRINTFORML 「아라?   그다지 내켜하는 마음이 아닌 것 같구나?」
			PRINTFORMW 「뭐, 어느 쪽이든 내가 하고 싶은 것 같이 시켜 받기 때문에, 각오 하세요♪」
		ELSE
			PRINTFORML 「후후, 그렇게 무서워하지 않아도 괜찮은거야?   거기까지 심한 것은 하지 않을 예정이니까」
			PRINTFORMW 「…… 뭐, 기분 나름으로 예정은 바뀌지만 말야♪」
		ENDIF
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 쓰러트리기 종료
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 쓰러트리기와 처리가 공통된 것으로 조금 저것인 느낌의 개소
;아무것도 하지 않고 RETURN 1만 돌려주는 것도 개미인 생각도 듭니다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
ENDIF