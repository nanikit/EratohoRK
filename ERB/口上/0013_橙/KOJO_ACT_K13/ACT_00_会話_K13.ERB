;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	첸구상 회화계 액션 구상 파일
;
;	회화계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 ACT0～9
;========================================================================
;·TFLAG:ACT(0=지금의기분을얘기한다/1=자신에게로의기분을얘기한다/2=성에대한이야기를한다/3=상냥하게위로한다/4=엄격하게위협한다/5=의상의변경을요구한다/6=합의를요구한다/7=벌이라고선언한다/8=고함친다/9=기분나쁘게웃는다)
;·TFLAG:ACT분류 == 0에 해당
;·ACT9, 기분나쁘게웃는다는 이상계 ACT분류로부터 파생하는 경우가 있다
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------
;─
;ACT0, 지금의기분을얘기한다
;─
;·지금의 기분에 대해 들어 온다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K13_0
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상C사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 지금의기분을얘기한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET) /TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;승마위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;조교 대상의 C에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상C사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상C사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상C사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER) /TEQUIP:페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마 안
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT1, 자신에게로의기분을얘기한다
;─
;·자신의 일을 어떻게 생각할까 얘기한다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일 조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K13_1
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상C사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 자신에게로의기분을얘기한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크()
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET) /TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;승마위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;조교 대상의 C에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상C사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상C사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상C사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER) /TEQUIP:페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크() && TEQUIP:성교봉사중
		PRINTFORML 「앗, 후~응?   응…… 나의 일, 어떻게 생각하고 있는 거야?」
		PRINTFORMW 「%FUNC_K13_CALLNAME()%의 기분, 아읏, 들려주었으면 좋구나……?」
		;체위에 의한 파생
		;SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
		;	CASE 1
		;		PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
		;	CASE 2
		;		PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
		;	CASE 3
		;		PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
		;	CASE 4
		;		PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
		;	CASE 5
		;		PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
		;	CASE 6
		;		PRINTFORMW 
		;ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마 안
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		IF FUNC_K13_ISLOVER()
			PRINTFORML 「저기…… 나의 일, 어떻게 생각하고 있어?」
			PRINTFORMW 「"연인"로서%FUNC_K13_CALLNAME()%의 대답을 (듣)묻고 싶구나……?」
		ELSEIF AFFECTION("조교 완료")
			PRINTFORML 「저기…… 나의 일, 좋아?   그렇지 않으면 싫어?」
			PRINTFORMW 「%FUNC_K13_CALLNAME()%의 기분, 들려주어요?」
		ELSE
			PRINTFORML 「저기…… 나의 일, 어떻게 생각하고 있는 거야?」
			PRINTFORMW 「정직하게 대답해도 좋아요?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT2, 성에대한이야기를한다
;─
;·조교자가 성에 대한 지식을 말하기 시작한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·조교자에게 성지식이 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K13_2
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상C사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT2, 성에대한이야기를한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET) /TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;승마위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;조교 대상의 C에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상C사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상C사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상C사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER) /TEQUIP:페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마 안
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT3, 상냥하게위로한다
;─
;·조교자가 상냥한 말로 위로해 온다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K13_3
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상C사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 상냥하게위로한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET) /TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;승마위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;조교 대상의 C에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상C사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상C사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상C사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER) /TEQUIP:페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마 안
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT4, 엄격하게위협한다
;─
;·어려운 말로 위협한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K13_4
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상C사용/성교봉사중/자위중 & 2)로 지문은 변화할 가능성이 있다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 엄격하게위협한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET) /TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;승마위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;조교 대상의 C에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상C사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상C사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상C사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER) /TEQUIP:페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT5, 의상의변경을요구한다
;─
;·조교자가 의상의 변경을 요구해 온다
;·조교 대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:줄이 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K13_5
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%에 옷을 벗어라고 명령했다…"되고 있어 이것이 변화하는 일은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
		PRINTFORMW 「그러면, 옷을 벗어 받을까」
		PRINTFORMW 「이봐요 이봐요, 멍하니 하지 않고, 빨리 벗는닷!」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상으로 페니스 있어
	;=================================================
	IF 구상기입체크(0) && PENIS(MASTER)
		;발기하고 있다
		IF TCVAR:MASTER:발기도 >= 1000
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상으로 페니스 없음
	;=================================================
	IF 구상기입체크(0) && !PENIS(MASTER)
		;젖고 있다
		IF PALAM:MASTER:윤활 > 500
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		IF RAND:2
			PRINTFORMW 「이봐요, 빨리 옷을 벗어?   싫다고는 말하게 하지 않아?」
		ELSE
			PRINTFORMW 「여기서 보고 있어 주기 때문에, 스스로 벗어?   빨리 하지 않으면 벌 해 버려?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT5_1, 옷을 입도록(듯이) 명령
;─
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크() && TFLAG:ACT파생 == 1
	PRINTFORML 「조금 생각났던 적이 있기 때문에, 옷을 입으세요?」
	PRINTFORMW 「이봐요, 옷을 입는거야!   빨리!」
	RETURN 1
ENDIF

;─
;ACT6, 합의를요구한다
;─
;·처음을 받아도 괜찮을까 얘기한다
;·처음=처녀 or동정(양쪽 모두의 경우도 있다)
;·이미 합의를 얻고 있는 경우는 발생하지 않는다
;·조교 대상이 처녀도 동정도 아닌 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K13_6
;-------------------------------------------------
;함수@TARGET_DESCRIPTION로 TEQUIP(성교중/조교대상C사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;후는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 합의를요구한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 남자의 경우
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:남자
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 후타나리의 경우
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:후타나리
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 여자 아이의 경우
	;=================================================
	IF 구상기입체크(0) && !PENIS(MASTER)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;성별 마다의 나누어 쓰고를 하지 않으면, 여기만 써 둡시다
	;남자, 후타나리, 여자 아이 분기를 모두 묻었을 경우는 불필요합니다. 삭제합시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT7, 벌이라고선언한다
;─
;·지금부터 벌 한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:벌플래그는 서 있다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP:밀어넘어뜨리기중 때에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K13_7
;-------------------------------------------------
;여기에서는 지문의 변화는 없다. 분기의 증감은 기호로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, 벌이라고선언한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「무뚝뚝하게응답한다/강하게응답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("무뚝뚝하게응답한다/강하게응답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「기분좋게해」(이 철면피!)
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("기분좋게해")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「좀더하드하게해」(이 돼지째!)
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("좀더하드하게해")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「허가를청한다」(허락해 받을 수 없었습니다)
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("허가를청한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「자위하기시작한다」(사람의 이야기를 들어라!)
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("자위하기시작한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT8, 고함친다
;─
;·큰 소리로 고함친다
;·턴방침이 "소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:벌플래그에 관련되지 않고, 발생할 가능성이 있다(벌중에 한층 더 고함칠 가능성등)
;-------------------------------------------------
@KOJO_ACT_K13_8
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 큰 소리로 고함쳤다…"만으로 파생은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 고함친다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크()
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
		PRINTFORMW 「아아, 이제(벌써)!   조금 전부터 이야이야뿐으로, 안절부절 해요!!」
		PRINTFORMW 「아 그렇게!   펫이 되고 싶지 않은거네!?   라면 단념해 주어도 괜찮지만」
		PRINTFORMW 「그 대신해, 이 장소에서 내장 베어 갈라, 산 채로 그 장물식등은 하는 일이 되어요!!」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「무뚝뚝하게응답한다/강하게응답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("무뚝뚝하게응답한다/강하게응답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		IF FUNC_K13_ISLOVER()
			IF RAND:2
				PRINTFORML 「최근 언제나 생각하지만……%FUNC_K13_CALLNAME()%, 요즈음 너무 우쭐해진 것이야!」
				PRINTFORMW 「자신의 입장을 잊었을 것이 아니네요?   나의 애완동물인 것이야, 너는!!」
			ELSE
				PRINTFORML 「무엇인 것이야, 그 태도는!!   『귀여워해져 당연』이라고라도 생각하고 있는 것이 아니야?   달라?」
				PRINTFORMW 「너무 빨고 있으면, 진심으로 그 사지 빼앗아요!!」
			ENDIF
		ELSE
			IF RAND:2
				PRINTFORML 「애완동물의 분수로, 나를 초조하게 한다니 건방지구나!!」
				PRINTFORMW 「자신의 입장을 모르고 있다면, 이번이야말로, 그 목 먹어 잘게 뜯어 줄까!?」
			ELSE
				PRINTFORML 「우쭐해지는 것이 아니에요!!」
				PRINTFORMW 「너 같은 그즈를, 내가 일부러 애완동물로서 길러 주고 있어!!   그렇겠지!?」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT9, 기분나쁘게웃는다
;─
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·화제 『비웃는다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K13_9
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상C사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 기분나쁘게웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크()
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크() && FIRSTACT(0)
		PRINTFORMW 「킥킥……」
		PRINTFORMW 「응?   별로 아무것도 아니야?   후후……」
		RETURN 1
	ENDIF
	
	;PENIS(TARGET)/! PENIS(TARGET) /TEQUIP:조교자페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교중
			;정상위				체중을 맡겨 %CALLNAME:MASTER%의 질내에 묻고
			CASE 1
				PRINTFORMW 
			;후배위				배후로부터 %CALLNAME:MASTER%의 질내에 묻고
			CASE 2
				PRINTFORMW 
			;승마위				%CALLNAME:MASTER%의 암컷 구멍에 근원까지 물어지고
			CASE 3
				PRINTFORMW 
			;대면좌위			밀착한 %CALLNAME:MASTER%의 질에 애무되고
			CASE 4
				PRINTFORMW 
			;배면좌위			%CALLNAME:MASTER%에 삽입해 등을 껴안고
			CASE 5
				PRINTFORMW 
			;애널섹스		%CALLNAME:MASTER%의 항문에 묻고
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;조교 대상의 C에의 애무를 하고 있는 상황. TEQUIP 관리인 것으로 가능성은 비교적 있다
	;=================================================
	;조교대상C사용
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TEQUIP:조교대상C사용, 1, 2)
		;자위·애무 계속중
		IF TEQUIP:조교대상C사용 == 1
			PRINTFORMW 
		;페라·쿤닐링구스 계속중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;PENIS(MASTER)/! PENIS(MASTER) /TEQUIP:페니스밴드 등으로 분기 해도 괜찮다
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;체위에 의한 파생
		SELECTCASE TEQUIP:성교봉사중
			;정상위시킨다				크게 벌어진 가랑이의 사이에 맞아들이고
			CASE 1
				PRINTFORMW 
			;후배위시킨다				배후로부터 질내에 삽입되고
			CASE 2
				PRINTFORMW 
			;역강간					미끈미끈의 질로 큐우큐우 짜내고
			CASE 3
				PRINTFORMW 
			;대면좌위시킨다				체를 밀착시켜 질 안쪽으로 환영해
			CASE 4
				PRINTFORMW 
			;배면좌위시킨다				등을 맡겨 만끽해
			CASE 5
				PRINTFORMW 
			;애널섹스시킨다		장액에 미끌거리는 항문으로 억압
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크()
		SELECTCASE RAND:3
			CASE 0
				PRINTFORML 「킥킥……」
			CASE 1
				PRINTFORML 「우후후……」
			CASE 2
				PRINTFORML 「아핫……♪」
		ENDSELECT
		PRINTFORMW 「응?   아, 조금, 걱정거리♪」
		RETURN 1
	ENDIF
ENDIF