;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	보라색 구상 숍 이벤트 구상 파일
;
;	숍으로부터 불려 가는 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;숍 이벤트 구상
;========================================================================

;─
;EV101, 의상 변경
;─
;·숍으로부터 의상 변경을 실시했을 경우에 표시
;-------------------------------------------------
@KOJO_FEMALEYOU_EVENT_K22_101(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=
;  1의 위…(0, 이제(벌써) 입고 있다, 1, 갈아입은, 2, 부끄러워서 입을 수 없는, 3, 싫어해 입지 않는, 9, 가지고 있지 않다)
;  10의 위…(0, 통상, 10, 반나체, 20, 전라, 30, 속옷 없음)
;  백의 위… 코스프레를 위해서(때문에) 보류
;  천의 위…(0, 조교자, 1000, 조교 대상)
;  예(조교 대상이 부끄러워해 전라로 할 수 없었다=1022)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 갈아입었다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG < 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;통상이 되었다
		CASE 1
			PRINTFORMW 「뭐, 기본이군요」
		;반나체가 되었다
		CASE 11
			PRINTFORMW 「응, 속옷 모습은 역시 섹시하구나」
			PRINTFORMDW … 자화자찬인가
		;전라가 되었다
		CASE 21
			PRINTFORMW 「아라, 보여져 부끄러운 것 같은 몸이 아니라요?」
			PRINTFORMDW 확실히 스타일은 발군이지만, 그것도 어떨까라고 생각한다
	ENDSELECT
	RETURN 2
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 부끄러워해 갈아입을 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG < 1000 && (ARG % 10) == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 싫어해 갈아입을 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG < 1000 && (ARG % 10) == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 갈아입는 것을 그만둔	FIRSTEVENT(30)
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%의 의상은 벌써[　　]로 설정되어 있습니다
;------------------------------------------------
IF 구상 기입 체크(0) && ARG < 1000 && (ARG % 10) == 0
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상을 갈아입게 했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG >= 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;통상이 되었다
		CASE 1001
			PRINTFORMW 「응, 시시하네요」
			PRINTFORMW 「가끔 씩은 바뀐 옷으로도 입어 보지 않을래?」
		;반나체가 되었다
		CASE 1011
			PRINTFORMW 「속옷 모습… 어딘지 모르게 얼이 빠지고 있어요」
			PRINTFORMDW … 라면 벗기지마
		;전라가 되었다
		CASE 1021
			PRINTFORMW 「조교한다면 역시 전라일까?」
			PRINTFORMW 「숨기지 않아도 괜찮지 않아, 서로알몸은 언제나 보고 있을까요?」
	ENDSELECT
	RETURN 2
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 부끄러워해 갈아입을 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG >= 1000 && (ARG & 10) == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 싫어해 갈아입을 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG >= 1000 && (ARG & 10) == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상을 갈아입게 하는 것을 그만둔 FIRSTEVENT(70)
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:MASTER%의 의상은 벌써[　　]로 설정되어 있습니다
;------------------------------------------------
IF 구상 기입 체크(0) && ARG >= 1000 && (ARG % 10) == 0
ENDIF
[SKIPEND]

;─
;EV102, 조교자 변경
;─
;·조교자를 교대시켰을 때에 표시
;-------------------------------------------------
@KOJO_FEMALEYOU_EVENT_K22_102(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 구상주가 다른 캐릭터에게 맡긴, 1, 구상주가 조교자가 되었다)
;구조교자는 「%CALLNAME:SHOP_OLDTARGET%」, 신조교자는 「%CALLNAME:SHOP_NEWTARGET%」로 참조할 수 있다
;구상주는 TARGET로 참조
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;다른 조교자에게 교대
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 0
	;조교 대상의 반항각인 Lv5 이상
	IF MARK:(FLAG:주인님):반항각인 >= 5
		PRINTFORMW 「아라아라, 상당히 미움받아 버린듯 하네」
		PRINTFORMW 「어쩔 수 없기 때문에 다른 사람에게 조금 맡겨 봅시다」
		PRINTFORMW 「이것으로 조금은 내가 얼마나 상냥했는지 아는 것이지요」
	;조교 대상의 반항각인 Lv4 이하
	ELSE
		PRINTFORMW 「나 혼자만 즐길 생각이 치워요」
		PRINTFORMW 「그렇다고 하는 것으로 오늘부터 다른 사람에게 충분히 귀여워해 받으세요」
		PRINTFORMW 「아아, 물론 나도 또 귀여워해 주기 때문에, 안심해도 좋아요」
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;보라색님이 조교자가 되었다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	;조교 대상의 반항각인 Lv5 이상
	IF MARK:반항각인 >= 5
		PRINTFORMW 「이것은 분명하게 예의 범절 없으면 안되구나…」
		PRINTFORMW 「안심해도 좋아요, 식의 예의 범절보다 훨씬 간단한 것 같은 것」
	;조교 대상의 반항각인 Lv4 이하
	ELSE
		PRINTFORMW 「자,%CNAME(MASTER, SHOP_NEWTARGET)%는 나에게 어떤 일을 되고 싶은 것일까?」
	ENDIF
	RETURN 2
ENDIF

;─
;EV110, 마력 사용
;─
;·마력을 소비해 어떠한 특전을 구입했을 때에 표시
;-------------------------------------------------
@KOJO_FEMALEYOU_EVENT_K22_110(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 마력 사용전, 1, 조교 기교, 2, 지식을 얻기 전, 3, 약학지식 취득, 4, 매혹 취득, 5, 금단의지식 취득, 6, 도구사용 취득,
;	7, 기술을 얻기 전, 8, 혀사용 취득, 9, 기묘한손가락취득, 10, 결박능숙, 11, 새디즘 소거/새디즘 취득, 12, 불결무시 취득, 38, 착의플레이선호, 39, 허리사용)
;이벤트 구상:아이템
;인수 내역…
;ARG=(13, 아이템 연성전, 14, 로터 연성, 15, 바이브 연성, 16, 애널비즈 연성, 17, 페니스밴드 연성, 18, 애널바이브 연성
; 19, 클리캡 연성, 20, 니플캡 연성, 21, 오나홀 연성)
;이벤트 구상:고문도구 연성
;인수 내역…
;ARG=(22, 고문도구 연성전, 23, 아이마스크 연성, 24, 채찍 연성, 25, 줄연성, 26, 볼개그 연성, 27,   바늘 연성, 28, 관장세트 연성
; 29, 삼각목마 연성, 30, 대거울 연성, 31, 목걸이 연성, 32, 도구를 커스터마이즈 했다)
;그 외 이벤트
;인수 내역…
;ARG=(33, 후타나리화/되돌리는, 34, 거유화/되돌리는, 35, 빈유화/되돌리는, 36, 정력의 한계 가격인상승)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;보라색님의 조교 기교가 업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「네, 레벨 업이야―」
		PRINTFORMDW … 하아
		PRINTFORMW 「레벨 업이야―」
		PRINTFORMDW … 웃는 얼굴이 무섭다
	;아라이먼트부
	ELSE
		PRINTFORMW 「네, 레벨 업이야―」
		PRINTFORMDW … 하아
		PRINTFORMW 「레벨 업이야―」
		PRINTFORMDW … 웃는 얼굴이 무섭다
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;보라색님SM상품 구입
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && INRANGE(ARG, 23, 31)
	;채찍 연성
	IF ARG == 24
		PRINTFORMW 「아라, 틈을 열면 왜일까 이런 것이 나와 버린 원 어떻게 하지요」
		PRINTFORMDW 단조롭게 읽기의 대사로 한 손에 채찍을 꽉 쥐어, 붕붕 휘두르는 그 모습은 어쩐지 수상한 일 이 이상 없었다
	;그 외
	ELSE
		PRINTFORMW 「그런데…」
		PRINTFORMW 「이런 것은 좋아할까?」
		PRINTFORMDW 틈부터 꺼낸 SM상품 한 손에 보라색은 빙그레 웃어 보였다
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;보라색님후타나리화 or후타나리 소거
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 33
	;후타나리화
	IF TALENT:TARGET:후타나리
		;조교 대상이 남자
		IF TALENT:MASTER:남자
			PRINTFORMW 「경계를 만지작거리면 알고 계시는 대로」
			PRINTFORMW 「자, 이것을 어째서 받을까?」
			PRINTFORMW 「그렇지 않으면 이것으로 어째서 줄까?」
		;조교 대상이 처녀
		ELSEIF TALENT:MASTER:처녀
			PRINTFORMW 「경계를 만지작거리면 알고 계시는 대로」
			PRINTFORMW 「자, 이것을 어째서 받을까?」
			PRINTFORMW 「그렇지 않으면 이것으로 어째서 줄까?」
		;조건 없음
		ELSE
			PRINTFORMW 「경계를 만지작거리면 알고 계시는 대로」
			PRINTFORMW 「자, 이것을 어째서 받을까?」
			PRINTFORMW 「그렇지 않으면 이것으로 어째서 줄까?」
		ENDIF
	;후타나리 소거
	;ELSE
	ENDIF
	RETURN 2
ENDIF

;─
;EV111, 주인님으로서 새로운 조교자를 고용했다
;─
@KOJO_FEMALEYOU_EVENT_K22_111(ARG)
;-------------------------------------------------
;·새로운 조교자가 TARGET가 되어 있기 (위해)때문에, 구상주는 TARGET는 아니기 때문에 주의
;·구상주를 참조하고 싶은 경우는 FLAG:주인님으로
;-------------------------------------------------
;반환값(1, 표시, 2, 표시해 가입한 캐릭터의 가입 구상을 스킵)
;IF 구상 기입 체크(0)
	;CSTR:(FLAG:주인님):호칭목록 = 전의 이름_부르게 하고 싶은 이름 /%CSTR:(FLAG:주인님):호칭목록%
;ENDIF

;─
;EV112, 보라색님이 조교자로서 가입했다
;─
@KOJO_EVENT_K22_112(ARG)
;-------------------------------------------------
;·주인님의 이름은 「FLAG:2」로 참조할 수 있다
;·주인님이 고용 구상으로 반환값 2를 돌려주고 있으면 표시되지 않는다
;-------------------------------------------------
;여기는 조교 대상의 성별을 불문하고 표시시키고 싶은 감이 있으므로 FEMALEYOU를 제외합니다;p@/L
;-------------------------------------------------
IF 구상기입체크(100)
	;조교 대상의 반항각인 Lv5 이상
	IF MARK:(FLAG:주인님):반항각인 >= 5
		IF FLAG:구상
			PRINTFORMW 「구별하고가 없는 아이는 없을까?」
			PRINTFORMDW 틈중에서 스키마 요괴가 나타났다
		ENDIF
	;조교 대상의 반항각인 Lv4 이하
	ELSE
		IF FLAG:구상
			PRINTFORMW 「왠지 즐거운 듯 하는 일을 하고 있는 것 같구나, 나도 혼합해 주시지 않아?」
			PRINTFORMDW 틈중에서 스키마 요괴가 나타났다
		ENDIF
	ENDIF
	RETURN 2
ENDIF