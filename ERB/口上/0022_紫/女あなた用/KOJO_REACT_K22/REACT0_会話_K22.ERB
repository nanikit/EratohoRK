;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	유카리 구상 회화계 개별 리액션 구상 파일
;
;	회화계 리액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 REACT0～9
;========================================================================
;·1231판보다 TFLAG:REACT부하가 도입되어 선택한 COM가 자연스러운 행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운 행동/1=무리가 없는 행동/2=무리를 한 행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 지금의기분을얘기한다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
@KOJO_FEMALEYOU_REACT_K22_0
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일 하고 싶다)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따르는 것으로, 리액션이빈정거린다 이외의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「응… 이제(벌써) 한번 밀기 필요할까요」
		PRINTFORMDW 턱에 손가락을 대고 유카리는 중얼 중얼거렸다
	;아라이먼트부
	ELSE
		PRINTFORMW 「거친에?   그래서 저항하고 있을 생각일까?」
		PRINTFORMDW 기쁜듯이 유카리는 웃고 있다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게대답한다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「아라, 상냥하게 여겨지는 것은 싫을까?」
		PRINTFORMW 「혹시, 얻어맞거나 매도되는 편이 기호?」
		PRINTFORMDW … 지금, 분기점에 서 있을 생각이 든다
	;아라이먼트부
	ELSE
		PRINTFORMW 「아라, 아직 그렇게 건강해 보이는 거네?」
		PRINTFORMW 「그렇다면 좀 더 힘들게 해도 괜찮아요?」
		PRINTFORMDW 강하게 도전해 보았지만 심술궂은 것 같게 유카리는 웃고 있다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「아라아라 어머나」
		PRINTFORMW 「조금은 센스 있은 대사를 말할 수 있는 거네」
		PRINTFORMW 「그렇지만 30점이라고 하는 곳이군요, 그러면 안 돼요」
	;아라이먼트부
	ELSE
		PRINTFORMW 「아직 그런 여유가 있는 거네」
		PRINTFORMW 「기뻐요, 곧바로 접혀 버려 시시한 것」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;굴복각인 Lv7 이상
	IF MARK:굴복각인 >= 7
		;역강간으로 질내사정의 뒤
		IF TFLAG:전회질내사정 == 2
			PRINTFORMW 「그 거, 혹시 이제(벌써) 쾌락의 포로라는 것일까?」
			PRINTFORMW 「후후, 좋아요…%CNAME(MASTER, TARGET)%가 온순해 있다면, 좀 더 기분 좋게 해 준다」
			PRINTFORMDW 유카리는 그렇게 말해%CALLNAME:MASTER%의 목에 팔을 돌렸다
			PRINTFORMW 「하지만, 분명하게 나도 기분 좋게 해 주지 않으면 안 돼요」
		;섹스로 질내사정의 뒤
		ELSEIF TFLAG:전회질내사정 == 1
			;아라이먼트정
			IF ALI(0)
				PRINTFORMW 「아라, 기쁜 일 말해 주어요」
				PRINTFORMW 「답례에 좀더 좀더 기분 좋게 해 준다」
				PRINTFORMW 「물론, 충분히 안에 내 주어요」
			ELSE
				PRINTFORMW 「여기까지 말해지고 기뻐하다니 어떻게든 하고 있어요?」
				PRINTFORMW 「그렇지만, 그러니까군요」
				PRINTFORMW 「당신을 기르고 있는 것은…… 우후」
			ENDIF
		;조건 없음
		ELSE
			PRINTFORMW 「솔직해져 왔군요」
			PRINTFORMW 「에에, 분명하게 말하는 것이 (들)물을 수 있다면…」
			PRINTFORMW 「포상을 주어요」
			PRINTFORMDW 유카리는 요염하게 미소를 띄우고%CALLNAME:MASTER%를 응시하고 있었다
		ENDIF
	;굴복각인 Lv6 이하
	ELSE
		PRINTFORMW 「후응…」
		PRINTFORMDW 유카리는 심하고 매정했다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 태연하게 따르는 것 경우
;-------------------------------------------------
;IF 구상 기입 체크(0) && TFLAG:ACT파생 == 4
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일 하고 싶다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
;IF 구상 기입 체크(0) && TFLAG:ACT파생 == 5
;ENDIF
;REACT0, 지금의기분을얘기한다

;────────────────────────────────────
;REACT1, 자신에게로의기분을얘기한다·개별 리액션
;────────────────────────────────────
;·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
@KOJO_FEMALEYOU_REACT_K22_1
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다!/2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;아라이먼트정
	IF CFLAG:아라이먼트 >= 0
		PRINTFORMW 「애매하구나…」
		PRINTFORMDW 유카리는 불만인듯한 반응을 보였다
	;아라이먼트부
	ELSE
		PRINTFORMW 「애매하구나…」
		PRINTFORMDW 유카리는 불만인듯한 반응을 보였다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게대답한다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「같은 일은 있을 이유 없다」
		PRINTFORMW 「상식 같은거 통하지 않아요, 여기다」
		PRINTFORMDW 유카리는 즐거운 듯이 웃고 있다
	;아라이먼트부
	ELSE
		PRINTFORMW 「같은 일은 있을 이유 없다」
		PRINTFORMW 「상식 같은거 통하지 않아요, 여기다」
		PRINTFORMW 「언제까지 그 대사를 말할 수 있을까 기대하고 있어요, 노력해」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「화내요… 좋아하지 않으면 곤란한 것」
		PRINTFORMDW 말장난이라도 하는것 같이, 유카리는%CALLNAME:MASTER%의 어조를 흉내내 장난쳐 보인다
		PRINTFORMW 「안 돼요, 좀 더 센스 있은 대사가 아니면」
	;아라이먼트부
	ELSE
		PRINTFORMW 「아라?   야유의 생각일까」
		PRINTFORMW 「그러면, 그렇구나… 하면 나는 거기의 『가죽』과 『고기』를 사용해 볼까」
		PRINTFORMDW 그렇게 말해 유카리는%CALLNAME:MASTER%의 사타구니를 가리켰다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 적은지, REACT분류가 태연하게 따른다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「아라아라?」
		PRINTFORMW 「내가 언제 심한 일을 했을까?」
		PRINTFORMDW 유카리는 웃어 그렇게 대답했다
	;아라이먼트부
	ELSE
		PRINTFORMW 「아라아라?」
		PRINTFORMW 「내가 언제 심한 일을 했을까?」
		PRINTFORMDW 유카리가 미소를 이쪽에 향한다
		PRINTFORMDW 그것은 이상해서 웃고 있는 것이 아닌 것을 분명히 알았다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 1000이상
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 4
	;역강간으로 질내사정의 뒤
	IF TFLAG:전회질내사정 == 2
		PRINTFORMW 「혹시… 엣치해 주는 상대라면 누구라도 좋은거야?」
		PRINTFORMW 「후후후, 농담이야」
		PRINTFORMDW 유카리는 웃어%CALLNAME:MASTER%의 입술을 손가락으로 가볍게 눌렀다
		RETURN 1
	ENDIF
	
	;섹스로 질내사정의 뒤
	IF TFLAG:전회질내사정 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「역시, 그래요」
			PRINTFORMW 「그렇지 않았으면, 단순한 색광인거야」
			PRINTFORMW 「당신은 그런 아이가 아니라고 믿고 있어요」
		;아라이먼트부
		ELSE
			PRINTFORMW 「분명하게 사랑이 전해지고 있던 것 같고 기뻐요」
			PRINTFORMW 「앞으로도, 애정을 충분히 따라 준다」
		ENDIF
		RETURN 1
	ENDIF
	
	;조건 없음
	PRINTFORMW 「아라아라 아라아라… 곤란해요」
	PRINTFORMW 「그렇지만 어쩔 수 없네요, 이런 미소녀에게 있을 수 있는이나 이것이나로 여겨진 것은…」
	PRINTFORMDW 그것은 그다지 관계없는 것이 아닐까 생각합니다
	PRINTFORMW 「우후후, 공기를 읽을 수 없는 아이네?」
	PRINTFORMDW 유카리는 웃는 얼굴인 채였지만, 기분을 나쁘게 했던 것은 분명히 알았다
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 5
	;역강간으로 질내사정의 뒤
	IF TFLAG:전회질내사정 == 2
		PRINTFORMW 「하아… 역시 사내 아이네…」
		PRINTFORMW 「더 이상 무엇을 되고 싶은 것일까요, 완전히」
		PRINTFORMDW 유카리는 기가 막히고 있는, 이 혐오의 표정은 간파할 수 없다
		RETURN 1
	ENDIF
	
	;섹스로 질내사정의 뒤
	IF TFLAG:전회질내사정 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「아라아라, 아직 부족한거야?」
			PRINTFORMW 「더 이상은 조금 마음이 상하지만……」
			PRINTFORMW 「그렇지만, 당신이 바란다면 어쩔 수 없네요?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「뭐?」
			PRINTFORMW 「채찍이나 바늘이나 목마라는 이름의 애정을 갖고 싶은 것?」
			PRINTFORMW 「후회해도 몰라요…… 우후후」
		ENDIF
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「좀 더 해 주었으면 한, 인가…」
		PRINTFORMW 「역시 엣치하고 싶은거야? 하고 싶은거네?」
		PRINTFORMDW 유카리는 한숨을 쉬었다
	;아라이먼트부
	ELSE
		PRINTFORMW 「후후… 도대체 어떤 일을 되고 싶은 것일까요」
		PRINTFORMDW 유카리는 입가에만 미소를 띄우고%CALLNAME:MASTER%를 내려다 본다
		PRINTFORMW 「사실… 욕구 깊은 거네, 인간은」
	ENDIF
	RETURN 1
ENDIF
;REACT1, 자신에게로의기분을얘기한다

;────────────────────────────────────
;REACT2, 성에대한이야기를한다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_FEMALEYOU_REACT_K22_2
;-------------------------------------------------
;  파생할 가능성이 있는 COM는 우선 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는 방법도 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「아라아라, 입다물어 버리는 거야?」
		PRINTFORMW 「유카리 곤란해 버리는, 그렇지 않으면 수줍어하고 있는 거야?」
		PRINTFORMDW 무슨 말을 해도%CALLNAME:MASTER%는 입다물고 있다
		PRINTFORMW 「재미없네요…」
	;아라이먼트부
	ELSE
		PRINTFORMW 「침묵은 돈이든지, 라고 하지만…」
		PRINTFORMW 「이 경우는 안 돼요, 내가 시시한 것」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「좀 더 분명히 말했으면 좋네요」
		PRINTFORMW 「너무 우물쭈물 되어도, 시시해요」
		RETURN 1
	ENDIF
	
	;REACT분류"소극적으로 한다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 0
		PRINTFORMW 「아라아라, 부끄러워해 버려 사랑스러워요」
		PRINTFORMW 「… 뭐라고 말한다고 생각해?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무뚝뚝하게대답한다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「아라, 반항적」
		PRINTFORMW 「붙임성이 없으면, 교제는 어려워요」
	;아라이먼트부
	ELSE
		PRINTFORMW 「혹시…」
		PRINTFORMW 「혼자서 하는 것조차 없는… 같은건 없네요?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성좋게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("붙임성좋게대답한다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「정직하게 대답해 받을 수 있는 것은 좋지만」
		PRINTFORMW 「좀 더, 저기…」
	;아라이먼트부
	ELSE
		PRINTFORMW 「응… 조교가 진행되고 있다고 해석하면 좋은 것일까」
		PRINTFORMDW 유카리는 석연치 않는 모습으로 고민하고 있었다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("강하게대답한다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「그렇게 울컥하게 안 돼도 괜찮아요」
		PRINTFORMW 「울컥하게 되면 될수록, 자신의 바닥의 얕음을 드러낼 뿐이야」
	;아라이먼트부
	ELSE
		PRINTFORMW 「이 정도로 그런 반응하는 거야?」
		PRINTFORMDW 아휴라고 하는 표정으로 유카리는 이쪽을 업신여기고 있다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;벌포인트 가산 파생:「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「그런 일이 용서될 이유 없지요」
		PRINTFORMW 「이봐요 이봐요, 빨리 전부 자백 하세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;파생:흐음
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		PRINTFORMW 「안 돼요♪」
		PRINTFORMW 「들려주어 주지 않으면 심한 일 해 버린다♪」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게해줘
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("기분좋게해줘")
	;=================================================
	;벌포인트 가산 파생:「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「기분 좋게 하는 것은%CNAME(MASTER, TARGET)%쪽이 아니고?」
		PRINTFORMW 「원숭이같이 번창해져도 곤란해요」
		RETURN 1
	ENDIF
	
	;=================================================
	;파생:흐음
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		PRINTFORMW 「그것은%CNAME(MASTER, TARGET)%나름이야」
		PRINTFORMW 「합격점이라면 기분 좋게 해 주어요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("자위하기시작한다")
	;=================================================
	;REACT파생 1, 「마음대로 자위 하지마」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「%CNAME(MASTER, TARGET)%응…」
		PRINTFORMW 「혼자서 하는 것으로 해도 이 타이밍은 이상해요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「마음대로 자위 하고 있지만 좋은가」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 0
		PRINTFORMW 「… 이야기를 듣고 있을까」
		PRINTFORMW 「설마 이런 건 반찬으로 된다고 생각하지 않았어요…」
		RETURN 1
	ENDIF
ENDIF
;REACT2, 성에대한이야기를한다

;────────────────────────────────────
;REACT3, 상냥하게위로한다·개별 리액션
;────────────────────────────────────
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_FEMALEYOU_REACT_K22_3
;-------------------------------------------------
;  파생할 가능성이 있는 COM에는 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는 방법도 있습니다
;-------------------------------------------------
;  우선 회화계 COM를 모두 망라되어 있습니다만
;  10 접수대의 애무계 COM, 50 접수대 이후의 그 외 COM등도 상황에 따라서는 선택 가능하게 됩니다
;  필요에 따라서 분기를 증감해 주세요
;-------------------------------------------------
RETURN 0
;REACT3, 상냥하게위로한다

;────────────────────────────────────
;REACT4, 엄격하게위협한다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만
;-------------------------------------------------
@KOJO_FEMALEYOU_REACT_K22_4
;-------------------------------------------------
RETURN 0
;REACT4, 엄격하게위협한다

;────────────────────────────────────
;REACT5, 의상의변경을요구한다·개별 리액션
;────────────────────────────────────
;·조교 대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가"소극적으로 한다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가"적극적으로 한다", "태연하게 한다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
@KOJO_FEMALEYOU_REACT_K22_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
ENDIF
;REACT5, 의상의변경을요구한다
[SKIPEND]
;────────────────────────────────────
;REACT6, 합의를요구한다·개별 리액션
;────────────────────────────────────
;·조교 대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
@KOJO_FEMALEYOU_REACT_K22_6
;-------------------------------------------------
;수고를 싫어하지 않는 것이면, TALENT:MASTER:남자등으로 내부에서 나누는 것도 있을지도 모릅니다
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따르는 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && IS_COMGRONAME("소극적으로 한다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 마음대로 하면 좋다고 대답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 태연하게 따르는 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && IS_COMGRONAME("태연하게 한다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && IS_COMGRONAME("적극적으로 한다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게해줘 주는 것이라면 처음을 주어도 괜찮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && IS_COMGRONAME("받아들인다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서 "거부"에 세트 된다)
;-------------------------------------------------
IF 구상 기입 체크(0) && IS_COMGRONAME("거부")
ENDIF
;REACT6, 합의를요구한다
[SKIPEND]

;────────────────────────────────────
;REACT7, 벌이라고선언한다·개별 리액션
;────────────────────────────────────
;·선언해도 벌이 발생한다고는 할 수 없다
;-------------------------------------------------
@KOJO_FEMALEYOU_REACT_K22_7
;-------------------------------------------------
RETURN 0
;REACT8, 고함친다

;────────────────────────────────────
;REACT9, 기분나쁘게웃는다·개별 리액션
;────────────────────────────────────
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_FEMALEYOU_REACT_K22_9
;-------------------------------------------------
RETURN 0
;REACT9, 기분나쁘게웃는다