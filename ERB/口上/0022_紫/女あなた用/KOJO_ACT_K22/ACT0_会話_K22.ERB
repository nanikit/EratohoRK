;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	유카리 구상 회화계 액션 구상 파일
;
;	회화계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 ACT0～9
;========================================================================
;·TFLAG:ACT(0=지금의기분을얘기한다/1=자신에게로의기분을얘기한다/2=성에대한이야기를한다/3=상냥하게위로한다/4=엄격하게위협한다/5=의상의변경을요구한다/6=합의를요구한다/7=벌이라고선언한다/8=고함친다/9=기분나쁘게웃는다)
;·TFLAG:ACT분류 == 0에 해당
;·ACT9, 기분나쁘게웃는다는 이상계 ACT분류로부터 파생하는 경우가 있다
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------
;────────────────────────────────────
;ACT0, 지금의기분을얘기한다
;────────────────────────────────────
;·지금의 기분에 대해 들어 온다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_0
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 지금의기분을얘기한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;역강간으로 질내사정의 뒤
	IF TFLAG:전회질내사정 == 2
		PRINTFORMW 「후후후… 가득 냈군요」
		PRINTFORMW 「저기, 지금 어떤 기분일까?」
		PRINTFORMW 「당신을 자유롭게 하는 여자의 안에 낸 기분은?」
		PRINTFORMDW 유카리는 쾌락의 여운에 잠긴다%CALLNAME:MASTER%에 강요했다
		RETURN 1
	ENDIF
	
	;섹스로 질내사정의 뒤
	IF TFLAG:전회질내사정 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「우후후…… 내 버렸다. 안에 내 버렸다」
			PRINTFORMW 「저기, 나의 정액을 받아 들인 감상을 들려주어?」
			PRINTFORMW 「이봐요, 느끼겠죠?   나의 뜨거운거야」
		;아라이먼트부
		;응 어떤 기분? 질내사정되어 어떤 기분?
		ELSE
			PRINTFORMW 「우후후…… 중에 나와 버렸군요?」
			PRINTFORMW 「안쪽으로부터 더럽혀지는 기분은, 어떨까?」
		ENDIF
		RETURN 1
	ENDIF
	
	;조교 개시 직후
	IF TFLAG:경과시간 == 1
		PRINTFORMW 「기분의 정도는 어떨까?」
		PRINTFORMDW 유카리는 매우 정중한 행동과 어조에서는%CALLNAME:MASTER%에 말을 걸었다
		PRINTFORMW 「시작하기 전에 들어 두지 않으면, 가감(상태)가 미쳐 버리는 일도 있습니다」
		PRINTFORMDW 그렇게 말해 즐거운 듯이 유카리는 웃었다
		RETURN 1
	ENDIF
	
;일반
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「그런데 지금은 어떤 기분?」
		PRINTFORMW 「가르쳐 주시지 않아?   이런 미소녀에게 마음대로 몸을 만지작거려지는 기분을…」
		PRINTFORMDW … 미소녀인가
		PRINTFORMW 「… 뭐야, 그 얼굴은?」
	;아라이먼트부
	ELSE
		PRINTFORMW 「그런데…」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%는 이렇게 해 타인에게 몸을 주물러대지면 어떤 기분인 것일까?」
		PRINTFORMDW %CALLNAME:MASTER%는 서투른 대답은 생활과 모습을 살핀다
		PRINTFORMW 「후후후… 아직도 즐겁게 해 주는 것 같구나, 기뻐요」
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT1, 자신에게로의기분을얘기한다
;────────────────────────────────────
;·자신의 일을 어떻게 생각할까 얘기한다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일 조교중에서는 억제된다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_1
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 자신에게로의기분을얘기한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;역강간으로 질내사정의 뒤
	IF TFLAG:전회질내사정 == 2
		PRINTFORMW 「저기, 이렇게 기분의 좋은 일을 해 주는 나를,%CNAME(MASTER, TARGET)%는 도대체 어떤 식으로 생각하고 있는 거야?」
		PRINTFORMDW 넓적다리를 타는 백탁액을 손가락으로 건져 올려, 과시하도록(듯이) 손가락과 혀로 희롱한다
		PRINTFORMW 「가르쳐 주시지 않아? 　%CNAME(MASTER, TARGET)%의 기분을…」
		RETURN 1
	ENDIF
	
	;섹스로 질내사정의 뒤
	IF TFLAG:전회질내사정 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「저기, 당신은 나의 일 좋아하네요?」
			PRINTFORMW 「왜냐하면, 그렇겠지. 나의 더러운 욕망의 덩어리를 받아 들여 준 것인거야」
			PRINTFORMW 「좋아하지도 않으면, 그런 일 할 수 없네요?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「이렇게 애정 따르고 있기 때문에, 응해 주어도 좋네요?」
			PRINTFORMW 「이봐요, 조금 전도 따라 주었지 않아」
			PRINTFORMW 「정액이라는 이름의 애정을」
		ENDIF
		RETURN 1
	ENDIF
	
	;조교 개시 직후
	IF TFLAG:경과시간 == 1
		PRINTFORMW 「%CNAME(MASTER, TARGET)%는 마음대로 자신의 몸을 만지작거리는 미소녀의 일을 어떻게 생각하고 있을까?」
		PRINTFORMDW … 미소녀네
		PRINTFORMDW %CALLNAME:MASTER%는 뭐라고도 말할 수 없는 얼굴로 유카리를 본다
		PRINTFORMDW 오늘의 조교는 스키마송리카등 시작되었다
		RETURN 1
	ENDIF
	
;일반
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「그런데…%CNAME(MASTER, TARGET)%는 나의 일 좋아하네요?   이렇게 상냥하게 해 받고 있기 때문에」
		PRINTFORMDW 상냥한… 인가?
	;아라이먼트부
	ELSE
		PRINTFORMW 「그런데…%CNAME(MASTER, TARGET)%는 나의 일 좋아하네요?   이렇게 상냥하게 해 받고 있기 때문에」
		PRINTFORMDW … 진심으로 말하고 있을까
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT2, 성에대한이야기를한다
;────────────────────────────────────
;·조교자가 성에 대한 지식을 말하기 시작한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·조교자에게 성지식이 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_2
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT2, 성에대한이야기를한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;역강간으로 질내사정의 뒤
	IF TFLAG:전회질내사정 == 2
		IF FLAG:구상
			PRINTFORMW 「알고 있을까?」
			PRINTFORMW 「요괴는 매우 장수 하는 분, 보통 성행위는 임신하다니 우선 없는거야」
			PRINTFORMW 「무엇을 말하고 싶은가는? 알고 있는 주제에… 방편 마음껏이라는 것」
			PRINTFORMDW 함축이 있는 웃음을 보여, 유카리는 방금전의 행위에 대해 이것 저것 이야기를 시작했다
		ENDIF
		RETURN 1
	ENDIF
	
	;섹스로 질내사정의 뒤
	IF TFLAG:전회질내사정 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「이것, 잘 되어 있겠죠?」
			PRINTFORMW 「능숙하게 가면, 분명하게 아이라도 생기게 되는거야」
			PRINTFORMW 「…… 우후후, 놀랐어?」
			PRINTFORMW 「그렇지만, 요괴는 장수 하기 때문에, 아이 같은거 좀처럼 할 수 없는거야」
			PRINTFORMW 「유감이어요?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「이것, 잘 되어 있겠죠?」
			PRINTFORMW 「능숙하게 가면, 분명하게 아이라도 생기게 되는거야」
			PRINTFORMW 「…… 우후후, 놀랐어?」
			PRINTFORMW 「그렇지만, 요괴는 장수 하기 때문에, 아이 같은거 좀처럼 할 수 없는거야」
			PRINTFORMW 「그러니까, 나는 즐겨 마음껏이라는 것♪」
		ENDIF
		RETURN 1
	ENDIF
		
	;조교 개시 직후
	IF TFLAG:경과시간 == 1
		PRINTFORMW 「아아, 그렇게 말하면 (듣)묻고 싶은 것이 있었어」
		PRINTFORMW 「당신, 성교육은 어느 정도 받고 계실까?」
		PRINTFORMDW … 단순한 성희롱이 아닌지, 이것?
		RETURN 1
	ENDIF
	
;일반
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「그래그래…%CNAME(MASTER, TARGET)%의 성 경험이 어느 정도인가 알고 싶은 것이지만」
		PRINTFORMW 「상세를 가르쳐 받을 수 있을까?」
	;아라이먼트부
	ELSE
		PRINTFORMW 「%CNAME(MASTER, TARGET)%는 주에 어느 정도 자위 하고 있는 거야?」
		PRINTFORMDW … 직구 지나다
		PRINTFORMW 「확인이야, 확인」
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT3, 상냥하게위로한다
;────────────────────────────────────
;·조교자가 상냥한 말로 위로해 온다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_3
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 상냥하게위로한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT4, 엄격하게위협한다
;────────────────────────────────────
;·어려운 말로 위협한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_4
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화할 가능성이 있다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 엄격하게위협한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT5, 의상의변경을요구한다
;────────────────────────────────────
;·조교자가 의상의 변경을 요구해 온다
;·조교 대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:쓰러트리기중에는 발생하지 않는다
;·TEQUIP:줄이 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_5
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%에 옷을 벗어라고 명령했다…"되고 있어 이것이 변화하는 일은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ! TFLAG:ACT파생
;ENDIF

;────────────────────────────────────
;ACT5_1, 옷을 입도록(듯이) 명령
;────────────────────────────────────
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
;ENDIF

;────────────────────────────────────
;ACT6, 합의를요구한다
;────────────────────────────────────
;·처음을 받아도 괜찮을까 얘기한다
;·처음=처녀 or동정(양쪽 모두의 경우도 있다)
;·이미 합의를 얻고 있는 경우는 발생하지 않는다
;·조교 대상이 처녀도 동정도 아닌 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_6
;-------------------------------------------------
;함수@TARGET_DESCRIPTION로 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;후는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 합의를요구한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT7, 벌이라고선언한다
;────────────────────────────────────
;·지금부터 벌 한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:벌플래그는 서 있다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP:쓰러트리기중시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_7
;-------------------------------------------------
;여기에서는 지문의 변화는 없다. 분기의 증감은 기호로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, 벌이라고선언한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT8, 고함친다
;────────────────────────────────────
;·큰 소리로고함친다
;·턴방침이 "소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:벌플래그에 관련되지 않고, 발생할 가능성이 있다（벌중에 한층 더고함친다가능성등 ）
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_8
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 큰 소리로 고함쳤다…"만으로 파생은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 고함친다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT9, 기분나쁘게웃는다
;────────────────────────────────────
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·화제 『비웃는다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_9
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 기분나쁘게웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF