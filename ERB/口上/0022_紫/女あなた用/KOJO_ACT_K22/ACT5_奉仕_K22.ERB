;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	유카리 구상 봉사계 액션 구상 파일
;
;	봉사계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;봉사계 ACT50～57
;========================================================================
;·TFLAG:ACT(50=손으로애무를강제/51=입으로애무를강제/52=파이즈리/53=가랑이/55=다리빨고강제/56=이라마치오/57=가슴애무강제)
;·TFLAG:ACT분류 == 5에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "봉사")로 줍는 것이 가능
;·조교 대상으로, 조교자의 신체를 애무하도록(듯이) 명령한다
;·기본적으로는 명령인 것으로, 이 단계에서는 따를지 어떨지 확정하고 있지 않다(ACT56, 이라마치오는 거부 할 수 없다)
;-------------------------------------------------
;·ACT54, 풋잡은 12/04/08판으로 숙청
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;─
;ACT50, 손으로애무를강제
;─
;·애무 강제/자위 강제/페니스밴드 자위 강제
;·조교 대상(MASTER)으로, 손에 의한 애무를 강제한다
;·조교자에게 페니스가 있으면 반드시 자위 강제가 된다(페니스가 있으면 통상의 애무 강제는 일어나지 않는다)
;·조교자가 페니스밴드장착 안은 페니스밴드 자위 강제가 된다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(줄/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교 대상이 따르는지 아닌지는 이 시점에서는 미정
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_50
;-------------------------------------------------
;·TFLAG:ACT파생(0=애무 강제/1=자위 강제/2=페니스밴드 자위 강제)
;-------------------------------------------------
;·지문은 ACT파생 마다 분기. 각 ACT파생내에서는 분기 없음
;·ACT파생 0, 애무때만 쾌B/쾌ASOURCE 증가
;·그 외의 경우는 쾌C한정
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_0, 애무 강제 「자신의 성기를 손으로애무 하라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ! TFLAG:ACT파생
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_1, 자위 강제 「자신의 남성기를 손으로 잡아당기라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_2, 페니스밴드 자위 강제 「페니스밴드를 손으로 잡아 움직이라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;페니스밴드에의 애무는 쾌CSOURCE가 감소해, 쾌VSOURCE가 증가한다
;장착하고 있는 시점에서 대상이 처녀일 가능성은 없다
;-------------------------------------------------
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
ENDIF
[SKIPEND]

;─
;ACT51, 입으로애무를강제
;─
;·쿤닐링구스/펠라치오/안면승마/안면승마애널/
;·조교 대상으로, 입에 의한 애무를 강제한다
;·조교자가 후타나리의 경우, 조교 대상이 위로돌리고라면 1/2으로 안면승마, 그렇지 않으면 페라 강제가 된다
;·조교자에게 페니스가 없고, 조교 대상이 위로돌리고라면 안면승마가 된다
;·안면승마가 선택되고 있었을 때, 안면승마애널에 변화하는 일이 있다
;·모든 조건으로부터 빠졌을 경우는 통상의 쿤닐링구스가 된다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각목마) 장착중은 발생하지 않는다
;·조교자가 페니스밴드를 장착하고 있는 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_51
;-------------------------------------------------
;·TFLAG:ACT파생(0=쿤닐링구스/1=페라/2=안면승마/3=안면승마애널)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51, 입으로애무를강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GROUPMATCH(TFLAG:ACT파생, 0, 2, 3) > 0
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「어때?   나의\@ TFLAG:ACT파생 == 3 ? 엉덩이. 후후 # 보지 \@」
		PRINTFORMW 「빨고 싶어져 오지 않아?」
	;아라이먼트부
	ELSE
		PRINTFORMW 「이봐요, 빠세요」
		PRINTFORMW 「버터 발라 두어 주었어요?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_1, 펠라치오
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;전회가 페라
	IF IS_PREVACTNAME("입으로애무를강제")
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「읏…… 좋아요, 능숙해요」
			PRINTFORMW 「이 상태로 계속해……」
		;아라이먼트부
		ELSE
			PRINTFORMW 「누가 멈추어도 좋다고 말했을까?」
			PRINTFORMW 「빨리 계속하세요」
			PRINTFORMW 「그렇지 않으면 무리하게 밀어넣어지는 것이 기호?」
		ENDIF
		RETURN 1
	ENDIF
	
	;전회가 이라마치오
	IF IS_PREVACTNAME("이라마치오")
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「미안해요, 무리하게로. 괴로왔던 것이겠지?」
			PRINTFORMW 「이번은 자신의 페이스로 해 봐」
		;아라이먼트부
		ELSE
			PRINTFORMW 「후우…… 무리하게 범하는 것 지치게 되었어요」
			PRINTFORMW 「당신으로부터 해 주시지 않아?」
		ENDIF
		RETURN 1
	ENDIF
	
	;전회가 파이즈리
	IF IS_PREVACTNAME("파이즈리")
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「가슴의 사이부터 나오고 있는 앞가 외로운 것 같아」
			PRINTFORMW 「입으로 상대 해 주시지 않아?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「분명하게 전체에 봉사 하세요」
			PRINTFORMW 「하아?   닿지 않아?」
			PRINTFORMW 「그 입은 불평하는 일 밖에 할 수 없는거네. 기가 막혔어요」
		ENDIF
		RETURN 1
	ENDIF
	
	;아이마스크
	IF TEQUIP:아이마스크
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「저기, 입술에 해당되고 있는 것 아네요?」
			PRINTFORMW 「빨아 보지 않을래?」
			PRINTFORMW 「맛있어요?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「저기, 입술에 해당되고 있는 것, 아네요?」
			PRINTFORMW 「그대로, 입을 열어 무세요」
			PRINTFORMW 「그렇지 않으면 무리하게 밀어넣어 버려요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;긴박중
	IF TEQUIP:줄
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「자, 나를 기분 좋게 해 줘」
			PRINTFORMW 「응, 몸을 움직일 수 없어?」
			PRINTFORMW 「괜찮아요. 입은 자유롭게 사용할 수 있겠죠?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「그런데, 나를 기분 좋게 해 받을까요」
			PRINTFORMW 「우후후…… 속박되고 있기 때문에 할 수 없는……」
			PRINTFORMW 「당신의 그 입은, 그렇게 좋은 것을 말하기 위한 것이었을까?」
		ENDIF
		RETURN 1
	ENDIF
	
	;조교자가 자위중
	IF GETBIT(TEQUIP:자위중, 1)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「읏…… 응, 보고 있을 뿐이 아니고, 당신도 도와 주시지 않아?」
			PRINTFORMW 「이봐요, 자지의 끝 쪽 빨아?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「읏…… 응, 언제까지 멍─하니 하고 있을 생각?」
			PRINTFORMW 「빨리 빠세요. 명령이야?」
		ENDIF
		RETURN 1
	ENDIF
	
	;대상이 자위중
	IF GETBIT(TEQUIP:자위중, 0)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「저기, 당신뿐 기분 좋아지고 있는 것은 간사해요」
			PRINTFORMW 「나도 기분 좋게 해?」
			PRINTFORMW 「이봐요, 나의 자지를 당신의 입으로…… 말야?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「자신뿐 기분 좋아져서 된다고는 생각하지 않네요?」
			PRINTFORMW 「그렇구나, 그 야무지지 못하게 연 입으로 봉사해 받을까요」
		ENDIF
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「우후후…… 당신의 일 생각하고 있으면, 이렇게 되어 버렸어」
		PRINTFORMW 「책임 취해 진정시켜 주시지 않아?」
		PRINTFORMW 「당신의 그 사랑스러운 입으로」
	;아라이먼트부
	ELSE
		PRINTFORMW 「자, 근무의 시간이야」
		PRINTFORMW 「그 건방진 입으로 주인님에 봉사 하세요」
	ENDIF
	RETURN 1
ENDIF

;─
;ACT52, 파이즈리
;─
;·조교 대상으로 파이즈리에서의 봉사를 강제한다
;·조교 대상이 남자의 경우는 발생하지 않는다
;·조교자에게 페니스가 없는 경우는 발생하지 않는다
;·턴방침이 "이상"의 경우는 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(줄/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_52
;-------------------------------------------------
;·ACT파생없음. 지문에 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT52, 파이즈리
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ! TFLAG:ACT파생
;ENDIF

;─
;ACT53, 가랑이
;─
;·조교 대상으로 페니스밴드, 혹은 페니스를 가랑이에 끼워 스치고라고 명령한다
;·조교자에게 봉이 없다(페니스/페니스밴드) 경우는 발생하지 않는다
;·조교 대상으로 페니스가 있는 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"의 경우는 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:삼각목마 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교 대상으로 다리가 없으면 발생하지 않는다(현재 상태로서는 토지코가 조교 대상인 경우만이라고 생각된다)
;·REACT분류가 거부/날뛴다/도망치는인 경우는 거절 취급
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_53
;-------------------------------------------------
;·TFLAG:ACT파생(0=가랑이/1=페니스밴드 가랑이)
;-------------------------------------------------
;·안면승마 계, 자위계는 해제된다
;·지문은 ACT파생로 분기. 파생내에서는 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_0, 가랑이 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자에게 페니스가 있는 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && ! TFLAG:ACT파생
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_1, 페니스밴드 가랑이 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 페니스밴드장착 안의 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
ENDIF
[SKIPEND]

;─
;ACT55, 다리빨고강제
;─
;·조교자가 다리를 빨라고 명령해 온다
;·이상계로부터 발생하는 경우 있어
;·턴방침이 "소프트"라면 발생하지 않는다
;·오늘의 방침이 "러브러브"라면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교자에게 다리가 없는 경우는 발생하지 않는다
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절 취급
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_55
;-------------------------------------------------
;·지문에 분기 없음@TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT55, 다리빨고강제
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ! TFLAG:ACT파생
;ENDIF

;─
;ACT56, 이라마치오
;─
;·명령하는 것은 아니고, 억지로 페니스를 입에 돌진한다
;·조교 대상의 반응은 미정이지만, 거절하는 일은 할 수 없다
;·가학계로부터 발생하는 경우 있어
;·턴방침이 "노멀", "소프트"라면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교자에게 페니스가 없는 경우는 발생하지 않는다(페니스밴드에 의한 이라마치오는 없다)
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_56
;-------------------------------------------------
;·자위중이면 해제된다
;·지문에 분기 없음. 지문은↓
;·%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 머리를 잡아, 난폭하게 %CALLNAME:MASTER%의 목까지 찔러 넣었다…
;-------------------------------------------------
;·조교자의 발기 정도는 TCVAR:발기로 참조할 수 있다
;·TCVAR:발기(0= 아직 부드럽다/1=서서히 경도를 더하고 있다/2=발기한/2이상=뒤로 젖혔다)적인 느낌으로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT56, 이라마치오
;━━━━━━━━━━━━━━━━━━━━━━━━
;필요에 따라서 분기를 증감해 주세요
;-------------------------------------------------
;IF 구상 기입 체크(0) && ! TFLAG:ACT파생
;ENDIF

;─
;ACT57, 가슴애무강제
;─
;·조교 대상으로, 조교자의 가슴을 애무하도록(듯이) 강제한다
;·조교자가 남자의 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:줄장착중은 입으로 가슴애무강제에 파생한다. 다만 볼개그를 장착중의 경우는 튕겨진다
;·볼개그가 없으면,1/2의 확률로 입으로애무가 된다
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절 취급
;·PALAM가 치정선행이나 반항 선행인가로"부끄러워서 봉사 할 수 없었다/싫어해 요구에 응하지 않았다"에 파생한다
;-------------------------------------------------
@KOJO_FEMALEYOU_ACT_K22_57
;-------------------------------------------------
;·TFLAG:ACT파생(0=손으로 가슴애무강제/2=입으로 가슴애무강제)
;-------------------------------------------------
;·지문은 ACT파생로 분기. 파생내에서는 분기 없음
;·@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;·안면승마는 해제된다
;·REACT에 대해, 위로돌리고가 아니고, ACT파생가 입으로애무가 아닌 경우,1/2그리고 뒤로부터 애무에 파생한다
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_0, 손으로 가슴애무를 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ! TFLAG:ACT파생
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_2, 입으로 가슴애무를 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
ENDIF
[SKIPEND]