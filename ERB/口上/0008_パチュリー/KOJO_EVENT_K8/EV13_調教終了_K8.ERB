;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	파츄리 구상 조교 종료 이벤트 구상 파일
;
;	조교 종료 이벤트 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 조교 종료 구상
;========================================================================
;·@EVENTCOMEND로부터 불려 간다
;·구상주가 조교 종료하기 직전에 표시
;·ARG:1, ARG:2의 경우는 지문 표시 후에 PRINTL로 공행. 그 뒤로 구상을 표시
;·ARG:0의 경우는 고유의 지문 없음. 구상 표시전에 PRINTL로 공행이 들어간다
;·RESULT 0이상을 돌려주면 구상 표시 후에 PRINTL로 공행이 들어간다
;-------------------------------------------------
;관련 변수…
;FLAG:일상 제어 조교 종료시 상황(1=통상/2=정력 0/3=한번휴식/4=실신했다)
;-------------------------------------------------
@KOJO_EVENT_K8_13(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 보통으로 종료, 1, 조교 대상이 피곤한, 2, 조교 대상이 실신했다)
;-------------------------------------------------

;─
;ARG:1, ARG:2, 피로·실신에 의한 조교 종료 FIRSTEVENT(0)~
;─
;·조교 대상 소모도에 응해 피로/실신으로 나누어진다
;·호출 타이밍은 지문의 표시→PRINTL→구상의 표시
;·EVENT201, 간호(일상 이벤트)의 개소를 쓰는 경우는, 능숙하게 제휴할 수 있도록(듯이)하면 좋을 것입니다
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 피곤한 또는 실신했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && (ARG == 1 || ARG == 2)
	PRINTFORMW 「조금 너무 해 버렸을까」
	RETURN 1
ENDIF

;─
;ARG:0보통으로 종료 FIRSTEVENT(10)~
;─
;·통상의 조교 종료시의 구상
;·지문 없음. PRINTL에 의한 공행의 뒤로 구상 표시
;·테두리마다 이벤트 번호를 털어 있습니다만, 임의로 설정하는 일은 가능합니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;대만족 보너스를 얻고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:이번에대만족
	;=================================================
	;조교 대상의 정력 0
	;=================================================
	IF 구상기입체크(100) && FLAG:일상제어 == 2
		PRINTFORMW 「나로서도 완벽하구나」
		PRINTFORMW 「이것으로 잠시 마력이 곤란할 것도 없을 것입니다」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「성과는 최상이군요」
		PRINTFORMW 「나에게는 반드시 조교의 재능이 있는 것이예요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조건 없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「이번은 이 정도로 해 두어 주어요」
		RETURN 1
	ENDIF
ENDIF
