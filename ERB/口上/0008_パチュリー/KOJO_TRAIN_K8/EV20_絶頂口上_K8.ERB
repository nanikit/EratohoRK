;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	파츄리 구상 조교 이벤트 구상 파일
;
;	조교 턴중 이벤트, 절정 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;☆잠정 사양@/L

;========================================================================
;EV20, 절정 구상
;========================================================================
;·조교자나 조교 대상이 절정에 이를 때 표시
;· 각 절정은 개별의 호출을 가지지 않고, 모두 파생으로 처리된다
;-------------------------------------------------
;=================================================
;관련 변수…
;
;TFLAG:전회 질내사정, (1=V성교/2=V성교 봉사/3=V성교(하늘 쏘아 맞히고)/4=V성교 봉사(하늘 쏘아 맞히고)/5=A성교/6=A성교 봉사/7=A성교(하늘 쏘아 맞히고)/8=A성교 봉사(하늘 쏘아 맞히고))
;TFLAG:REACT 분류, (0=없음/1=소극적 따른다/2=적극적 따른다/3=허가를 청한다/4=날뛴다/5=요구에 응하지 않는, 거부/6=도망치는, 발버둥 친다)
;NOWEX:사정, (1=하늘 사정/2=사정/3=대량 사정/4=사정(조루)/5=A책사정)
;TEQUIP:자위중, (bit0=조교 대상/bit1=조교자)
;
;=================================================
;함수류…
;
;MASTER_EX
;인  수:ARG:0… 애무자→1. 조교 대상 자신/2. 조교자/4. 조수 1/8. 조수 2/16. 조수 3
;                      /64. 도구/128. 변소 이용자  생략 하면 애무자를 묻지 않는다
;              비트화에서의 지정이 가능
;       ARG:1…(생략가능) 절정의 종별→1. C절정/2. V절정/3. A절정/4. B절정
;                                  /11. 분유/12. 사정/13. 방뇨
;ARG:0(bit0 =조교 대상 자신 /bit1 =조교자 /bit2 =조수 1/bit3 =조수 2/bit4 =조수 3/bit6 =도구 /bit7 =변소 이용자)
;-------------------------------------------------
;TARGET_EX
;인  수:ARG:0… 애무자→1. 조교 대상/2. 조교자 자신/4. 조수 1/8. 조수 2/16. 조수 3
;              비트화에서의 지정이 가능. 생략 하면 애무자를 묻지 않는다
;       ARG:1…(생략가능) 절정의 종별→1. c절정/2. v절정/3. a절정/4. b절정
;                                  /11. 분유/12. 사정
;ARG:0(bit0 =조교 대상 /bit1 =조교자 자신 /bit2 =조수 1/bit3 =조수 2/bit4 =조수 3)
;-------------------------------------------------
@KOJO_EVENT_K8_20(ARG)
;-------------------------------------------------
;TFLAG:전회 질내사정;(1=V성교/2=V성교 봉사/3=V성교(하늘 쏘아 맞히고)/4=V성교 봉사(하늘 쏘아 맞히고)/5=A성교/6=A성교 봉사/7=A성교(하늘 쏘아 맞히고)/8=A성교 봉사(하늘 쏘아 맞히고))
;-------------------------------------------------
;─
;질내 사정(MASTER→TARGET)
;─
;·V성교 봉사계로 질내 사정하는 경우에 표시
;·TFLAG:뽑지 않고는 뽑지 않는 판정 마다+1된다
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;-------------------------------------------------
IF 구상기입체크(100) && GROUPMATCH(TFLAG:전회질내사정, 2, 4) > 0
	PRINTFORMW 「한계가 온듯 하네」
	CALL EFFECT_SHOOT_K8
	PRINTFORMW 「이렇게 하고 있으면 얼마라도 흡수할 수 있을 것 같은 생각이 들어요」
	RETURN 1
ENDIF

;─
;애널내 사정(MASTER→TARGET)
;─
;·A성교 봉사계로 장내 사정하는 경우에 표시
;·TFLAG:106, 뽑지 않고는 뽑지 않는 판정 마다+1된다
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;-------------------------------------------------
IF 구상기입체크(0) && GROUPMATCH(TFLAG:전회질내사정, 6, 8) > 0
ENDIF

;─
;질내 사정(TARGET→MASTER)
;─
;·V성교계로 질내 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;·현재의 사양에서는 조교자가 하늘 사정하는 것은 매우 희(거의 없어?)
;-------------------------------------------------
IF 구상기입체크(0) && GROUPMATCH(TFLAG:전회질내사정, 1, 3) > 0
ENDIF

;─
;애널내 사정(TARGET→MASTER)
;─
;·A성교계로 장내 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;·현재의 사양에서는 조교자가 하늘 사정하는 것은 매우 희(거의 없어?)
;-------------------------------------------------
IF 구상기입체크(0) && GROUPMATCH(TFLAG:전회질내사정, 5, 7) > 0
ENDIF

[SKIPSTART]
부위별의 절정내에서 처리하지 않는 경우는 여기서 보완@/L
;─
;동시 절정
;─
;·조교자와 조교 대상이 함께 절정 하는 경우에 표시
;-------------------------------------------------
IF 구상 기입 체크(0) && TRUECHECK(NOWEX:MASTER:C절정, NOWEX:MASTER:V절정, NOWEX:MASTER:A절정, NOWEX:MASTER:B절정) && TRUECHECK(NOWEX:C절정, NOWEX:V절정, NOWEX:A절정, NOWEX:B절정)
ENDIF
[SKIPEND]

;─
;조교 대상 사정
;─
;·조교 대상이 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 가감(상태)해 사용하는 것이 바람직함
;-------------------------------------------------
IF 구상기입체크(100) && NOWEX:MASTER:사정
	;입안
	IF ((IS_NOWACTNAME("구에서 애무") && TFLAG:ACT파생 == 1) || MC_PLAYER(1) == 1)
		PRINTFORMW 「%RANDLIPS_K8(2)%」
		CALL EFFECT_SHOOT_K8
		PRINTFORMW 「%RANDLIPS_K8(3)%」
		RETURN 1
	;자위
	ELSEIF GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 「……………………」
		CALL EFFECT_SHOOT_K8
		PRINTFORMW 「……」
	;하늘 쏘아 맞히고
	ELSEIF NOWEX:MASTER:사정 == 1
		PRINTFORMW 「비록 정액이 하늘에서도 절정 시켜 버리면 된다」
		CALL EFFECT_SHOOT_K8
		PRINTFORMW 「마력은 확실히 솟아 오르기 시작하고 있으니까」
	;초조하게 해 돌파
	ELSEIF TCVAR:MASTER:초조하게해번 > 1
		PRINTFORMW 「사정을 충분히 초조하게 하는 것에 의해 마
		CALL EFFECT_SHOOT_K8
		PRINTFORMW 「빠르다」
		RETURN 1
	;조루
	ELSEIF NOWEX:MASTER:사정 == 4
		PRINTFORMW 「저것이것 이제(벌써)」
		CALL EFFECT_SHOOT_K8
		PRINTFORMW 「좀 더 걸린다고 생각하고 있었어요」
		RETURN 1
	ELSE
		PRINTFORMW 「뒤는 약간 힘을 더하면」
		CALL EFFECT_SHOOT_K8
		PRINTFORMW 「상당히 좋은 페이스로 진행되고 있는 것이 아닐까」
		RETURN 1
	ENDIF
ENDIF

;─
;조교자 사정
;─
;·조교자가 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;·현재의 사양에서는 조교자가 하늘 사정하는 것은 매우 희(거의 없어?)
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:사정
ENDIF

[SKIPSTART]
부위별로 절정을 나누어 쓰지 않는 경우는 여기서 보완@/L
;─
;조교 대상 절정
;─
;·조교 대상이 절정 하는 경우에 표시
;-------------------------------------------------
IF 구상 기입 체크(0) && TRUECHECK(NOWEX:MASTER:C절정, NOWEX:MASTER:V절정, NOWEX:MASTER:A절정, NOWEX:MASTER:B절정)
ENDIF

;─
;조교자 절정
;─
;·조교자가 절정 하는 경우에 표시
;-------------------------------------------------
IF 구상 기입 체크(0) && TRUECHECK(NOWEX:C절정, NOWEX:V절정, NOWEX:A절정, NOWEX:B절정)
ENDIF
[SKIPEND]

;─
;조교 대상 C절정
;─
;·조교 대상이 C절정 하는 경우에 표시
;·여기에서는 「여자 아이의 C절정」을 상정하고 있다
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:MASTER:C절정
ENDIF

;─
;조교 대상 V절정
;─
;·조교 대상이 V절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:MASTER:V절정
ENDIF

;─
;조교 대상 A절정
;─
;·조교 대상이 A절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:MASTER:A절정
ENDIF

;─
;조교 대상 B절정
;─
;·조교 대상이 B절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:MASTER:B절정
ENDIF

;─
;조교자 C절정
;─
;·조교자가 C절정 하는 경우에 표시
;·여기에서는 「여자 아이의 C절정」을 상정하고 있다
;·사정은 사정 구상의 분기에서 쓰는 것이 바람직함
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:C절정
ENDIF

;─
;조교자 V절정
;─
;·조교자가 V절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:V절정
ENDIF

;─
;조교자 A절정
;─
;·조교자가 A절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:A절정
ENDIF

;─
;조교자 B절정
;─
;·조교자가 B절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:B절정
ENDIF

;─
;조교 대상 물총
;─
;·조교 대상이 물총의 경우에 표시
;·현상, ACT손가락 삽입할 수 있는 때에 G스팟 고문에 파생했을 경우에게만 일어난다
;·주인님의 물총은 없다
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:MASTER:물총
ENDIF

;─
;조교 대상 실금
;─
;·조교 대상이 실금 하는 경우에 표시
;·조교 대상으로 소질:소변버릇이 없으면 발생하지 않는다
;·C절정없이 실금만이라고 하는 것은 드물게 있는 케이스
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:MASTER:방뇨
ENDIF
