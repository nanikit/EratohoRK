;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;병아리 구상 성교 봉사계 액션 구상 파일

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;


;          #
;성교 봉사계 ACT95~103
;          #
[SKIPSTART]
	#TFLAG:ACT
		95=정상위 시킨다
		96=후배위 시킨다
		97=대면좌위 시킨다
		98=배면좌위 시킨다
		99=애널 섹스 시킨다
		103=역강간

	#TFLAG:ACT 분류 == 10에 해당
	#범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "성 봉")로 참조하는 것이 가능
	#원칙으로서 조교 대상→조교자의 삽입이 된다
	#체위의 제휴에 의해 TFLAG:ACT 파생으로 파생한다
	#각 ACT 에 있어서 주도권은@SEX_INITIATIVE에서 정의된다

	관련 변수…
		FIRSTACT
			첫회 ACT 판정 함수
			TFLAG:ACT를 자동으로 참조, 현재 실행되고 있는 ACT 마다 번호가 독립해 보존된다
[SKIPEND]


;/////////////////////
;ACT95, 정상위 시킨다//
;/////////////////////
[SKIPSTART]
	#조교자가 조교 대상으로 정상위에서의 삽입을 명한다
	#조교 대상으로 페니스, 페니스 밴드가 없는 경우는 발생하지 않는다
	#조교자가 페니스 밴드 장착중의 경우는 발생하지 않는다
	#조교 대상이 위로 돌려, 한편 성교 봉사중이 아닌 경우는 발생하지 않는다
	#조교 대상의 발기도가 1000 이하의 경우는 발생하지 않는다
	#밀어 넘어뜨리기중에는 발생하지 않는다
	#조교자가 남자의 경우는 발생하지 않는다
	#조교 대상이 오나홀 사용중의 경우에는 발생하지 않는다
	#성교중으로부터는 파생하지 않는다
	#배면좌위에서의 성교 봉사중으로부터는 파생하지 않는다
	#A성교 봉사중으로부터는 파생하지 않는다
	#주도권은 조교 대상"SEX_INITIATIVE(MASTER)"
	
	*REACT 분류가 "날뛴다", "거부"였던 경우는 캔슬된다
	*조교 대상이 동정이라면 동정 상실이 일어난다
	*조교자가 처녀라면 처녀 상실이 일어난다
	*이것들의 처리는@ACTION_APPLY2_95에서 행해진다
[SKIPEND]
@KOJO_MALE_ACT_K39_95 ;TFLAG:ACT 파생(0=통상/1=연속/2=V성봉으로부터/3=A성교로부터/4=V성교로부터/5=A성봉으로부터)
#LOCALSIZE  1
#LOCALSSIZE 1
VARSET LOCAL
VARSET LOCALS
LOCAL :0  = GET_KFLAG_K39("상사상애", (FLAG:주인님))
LOCALS:0 '= RAND_SPLIT("출/사정")

;성교 봉사계 첫회용 처리
CALL CHK_DEBUG_AND_FIRSTTIME_10_K39

[SKIPSTART]
;조교 대상이 동정
;==============
IF 구상 기입 체크(0) && TALENT:MASTER:동정
	PRINTFORMW 「%CNAME(MASTER, TARGET)%의 처음의 여자가 될 수 있다니…」
	PRINTFORMW 「기쁩니다. 에에, 정말로…」
	RETURN 1
ENDIF
[SKIPEND]

;뽑지 않고 속행
;============
IF TFLAG:뽑지않고
	;범용 처리
	CALL PRINT_KOJO_ACT_GRU10_0
	RETURN 1
ENDIF

;ACT 파생 1:연속
;===============
IF TFLAG:ACT파생 == 1
	;범용 처리
	CALL PRINT_KOJO_ACT_GRU10_1_K39
	RETURN 1
ENDIF

;ACT 파생 2:체위 변경 //다른 V성교 봉사로부터
;===================
IF TFLAG:ACT파생 == 2
	;초조하게 해 안
	;========
	IF TCVAR:MASTER:초조하게해번 > 1
		;상사상애
		IF LOCAL:0
			PRINTFORMW 「후후, 그렇게 안에%LOCALS:0%하고 싶은거야?」
			CALL HTMLPRINTW("「간단하게 가게 할 생각은 없기 때문에, 최대한 노력해%HTML_PS_K39()%」")
		ELSE
			PRINTFORMW 「그렇게 당황하는 일 없다고 생각합니다만, 이제(벌써) 가고 싶어서 견딜 수 없네요. 후후」
			CALL HTMLPRINTW(@"「그렇지만, 조금만 더 참고 받아요%HTML_PS_K39()%」")
		ENDIF
		RETURN 1
	ENDIF

	;초조하게 해 종료
	;==========
	IF TCVAR:MASTER:초조하게해번 == 1
		PRINTFORMW 「\@ LOCAL:0 ? 응응! 저기%RAND_SPLIT("/")% # 좋아요\@, 이대로%LOCALS:0%해」
		PRINTFORMW 「%TNAME_K39()%안,%CNAME(MASTER, TARGET)%로 가득 해\@ !LOCAL:0 ? 주세요# \@…」
		RETURN 1
	ENDIF

	;초조하게 해 없음
	;==========
	IF !TCVAR:MASTER:초조하게해번
		;조교자가 갈 것 같다
		;================
		IF PALAM:쾌V > 8500
			CALL QUAKE(4)
			{
			CALL HTMLPRINTW(
			@"「후앗, 아앗%HTML_AH_K39()%  아, 앗" + 
			@"%HTML_AH_K39()%  개, 이런 건… 이제(벌써), 이제(벌써)!」")
			}
			PRINTFORMDW %CALLNAME:TARGET%는 쾌락에 전율하면서도%CALLNAME:MASTER%의 공격에 맞추어, 격렬하게 허리를 흔들고 있다…
			RETURN 1
		ENDIF

		;상사상애
		;========
		IF LOCAL:0
			;조교 대상이 갈 것 같다
			IF TCVAR:MASTER:갈것같다
				CALL QUAKE(4)
				{
				CALL HTMLPRINTW(
				@"「야%RAND_SPLIT("/")%%HTML_AH_K39()%  팥고물," + 
				@"아%HTML_AH_K39()%  는, 격렬하닷……!」")
				}
				PRINTFORMDW 스스로의 한계를 깨달았다%CALLNAME:MASTER%는%CALLNAME:TARGET%를 껴안아, 허리를 격렬하게 내던졌다…
			ELSE
				CALL HTMLPRINTW(@"「우후%HTML_AH2_K39()%  역시 이 (분)편이 뭔가 안심한다%HTML_PS_K39()%」")
				PRINTFORMW 「좋아, 빨리 와… 이대로 마음껏 범해……」
			ENDIF
			RETURN 1
		ENDIF

		;그 외
		;======
		IF !LOCAL:0
			;조교 대상이 갈 것 같다
			IF TCVAR:MASTER:갈것같다
				CALL QUAKE(4)
				{
				CALL HTMLPRINTW(
				@"「앗%HTML_AH_K39()%  아%RAND_SPLIT("응/")%%HTML_AH_K39()%　" + 
				@"는, 격렬합니닷!   아%HTML_AH_K39()%　%RAND_SPLIT("응/")%……!」")
				}
				PRINTFORMDW 스스로의 한계를 깨달았다%CALLNAME:MASTER%는%CALLNAME:TARGET%를 껴안아, 허리를 격렬하게 내던졌다…
			ELSE
				CALL HTMLPRINTW(@"「%CNAME(MASTER, TARGET)%의 얼굴이 보이면 안심합니다%HTML_PS_K39()%」")
				PRINTFORMW 「자, 좋아요. 이대로 나를 범해 주세요…」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
ENDIF

[SKIPSTART]
A섹스는 구상으로 봉인. 그리고 남자 전용 구상인 것으로 파생 4의 V성교계로부터는 있을 수 없다
;ACT 파생 3, 4 //A성교계, V성교계로부터
;=============
IF GROUPMATCH(TFLAG:ACT 파생, 3, 4) > 0
ENDIF
[SKIPEND]

;ACT 파생 5:체위 변경 //A성교 봉사계로부터
;===================
IF TFLAG:ACT파생 == 5
	;상사상애
	IF LOCAL:0
		PRINTFORMW 「저기, 여기가 안타까운거야…」
		PRINTFORMW 「이번은 전에 삽입할 수 있어…」
	;그 이외
	ELSE
		PRINTFORMW 「나, 여기가 안타까워서…」
		PRINTFORMW 「이번은 전에 삽입할 수 있어 주세요…」
	ENDIF
	RETURN 1
ENDIF

;조교 대상이 갈 것 같다
;==================
IF TCVAR:MASTER:갈것같다
	;범용 처리
	CALL PRINT_KOJO_ACT_GRU10_2_K39
	RETURN 1
ENDIF

;조교자가 정욕
;============
IF T_COND("정욕")
	IF RAND:2
		PRINTL
		PRINTFORMDW %CALLNAME:TARGET%는 약간 억지로%CALLNAME:MASTER%를 끌어 들였다
		PRINTFORMDL 상기 한 그 표정으로부터는 이미 신으로서의 위엄은 없어져
		PRINTFORMDW 다만%CALLNAME:MASTER%를 미칠 듯이 요구하는, 정욕에 물든 한사람의 여자의 모습이 거기에는 있었다…
	ENDIF
	;상사상애
	IF LOCAL:0
		PRINTFORMW 「저기 읏, 빠, 빠르게 삽입할 수 있어!」
		PRINTFORMW 「이, 이제(벌써), 기다릴 수 없는거야!   두 이상해져 버려!」
	;그 외
	ELSE
		PRINTFORMW 「빠르게! 빨리, 삽입할 수 있어 주세욧!」
		PRINTFORMW 「나, 이제 기다릴 수 없습니다!」
	ENDIF
	;쓴 시점에서는 캔슬이 되는 일은 없었다. 뭐 좋지만
	PRINTFORMDW 흘러넘치기 시작하는 애액은%VAGINA_DESCRIPTION2(TARGET)%를 전해져 떨어져 침대 위에 둥근 스며들고를 만들고 있다
	PRINTFORMDW %CALLNAME:MASTER%는%CALLNAME:TARGET%의 요구에 응하는 대로, 질구에 페니스를 주면 단번에 최안쪽까지 꿰뚫었다…
	RETURN 1
ENDIF

;그 외·조건 없음 //범용 ACT에는 패스하지 않는다
;================
IF 구상기입체크(100)
	PRINTL
	PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 손을 잡아 상냥하고 초대 그만둘 수 있었다
	PRINTFORMDW %ANUS_DESCRIPTION(TARGET)%가 드러날수록 양 다리를 크게 넓혀, 그%VAGINA_DESCRIPTION2(TARGET)%를 기대에 실룩거리게 하고 있다…
	;상사상애
	IF LOCAL:0
		CALL HTMLPRINTW(@"「역시 서로의 얼굴이 보이는 것은 좋네요…%HTML_PS_K39()%」")
		PRINTFORMW 「이봐요, 좋아… 망가져 버릴 정도로 마음껏 찔러…」
	;그 외
	ELSE
		PRINTFORMW 「자, 좋아요. 와 주세요…」
	ENDIF
	PRINTFORMDL %CALLNAME:MASTER%는 애액의 방울져 떨어지는 질구로 가볍게 친숙해 지게 한 뒤,
	PRINTFORMDW %CALLNAME:TARGET%의 반응을 물으면서 질벽을 밀어 헤쳐, 페니스를 안쪽으로 진행시켜 간다…
	RETURN 1
ENDIF


;////////////////////
;ACT96, 후배위 시킨다//
;////////////////////
[SKIPSTART]
	#조교자가 조교 대상으로 후배위에서의 삽입을 명한다
	#조교 대상으로 페니스, 페니스 밴드가 없는 경우는 발생하지 않는다
	#조교자가 페니스 밴드 장착중의 경우는 발생하지 않는다
	#조교 대상이 위로 돌려, 한편 성교 봉사중이 아닌 경우는 발생하지 않는다
	#조교 대상의 발기도가 1000 이하의 경우는 발생하지 않는다
	#밀어 넘어뜨리기중에는 발생하지 않는다
	#조교자가 남자의 경우는 발생하지 않는다
	#조교 대상이 오나홀 사용중의 경우에는 발생하지 않는다
	#성교중으로부터는 파생하지 않는다
	#대면좌위에서의 성교 봉사중으로부터는 파생하지 않는다
	#A성교 봉사중으로부터는 파생하지 않는다
	#주도권은 조교 대상"SEX_INITIATIVE(MASTER)"

	*REACT 분류가 "날뛴다", "거부"였던 경우는 캔슬된다
	*조교 대상이 동정이라면 동정 상실이 일어난다
	*조교자가 처녀라면 처녀 상실이 일어난다
	*이것들의 처리는@ACTION_APPLY2_96에서 행해진다
[SKIPEND]

@KOJO_MALE_ACT_K39_96 ;TFLAG:ACT 파생(0=통상/1=연속/2=V성교 봉사로부터/3=A성교로부터/4=V성교로부터/5=A성교 봉사로부터)
;※v1. 020 현재, ACT 파생 3/4/5은 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
#LOCALSIZE  1
#LOCALSSIZE 1
VARSET LOCAL
VARSET LOCALS
LOCAL :0  = GET_KFLAG_K39("상사상애", (FLAG:주인님))
LOCALS:0 '= RAND_SPLIT("출/사정")

;성교 봉사계 첫회용 처리
CALL CHK_DEBUG_AND_FIRSTTIME_10_K39

;뽑지 않고 속행
;============
IF TFLAG:뽑지않고
	;범용 처리
	CALL PRINT_KOJO_ACT_GRU10_0
	RETURN 1
ENDIF

;ACT 파생 1:연속
;===============
IF TFLAG:ACT파생 == 1
	;범용 처리
	CALL PRINT_KOJO_ACT_GRU10_1_K39
	RETURN 1
ENDIF

;ACT 파생 2:체위 변경 //다른 V성교 봉사계로부터
;===================
IF TFLAG:ACT파생 == 2
	;초조하게 해 안
	;========
	IF TCVAR:MASTER:초조하게해번 > 1
		IF LOCAL:0 ;호의, 호감도로 분기
			PRINTFORMW 「후후, 그렇게 안에%LOCALS:0%하고 싶은거야?」
			CALL HTMLPRINTW(@"「간단하게 가게 할 생각은 없기 때문에, 최대한 노력해%HTML_PS_K39()%」")
		ELSE
			PRINTFORMW 「그렇게 당황하는 일 없다고 생각합니다만, 이제(벌써) 가고 싶어서 견딜 수 없네요. 후후」
			CALL HTMLPRINTW(@"「그렇지만, 조금만 더 참고 받아요%HTML_PS_K39()%」")
		ENDIF
		RETURN 1
	ENDIF

	;초조하게 해 종료
	;==========
	IF TCVAR:MASTER:초조하게해번 == 1
		IF LOCAL:0 ;호의, 호감도로 분기
			CALL HTMLPRINTW(@"「응%RAND_SPLIT("응/")%%HTML_AH_K39()%  군요, 이대로%LOCALS:0%해」")
			PRINTFORMW 「%TNAME_K39()%안,%CNAME(MASTER, TARGET)%로 가득 해…」
		ELSE
			PRINTFORMW 「좋아요, 이대로%LOCALS:0%해」
			PRINTFORMW 「나의 안,%CNAME(MASTER, TARGET)%로 가득 해 주세요…」
		ENDIF
		RETURN 1
	ENDIF

	;초조하게 함 없음
	;==========
	IF !TCVAR:MASTER:초조하게해번
		;조교자가 갈 것 같다
		IF PALAM:쾌V >= 8500
			CALL QUAKE(4)
			{
			CALL HTMLPRINTW(
			@"「후앗, 아앗%HTML_AH_K39()%  아%RAND_SPLIT("″/")%," + 
			@"아%HTML_AH_K39()%  개, 이런 건… 이제(벌써), 이제(벌써)!」")
			}
			PRINTFORMDW %CALLNAME:TARGET%는 쾌락에 전율하면서 배후로부터의 공격에 맞추어, 격렬하게 허리를 흔들고 있다…
		;상사상애
		ELSEIF LOCAL:0 && PALAM:쾌V < 8500
			IF TCVAR:MASTER:갈것같다
				CALL QUAKE(4)
				{
				CALL HTMLPRINTW(
				@"「야%RAND_SPLIT("/")%%HTML_AH_K39()%  팥고물," + 
				@"아%HTML_AH_K39()%  는, 격렬하닷……!」")
				}
			ELSE
				PRINTFORMW 「이 모습, 굉장히 흥분해 버리는 것… 이런, 짐승 같은 모습인데…」
				PRINTFORMW 「좋아, 와…… 망가져 버릴 정도로, 마음껏 범해……」
			ENDIF
		;그 이외
		ELSE
			IF TCVAR:MASTER:갈것같다
				CALL QUAKE(4)
				{
				CALL HTMLPRINTW(
				@"「아, 앙%RAND_SPLIT("/")%%HTML_AH_K39()%　" + 
				@"는, 격렬합니닷!   아%HTML_AH_K39()%  응……!」")
				}
			ELSE
				PRINTFORMW 「%CNAME(MASTER, TARGET)%의 얼굴이 안보이면, 조금 불안한 것입니다만…」
				PRINTFORMW 「나, 그런데도 흥분해 버려…… 자신이 억제할 수 없게 될 것 같습니다……」
			ENDIF
		ENDIF
		;특정의 조건의 경우에 지문을 추가 표시한다
		SIF TCVAR:MASTER:갈것같다
			PRINTFORMDW %CALLNAME:MASTER%는 스스로의 한계를 감지해,%CALLNAME:TARGET%의 배후로부터 허리를 격렬하게 내던졌다…
		RETURN 1
	ENDIF
ENDIF

;ACT 파생 5:A성교 봉사계로부터
;===========================
IF TFLAG:ACT파생 == 5
	IF LOCAL:0 ;호의, 호감도로 분기
		PRINTFORMW 「저기, 여기가 안타까운거야…」
		PRINTFORMW 「이번은 전에 삽입할 수 있어…」
	ELSE
		PRINTFORMW 「나, 여기가 안타까워서…」
		PRINTFORMW 「이번은 전에 삽입할 수 있어 주세요…」
	ENDIF
	RETURN 1
ENDIF

;조교 대상이 갈 것 같다
;==================
IF TCVAR:MASTER:갈것같다
	;범용 처리
	CALL PRINT_KOJO_ACT_GRU10_2_K39
	RETURN 1
ENDIF

;조교자가 정욕
;============
IF T_COND("정욕")
	{
	PRINTFORMDL 엉덩이를 높게 올려, 젖는다%VAGINA_DESCRIPTION2(
	TARGET)%를실룩거리게하는음미로운%CALLNAME:TARGET%의모습이%CALLNAME:MASTER%의욕정을부추긴다
	}
	PRINTFORMDL 말 없게 흘러넘치기 시작하는 애액은,%CALLNAME:TARGET%의 넓적다리를 타 떨어져 큰 스며들고를 만들고 있다…
	PRINTL
	IF LOCAL:0 ;호의, 호감도로 분기
		PRINTFORMW 「저기 읏, 초조하게 하지 말고… 빠, 빨리 삽입할 수 있어…!」
		PRINTFORMW 「짐승같이 범햇!%TNAME_K39()%의 보지, 질척질척 휘저엇…!」
	ELSE
		PRINTFORMW 「초조하게 하지 말고… 빠르게! 빨리, 주세욧!」
		PRINTFORMW 「나, 이제 기다릴 수 없습니다!」
	ENDIF
	PRINTFORMDL 그 음미로운님이 흥분했다%CALLNAME:MASTER%는,
	PRINTFORMDW %CALLNAME:TARGET%의 허리의 잘록함을 몹시 거칠게 잡으면, 질구에 준 페니스를 단번에 최안쪽까지 돌진했다…
	RETURN 1
ENDIF

;그 외·조건 없음 //범용 ACT에는 패스하지 않는다
;================
IF 구상기입체크(100)
	;PRINTFORMDL 엉덩이를 높게 올리고 젖는%VAGINA_DESCRIPTION2(TARGET)%를 실룩거리게 하는 음미로운 %CALLNAME:TARGET%의 모습이, %CALLNAME:MASTER%의 욕정을 부추긴다…
	;PRINTL
	IF LOCAL:0 ;호의, 호감도로 분기
		PRINTFORMW 「이봐요,%CNAME(MASTER, TARGET)%뒤로부터 하는 것 좋아하겠지?」
		CALL HTMLPRINTW(@"「저기, 빨리 와… 이제(벌써), 기다릴 수 없는거야…%HTML_PS_K39()%」")
	ELSE
		{
		CALL HTMLPRINTW(
		@"「この格好でされるのって、ちょっと%RAND_SPLIT("
		흥분해버립니다/부끄럽네요")%…%HTML_PS_K39()%」")
		}
		PRINTFORMW 「자, 언제라도 좋아요. 뒤로부터 범해 주세요…」
	ENDIF
	PRINTFORMDL 기대로 가득 찬 표정으로 되돌아 보고 있다%CALLNAME:TARGET%를 슬쩍 봐,%VAGINA_DESCRIPTION2(TARGET)%를 켜 페니스에 애액을 쳐바른다
	PRINTFORMDW 첨단을 삽입해 얕은 부분에서 친숙해 지게 한 뒤, 꿈틀거리는 질벽을 밀어 헤치면서 근원까지 묻었다…
	RETURN 1
ENDIF


;///////////////////////
;ACT97, 대면좌위 시킨다//
;///////////////////////
[SKIPSTART]
	#조교자가 조교 대상으로 대면좌위에서의 삽입을 명한다
	#조교 대상으로 페니스, 페니스 밴드가 없는 경우는 발생하지 않는다
	#조교자가 페니스 밴드 장착중의 경우는 발생하지 않는다
	#조교 대상이 위로 돌려, 한편 성교 봉사중이 아닌 경우는 발생하지 않는다
	#조교 대상의 발기도가 1000 이하의 경우는 발생하지 않는다
	#밀어 넘어뜨리기중에는 발생하지 않는다
	#조교자가 남자의 경우는 발생하지 않는다
	#조교 대상이 오나홀 사용중의 경우에는 발생하지 않는다
	#성교중으로부터는 파생하지 않는다
	#배면좌위에서의 성교 봉사중으로부터는 파생하지 않는다
	#A성교 봉사중으로부터는 파생하지 않는다
	#주도권은 조교 대상"SEX_INITIATIVE(MASTER)"

	*REACT 분류가 "날뛴다", "거부"였던 경우는 캔슬된다
	*조교 대상이 동정이라면 동정 상실이 일어난다
	*조교자가 처녀라면 처녀 상실이 일어난다
	*이것들의 처리는@ACTION_APPLY2_97에서 행해진다
[SKIPEND]
@KOJO_MALE_ACT_K39_97 ;TFLAG:ACT 파생(0=통상/1=연속/2=V성교 봉사계로부터/3=A성교계로부터/4=V성교계로부터/5=A성교 봉사계로부터)
#LOCALSIZE  1
#LOCALSSIZE 1
VARSET LOCAL
VARSET LOCALS
LOCAL :0  = GET_KFLAG_K39("상사상애", (FLAG:주인님))
LOCALS:0 '= RAND_SPLIT("출/사정")

;성교 봉사계 첫회용 처리
CALL CHK_DEBUG_AND_FIRSTTIME_10_K39

;뽑지 않고 속행
;============
IF TFLAG:뽑지않고
	;범용 처리
	CALL PRINT_KOJO_ACT_GRU10_0
	RETURN 1
ENDIF

;ACT 파생 1:연속
;===============
IF TFLAG:ACT파생 == 1
	;범용 처리
	CALL PRINT_KOJO_ACT_GRU10_1_K39
	RETURN 1
ENDIF

;ACT 파생 2:체위 변경 //다른 V성교 봉사계로부터
;===================
IF TFLAG:ACT파생 == 2
	;초조하게 해 안
	;========
	IF TCVAR:MASTER:초조하게해번 > 1
		IF LOCAL:0 ;호의, 호감도로 분기
			PRINTFORMW 「후후, 그렇게 안에 내고 싶은거야?」
			CALL HTMLPRINTW(@"「간단하게 가게 할 생각은 없기 때문에, 최대한 노력해%HTML_PS_K39()%」")
		ELSE
			PRINTFORMW 「그렇게 당황하는 일 없다고 생각합니다만, 이제(벌써) 가고 싶어서 견딜 수 없네요」
			CALL HTMLPRINTW(@"「그렇지만, 조금만 더 참고 받아요%HTML_PS_K39()%」")
		ENDIF
		RETURN 1
	ENDIF

	;초조하게 해 종료
	;==========
	IF TCVAR:MASTER:초조하게해번 == 1
		IF LOCAL:0 ;호의, 호감도로 분기
			PRINTFORMW 「%KOJO_RAND_K39(("키스"), 1)%  응, 이대로 안에%LOCALS:0%해」
			PRINTFORMW 「얼싸안은 채로,%TNAME_K39()%안에 쏟아…」
		ELSE
			PRINTFORMW 「좋아요, 이대로%LOCALS:0%해」
			PRINTFORMW 「나의 안,%CNAME(MASTER, TARGET)%로 가득 해 주세요…」
		ENDIF
		RETURN 1
	ENDIF

	;초조하게 해 없음
	;==========
	IF !TCVAR:MASTER:초조하게해번
		;조교자가 갈 것 같다
		IF PALAM:쾌V > 8500
			CALL QUAKE(4)
			{
			CALL HTMLPRINTW(
			@"「후앗%HTML_AH_K39()%  아%HTML_AH_K39()%　" + 
			@"아, 앗%HTML_AH_K39()%  개, 이런 건… 이제(벌써), 이제(벌써)!」")
			}
			PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%에 제대로 매달리면서, 격렬하게 허리를 흔들고 있다…
		;상사상애 또한 조교자의 쾌V가 8500 이하
		ELSEIF LOCAL:0 && PALAM:쾌V < 8500
			;조교 대상이 갈 것 같다
			IF TCVAR:MASTER:갈것같다
				CALL QUAKE(4)
				{
				CALL HTMLPRINTW(
				@"「야앗%HTML_AH_K39()%   팥고물%HTML_AH_K39()%　" + 
				@"아%HTML_AH_K39()%  는, 격렬하닷……!」")
				}
				PRINTFORMDW 스스로의 한계를 깨달았다%CALLNAME:MASTER%는%CALLNAME:TARGET%를 껴안으면서, 허리를 격렬하게 밀어올렸다…
			ELSE
				CALL HTMLPRINTW(@"「우후, 얼굴이 가깝고 조금 두근두근 해 버린다…%HTML_PS_K39()%」")
				PRINTFORMW 「저기, 키스 해. 강하게 안아. 마음껏, 밀어올려…\@ TENSION(TARGET, 8) == "높다" ?!# \@」
			ENDIF
		;그 외
		ELSE
			;조교 대상이 갈 것 같다
			IF TCVAR:MASTER:갈것같다
				CALL QUAKE(4)
				{
				CALL HTMLPRINTW(
				@"「앗%HTML_AH_K39()%  팥고물%HTML_AH_K39()%　" + 
				"는, 격렬합니닷!   아,   응……!」")
				}
				;PRINTFORMDW 스스로의 한계를 깨달은 %CALLNAME:MASTER%는 %CALLNAME:TARGET%를 껴안으면서, 허리를 격렬하게 밀어올렸다…
			ELSE
				{
				CALL HTMLPRINTW(
				@"「%CNAME(MASTER, TARGET)%가 매우 근처에 느껴져," + 
				"조금 두근두근 해 버립니다%HTML_PS_K39()%」")
				}
				PRINTFORMW 「자, 확실히 안고 있어 주세요……」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;ACT 파생 5:A성교 봉사계로부터
;===========================
IF TFLAG:ACT파생 == 5
	IF LOCAL:0 ;호의, 호감도로 분기
		PRINTFORMW 「저기, 여기가 안타까운거야…」
		PRINTFORMW 「이번은 전에 삽입할 수 있어…」
	ELSE
		PRINTFORMW 「나, 여기가 안타까워서…」
		PRINTFORMW 「이번은 전에 삽입할 수 있어 주세요…」
	ENDIF
	;조교 대상이 갈 것 같은 경우
	SIF TCVAR:MASTER:갈것같다
		PRINTFORMW 「그렇지만%CNAME(MASTER, TARGET)%는 이제 한계같다\@ !LOCAL:0 ? 입니다# \@군요? 후후」
	RETURN 1
ENDIF

;조교 대상이 갈 것 같다
;==================
IF TCVAR:MASTER:갈것같다
	;범용 처리
	CALL PRINT_KOJO_ACT_GRU10_2_K39
	RETURN 1
ENDIF

;조교자가 정욕
;============
IF T_COND("정욕")
	{
	CALL HTMLPRINTL(
	@"「읏%HTML_AH_K39()%  … 응쿠," + 
	@"\@ LOCAL:0 ? 응 빠르게! 빨리 삽입할 수 있엇…# 빠르게! 빨리 삽입할 수 있어 주세욧\@！」")
	}
	IF LOCAL:0 ;호의, 호감도로 분기
		PRINTFORMW 「이제 기다릴 수 없는거야! 얼싸안은 채로, 마음껏 찔러…」
	ELSE
		PRINTFORMW 「나, 이제 인내 할 수 없어서…」
	ENDIF
	PRINTFORMDW 정욕에 끓어오른다%CALLNAME:TARGET%는%CALLNAME:MASTER%의 몸에 손발을 걸어, 삽입을 애타게 기다리고 있다…
	RETURN 1
ENDIF

;그 외·조건 없음 //범용 ACT에는 패스하지 않는다
;================
IF 구상기입체크(100) ;100%
	IF LOCAL:0 ;호의, 호감도로 분기
		CALL HTMLPRINTW(@"「다른 것도 싫지 않지만, 서로 사랑하고 있는 느낌이 들어 이것이 제일 좋아%HTML_PS_K39()%」")
	ELSE
		CALL HTMLPRINTL(@"「우후, 이 모습 정말 좋아합니다%HTML_PS_K39()%」")
		PRINTFORMW 「확실히 안으면서 찔러 주세요」
	ENDIF
	PRINTFORMDW %CALLNAME:TARGET%는 응석부리도록(듯이)%CALLNAME:MASTER%의 몸에 손발을 걸어 왔다…
	RETURN 1
ENDIF


;//////////////////////
;ACT98, 배면좌위 시킨다//
;//////////////////////
[SKIPSTART]
	#조교자가 조교 대상으로 배면좌위에서의 삽입을 명한다
	#조교 대상으로 페니스, 페니스 밴드가 없는 경우는 발생하지 않는다
	#조교자가 페니스 밴드 장착중의 경우는 발생하지 않는다
	#조교 대상이 위로 돌려, 한편 성교 봉사중이 아닌 경우는 발생하지 않는다
	#조교 대상의 발기도가 1000 이하의 경우는 발생하지 않는다
	#밀어 넘어뜨리기중에는 발생하지 않는다
	#조교자가 남자의 경우는 발생하지 않는다
	#조교 대상이 오나홀 사용중의 경우에는 발생하지 않는다
	#성교중으로부터는 파생하지 않는다
	#정상위, 대면좌위에서의 성교 봉사중으로부터는 파생하지 않는다
	#A성교 봉사중으로부터는 파생하지 않는다
	#주도권은 조교 대상"SEX_INITIATIVE(MASTER)"

	*REACT 분류가 "날뛴다", "거부"였던 경우는 캔슬된다
	*조교 대상이 동정이라면 동정 상실이 일어난다
	*조교자가 처녀라면 처녀 상실이 일어난다
	*이것들의 처리는@ACTION_APPLY2_98에서 행해진다
[SKIPEND]
@KOJO_MALE_ACT_K39_98 ;TFLAG:ACT 파생(0=통상/1=연속/2=V성교 봉사계로부터/3=A성교계로부터/4=V성교계로부터/5=A성교 봉사계로부터)
#LOCALSIZE 1
#LOCALSSIZE 1
VARSET LOCAL
VARSET LOCALS
LOCAL:0  = GET_KFLAG_K39("상사상애", (FLAG:주인님))
LOCALS:0 '= RAND_SPLIT("출/사정")

;성교 봉사계 첫회용 처리
CALL CHK_DEBUG_AND_FIRSTTIME_10_K39

;뽑지 않고 속행
;============
IF TFLAG:뽑지않고
	CALL PRINT_KOJO_ACT_GRU10_0
	RETURN 1
ENDIF

;ACT 파생 1:연속
;===============
IF TFLAG:ACT파생 == 1
	CALL PRINT_KOJO_ACT_GRU10_1_K39
	RETURN 1
ENDIF

;ACT 파생 2:체위 변경 //다른 V성교 봉사계로부터
;===================
IF TFLAG:ACT파생 == 2
	;초조하게 해 안
	;========
	IF TCVAR:MASTER:초조하게해번 > 1
		IF LOCAL:0 ;호의, 호감도로 분기
			PRINTFORMW 「후후, 그렇게 안에%LOCALS:0%하고 싶은거야?」
			CALL HTMLPRINTW(@"「간단하게 가게 할 생각은 없기 때문에, 최대한 노력해%HTML_PS_K39()%」")
		ELSE
			PRINTFORMW 「그렇게 당황하는 일 없다고 생각합니다만, 이제(벌써) 가고 싶어서 견딜 수 없네요」
			CALL HTMLPRINTW(@"「그렇지만, 조금만 더 참고 받아요%HTML_PS_K39()%」")
		ENDIF
		RETURN 1
	ENDIF

	;초조하게 해 종료
	;=========
	IF TCVAR:MASTER:초조하게해번 == 1
		IF LOCAL:0 ;호의, 호감도로 분기
			PRINTFORMW 「%KOJO_RAND_K39(("키스"), 1)%  이대로 안에%LOCALS:0%해」
			PRINTFORMW 「뒤로부터 안은 채로,%TNAME_K39()%안에 쏟아…」
		ELSE
			PRINTFORMW 「좋아요, 이대로%LOCALS:0%해」
			PRINTFORMW 「나의 안,%CNAME(MASTER, TARGET)%로 가득 해 주세요…」
		ENDIF
		RETURN 1
	ENDIF

	;초조하게 해 없음
	;==========
	IF !TCVAR:MASTER:초조하게해번
		;조교자가 갈 것 같다
		IF PALAM:쾌V > 8500
			CALL QUAKE(4)
			{
			CALL HTMLPRINTW(
			@"「후앗%HTML_AH_K39()%  아%HTML_AH_K39()%　" + 
			@"아, 앗%HTML_AH_K39()%  개, 이런 건… 이제(벌써), 이제(벌써)!」")
			}
			{
			PRINTFORMDW 
			%CALLNAME:TARGET%는쾌락에전율하면서、%CALLNAME:
			MASTER%의공격에맞추고、격렬하게허리를흔들고있다…
			}
		;상사상애 또한 조교자가 갈 것 같지 않는 경우
		ELSEIF LOCAL:0 && PALAM:쾌V < 8500
			IF TCVAR:MASTER:갈것같다
				CALL QUAKE(4)
				{
				CALL HTMLPRINTW(
				@"「야%RAND_SPLIT("/")%! 아읏%HTML_AH_K39()%　" + 
				@"아%RAND_SPLIT("/")%%HTML_AH_K39()%  는, 벗길 수 있는 해……!」")
				}
				;PRINTFORMDL 스스로의 한계를 깨달은 %CALLNAME:MASTER%는 %CALLNAME:	TARGET%의 양팔을 잡아, 난폭하게 질내를 마구 후벼팠다…
				;PRINTFORMDW %CALLNAME:TARGET%는 소리가 되지 않는 교성을 올려, 힘 없고, 다만 격렬한 공격을 받아 들이고 있다…
			ELSE
				CALL HTMLPRINTW(@"「이 모습은 장난감으로 되고 있는 것 같아, 조금 흥분해 버린다…%HTML_PS_K39()%」")
				PRINTFORMW 「좋아…%CNAME(MASTER, TARGET)%가 좋아하는 것 같게해. 망가져 버릴 정도로 격렬하게 범해……」
			ENDIF
		;그 이외
		ELSE
			IF TCVAR:MASTER:갈것같다
				CALL QUAKE(4)
				{
				CALL HTMLPRINTW(
				@"「앗%HTML_AH_K39()%  팥고물%RAND_SPLIT("/")%…" + 
				@"%HTML_AH_K39()%  는, 격렬합니닷… 앗%HTML_AH_K39()%  응……!」")
				}
				;PRINTFORMDL 스스로의 한계를 깨달은 %CALLNAME:MASTER%는 %CALLNAME:TARGET%의 양팔을 잡아, 난폭하게 질내를 마구 후벼팠다…
				;PRINTFORMDW %CALLNAME:TARGET%는 소리가 되지 않는 교성을 올려, 힘 없고, 다만 격렬한 공격을 받아 들이고 있다…
			ELSE
				CALL HTMLPRINTW(@"「이 모습은 뭔가 굉장히 추잡하네요%HTML_PS_K39()%」")
				PRINTFORMW 「자, 좋아요. 이대로 나를 범해 주세요…」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;ACT 파생 5:A성교 봉사계로부터
;===========================
IF TFLAG:ACT파생 == 5
	IF LOCAL:0 ;호의, 호감도로 분기
		PRINTFORML 「저기, 여기가 안타까운거야…」
		PRINTFORMW 「이번은 전에 삽입할 수 있어…」
		SIF TCVAR:MASTER:갈것같다
			CALL HTMLPRINTW(@"「그렇지만…,%CNAME(MASTER, TARGET)%는 삽입할 수 있었던 순간에 가 버릴 것 같지만. 후후…%HTML_PS_K39()%」")
	ELSE
		PRINTFORML 「나, 여기가 안타까워서…」
		PRINTFORMW 「이번은 전에 삽입할 수 있어 주세요…」
		SIF TCVAR:MASTER:갈것같다
			CALL HTMLPRINTW(@"「그렇지만%CNAME(MASTER, TARGET)%는 이제 한계같네요? 후후%HTML_PS_K39()%」")
	ENDIF
	RETURN 1
ENDIF

;조교 대상이 갈 것 같다
;==================
IF TCVAR:MASTER:갈것같다
	;범용 처리
	CALL PRINT_KOJO_ACT_GRU10_2_K39
	RETURN 1
ENDIF

;조교자가 정욕
;============
IF T_COND("정욕")
	IF LOCAL:0 ;호의, 호감도로 분기
		PRINTFORMW 「저기 읏, 빠, 빠르게 삽입할 수 있어!」
		PRINTFORMW 「%TNAME_K39()%의 보지, 엉망진창으로 찔러……!」
	ELSE
		PRINTFORMW 「빠르게! 빨리 삽입할 수 있어 주세욧!」
		PRINTFORMW 「나, 이제 기다릴 수 없습니다!」
	ENDIF
	PRINTFORMDW 정욕에 끓어오른다%CALLNAME:TARGET%는%CALLNAME:MASTER%의 몸에 손발을 걸어, 삽입을 애타게 기다리고 있다…
	RETURN 1
ENDIF

;그 외·조건 없음 //범용 ACT에는 패스하지 않는다
;================
IF 구상기입체크(100)
	IF LOCAL:0 ;호의, 호감도로 분기
		PRINTFORML 「이렇게 해 뒤로부터 껴안을 수 있고 하는 것 좋아…」
		PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 손을 잡아, 스스로의 유방에 이끌었다…
		PRINTFORMW 「이봐요, 좋을대로 만지작거려… 나의 신체는 전부%CNAME(MASTER, TARGET)%의 물건이야?」
	ELSE
		SELECTCASE RANDACT(2, 50)
			CASE 0
				PRINTFORMW 「뒤로부터 안으면서 시라고 주세요…」
			CASE 1
				PRINTFORMW 「나의 몸, 좋을대로 만지작거려 주세요…」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF


;/////////////////////////////
;ACT99, 애널 섹스 시킨다//
;/////////////////////////////
[SKIPSTART]
	#조교자가 조교 대상으로 아누스에의 삽입을 명한다
	#조교 대상으로 페니스, 페니스 밴드가 없는 경우는 발생하지 않는다
	#조교자가 페니스 밴드 장착중의 경우는 발생하지 않는다
	#조교 대상이 위로 돌려, 한편 성교 봉사중이 아닌 경우는 발생하지 않는다
	#조교 대상의 발기도가 1000 이하의 경우는 발생하지 않는다
	#턴 방침이 "소프트"로 A성교 봉사중이 아닌 경우는 발생하지 않는다
	#밀어 넘어뜨리기중에는 발생하지 않는다
	#조교자가 남자의 경우는 발생하지 않는다
	#조교 대상이 오나홀 사용중의 경우에는 발생하지 않는다
	#성교중으로부터는 파생하지 않는다
	#V성교 봉사중으로부터는 파생하지 않는다
	#조교자의 A감각이 2 이하의 경우는 발생하지 않는다
	#주도권은 조교 대상"SEX_INITIATIVE(MASTER)"
	#REACT 분류가 "날뛴다", "거부"였던 경우는 캔슬된다
[SKIPEND]
@KOJO_MALE_ACT_K39_99 ;TFLAG:ACT 파생(0=통상/1=연속/2=A성교계로부터/3=V성교 봉사계로부터/4=V성교계로부터)
#LOCALSIZE  1
#LOCALSSIZE 1
VARSET LOCAL
VARSET LOCALS
LOCAL :0  = GET_KFLAG_K39("상사상애", (FLAG:주인님))
LOCALS:0 '= RAND_SPLIT("출/사정")

;성교 봉사계 첫회용 처리
;====================
IF !FLAG:오토모드 && FIRSTTIME(195)
	;성교 봉사 판정에 대입
	KFLAG:TARGET = 1
ELSEIF FLAG:오토모드 && FLAG:오토모드표시설정 == 0 && FIRSTTIME(196)
	;오토 모드시 용 처리
	CALL PRINT_RAINBOW2W("첫회", 20)
	;데이터 로드 처리
	CALL GAME_OVER_K39
	FORCEWAIT
	RETURN 1
ELSE
	;그 외라면 처리 없음
ENDIF

;뽑지 않고 속행
;============
IF TFLAG:뽑지않고
	CALL PRINT_KOJO_ACT_GRU10_0
	RETURN 1
ENDIF

;ACT 파생 1:연속
;===============
IF TFLAG:ACT파생 == 1
	CALL PRINT_KOJO_ACT_GRU10_1_K39
	RETURN 1
ENDIF

;ACT 파생 2~4 //기본적으로 상정하고 있는 것은 ACT 파생 3
;============= //발생 할 수 있는 것도 ACT 파생 3만일 것이지만, 조건은 일단 커버해 두는적인 저것
IF INRANGE(TFLAG:ACT파생, 2, 4)
	;A감각 4이상
	;===========
	IF ABL:TARGET:A감각 >= 4
		{
		PRINTFORMDW 
		%CALLNAME:TARGET%는자포자기하고잘랐다%ANUS_DESCRIPTION(
		TARGET)%를과시하고、%CALLNAME:MASTER%의욕정을부추기고있다…
		}
		IF LOCAL:0 ;호의, 호감도로 분기
			PRINTFORML 「저기, 봐… 갖고 싶어서, 이렇게 흠칫흠칫 해 버리고 있어…」
			PRINTFORMW 「엉덩이로 하는 것을 좋아하는 신님 같은거 최악이어요… 그렇지만, 인내 할 수 없는거야…」
		ELSE
			PRINTFORML 「저… 뒤가 안타깝습니다… 이번은 엉덩이로 시라고 주세요」
			PRINTFORMW 「나를 이런 식으로 한 책임, 제대로 취해 주세요…」
		ENDIF
		[SKIPSTART]
		PRINTFORMDL %CALLNAME:MASTER%는 %CALLNAME:TARGET%의 질구로부터 흘러넘치는 애액을 건져 올려, 페니스에 바르면
		PRINTFORMDW 입구에서 가볍게 친숙해 지게 한 뒤, 단번에 최안쪽까지 찔렀다…
		[SKIPEND]
		RETURN 1
	ENDIF

	;아직 친숙해 지지 않았다
	;================
	IF ABL:TARGET:A감각 < 4
		PRINTFORMDW %CALLNAME:TARGET%는 부끄러운 듯이%CALLNAME:MASTER%의 눈앞에%ANUS_DESCRIPTION(TARGET)%를 쬐고 있다…
		IF LOCAL:0 ;호의, 호감도로 분기
			PRINTFORML 「이번은 엉덩이에 줘…」
			PRINTFORMW 「아직 여기는 익숙해지지 않는거야. 부탁해요, 천천히 시라고…」
		ELSE
			PRINTFORML 「이번은 뒤로부터 해 주세요」
			PRINTFORMW 「아직 익숙해지지 않기 때문에, 천천히 부탁드릴게요…」
		ENDIF
		;PRINTFORMDL %CALLNAME:MASTER%는 %CALLNAME:TARGET%의 질구로부터 손가락끝에 애액을 건져 올려, 2 개의 손가락으로 장내를 가볍게 휘저은 뒤
		;PRINTFORMDW 충분히 애액을 쳐바른 페니스를 장벽의 저항을 느끼면서, 신중하게 안쪽으로 진행해 갔다…
		RETURN 1
	ENDIF
ENDIF

;조교 대상이 갈 것 같다
;==================
IF TCVAR:MASTER:갈것같다
	IF LOCAL:0 ;호의, 호감도로 분기
		CALL HTMLPRINTW(@"「우후%HTML_AH2_K39()%  당장 폭발할 것 같구나?」")
		PRINTFORMW 「이봐요, 당황하지 마. 천천히, 신중하게 말야」
	ELSE
		PRINTFORMW 「그 모습이라고, 곧바로 가 버릴 것 같네요」
		CALL HTMLPRINTW(@"「그렇지만, 조금은 즐겁게 해 주세요? 기대하고 있으니까%HTML_PS_K39()%」")
	ENDIF
	PRINTFORMDW %CALLNAME:TARGET%는 사정 직전의 페니스가 폭발하지 않게 괄약근을 느슨한,%ANUS_DESCRIPTION(TARGET)%의 최안쪽으로 이끌었다…
	RETURN 1
ENDIF

;조교자가 정욕
;============
IF T_COND("정욕")
	PRINTFORMDL %CALLNAME:TARGET%는 끓어오르는 정욕을 억제하지 않고, 스스로 끈적한 애액을 아누스에 쳐바름
	PRINTFORMDW %CALLNAME:MASTER%의 삽입을 재촉했다…
	IF LOCAL:0 ;호의, 호감도로 분기
		PRINTFORMW 「저기 읏, 빠, 빠르게 삽입할 수 있어!」
		PRINTFORMW 「엉덩이가 쑤셔, 이상해져 버리는……!」
	ELSE
		PRINTFORMW 「빠르게! 빨리, 삽입할 수 있어 주세욧!」
		PRINTFORMW 「엉덩이가 안타까워서, 기다릴 수 없습니다!」
	ENDIF
	;PRINTFORMDL %CALLNAME:MASTER%는 %CALLNAME:TARGET%의 질구로 페니스에 애액을 친숙해 지게 한 후, 그것을 전체에 펴바름
	;PRINTFORMDW 첨단을 주면, 기대에 실룩거리는 아누스를 단번에 최안쪽까지 관철했다…
	RETURN 1
ENDIF

;그 외·조건 없음
;================
IF 구상기입체크(100)
	;A감각 4이상
	;===========
	IF ABL:TARGET:A감각 >= 4
		PRINTFORMDW %CALLNAME:TARGET%는 자포자기하고 자른 아누스를 과시해,%CALLNAME:MASTER%의 욕정을 부추기고 있다…
		PRINTL
		IF LOCAL:0 ;호의, 호감도로 분기
			PRINTFORMW 「저기, 이제(벌써) 준비 되어있기 때문에…」
			PRINTFORMW 「좋아… 사양하지 말아줘. 마음껏 찔러…」
		ELSE
			PRINTFORMW 「나는 언제라도 좋아요…」
			PRINTFORMW 「사양말고, 마음껏 찔러 주세요…」
		ENDIF
		;PRINTFORMDL %CALLNAME:MASTER%는 %CALLNAME:TARGET%의 질구로부터 흘러넘치는 애액을 건져 올려, 페니스에 바르고
		;PRINTFORMDW 첨단을 아누스에 주면, 단번에 최안쪽까지 찔렀다…
		RETURN 1
	ENDIF

	;아직 친숙해 지지 않았다
	;================
	IF ABL:TARGET:A감각 < 4
		PRINTFORMDW %CALLNAME:TARGET%는 부끄러운 듯이%CALLNAME:MASTER%의 눈앞에 아누스를 쬐고 있다…
		IF LOCAL:0 ;호의, 호감도로 분기
			PRINTFORMW 「그렇게 볼 수 있으면, 조금 부끄럽다…」
			PRINTFORMW 「그렇지만, 좋아…… 와…」
		ELSE
			PRINTFORMW 「그렇게 차분히 볼 수 있으면 부끄럽습니다…」
			PRINTFORMW 「그렇지만, 좋아요. 와 주세요…」
		ENDIF
		;PRINTFORMDL %CALLNAME:MASTER%는 %CALLNAME:TARGET%의 질구로부터 손가락끝에 애액을 건져 올려, 2 개의 손가락으로 장내를 가볍게 휘저은 뒤
		;PRINTFORMDW 충분히 애액을 쳐바른 페니스를 장벽의 저항을 느끼면서, 신중하게 안쪽으로 진행해 갔다…
		RETURN 1
	ENDIF
ENDIF


;//////////////////
;ACT103, 역강간//
;//////////////////
[SKIPSTART]
	#조교자가 승마위로 억지로 성교에 이른다
	#조교 대상으로 페니스, 페니스 밴드가 없는 경우는 발생하지 않는다
	#조교자가 페니스 밴드 장착중의 경우는 발생하지 않는다
	#조교 대상의 발기도가 1000 이하의 경우는 발생하지 않는다
	#조교자가 남자의 경우는 발생하지 않는다
	#조교 대상이 오나홀 사용중의 경우에는 발생하지 않는다
	#성교중으로부터는 파생하지 않는다
	#A성교 봉사중으로부터는 파생하지 않는다
	#주도권은 조교 대상"SEX_INITIATIVE(TARGET)"

	*다른 성교 봉사계와 달리, 조교 대상은 저항 할 수 없다
	*조교 대상이 동정이라면 동정 상실이 일어난다
	*조교자가 처녀라면 처녀 상실이 일어난다
	*이것들의 처리는@ACTION_APPLY2_103에서 행해진다
[SKIPEND]
@KOJO_MALE_ACT_K39_103 ;TFLAG:ACT 파생(0=통상/1=연속/2=V성교 봉사계로부터/3=A성교계로부터/4=V성교계로부터/5=A성교 봉사계로부터)
#LOCALSIZE 1
#LOCALSSIZE 1
VARSET LOCAL
VARSET LOCALS
LOCAL:0  = GET_KFLAG_K39("상사상애", (FLAG:주인님))
LOCALS:0 '= RAND_SPLIT("출/사정")

;성교 봉사계 첫회용 처리
CALL CHK_DEBUG_AND_FIRSTTIME_10_K39

;성교계로부터 파생하면 0을 돌려준다
SIF INRANGE(TFLAG:ACT파생, 3, 4)
	RETURN 0

[SKIPSTART]
;첫회
;====
IF FIRSTACT(1)
	IF TALENT:MASTER:동정
		PRINTFORML 「처음은 구멍으로부터 요구하기를 원했지만… 나, 이제 인내 할 수 없습니다…」
		PRINTFORMW 「그렇지만,%CNAME(MASTER, TARGET)%의 처음의 여자가 될 수 있는 것이 기쁜 것은 사실이에요…」
	ELSE
		PRINTFORMW 「두 명의 최초가 이런 형태로 미안해요…」
		PRINTFORMW 「나, 이제 인내 할 수 없어서…」
	ENDIF
	;성교 봉사 판정에 대입
	KFLAG:TARGET = 1
	RETURN 1
ENDIF
[SKIPEND]

;뽑지 않고 속행 //역강간은 범용 처리는 사용하지 않는다
;============
IF TFLAG:뽑지않고
	CALL QUAKE(4)
	IF LOCAL:0 ;호의, 호감도로 분기
		SELECTCASE RANDACT(2, 10)
			CASE 0
				CALL HTMLPRINTW(@"「앗%HTML_AH_K39()%  아%HTML_AH_K39()%  나… 어째서, 이렇게……!」")
				CALL QUAKE(4)
				CALL HTMLPRINTW(@"「흥분이, 멈추지 않고… 테에…!   아…%HTML_PS2_K39()%」")
			CASE 1
				{
				CALL HTMLPRINTW(
				@"「응읏%HTML_AH_K39()%  아%HTML_AH_K39()%　" + 
				@"아%HTML_AH_K39()%  중이…… 스쳐, 라고……!」")
				}
		ENDSELECT
	ELSE
		SELECTCASE RANDACT(2, 11)
			CASE 0
				{
				CALL HTMLPRINTW(
				@"「읏%HTML_AH_K39()%  응%HTML_AH_K39()%  아" + 
				@"%HTML_AH_K39()%  좋습니닷, 좀 더 밀어올려…」")
				}
				CALL QUAKE(4)
				PRINTFORMW 「나, 굉장히 흥분해 버려…… 멈추지 않고…… 응앗…!」
			CASE 1
				{
				CALL HTMLPRINTW(
				@"「응앗%HTML_AH_K39()%  팥고물" + 
				@"%HTML_AH_K39()%  나, 이런 일 해… 에서도, 멈추어지지 않아서……」")
				}
		ENDSELECT
	ENDIF
	;조교자의 쾌V가 일정 이상
	SIF PALAM:TARGET:쾌V > 8500
		PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER% 위에서 격렬하게 허리를 거절해 쾌락을 탐내고 있다…
	RETURN 1
ENDIF

;ACT 파생 1:연속
;===============
IF TFLAG:ACT파생 == 1
	;초조하게 해 안
	;========
	IF TCVAR:MASTER:초조하게해번 > 1
		IF LOCAL:0 ;호의, 호감도로 분기
			PRINTFORMW 「이봐요, 이렇게 해 차분히 희롱해지는 것도 나쁘지 않지요?」
			CALL HTMLPRINTW(@"「액신의 허리사용, 충분히 즐겨%HTML_PS_K39()%」")
	 	ELSE
	 		PRINTFORMW 「서로의 감촉이 자주(잘) 전해져, 이런 것도 좋네요…」
	 		CALL HTMLPRINTW(@"「나의 허리사용, 차분히 맛보여 받아요%HTML_PS_K39()%」")
	 	ENDIF
		RETURN 1
	ENDIF

	;초조하게 해 종료
	;==========
	IF TCVAR:MASTER:초조하게해번 == 1
	 	IF LOCAL:0 ;호의, 호감도로 분기
			CALL HTMLPRINTW(@"「응%RAND_SPLIT("응/")%%HTML_AH_K39()%  이것으로 좋을까」")
	 		PRINTFORMW 「이봐요, 자궁의 입구에 해당되고 있는 것 알아?」
	 		PRINTFORMW 「이대로%LOCALS:0%해… 자궁의 안,%CNAME(MASTER, TARGET)%로 가득 해…」
	 	ELSE
			PRINTFORMW 「이것으로, 좋을까요」
			PRINTFORMW 「압니까?%CNAME(MASTER, TARGET)%의 것이 자궁의 입구까지 도착해 있어」
	 		PRINTFORMW 「자, 좋아요… 나의 가장 안쪽에%LOCALS:0%해 주세요…」
	 	ENDIF
		RETURN 1
	ENDIF

	;초조하게 함 없음
	;==========
	IF !TCVAR:MASTER:초조하게해번
		CALL QUAKE(4)
		IF LOCAL:0 ;호의, 호감도로 분기
			;조교 대상이 정욕
			IF M_COND("정욕")
				CALL HTMLPRINTW(@"「뭐, 뭐야 이것…… 이런 것 모른닷%HTML_AH_K39()%  모르닷!」")
				CALL QUAKE(4)
				{
				CALL HTMLPRINTW(
				@"「%RAND_SPLIT("")%%HTML_AH_K39()%  아" + 
				@"%HTML_AH_K39()%  이상하고, 이상해져 버린다앗…!」")
				}
			ELSE
				SELECTCASE RANDACT(2, 20)
					CASE 0
						{
						CALL HTMLPRINTW(
						@"「아읏%HTML_AH_K39()%  나, 기분 좋아서…" + 
						@"멈추지 않게 되어 버린다……%HTML_PS2_K39()%」")
						}
						CALL QUAKE(4)
						{
						CALL HTMLPRINTW(
						@"「어째서!   개, 이렇게%RAND_SPLIT("/")%… 느껴 버려!   " + 
						@"아%HTML_AH_K39()%  응…%HTML_PS2_K39()%」")
						}
					CASE 1
						CALL HTMLPRINTW(@"「앗%HTML_AH_K39()%  나%HTML_AH_K39()%  격렬하닷!   격렬해…!」")
						{
						CALL HTMLPRINTW(
						@"「안돼엣%HTML_AH_K39()%  그렇게, 찌르면…" + 
						@"%HTML_AH_K39()%  망가진닷!   부서져 버린다앗…!」")
						}
				ENDSELECT
			ENDIF
		ELSE
			SELECTCASE RANDACT(2, 21)
				CASE 0
					CALL HTMLPRINTW(@"「응읏, 아앗%HTML_AH_K39()%  허리가, 멈추지 않고……%HTML_PS2_K39()%」")
					PRINTFORMW 「나, 액신인데…… 이렇게…… 흐트러져……」
				CASE 1
					{
					CALL HTMLPRINTW(
					@"「아읏%HTML_AH_K39()%  아%HTML_AH_K39()%　" + 
					@"벗길 수 있는 쉿… 격렬합니다……%HTML_PS2_K39()%」")
					}
					{
					CALL HTMLPRINTW(
					@"「이, 이렇게…, 밀어올릴 수 있으면, 나…%HTML_AH_K39()%　" + 
					@"응아……%HTML_PS2_K39()%」")
					}
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;ACT 파생 2:체위 변경 //다른 V성교 봉사계로부터
;===================
IF TFLAG:ACT파생 == 2
	;초조하게 해 안
	;========
	IF TCVAR:MASTER:초조하게해번 > 1
		IF LOCAL:0 ;호의, 호감도로 분기
			PRINTFORMW 「후후, 이 모습이라고 가게 하는 것도 가게 하지 않는 것도 나나름이군요」
			CALL HTMLPRINTW(@"「이봐요, 조금 심술쟁이 해 준다…%HTML_PS_K39()%」")
		ELSE
			PRINTFORMW 「후후, 안됩니다. 아직 가게 해 주지 않습니다」
			CALL HTMLPRINTW(@"「조금만 더 즐겁게 해 받아요%HTML_PS_K39()%」")
		ENDIF
		RETURN 1
	ENDIF

	;초조하게 해 종료
	;==========
	IF TCVAR:MASTER:초조하게해번 == 1
		IF LOCAL:0 ;호의, 호감도로 분기
			PRINTFORMW 「저기 알아?%CNAME(MASTER, TARGET)%의 것이 자궁의 입구까지 도착해 있어」
			PRINTFORMW 「좋아, 이대로%LOCALS:0%해. 자궁의 안에 쏟아…」
		ELSE
			PRINTFORMW 「좋아요, 이대로 안에%LOCALS:0%해 주세요」
			PRINTFORMW 「자궁의 안까지%CNAME(MASTER, TARGET)%로 가득 해 주세요…」
		ENDIF
		RETURN 1
	ENDIF

	;초조하게 함 없음
	;===========
	IF !TCVAR:MASTER:초조하게해번
		IF LOCAL:0 ;호의, 호감도로 분기
			CALL HTMLPRINTW(@"「우후, 이번은 내가 범해 준다……%HTML_PS_K39()%」")
			PRINTFORMW 「%CNAME(MASTER, TARGET)%는 그대로 좋으니까… 나의 추잡한 모습, 잘 보고 있어…」
		ELSE
			CALL HTMLPRINTW(@"「이번은 나부터 시라고 줍니다%HTML_PS_K39()%」")
			PRINTFORMW 「%CNAME(MASTER, TARGET)%는 조금 쉬고 있어 주세요…」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;ACT 파생 5:A성교 봉사계로부터
;===========================
IF TFLAG:ACT파생 == 5
	IF LOCAL:0 ;호의, 호감도로 분기
		PRINTFORMW 「읏, 이제 안됏! 앞이 쑤셔 버려, 인내 할 수 없는……!」
		PRINTFORMW 「-이번은 내가 범해 준다. 지금… 삽입할 수 있기 때문에, 움직이지 마……」
	ELSE
		PRINTFORML 「%CNAME(MASTER, TARGET)%는 조금 쉬고 있어 주세요…?」
		CALL HTMLPRINTW(@"「뒤는 내가…… 읏%HTML_AH_K39()%  아…%HTML_PS2_K39()%」")
	ENDIF
	RETURN 1
ENDIF

;조교 대상이 갈 것 같다
;==================
IF TCVAR:MASTER:갈것같다
	IF LOCAL:0 ;호의, 호감도로 분기
		PRINTFORMW 「이봐요, 안에서 가게 해 준다.%CNAME(MASTER, TARGET)%도 여기서 가고 싶겠지요?」
		CALL HTMLPRINTW(@"「그렇지만, 너무 간단하게는 가지 말아요%HTML_PS_K39()%」")
	ELSE
		PRINTFORMW 「읏, 당장 가 버릴 것 같네요」
		CALL HTMLPRINTW(@"「그렇지만, 조금은 즐겁게 해 주세요%HTML_PS_K39()%」")
	ENDIF
	RETURN 1
ENDIF

;그 외·조건 없음
;================
IF 구상기입체크(100)
	IF LOCAL:0 ;호의, 호감도로 분기
		{
		PRINTFORMW 「%RAND_SPLIT(
		"이봐요, 움직이지 말고…… 내가 범해 주기 때문에…/" + 
		@"%CNAME(MASTER, TARGET)%는 가만히 하고 있어. 액신의 진심, 보여 주어요…/" + 
		@"%CNAME(MASTER, TARGET)%는 아무것도 하지 않아도 괜찮아요. 내가 즐겁게 해 준다…/" + 
		@"%CNAME(MASTER, TARGET)%를 갖고 싶고…… 이제 인내 할 수 없닷…!")%」
		}
	ELSE
		{
		PRINTFORMW 「%RAND_SPLIT(
		", 나부터 시라고 줍니다. 움직이지 말아 주세요…/" + 
		"걱정 필요 없어요. 뒤는 전부, 나에게 맡겨…/" + 
		"지금부터 무엇을 하는지, 말하지 않아도 아는군요? 후후")%」
		}
	ENDIF
	{
	PRINTFORMDW %CALLNAME:TARGET%는%RAND_SPLIT(
	"발정해 버린 암컷의 표정으로/" + 
	@"애액이 방울져 떨어진다%VAGINA_DESCRIPTION2(TARGET)%를 과시하면서/" + 
	"스스로를 손가락으로 위로하면서/" + 
	"정욕에 불타는 여자의 표정으로")%%CALLNAME:MASTER%에걸쳐、단번에페니스를질안쪽까지물었다…
	}
	RETURN 1
ENDIF


;     
;함수 두는 곳
;     

;뽑지 않고 용. 역강간 이외로 사용하는 것
;////////////////////////////////////
@PRINT_KOJO_ACT_GRU10_0
#LOCALSIZE 1
VARSET LOCAL
LOCAL:0 = GET_KFLAG_K39("상사상애", (FLAG:주인님))

CALL QUAKE(4)
;조교자의 PALAM에 의해 파생
SELECTCASE PALAM:TARGET:쾌V || PALAM:TARGET:쾌A
	CASE IS >= 8500
		{
		CALL HTMLPRINTW(
		@"「응아앗%HTML_AH_K39()%  안됏%HTML_AH_K39()%　" + 
		@"굉장하다앗!   아…%HTML_PS2_K39()%」")
		}
		CALL QUAKE(4)
		IF RAND:2
			IF LOCAL:0 ;호의, 호감도로 분기
				{
				CALL HTMLPRINTW(
				@"「おかしくなっちゃう%HTML_AH_K39()%　%RAND_SPLIT(
				"양/느낌")%지나앗、이상해져버린닷……%HTML_PS2_K39()%」")
				}
			ELSE
				CALL HTMLPRINTW(@"「나, 나! 이런 건, 이제(벌써)…… 참을 수 있는…%HTML_PS2_K39()%」")
			ENDIF
		ELSE
			{
			CALL HTMLPRINTW(
			@"「범해\@ !LOCAL:0 ? 주세요# \@, 좀 더!   \@ IS_NOWACTNAME("애널 섹스 시킨다") 
			? 엉덩이를 범해엣# 좀 더\@!   응아……%HTML_PS2_K39()%」")
			}
		ENDIF
	CASE IS >= 5000
		{
		CALL HTMLPRINTW(
		@"「아%RAND_SPLIT("/")%%HTML_AH_K39()%　%RAND_SPLIT("/")%응%RAND_SPLIT("/")%、" + 
		@"아%HTML_AH_K39()%　%RAND_SPLIT("응//읏")%아…%HTML_PS2_K39()%」")
		}
		CALL QUAKE(4)
		SELECTCASE AUTORAND(4, 195)
			CASE 0
				{
				CALL HTMLPRINTW(
				@"「아아읏, 찔러\@ LOCAL:0 ?# 주세요\@" + 
				@"%HTML_AH_K39()%  좀 더, 안쪽까지……%HTML_PS2_K39()%」")
				}
			CASE 1
				{
				CALL HTMLPRINTW(
				@"「응아앗%HTML_AH_K39()%  안됏%HTML_AH_K39()%　" + 
				@"굉장하다앗…… 아앗…%HTML_PS2_K39()%」")
				}
			CASE 2
				{
				CALL HTMLPRINTW(
				@"「앗%HTML_AH_K39()%  아!   히, 아훗…" + 
				@"아%HTML_AH_K39()%  아%HTML_PS2_K39()%」")
				}
			CASE 3
				{
				CALL HTMLPRINTW(
				@"「%RAND_SPLIT("응, 쿳!   아, 아, 아/아앗, 우읏!   아…… 응아앗")%" + 
				@"…%HTML_PS2_K39()%」")
				}
				PRINTFORMW 「좋다\@ LOCAL:0 ? 야# 예요\@… 좀 더\@ LOCAL:0 ? 격렬하게 해도# 찔러 주세요\@……」
		ENDSELECT
ENDSELECT


;ACT 파생 1용(역강간은 제외함)
;//////////////////////////
@PRINT_KOJO_ACT_GRU10_1_K39
#LOCALSIZE  1
#LOCALSSIZE 1
VARSET LOCAL
VARSET LOCALS
LOCAL :0  = GET_KFLAG_K39("상사상애", (FLAG:주인님))
LOCALS:0 '= RAND_SPLIT("출/사정")

SELECTCASE TCVAR:MASTER:초조하게해번
	;초조하게 해 안
	CASE IS > 1
		;ACT 마다의 파생
		SELECTCASE GET_ACTNAME(TFLAG:ACT)
			CASE "정상위 시킨다", "배면좌위 시킨다"
				IF LOCAL:0 ;호의, 호감도로 분기
					PRINTFORMW 「이봐요, 이렇게 해 차분히 희롱해지는 것도 나쁘지 않지요?」
					CALL HTMLPRINTW(@"「액신의 허리사용, 충분히 즐겨%HTML_PS_K39()%」")
				ELSE
					PRINTFORMW 「서로의 감촉이 자주(잘) 전해져, 이런 것도 좋네요…」
					CALL HTMLPRINTW(@"「나의 안의 감촉, 차분히 맛보여 받아요%HTML_PS_K39()%」")
				ENDIF
			CASE "후배위 시킨다"
				IF LOCAL:0 ;호의, 호감도로 분기
					PRINTFORMW 「후후, 그렇게 열중해 버려」
					CALL HTMLPRINTW(@"「뒤로부터 하면 흥분해? 그렇지만, 좀 더 참아%HTML_PS_K39()%」")
				ELSE
					PRINTFORMW 「읏, 격렬하네요. 그렇게 가고 싶습니까?」
					CALL HTMLPRINTW(@"「그렇지만, 조금만 더 참고 받아요%HTML_PS_K39()%」")
				ENDIF
			CASE "대면좌위 시킨다"
				IF LOCAL:0 ;호의, 호감도로 분기
					CALL HTMLPRINTW(@"「우후%HTML_AH2_K39()%  꽉 되면 움직일 수 없지요?」")
					PRINTFORMW 「아직 가서는 안돼…… 좀 더 이대로 있고%RAND_SPLIT("시키고/")%라고…」
				ELSE
					PRINTFORMW 「안됩니다, 아직 가지 마」
					PRINTFORMW 「조금만 더…, 이대로 있고%RAND_SPLIT("시켜/")%라고 주세요…」
				ENDIF
			;CASE "배면좌위 시킨다"
			CASE "애널 섹스 시킨다"
				IF LOCAL:0 ;호의, 호감도로 분기
					{
					CALL HTMLPRINTW(
					@"「후후,\@ TENSION(TARGET, 8) == "높다" ? 안돼# 안 돼요\@" + 
					@"%HTML_AH2_K39()%  아직 가게 하고 있어 주지 않아요」")
					}
					PRINTFORMW 「왜냐하면…?%CNAME(MASTER, TARGET)%의 안타까운 것 같은 소리, 좀 더 (듣)묻고 있고 싶은 것…」
				ELSE
					CALL HTMLPRINTW(@"「슬슬 한계같네요? 후후…%HTML_PS_K39()%」")
					PRINTFORMW 「그렇지만 안됩니다. 좀 더 나에게 교제해 받을테니까…」
				ENDIF
		ENDSELECT
	;초조하게 해 종료
	CASE 1
		;ACT 마다의 파생
		SELECTCASE GET_ACTNAME(TFLAG:ACT)
			CASE "정상위 시킨다"
				IF LOCAL:0 ;호의, 호감도로 분기
					PRINTFORMW 「이봐요, 좋아…%TNAME_K39()%의 가장 안쪽에%LOCALS:0%해……」
				ELSE
					PRINTFORMW 「자, 좋아요… 나의 가장 안쪽에%LOCALS%해 주세요…」
				ENDIF
			CASE "후배위 시킨다"
				IF LOCAL:0 ;호의, 호감도로 분기
					PRINTFORML 「좋, 아요…, 가게 해 준다」
					PRINTFORMW 「이봐요,%TNAME_K39()%의 가장 안쪽에%LOCALS:0%해……」
				ELSE
					PRINTFORML 「좋은, 이에요…, 가게 해 줍니다」
					PRINTFORMW 「나의 가장 안쪽에,%LOCALS:0%해 주세요……」
				ENDIF
			CASE "대면좌위 시킨다"
				CALL HTMLPRINTW(@"「앗%HTML_AH_K39()%  안쪽에, 맞앗…%HTML_PS2_K39()%」")
				IF LOCAL:0 ;호의, 호감도로 분기
					PRINTFORMW 「이봐요, 좋아…%TNAME_K39()%의 가장 안쪽에%LOCALS:0%해……」
					PRINTFORMW 「키스 하면서 가게 해 준다…」
				ELSE
					PRINTFORMW 「좋아요… 나의 가장 안쪽에%LOCALS:0%해 주세요……」
					PRINTFORMW 「이봐요, 키스 하면서 가게 해 줍니다…」
				ENDIF
			CASE "배면좌위 시킨다"
				IF LOCAL:0 ;호의, 호감도로 분기
					PRINTFORML 「이대로 안에%LOCALS:0%해」
					PRINTFORMW 「좀 더%CNAME(MASTER, TARGET)%를 느끼게 해…」
				ELSE
					PRINTFORMW 「좋아요… 이대로 안에%LOCALS:0%해 주세요…」
				ENDIF
			CASE "애널 섹스 시킨다"
				IF LOCAL:0 ;호의, 호감도로 분기
					PRINTFORMW 「이봐요, 좋아……,%TNAME_K39()%의 엉덩이안에%LOCALS:0%해…」
				ELSE
					PRINTFORMW 「자, 좋습니다, 나…… 나의 엉덩이로 가게 해 줍니다…」
				ENDIF
		ENDSELECT
	;초조하게 해 없음
	CASEELSE
		;V성교 봉사계. 대사의 처리 //역강간은 범용 처리하지 않기 때문에 제외
		IF IS_NOWACTNAME("정상위 시킨다/후배위 시킨다/대면좌위 시킨다/배면좌위 시킨다") && V_SEX(MASTER)
			CALL QUAKE(4)
			;PALAM:쾌V파생
			SELECTCASE PALAM:TARGET:쾌V
				;조교자가 갈 것 같다
				CASE IS >= 8500
					IF LOCAL:0 ;호의, 호감도로 분기
						{
						CALL HTMLPRINTW(
						@"「후아앗%HTML_AH_K39()%  아%HTML_AH_K39()%　" + 
						@"나, 아, 아앗…%HTML_PS2_K39()%」")
						}
						CALL QUAKE(4)
						{
						CALL HTMLPRINTW(
						@"「안됏%HTML_AH_K39()%  죽어 버린닷!   " + 
						@"이렇게, 되면……%HTML_PS2_K39()%」")
						}
					ELSE
						{
						CALL HTMLPRINTW(
						@"「아아웃%HTML_AH_K39()%  응… 앗%HTML_AH_K39()%　" + 
						@"아%HTML_AH_K39()%  아…%HTML_PS2_K39()%」")
						}
						CALL QUAKE(4)
						{
						CALL HTMLPRINTW(
						@"「이, 이런 건은…… 얏%HTML_AH_K39()%  %HTML_AH_K39()%　" + 
						@"후~, 응읏…%HTML_PS2_K39()%」")
						}
					ENDIF
				;PALAM:쾌V가 5000이상
				CASE IS >= 5000
					IF LOCAL:0
						{
						CALL HTMLPRINTW(
						@"「아아웃, 읏%HTML_AH_K39()%  나!   " + 
						@", 안쪽에… 맞아앗……%HTML_PS2_K39()%」")
						}
					ELSE
						{
						CALL HTMLPRINTW(
						@"「아, 앗%HTML_AH_K39()%  개, 이런 건… 야, 야읏!   " + 
						@"후~……%HTML_PS2_K39()%」")
						}
					ENDIF
				;그 외
				CASEELSE
					IF LOCAL:0
						{
						CALL HTMLPRINTW(
						@"「%RAND_SPLIT("나는/")%괜찮아, 그러니까……" + 
						@"군요, 좀 더 격렬하고…… 응읏%HTML_PS2_K39()%」")
						}
					ELSE
						CALL HTMLPRINTW(@"「좋은, 이에요…… 좀 더, 격렬하게 시라고 주세요…… 읏%HTML_PS2_K39()%」")
					ENDIF
			ENDSELECT
		;A성교 봉사계
		ELSE
			IF 0
			;조교 대상이 참고 있다
			ELSEIF TCVAR:MASTER:인내
				PRINTFORMDL 갈 것 같게 되었다%CALLNAME:MASTER%는 조금 삽입질의 페이스를 떨어뜨렸다…
				IF LOCAL:0 ;호의, 호감도로 분기
					PRINTFORMW 「저기 읏, 이런 것 싫엇… 좀 더, 좀 더 움직여요…」
					CALL HTMLPRINTW(@"「그렇지만…… 곧바로 가서는 안되니까%HTML_PS_K39()%」")
				ELSE
					PRINTFORMW 「읏, 슬슬 한계같네요…」
					PRINTFORMW 「%CNAME(MASTER, TARGET)%가 움직이지 않으면, 내가 조금 움직여 줄까요…?」
					CALL FADE_TO_CLEARLINE(12, "「킥킥…」")
				ENDIF
			;조교 대상이 갈 것 같다
			ELSEIF !TCVAR:MASTER:인내 && TCVAR:MASTER:갈것같다
				CALL QUAKE(4)
				IF LOCAL:0 ;호의, 호감도로 분기
					{
					CALL HTMLPRINTW(
					@"「야앗%HTML_AH_K39()%  팥고물, 아앗" + 
					@"%HTML_AH_K39()%  는, 격렬해……!」")
					}
				ELSE
					{
					CALL HTMLPRINTW(
					@"「앗%HTML_AH_K39()%  팥고물%HTML_AH_K39()%　" + 
					"는, 격렬합니닷!   아… 응앗……!」")
					}
				ENDIF
				{
				PRINTFORMDW 스스로의 한계를 깨달았다%CALLNAME:MASTER%는,\@ 
				ABL:A감각 < 4 
				? 아직도 친숙해 지지 않는다# 자포자기하고 잘랐다
				\@%CALLNAME:TARGET%의 아누스를 격렬하게 범했다…
				}
				PRINTL
			;그 외
			ELSE
				CALL QUAKE(4)
				IF LOCAL:0
					{
					CALL HTMLPRINTW(
					@"「응읏%HTML_AH_K39()%  아, 앗%HTML_AH_K39()%　" + 
					@"엉덩이안이… 스쳐, 라고……%HTML_PS2_K39()%」")
					}
				ELSE
					{
					CALL HTMLPRINTW(
					@"「응앗%HTML_AH_K39()% 팥고물%HTML_AH_K39()%　" + 
					@"나, 엉덩이로… 이렇게…%HTML_PS2_K39()%」")
					}
				ENDIF
			ENDIF
		ENDIF

		;지문을 처리. ACT에 의해 파생 //역강간은 범용 처리하지 않기 때문에 제외
		SELECTCASE GET_ACTNAME(TFLAG:ACT)
			CASE "정상위 시킨다"
				{
				PRINTFORMDW \@ PALAM:쾌V >= 8500 
				? %CALLNAME:TARGET%는%CALLNAME:MASTER%의 허리에 다리 확실히를 관련되어, 스스로도 끝까지 오르려고 하고 있다
				# %CALLNAME:MASTER%가 돌진할 때에,%CALLNAME:TARGET%의 달콤한 교성이 근처에 울렸다\@…
				}
			CASE "후배위 시킨다"
				{
				PRINTFORMDW \@ PALAM:쾌V >= 8500 
				? %CALLNAME:TARGET%는 등을 활처럼 하면서 신체를 삐걱삐걱 진동시켜 스스로도 끝까지 오르려고 하고 있다
				# %CALLNAME:MASTER%가 배후로부터 돌진할 때에,%CALLNAME:TARGET%의 달콤한 교성이 근처에 울렸다\@…
				}
			CASE "대면좌위 시킨다"
				{
				PRINTFORMDW \@ PALAM:쾌V >= 8500 
				? 격렬한 밀어올림과 함께 키스가 세례를 퍼부어져%CALLNAME:TARGET%는 미친 것처럼 허덕이고 있다
				# %CALLNAME:TARGET%는%CALLNAME:MASTER%에 강하게 껴안을 수 있어 밀어올릴 수 있을 때에 달콤한 한숨을 흘렸다\@…
				}
			CASE "배면좌위 시킨다"
				{
				PRINTFORMDW \@ PALAM:쾌V >= 8500 
				? 격렬한 공격과 함께 유방을 주물러져%CALLNAME:TARGET%는 미친 것처럼 허덕이고 있다
				# %CALLNAME:MASTER%가 배후로부터 돌진할 때에,%CALLNAME:TARGET%의 달콤한 교성이 근처에 울렸다\@…
				}
			CASE "애널 섹스 시킨다"
				PRINTFORMDL 삽입질을 반복할 때에,%ANUS_DESCRIPTION(TARGET)%를 범해진다%CALLNAME:TARGET%의 달콤한 교성이 울려 퍼진다…
				PRINTFORMDL 심심한 듯이 실룩거린다%CALLNAME:TARGET%의%VAGINA_DESCRIPTION2(TARGET)%로부터는 애액이 끝 없게 흐르기 시작해,
				PRINTFORMDW 그 모양이%CALLNAME:MASTER%의 욕정에 불을 붙인다…
		ENDSELECT
ENDSELECT


;조교 대상이 가 그렇게의 범용 처리
;//////////////////////////
@PRINT_KOJO_ACT_GRU10_2_K39
#LOCALSIZE  1
#LOCALSSIZE 3
VARSET LOCAL
VARSET LOCALS
LOCAL :0  = TENSION(TARGET, 8) == "높다"
LOCALS:0 '= \@ LOCAL:0 ? %HTML_AH2_K39()%　 # 。 \@
LOCALS:1 '= RAND_SPLIT("출/사정")
LOCALS:2 '= HTML_PS_K39()

;조교 대상이 갈 것 같다
IF TCVAR:MASTER:갈것같다
	;호의, 호감도로 분기
	IF GET_KFLAG_K39("상사상애", (FLAG:주인님))
		SELECTCASE AUTORAND(3, 196)
			CASE 0
				CALL HTMLPRINTW(@"「저기… 최후는 나의 안에서%LOCALS:0%%CNAME(MASTER, TARGET)%도, 그 쪽이 좋지요?」")
			CASE 1
				CALL HTMLPRINTW(@"「이봐요, 어차피라면 여기에%LOCALS:0%가득%LOCALS:1%해도 좋으니까…」")
			CASE 2
				CALL HTMLPRINTW(@"「저기, 와……%LOCALS:0%최후는 역시, 나의 안에서 받아 들여 주고 싶고……」")
			CASE 3
				CALL HTMLPRINTW(@"「좀 더 참아요%LOCALS:0%어차피라면, 나의 안에서 실시했으면 좋고…」")
		ENDSELECT
	ELSE
		SELECTCASE AUTORAND(3, 197)
			CASE 0
				CALL HTMLPRINTW(@"「넣은 순간에 가 버릴 것 같네요? 후후…%LOCALS:2%」")
			CASE 1
				CALL HTMLPRINTW(@"「… 좋아요%LOCALS:0%%CNAME(MASTER, TARGET)%의 액, 나의 안에서 받아 들여 줍니다……」")
			CASE 2
				CALL HTMLPRINTW(@"「좋아요, 최후는 여기에. 나라고…, 최초부터 그럴 생각였고%LOCALS:2%」")
			CASE 3
				CALL HTMLPRINTW(@"「조금만 더 참아 주세요. 나도, 그……, 조금은 즐기고 싶으며%LOCALS:2%」")
		ENDSELECT
	ENDIF
ENDIF

;////////////////////
;성교 봉사계 첫회용 처리
;////////////////////
@CHK_DEBUG_AND_FIRSTTIME_10_K39
[IF_DEBUG]
	;비디버그시, 첫회
	IF !FLAG:오토모드 && FIRSTTIME(195)
		;성교 봉사 판정에 대입
		KFLAG:TARGET = 1
	;디버그시, 오토 모드의 경우 //현재의 사양에서는 오토 모드는 Emuera의 디버그 모드에서의 기동이 전제
	ELSEIF FLAG:오토모드 && FLAG:오토모드표시설정 == 0 && FIRSTTIME(196)
		;성교 봉사 판정에 대입
		;KFLAG:TARGET = 1
		;오토 모드시 용 처리
		CALL PRINT_RAINBOW2W("첫회", 20)
		;데이터 로드 처리
		CALL GAME_OVER_K39
		FORCEWAIT
		RETURN 1
	ELSE
		;그 외라면 처리 없음
	ENDIF
[ENDIF]
