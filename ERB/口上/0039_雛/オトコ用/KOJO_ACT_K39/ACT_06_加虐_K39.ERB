;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;병아리 구상 가학계 액션 구상 파일
;     Curse your own stupidity!

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;


;        
;가학계 ACT60~68
;        
[SKIPSTART]
	#TFLAG:ACT
		60=스팽킹
		61=채찍
		62=바늘
		63=줄
		64=아이마스크
		65=볼개그
		66=매도
		67=삼각 목마
		68=관장 세트
	#TFLAG:ACT 분류 == 6에 해당
	#범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "가학")로 참조하는 것이 가능
	#TEQUIP계는 기본, ACT 개시시점으로써 장착된다
	#ACT에 따라서는 조교 대상의 반응하기 나름으로 캔슬되는 것이 있다
	#줄/아이마스크/볼개그/삼각 목마는 TEQUIP로 관리되고 해당 ACT/REACT 이외에도 TEQUIP:줄등으로 참조할 수 있다
[SKIPEND]


;여기는 의지 없다 감 있다
;병아리님의 경우, 희희낙락 해 가학계에 힘쓰는 모습이 상상하기 어렵다


;/////////////////////
;ACT60, 스팽킹//
;/////////////////////
[SKIPSTART]
	#조교자가 조교 대상의 엉덩이를 손바닥에서 두드린다
	#ACT 개시시점으로써 벌써 두드리고 있다
	#성교 봉사중에는 발생하지 않는다
	#안면 승마계는 해제된다
	#조교자가 자위중인 경우는 해제된다
	#지문에 분기 없음

	「나스팽킹을 엉덩이 드럼이라고 하는 사람 싫다!」
[SKIPEND]
@KOJO_MALE_ACT_K39_60 ;ACT 파생 없음

;ACT 파생 0으로 결정하고 쳐 둔다
IF !TFLAG:ACT파생
	;연속 실행 //이것이 성립하는 것은 드물다고 생각한다
	;========
	IF TFLAG:ACT == TFLAG:전ACT
		PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 엉덩이를 계속 두드리고 있다…
		;전회 턴 REACT 분류로 파생 //여기서 전회 턴 REACT 분류를 참조 해서는 안된 생각도 든다…
		SELECTCASE PREVCOMGRONAME()
			CASE "적극적 따른다", "태연하게 따른다"
				IF GET_KFLAG_K39("상사상애")
					PRINTFORMW 「이것으로 그렇게 환영받아도, 조금 복잡할지도…」
				ELSE
					PRINTFORMW 「%CNAME(MASTER, TARGET)%가 기뻐해 주는 것은 최상입니다만…」
					PRINTFORMW 「조금 복잡한 기분이 되네요……」
				ENDIF
			CASE "받아들인다"
				IF GET_KFLAG_K39("상사상애")
					PRINTFORMW 「상당히 즐기고 있는 것 같구나」
					CALL HTMLPRINTW(@"「이런 일에서도 기뻐하다니 정말로 곤란한 사람%HTML_PS_K39()%」")
				ELSE
					PRINTFORMW 「아무래도 이것을 좋아하게 되어 버린 것 같네요…」
				ENDIF
			CASEELSE
				IF GET_KFLAG_K39("상사상애")
					PRINTFORMW 「이봐요, 움직이지 마. 아직 참을 수 있을까요?」
				ELSE
					PRINTFORMW 「미안해요, 좀 더 참아 주세요」
				ENDIF
		ENDSELECT
		RETURN 1
	ENDIF

	;그 외·조건 없음 //확률로 범용 ACT에 패스
	;================
	IF 구상기입체크(55)
		IF GET_KFLAG_K39("상사상애")
			SELECTCASE RANDACT(2, 50)
				CASE 0
					PRINTFORMW 「이봐요, 여기에 엉덩이를 향하여」
					PRINTFORMW 「싫을 것 같은 얼굴 해도, 본심은 다른거죠?」
				CASE 1
					PRINTFORMW 「이것, 약간의 벌타임이라는 곳이군요」
					CALL HTMLPRINTW(@"「그렇지만, 어차피 기쁘게 하는 것만으로 짊어지는데%HTML_PS_K39()%」")
			ENDSELECT
		ELSE
			SELECTCASE RANDACT(3, 51)
				CASE 0
					PRINTFORMW 「갑작스럽지만 벌의 시간입니다」
					PRINTFORMW 「각오 해 주세요?」
				CASE 1
					PRINTFORMW 「이것도 액을 쫓기 때문에(위해)라고 생각해 참아 주세요…」
				CASE 2
					PRINTFORMW 「자, 이쪽에 엉덩이를 향하여 주세요」
					PRINTFORMW 「무엇을 하는지, 말하지 않아도 아는군요?」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 0


;///////////
;ACT61, 채찍//
;///////////
[SKIPSTART]
	#조교자가 채찍을 크게 치켜들고 마음껏 찍어내린다
	#ACT 개시시점으로써 벌써 두드리고 있다
	#턴 방침이 "소프트"라면 발생하지 않는다
	#성교중/성교 봉사중에는 발생하지 않는다
	#지문에 분기 없음
[SKIPEND]
@KOJO_MALE_ACT_K39_61 ;ACT 파생 없음

;ACT 파생 0으로 결정하고 쳐 둔다
IF !TFLAG:ACT파생
	;그 외·조건 없음 //확률로 범용 액션에
	;================
	IF 구상기입체크(66)
		;호의, 호감도로 분기
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「이봐요, 등을 돌려?」
			PRINTFORMW 「싫을 것 같은 얼굴 해도, 본심은 다른거죠?」
		ELSE
			SELECTCASE RANDACT(3, 50)
				CASE 0
					PRINTFORMW 「갑작스럽지만 벌의 시간입니다」
					PRINTFORMW 「각오 해 주세요?」
				CASE 1
					PRINTFORMW 「이것도 액을 쫓기 때문에(위해)라고 생각해 참아 주세요…」
				CASE 2
					PRINTFORMW 「마음의 준비는 좋을까요?」
					PRINTFORMW 「무엇을 하는지, 말하지 않아도 아는군요?」
			ENDSELECT
		ENDIF
		SIF !RAND:3
			CALL FADE_TO_CLEARLINE(15, "「후후…」")
		RETURN 1
	ENDIF
ENDIF
RETURN 0


;///////////
;ACT62, 바늘//
;///////////
[SKIPSTART]
	#유두와 성기에 바늘을 찌른다
	#ACT 개시시점으로써 벌써 찌르고 있다
	#조교 대상으로 페니스가 있는 경우는 페니스에, 없는 경우는 클리토리스에 찌른다
	#가슴과 성기(페니스/바기나)로부터 출혈해, 혈액 더러움이 추가된다
	#턴 방침이 "소프트", "노멀"이라면 발생하지 않는다
	#성교중/성교 봉사중에는 발생하지 않는다
	#안면 승마계, 자위계는 해제된다
	#지문은 조교 대상의 페니스의 유무로 분기
[SKIPEND]
@KOJO_MALE_ACT_K39_62 ;ACT 파생 없음
#LOCALSSIZE 1
VARSET LOCALS
LOCALS:0 = \@ TENSION(TARGET, 8) == "높다" ? 우대신…# \@

;조교 대상으로 페니스 있어 //ACT 파생 0으로 결정하고 쳐 둔다
;====================
IF !TFLAG:ACT파생 && PENIS(MASTER)
	;그 외·조건 없음 //확률로 범용 액션에
	;================
	IF 구상기입체크(77)
		;호의, 호감도로 파생
		IF GET_KFLAG_K39("상사상애")
			SELECTCASE RANDACT(2, 50)
				CASE 0
					CALL HTMLPRINTW(@"「자, 바늘로 즐겁게 해 준다%HTML_PS_K39()%」")
					PRINTFORMW 「조금 떨고 있지만, 그것은 기대? 그렇지 않으면 불안?」
					PRINTFORMW 「저기, 어느 쪽이야? 우후후」
				CASE 1
					PRINTFORMW 「이봐요, 싫을 것 같은 얼굴 해도, 본심은 다른거죠?%LOCALS:0%」
					CALL HTMLPRINTW(@"「소망 대로, 즐겁게 해 준다%HTML_PS_K39()%」")
			ENDSELECT
		ELSE
			SELECTCASE RANDACT(3, 51)
				CASE 0
					PRINTFORMW 「자, 액신의 바늘 중재를 즐겨 받아요」
					PRINTFORMW 「각오 해 주세요?」
				CASE 1
					PRINTFORMW 「이것도 액을 쫓기 때문에(위해)라고 생각해 참아 주세요…」
				CASE 2
					PRINTFORMW 「마음의 준비는 좋을까요?」
					PRINTFORMW 「지금부터 무엇을 하는지, 이 바늘을 보면 아는군요?%LOCALS:0%」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 0


;///////////
;ACT63, 줄//
;///////////
[SKIPSTART]
	#조교 대상을 줄로 묶으려고 한다
	#지문은"묶으려고 했다…"되고 있지만 처리적으로는 ACT 개시시점으로써 속박되고 있다
	#TEQUIP:줄로 묶기의 강도를 참조할 수 있다
	#이미 장비 하고 있었을 경우는 해제가 된다
	#이미 장착 끝나, 한편 TFLAG:벌플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
	#TEQUIP:줄자체는 REACT 분류가 "허가를 청한다"의 경우에 해제된다
	#v1. 020 시점의 사양으로 이 거동을 확실히 참조하는 수단 없음. 본체측의 개선이 요구된다
		(REACT63 이외에도 캔슬 처리가 되는 경우가 있기 때문에(위해))
	#성교중/성교 봉사중에는 발생하지 않는다
	#구속시에 자위나 안면 승마, 조교 대상의 페니스 사용중은 해제된다
	#지문은 구속, 해제로 분기
[SKIPEND]
@KOJO_MALE_ACT_K39_63 ;TFLAG:ACT 파생(0=구속/1=해제)

[SKIPSTART]
;ACT63_0, 줄장착 //반항 각인, 조교자 조교 레벨, 기교, 결박능숙 소질에 응해 구속 SOURCE 증가
;=============== //구속 SOURCE에 응해 강도 상승
IF ! TFLAG:ACT 파생
ENDIF

;ACT63_1, 줄해제 //줄해제에 파생한 시점에서, "TEQUIP:줄 = 0"이 되므로 해제시에 묶어 강도는 참조 할 수 없다
;=============== //장착과 강도로 같은 TEQUIP를 재사용하고 있기 때문에(위해), 이와 같이 되어 버린다
IF TFLAG:ACT 파생 == 1
ENDIF
[SKIPEND]
RETURN 0


;///////////////////
;ACT64, 아이마스크//
;///////////////////
[SKIPSTART]
	#조교자가 아이마스크를 대게 하려고 한다
	#지문에서는"대게 하려고 했다…"되고 있지만 처리상은 ACT 개시시점으로써 장착되고 있다
	#이미 장비 하고 있었을 경우는 해제가 된다
	#이미 장착 끝나, 한편 TFLAG:벌플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
	#TEQUIP:아이마스크는 REACT 분류가 "허가를 청한다"(이었)였던 경우에 해제된다
	#v1. 020 시점의 사양으로 이 거동을 확실히 참조하는 수단 없음. 본체측의 개선이 요구된다
		(REACT64 이외에도 캔슬 처리가 되는 경우가 있기 때문에(위해))
	#성교중/성교 봉사중에는 발생하지 않는다
	#안면 승마계는 해제된다
	#조교자가 자위중이었던 경우는 해제된다
	#지문은 장착, 해제로 분기
[SKIPEND]
@KOJO_MALE_ACT_K39_64 ;TFLAG:ACT 파생(0=장착/1=해제)

[SKIPSTART]
;ACT64_0, 아이마스크 장착 //조교 대상으로 페니스가 있는 경우, 마조끼에 응해 발기도 상승
;=======================
IF ! TFLAG:ACT 파생
ENDIF

;ACT64_1, 아이마스크 해제
;=======================
IF TFLAG:ACT 파생 == 1
ENDIF
[SKIPEND]
RETURN 0


;/////////////////////
;ACT65, 볼개그//
;/////////////////////
[SKIPSTART]
	#조교자가 볼개그를 끼우게 하려고 한다
	#지문에서는"끼우게 하려고 했다…"되고 있지만 처리상은 ACT 개시시점으로써 장착되고 있다
	#이미 장비 하고 있었을 경우는 해제가 된다
	#이미 장착 끝나, 한편 TFLAG:벌플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
	#TEQUIP:볼개그는 REACT 분류가 "허가를 청한다"(이었)였던 경우에 해제된다
	#v1. 020 시점의 사양으로 이 거동을※확실히 참조하는 수단 없음. 본체측의 개선이 요구된다
		※REACT64 이외에도 캔슬 처리가 되는 경우가 있기 때문에(위해)
	#턴 방침이 "소프트", "노멀"의 경우는 발생하지 않는다
	#성교중/성교 봉사중에는 발생하지 않는다
	#안면 승마계는 해제된다
	#조교자가 자위중이었던 경우는 해제된다
	#지문은 장착, 해제로 분기
[SKIPEND]
@KOJO_MALE_ACT_K39_65 ;TFLAG:ACT 파생(0=장착/1=해제)

[SKIPSTART]
;ACT65_0, 볼개그 장착 //조교 대상으로 페니스가 있는 경우, 마조끼에 응해 발기도 상승
;=========================
IF ! TFLAG:ACT 파생
ENDIF

;ACT65_1, 볼개그 해제
;=========================
IF TFLAG:ACT 파생 == 1
ENDIF
[SKIPEND]
RETURN 0


;/////////////
;ACT66, 매도//
;/////////////
[SKIPSTART]
	#도발하도록(듯이) 조교 대상의 치태를 소리에 낸다
	#회화계로부터 발생하는 경우도 있다
	#턴 방침이 "소프트"라면 발생하지 않는다
	#회화계로부터 파생하는 경우도 있다
	#지문에 분기 없음
[SKIPEND]
@KOJO_MALE_ACT_K39_66 ;ACT 파생 없음
;IF ! TFLAG:ACT 파생
;ENDIF
RETURN 0


;/////////////////
;ACT67, 삼각 목마//
;/////////////////
[SKIPSTART]
	#조교 대상을 목마에 실으려고 한다
	#지문은"실으려고 했다…"되고 있지만 처리상은 ACT 개시시점으로써 실려지고 있다
	#실은 쾌C는 아니고 쾌V가 들어간다(나있는 목마!?)
	ASTER가 처녀라면, 처녀 상실이 일어난다
		(@ACTION_APPLY2로 처리. REACT 분류가 허가를 청하는 것 경우는 회피된다)
	#조교 대상이 남자의 경우는 발생하지 않는다
	#턴 방침이 "소프트", "노멀"때에는 발생하지 않는다
	#TEQUIP(바이브/애널 바이브/애널 비즈/관장기＋플러그/페니스 밴드) 장착시에는 발생하지 않는다
	#성교중/성교 봉사중에는 발생하지 않는다
	#야외 플레이시에는 발생하지 않는다
	#밀어 넘어뜨리기중에는 발생하지 않는다
	#이상계로부터 발생하는 경우도 있다
	#이미 장비 하고 있었을 경우는 해제가 된다
	#이미 장착 끝나, 한편 TFLAG:벌플래그가 서 있는 경우는 발생하지 않는다(즉, 해제되지 않는다)
	#TEQUIP:삼각 목마는 REACT 분류가 "허가를 청한다"(이었)였던 경우에 해제된다
	#v1. 020 시점의 사양으로 이 거동을※확실히 참조하는 수단 없음. 본체측의 개선이 요구된다
		※REACT67 이외에도 캔슬 처리가 되는 경우가 있기 때문에(위해)
	#지문은 개시, 종료로 분기
[SKIPEND]
@KOJO_MALE_ACT_K39_67 ;TFLAG:ACT 파생(0=개시/1=종료)
;사내 아이는 목마에 실어 받을 수 없는 것이 현재의 사양
RETURN 0


;///////////////////
;ACT68, 관장 세트//
;///////////////////
[SKIPSTART]
	#관장액의 주입/추가/배출
	#최초의 주입시(ACT 파생=0)는 양 1~3. 추가시(ACT 파생=2)는 양 1~4. 각각 랜덤으로 처리된다
	#이미 장비 하고 있는 경우, 관장액추가가 된다
	#관장액이 5병 들어가 있는 경우, 관장 플러그 해제가 된다
	#관장액의 분량은 TEQUIP:관장기＋플러그로 참조할 수 있다
	#TEQUIP(애널 바이브/애널 비즈/페니스 밴드/삼각 목마) 장착시에는 발생하지 않는다
	#성교중/성교 봉사중에는 발생하지 않는다
	#턴 방침이 "소프트"라면 발생하지 않는다
	#도구계로부터 파생하는 경우도 있다
	#지문은 ACT 파생에 의해 분기. 파생내에서는 분기 없음
[SKIPEND]
@KOJO_MALE_ACT_K39_68 ;TFLAG:ACT 파생(0=주입/1=배출/2=추가)

[SKIPSTART]
;ACT68_0, 관장기＋플러그 장착
;===========================
IF ! TFLAG:ACT 파생
ENDIF

;ACT68_1, 관장기＋플러그 해제 //여기에 파생하는 시점에서 액량은 5가 되어 있다
;===========================
IF TFLAG:ACT 파생 == 1
ENDIF

;ACT68_2, 관장액추가         //이미 주입 끝난 경우에 이 개소에 파생
;===================
IF TFLAG:ACT 파생 == 2
ENDIF
[SKIPEND]
RETURN 0
