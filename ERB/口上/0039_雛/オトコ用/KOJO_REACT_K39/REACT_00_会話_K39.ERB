;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;히나 구상 회화계 개별 리액션 구상 파일

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;


;        #
;회화계 REACT0～9
;        #
[SKIPSTART]
	#TFLAG:ACT
		0=지금의기분을얘기한다
		1=자신에게로의기분을얘기한다
		2=성에대한이야기를한다
		3=상냥하게위로한다
		4=엄격하게위협한다
		5=의상의변경을요구한다
		6=합의를요구한다
		7=벌이라고선언한다
		8=고함친다
		9=기분나쁘게웃는다

회화계는 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
그리고 완결시켜 버리는 것이 바람직하다고 생각합니다
[SKIPEND]

;///////////////////////
;REACT0, 지금의기분을얘기한다
;///////////////////////
[SKIPSTART]
·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다

·TFLAG:ACT파생
  ├0=말씨 나쁘게 대답했다
  ├1=용기를 내 강하게 맞았다
  ├2=야유
  ├3=분명히 지금의 기분을 고했다
  ├4=그저 나쁘지는 않다
  └5=기분 좋은 일 하고 싶다)
[SKIPEND]
@KOJO_MALE_REACT_K39_0
#DIM DYNAMIC FAV_RATING
FAV_RATING = GET_KFLAG_K39("상사상애")

;부하 2라면 REACT를 날린다
SIF GET_COM_RANK() == "무리를 한 행동"
	RETURN 0

;ACT파생, 0 말씨 나쁘게 질문에 답한 // REACT분류가 소극적 따르는 것으로, 리액션이빈정거린다 이외의 경우
;================================
IF !TFLAG:ACT파생
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	PRINTFORMW 「\@ FAV_RATING ? 응, 뭔가 말했어?  # 미안해요, 자주(잘) 들리지 않았던 것입니다만… \@」
	RETURN 1
ENDIF

;ACT파생, 1 용기를 내 강하게 맞은 // 리액션이강하게대답한다의 경우. REACT분류는 "거부"에 세트 된다
;==================================
IF TFLAG:ACT파생 == 1
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	PRINTFORMW 「\@ FAV_RATING ? 상당히, 위세가 좋구나 # 건강은 좋은 것 같네요 \@…」
	RETURN 1
ENDIF

;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」 // 리액션이빈정거린다의 경우. REACT분류는 "거부"에 세트 된다
;==========================================
IF TFLAG:ACT파생 == 2
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORML 「뭐야 그것, 야유?」
		PRINTFORMW 「그러한 것은 좋아하지 않지만…」
	ELSE
		PRINTFORML 「야유, 입니까…?」
		PRINTFORMW 「너무 그러한 것은, 좋아하지 않습니다만…」
	ENDIF
	RETURN 1
ENDIF

;ACT파생, 3분명히 지금의 기분을 고한 // REACT분류가 적극적 따르는 것 경우
;=======================================
IF TFLAG:ACT파생 == 3
	;첫회
	IF FIRSTREACT(30)
		{
		CALL HTMLPRINTW(@"「\@ FAV_RATING 
		? 후후, 즐겨 받아지고 있는 것 같구나 
		# 그렇습니까. 즐겨 받아지고 있는 것 같고 최상입니다 \@%HTML_PS_K39()%」")
		}
		RETURN 1
	ENDIF

	;상사상애
	IF FAV_RATING
		;역강간으로 질내사정의 뒤
		IF TFLAG:전회질내사정 == 2
			PRINTFORML 「뭐, 당연하지만 말야!」
			PRINTFORMW 「나의 질내에 이렇게 내 둬, 좋지 않았다고 말할 수 있을 이유 없네요」
		;통상
		ELSE
			PRINTFORMW 「언제나 그 정도 솔직하다면, 나도 다양하게 편한 것이지만 말야?」
		ENDIF
		;페이드 아웃
		CALL FADE_TO_CLEARLINE(20, @"「%RAND_SPLIT("후후/킥킥/키득")%…」")
		RETURN 1
	ENDIF

	;조교 대상으로부터의 호의 1000이상
	IF AFFECTION("조교가 끝난 상태", TARGET)
		;역강간으로 질내사정의 뒤  와  그 외
		{
		CALL HTMLPRINTW(@"「\@ TFLAG:전회질내사정 == 2 
		? 나의 질내가 그렇게 좋았던 것입니까…?  
		# 우대신…, 나도%CNAME(MASTER, TARGET)%의 일 좋아해요%HTML_PS_K39()% \@」")
		}
		RETURN 1
	ENDIF

	;조교 대상으로부터의 호의 500이상
	IF AFFECTION("눈을 뜸")
		;역강간으로 질내사정의 뒤
		IF TFLAG:전회질내사정 == 2
			PRINTFORML 「상태가 좋았어? 　%RAND_SPLIT("추잡합니다/천한 말투입니다")%군요…」
			CALL HTMLPRINTW(@"「그렇지만, 후후. 즐겨 받을 수 있던 것 같고 최상입니다…%HTML_PS_K39()%」")
		;통상
		ELSE
			CALL HTMLPRINTW(@"「상당히 솔직해져 왔어요. 후후」")
		ENDIF
		RETURN 1
	ENDIF

	;그 외
	CALL HTMLPRINTW(@"「\@ FAV_RATING ? 우대신, 사랑스럽다 # 솔직하고 좋아요 \@%HTML_PS_K39()%」")
	RETURN 1
ENDIF

;ACT파생, 4그저 나쁘지는 않은 // REACT분류가 태연하게 따르는 것 경우
;=============================
IF TFLAG:ACT파생 == 4
	IF TEQUIP:성교봉사중
		{
		PRINTFORMW 「\@ FAV_RATING 
		? 그저라는건 뭐야 그것…, 뭔가 화납니다만―!?  
		# 그것은… 솔직하게는 기뻐할 수 없네요. 과연, 지금의 상황에서는… \@」
		}
		RETURN 1
	ENDIF

	;그 외
	{
	PRINTFORMW 「\@ FAV_RATING 
	? 조금 걸려요. 그 말투… 
	# 그것은 최상입니다. 조금 석연으로 하지 않지만… \@」
	}
	RETURN 1
ENDIF

;ACT파생, 5 기분 좋은 일 하고 싶은 // ;REACT분류가 받아들이는 것 경우
;===============================
;IF TFLAG:ACT파생 == 5
;ENDIF

;COM0, 침묵한다 // 지금의기분을얘기한다에 있어서 COM"침묵한다"는(질문에 답하지 않는다) 취급
;===============
IF IS_NOWCOMNAME("침묵한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORMW 「저기…, 어째서 입다물고 있는 거야?」
	ELSE
		PRINTFORML 「아무것도 말하고 싶지 않습니까?」
		PRINTFORMW 「나, 그러면 곤란해 버립니다…」
	ENDIF
	RETURN 1
ENDIF

;COM7, 용서를빈다
;================
IF IS_NOWCOMNAME("용서를빈다")
	;「의견을 내다니 좋은 담력이군요!」벌포인트+
	IF TFLAG:REACT파생 == 1
		{
		CALL HTMLPRINTW(@"「\@ FAV_RATING 
		? %RAND_SPLIT("/후")%, 다～째%HTML_PS_K39()% 
		# 미안해요. 이제 와서, 늦습니다… \@」")
		}
		RETURN 1
	ENDIF

	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	{
	CALL HTMLPRINTW(@"「\@ FAV_RATING 
	? %RAND_SPLIT("/후")%, 어떻게 하지%RAND_SPLIT("인/그럴까")%%HTML_PS_K39()% 
	# 이제(벌써)! 언제나 내가 허락한다고 생각되어도 곤란합니다… \@」")
	}
	RETURN 1
ENDIF

;COM33, 무서워한다
;==============
IF IS_NOWCOMNAME("무서워한다")
	;파생 1이라면 범용 리액션에
	SIF TFLAG:REACT파생 == 1
		RETURN 0

	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORML 「저기…, 어째서무서워한다의?」
		PRINTFORMW 「그러한 것 그만두어%RAND_SPLIT("갖고 싶지만/")%…」
	ELSE
		PRINTFORMW 「무서워하게 해 버린 것 같네요…」
	ENDIF
	RETURN 1
ENDIF

;COM14, 쾌감을참는다
;======================
IF IS_NOWCOMNAME("쾌감을참는다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		IF FAV_RATING
			PRINTFORML 「몸 삐걱삐걱 시켜, 필사적으로 참고 있는 것 같지만…」
			PRINTFORMW 「무엇을 그렇게 참고 있는 거야…?」
			CALL HTMLPRINTW(@"「… 은 지금 들어도, 조금 무리같구나…%HTML_PS_K39()%」")
		ELSE
			PRINTFORMW 「뭔가 필사적으로 견디고 있는 것 같습니다만, 도대체 어떻게 한 것이지요?」
			CALL HTMLPRINTW(@"「아, 대답하지 않아도 괜찮아요. 어차피 도취되어 듣지 않기 때문에. 우훗%HTML_PS_K39()%」")
		ENDIF
		RETURN 1
	ENDIF

	;오나홀 장착중
	IF TEQUIP:오나홀
		IF FAV_RATING
			PRINTFORMW 「필사적으로 참고 있는 것 같지만, 지금 내가 훑으면 어떻게 되어 버릴까?」
			PRINTFORMW 「저기, 어때?   좀 더 분명히 말해」
		ELSE
			PRINTFORMW 「이를 악물지 않으면, 가 버릴 것 같습니까?」
			PRINTFORMW 「내가 움직여 봐도 괜찮아요?   킥킥…」
		ENDIF
		RETURN 1
	ENDIF

	;그 이외
	IF FAV_RATING
		PRINTFORMW 「참지 않으면 가 버릴 것 같아?」
		CALL HTMLPRINTW(@"「후훗, 사랑스럽네요%HTML_PS_K39()%」")
	ELSE
		PRINTFORMW 「참지 않으면 가 버릴 것 같습니까?」
	ENDIF
	RETURN 1
ENDIF

;COM15, 쾌감을받아들인다
;========================
IF IS_NOWCOMNAME("쾌감을받아들인다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;오나홀 장착중
	IF TEQUIP:오나홀
		IF FAV_RATING
			PRINTFORML 「즐겨 받아지고 있는 것 같구나?」
			PRINTFORMW 「그렇게 기분 좋으면, 이대로 그것안에 내 버리면?」
		ELSE
			PRINTFORMW 「우후, 기분 좋은 것 같고 최상이네요♪」
			PRINTFORMW 「내가 훑어 좀 더 자주(잘) 해 줄까요?」
		ENDIF
		;페이드 아웃
		CALL FADE_TO_CLEARLINE(20, @"「%RAND_SPLIT("후후/키득")%…」")
		RETURN 1
	ENDIF

	;그 외
	{
	CALL HTMLPRINTW(@"「\@ FAV_RATING 
	? 솔직하고 사랑스럽다%HTML_AH2_K39()%  이제(벌써) 키스 하고 싶어져 버린다%RAND_SPLIT("원/")% 
	# 솔직하게 느껴 주면, 나도 기쁩니다%HTML_PS_K39()% \@」")
	}
	RETURN 1
ENDIF


;////////////////////////////
;REACT1, 자신에게로의기분을얘기한다
;////////////////////////////
[SKIPSTART]

·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다

·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…
  ├1=그런 일이 있을 이유 없다!
  ├2=곤란하네요… 좋아하지 않다고 말하면 화내?
  ├3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다
  ├4=분명히 좋아라고 고했다
  └5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)

[SKIPEND]
@KOJO_MALE_REACT_K39_1
#DIM DYNAMIC FAV_RATING
FAV_RATING = GET_KFLAG_K39("상사상애")

;부하 2라면 REACT를 날린다
SIF GET_COM_RANK() == "무리를 한 행동"
	RETURN 0

;REACT1_0
;「싫지 않은… 일지도 모른다…」 // REACT분류가 소극적 따르는 것 경우
;================================
IF !TFLAG:ACT파생
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORMW 「이, 이제 와서 그것은 없지요!?   조금 심하지 않아…?」
	ELSE
		PRINTFORML 「그래, 입니까…」
		PRINTFORMW 「일단 미움받지 않다는 일로, 기뻐해도 괜찮을까요…」
	ENDIF
	RETURN 1
ENDIF

;REACT1_1
;「그런 일이 있을 이유 없다!」 // 리액션이강하게대답한다의 경우
;============================
;REACT분류는 "거부"에 세트 된다
IF TFLAG:ACT파생 == 1
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF 0
	ELSEIF FAV_RATING
		PRINTFORML 「하? 도대체 무슨 말을 하고 있어!?」
		PRINTFORMW 「의미를 모르지만?」
	ELSEIF AFFECTION("몹시 사랑함", (FLAG:주인님)) && TALENT:TARGET:주인님
		PRINTFORMW 「액신이라고 해도 한사람의 여자입니다. 너무 심한 일, 말하지 마……」
		PRINTFORMDW %CALLNAME:TARGET%는 몸을 진동시켜 울 것 같은 얼굴로 숙여 버렸다…
	ELSE
		PRINTFORMW 「그래, 입니까…」
	ENDIF
	RETURN 1
ENDIF

;REACT1_2,
;야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」 // 리액션이빈정거린다의 경우
;==============================================
IF TFLAG:ACT파생 == 2
	;조교 대상에의 호감도 3000이상
	IF AFFECTION("맹목적으로 사랑", (FLAG:주인님)) && TALENT:TARGET:주인님 && !FAV_RATING
		PRINTFORMW 「그, 그런! 짓궂다니, 심하다…」
		PRINTFORMW 「나는…,%CNAME(MASTER, TARGET)%의 일, 이렇게……」
		PRINTFORMDW %CALLNAME:TARGET%는 당장 울기 시작할 것 같다…
		RETURN 1
	ENDIF

	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF 0
	ELSEIF FAV_RATING
		PRINTFORMW 「화내요!? 당연하겠지요」
	ELSEIF AFFECTION("몹시 사랑함", (FLAG:주인님)) && TALENT:TARGET:주인님
		PRINTFORMW 「부탁 이제 그만두어! 그 이상, 말하지 마…」
	ELSE
		PRINTFORMW 「별로? 분노는 하지 않지만. 그렇지만…」
	ENDIF
	RETURN 1
ENDIF

;REACT1_3
;「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」 // REACT분류가 적극적 따르는 것으로 조교 대상 호의가 적은 경우
;========================================================== // 혹은 REACT분류가 태연하게 따른다
IF TFLAG:ACT파생 == 3
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF 0
	ELSEIF FAV_RATING
		PRINTFORMW 「나, 그렇게 심한 일 해 왔을까…」
	ELSEIF AFFECTION("몹시 사랑함", (FLAG:주인님)) && TALENT:TARGET:주인님
		PRINTFORML 「그래, 입니까…」
		PRINTFORMW 「나, 조금은 기대해도 될까요」
	ELSE
		PRINTFORML 「그렇습니까…?」
		PRINTFORMW 「그렇지만, 응. 곤란하군요…」
	ENDIF
	RETURN 1
ENDIF

;REACT1_4
;분명히 좋아한다고 고한 // REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 1000이상
;======================
IF TFLAG:ACT파생 == 4
	;첫회
	IF FIRSTREACT(40)
		IF FAV_RATING
			PRINTFORML 「에에, 알고 있었어요?」
			CALL FADE_TO_CLEARLINE(20, "「킥킥…」")
			CALL FADEIN(15, "「그렇지만…」")
			PRINTFORMW 「정말로 기쁜…… 여기서 고마워요라고 말하는 것도 이상하지만…」
			PRINTFORMW 「고마워요, 이 일은 잊지 않아요. 쭉……」
			;여기에 뭔가 가르쳐도 좋은 것 같은 생각도 들지만, 경우에 따라서는 무의미하게 될 가능성도
		ELSEIF AFFECTION("몹시 사랑함", (FLAG:주인님)) && TALENT:TARGET:주인님
			PRINTFORML 「정말로 나 따위로 좋을까요?」
			PRINTFORMW 「그렇지만, 기쁩니다…」
		ELSE
			PRINTFORMW 「지, 진심입니까?」
			PRINTFORMW 「그 기분은 기쁩니다만, 나는…」
		ENDIF
		RETURN 1
	ENDIF

	;역강간으로 질내사정의 뒤
	IF TFLAG:전회질내사정 == 2
		{
		CALL HTMLPRINTW(@"「우후,\@ FAV_RATING 
		? 우대신, 고마워요 # 감사합니다 \@%HTML_PS_K39()%」")
		}
		RETURN 1
	ENDIF

	;그 외
	IF 0
	ELSEIF FAV_RATING
		PRINTFORMW 「에에, 알고 있어요. 당연하겠지?」
		PRINTFORMW 「킥킥…」
	ELSEIF AFFECTION("맹목적으로 사랑")
		PRINTFORML 「기쁩니다. 고마워요…」
		PRINTFORMW 「나도%CNAME(MASTER, TARGET)%의 일, 정말 좋아합니다…」
	ELSE
		PRINTFORMW 「기쁩니다. 에에, 정말로…」
	ENDIF
	RETURN 1
ENDIF

;REACT1_5,
;「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」 // REACT분류가 받아들이는 것 경우
;========================================================
IF TFLAG:ACT파생 == 5
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;V성교 봉사로 질내사정의 뒤
	IF TFLAG:전회질내사정 == 2
		IF FAV_RATING
			PRINTFORML 「나의 안에 이만큼 쏟아 둬, 좀 더 굉장한 일이라든지…」
			PRINTFORMW 「욕심부리는 것도 대충 해두지 않으면, 불필요한 액을 끌어 들여 버릴지도 몰라?」
			PRINTFORM 「뭐, 액신인 나와 이런 일 하고 있는 시점에서…, 이제(벌써) 이제 와서는 생각도 들지만. 후후
			CALL PRINT_SYMBOL_K39
		ELSE
			PRINTFORML 「나에게 질내사정하는 것보다 굉장한 일, 입니까…」
			PRINTFORMW 「저, 혹시…… 엉덩이로 하고 싶습니까?」
		ENDIF
		RETURN 1
	ENDIF

	;상사상애
	IF FAV_RATING
		SELECTCASE RANDREACT(2, 52)
			CASE 0
				PRINTFORMW 「이제 와서 좀 더 굉장한 일 같은거 없다고 생각하지만?」
				PRINTFORMW 「재미있는 일을 말해요. 킥킥…」
			CASE 1
				PRINTFORMW 「～응, 좀 더 굉장한 일이라고 말해져도 곤란하지만…」
				PRINTFORMW 「좀 더 구체적으로 말해 주지 않겠어?」
		ENDSELECT
		RETURN 1
	ENDIF

	;조교 대상에의 호감도 1000이상
	IF AFFECTION("권애")
		SELECTCASE RANDREACT(3, 53)
			CASE 0
				PRINTFORML 「좀 더 굉장한 일, 입니까…?」
				CALL HTMLPRINTW(@"「좋아요,%CNAME(MASTER, TARGET)%가 그렇게 바란다면%HTML_PS_K39()%」")
			CASE 1
				PRINTFORMW 「어떤 일을 하면 좋을 것입니다%RAND_SPLIT("인가/")%…」
				PRINTFORMW 「좀 더 자세하게 가르쳐 받을 수 있습니까? 　%RAND_SPLIT("")%후후」
			CASE 2
				PRINTFORMW 「어차피라면, 구체적으로 말해 받는 것이 좋습니다만…」
				PRINTFORMW 「그렇지만%CNAME(MASTER, TARGET)%자신, 그것을 입에 내 말하는 것은 부끄러운 것 같네요?」
				CALL FADE_TO_CLEARLINE(15, @"「%RAND_SPLIT("후후/키득")%…」")
		ENDSELECT
		RETURN 1
	ENDIF

	;그 이외
	PRINTFORMW 「정말입니까…? 그렇지만, 나는 더 이상 무엇을 하면 좋을 것입니다?」
	PRINTFORMDW 히나는 뭔가를 망상해 버렸는지, 부끄러움에 기절 하고 있다…
	RETURN 1
ENDIF

;;;;;;;;;;;;;;;;;;;
;이하 SELECTCOM 분기;
;;;;;;;;;;;;;;;;;;;

;COM0:침묵한다
;===============
IF IS_NOWCOMNAME("침묵한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF 0
	;파생하면 연주한다
	ELSEIF TFLAG:REACT파생 == 1
		RETURN 0
	ELSEIF FAV_RATING
		PRINTFORMW 「무엇으로 여기서 입다무는 거야?」
		PRINTFORMW 「조금 의미를 모르지만?」
		RETURN 1
	ELSEIF AFFECTION("맹목적으로 사랑", (FLAG:주인님)) && TALENT:TARGET:주인님
		PRINTFORMW 「그 침묵이%CNAME(MASTER, TARGET)%의 대답이군요」
		PRINTFORMW 「그래, 군요…. 액신 같은거 싫어, 군요……」
		RETURN 1
	ELSE
		PRINTFORMW 「그것이%CNAME(MASTER, TARGET)%의 대답이군요…」
		RETURN 1
	ENDIF
ENDIF

;COM7:용서를빈다
;================
IF IS_NOWCOMNAME("용서를빈다")
	;벌포인트 가산
	IF TFLAG:REACT파생 == 1
		IF FAV_RATING
			PRINTFORMW 「저기, 장난치고 있는 거야?」
			PRINTFORMDW %CALLNAME:TARGET%는%RAND_SPLIT("화나 있다/감정을 해쳤다")%같다…
		ELSE
			PRINTFORML 「나의 일을 좋아하는가 어떤가 (듣)묻고 있습니다?」
			PRINTFORMW 「허락해라는건 어떻게 말하는 일입니까…」
		ENDIF
		RETURN 1
	ENDIF

	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORMW 「하? 허락해라는건 어떻게 말하는 일!?」
		PRINTFORMW 「분명하게 설명해 받을 필요가 있다고 생각합니다만―?」
	ELSEIF AFFECTION("맹목적으로 사랑")
		PRINTFORMW 「용서를빈다(정도)만큼 내가 싫습니까…」
	ELSE
		PRINTFORML 「저…, 허락한다든가, 허락하지 않는다든가」
		PRINTFORMW 「그런 일은 아닙니다만…」
	ENDIF
	RETURN 1
ENDIF

;COM33:무서워한다
;=============
IF IS_NOWCOMNAME("무서워한다")
	;벌포인트 가산
	IF TFLAG:REACT파생 == 1
		IF FAV_RATING
			PRINTFORMW 「그러한 것 그만두었으면 좋지만…」
			PRINTFORMW 「농담이라도 다치는 일도 있는거야?」
		ELSE
			PRINTFORMW 「그렇게 명백하게 두려워해지면, 다칩니다…」
		ENDIF
		RETURN 1
	ENDIF

	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		{
		PRINTFORMW 「%RAND_SPLIT(
		"농담이라고 해도 웃을 수 없지만/" + 
		"그것, 무슨 농담/" + 
		"뭐야 그것, 장난치고 있어")%!?」
		}
	ELSE
		PRINTFORML 「%CNAME(MASTER, TARGET)%에 있어, 나는 그러한 존재입니까…」
		PRINTFORMW 「그래, 군요. 액신인거야…」
	ENDIF
	RETURN 1
ENDIF


;////////////////////////////
;REACT2, 성에대한이야기를한다
;////////////////////////////
@KOJO_MALE_REACT_K39_2
#DIM DYNAMIC FAV_RATING
FAV_RATING = GET_KFLAG_K39("상사상애")

;부하 2라면 REACT를 날린다
SIF GET_COM_RANK() == "무리를 한 행동"
	RETURN 0

;지문은@REACTION_MESSAGE로 범용 처리되는 분만

;COM1:침묵한다
;==============
IF IS_NOWCOMNAME("침묵한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	{
	PRINTFORMW 「\@ FAV_RATING 
	? 뭐야 입다물어 버려… # 그다지 (듣)묻고 싶지 않았던 것입니까?  \@」
	}
	RETURN 1
ENDIF

;COM1:여리게대답한다
;====================
IF IS_NOWCOMNAME("여리게대답한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	{
	PRINTFORMW 「\@ FAV_RATING 
	? 뭐야 그것. 그다지 재미있지 않았다 
	# 흥미가 없는 것일까요… \@?」
	}
	RETURN 1
ENDIF

;COM7:용서를빈다
;================
IF IS_NOWCOMNAME("용서를빈다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	{
	PRINTFORMW 「\@ FAV_RATING 
	? 인의 이야기는 끝까지 (들)물어라고 배우지 않았어?  
	# 안됩니다. 사람의 이야기는 끝까지 (들)물어 주세요 \@」
	}
	RETURN 1
ENDIF

;COM8:기분좋게해줘
;====================
IF IS_NOWCOMNAME("기분좋게해줘")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORML 「이야기를 들어 흥분해 버렸어?」
		PRINTFORMW 「그러면, 즉시 실천해 봐?」
	ELSE
		PRINTFORML 「흥분해 버린 것입니까?」
		CALL HTMLPRINTW(@"「후훗, 사랑스러운 곳 있군요%HTML_PS_K39()%」")
	ENDIF
	RETURN 1
ENDIF

;COM10:부끄러워한다
;===================
IF IS_NOWCOMNAME("부끄러워한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		SELECTCASE RANDREACT(2, 100)
			CASE 0
				PRINTFORMW 「그렇게부끄러워한다 같은 이야기였다%RAND_SPLIT("? /그럴까…")%」
			CASE 1
				PRINTFORMW 「언제나 좀 더 굉장한 일 하고 있는데…」
				CALL HTMLPRINTW(@"「부끄러워한다는, 재미있는거네…%HTML_PS_K39()%」")
			CASE 2
				PRINTFORML 「그렇게 부끄러워해지면, 조금…」
				PRINTFORMW 「여기까지 부끄러워지지 않아……」
		ENDSELECT
	ELSE
		PRINTFORML 「부끄럽지만, 좀 더 (듣)묻고 싶을 것입니다?」
		CALL HTMLPRINTW(@"「우후%HTML_PS_K39()%  에서는, 이런 것은 어떻습니까…」")
		PRINTFORMDW %CALLNAME:TARGET%는 한층 더 과격한 이야기를 시작했다…
	ENDIF
	RETURN 1
ENDIF

;COM33:무서워한다
;==============
IF IS_NOWCOMNAME("무서워한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORMW 「무서워한다 같은 이야기가 아니라고 생각하지만?」
	ELSE
		PRINTFORML 「조금 이야기가 과격했습니까…?」
		{
		CALL HTMLPRINTW(@"「\@ TALENT:MASTER:장신 
		? 겉보기와는 달리,  # 의외로 
		\@신선한 응이군요%HTML_PS_K39()%」")
		}
	ENDIF
	RETURN 1
ENDIF

;COM50:자위하기시작한다
;====================
IF IS_NOWCOMNAME("자위하기시작한다")
	{
	PRINTFORML 「\@ FAV_RATING 
	? 흥분해 인내 할 수 없게 되어 버렸어 # 그렇게 흥분해 버린 것입니까 \@?」
	}
	{
	CALL HTMLPRINTW(@"「\@ FAV_RATING 
	? 킥킥…, 재미있는 사람 # 재미있는 사람이군요 
	\@%HTML_PS_K39()%」")
	}
	RETURN 1
ENDIF


;////////////////////
;REACT3, 상냥하게위로한다
;////////////////////
@KOJO_MALE_REACT_K39_3
#DIM DYNAMIC FAV_RATING
FAV_RATING = GET_KFLAG_K39("상사상애")

;부하 2라면 REACT를 날린다
SIF GET_COM_RANK() == "무리를 한 행동"
	RETURN 0

;지문은@REACTION_MESSAGE로 범용 처리되는 분만

;REACT3, COM0:침묵한다
;=====================
IF IS_NOWCOMNAME("침묵한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		{
		PRINTFORMW 「%RAND_SPLIT("
		어째서입다물고있어？　응、기분고쳐요…/" + 
		"이야기라고 주지 않으면, 아무것도 모르지만?")%」
		}
	ELSE
		{
		PRINTFORMW 「%RAND_SPLIT(
		"나, 뭔가 비위에 거슬리는 일에서도 말해 버린 것입니까…/" + 
		"지금은 아무것도 말하고 싶지 않은 느낌입니까?")%」
		}
	ENDIF
	RETURN 1
ENDIF

;REACT3, COM1:여리게대답한다
;===========================
IF IS_NOWCOMNAME("여리게대답한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		{
		PRINTFORMW 「%RAND_SPLIT(
		"그 만큼 기운이 없으면, 조금 걱정으로 되어요…/" + 
		"응, 정말로 괜찮아? /" + 
		@"그러한 태도, 답지 않다고 생각하지만\@ TENSION(TARGET, 8) == "높다" ? ?  # … \@")%」
		}
	ELSE
		{
		PRINTFORMW 「%RAND_SPLIT(
		"기운이 없네요… 괜찮습니까? /" + 
		"불안이나 걱정거리가 있다면, 숨기지 않고 말해 주면 고맙습니다만…")%」
		}
	ENDIF
	RETURN 1
ENDIF

;REACT3, COM2:무뚝뚝하게대답한다
;=============================
IF IS_NOWCOMNAME("무뚝뚝하게대답한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	{
	PRINTFORMW 「\@ FAV_RATING 
	? %RAND_SPLIT("기분 나쁜 것 같구나…/뭐야 그것. 사랑스럽지 않네요……")% 
	# %RAND_SPLIT("기분 나쁜 것 같네요…/그다지 감탄하지 않네요. 그러한 태도……")% \@」
	}
	RETURN 1
ENDIF

;REACT3, COM3:붙임성 있게 응답한다
;=============================
IF IS_NOWCOMNAME("붙임성좋게대답한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		SELECTCASE RANDREACT(2, 30)
			CASE 0
				CALL HTMLPRINTW(@"「%RAND_SPLIT("/후")%%HTML_PS_K39()%」")
			CASE 1
				PRINTFORMW 「언제나 그런 느낌이라면 좋은데?」
				PRINTFORMW 「킥킥…」
		ENDSELECT
	ELSE
		CALL HTMLPRINTW(@"「네, 순수가 제일이군요…%HTML_PS_K39()%」")
	ENDIF
	RETURN 1
ENDIF

;REACT3, COM4:강하게대답한다
;===========================
IF IS_NOWCOMNAME("강하게대답한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORMW 「그것, 사랑스럽지 않아?」
	ELSE
		PRINTFORML 「조금 거역해 보고 싶어졌다든가?」
		PRINTFORMW 「어려운 사람이군요…」
	ENDIF
	RETURN 1
ENDIF

;REACT3, COM5:빈정거린다
;=======================
IF IS_NOWCOMNAME("빈정거린다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORMW 「저기, 어째서…… 어째서 그런 일 말해……」
	ELSE
		PRINTFORML 「사람의 호의를 솔직하게 받아들일 수 없습니까?」
		PRINTFORMW 「어려운 사람이군요…」
	ENDIF
	RETURN 1
ENDIF

;REACT3, COM6:더하드하게
;===============================
[SKIPSTART]
	좀 더 하드하게, 라고 하는 것은 가학계는 아니고 이상계를 유발하는 것
	피스트계나 육변기()가 발생하지 않는 조교자의 경우, 방치플레이가 되어 버리는 것이 저것
	벌포인트가 모여 있으면 가학계에 기울 가능성도 없는 것은 아니지만
[SKIPEND]

IF IS_NOWCOMNAME("더하드하게")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;벌포인트 가산
	IF TFLAG:REACT파생 == 1
		IF FAV_RATING
			PRINTFORML 「엣, 좀 더 하드하게?」
			PRINTFORMW 「벌이라도 좋은거야?」
		ELSE
			PRINTFORML 「좀 더 하드하게, 입니까…」
			PRINTFORMW 「저, 벌이라도 좋을까요?」
		ENDIF
		RETURN 1
	ENDIF

	;그 이외
	IF FAV_RATING
		PRINTFORMW 「그것은 좋지만, 사양말고 말해 버려도 괜찮은거야?」
	ELSE
		CALL HTMLPRINTW(@"「-알았습니다. 그럼, 각오 해 주세요%HTML_PS_K39()%」")
	ENDIF
	RETURN 1
ENDIF

;REACT3, COM7:용서를빈다
;========================
IF IS_NOWCOMNAME("용서를빈다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;벌포인트 가산
	IF TFLAG:REACT파생 == 1
		{
		CALL HTMLPRINTW(@"「\@ FAV_RATING 
		? 후후%HTML_PS_K39()%  다～메!  
		# 안됩니다.%CNAME(MASTER, TARGET)%에는 벌이 필요같기 때문에… \@」")
		}
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORMW 「뭐야 그것… 상냥하게 여겨지는 것이 싫어!?」
	ELSE
		PRINTFORML 「상냥하게 여겨지는 것이 싫습니까?
		PRINTFORMW 「곤란한 사람이군요…」
	ENDIF
	RETURN 1
ENDIF

;REACT3, COM8:기분좋게해줘
;===========================
IF IS_NOWCOMNAME("기분좋게해줘")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORML 「졸라대게 해도 당돌한 느낌이지만…」
		CALL HTMLPRINTW(@"「%CNAME(MASTER, TARGET)%가 그렇게 말한다면 방법 없네요…%HTML_PS_K39()%」")
	ELSE
		PRINTFORML 「우후, 졸라댐입니까?」
		CALL HTMLPRINTW(@"「좋아요, 좀 더 기분 좋게 해 줍니다…%HTML_PS_K39()%」")
	ENDIF
	RETURN 1
ENDIF

;REACT3, COM10:부끄러워한다
;==========================
IF IS_NOWCOMNAME("부끄러워한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		CALL HTMLPRINTW(@"「후후, 사랑스럽다…%HTML_PS_K39()%」")
		PRINTFORML 「부끄러워한다일 같은거 없는데」
	ELSE
		PRINTFORML 「아라, 부끄러워한다일 같은거 없다고 생각합니다만」
		CALL HTMLPRINTW(@"「사랑스럽네요%HTML_PS_K39()%」")
	ENDIF
	RETURN 1
ENDIF

;REACT3, COM11:싫어한다
;====================
IF IS_NOWCOMNAME("싫어한다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORMW 「무엇이 그렇게 싫어…」
	ELSE
		PRINTFORMW 「위로받는 것이 그렇게 싫습니까?」
		PRINTFORMW 「곤란하군요…」
	ENDIF
	RETURN 1
ENDIF

;REACT3, COM13:겁없게웃는다
;========================
IF IS_NOWCOMNAME("겁없게웃는다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		{
		PRINTFORMW 「그런 것 붙여 웃는 것은 조금 무리가\@ 
		FAV_RATING ? 있는 것 같구나 # 없습니까 \@?」
		}
		CALL FADE_TO_CLEARLINE(15, @"「\@ FAV_RATING ? 킥킥 # 후후 \@…」")
		RETURN 1
	ENDIF

	;그 외
	PRINTFORMW 「\@ FAV_RATING ? 조금, 그것 사랑스럽지 않아?  # 상당히 비뚤어지고 있군요… \@」
	RETURN 1
ENDIF

;REACT3, COM14:쾌감을참는다
;============================
IF IS_NOWCOMNAME("쾌감을참는다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		IF FAV_RATING
			PRINTFORML 「몸 삐걱삐걱 시켜, 필사적으로 참고 있는 것 같지만…」
			PRINTFORMW 「무엇을 그렇게 참고 있는 거야…?」
			CALL HTMLPRINTW(@"「… 이라고 (듣)묻는 것은, 조금 무리가 있는 것 같구나%HTML_PS_K39()%」")
		ELSE
			PRINTFORMW 「뭔가 필사적으로 견디고 있는 것 같습니다만, 어떻게 한 것이지요?」
			PRINTFORMW 「아, 대답하지 않아도 괜찮아요. 어차피 들리지 않기 때문에. 우훗」
		ENDIF
		RETURN 1
	ENDIF

	;그 이외
	IF FAV_RATING
		PRINTFORMW 「참지 않으면 가 버릴 것 같아?」
		CALL HTMLPRINTW(@"「후후, 사랑스럽네요…%HTML_PS_K39()%」")
	ELSE
		PRINTFORMW 「참지 않으면 가 버릴 것 같습니까?」
		CALL FADE_TO_CLEARLINE(15, "「후후…」")
	ENDIF
	RETURN 1
ENDIF

;REACT3, COM15:쾌감을받아들인다
;==============================
IF IS_NOWCOMNAME("쾌감을받아들인다")
	;볼개그 장비중
	IF TEQUIP:볼개그
		CALL PRINT_REACT_CATEGORY1_1_K39
		RETURN 1
	ENDIF

	;그 외
	{
	CALL HTMLPRINTW(@"「\@ FAV_RATING 
	? 솔직하고 사랑스럽다%HTML_PS_K39()%  이제(벌써) 키스 하고 싶어져 버린다 
	# 솔직하게 느껴 주면 나도 기쁩니다%HTML_PS_K39()%\@」")
	}
	RETURN 1
ENDIF


;//////////////////
;REACT4, 엄격하게위협한다
;//////////////////
@KOJO_MALE_REACT_K39_4
#DIM DYNAMIC FAV_RATING
FAV_RATING = GET_KFLAG_K39("상사상애")

;부하 2라면 REACT를 날린다
SIF GET_COM_RANK() == "무리를 한 행동"
	RETURN 0

;TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만

;REACT4, COM0:침묵한다
;=====================
IF IS_NOWCOMNAME("침묵한다")
	{
	PRINTFORMW 「\@ FAV_RATING 
	? 조금 너무 위협했는지 해들 
	# 너무 위협해 버린 것입니까 \@…」
	}
	RETURN 1
ENDIF

;REACT4, 여리게대답한다
;======================
IF IS_NOWCOMNAME("여리게대답한다")
	;「이제(벌써) 이런 일 그만두어」
	IF TFLAG:REACT파생 == 1
		IF FAV_RATING
			PRINTFORML 「그런 일 말해…」
			PRINTFORMW 「사실은 좀 더 시라고 갖고 싶은 것이 아닌거야?」
		ELSE
			PRINTFORMW 「%CNAME(MASTER, TARGET)%가 분명하게 하는 일을 (듣)묻지 않는 것이 안 돼요?」
		ENDIF
		RETURN 1
	ENDIF

	;그 외
	{
	PRINTFORMW 「\@ FAV_RATING 
	? 뭔가 말하고 싶으면, 좀 더 큰 소리로 말해 주지 않으면 
	# 저, 자주(잘) 들리지 않습니다만\@…」
	}
	RETURN 1
ENDIF

;REACT4, COM5:빈정거린다
;=======================
IF IS_NOWCOMNAME("빈정거린다")
	IF FAV_RATING
		PRINTFORMW 「짓궂다 라고 상당히 여유군요…」
	ELSE
		PRINTFORML 「정말로 곤란한 사람이군요…」
		PRINTFORMW 「이것도 액의 탓인 것입니까」
	ENDIF
	RETURN 1
ENDIF

;REACT4, COM7:용서를빈다
;=======================
IF IS_NOWCOMNAME("용서를빈다")
	;첫회
	IF FIRSTREACT(70)
		IF FAV_RATING
			PRINTFORMW 「그런 말을 들어도 말야…」
			PRINTFORMW 「여기에도 형편이라는 것이 있는거야?」
		ELSE
			PRINTFORML 「안됩니다, 허락해 주지 않습니다」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 액을 쫓으려면, 어쩔 수 없는 것이랍니다?」
		ENDIF
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORMW 「언제나 허락한다고 생각하면 큰 실수%RAND_SPLIT("야/그러니까")%…」
	ELSE
		PRINTFORML 「나, 이것이라도%RAND_SPLIT("일단 『신』/액신")%이랍니다」
		PRINTFORMW 「너무 바보취급 해 받아서는 곤란합니다……」
	ENDIF
	RETURN 1
ENDIF

;REACT4, COM31:비명을올린다
;==========================
IF IS_NOWCOMNAME("비명을올린다")
	{
	PRINTFORMW 「\@ FAV_RATING 
	? %CNAME(MASTER, TARGET)%의 그런 모습, 그다지 보고 싶지 않은… 
	# 저, 좀 더 조용하게 해 주세요 \@…」
	}
	RETURN 1
ENDIF

;REACT4, COM33:무서워한다
;====================
IF IS_NOWCOMNAME("무서워한다")
	IF FAV_RATING
		PRINTFORMW 「조금 그것 심하지 않아?」
		PRINTFORMW 「나, 다쳤지만…」
	ELSE
		PRINTFORML 「왜 그렇게무서워한다의 것입니까?
		PRINTFORMW 「역시 액신은 기피되는 사람인 것입니까……」
	ENDIF
	RETURN 1
ENDIF


;////////////////////////////
;REACT5, 의상의변경을요구한다
;////////////////////////////
[SKIPSTART]
	·조교 대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
	·REACT분류가"소극적 따른다", "받아들인다"의 경우는 한 장 벗는다
	·REACT분류가"적극적 따른다", "태연하게 따른다"의 경우는 2매 벗는다
	·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
[SKIPEND]
@KOJO_MALE_REACT_K39_5 ;TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
#DIM DYNAMIC FAV_RATING
FAV_RATING = GET_KFLAG_K39("상사상애")

;여기에서는 부하 2를 연주하지 않는다

;[*]REACT분류가"소극적 따른다/받아들인다/적극적 따른다/태연하게 따른다"이외인 경우는 REACT분류 "거부"에 세트 된다

;REACT5_0, 옷을 벗도록(듯이) 명령
;============================
IF !TFLAG:ACT파생
	;소극적으로 따랐다(한 장 벗는다)
	;=========================
	IF IS_COMGRONAME("소극적 따른다/받아들인다")
		;전라가 되었다
		;-------------
		{
		IF (TEQUIP:MASTER:악세사리 + TEQUIP:MASTER:양말 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + 
		TEQUIP:MASTER:하반신윗도리 + TEQUIP:MASTER:상반신윗도리 + TEQUIP:MASTER:전신윗도리) == 0
		}
			{
			CALL HTMLPRINTW(@"「이것으로 이제(벌써) 숨기는 것은\@ FAV_RATING 
			? 없네요 # 없겠네요 \@…%HTML_PS_K39()%」")
			}
			;호의 호감도로 파생
			IF FAV_RATING
				;REACT분류가"받아들인다"
				IF IS_COMGRONAME("받아들인다")
					IF RAND:2
						PRINTFORMW 「후후,%CNAME(MASTER, TARGET)%도 그렇지만도 않은 것 같다. 그렇겠지?」
					ELSE
						PRINTFORMW 「지금부터 나로 되는 일에 기대하고 있겠지요?」 
						CALL HTMLPRINTW(@"「숨겨도\@ TENSION(TARGET, 8) == "높다" ? 다～메 # 안돼 \@…%HTML_PS_K39()%」")
					ENDIF
				;REACT분류가"소극적 따른다"
				ELSE
					PRINTFORMW 「%CNAME(MASTER, TARGET)%는 전부 벗어버린다의, 조금 싫은소리 도미이지만. 후훗」
				ENDIF
			ELSE
				IF IS_COMGRONAME("받아들인다")
					PRINTFORMW 「… 알아요. 앞으로의 일에 기대하고 있습니다?」
					PRINTFORMW 「그런 얼굴 해 부정해도 안됩니다. 후후……」
				ELSE
					PRINTFORMW 「어쩔 수 없어서 느낌이군요? 뭐,%RAND_SPLIT("좋습니다/별로 상관하지 않습니다")%지만」
				ENDIF
			ENDIF
			RETURN 1
		ENDIF

		;완만한 움직임으로 벗었다
		;--------------------
		IF IS_COMGRONAME("받아들인다")
			IF FAV_RATING
				{
				CALL HTMLPRINTW(@"「\@ TENSION(TARGET, 8) == "높다" || IS_CONDITION(TARGET, "정욕") 
				? 후후… 속상하지만, 흥분해 버린다%HTML_PS_K39()% 
				# 상당히 거드름 붙여 주지 않은… \@」")
				}
			ELSE
				PRINTFORMW 「상당히 거드름 붙여 주는군요?」
				PRINTFORMW 「뭐, 별로 당황할 필요도 없지만」
			ENDIF
			RETURN 1
		ENDIF

		;어쩔 수 없이 벗었다
		;--------------
		IF IS_COMGRONAME("소극적 따른다")
			{
			CALL HTMLPRINTW(@"「\@ FAV_RATING 
			? 그%HTMLFONT("<b>『아휴, 어쩔 수 없구나』</b>", , COLOR2_K39("WHITE"))%는 얼굴 그만두어 주지 않겠어?  
			# %RAND_SPLIT("말해져 어쩔 수 없이, 라는 느낌이군요. 별로 좋지만…/벗는 것은 싫었던 것입니까…?")% \@」")
			}
			RETURN 1
		ENDIF
	ENDIF

	;적극적으로 따랐다(2매 벗는다)
	;=========================
	IF IS_COMGRONAME("적극적 따른다/태연하게 따른다")
		;전라가 되었다
		;------------
		{
		IF (TEQUIP:MASTER:악세사리 + TEQUIP:MASTER:양말 + TEQUIP:MASTER:하반신속옷 + TEQUIP:MASTER:상반신속옷 + 
		TEQUIP:MASTER:하반신윗도리 + TEQUIP:MASTER:상반신윗도리 + TEQUIP:MASTER:전신윗도리) == 0
		}
			{
			PRINTFORML 「후후,\@ IS_COMGRONAME("적극적 따른다") ? 적극적 # 솜씨가 좋아요 \@\@ 
			FAV_RATING ? 군요 # 군요 \@. 그렇지만…」
			}
			IF FAV_RATING
				PRINTFORMW 「그렇게 당당히 보게 되면, 내 쪽이 조금 부끄러워져 버릴지도…」
				SIF !RAND:3
					CALL HTMLPRINTW(@"「～응이라고%HTML_PS_K39()%  이제 와서 지난다 라고 말해질 것 같지만. 후후」")
			ELSE
				PRINTFORMW 「거기까지 당당히 보게 되면, 여기가 부끄러워져 버립니다…」
				SIF !RAND:3
					PRINTFORMW 「뭐, 내가 말할 수 있던 의리가 아니지만. 키득」
			ENDIF
			RETURN 1
		ENDIF

		;그 외
		;------
		{
		CALL HTMLPRINTW(@"「\@ FAV_RATING 
		? 우대신%HTML_PS_K39()%  빨리 시 싶어서 기다릴 수 없는거야?  
		# 적극적으로 매우 좋네요%HTML_PS_K39()% \@」")
		}
		RETURN 1
	ENDIF

	;벗지 않았다
	;============
	IF IS_COMGRONAME("거부")
		;부끄러워서 벗겨지지 않았다
		;------------------------
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
			;첫회
			IF FIRSTREACT(50)
				IF FAV_RATING
					PRINTFORMW 「%CNAME(MASTER, TARGET)%가 부끄럽다고 말하기 시작하는 것은 조금 의외이구나…」
					PRINTFORMW 「정직…, 이제 그런 단계가 아니라고 생각하지만?」
				ELSE
					PRINTFORML 「나에게 보여지는 것이 그렇게 부끄럽습니까?」
					CALL HTMLPRINTW(@"「상당히 순수하네요%HTML_PS_K39()%」")
				ENDIF
				RETURN 1
			ENDIF

			;그 외
			IF FAV_RATING
				PRINTFORMW 「이제 와서 부끄럽다든가……, 조금 그것은 없는 것이 아니야?」
			ELSE
				PRINTFORML 「곤란하군요…」
				PRINTFORMW 「벗어 받지 않으면, 액은 쫓을 수 없습니다만…」
			ENDIF
			RETURN 1
		ENDIF

		;싫어해 벗지 않았다
		;--------------------
		IF PALAM:MASTER:반항 > PALAM:MASTER:치정
			{
			PRINTFORMW 「\@ FAV_RATING 
			? 옷을 입은 채로 시 싶은거야 
			# 옷을 입은 채로 하는 것을 좋아합니까 \@?」
			}
			RETURN 1
		ENDIF
	ENDIF
ENDIF

;REACT5_1, 옷을 입도록(듯이) 명령
;============================
IF TFLAG:ACT파생 == 1
	;소극적으로 따랐다(한 장 입는다)
	;=========================
	IF IS_COMGRONAME("소극적 따른다/받아들인다")
		;특정의 조건인 경우
		;--------------------
		IF FAV_RATING && IS_CONDITION(TARGET, "정욕")
			PRINTFORML 「저기, 빨리 입어!」
			PRINTFORMW 「이제(벌써), 속상해서……. 기다릴 수 있지 않게 되어 버린다…」
			RETURN 1
		ENDIF

		;그 외
		IF FAV_RATING
			PRINTFORML 「일부러 한 장씩이라든지……」
			PRINTFORMW 「적당, 속상한데요?」
		ELSE
			PRINTFORMW 「저, 좀 더 빨리 입었으면 좋겠다의이지만…」
		ENDIF
		RETURN 1
	ENDIF

	;적극적으로 따랐다(2매 입는다)
	;=========================
	IF IS_COMGRONAME("적극적 따른다/태연하게 따른다")
		{
		CALL HTMLPRINTW(@"「\@ FAV_RATING 
		? 후후, 그렇게 당황하지 않아도 괜찮은데… 
		# 그것으로 좋아요. 자꾸자꾸 입어 주세요 
		\@%HTML_PS_K39()%」")
		}
		RETURN 1
	ENDIF

	;입지 않았다…
	;============
	IF IS_COMGRONAME("거부")
		{
		PRINTFORML 「\@ FAV_RATING 
		? 무슨 일이야? 벌거벗은 채로 시 싶은거야 
		# 어머나, 알몸이 좋습니까… \@?」
		}
		{
		CALL HTMLPRINTW(@"「\@ FAV_RATING 
		? 뭐, 나는 그런데도 좋지만 
		# 나는 상관하지 않지만 \@…%HTML_PS_K39()%」")
		}
		RETURN 1
	ENDIF
ENDIF


;////////////////////
;REACT6, 합의를요구한다
;////////////////////
@KOJO_MALE_REACT_K39_6
#DIM DYNAMIC FAV_RATING
FAV_RATING = GET_KFLAG_K39("상사상애")

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;조교 대상의 리액션에 응해 REACT분류 마다 파생한다;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;REACT6_0, 작은 소리로 승낙하는 // REACT분류가 소극적 따르는 것 경우
;============================
IF IS_COMGRONAME("소극적 따른다")
	PRINTFORMW 「기쁘다\@ !FAV_RATING ? 입니다 # \@……, 고마워요……」
	RETURN 1
ENDIF

;REACT6_1, 마음대로 하면 좋다고 대답한 // REACT분류가 태연하게 따르는 것 경우
;==================================
IF IS_COMGRONAME("태연하게 따른다")
	PRINTFORMW 「상당히 던지고 해\@ FAV_RATING ? 인 거네?  # 이군요.\@  마치 인사같이…」
	PRINTFORMW 「\@ FAV_RATING ? 뭐 # \@에서도……,%CNAME(MASTER, TARGET)%가 그것으로 좋다고 말한다면…」
	RETURN 1
ENDIF

;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고하는 // REACT분류가 적극적 따르는 것 경우
;========================================
IF IS_COMGRONAME("적극적 따른다")
	PRINTFORMW 「기쁘다\@ !FAV_RATING ? 입니다 # \@……, 고마워요……」
	{
	CALL HTMLPRINTW(@"「다만, 거기까지 분명히 말해지면…, 조금 수줍고\@ 
	!FAV_RATING 	? 무릎 # 가격 상승 \@%HTML_PS_K39()%」")
	}
	RETURN 1
ENDIF

;REACT6_3, 기분좋게해줘 주는 것이라면 처음을 주어도 괜찮은 // REACT분류가 받아들이는 것 경우
;============================================================
IF IS_COMGRONAME("받아들인다")
	PRINTFORMW 「어떤 종류의 거래라고 하는 것\@ FAV_RATING ? 야?  # 입니까… \@」
	{
	PRINTFORMW 「본래 바란 형태\@ FAV_RATING 
	? 가 아니다 # 가 아닙니다 
	\@하지만,%CNAME(MASTER, TARGET)%가 그것으로 좋다고 말한다면……」
	}
	RETURN 1
ENDIF

[SKIPSTART]
;REACT6_X, 하는대로
;====================
IF 구상 기입 체크(0) && IS_NOWCOMNAME("하는대로")
	IF FAV_RATING
		PRINTFORMW 「뭐야 그것……, (들)물어 손해본 기분…」
	ELSE
		PRINTFORMW 「들리지 않았다…, 입니까?」
		PRINTFORMW 「조금 안심한 것 같은, 실망한 것 같은…」
		PRINTFORMW 「뭐라고도 말할 수 없는 기분이군요……」
	ENDIF
	RETURN 1
ENDIF
[SKIPEND]


;REACT6_4, 합의하지 않는 // REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서 "거부"에 세트 된다)
;=====================
IF IS_COMGRONAME("거부")
	;첫회 또한 상사상애는 아니다
	;========================
	IF FIRSTREACT(40) && !FAV_RATING
		PRINTFORML 「그래…, 군요. 액신은…」
		PRINTFORMW 「이런 더러워진 여자가 처음은 싫어, 군요……」
		PRINTFORMDW %CALLNAME:TARGET%는 숙여, 당장 울기 시작할 것 같다……
		RETURN 1
	ENDIF

	;그 외
	;------
	IF FAV_RATING
		PRINTFORMW 「이…, 이제 와서 거절당한다고는 생각하지 않았어요……」
		PRINTFORMW 「그렇지만 방법 없네요.%CNAME(MASTER, TARGET)%가 그렇게 말한다면」
	ELSE
		PRINTFORML 「그래, 입니까……」
		PRINTFORMW 「(이)군요…… 나, 액신인거야……」
		PRINTFORMDW %CALLNAME:TARGET%는 얼굴을 돌려, 울고 있는 것 같다……
	ENDIF
	RETURN 1
ENDIF


;//////////////////////////
;REACT7, 벌이라고선언한다
;//////////////////////////
@KOJO_MALE_REACT_K39_7
#DIM DYNAMIC FAV_RATING
FAV_RATING = GET_KFLAG_K39("상사상애")

;부하 2라면 REACT를 날린다
SIF GET_COM_RANK() == "무리를 한 행동"
	RETURN 0

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;선언해도 벌이 발생한다고는 할 수 없다
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;REACT7, COM0:침묵한다
;=====================
IF IS_NOWCOMNAME("침묵한다")
	{
	PRINTFORMW 「\@ FAV_RATING 
	? 뭐야 입다물어 버려 # 여기서 침묵, 입니까 \@……」
	}
	RETURN 1
ENDIF

;REACT7, COM1:여리게대답한다
;===========================
IF IS_NOWCOMNAME("여리게대답한다")
	;「이제(벌써) 이런 일 그만두어」
	IF TFLAG:REACT파생 == 1
		IF FAV_RATING
			PRINTFORMW 「안돼…… 지금쯤 그런 일 말해도, 이미 늦기 때문에……」
		ELSE
			PRINTFORML 「미안해요…」
			PRINTFORMW 「이것도 액을 쫓기 때문에(위해)입니다. 단념해 주세요…」
		ENDIF
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		PRINTFORML 「저기, 지금 뭔가 말했어…?」
		PRINTFORMW 「자주(잘) 들리지 않았지만……」
	ELSE
		PRINTFORML 「저, 뭔가 말했습니까…?」
		PRINTFORMW 「자주(잘) 들리지 않았던 것이지만…」 
	ENDIF
	RETURN 1
ENDIF

;REACT7, COM5:빈정거린다
;=======================
IF IS_NOWCOMNAME("빈정거린다")
	IF FAV_RATING
		{
		PRINTFORMW 「%RAND_SPLIT(
		"지금 그런 일 말하는 것은, 역효과라고 생각하지만? /" + 
		"상당히 여유가 있는 것 같구나…/" + 
		"재미있는 일 말해요…")%」
		}
	ELSE
		{
		PRINTFORMW 「%RAND_SPLIT(
		"아직 그런 일 말할 수 있을 여유가 있군요…/" + 
		"그것은 역효과라고 생각합니다만…/" + 
		"보통 수단으로는 가지 않은 느낌이군요…")%」
		}
	ENDIF
	RETURN 1
ENDIF

;REACT7, COM6:더하드하게
;===============================
IF IS_NOWCOMNAME("더하드하게")
	;벌포인트 가산
	IF TFLAG:REACT파생 == 1
		{
		PRINTFORMW 「\@ FAV_RATING 
		? 보통 벌은 어딘지 부족한…, 이라는 일?  
		# 액 있고군요… 에서도, 정말로 좋습니까?  \@」
		}
		RETURN 1
	ENDIF

	;그 외
	IF FAV_RATING
		{
		PRINTFORMW 「%RAND_SPLIT(
		"그것은 좋지만/" + 
		"나는 상관없지만/" + 
		@"아무튼,%CNAME(MASTER, TARGET)%가 그렇게 말한다면. 그렇지만")%…」
		}
		CALL HTMLPRINTW(@"「%RAND_SPLIT("나중에 후회하지 않게/어떻게 되어도 몰라요")%%HTML_PS_K39()%」")
	ELSE
		PRINTFORMW 「%RAND_SPLIT("이것은 조금 중증입니까/곤란한 사람, 이군요")%…」
	ENDIF
	RETURN 1
ENDIF

;REACT7, COM7:용서를빈다
;=======================
IF IS_NOWCOMNAME("용서를빈다")
	;첫회
	IF FIRSTREACT(0)
		IF FAV_RATING
			PRINTFORMW 「(이)다～째!   킥킥…」
		ELSE
			PRINTFORML 「안됩니다……」
			PRINTFORMW 「이것도 액을 쫓기 때문에(위해)입니다. 체념해 주세요?」
		ENDIF
		RETURN 1
	ENDIF

	;벌포인트 가산
	IF TFLAG:REACT파생 == 1
		IF FAV_RATING
			{
			PRINTFORMW 「%RAND_SPLIT(
			"이제(벌써)! 구별하고가 없는 것 말하지 마/" + 
			"이제 와서 뒤늦음이야. 나쁘지만…/" + 
			"… 유감. 조금 늦었지요")%」
			}
		ELSE
			PRINTFORML 「그런 일 말해도 안됩니다…」
			PRINTFORMW 「역시 벌이 필요같네요?」
		ENDIF
		RETURN 1
	ENDIF

	;그 외
	{
	CALL HTMLPRINTW(@"「\@ FAV_RATING 
	? 다～째…! 허락해 주지 않아요…%HTML_PS_K39()% 
	# 저, 미안해요. 그렇게 말할 수는 없습니다… \@」")
	}
	RETURN 1
ENDIF

;REACT7, COM31:비명을올린다
;==========================
IF IS_NOWCOMNAME("비명을올린다")
	IF FAV_RATING
		PRINTFORMW 「부탁…… 떠드는 것은 그만두어……」
	ELSE
		PRINTFORML 「그렇게 비명을 질러도 안됩니다…」
		PRINTFORMW 「단념해 주세요…?」
	ENDIF
	RETURN 1
ENDIF

;REACT7, COM33:무서워한다
;====================
IF IS_NOWCOMNAME("무서워한다")
	{
	PRINTFORMW 「\@ FAV_RATING 
	? 무서워해 보여도 안돼…… 
	# 무서워해 보여도 안됩니다 \@」
	}
	;우우 n…
	RETURN 1
ENDIF


;//////////////
;REACT8, 고함친다
;//////////////
@KOJO_REACT_K39_8
;TRAIN_MESSAGE2의 처리 없음
RETURN 0


;////////////////////
;REACT9, 기분나쁘게웃는다
;////////////////////
@KOJO_MALE_REACT_K39_9
;TRAIN_MESSAGE2의 처리 없음
SELECTCASE TRAINNAME:SELECTCOM
	CASE "침묵한다"
		PRINTFORMDW 침묵했다%CALLNAME:MASTER%를 무시하는것 같이,%CALLNAME%는 계속 웃었다…
		RETURN 1
	CASE "여리게대답한다"
		PRINTFORMDW %CALLNAME%는%CALLNAME:MASTER%의 말에는 답하지 않고, 언제까지나 웃고 있다…
		RETURN 1
	CASE "용서를빈다"
		PRINTFORMDW 용서를빈다%CALLNAME:MASTER%에는 대답하지 않고,%CALLNAME%는 계속 언제까지나 웃었다…
		RETURN 1
	CASE "비명을올린다"
		PRINTFORMDW %CALLNAME:MASTER%의 비명을 기분에 두는 일 없이,%CALLNAME%는 계속 웃은… 
		RETURN 1
	CASE "무서워한다"
		PRINTFORMDW 무서워한다%CALLNAME:MASTER%를 즐거운 듯이 응시해%CALLNAME%는 계속 웃었다…
		RETURN 1
	;조건으로부터 빠지면 RETURN 0
	CASEELSE
		RETURN 0
ENDSELECT


;////////
;함수류//
;////////

;TEQUIP:볼개그용
;=====================
@PRINT_REACT_CATEGORY1_1_K39
#DIM  DYNAMIC FAV_RATING
#DIMS DYNAMIC STRING
FAV_RATING = GET_KFLAG_K39("상사상애")
STRING    '= FAV_RATING ? "킥킥" # "후후"

SELECTCASE TRAINNAME:SELECTCOM
	CASE "침묵한다"
		IF FAV_RATING
			SELECTCASE AUTORAND(3, 200)
				CASE 0
					PRINTFORMW 「어차피 말할 수 없기 때문에 같다는 일?」
					PRINTFORMW 「%STRING%…」
				CASE 1
					CALL HTMLPRINTW(@"「뭐, 입다물고 있어도 같은 일이군요…%HTML_PS_K39()%」")
				CASE 2
					PRINTFORMW 「어차피 말해도 모르기 때문에 함께이지만…」
					PRINTFORMW 「그렇다 치더라도, 입다물고 있는 것은 어떨까라고 생각해요?」
			ENDSELECT
		ELSE
			PRINTFORMW 「확실히 지금 말할 수 없는 것은 압니다만…」
		ENDIF
	CASE "무뚝뚝하게대답한다"
		{
		PRINTFORMW 「\@ FAV_RATING 
		? 자주(잘) 들리지 않지만…, 기분 나쁜 것 같구나 
		# 잘 모릅니다만…, 기분 나쁜 것 같네요 \@」
		}
	CASE "붙임성좋게대답한다"
		IF FAV_RATING
			PRINTFORMW 「응…, 자주(잘) 알아 들을 수 없지만…」
			PRINTFORMW 「우선 기분은 좋은 것 같구나?」
		ELSE
			PRINTFORMW 「조금 자주(잘) 알아 들을 수 있지 않지만…」
			PRINTFORMW 「즐거운 것 같고 최상이네요?」
		ENDIF
	CASE "더하드하게"
		IF FAV_RATING
			PRINTFORMW 「자주(잘) 들리지 않지만…」
			PRINTFORMW 「내가 괴롭혔으면 좋은거야?」
		ELSE
			PRINTFORMW 「그렇네요, 자주(잘)은 알아 들을 수 있지 않지만…」
			PRINTFORMW 「좀 더 격렬하게 했으면 좋을까요?」
		ENDIF
	CASE "부끄러워한다"
		IF FAV_RATING
			PRINTFORMW 「부끄러워하고 있는 것 같지만, 자주(잘) 알지 않네요…」
		ELSE
			PRINTFORMW 「뭔가 부끄러운 것 같습니다만…」
			PRINTFORMW 「이래서야 자주(잘) 알아 들을 수 없네요…」
		ENDIF
	CASE "싫어한다"
		IF FAV_RATING
			PRINTFORMW 「잘 모르지만…」
			PRINTFORMW 「무엇을 그렇게 싫어하고 있는 거야…?」
		ELSE
			PRINTFORMW 「잘 모릅니다만…」
			PRINTFORMW 「무엇이 그렇게 싫은 것이지요?」
		ENDIF
	CASE "쾌감을받아들인다"
		{
		CALL HTMLPRINTW(@"「\@FAV_RATING 
		? 무슨 말을 하고 있을까 모르지만, 즐겨 받아지고 있는 것 같구나 
		# 좋게 모릅니다만…, 우선 즐겨 받아지고 있는 것 같네요 \@ 」")
		}
		CALL FADE_TO_CLEARLINE(15, "「%STRING%…」")
	CASE "무서워한다"
		{
		PRINTFORMW 「무서워하고 있는 것 같다\@ FAV_RATING 
		? 이지만 # 입니다만 \@좋게 분인가\@ FAV_RATING 
		? 등 없네요 # 선이군요 \@」
		}
	CASEELSE
		IF FAV_RATING
			PRINTFORMW 「저기, 무슨 말을 하고 있는지, 모르지만?」
		ELSE
			PRINTFORMW 「과연 지금 들어야 할 것은 아니었던 것이군요」
		ENDIF
ENDSELECT
SIF IS_NOWCOMNAME("붙임성좋게대답한다/더하드하게/쾌감을받아들인다/여리게대답한다/기분좋게해줘/부끄러워한다/빈정거린다")
	CALL FADE_TO_CLEARLINE(15, "「%STRING%…」")
