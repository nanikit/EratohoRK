;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	히나 구상 고통계 반응, 범용 리액션 구상 파일
;
;Version		Reason
;1. 11			COM 파일을 계통별로 분할
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;=====================================================================
;고통계 반응
;=====================================================================
;·가학계 행동이나 삽입을 수반하는 각종 행동으로 볼 수 있다
;·v1. 010 현재, 고통계에 의존도 변화의 처리 없음
;-------------------------------------------------

;─
;COM30, 아픔을참는다
;─
;·REACT분류:소극적 따른다
;·REACT인상:약간좋은인상
;·SOURCE:아픔이 없는 경우는 튕겨진다
;-------------------------------------------------
;·트리거가 되는 행동, 소스, 파라미터
;·SOURCE:아픔/구속
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:가슴애무/입으로애무/애널애무/애널빨고/키스/파이즈리한다
;ACT분류:도구
;	ACT:바이브/애널바이브/애널비즈
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트
;-------------------------------------------------
;·TFLAG:REACT파생(0=아픔을 인내/1=아프지만기분좋다！)
;-------------------------------------------------
@KOJO_MALE_COM_K39_30
;-------------------------------------------------
VARSET LOCAL
LOCAL:0 = GET_KFLAG_K39("상사상애")
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1:아프지만기분좋다！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	SELECTCASE GET_COM_RANK()
		CASE "자연스러운 행동"
			PRINTFORMW 「다만 아프다고 만\@ LOCAL:0 ? 가 아닌 것 같구나 # 는 아닌 것 같네요 \@…」
		CASE "무리가 없는 행동"
			PRINTFORMW 「후후, 그 소리…… 다만 아픔에 참고 있다\@ LOCAL:0 ? 라는 것이 아닌 것 같구나 # 라고 할 것은 아닌 것 같네요 \@」
		CASE "무리를 한 행동"
			PRINTFORMW 「혹시,%CNAME(MASTER, TARGET)%…… 조금 자주(잘)\@ LOCAL:0 ? 되어 버렸다든가 # 되어 버린 것입니까 \@?」
	ENDSELECT
	PRINTFORM 「\@ LOCAL:0 ? 이봐요… 어때? 속이지 말고, 정직하게 말해 봐 # 후후, 괜찮아요. 이런 일로 경멸하거나는 하지 않기 때문에 \@…
	CALL PRINT_SYMBOL_K39
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0:아픔을 인내
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;무리를 한 행동
	;=================================================
	IF GET_COM_RANK() == "무리를 한 행동"
		PRINTFORMW 「아프다면 참지 않고, 그렇게 말하면 좋다\@ LOCAL:0 ? 의 것에 # 와 사%RAND_SPLIT("있습니다만/우노입니다만")% \@…」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 조금 무리해\@ LOCAL:0 ? 없다 # 전과 \@?」
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류:애무(자위/조개 우려라/가랑이다리/코키 이외)
	;=================================================
	IF MENUMATCH(TFLAG:ACT, "애무")
		;상사상애
		IF LOCAL:0
			PRINTFORMW 「지금 것이 아팠어?」
			PRINTFORMW 「이제 그렇게 서투르지 않을 것이지만…」
		;그 외
		ELSE
			SELECTCASE RANDCOM(2, 100)
				CASE 0
					PRINTFORMW 「미안해요, 조금 아팠던 것입니까?」
				CASE 1
					PRINTFORMW 「조심하고 한 생각이었던 것입니다만」
					PRINTFORMW 「아파하게 해 버린 것 같네요…」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류:도구(바이브/A바이브/A비즈만)
	;=================================================
	IF MENUMATCH(TFLAG:ACT, "도구")
		;상사상애
		IF LOCAL:0
			SELECTCASE RANDCOM(2, 200)
				CASE 0
					PRINTFORMW 「좀 더 친숙해 지게 할 필요가 있는 것 같구나…」
				CASE 1
					PRINTFORMW 「이것 사용하는 것은 아직 빨랐던 것 같구나…?」
					PRINTFORMW 「알았다. 좀 더 길들이고 나서 시라고 준다♪」
			ENDSELECT
		;그 이외
		ELSE
			SELECTCASE RANDCOM(2, 201)
				CASE 0
					PRINTFORMW 「아직 아픔이 있을까요?」
					PRINTFORMW 「친숙해 질 때까지 좀 더 걸릴 것 같네요…」
				CASE 1
					PRINTFORMW 「이것을 사용하는 것은 조금 너무 빨랐는지요…」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류:성교(성교계 모두)
	;=================================================
	IF MENUMATCH(TFLAG:ACT, "성교")
		;상사상애
		IF LOCAL:0
			PRINTFORML 「아직 아픔이 있는 거야?」
			PRINTFORMW 「좀 더 친숙해 지게 할 필요가 있는 것 같구나…」
		;그 이외
		ELSE
			PRINTFORML 「아직 아픔이 있을까요?」
			PRINTFORMW 「친숙해 질 때까지 좀 더 걸릴 것 같네요…」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류:봉사(이라마치오만?)
	;=================================================
	IF MENUMATCH(TFLAG:ACT, "봉사")
		IF 0
		ELSEIF IS_NOWACTNAME("이라마치오")
			;상사상애
			IF LOCAL:0
				PRINTFORMW 「저기, 괴로운거야? 호흡을 할 수 없는거야?」
				PRINTFORMW 「나쁘다고 알고 있어도, 그러한 얼굴 되면…… 굉장히 흥분해 버린다…」
			;그 이외
			ELSE
				PRINTFORMW 「역시 목 안까지 찔리는 것은 괴로울까요」
				PRINTFORMW 「그렇지만 흥분하네요, 이것……」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류:가학(매도 이외)
	;=================================================
	IF MENUMATCH(TFLAG:ACT, "가학")
		;상사상애
		IF LOCAL:0
			PRINTFORMW 「후…… 후후훗, 뭐라고 하는 사랑스러운거야…」
			PRINTFORMW 「앗, 싫다…, 조금 젖어 와 버렸다…」
		;그 이외
		ELSE
			SELECTCASE RANDCOM(3, 600)
				CASE 0
					PRINTFORMW 「꽤 노력하네요」
					PRINTFORM 「좋아요, 매우
					CALL PRINT_SYMBOL_K39
				CASE 1
					PRINTFORM 「아픔에 참는 표정이, 매우 좋아요
					CALL PRINT_SYMBOL_K39
				CASE 2
					PRINTFORMW 「어째서인가 모르지만…」
					PRINTFORMW 「%CNAME(MASTER, TARGET)%의 고민의 표정을 보면, 오슬오슬 해 버립니다…」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류:이상(피스트계만)
	;=================================================
	IF MENUMATCH(TFLAG:ACT, "이상")
		PRINTFORMW 「\@ LOCAL:0 ? 내가 말하는 것도 이상한 이야기이지만 # 내가 말하는 것도 이상한 이야기입니다만 \@…」
		PRINTFORMW 「자주(잘)\@ LOCAL:0 ? 견딜 수 있어요 # 자주(잘) 참을 수 있네요 \@?」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「\@ LOCAL:0 ? 그렇게 아프면 빨리 말하면 좋은데, 라고 생각하는데 # 미안해요. 조금 아팠던 것입니까 \@…」
	RETURN 1
ENDIF
;COM30, 아픔을참는다

;─
;COM31, 비명을올린다
;─
;·REACT분류:소극적 따른다
;·REACT인상:0에서멀어짐
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류:애무
;	ACT:가슴애무/입으로애무/애널애무/애널빨고/키스/파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:바이브/애널바이브/애널비즈
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그/매도/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트/공중육변기플레이
;-------------------------------------------------
;·TFLAG:REACT파생(0=아픔에 의한 비명/1=공포에 의한 비명)
;-------------------------------------------------
@KOJO_MALE_COM_K39_31
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;부하 2:무리를 한 행동
;━━━━━━━━━━━━━━━━━━━━━━━━
;·부하 2는 정리해 버린다
;-------------------------------------------------
IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「일부러인것 같은 비명은%RAND_SPLIT("정말/")%귀에 거슬림인 만일 수 있는」
		PRINTFORMW 「나쁘지만……, (들)물으면 알아 버리는거네요. 그러한 것…」
	ELSE
		PRINTFORMW 「무리해서까지 비명을올린다 필요가 있습니까?」
		PRINTFORMW 「%RAND_SPLIT("후후…,/")%알아 버립니다. 그러한 것」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1:공포에 의한 비명
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1 && TFLAG:REACT부하 < 2
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「뭐…, 나에게도 책임이 있겠지만……」
		PRINTFORMW 「정직,%RANDTEXT_K39("지금에 되어", "그런 식으로")%무서워해지면, 조금 낙담하지만%RANDTEXT_K39("군요", "＿")%?」
	ELSE
		;대상 한정:남자이니까 여기서 TALENT는 참조하지 않는다
		PRINTFORMW 「%RANDTEXT_K39("남의 주제에", "＿")%비명을올린다 정도 무섭습니까? 다만, 그것이…」
		PRINTFORMW 「이 나에 대해서인가, 지금의 상황에 대해서인가」
		PRINTFORMW 「어쩌면, 그 양쪽 모두라든지? 조금 기분으로는 됩니다%RANDTEXT_K39("군요", "＿")%\@ TENSION(TARGET, 8) != "높다" ? … # \@」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0:아픔에 의한 비명
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생 && TFLAG:REACT부하 < 2
	IF 0
	ELSEIF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「아아읏, 이제(벌써)! 오슬오슬 해 버린다…」
		PRINTFORMW 「저기, 좀 더 듣게 해…」
	ELSEIF AFFECTION("몹시 사랑함")
		PRINTFORMW 「상당히 달콤한 소리로 우는군요」
		PRINTFORMW 「조금 흥분해 왔습니다…」
	ELSEIF AFFECTION("권애")
		PRINTFORMW 「좋은 느낌이군요, 좀 더 들려주세요…」
	ELSEIF AFFECTION("혐오")
		PRINTFORMW 「괴롭습니까? 괴롭습니까?」
		PRINTFORMW 「그렇지만,%CNAME(MASTER, TARGET)%가 안 돼요…」
	ELSE
		PRINTFORML 「그런 비명을 올려지면…」
		PRINTFORMW 「조금 흥분해 버리네요……」
	ENDIF
	RETURN 1
ENDIF

;─
;COM32, 날뛴다
;─
;·파생에 의해 날뛰고 성공(REACT분류:날뛴다), 날뛰고 실패(REACT분류:소극적 따른다)로 나누어진다
;·날뛰고가 통과하면 도구계/성교계/성교 봉사계 커맨드를 방해한 취급이 된다
;·벌포인트 추가
;·REACT인상:약간나쁜인상
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스
;		성기를서로켠다/파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그
;ACT분류:휴게
;	ACT:치료한다
;ACT분류:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다
;-------------------------------------------------
;·TFLAG:REACT파생(0=날뛰고 성공
;                   1=줄구속에 의한 날뛰고 실패
;                    2=제지에 의한 날뛰고 실패)
;-------------------------------------------------
@KOJO_MALE_COM_K39_32
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;날뛰고 실패!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && INRANGE(TFLAG:REACT파생, 1, 2)
	;=================================================
	;부하 2:무리를 한 행동
	;=================================================
	IF GET_COM_RANK() == "무리를 한 행동"
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「무리하게 날뛰어 보이는 의미가 잘 모르지만… 아무튼」
			{
			PRINTFORM 「상관하기를 원해서 하고 있다고 생각하면,\@ TENSION(TARGET, 8) == "높다" 
			? 사랑스럽다 # 흐뭇함 \@는%RAND_SPLIT("생각되지 않지도 없다/생각이 들지 않는 것은 아니다")%지만, 말야
			}
			CALL PRINT_SYMBOL_K39
		ELSE
			;//RAND_SPLIT는("hoge/")으로 hoge와 공문자열을 돌려준다
			PRINTFORMW 「그렇게 날뛰어 보이는 것이,%CNAME(MASTER, TARGET)%의%RAND_SPLIT("본심/본의")%라고도 생각되지 않지만%RAND_SPLIT("군요/")%」
			PRINTFORMW 「다만…, 무엇이 목적으로 그런 일을 하는지, 조금 기분으로는 됩니다」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1:줄구속에 의한 날뛰고 실패
	;=================================================
	IF TFLAG:REACT파생 == 1
		;상사상애
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「꽤 재미있지만…」
			PRINTFORMW 「그 거 어떤 의미도 없다고 생각해요?」
		;그 이외
		ELSE
			CALL FADEIN(10, "「저, 저……」")
			PRINTFORMW 「그 상태로 날뛰어도 쓸데없다고 생각합니다만…」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 2:제지에 의한 날뛰고 실패
	;=================================================
	IF TFLAG:REACT파생 == 2
		VARSET LOCALS
		LOCALS:0 '= RAND_SPLIT("날뛰어/저항해/그런 일 해")
		;=================================================
		;ACT분류:휴게
		;=================================================
		IF MENUMATCH(TFLAG:ACT, "휴게")
			;상사상애
			IF GET_KFLAG_K39("상사상애")
				PRINTFORMW 「저기, 어째서 날뛰는 거야?」
				PRINTFORMW 「의미를 모르지만…」
			;그 이외
			ELSE
				PRINTFORMW 「저, 지금 날뛰는데 어떤 의미가%RAND_SPLIT("? /있겠지요")%」
				PRINTFORMW 「\@ IS_NOWACTNAME("치료한다") ? 나에게 치료되는 것이가 싫습니까? 그렇지 않으면 # 의미 따위 없고 \@, 다만 날뛰고 싶은 것뿐입니까……」
			ENDIF
			RETURN 1
		ENDIF
		
		;---------------------
		;그 외
		;---------------------
		;상사상애
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「이제(벌써),\@ IS_CONDITION(TARGET, "분노") ? 적당히 해 # 조금 침착해 \@하세요!」
			PRINTFORMW 「%LOCALS:0%도 쓸데없다고 모르는거야?」
		;그 이외
		ELSE
			PRINTFORMW 「안됩니다, 얌전하게 해 주세요」
			PRINTFORMW 「어쨌든,%LOCALS:0%도 쓸데없어요…」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;=================================================
;REACT파생 0:날뛰고가 다녔다
;=================================================
IF 구상기입체크(100) && !TFLAG:REACT파생
	;상사상애
	IF GET_KFLAG_K39("상사상애") 
		PRINTFORML 「이제(벌써), 어째서 그렇게 날뛰는 거야?」
		PRINTFORMW 「적당히 해요…」
	;그 외
	ELSE
		PRINTFORMW 「저, 그렇게 날뛰지 말아 주세요…」
	ENDIF
	RETURN 1
ENDIF
;COM32, 날뛴다

;─
;COM33, 무서워한다
;─
;·REACT분류:허가를청한다
;·일정 조건으로 분노를 사, 벌포인트 추가
;-------------------------------------------------
;·트리거 이벤트제
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류:1:애무
;	ACT:풋잡한다/밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:애널바이브/애널비즈
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:줄/아이마스크/볼개그/매도/관장세트
;ACT분류:7:이상
;	ACT:방치플레이/공중육변기플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=두려움에 대한 초조)
;-------------------------------------------------
@KOJO_MALE_COM_K39_33
;-------------------------------------------------
VARSET LOCAL
LOCAL:0 = GET_KFLAG_K39("상사상애")
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1:두려움에 대한 초조
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	;=================================================
	;부하 2한편 실은 싫어
	;=================================================
	IF GET_COM_RANK() == "무리를 한 행동" && !COMCOR_POSI()
		PRINTFORMW 「그러한\@ LOCAL:0 ? 눈으로 볼 수 있으면, 조금 기분이 나빠요 # 시선을 향할 수 있는 것은, 별로 좋아하지는 않습니다 \@……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「%CNAME(MASTER, TARGET)%의 그러한 모습, 너무 보고 싶고\@ LOCAL:0 ? 없네요 # 없겠네요 \@……」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0:무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;━━━━━━━━━━━━━━━━━━━━━━━━
	;부하 2:무리를 한 행동
	;━━━━━━━━━━━━━━━━━━━━━━━━
	IF GET_COM_RANK() == "무리를 한 행동"
		IF LOCAL:0
			PRINTFORMW 「저기, 농담이라도%RAND_SPLIT("그러한 것/무서워해 보인다든가")%멈추었으면 좋지만?」
			PRINTFORMW 「이렇게 외관%RAND_SPLIT("끊어/라고도")%, 꽤 손상되기 때문에…」
		ELSE
			PRINTFORMW 「저…,%RAND_SPLIT("일부러/무리하게")%무서워해 보이는 일에 어떤 의미가\@ TENSION(TARGET, 8) == "높다" ? ?  # 있겠지요 \@」
			CALL FADEIN(10, "「뭐, 그렇네요……」")
			{
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의\@ COMCOR_POSI() 
			? 두려움의 대상은 이 나는 아니고, 조금 다른 것 같지만. 그런데 # 본심은 모릅니다만, 그러한 시선을 향할 수 있으면 손상됩니다…… \@」
			}
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF 0
	ELSEIF LOCAL:0
		PRINTFORMW 「어, 어째서…」
		PRINTFORMW 「저기, 무서워하는 것은 그만두어요…」
	ELSEIF AFFECTION("몹시 사랑함")
		PRINTFORMW 「무서워하다니 너무 합니다…」
	ELSEIF AFFECTION("권애")
		PRINTFORMW 「그렇게 무서워하지 말아 주세요…」
	ELSEIF AFFECTION("혐오")
		PRINTFORMW 「그렇게 무섭습니까…?」
		PRINTFORMW 「확실히 나는 액신이지만…… 그렇게 명백하게 두려워해지면 다칩니다…」
	ELSE
		PRINTFORMW 「아무리 액신이니까 라고, 그렇게 무서워하는 일 없다고 생각합니다만…」
	ENDIF
	RETURN 1
ENDIF
