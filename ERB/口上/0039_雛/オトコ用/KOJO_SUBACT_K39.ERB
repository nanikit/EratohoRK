;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;	히나 구상 추가 액션 구상 파일

;	조교자가 조교 지시 이외에 실시하는 행동의 파일

;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;조교 액션·커맨드 구상
;========================================================================

;─
;탈의 액션 구상
;─
;·조교자 액션의 앞에 강제탈의나 자주탈의가 발생했을 때에 표시
;·이 구상의 시점에서는 아직 TEQUIP는 갱신되어 있지 않다
;·첫회 분기는 「처음 보았다/보였다」란 할 수 없다.
;  숍 메뉴로 벗게 했던 적이 있을지도 모르고, 조교의 흐름으로 완전하게 벗게 하지 않고서 노출만 하고 있는 경우도 있다.
;  어쩌면 조수였던 때에 본 적이 있을지도 모른다. 그러한 의미에서는 간이적인 것이다.
;  엄밀한 의미에서의 「초견」플래그를 준비할지 어떨지는 요망하는 대로라고 하는 일로.
;-------------------------------------------------
@KOJO_MALE_DATUI_K39
;-------------------------------------------------
;관련 변수…
;강제탈의:TFLAG:102  자주탈의:TFLAG:103
;=(-1, 강제탈의에 저항, 1, 윗도리, 2, 하반신윗도리, 3, 웃도리, 4, 상반신 전부, 5, 하반신 전부,
;  6, 액세서리, 7, 액세서리 이외 모두, 8, 전라까지, 9, 속옷만)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의에 저항된 FIRSTDATUI(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 < 0
	;=================================================
	;첫회
	;=================================================
	IF FIRSTDATUI(0)
		PRINTFORMW 「그렇게 벗는 것이 싫어\@ !GET_KFLAG_K39("상사상애") ? 입니까 # \@?」
		RETURN 1
	ENDIF
	
	;=================================================
	;자신은 벗었다
	;=================================================
	IF TFLAG:자주탈의
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「뭐 별로 좋지만」
			PRINTFORMW 「나는 벗기게 해 받아요」
		ELSE
			PRINTFORMW 「방법 없네요」
			PRINTFORMW 「우선 나는 벗기게 해 받습니다만」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「옷 입은 채로 시 싶은거야?」
	ELSE
		PRINTFORMW 「나의 앞에서 벗는 것은 싫습니까?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;두 사람 모두 뭔가 벗은 FIRSTDATUI(10)
;------------------------------------------------
;·주로 섹스계 행동의 예비 동작으로 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 > 0 && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의")
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「저기, 서로 서로 보여주기 해 봐?   킥킥」
	ELSE
		PRINTFORMW 「준비는 좋은 것 같네요?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·액세서리만 FIRSTDATUI(20)
;------------------------------------------------
;·주로 양말. 그 밖에 장갑이나 소매 따위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 == 6 && !IS_NOWASSIACTNAME(, "탈의") && !TEQUIP:아이마스크
	;=================================================
	;전라가 되었다(벗기 전은 양말 이외 알몸이라든지 그러한 느낌)
	;=================================================
	IF TFLAG:자주탈의 && ZENRACHECK(0)
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「이봐요, 좀 더 자주(잘) 봐」
			PRINTFORMW 「당신에게는 전부 보았으면 좋은거야…」
		ELSE
			PRINTFORMW 「나의 몸 어떻습니까?」
			PRINTFORMW 「좀 더 자주(잘) 봐도 좋아요♪」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「이것은 이제(벌써) 요점\@ GET_KFLAG_K39("상사상애") ? 등 없네요 # 선이군요 \@」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·만p FIRSTDATUI(30)
;------------------------------------------------
;·이번 탈의로 성기가 보이게 되었을 경우에 표시
;·팬티 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의") && !TEQUIP:아이마스크
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「저기, 조금 젖어 버리고 있는 것, 알아?」
	ELSE
		PRINTFORMW 「조금 젖어 오고 있는 것, 압니까?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·상반신 전부 벗었다
;-------------------------------------------------
;·이번 탈의로 상반신 전부 벗었을 경우에 표시(50%)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(50) && TFLAG:자주탈의 && B_DATUICHECK(0) == 4 && !IS_NOWASSIACTNAME(, "탈의")
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「너무 초조하게 하는 것도 뭐 하고…」
		PRINTFORM 「이 정도로 좋네요
	ELSE
		PRINTFORMW 「조금 벗는 것이 너무 빠릅니까?」
		PRINTFORM 「그렇지만, 나도 기다릴 수 없는 것이에요
	ENDIF
	CALL PRINT_SYMBOL_K39
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·말하는 p FIRSTDATUI(40)
;------------------------------------------------
;·이번 탈의로 젖가슴이 보이게 되었을 경우에 표시
;·브라 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의") && !TEQUIP:아이마스크
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「%RAND_SPLIT("이봐요, 이것을 보고 싶었던 것이겠지? /우후, 시선이 추잡하네요♪")%」
	ELSE
		PRINTFORMW 「%RAND_SPLIT("학수 고대였던 것입니까/웃 사양말고 봐도 좋아요")%?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·팬티 보이고 FIRSTDATUI(5)
;------------------------------------------------
;·이번 탈의로 팬티(캐릭터에 의해 드로워즈·트렁크스)가 보이게 되었을 경우에 표시
;·하반신속옷이 없는 캐릭터는 쓰는 필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의") && !TEQUIP:아이마스크
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「그렇게 볼 수 있으면 조금 부끄러울지도」
		PRINTFORM 「그렇지만%CNAME(MASTER, TARGET)%에라면, 보여져도 괜찮아…
		CALL PRINT_SYMBOL_K39
	ELSE
		PRINTFORMW 「나의 속옷 모습이 그렇게 신경이 쓰입니까?」
		PRINTFORMW 「그렇지만, 그렇게 보여지면, 조금 부끄럽네요…」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·브라 보이고 FIRSTDATUI(60)
;------------------------------------------------
;·이번 탈의로 브라(캐릭터에 의해 스포트브라·사라시)가 보이게 되었을 경우에 표시
;·상반신속옷이 없는 캐릭터는 쓰는 필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의") && !TEQUIP:아이마스크
	IF GET_KFLAG_K39("상사상애")
		SELECTCASE RANDDATUI(3, 60)
			CASE 0
				PRINTFORMW 「속옷 모습이 오히려 흥분한다고?」
			CASE 1
				PRINTFORMW 「저기, 이 브라 사랑스럽다고 생각하지 않아?」
			CASE 2
				PRINTFORMW 「새로운 것 붙여 보았지만, 깨닫고 있어?」
		ENDSELECT
	ELSE
		PRINTFORMW 「사람에 따라서는 알몸보다 속옷 모습이 흥분하는 것 같습니다만」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%는 어떨까요?   후후」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·전라에 FIRSTDATUI(7)·RANDDATUI(8) ～ (11)
;------------------------------------------------
;·「전라까지 벗었다」만이 아니고, 「지금 벗었으므로 전라가 되었다」라고 하는 의미
;·「윗도리 밖에 남지 않은 상태로 윗도리를 벗었다」등도 포함된다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && ZENRACHECK(0) && !IS_NOWASSIACTNAME(, "탈의") && !TEQUIP:아이마스크
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「%RAND_SPLIT("벌써 보고 질렸다든가 말하지 말아/이봐요, 칭찬한다면 지금 가운데야")%?」
	ELSE
		PRINTFORMW 「나의 알몸은 어떻습니까?」
		PRINTFORMW 「부끄러워하지 않고, 좀 더 봐도 좋아요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·액세서리만 FIRSTDATUI(80)
;------------------------------------------------
;·주로 양말. 그 밖에 장갑이나 소매 따위
;·벗긴 중에 접어들지 않는다! 되기 십상인 것으로 이것만은 부위별 텐프레를 준비
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 == 6
	;=================================================
	;전라가 되었다
	;=================================================
	IF ZENRACHECK(1)
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「네, 이것으로 전부군요」
			PRINTFORM 「우후, 부끄러워하지 않아도 괜찮은데
		ELSE
			PRINTFORMW 「이것으로 전부 무릎」
			PRINTFORM 「부끄럽습니까?   에서도, 그렇게 신경쓰는 일 없어요
		ENDIF
		CALL PRINT_SYMBOL_K39
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「\@ GET_KFLAG_K39("상사상애") ? 이봐요, 이런 건 이제 필요하지 않지요?  # , 불필요한 것은 벗어 버려요 \@」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·고추 보여라 FIRSTDATUI(90)
;------------------------------------------------
;·이번 탈의로 남성기가 보이게 되었을 경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && PENIS(MASTER)
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「조금 커지고 있어?」
		PRINTFORM 「우후, 이미 기대하고 있을까요
		CALL PRINT_SYMBOL_K39
	ELSE
		PRINTFORMW 「아라, 보여 버렸어요」
		PRINTFORMW 「이제 와서 숨겨도 늦어요?   후후」
	ENDIF
	RETURN 1
ENDIF

[SKIPSTART]
남자 한정 구상인 것으로 기능하지 않는 개소
이 근처를 쓰고 있는 시점에서는 한정 구상의 예정은 아니었어? 이미 스스로도 생각해 낼 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·그대로응 보여라 FIRSTDATUI(100)
;------------------------------------------------
;·이번 탈의로 여성기가 보이게 되었을 경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && ! TALENT:MASTER:남자
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「저기, 조금 젖어 오고 있는 것 같지만」
		PRINTFORMW 「이제 인내 할 수 없는 느낌?   킥킥」
	ELSE
		PRINTFORMW 「조금 젖고 있는 것 같게 보입니다만, 기분탓입니까?」
		PRINTFORMW 「우후, 이제 기다릴 수 없는 것 같네요♪」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·젖가슴 보여라 FIRSTDATUI(110)
;------------------------------------------------
;·이번 탈의로 젖가슴이 보이게 되었을 경우에 표시
;·브라 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;·조교 대상이 남자라고 표시하지 않기 때문에, 남자 한정 구상에서는 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 3 && ! TALENT:MASTER:남자
	;조교 대상이 거유
	IF TALENT:MASTER:거유
		;조교자도 거유
		IF TALENT:TARGET:거유
			IF GET_KFLAG_K39("상사상애")
				PRINTFORMW 「저기, 조금 커졌어?」
				PRINTFORMW 「조금 비교해 봐?   킥킥」
			ELSE
				PRINTFORMW 「조금 크기를 비교해 볼까요?   후후」
			ENDIF
		;조교자가 거유가 아닌 경우
		ELSE
			IF GET_KFLAG_K39("상사상애")
				PRINTFORMW 「우후, 언제 봐도 커서, 조금 부럽네요♪」
			ELSE
				PRINTFORMW 「크고 조금 부럽네요♪」
			ENDIF
		ENDIF
	;조교 대상이 빈유
	ELSEIF TALENT:MASTER:빈유
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「우후, 사랑스러운 젖가슴이군요♪」
		ELSE
			PRINTFORMW 「우후, 사랑스러운 젖가슴이군요♪」
		ENDIF
	;그 외
	ELSE
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「자, 당신의 젖가슴 보여 줘?」
		ELSE
			PRINTFORMW 「그럼, 당신의 젖가슴, 보여 받아요♪」
		ENDIF
	ENDIF
	RETURN 1
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·팬티 보여라 FIRSTDATUI(120)
;------------------------------------------------
;·이번 탈의로 하반신속옷이 보이게 되었을 경우에 표시
;·조교 대상이 하반신속옷이 없는 캐릭터 한정의 구상이면 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 2
	;조교 대상이 남자
	IF TALENT:MASTER:남자
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「이제 그렇게 해 버려…」
			PRINTFORMW 「조금 성질이 급한 것이 아니야?」
		ELSE
			PRINTFORMW 「이미 부풀리고 있군요?」
			PRINTFORMW 「기다릴 수 없습니까?   우대신」
		ENDIF
	[SKIPSTART]
	;조교 대상이 여자
	ELSE
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「사랑스러운 속옷이군요, 자주(잘) 어울리고 있어요?」
			PRINTFORMW 「아라, 조금 젖어 오고 있는 것 같구나」
		ELSE
			PRINTFORMW 「사랑스러운 속옷이군요♪」
			PRINTFORMW 「그렇지만, 조금 스며드가 되고 있는 것 같아지만?   후후」
		ENDIF
	[SKIPEND]
	ENDIF
	RETURN 1
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·브라 보여라 FIRSTDATUI(130)
;------------------------------------------------
;·이번 탈의로 브라 상반신속옷이 보이게 되었을 경우에 표시
;·조교 대상이 남자라고 표시하지 않기 때문에, 남자 한정 구상에서는 쓸 필요가 없다
;·조교 대상이 상반신속옷이 없는 캐릭터 한정의 구상이면 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 2 && ! TALENT:MASTER:남자
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「우후, 사랑스럽네요, 그것」
		PRINTFORMW 「밖의 세계에는 그런 벌이 가득 있을까?」
	ELSE
		PRINTFORMW 「아라, 사랑스러운 속옷이군요」
	ENDIF
	RETURN 1
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·전라에 FIRSTDATUI(140)
;------------------------------------------------
;·「전라까지 벗겼다」만이 아니고, 「지금 벗겼으므로 전라가 되었다」라고 하는 의미
;·「윗도리 밖에 남지 않은 상태로 윗도리를 벗겼다」등도 포함된다
;·「젖가슴 보여라」～「팬티 보여라」를 썼을 경우는 기입할 필요는 없다(상층의 분기에 먹혀져 여기까지 떨어져 오지 않기 때문에)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && ZENRACHECK(1)
	;조교 대상이 남자
	IF TALENT:MASTER:남자
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「네, 이것으로 전부군요♪  는…」
			PRINTFORMW 「이미 그렇게 되어 있는 거야?」
		ELSE
			PRINTFORMW 「자, 이것으로 전부 가」
			PRINTFORMW 「아라, 이미 커지고 있네요?」
		ENDIF
	[SKIPSTART]
	;조교 대상이 여자
	ELSE
		IF GET_KFLAG_K39("상사상애")
			PRINTFORMW 「우후, 매우 예쁘다」
			PRINTFORMW 「자, 함께 즐깁시다♪」
		ELSE
			PRINTFORMW 「우후, 매우 예뻐요♪」
		ENDIF
	[SKIPEND]
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·뭔가 벗은 FIRSTDATUI(150)
;------------------------------------------------
;·자위·봉사계의 예비 동작이 되기 쉽다
;·여기까지의 자주탈의 조건에 히트 하지 않는 경우, 이 개소가 보완한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의") && !TEQUIP:아이마스크
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「그런 눈으로 볼 수 있으면, 조금 부끄럽지만…」
	ELSE
		PRINTFORMW 「내가 벗는 곳이, 그렇게 신경이 쓰입니까?   우대신」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·뭔가 벗긴 FIRSTDATUI(160)
;------------------------------------------------
;·애무계의 예비 동작이 되기 쉽다
;·여기까지의 강제탈의 조건에 히트 하지 않는 경우, 이 개소가 보완한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「이봐요, 벗게 해 주기 때문에 움직이지 마」
	ELSE
		PRINTFORMW 「네, 불필요한 것은 벗어 버려요」
	ENDIF
	RETURN 1
ENDIF


;========================================================================
;추가 액션 구상
;========================================================================
;·조교자가 추가행동을 취했을 때에 범용적으로 표시
;·쓰는 경우, 액션 구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------

;─
;EXTRA1, C애무
;─
;·추가로 애무 또는 자위를 실시한다
;·조교 대상으로 페니스가 없는 경우는 애무, 어느 경우는 자위
;-------------------------------------------------
@KOJO_MALE_ACT_EXTRA_K39_1
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_0, 애무한다
;-------------------------------------------------
;·조교 대상으로 페니스가 없는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ! PENIS(MASTER)
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「이봐요, 코코도 함께 만지작거려 준다」
		PRINTFORMW 「이렇게 볼록 시켜 버려, 기대하고 있던 것이겠지?」
	ELSE
		PRINTFORMW 「자, 여기도 함께 만지작거려 줍시다」
		PRINTFORMW 「맡겨 주세요, 상냥하게 손대어 줄테니까♪」
	ENDIF
	RETURN 1
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_1, 자위 한다
;-------------------------------------------------
;·조교 대상으로 페니스가 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && PENIS(MASTER)
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「역시 코코 만지작거려지는 것이 제일 좋은거야?」
		PRINTFORMW 「우후, 이대로 훑어 가게 해 버릴까♪」
	ELSE
		PRINTFORMW 「코코도 만지작거리지 않으면 어딘지 부족합니까?」
		PRINTFORMW 「후후, 자꾸자꾸 커져. 몸은 정직하네요♪」
	ENDIF
	RETURN 1
ENDIF

;─
;EXTRA2, V애무
;─
;·추가로 바지 애무를 실시한다
;·조교 대상이 남자의 경우 발생하지 않는다
;·조교 대상이 처녀의 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_MALE_ACT_EXTRA_K39_2
;-------------------------------------------------
[SKIPSTART]
IF 구상 기입 체크(0)
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「이봐요, 질척질척 말하고 있는 것 들려?」
		PRINTFORMW 「이 근처 켜면 좀 더 좋은 것이 아니야?」
	ELSE
		PRINTFORMW 「이제 흘러넘치기 시작해 멈추지 않는 느낌이군요?」
		PRINTFORMW 「손가락이 빨려 들여가도록(듯이) 들어가 버립니다♪」
	ENDIF
	RETURN 1
ENDIF
[SKIPEND]
RETURN 0

;─
;EXTRA3, A애무
;─
;·추가로 애널애무를 실시한다
;-------------------------------------------------
@KOJO_MALE_ACT_EXTRA_K39_3
;-------------------------------------------------
IF 구상기입체크(100)
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「이봐요, 엉덩이 만지작거려지는 것 좋아하겠지?」
		PRINTFORMW 「저기, 손가락 넣어도 괜찮아?   우대신」
	ELSE
		PRINTFORM 「손댈 때에 움찔움찔 해 사랑스럽네요
		CALL PRINT_SYMBOL_K39
	ENDIF
	RETURN 1
ENDIF

;─
;EXTRA4, B애무
;─
;·추가로 유두 고문을 실시한다
;-------------------------------------------------
@KOJO_MALE_ACT_EXTRA_K39_4
;-------------------------------------------------
IF 구상기입체크(100)
	IF GET_KFLAG_K39("상사상애")
		PRINTFORMW 「이렇게 서게 해 버려, 기대하고 있었어?」
		PRINTFORMW 「이봐요, 조금 아픈 위가 기분이 좋지요?」
	ELSE
		PRINTFORMW 「이제(벌써) 딱딱해질 것 같네요」
		PRINTFORMW 「직접 만지작거리지 않고, 초조해 해지는 것은 어떻습니까?」
	ENDIF
	RETURN 1
ENDIF


;─
;미약등사용구상
;─
;·조교자가 미약이나 로션을 사용했을 때에 범용적으로 표시
;·「액션 구상」으로 기입 체크를=2로 했을 경우는 표시되지 않는다
;·쓰는 경우, 액션 구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
@KOJO_MALE_USE_K39
;-------------------------------------------------
RETURN 0

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션/미약동시 사용
;------------------------------------------------
;·주로 성교계 커맨드로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && GETBIT(TFLAG:미약등사용구상, 0) && GETBIT(TFLAG:미약등사용구상, 1)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션 사용
;------------------------------------------------
;·완구·성교계 커맨드등으로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && GETBIT(TFLAG:미약등사용구상, 0)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;미약사용
;------------------------------------------------
;·애무계 커맨드등으로부터 발생한다
;·키스로 발생했을 경우는 입으로 옮김이 되기 (위해)때문에, 그 케이스는 키스의 액션 구상내에서 처리하는 편이 좋다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && GETBIT(TFLAG:미약등사용구상, 1)
;ENDIF