;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	앨리스 구상(대상 한정) 회화계 액션 구상 파일
;
;		회화계 액션 구상을 정리한 파일
;
;		EXTRA 모드로 NO:9 사쿠야를 MASTER에 선택했을 경우만 표시
;
;Version		Reason
;1. 70			@구상 기입 체크 사양에. 그 외 섬세한 수정
;1. 62			일부 조건의 재검토. 오자, 탈자 수정
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 ACT0～9
;========================================================================
;·TFLAG:ACT(0=지금의기분을얘기한다/1=자신에게로의기분을얘기한다/2=성에대한이야기를한다/3=상냥하게위로한다/4=엄격하게위협한다/5=의상의변경을요구한다/6=합의를요구한다/7=벌이라고선언한다/8=고함친다/9=기분나쁘게웃는다)
;·TFLAG:ACT분류 == 0에 해당
;·ACT9, 기분나쁘게웃는다는 이상계 ACT분류로부터 파생하는 경우가 있다
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------
;관련 변수…
;FIRSTACT
;첫회 ACT 판정 함수
;TFLAG:ACT(TFLAG:90)를 자동으로 참조, 현재 실행되고 있는 ACT 마다 번호가 독립해 보존된다
;-------------------------------------------------
;─
;NO9_ACT0, 지금의기분을얘기한다 FIRSTACT(0) RANDACT(0)(1)
;─
;·지금의 기분에 대해 들어 온다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_0
;-------------------------------------------------
;@EQUIPACTION_DESCRIPTION가 관련되는 곳은, 우선 나누어 쓰지 않으면이라는 느낌?
;아무튼 귀찮고, 줄 순서도 머리가 아픈 곳. 일부 TEQUIP 분기는 어디엔가 통합해도 좋은 것 같지만
;라고 할까 이것, REACT도 상응하게 시추에이션 짠 것 쓸 필요가 있구나
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT0, 지금의기분을얘기한다
;RESULT치(=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해 미약등 구상을 스킵)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=============================================
	;첫회
	;=============================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「저기, 기분은 어떨까?」
		PRINTFORMW 「그렇게 단단해지는 일 없어요. 얌전하게 하고 있으면 귀여워해 주기 때문에」
		RETURN 1
	ENDIF
	
	;=============================================
	;성교계로 질내사정의 후
	;=============================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
		PRINTFORMW 「%CNAME(MASTER, TARGET)%에 질내사정… 최고군요♪」
		PRINTFORMW 「저기, 나에게 정복 된 기분은 어떨까?」
		RETURN 1
	ENDIF
	
	;=============================================
	;갈 것 같다
	;=============================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;-------------------------------------------------
		;갈 것 같은 것을 참고 있다
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:MASTER:갈것같다 > 5
			PRINTFORMW 「아라, 노력해요?」
			PRINTFORM 「%CNAME(MASTER, TARGET)%를 언제까지 견딜 수 있는지, 시험해 보는 것도 나쁘지 않네요
			CALL PRINT_SYMBOL_K14
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;초조하게 해 안
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:MASTER:초조하게해번 > 1
			PRINTFORMW 「저기, 초조해 해지고 있는 기분은 어때?」
			PRINTFORMW 「간단하게는 가게 하지 않아요, 나쁘지만. 후후…」
			RETURN 1
		ENDIF
		
		;-------------------------------
		;그 외
		;-------------------------------
		PRINTFORMW 「후후, 갈 것 같은거네?」
		PRINTFORMW 「그렇지만, 간단하게가게해는 재미있지 않네요」
		RETURN 1
	ENDIF
	
	;=============================================
	;볼개그 장착중
	;=============================================
	IF 구상기입체크(100) && TEQUIP:볼개그
		PRINTFORMW 「후후, 좋은 모습이군요. 지금의 기분은 어떨까?」
		PRINTFORMW 「이렇게 말해도 대답할 수 없겠지만 말이죠,♪」
		RETURN 1
	ENDIF
	
	;=============================================
	;아이마스크 장착중
	;=============================================
	IF 구상기입체크(100) && TEQUIP:아이마스크
		PRINTFORMW 「시야를 빼앗긴 기분은 어떨까?」
		RETURN 1
	ENDIF
	
	;=============================================
	;안면승마 안
	;=============================================
	IF 구상기입체크(100) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 「저기 사쿠야. 나에게 이런 일 되고 있는 기분은 어떨까?」
		PRINTFORMW 「그렇지만 이 상태에서는 대답할 수 없을까요? 후후후…」
		RETURN 1
	ENDIF
	
	;=============================================
	;자위중
	;=============================================
	IF 구상기입체크(100) && TEQUIP:자위중
		;-------------------------------------------------
		;조교 대상이 자위중
		;-------------------------------------------------
		IF 구상기입체크(100) && GETBIT(TEQUIP:자위중, 0)
			SELECTCASE ABL:MASTER:노출버릇
				;노출버릇 3이상
				CASE IS >= 3
					PRINTFORMW 「나에게 보여져 더욱 더 흥분하고 있는 것 같구나?」
				;그 외
				CASEELSE
					PRINTFORMW 「저기 사쿠야. 아직 볼 수 있는 것은 부끄러워?」
			ENDSELECT
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;조교자가 자위중
		;-------------------------------------------------
		IF 구상기입체크(100) && GETBIT(TEQUIP:자위중, 1)
			;아이마스크 장착중
			IF TEQUIP:아이마스크
				SELECTCASE RANDACT(2, 0)
					CASE 0
						PRINTFORMW 「저기 사쿠야. 들릴까? 이 소리…」
					CASE 1
						PRINTFORMW 「안보여도 알까요? 지금, 내가 무엇을 하고 있을까…」
				ENDSELECT
			;그 외
			ELSE
				SELECTCASE RANDACT(2, 1)
					CASE 0
						PRINTFORMW 「나의 이런 모습 보이고 붙일 수 있어, 어떤 기분일까?」
					CASE 1
						PRINTFORMW 「저기 사쿠야. 스스로도 시 싶게 안 돼?」
				ENDSELECT
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=============================================
	;성교중
	;=============================================
	IF 구상기입체크(100) && TEQUIP:성교중
		SELECTCASE TEQUIP:성교중
			;정상위, 승마위
			CASE 1, 3
				;전COM 파생 처리
				SELECTCASE TRAINNAME:PREVCOM
					;허리를흔드는
					CASE "허리를흔드는"
						PRINTFORMW 「그렇게 허리 흔들어 버려, 즐겨 받아지고 있는 것 같구나?」
					;적극적 반응
					CASE "쾌감을받아들인다", "적극적한다", "애정을담아준다"
						PRINTFORMW 「후후, 즐겨 받아지고 있는 것 같구나」
					;그 외
					CASEELSE
						PRINTFORMW 「즐기지 않은 것 같구나. 그다지 시 싶지 않은거야?」
				ENDSELECT
			;후배위
			CASE 2
				;전COM 파생 처리
				SELECTCASE TRAINNAME:PREVCOM
					;허리를흔드는
					CASE "허리를흔드는"
						PRINTFORMW 「뒤로부터 범해지는 것이 흥분할까? 저기 사쿠야…」
					;적극적 반응
					CASE "쾌감을받아들인다", "적극적한다", "애정을담아준다"
						PRINTFORMW 「당신… 이런 식으로 범해지는 것이 좋아 같구나?」
					;그 외
					CASEELSE
						PRINTFORMW 「뒤로부터 되는 것은 싫어?」
				ENDSELECT
			;대면좌위, 배면좌위
			CASE 4, 5
				;전COM 파생 처리
				SELECTCASE TRAINNAME:PREVCOM
					;허리를흔드는
					CASE "허리를흔드는"
						PRINTFORM 「완전히 흥분하고 있는 것 같구나? 포옹되고 있으면 흥분할까, 저기 사쿠야
						CALL PRINT_HEARTCBW_K14(COLOR2_K14("pink"), , , COLOR2_K14("ALICE"))
					;적극적 반응
					CASE "쾌감을받아들인다", "적극적한다", "애정을담아준다"
						PRINTFORMW 「이 모습으로 되는 것, 좋아 같구나?」
					;그 외
					CASEELSE
						PRINTFORMW 「기분이 타지 않는 것 같구나? 그다지 시 싶지 않은 것일까」
				ENDSELECT
			;애널섹스
			CASE 6
				;전COM 파생 처리
				SELECTCASE TRAINNAME:PREVCOM
					;허리를흔드는
					CASE "허리를흔드는"
						PRINTFORMW 「후후, 엉덩이가 그렇게 기분 좋은 것일까?」
					;적극적 반응
					CASE "쾌감을받아들인다", "적극적한다", "애정을담아준다"
						PRINTFORMW 「당신… 엉덩이로 하는 것을 좋아하는? 후훗」
					;그 외
					CASEELSE
						PRINTFORMW 「엉덩이로 하는 것은 싫어?」
				ENDSELECT
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=============================================
	;삼각목마 사용중
	;=============================================
	IF 구상기입체크(100) && TEQUIP:삼각목마
		PRINTFORMW 「나라도 생각하는거야? 이런 상황으로 기분 같은거 (들)물어서는 안된다고」
		PRINTFORMW 「후후후…」
		RETURN 1
	ENDIF
	
	;=============================================
	;조교 대상 상태가 화내
	;=============================================
	IF 구상기입체크(100) && M_COND("분노")
		PRINTFORMW 「상당히 기분이 나쁜 것 같구나?」
		PRINTFORMW 「도대체 무엇이 그렇게 재미있지 않은 것일까」
		RETURN 1
	ENDIF
	
	;통상이라면 좀 더 상층에서 좋을지도이지만 TEQUIP계를 먹고 싶지 않다든가 그런 느낌으로
	;=============================================
	;그 조교의 최초의 액션
	;=============================================
	IF 구상기입체크(100) && TFLAG:경과시간 == 1
		;상사상애
		IF AFFECTION("상사상애")
			PRINTFORMW 「저기, 기분은 어때? 라고 말할까…」
			PRINTFORMW 「내가 말을 걸고 있기 때문에, 좀 더 기쁜 듯한 얼굴 하세요!」
		;그 이외
		ELSE
			PRINTFORMW 「정직,%CNAME(MASTER, TARGET)%의 지금의 기분이라든지에 흥미없는거야…」
			PRINTFORMW 「나의 하고 싶은대로 하도록 해 받아요…  우후후…」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「저기 사쿠야. 기분은 어떠세요?」
	RETURN 1
ENDIF
;회화계는 근본적으로 다시 볼 필요 있어

;─
;NO9_ACT1, 자신에게로의기분을얘기한다
;─
;·자신의 일을 어떻게 생각할까 얘기한다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일 조교중에서는 억제된다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_1
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT1, 자신에게로의기분을얘기한다
;RESULT치(=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해 미약등 구상을 스킵)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	;IF 구상 기입 체크(0) && FIRSTACT(0)
	;ENDIF
	
	;=================================================
	;그 조교의 최초의 액션
	;=================================================
	IF 구상기입체크(100) && TFLAG:경과시간 == 1 && ONCEACT(0)
		PRINTFORMW 「저기… 조금 들려주었으면 좋지만」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 나의 일 어떻게 생각하고 있을까?」
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마 안
	;=================================================
	;IF 구상 기입 체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
	;ENDIF
	
	;=================================================
	;자위중
	;=================================================
	;IF 구상 기입 체크(0) && TEQUIP:자위중
	;ENDIF
	
	;=================================================
	;성교중
	;=================================================
	;IF 구상 기입 체크(0) && TEQUIP:성교중
	;ENDIF

	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「%CNAME(MASTER, TARGET)%는 나의 일 어떻게 생각하고 있을까?」
	PRINTFORMW 「저기, 들려줘…」
	RETURN 1
ENDIF

;─
;NO9_ACT2, 성에대한이야기를한다
;─
;·조교자가 성에 대한 지식을 말하기 시작한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·조교자에게 성지식이 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_2
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT2, 성에대한이야기를한다
;RESULT치(=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해 미약등 구상을 스킵)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0)
[SKIPSTART]
	;=================================================
	;첫회
	;=================================================
	IF 구상 기입 체크(0) && FIRSTACT(0)
	ENDIF
	;=================================================
	;안면승마 안
	;=================================================
	IF 구상 기입 체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
	ENDIF
	;=================================================
	;자위중
	;=================================================
	IF 구상 기입 체크(0) && TEQUIP:자위중
	ENDIF
	;=================================================
	;성교중
	;=================================================
	IF 구상 기입 체크(0) && TEQUIP:성교중
	ENDIF
	;=================================================
	;그 조교의 최초의 액션
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:경과시간 == 1
	ENDIF
[SKIPEND]
ENDIF

;─
;NO9_ACT3, 상냥하게위로한다
;─
;·조교자가 상냥한 말로 위로해 온다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_3
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT3, 상냥하게위로한다 
;RESULT치(=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해 미약등 구상을 스킵)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;성교계로 질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
		PRINTFORMW 「걱정하지 말아줘…」
		PRINTFORMW 「데 와 버리면, 분명하게 책임 취해 주기 때문에♪」
		RETURN 1
	ENDIF
	
	[SKIPSTART]
	;==================================================
	;안면승마 안
	;==================================================
	IF 구상 기입 체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
	ENDIF
	
	;==================================================
	;자위중
	;==================================================
	IF 구상 기입 체크(0) && TEQUIP:자위중
		PRINTW
	ENDIF
	
	;==================================================
	;성교중
	;==================================================
	IF 구상 기입 체크(0) && TEQUIP:성교중
	ENDIF
	
	;==================================================
	;갈 것 같다
	;==================================================
	IF 구상 기입 체크(0) && TCVAR:MASTER:갈것같다
		;-------------------------------------------------
		;갈 것 같은 것을 참고 있다
		;-------------------------------------------------
		IF 구상 기입 체크(0) && TCVAR:MASTER:갈것같다 > 5
			PRINTFORMW 「인내 같은거 하지 않고 가 버리세요」
			PRINTFORMW 「그렇지만, 참고 있는 모습은 조금 사랑스러울지도… 후후후」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;초조하게 해 안
		;-------------------------------------------------
		IF 구상 기입 체크(0) && TCVAR:MASTER:초조하게해번 > 1
			PRINTFORMW 「어때? 초조해 해지는 것은 괴로울까?」
			PRINTFORML 「나의 기분이 풀리면, 가게해 주어도 괜찮아요」
			PRINTFORMW 「후후후……」
			RETURN 1
		ENDIF
		
		;-------------------------------
		;그 외
		;-------------------------------
		PRINTFORMW 「갈 것 같은거네?」
		PRINTFORMW 「기다리고 있어… 곧바로가게해 주기 때문에♪」
		RETURN 1
	ENDIF
	[SKIPEND]
	
	;-------------------------
	;그 외
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMDW 앨리스는 사쿠야의 머리를 상냥하게 어루만졌다…
		PRINTFORMW 「후후, 좋아 좋아♪」
	ELSE
		PRINTFORMW 「나도 너무 심한은 하고 싶지 않은 것, 사쿠야」
		PRINTFORMW 「얌전하게 하고 있으면, 나쁘게는 하지 않아요」
	ENDIF
	RETURN 1
ENDIF

;─
;ACT4, 엄격하게위협한다
;─
;·어려운 말로 위협한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT4, 엄격하게위협한다
;RESULT치(=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해 미약등 구상을 스킵)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「기억해 둬 줘」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%를 어떻게 하는지, 모두 나나름이라는 일」
		PRINTFORMW 「이 의미를 모를 정도(수록), 바보가 아닌 것을 기대하고 있어요…」
		RETURN 1
	ENDIF
	
	;=================================================
	;성교계로 질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
		PRINTFORMW 「\@ IS_PREVCOMNAME("안에내지마！") ? 억지로 # %CNAME(MASTER, TARGET)%에 \@질내사정, 최고군요. 후후」
		PRINTFORM 「%CNAME(MASTER, TARGET)%가 분명하게 임신할 때까지, 매일질내에 흘러 들어가 주어요
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;-------------------------------------------------
		;갈 것 같은 것을 참고 있다
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:MASTER:갈것같다 > 5
			PRINTFORMW 「쓸데없는 저항은 그만두면 어때…?」
			PRINTFORMW 「어차피 곧 가 버리기 때문에」
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;초조하게 해 안
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:MASTER:초조하게해번 > 1
			SELECTCASE RANDACT(2, 0)
				CASE 0
					PRINTFORMW 「아라, 갈 것 같아?」
					PRINTFORMW 「안 돼요, 사쿠야. 좀 더 참아 줘…」
				CASE 1
					PRINTFORMW 「가고 싶어서 견딜 수 없는, 은 얼굴이군요?」
					PRINTFORMW 「이대로 쭉 초조하게 하고 있으면%CNAME(MASTER, TARGET)%, 어떻게 되어 버릴까. 저기?」
			ENDSELECT
			CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「후후……」")
			CLEARLINE 1
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;아이마스크 장착중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:아이마스크
		PRINTFORMW 「시야를 빼앗은 당신을 방치하면 어떻게 될까?」
		PRINTFORMW 「조금 재미있을 것 같은 것이긴 해요. 과연 하지 않지만」
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「\@ TENSION(TARGET, 8) == "높다" ? 킥킥 # 후후후 \@……」")
		CLEARLINE 1
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「얌전하게 하고 있으면, 나쁘게는 하지 않아요」
	PRINTFORMW 「쓸데없는 저항은 그만두는 것이 현명해요」
	RETURN 1
ENDIF

;─
;NO9_ACT5, 의상의변경을요구한다
;─
;·조교자가 의상의 변경을 요구해 온다
;·조교 대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:줄이 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_5
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT5_0, 옷을 벗도록(듯이) 명령
;RESULT치(=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해 미약등 구상을 스킵)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「자, 옷을 벗어 줘」 
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 모두, 보여 받아요♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 조교 대상을 몹시 사랑함
	;=================================================
	IF 구상기입체크(100) && AFFECTION("몹시 사랑함")
		PRINTFORMW 「자, 벗어…  나에게 전부 보여 줘…」
		PRINTFORMW 「후훗…  흥분해 버린다♪」
		RETURN 1
	ENDIF

	;-------------------------
	;그 외
	;-------------------------
	PRINTFORML 「그러면, 벗어 받을까」
	PRINTFORMW 「부끄러워하는 일 같은거 없어요」
	PRINTFORMW 「천천히… 나에게 잘 보이도록,♪」
	RETURN 1
ENDIF

;이 근처 범용이다
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	PRINTFORMW 「기분이 바뀌었어요. 저기, 옷을 입어 줘」
	PRINTFORMW 「어째서 사? 별로 이유 같은거 없어요」
	RETURN 1
ENDIF

;─
;NO9_ACT6, 합의를요구한다
;─
;·처음을 받아도 괜찮을까 얘기한다
;·처음=처녀 or동정(양쪽 모두의 경우도 있다)
;·이미 합의를 얻고 있는 경우는 발생하지 않는다
;·조교 대상이 처녀도 동정도 아닌 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
;@KOJO_NO9_MS_ACT_KXX_6
;-------------------------------------------------
;사쿠야씨처녀가 아니어서w
;-------------------------------------------------

;─
;NO9_ACT7, 벌이라고선언한다
;─
;·지금부터 벌 한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:벌플래그는 서 있다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP:밀어넘어뜨리기중 때에는 발생하지 않는다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT7, 벌이라고선언한다
;RESULT치(=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해 미약등 구상을 스킵)
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM측의 처리와 능숙하게 연결되도록(듯이) 조정해야 할
;1. 70이후에 한 번 다시 볼까…
;------------------------------------------------
IF 구상기입체크(100)
	;선언하면 실행해 받지 않으면
	TCVAR:행동예약 = 60
	;=================================================
	;전COM가가 「무뚝뚝하게응답한다/강하게응답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("무뚝뚝하게응답한다/강하게응답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 「아직 자신의 입장이 모르는 것 같네? …… 아휴예요」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 경우, 말로 전하는 것보다, 좀 더 직접적인 방법이 좋은 것일까. 저기?」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「기분좋게해」
	;(이 철면피!)
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("기분좋게해")
		PRINTFORMW 「당신이 그런 일 말하다니, 사쿠야」
		PRINTFORMW 「그렇지만, 지금의 입장을 분별해 받지 않으면 곤란해요」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「좀더하드하게해」
	;(이 돼지째!)
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("좀더하드하게해")
		PRINTFORMW 「기가 막혔군요, 사쿠야. 당신이 그런 일 말하다니」
		PRINTFORMW 「정말로 지금의 상황을 알고 있을까?」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「허가를청한다」
	;(허락해 받을 수 없었습니다)
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("허가를청한다")
		PRINTFORMW 「안 돼요, 허락할 생각은 없는 것」
		PRINTFORMW 「조금 벌이 필요같구나」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「자위하기시작한다」
	;(사람의 이야기를 들어라!)
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("자위하기시작한다")
		PRINTFORMW 「무엇을 마음대로 시작해 버리고 있을까」
		PRINTFORMW 「이것은 벌이 필요할까요」
		;TCVAR:행동예약 = 60
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「분수를 분별하지 않는 이 살인 돌에는…」
	PRINTFORMW 「아무래도 벌이 필요같, 구나」
	RETURN 1
ENDIF

;─
;NO9_ACT8, 고함친다
;─
;·큰 소리로 고함친다
;·턴방침이 "소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:벌플래그에 관련되지 않고, 발생할 가능성이 있다(벌중에 한층 더 고함칠 가능성등)
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT8, 고함친다
;RESULT치(=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해 미약등 구상을 스킵)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;전COM가가 「무뚝뚝하게응답한다/강하게응답한다/싫어한다/날뛴다/거부한다」의 언젠가
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("무뚝뚝하게응답한다/강하게응답한다/싫어한다/날뛴다/거부한다")
		PRINTFORMW 「정말, 거역하고 있을 뿐으로 안절부절 해요…」
		PRINTFORMW 「적당, 화가 나는거야! 그러한 것!」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「사실…, 사랑스럽지 않아요.%CNAME(MASTER, TARGET)%…」
	PRINTFORMW 「그 반항적인 얼굴도 안절부절 하는거야!」 
	RETURN 1
ENDIF

;─
;NO9_ACT9, 기분나쁘게웃는다
;─
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·화제 『비웃는다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_NO9_MS_ACT_K14_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;NO9_ACT9, 기분나쁘게웃는다
;RESULT치(=0, 범용 ACT에 흐르는, 1, 더 이상 표시하지 않는, 2, 표시해 미약등 구상을 스킵)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;아이마스크 장착중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:아이마스크
		PRINTFORMDW 시야를 빼앗겼다%CALLNAME:MASTER%의 귀에, 앨리스의 기분 나쁜 웃음소리가 닿는다…
		RETURN 1
	ENDIF
	
	;MENUMATCH등을 통하면 파생에 대응해 버리므로 오히려 TFLAG 그대로가 좋다든가 그런 곳
	;아무튼 좀 더 할 길이 있는 느낌이다
	;웃음계는 BASE든지 FAVOR든지 MENUMATCH로 판정해 함수화해도 괜찮은 것인지도 모른다든가 생각났지만 뒷전으로 한다
	;=================================================
	;이상계로부터 발생
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT분류 == 7
		PRINTFORMDW 앨리스의 기분 나쁜 웃음소리가 방에 울리고 있다…
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMDW 앨리스의 기분 나쁜 웃음소리가 방안에 울려 퍼진다…
	RETURN 1
ENDIF
;회화계는 조금 정 넣고 할 필요가 있네요