;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	앨리스 구상(대상 한정) 일상 이벤트 구상 파일
;
;	일상 이벤트로 불려 가는 구상을 정리한 파일
;
;	EXTRA 모드로 NO:9 사쿠야를 MASTER에 선택했을 경우만 표시
;
;Version		Reason
;1. 70			@구상 기입 체크 사양에
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;일상 이벤트 구상
;========================================================================
;·굴복각인*5％로 실행 굴복각인은 조교자 마다 판별된다
;·이벤트가 발생했을 때의 내역은 스킨십 67％, 일상 33％
;-------------------------------------------------

;────────────────────────────────────
;EV201, 간호
;────────────────────────────────────
;·조교중 기절한 후
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_201(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;간호
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	IF AFFECTION("상사상애")
		PRINTFORMW 「미안해요, 나, 너무 해 버렸군요…」
	ELSE
		PRINTFORMW 「저기 괜찮아? 나중의 일은 걱정하지 않아도 괜찮아요」
		PRINTFORMW 「천천히 쉬어 줘」
	ENDIF
	RETURN 1
ENDIF

;========================================================================
;스킨십（각종의 성 경험이 들어갑니다）
;========================================================================
;@DAILY_LIFE_1_0～로부터 랜덤으로 선택한다  RETURN 0되면 재추첨, RETURN 1( 0이외)으로 추첨으로부터 빠진다
;-------------------------------------------------

;────────────────────────────────────
;EV202, 스킨십 0
;────────────────────────────────────
;·부엌에서 성희롱
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_202(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 0_0사타구니 만지작거려
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 0_1젖가슴 만지작거려
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	PRINTFORMW 「저기 사쿠야…」
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「네네, 바쁘기 때문에 뒤로 해」")
	PRINTFORMW 「상관해 주지 않으면, 못된 장난해요?」
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「네네, 아무쪼록 자유롭게」")
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 0_2무시하면 단념해 나갔다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「저기, 안돼? 지금은 시 싶지 않은거야…?」
		PRINTFORMW 「…… 이제(벌써), 어쩔 수 없네요. 방해 해 나빴어요, 그대로 계속해 줘」
	ELSE
		PRINTFORMW 「아라, 무정하네요…」
		PRINTFORMW 「방해였던 것 같구나. 그러면, 그대로 계속해 줘」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 0_3몸을 맡겨 사타구니로 가게 되고
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 3
	PRINTW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 0_4몸을 맡겨 젖가슴으로
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 4
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「조, 조금 앨리스! 어디 손대고 있는거야!」")
	PRINTFORMW 「사쿠야가 좋다고 말한거야?」
	PRINTL
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「아니, 확실히, 말했습니다만…」")
	PRINTFORMW 「그렇겠지요? 이봐요, 얌전하게 해 줘」
	PRINTL
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「아, 아직, 하는 거야…?」")
	PRINTFORM 「물론
	CALL HEARTCB(COLOR2_K14("pink"), 1, "나눔고딕")
	PRINTFORMW   사쿠야를 가게 할 때까지, 그만두지 않아요? 후훗」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 0_5팬티갈아입게 해 주었습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 5
	PRINTFORMW 「아라아라, 이렇게 속옷을 더럽혀 버려…」
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「우… 그, 그것은…」")
	PRINTL
	PRINTFORMW 「이야이야 말해도, 결국 좋아져 버린 거네? 후후」
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「우, 시끄럽네요! 방법 없을 것입니다!」")
	PRINTL
	PRINTFORMW 「그래요? 방법 없네요? 후후후」
	CALL PRINTCOLORW(COLOR_K14("사쿠야"), "「아아… 뭔가 굉장히 분해요…」")
	PRINTFORM 「뭐 그래
	CALL HEARTCB(COLOR2_K14("pink"), 1, "나눔고딕")
	PRINTFORMW   갈아입음을 가져와 주기 때문에, 기분전환해요♪」
	RETURN 1
ENDIF

;────────────────────────────────────
;EV203, 스킨십 1
;────────────────────────────────────
;·식당에서 차 한 잔 하면서
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_203(ARG)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 1_0홍차 어떠세요?
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 1_1미약으로 불끈불끈 젖가슴 신경이 쓰인다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 1_2젖가슴 보게 되었습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 2
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 1_3서둘러 도망쳤습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 3
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 1_4달라붙어 봉사하면
;포상질질 끌어 or쿤닐링구스로 가게 되어 버렸어요 권
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 4
;ENDIF

;────────────────────────────────────
;EV204, 스킨십 2
;────────────────────────────────────
;·복도에서 억지로 키스
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_204(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 2_0복도에서 주인님에 불러 세울 수 있었습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 2_1삶
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 2_1몇분 후 만족해 바이바이
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 2
;ENDIF

;────────────────────────────────────
;EV205, 스킨십 3
;────────────────────────────────────
;·복도에서 젖가슴 비비어 비비어
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_205(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 3_0갑자기 등으로부터 껴안는동 쉬었군
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 3_1유
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 3_1만족하지 않게 된…
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 2
;ENDIF


;EV206, 207은 MASTER에 PENIS가 없는 경우, RETURN 0되고 불려 가는 일은 없다
;PENIS유에서도 TALENT:MASTER:동정의 경우, 같은 처리가 된다
;까닭에 당구상에서는 불요

;────────────────────────────────────
;EV208, 스킨십 6
;────────────────────────────────────
;·입욕중에 습격당한다
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_208(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 6_0주인님 목욕탕 강습
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 6_1이봐요 등을 밀어 준다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 6_2에서도 고추는 손대어 주지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 2
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 6_3당신은 참지 못하고가게해！
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 3
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 6_4결국 주인님 쪽이 인내 할 수 없게 되었습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 4
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 6_5그리고 손으로 가게 되었습니다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 5
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 6_6 （녀 당신의 경우）거품 투성이로 만지작거려져…
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 6
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 6_7 （녀 당신의 경우）몇번이나 절정 당했습니다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 7
;ENDIF

;────────────────────────────────────
;EV209, 스킨십 7
;────────────────────────────────────
;·주인님이 바이브로…
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_209(ARG)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 7_0페라의 연습을 하고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 7_1무엇 보고 있어!? (조교자가 청초·수줍음 소유의 경우 한정)
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 7_2어머나… 시험해 봐?
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 2
;ENDIF

;────────────────────────────────────
;EV210, 스킨십 8
;────────────────────────────────────
;·입욕중 주인님과 조수에 습격당해…
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_210(ARG)
;-------------------------------------------------
;#DIM SUBTARGET, 1
;동시 출연하는 캐릭터의 등록 번호를 SUBTARGET에
;SUBTARGET = ARG/100
;이벤트번호를 되돌린다
;ARG %= 100
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 8_0둘이서 목욕탕 강습
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 8_1둘이서 씻어 와→뒤로 선택지로 분기
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 8_2당신에 보여져 흥분하고 있는 모습
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 2
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 8_3차분히 감상하고 있으면 올려 버렸다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 3
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 8_4혼합해 주어요, 계(오)세요라고 말씀하신다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 4
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 8_5둘이서 매트 세탁을 해 주었습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 5
;ENDIF

;────────────────────────────────────
;EV211, 스킨십 9
;────────────────────────────────────
;·목욕탕으로부터 오르면 팬츠가 없었다
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_211(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 9_0스으스으 하지만 어쩔 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스킨십 9_1주인님 뚫어지게 봄
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 2
;ENDIF

;========================================================================
;일상（호감도 상승, 그 외 소프트인 경험 따위）
;========================================================================
;·
;-------------------------------------------------

;────────────────────────────────────
;EV212, 일상 0
;────────────────────────────────────
;·티타임
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_212(ARG)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;일상 0_0차는 어떠세요? 이후에 지문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 0
	IF AFFECTION("상사상애")
		SELECTCASE RANDEVENT(2, 0, DAILY_TARGET)
			CASE 0
				PRINTFORMW 「자, 티타임으로 합시다」
				PRINTFORMW 「티폿트를 신조 해 보았지만, 어때? 꽤 멋지다고 생각하지 않아?」
			CASEELSE
				PRINTFORMW 「저기 봐. 티컵을 갖추어져로 해 보았지만」
				PRINTFORMW 「뭐? 수줍어하고 있는 거야? 우훗, 재미있는 사람이네♪」
		ENDSELECT
	ELSE
		SELECTCASE RANDEVENT(2, 1, DAILY_TARGET)
			CASE 0
				PRINTFORMW 「꼭 좋았어요, 함께 차 한 잔 합시다」
				PRINTFORMW 「가끔 씩은 이런 것도 좋을 것입니다?」
			CASE 1
				PRINTFORMW 「저기, 쿠키를 구었지만%CNAME(MASTER, TARGET)%도 먹을까요?」
				PRINTFORMW 「이 홍차에 반드시 맞는다고 생각해요♪」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;일상 0_1주인님과 즐거운 한 때
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 1
;ENDIF

;────────────────────────────────────
;EV213, 일상 1
;────────────────────────────────────
;·낮잠
;·구상주가 낮잠자고 있는 장면
;·물을까 이것, 조교 대상이 프리덤 지나므로 어떻게든 하고 싶다w
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_213(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;일상 1_0낮잠 발견
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;일상 1_1받아 주세요 천천히
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 1
;ENDIF

;========================================================================
;반항 제거（반항각인을 가진 조교자의 아라이먼트가 높아지면 발생）
;========================================================================
;·이전에는 일상계를 off로 하고 있으면 발생하지 않았지만, 현재는 떼어내지고 있다
;-------------------------------------------------

;────────────────────────────────────
;EV214, 반항 제거
;────────────────────────────────────
;·반항 제거
;-------------------------------------------------
@KOJO_NO9_MS_EVENT_K14_214(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;반항 제거 0_0회유 되었습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	SELECTCASE RANDEVENT(2, 0, DAILY_TARGET)
		CASE 0
			PRINTFORMW 「회유라고 말하면, 뭔가 소문이 나쁘네요…」
		CASE 1
			PRINTFORMW 「조금은 좋게 된 느낌일까. 저기?」
	ENDSELECT
	RETURN 1
ENDIF