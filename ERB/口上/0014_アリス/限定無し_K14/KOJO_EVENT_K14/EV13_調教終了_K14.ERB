;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	앨리스 구상 조교 종료 이벤트 구상 파일
;
;	조교 종료 이벤트 구상을 정리한 파일
;
;Version		Reason
;1. 71			@구상 기입 체크 사양에. 그 외 섬세한 수정
;1. 60			단체 파일로서 독립
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV13, 조교 종료 구상
;========================================================================
;·@EVENTCOMEND로부터 불려 간다
;·구상주가 조교 종료하기 직전에 표시
;·ARG:1, ARG:2의 경우는 지문 표시 후에 PRINTL로 공행. 그 뒤로 구상을 표시
;·ARG:0의 경우는 고유의 지문 없음. 구상 표시전에 PRINTL로 공행이 들어간다
;·RESULT 0이상을 돌려주면 구상 표시 후에 PRINTL로 공행이 들어간다
;-------------------------------------------------
;관련 변수…
;FLAG:일상제어 조교 종료시 상황(1=통상/2=정력 0/3=한번휴식/4=실신했다)
;-------------------------------------------------
@KOJO_EVENT_K14_13(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 보통으로 종료, 1, 조교 대상이 피곤한, 2, 조교 대상이 실신했다)
;-------------------------------------------------

;────────────────────────────────────
;ARG:1, ARG:2, 피로·실신에 의한 조교 종료 FIRSTEVENT(0)～
;────────────────────────────────────
;·조교 대상 소모도에 응해 피로/실신으로 나누어진다
;·호출 타이밍은 지문의 표시→PRINTL→구상의 표시
;·EVENT201, 간호(일상 이벤트)의 개소를 쓰는 경우는, 능숙하게 제휴할 수 있도록(듯이)하면 좋을 것입니다
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 피곤함
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	PRINTFORMW 「――아휴. 그 모습은 이제 무리같구나」
	PRINTFORMW 「뭐 좋아요. 여기서 끝냅시다」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 실신한 FIRSTEVENT(30)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 2 && TALENT:TARGET:주인님
	PRINTFORMW 「너무 해 버린 것 같구나…」
	PRINTFORMW 「오늘은 끝냅시다」
	RETURN 1
ENDIF

;────────────────────────────────────
;ARG:0보통으로 종료 FIRSTEVENT(10)～
;────────────────────────────────────
;·통상의 조교 종료시의 구상
;·지문 없음. PRINTL에 의한 공행의 뒤로 구상 표시
;·테두리마다 이벤트번호를 털어 있습니다만, 임의로 설정하는 일은 가능합니다
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;특정 조건으로 진
;━━━━━━━━━━━━━━━━━━━━━━━━
;OP를 경과가 끝난 상태 또한 상사상애 첫회시의 이벤트를 경과했을 경우 한 번만 표시
;------------------------------------------------
IF GET_KFLAG_K14("상사상애") && KFLAG:(FLAG:주인님):50 == 1
	;첫회를 처리한 뒤는 RETURN 0으로 하위에 흘린다
	IF FIRSTEVENT(20)
		PRINTFORMW 「이런 시간이 언제까지 계속될까…」
		PRINTFORMW 「나, 생각하는 것… 이대로 때가 멈추어 버리면 좋은데, 는…」
		PRINTFORMDW 앨리스는 희미하게 외로운 듯한 표정을 띄우면서,%CALLNAME:MASTER%에 몸을 접대어 왔다…
		PRINTFORMW 「아, 응…… 꽉 해… 그렇게」
		PRINTFORMW 「응…… 행복… 이야」
		PRINTFORMDW 두 명은 얼싸안은 채로, 깊은 잠에 떨어져 갔다…
		RETURN 1
	ENDIF
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;대만족 보너스를 얻고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:이번에대만족 && TALENT:TARGET:주인님
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(10)
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 꽤 전망이 있어요…」
		PRINTFORMW 「그 상태로, 힘껏 노력하는거네」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF GET_KFLAG_K14("상사상애")
		PRINTFORM 「우훗, 매우 좋았어요…
		CALL PRINT_SYMBOL_K14
		PRINTFORMW 「그러면, 함께 잘까요」
		PRINTFORMDW 두 명은 침대에서 얼싸안은 채로, 깊은 잠에 떨어져 갔다…
	ELSE
		PRINTFORMW 「후후, 오늘은 좋았어요」
		PRINTFORM 「다음도 기대하고 있어요…
		CALL PRINT_SYMBOL_K14
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;상사상애（호의 3000이상·호감도 3000이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GET_KFLAG_K14("상사상애")
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「시간이 지나는 것이 빠르고 원망스럽다…」
	PRINTFORMW 「좀 더, 이렇게 하고 있고 싶은데…」
	PRINTFORMDW %CALLNAME:MASTER%가 달래는 동안에, 앨리스는 미소를 띄우면서 편한 숨소리를 세우기 시작했다…
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 완료（호의 2000이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("조교 완료") && TALENT:TARGET:주인님
	PRINTFORMW 「어딘지 부족한 얼굴을 하고 있어요」
	PRINTFORMW 「안 돼요, 그런 얼굴 해도. 나도 한가하지 않기 때문에」
	PRINTFORMW 「괜찮아… 또, 곧바로 귀여워해 주어요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;몹시 사랑함（호감도 2000이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("몹시 사랑함") && TALENT:TARGET:주인님
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(40)
		PRINTFORMDW 앨리스는%CALLNAME:MASTER%에 빠지기 시작한 것 같다…
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 좋아요…… 벌써 먹어 버리고 싶을 정도…」
		PRINTFORMW 「읏… 츗… 응, 응… 응…」
		PRINTFORMDW 앨리스는 조교가 끝난 뒤도,%CALLNAME:MASTER%의 입술을 계속 탐냈다…
		RETURN 1
	ENDIF
	
	;그 외
	PRINTFORMW 「후, 후훅…%CNAME(MASTER, TARGET)%, 정말로 사랑스럽네요…」
	;콘피그로 FADE가 ON의 경우
	IF ANIME_CONFIG("FADE")
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"　　　　　%KOJO_RAND_K14(("키스"), 1)%")
		CLEARLINE 1
	;OFF라면 PRINT
	ELSE
		PRINTFORMW 「%KOJO_RAND_K14(("키스"), 1)%」
	ENDIF
	PRINTFORMDW %CALLNAME:MASTER%의 입안을 유린해, 만족한 앨리스는 방을 뒤로 했다…
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교가 끝난 상태（호의 1000이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("조교가 끝난 상태") && TALENT:TARGET:주인님
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(50)
		PRINTFORMW 「뭐?   그렇게 외로운 것 같은 얼굴 해」
		PRINTFORMW 「걱정하지 않아도, 나는 어디에도 가지 않고」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%도 어디에도 갈 수 있지는 않아요」
		PRINTFORMW 「다음번을 기대하고 있어 줘♪」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「그래요… 솔직하게 하고 있으면 나쁘게는 하지 않아요…」
	PRINTFORMW 「다음은 또 이번이, 군요…… 나도 기대하고 있기 때문에♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;혐오（호의-100 미만）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("혐오")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(60)
		PRINTFORMW 「아라, 무서운 얼굴… 온화하지 않네요」
		PRINTFORMW 「그렇지만 싫지 않아요, 그러한 것」
		PRINTFORMDW 앨리스는 사악한 미소를 띄우면서,%CALLNAME:MASTER%에 가까워져 귓전으로 속삭였다…
		PRINTFORMW 「마음이 접혔다%CNAME(MASTER, TARGET)%가, 어떤 얼굴 하는 것인가…」
		PRINTFORMW 「지금부터 기다려지구나…… 후후후후후……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「그렇게 노려보지 말아줘」
	PRINTFORMW 「그런 얼굴 되면, 나……」
	PRINTFORMW 「자신을 억제할 수 없게 되어 버리기 때문에……」
	PRINTFORMW 「후후후후후……」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;공포（호의 200 미만·트라우마 Lv3 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("공포")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(70)
		PRINTFORMW 「완전히 무서워하고 있어요…」 
		PRINTFORMW 「그렇지만 말야… 그런 얼굴 되면, 나……」
		PRINTFORMW 「좀 더, 좀 더 심한 일 하고 싶어져 버려…♪」
		PRINTFORMW 「후후후후후……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「그렇게 두려워해지면, 좀 더 괴롭히고 싶어지지만?」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%… 알고 있어 주고 있을까, 저기?」
	PRINTFORMW 「후훅… 다음번을 기대하고 있어 줘……」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;반항（호의 0 이하·반항각인 Lv1 이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("반항")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(80)
		PRINTFORMW 「사용할 수 없네요. 흥을 깸이예요…」
		PRINTFORMW 「거역해 어떻게든 되는 상황이 아니라는 것이, 모르는 것일까?」
		PRINTFORMW 「미련하, 구나……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「아직 거역할 생각인 거네……」
	PRINTFORMW 「자신의 입장이 나빠질 뿐(만큼)인 것이, 모르는 것일까?」
	PRINTFORMW 「미련하, 구나……」
	RETURN 1
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;눈을 뜨고（호의 500이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
;·조교되는 것도 오히려 좋아하게 되었는지도 모른다…
;-------------------------------------------------
IF 구상 기입 체크(0) && AFFECTION("눈을 뜸")
ENDIF
;━━━━━━━━━━━━━━━━━━━━━━━━
;순응（호의 100이상·호감도 100이상）
;━━━━━━━━━━━━━━━━━━━━━━━━
;·정중하게 조교를 진행되어, 생각했던 것보다 싫지 않을지도…
;------------------------------------------------
IF 구상 기입 체크(0) && AFFECTION("순응")
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;초반（조건 없음）
;━━━━━━━━━━━━━━━━━━━━━━━━
;·여기까지의 조건에 히트 하지 않았던 경우
;------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(110)
		PRINTFORMW 「우선 이런 곳일까」
		PRINTFORMW 「당황할 필요도 없는 거네」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「시간은 얼마든지 있어요…」
	PRINTFORMW 「차분히 길들여 가 준다. 기대하고 있어 줘……」
	RETURN 1
ENDIF