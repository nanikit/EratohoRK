;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	앨리스 구상 그 외 조교 이벤트 구상 파일
;
;	그 외 조교 턴중 이벤트를 정리한 파일
;
;version		Reason
;1. 71			@구상 기입 체크 사양에
;1. 69			KOJO_TRAIN_K14.ERB로부터 EVENT21/22/23/24및 각인 구상을 분리
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;────────────────────────────────────
;EV21, 대만족 구상
;────────────────────────────────────
;·조교자의 대만족 보너스를 취득했을 경우에 표시
;-------------------------------------------------
@KOJO_EVENT_K14_21(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님대만족
;━━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;붙임성좋게대답한다
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("붙임성좋게대답한다")
		PRINTFORM 「후후, 애교가 있는 아이는 정말 좋아해요
		CALL PRINT_SYMBOL_K14
		SIF GET_COM_RANK() == "무리를 한 행동"
			PRINTFORMW 「조금 일부러다운 생각도 들지만, 말야」
		PRINTFORMDW 앨리스는\@ TFLAG:REACT부하 > 2 ? 만면의 # \@웃음을 띠어,%CALLNAME:MASTER%를 응시하고 있다
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌감을받아들인다/기분좋다！
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("쾌감을받아들인다/기분좋다！")
		PRINTFORM 「솔직하고 사랑스럽네요
		CALL PRINT_SYMBOL_K14
		PRINTFORMDW 앨리스는 만면이 웃음을 띠어,%CALLNAME:MASTER%를 응시하고 있다
		RETURN 1
	ENDIF
	
	;=================================================
	;안에내！
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("안에내！")
		PRINTFORMW 「후훗, 질내사정을 졸라댐이라니……」
		PRINTFORM 「정말 사랑스러운 것일까…
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	[SKIPSTART]
	;=================================================
	;비명을올린다
	;=================================================
	IF 구상 기입 체크(0) && IS_NOWCOMNAME("비명을올린다")
		IF AFFECTION("상사상애")
			PRINTFORMW 「달콤한 소리로, 울까……」
			PRINTFORMW 「저기, 좀 더……, 좀 더 들려줘」
		ELSE
			PRINTFORMW 「좋은 소리로 울어요……」
			PRINTFORMW 「… 견딜 수 없어요. 그 한심한 얼굴도 멋져요?」
			CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), " 「후후…」")
			CLEARLINE 1
		ENDIF
		RETURN 1
	ENDIF
	[SKIPEND]
	
	;=================================================
	;애정을담아준다
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("애정을담아준다")
		IF AFFECTION("상사상애")
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 마음이 전해져 와요…」
			PRINTFORMDL 앨리스는 다 녹은 표정을 띄워, 몸을 접대어 왔다…
			PRINTL
			PRINTFORM 「좋아해요…, 너무 좋아…\@ CFLAG:TARGET:의존도 > CFLAG:MASTER:의존도 ?!  # \@
			CALL PRINT_HEARTCBW_K14(COLOR2_K14("pink"), , , COLOR2_K14("ALICE"))
			PRINTFORMDW 앨리스는 물기를 띤 눈동자로%CALLNAME:MASTER%를 응시하고 있다…
		ELSE
			PRINTFORMW 「이렇게 되어…, 나로, 나 따위에…」
			PRINTFORMDW 앨리스는 다 녹은 표정을 띄워, 몸을 접대어 왔다…
			PRINTFORM 「후후, 나쁘지 않아요. 이런 것…
			CALL PRINT_SYMBOL_K14
		ENDIF
		RETURN 1
	ENDIF
	
	;SELECTCOM == 15는 상층에서 처리
	;=================================================
	;REACT분류가 적극적 따른다(적극적한다, 하는대로등)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("적극적으로 한다/받아들인다")
		PRINTFORMW 「좋아요……, 매우 좋은 느낌…」
		PRINTFORMDW 앨리스는 물기를 띤 눈동자로%CALLNAME:MASTER%를 응시하고 있다…
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;그 외를 처리
	;---------------------------------------------
	PRINTFORMW 「후후, 좋은 느낌이군요」
	PRINTFORM 「%CNAME(MASTER, TARGET)%, 나쁘지 않아요
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;────────────────────────────────────
;EV22, 상태변화 구상
;────────────────────────────────────
;·상태 이상이 붙었을 때에 표시
;·TCVAR:상태변화가 조교자, TCVAR:MASTER:상태변화가 조교 대상
;·상태변화가 일어나면 3 턴 지속한다
;-------------------------------------------------
;·TCVAR:상태변화지속을 일률 3 턴이라든지()
;-------------------------------------------------
@KOJO_EVENT_K14_22(ARG)
;-------------------------------------------------

;*************************************************
;EV22_0, 조교자 상태변화
;*************************************************
;T_COND TARGET가 지정한 상태라면 진을 돌려준다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[피폐]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && T_COND("피폐") && !TFLAG:한번휴식
	;상태변화지속 턴을 2에
	TCVAR:상태변화지속 = 2
	;2/3의 확률로 표시 없음 RESULT에 0을 돌려준다
	IF RAND:3
		RETURN 0
	;1/3의 확률로 표시
	ELSE
		SELECTCASE TENSION(TARGET, 8)
			CASE "높다"
				PRINTFORMW 「지쳤지만… 기분 좋은 피로라는 느낌이군요…♪」
			CASE "일반"
				PRINTFORMW 「조금 지쳤군요…」
			CASE "낮다"
				PRINTFORMW 「후우…… 지쳤군요……」
		ENDSELECT
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[쇠약]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && T_COND("쇠약") && !TFLAG:한번휴식
	TCVAR:상태변화지속 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「하아…… 하아……」
		PRINTFORMDW 앨리스는 어깨로 숨을 쉬면서, 다 지친 표정을 보였다
		PRINTFORMW 「…… 조금 너무 해 버렸군요…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[무기력]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && T_COND("무기력")
	TCVAR:상태변화지속 = 2
	IF RAND:3
		RETURN 0
	ELSE
		SELECTCASE TENSION(TARGET, 8)
			CASE "높다"
				PRINTFORMW 「잘 모르지만… 갑자기 이것도 저것도 싫게 되었어요…」
			CASE "일반"
				PRINTFORMW 「무엇인가… 아무것도 할 마음이 생기지 않네요…」
			CASE "낮다"
				PRINTFORMW 「뭔가 흥을 깸이군요……」
		ENDSELECT
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[몽롱]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && T_COND("몽롱")
	TCVAR:상태변화지속 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「읏…! 머리가 어질 어질해요…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[정욕]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && T_COND("정욕")
	;정욕은 3 턴인 채로
	;TCVAR:상태변화지속 = 2
	IF RAND:3
		RETURN 0
	ELSE
		SELECTCASE TENSION(TARGET, 8)
			CASE "높다"
				PRINTFORML 「쿳……! 이제 안돼…, 인내 할 수 없다……!」
				PRINTFORML 「철저하게 귀여워해 주기 때문에 각오 해…」
			CASE "일반"
				PRINTFORML 「안돼, 이런 건… 억제하지 않으면…」
				PRINTFORML 「그렇지만,%CNAME(MASTER, TARGET)%를 엉망진창으로 하고 싶어서, 나…」
			CASE "낮다"
				PRINTFORML 「나… 스스로도 무서울 정도 흥분하고 있다…」
		ENDSELECT
		PRINTFORMDW %CALLNAME:MASTER%를 응시하는 앨리스의 눈동자는, 요염하게 물기를 띠고 있다…
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[분노]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && T_COND("분노")
	IF AFFECTION("상사상애")
		TCVAR:상태변화지속 = 1
	ELSEIF AFFECTION("몹시 사랑함")
		IF RAND:3
			TCVAR:상태변화지속 = 2
		ELSE
			TCVAR:상태변화지속 = 1
		ENDIF
	ELSEIF AFFECTION("권애")
		IF RAND:3
			TCVAR:상태변화지속 = 3
		ELSE
			TCVAR:상태변화지속 = 2
		ENDIF
	ELSE
		TCVAR:상태변화지속 = 3
	ENDIF
	IF RAND:3
		RETURN 0
	ELSE
		;앨리스님만족도 파생 처리
		SELECTCASE TENSION(TARGET, 8)
			CASE "높다"
				PRINTFORML 「나쁘지 않은 기분이었지만… 완전히 엉망으로 해 주었군요?」
				PRINTFORMW 「그 대상은 비싸게 들기 때문에, 각오 해…」
			CASE "일반"
				PRINTFORML 「부탁이니까, 너무 화나게 하지 말아줘」
				PRINTFORMW 「어디까지 자신을 누를 수 있는지, 그다지 자신이 없어요…」
			CASE "낮다"
				PRINTFORMW 「나를 화나게 해도 좋은 일은 없어요…」
		ENDSELECT
		RETURN 2
	ENDIF
ENDIF

;100%
;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[싫증]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && T_COND("지루함")
	;조교의 경과시간
	SELECTCASE TFLAG:경과시간
		;길다
		CASE IS >= 60
			PRINTFORMW 「과연, 흥이 깨어 왔군요……」
			TCVAR:상태변화지속 = 3
		;보통
		CASE IS >= 30
			PRINTFORMW 「하나 더, 흥이 타지 않아요……」
			TCVAR:상태변화지속 = 2
		;짧다
		CASEELSE
			PRINTFORMW 「무엇인가, 좀 더,……」
			TCVAR:상태변화지속 = 1
	ENDSELECT
	RETURN 2
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[울적]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && T_COND("울적")
	TCVAR:상태변화지속 = 2
	IF RAND:3
		RETURN 0
	ELSE
		RETURN 2
	ENDIF
ENDIF
;TALENT:광기가 있는 경우에게만 1/5의 확률로 울적화
[SKIPEND]

;*************************************************
;EV22_1, 조교 대상 상태변화
;*************************************************
;M_COND MASTER가 지정한 상태라면 진을 돌려준다
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[피폐]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && M_COND("피폐") && !TFLAG:한번휴식
	TCVAR:MASTER:상태변화지속 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「조금 지친 것 같구나?」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[쇠약]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && M_COND("쇠약") && !TFLAG:한번휴식
	TCVAR:MASTER:상태변화지속 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「… 심한 모양이군요. 조금 너무 했을까…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[무기력]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && M_COND("무기력")
	TCVAR:MASTER:상태변화지속 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「… 마음에 들지 않네요. 그 김이 빠진 태도…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[몽롱]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && M_COND("몽롱")
	TCVAR:MASTER:상태변화지속 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORML 「발 밑이 휘청거리고 있어요. 도대체 어떻게 해 버렸을까, 저기?」
		PRINTFORMW 「후후후…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[정욕]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(33) && M_COND("정욕")
	;아이마스크 장착중
	IF TEQUIP:아이마스크
		PRINTFORMW 「하? 뭐야. 눈가리개 되고 있는데 콧김 난폭하게 해 버려」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 흥분해 버리고 있는 것?」
	;그 외
	ELSE
		PRINTFORM 「조금 건방지구나? 이
		CALL PRINTFONTSTYLE(COLOR2_K14("ALICE"), "나", 1)
		PRINTFORMW 를 그런 눈으로 응시하다니」
	ENDIF
	SIF AFFECTION("상사상애")
		PRINTFORMW 「그렇지만 그렇구나…, 기분은 나쁘지 않아요. 후훗」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[분노]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && M_COND("분노")
	IF AFFECTION("상사상애")
		TCVAR:MASTER:상태변화지속 = 1
	ELSEIF AFFECTION("조교 완료")
		IF RAND:3
			TCVAR:MASTER:상태변화지속 = 2
		ELSE
			TCVAR:MASTER:상태변화지속 = 1
		ENDIF
	ELSEIF AFFECTION("조교가 끝난 상태")
		IF RAND:3
			TCVAR:MASTER:상태변화지속 = 3
		ELSE
			TCVAR:MASTER:상태변화지속 = 2
		ENDIF
	ELSE
		TCVAR:MASTER:상태변화지속 = 3
	ENDIF
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「아라, 기분 나쁜 것 같구나?」
		PRINTFORMW 「다만, 너무 현명한 태도라고는 할 수 없어요. 그러한 것…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[싫증]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && M_COND("지루함")
	TCVAR:MASTER:상태변화지속 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「뭔가 화나요, 그 태도…」
		RETURN 2
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[울적]화
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && M_COND("울적")
	TCVAR:MASTER:상태변화지속 = 2
	IF RAND:3
		RETURN 0
	ELSE
		PRINTFORMW 「조금 모습이 이상하네요…」
		RETURN 2
	ENDIF
ENDIF

;────────────────────────────────────
;EV23, 조교 대상 능력치 업 구상
;────────────────────────────────────
;·조교중에 조교 대상의 능력치가 상승했을 때에 표시
;-------------------------------------------------
@KOJO_EVENT_K14_23(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(이번 상승한 ABL 번호/각 ARG 번호는 ABL의 거기에 상당)
;
;ARG:0 순종/1욕망/2기교/3 Ｃ감각/4 Ｖ감각/5 Ａ감각/6 Ｂ감각/7봉사정신/8노출증/9레즈끼/10ＢＬ끼/11마조끼
;12자위중독/13정액중독/14레즈중독/15고통중독/16구속중독
;
;ARG:20 회화/21애무/22도구/23성교/24수치/25봉사/26가학/27이상
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[순종]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 0
	;보기 힘들기 때문에 개행
	PRINTL 
	PRINTFORMW 「순수가 1번이군요」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[욕망]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	PRINTL 
	PRINTFORMW 「그래요…  좀 더 욕망에 정직하게 되세요…」
	PRINTFORM 「내가, 전부 받아 들여 준다%RANDTEXT_K14("로부터", "원")%
	IF RAND:3
		PRINTW 」
	ELSE
		CALL PRINT_SYMBOL_K14
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[기교]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 2
	PRINTL 
	SELECTCASE RANDEVENT(2, 0)
		CASE 0
			PRINTFORMW 「조금 능숙해진 느낌일까?」
		CASE 1
			PRINTFORMW 「다소는 사용할 수 있는 기분이 들었을까, 저기?」
			IF !RAND:3
				CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「후후……」")
				CLEARLINE 1
			ENDIF
	ENDSELECT
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;Ｃ/Ｖ/Ａ/Ｂ감각중 한쪽이 업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GROUPMATCH(ARG, 3, 4, 5, 6)
	PRINTL
	SELECTCASE RANDEVENT(2, 0)
		CASE 0
			PRINTFORMW 「조금 예민해진 것 같구나?」
		CASE 1
			PRINTFORMW 「감도가 좋아진 것 같구나…?」
	ENDSELECT
	RETURN 2
ENDIF

[SKIPSTART]
이하 모두 OFF 상 comment out 해 둔다
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[Ｃ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG == 3
	PRINTL 
	PRINTFORMW 「감도가 좋아진 것 같구나…?」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[Ｖ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG == 4
	PRINTL 
	PRINTFORMW 「감도가 좋아진 것 같구나…?」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[Ａ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG == 5
	PRINTL 
	PRINTFORMW 「감도가 좋아진 것 같구나…?」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[Ｂ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG == 6
	PRINTL 
	PRINTFORMW 「감도가 좋아진 것 같구나…?」
	RETURN 2
ENDIF
[SKIPEND]

;────────────────────────────────────
;EV24, 조교자 능력치 업 구상
;────────────────────────────────────
;·조교중에 조교자의 능력치가 상승했을 때에 표시
;-------------------------------------------------
@KOJO_EVENT_K14_24(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(이번 상승한 ABL 번호)
;
;ARG:0 순종/1욕망/2기교/3 Ｃ감각/4 Ｖ감각/5 Ａ감각/6 Ｂ감각/7봉사정신/8노출증/9레즈끼/10ＢＬ끼/11마조끼
;12자위중독/13정액중독/14레즈중독/15고통중독/16구속중독
;
;ARG:20 회화/21애무/22도구/23성교/24수치/25봉사/26가학/27이상
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[욕망]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	;보기 힘들기 때문에 개행
	PRINTL 
	PRINTFORMW 「좀 더 나를 즐겁게 해…」
	RETURN 2
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[기교]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[Ｃ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[Ｖ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG == 4
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[Ａ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG == 5
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[Ｂ감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG == 6
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[레즈끼]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG == 9
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[자위중독]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG == 12
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[애무]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ARG == 21
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스님[가학]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 26
	PRINTL
	SELECTCASE ABL:TARGET:가학
		CASE IS >= 9
			PRINTFORML 「조금 자신이 알지 않게 되어 왔어요」
			PRINTFORMW 「그렇지만, 이제(벌써) 제지당할 것 같지 않다…」
		CASE IS >= 6
			PRINTFORMW 「자신에게 이런 면이 있다니 몰랐어요…」
		CASE IS >= 3
			PRINTFORMW 「너무 좋은 소리로 울어지면, 그만둘 수 없게 될 것 같다…」
		CASEELSE
			PRINTFORML 「스스로도 잘 모르지만」
			PRINTFORMW 「뭔가 흥분해요, 이것…」
	ENDSELECT
	RETURN 2
ENDIF

;────────────────────────────────────
;EV29, 굴복·고통각인에 의한 반항각인 상한 처리
;────────────────────────────────────
;·반항각인 Lv상한의 처리
;·반항각인은 10-(고통각인+굴복각인)/2를 넘지 않는다
;-------------------------------------------------
@KOJO_EVENT_K14_29(ARG)
;-------------------------------------------------
;%CALLNAME:TARGET%가 주는 쾌락\@ MARK:MASTER:고통각인? 라고 고통 # \@가 %CALLNAME:MASTER%로부터 반항의 기력을 빼앗아 간다…
;반항각인은{MARK:반항각인}이 되었다
;-------------------------------------------------
;물건은 시험삼아 조금 써 본다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;반항각인 상한 처리
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;계층 이벤트. 3개 보면 ELSE 이후에 처리라든지 그런 느낌으로
	SELECTCASE ORDEREVENT(0)
		CASE 0
			PRINTFORMW 「조금은 솔직해진 것 같지만……, 아직도 지금부터라는 곳이군요」
		CASE 1
			PRINTFORML 「%CNAME(MASTER, TARGET)%도 다소는 좋게 되었을까?」
			PRINTFORMW 「… 읏, 나의 기분탓일지도 모르지만」
		CASE 2
			PRINTFORMW 「미련한%CNAME(MASTER, TARGET)%에서도, 거역해도 쓸데없다는 일을 알아차렸을까. 저기?」
		CASEELSE
			PRINTFORML 「무익한 저항을 계속한 곳에서, 아무것도 얻는 것은 없다」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%가 거기에 깨달을 수 없을 정도(수록), 바보가 아니면 좋지만 말야?」
			;페이드 인
			CALL FADEIN(13, @"「왜냐하면 그것은……,%CNAME(MASTER, TARGET)%자신이기 때문에이기도 하기 때문에」")
			;페이드 아웃
			CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「\@ ALI(0) ? 후후후 # 우후후 \@…」")
			SIF ANIME_CONFIG("FADE")
				CLEARLINE 1
	ENDSELECT
	RETURN 1
ENDIF

;========================================================================
;각인 취득 구상
;========================================================================
;·조교중에 각인을 취득시켰을 경우에 표시. 어느 레벨로 구상 표시할까는 가감(상태) 자유로운 것으로 개개로 조정하는 것이 바람직함
;·기입 체크가=1의 경우, 동시에 복수 취득했을 때는 이 파일로 맨 위의 것만을 표시한다
;·=2의 경우, 취득한 각인의 구상 모든 것을 위로부터 순서에 표시한다
;·각인 취득에 의한 소질 변동전에 표시하기 위해(때문에), 소질 취득/상실할까는 이 시점에서의 TALENT의 유무로 판정할 수 있다
;-------------------------------------------------
@KOJO_MARK_K14(ARG:0, ARG:1, ARG:2, ARG:3, ARG:4)
#DIM 기입체크
#DIM 모두순서에표시
모두순서에표시 = 0	;각인 취득 구상 「모두 순서에 표시」를 위한 초기화 처리
;-------------------------------------------------
;인수 내역…
;ARG:(각인 번호)=(이번 취득한 레벨)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;[트라우마]취득
기입체크 = 1	;0. 비표시/1. 동시에 취득한 각인을 이것 이하 표시하지 않는다/2. 모두 순서에 표시한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 기입체크 && ARG:4
	SETBIT 모두순서에표시, 4
	SELECTCASE ARG:4
		;트라우마 Lv1~Lv9
		CASE 1 TO 9
			;첫회
			IF FIRSTMARK(0)
				PRINTFORMW 「――과연, 저기.%CNAME(MASTER, TARGET)%는%ACTSTR(CFLAG:MASTER:트라우마)%가 트라우마, 라고」
				PRINTFORMW 「%RAND_SPLIT("어머나? 속여도 이미 늦어요/거짓말 해도 안돼")%。%CNAME(MASTER, TARGET)%는 알기 쉬운거야」
				PRINTFORML 「뭐 그렇지만――」
				;페이드 아웃, 조금 빠른
				CALL FADE(8, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「\@ ALI(0) ? 후후 # 후훅 \@…」")
				;FADE 관련의 코드를 깨끗이 시키는 함수를 만들었으므로, 향후, 이 손의 녀석은 불필요하게 된다
				SIF ANIME_CONFIG("FADE")
					CLEARLINE 1
				PRINTFORMW 「조금 재미있어져 왔을까. 저기?」
			;첫회 이후는 RANDMARK로
			ELSE
				;ORDERMARK에서도 재미있는 것인지도 모른다
				SELECTCASE RANDMARK(4, 0)
					CASE 0
						PRINTFORMW 「아라,%ACTSTR(CFLAG:MASTER:트라우마)%가 상당히 견딘 것 같구나?」
					CASE 1
						PRINTFORMW 「%ACTSTR(CFLAG:MASTER:트라우마)%가 그렇게 싫었을까」
					CASE 2
						PRINTFORMW 「%CNAME(MASTER, TARGET)%……、%ACTSTR(CFLAG:MASTER:트라우마)%가 싫은거네」
						PRINTFORMW 「그러니까 라고, 나는 손대중 할 생각 없지만. 후후…」
					CASE 3
						PRINTFORMW 「헤에, 그렇게.%CNAME(MASTER, TARGET)%、%ACTSTR(CFLAG:MASTER:트라우마)%에 약해…」
						PRINTFORMW 「뭐 그렇지만…, 나에게 알려진 것은 맛이 없었지요?  %RAND_SPLIT("킥킥/킥킥/후후후")%…」
				ENDSELECT
				;첫회를 포함해 트라우마 Lv1로부터 5까지는 덮어 쓰는 것 없음
				;Lv6로부터는 RANDMARK 나름이라고 하는 곳이지만, 연속해 같은 것이 표시될 가능성은 한 번 만이라고 하는 일로 타협
				;원래 모든 Lv 쓴다면 FIRST도 RAND도 필요하지 않고. 아무튼 어떻게든 FIRSTMARK의 차례를 만들고 싶었다고 하는 것이 본심일지도 모른다
				;첫회는 SELECTCASE ARG:4의 CASE 1사용하라고 한편 만 없는 것은 불평 없음으로
			ENDIF
		;트라우마 Lv10…[겁쟁이]가 없는 경우, 그것을 얻는다
		;              이미[겁쟁이]가 있어[광기]가 없는 경우,[유치]가 있는 경우는 100%, 없는 경우는 1/3의 확률로[광기]를 얻는다
		;              [광기]의 취득 판정에 새어, 한편[유치]를 가지지 않는 경우,[유치]를 얻는다(유아 퇴행 한다)
		;              현상,[광기]와[유치]는 어느 쪽으로 구를까 먼저 판정할 방법은 없다(요망이 있으면…). 우선 머리가 이상해진다고 해석해 주세요
		CASE 10
			;미묘한 점이다. 10000이라면 너무 낮을 생각은 든다
			IF CFLAG:MASTER:의존도 > 12000
				;조교 대상이 겁쟁이/광기/유치중 1개이상을 소지
				IF TRUECHECK(TALENT:MASTER:겁쟁이, TALENT:MASTER:광기, TALENT:MASTER:유치) > 1
					PRINTFORM 「뭐, 방법 없네요\@ AFFECTION("상사상애") ? … # \@。
					IF AFFECTION("상사상애")
						PRINTFORMW 분명하게 책임은 취해 주어요」
						PRINTFORMW 「쭉 나의 옆에서 귀여워해 주기 때문에,%CNAME(MASTER, TARGET)%는 아무것도 걱정하지 않아도 괜찮은거야…」
					ELSE
						PRINTFORMW %CNAME(MASTER, TARGET)%자신도 바란 결과지요?」
						PRINTFORMW 「앞으로도, 나의 인형으로서 귀여워해 주기 때문에…」
						CALL FADEIN(10, "「――안심해도 좋아요」")
						CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「우후후…」")
						SIF ANIME_CONFIG("FADE")
							CLEARLINE 1
					ENDIF
				;조교 대상이 겁쟁이/광기/유치중 1개를 소지
				ELSE
					PRINTFORMW 「언젠가 이런 일이 되는 것이 아닌가 하고 생각하고 있었다. 그렇지만…」
					PRINTFORMW 「%CNAME(MASTER, TARGET)%자신도 바라지 않는 한, 이렇게는 안 되었을 것이야」
					PRINTFORMW 「뭐, 뭐가 되었든…, 나는%CNAME(MASTER, TARGET)%의 일,\@ AFFECTION("상사상애") ? 앞으로도 쭉 사랑해 주어요 # 버리거나는 하지 않으니까 \@…」
				ENDIF
			ELSE
				IF AFFECTION("상사상애")
					PRINTFORML 「뭐라고 할까……」
					PRINTFORMW 「만회 할 수 없는 것을 해 버린 것 같아요…」
				ELSE
					PRINTFORML 「우, 이것은 과연……」
					PRINTFORMW 「나, 너무 해 버렸을까…」
				ENDIF
			ENDIF
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 4
	ENDSELECT
	SIF GETBIT(모두순서에표시, 4) && 기입체크 == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[고통각인]취득
기입체크 = 1	;0. 비표시/1. 동시에 취득한 각인을 이것 이하 표시하지 않는다/2. 모두 순서에 표시한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 기입체크 && ARG:0
	SETBIT 모두순서에표시, 0
	SELECTCASE ARG:0
		;고통각인 Lv2
		CASE 2
			PRINTFORMW 「%CNAME(MASTER, TARGET)%에는 좀 더 아픔에 익숙해 받지 않으면,」
		;고통각인 Lv5
		CASE 5
			PRINTFORMW 「조금은 고통에도 익숙해져 왔을까요」
		;고통각인 Lv7
		CASE 7
			PRINTFORMW 「저기, 아픈 것이 좋아하게 되어 왔지 않아…?」
			PRINTFORMW 
		;고통각인 Lv9…[기장]이 있는 경우, 그것을 잃는다
		;CASE 9
		;	PRINTFORMW 「아라…?   심이 접혀 버린 것 같구나」
		;	PRINTFORMW 「그것으로 좋은 것…  다부진 인형 같은거 사랑스럽지 않은 것…♪」
		;고통각인 Lv10
		CASE 10
			PRINTFORM 「후후…, 이제(벌써) 훌륭한 변태 마조,…
			CALL PRINT_SYMBOL_K14
			PRINTFORML 「아라, 신경쓰는 일 없다고 생각해요?  %CNAME(MASTER, TARGET)%에는 적당하다고 생각하고」
			CALL FADEIN(8, "「나로 해도…」")
			PRINTFORMW 「싫지 않기도 하고? 그러한 것. 후후…」
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 0
	ENDSELECT
	SIF GETBIT(모두순서에표시, 0) && 기입체크 == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[반항각인]취득
기입체크 = 1	;（0, 비표시, 1, 동시에 취득한 각인을 이것 이하 표시하지 않는, 2, 모두 순서에 표시한다）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG:3
	SETBIT 모두순서에표시, 3
	SELECTCASE ARG:3
		;반항각인 Lv2
		CASE 2
			PRINTFORMW 「그것이 어떤 결과에 연결되는지, 자주(잘) 생각한 (분)편이 몸을 위해서(때문에)…」
		;반항각인 Lv5
		CASE 5
			PRINTFORMW 「어디까지나 나에게 반항할 생각일까…?」
		;반항각인 Lv7
		CASE 7
			PRINTFORMW 「그런 태도가 언제까지나 용서된다고 생각하면 큰 실수야」
		CASE 8, 9
			PRINTFORMW 「사실, 허락하기 어렵네요.%CNAME(MASTER, TARGET)%…」
		;반항각인 Lv10
		CASE 10
			PRINTFORMW 「허락하기 어렵네요, 그 태도…」
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 3
	ENDSELECT
	SIF GETBIT(모두순서에표시, 3) && 기입체크 == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[굴복각인]취득
기입체크 = 1	;（0, 비표시, 1, 동시에 취득한 각인을 이것 이하 표시하지 않는, 2, 모두 순서에 표시한다）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 기입체크 && ARG:2
	SETBIT 모두순서에표시, 2
	SELECTCASE ARG:2
		;굴복각인 Lv2
		CASE 2
			PRINTFORMW 「조금은 솔직해졌을까」
		;굴복각인 Lv5
		CASE 5
			PRINTFORMW 「\@ ALI(0) ? 후후 # 후훅 \@, 좋은 경향이군요」
		;굴복각인 Lv7…[반항적]이 있는 경우, 그것을 잃는다
		CASE 7
			PRINTFORMW 「간신히 거역해도 쓸데없는 것이 안 것 같, 구나…?」
		;굴복각인 Lv10…[프라이드높음]이 있는 경우, 그것을 잃는다
		CASE 10
			PRINTFORMW 「그래요……, 시시한 프라이드 같은거 버리게 되세요」
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 2
	ENDSELECT
	SIF GETBIT(모두순서에표시, 2) && 기입체크 == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[쾌락각인]취득
기입체크 = 1	;（0, 비표시, 1, 동시에 취득한 각인을 이것 이하 표시하지 않는, 2, 모두 순서에 표시한다）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 기입체크 && ARG:1
	SETBIT 모두순서에표시, 1
	SELECTCASE ARG:1
		;쾌락각인 Lv2
		CASE 2
			PRINTFORMW 「아무리 말로 부정해도, 다 속일 수 있는 것은 아니에요…」
		;쾌락각인 Lv5…[쾌감을부정]이 있는 경우, 그것을 잃는다
		CASE 5
			PRINTFORM 「후훅, 체는 정직하구나…
			CALL PRINT_SYMBOL_K14
		;쾌락각인 Lv7…[억압]이 있는 경우, 그것을 잃는다
		CASE 7
			PRINTFORMW 「그래요…, 자신을 좀 더 해방해…」
		;쾌락각인 Lv10…[쾌감에솔직]이 없는 경우, 그것을 얻는다
		CASE 10
			PRINTFORMW 「그래요, 그것으로 좋은거야…」
			PRINTFORM 「지금부터, 좀 더 귀여워해 주어요…
			CALL PRINT_SYMBOL_K14
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 1
	ENDSELECT
	SIF GETBIT(모두순서에표시, 1) && 기입체크 == 1
		RETURN 1
ENDIF

;각인 취득 구상 「모두 순서에 표시」를 위한 종단 처리
RETURN 모두순서에표시 != 0
