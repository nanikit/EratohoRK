;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	앨리스 구상 이상계 개별 리액션 구상 파일
;
;	이상계 리액션 구상을 정리한 파일
;
;Version		Reason
;1. 71			@구상 기입 체크 사양에
;1. 65			TFLAG:REACT 부하 < 2의 경우 COM에 흐르도록(듯이)
;1. 62			분기를 COM마다. SELECTCOM를 IS_NOWCOMNAME에
;1. 60			ACT 분류 단체 파일로서 독립
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;=================================================
;관련 변수…
;
;TFLAG:REACT 분류(0=없음/1=소극적 따른다/2=적극적 따른다/3=허가를 청한다/4=날뛴다/5=요구에 응하지 않는, 거부/6=도망치는, 발버둥 친다)
;IS_COMGRONAME("없음/소극적 따른다/적극적 따른다/허가를 청한다/날뛴다/거부/도망친다")
;
;CONDITION (상태치로부터 캐릭터 라인을 취득한다  TFLAG:60, 61으로 TARGET와 MASTER를 별개에 참조)
;
;TENSION (BASE치를 대략적으로 나타낸다  0, 체력, 1, 기력, 2, 사정, 3, 모유, 4, 소변욕, 5, 이성, 6, 흥미, 7, 초조해, 8, 만족)
;50% 이상은 높은,25%이상은 일반,25% 미만은 낮다

;FIRSTREACT
;첫회 ACT 판정 함수
;TFLAG:ACT(TFLAG:90)를 자동으로 참조, 현재 실행되고 있는 REACT 마다 번호가 독립해 보존된다
;
;ONCEREACT
;조교내미독판정 함수  조교전에 초기화된다
;그 외의 사양은 거의 FIRSTREACT와 같이
;=================================================

;─
;이상계
;─

;-------------------------------------------------
;통상, 이상계는 방치 플레이 이외에 떨어지는 일은 우선 없는 것
;그리고 fist계나 육변기는 취미가 아니고, 보고 싶지도 않다. 이런 일로 구상측에서 봉인
;그런 (뜻)이유로 방치 플레이만
;-------------------------------------------------
;라고는 해도, 일정한 조건으로 건방진 노예를 아프게 하는 것은있음으로 생각하므로
;어떠한 특수 효과를 생각해도 좋은 것인지도, 라고 할 생각은 들고 있다
;문제는, 그것이 결국 포상 밖에 되지 않는 것이다!
;-------------------------------------------------
;-------------------------------------------------
;*************************************************
;REACT73, 방치 플레이·개별 리액션 RANDREACT(0) ~ (7)
;*************************************************
;=================================================
@KOJO_REACT_K14_73
;=================================================

;━━━━━━━━━━━━━━━━━━━━━━━━
;침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다") && TFLAG:REACT부하 < 2
	IF AFFECTION("상사상애")
		PRINTFORMW 「후후, 조금 불안한 느낌일까?」
		PRINTFORM 「그렇지만 안 돼요. 좀 더 그대로 있어 줘
		CALL PRINT_SYMBOL_K14
	ELSE
		PRINTFORMW 「뭐야, 입다물어 버려」
		PRINTFORMW 「설마 외롭다든가 말하지 않네요?」
		PRINTFORM 「뭐, 무슨 말을 한 곳에서 지금은 상대로 할 생각은 없어요. 나쁘지만
		CALL PRINT_SYMBOL_K14
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;심약구응답하는 RANDREACT(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("심약구응답한다") && TFLAG:REACT부하 < 2
	SELECTCASE RANDREACT(2, 0)
		CASE 0
			PRINTFORMW 「기운이 없네요? 혹시 불안하다든가?」
			PRINTFORM 「뭐, 그런%CNAME(MASTER, TARGET)%를 방치해 바라보는 것도 나쁘지 않아요
			CALL PRINT_SYMBOL_K14
			SIF !AFFECTION("상사상애")
				PRINTFORMW 「후후후……」
		CASE 1
			PRINTFORMW 「불안해 침착하지 않은 것 같구나?」
			PRINTFORMW 「나쁘지만, 좀 더 그대로 있어 받기 때문에」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강하게 응답하는 RANDREACT(1)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("강하게 응답한다") && TFLAG:REACT부하 < 2
	SELECTCASE RANDREACT(2, 1)
		CASE 0
			PRINTFORMW 「이런 때에 용감한 일 말해도 의미가 없다고 생각하지만 말야」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%가 무슨 말을 한 곳에서, 상대로 할 생각은 없는거야」
			PRINTFORMW 「적어도 현재, 는요……」
		CASE 1
			PRINTFORMW 「…………」
			PRINTFORMDW 앨리스는 식은 눈으로%CALLNAME:MASTER%를 보고 있다…
	ENDSELECT
	RETURN 1
ENDIF

;이 야비한 돼지째! 에 파생하면 연주한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;좀 더 하드하게 해 RANDREACT(2)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("좀 더 하드하게 해") && TFLAG:REACT파생 < 1 && TFLAG:REACT부하 < 2
	SELECTCASE RANDREACT(2, 2)
		CASE 0
			PRINTFORMW 「지금 그런 일 말해져도, 저기?」
			PRINTFORMW 「좋으니까…, 말해진 대로, 거기서 얌전하게 하고 있어 줘」
			PRINTFORM 「심심함 기분인%CNAME(MASTER, TARGET)%의 모습, 여기로부터 보게 해 받기 때문에
			CALL PRINT_SYMBOL_K14
			SIF !AFFECTION("상사상애")
				PRINTFORMW 「후훗……」
		CASE 1
			PRINTFORMW 「아라, 방치해지는 것은 침착하지 않아?」
			PRINTFORM 「뭐 그렇지만…, 지금은 아무것도 해 주지 않아요. 나쁘지만
			CALL PRINT_SYMBOL_K14
	ENDSELECT
	RETURN 1
ENDIF

;의견하다니 좋은 담력이군요! 에 파생하면 연주한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;허가를 청하는 RANDREACT(3)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("허가를 청한다") && TFLAG:REACT파생 < 1
	SELECTCASE RANDREACT(2, 3)
		CASE 0
			PRINTFORMW 「나쁘네요. (듣)묻지 않았어요」
			PRINTFORMW 「아아, 다시 말하지 않아도 괜찮아요. 어차피 (들)물을 생각은 없기도 하고,」
			PRINTFORMW 「후후……」
		CASE 1
			PRINTFORMW 「이것은 예상외의 반응이군요…」
			PRINTFORM 「원래, 무엇을 허락하면 좋은 것인지… 나는 모르지만
			CALL PRINT_SYMBOL_K14
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;기분 좋게 해 RANDREACT(4)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("기분 좋게 해") && TFLAG:REACT파생 < 1 && TFLAG:REACT부하 < 2
	;=================================================
	;상사상애 한편,1/3의 확률로 표시
	;=================================================
	IF 구상기입체크(100) && AFFECTION("상사상애") && !RAND:3
		PRINTFORMW 「우, 그런 일 말해지면 조금 마음이 요동하는데……」
		PRINTFORMW 「그렇지만 역시 안돼. 그대로 얌전하게 하고 있어」
		PRINTFORMW 「다음에 충분히 귀여워해 주기 때문에. 후후…」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(60)
		SELECTCASE RANDREACT(3, 4)
			CASE 0
				PRINTFORMW 「안 돼요, 그런 일 말해도」
				PRINTFORMW 「지금은 아무것도 할 생각은 없는거야. 그리고, 그래그래…」
				PRINTFORM 「마음대로 뭔가 하는 것을 인정할 생각도 없어요? 나·쁘·지·만
				CALL PRINT_SYMBOL_K14
			CASE 1
				PRINTFORMW 「유감이구나? 지금은 그런 기분은 되지 않는거야」
				PRINTFORMW 「우선, 내가 좋다고 말할 때까지 얌전하게 하고 있어」
			CASE 2
				PRINTFORMW 「이 상황으로 졸라댐이라든지……, 조금 생각하면 쓸데없다고 깨달을 것 같은 것이지만」
				PRINTFORMW 「뭐, 나쁘지만 단념해. 거기서 얌전하게 해 주세요…」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;부끄러워한다(상황에 대해서) RANDREACT(5)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("부끄러워한다") && TFLAG:REACT파생 < 1
	SELECTCASE RANDREACT(2, 5)
		CASE 0
			PRINTFORMW 「도대체 무엇이 부끄러운 것인지 잘 모르지만…」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%가 부끄러워하는 모습을 바라보는 것은 나쁘지 않네요. 후후」
		CASE 1
			PRINTFORMW 「뭐야, 나에게 가만히 응시할 수 있는 것이 부끄러운 것?」
			PRINTFORMW 「라고 말해도…? 한 눈을 팔 수도 없고. 거기에…」
			PRINTFORM 「이런 것도 나쁘지 않아요. 때에게는,
			CALL PRINT_SYMBOL_K14
	ENDSELECT
	RETURN 1
ENDIF

;마음대로 자위 하지마! 에 파생하면 연주한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;자위하기 시작하는 RANDREACT(6)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("자위하기 시작한다") && TFLAG:REACT파생 < 1
	SELECTCASE RANDREACT(2, 6)
		CASE 0
			PRINTFORMW 「…… 아휴,. 정말, 몹시 질려요」
		CASE 1
			PRINTFORMW 「나는 거기서 얌전하고하고 있는 듯 하게 말했을 것이지만, 저기?」
			PRINTFORMW 「제멋대로인 일, 하지 말아 줘……」
		CASE 2
			PRINTFORMW 「그런 일 해 나의 기분을 끌려고 해도 안 돼요」
			PRINTFORMW 「그런 것, 볼 생각도 하지 않고. 마음대로 하면 좋아요」
			PRINTFORMW 「그렇지만, 이렇게 해 차갑게 다루어지는 것도 흥분해 버리겠지요?」
			PRINTFORMW 「정말, 어쩔 수 없는 변태, 군요…」
		CASE 3
			PRINTFORMW 「…………」
			PRINTFORMDW 앨리스는 경멸해 버린 눈으로%CALLNAME:MASTER%를 보고 있다…
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;도구를 제외하는 RANDREACT(7)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("도구를 제외한다")
	SELECTCASE RANDREACT(2, 7)
		CASE 0
			PRINTFORMW 「저기, 전에 말하지 않았을까?」
			PRINTFORMW 「제멋대로인 일 되면 곤란한거야. 거기에…」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%자신에게 도움이 되지 않는다고 생각해요. 그러한 것……」
		CASE 1
			PRINTFORMW 「제외하고 싶었던 것이라면 말하면 좋은데? 뭐라고 말한 곳에서…」
			PRINTFORMW 「그것을 인정할 생각도 없고, 마음대로 제외하는 것도 인정할 생각은 없는 것, 나쁘지만」
			PRINTFORMW 「제멋대로인 흉내를 한 외상은 지불해 받아요. 머지않아 그 중,」
	ENDSELECT
	RETURN 1
ENDIF

;***********************************************************
;REACT75, 페니스 밴드 장착
;***********************************************************
;조교자가 페니스 밴드를 몸에 익혀 과시한다고 하는 시추에이션
;조교자가 처녀인 경우, 처녀 상실이 된다
;이미 장비 하고 있는 경우는 해제가 된다
;-------------------------------------------------
;REACT시의 본체 지문은@TRAIN_MESSAGE2에는 없음
;@REACTION_MESSAGE로 COM마다 처리된다
;-------------------------------------------------

;=================================================
@KOJO_REACT_K14_75
;=================================================
;우선 테두리만으로 뒷전
;-------------------------------------------------
;오토 모드용 처리
IF !TFLAG:ACT파생 && TCVAR:처녀상실 && FLAG:오토모드 && FLAG:오토모드표시설정 == 0 && FIRSTREACT(100)
	CALL PRINT_RAINBOW2W("처녀 상실", 20)
	CALL GAME_OVER_K14
	FORCEWAIT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장착
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ! TFLAG:ACT 파생
;여기를 COM마다는 너무 귀찮기 때문에 분기를 나누지 않고 정리해 버려도 괜찮을까, 라고 하는 개인적 인상
;라고 할까 거동에 따라서는 조건식을 빈틈없이 채운 다음 행동 예약일까w
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;해제
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:ACT 파생 == 1
;ENDIF