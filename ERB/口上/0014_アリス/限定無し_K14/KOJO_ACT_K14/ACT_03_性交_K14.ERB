;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	앨리스 구상 성교계 액션 구상 파일
;
;	성교계 액션 구상을 정리한 파일
;
;Version		Reason
;1. 71			@구상 기입 체크 사양에. 그 외 섬세한 수정
;1. 61			TFLAG:ACT파생분기를 각파생 마다 분할. 일부 구상의 함수화. 그 외 가필 수정
;1. 60			ACT분류단체 파일로서 독립
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;성교계 ACT30～35
;========================================================================
;·TFLAG:ACT(30=정상위/31=후배위/32=기승위/33=대면좌위/34=배면좌위/35=애널섹스)
;·TFLAG:ACT분류 == 3에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "성교")로 줍는 것이 가능
;·원칙으로서 조교자→조교 대상의 삽입이 된다
;·넣으려고 하고 있는 곳에 즈음한다
;·미약·로션을 사용하는 케이스가 있다
;·체위의 제휴에 의해 TFLAG:ACT파생로 파생한다
;· 각 ACT 에 있어서 주도권은@SEX_INITIATIVE에서 정의된다
;-------------------------------------------------
;관련 플래그…
;TFLAG:ACT파생
;(=0, 통상, 1, 동일 체위 연속, 2, 체위 변경, 3, Ａ⇔Ｖ의 꽂아 바꾸어, 4, 통상⇔역강간의 교대, 5, 통상⇔역애널의 교대)
;TFLAG:전회질내사정
;(1=Ｖ성교/2=Ｖ성교 봉사/3=Ｖ성교(하늘 쏘아 맞히고)/4=Ｖ성교 봉사(하늘 쏘아 맞히고)/5=Ａ성교/6=Ａ성교 봉사/7=Ａ성교(하늘 쏘아 맞히고)/8=Ａ성교 봉사(하늘 쏘아 맞히고))
;TFLAG:106 뽑지않고
;-------------------------------------------------
;관련 함수…
;FIRSTACT
;첫회 ACT 판정 함수
;TFLAG:ACT(TFLAG:90)를 자동으로 참조, 현재 실행되고 있는 ACT 마다 번호가 독립해 보존된다
;
;ONCEACT
;조교내미독판정 함수  조교전에 초기화된다
;그 외의 사양은 거의 FIRSTACT와 같이
;
;RANDACT
;랜덤 이벤트의 표시순서 정리 함수
;전종 볼 때까지 같은 숫자는 돌려주지 않는다. 모두 표시하면 초기화
;@KOJO_ACT_KX1_Y1 마다 관리된다
;-------------------------------------------------

;────────────────────────────────────
;ACT30, 정상위
;────────────────────────────────────
;·정상위·조교자가 조교 대상으로 삽입한다
;·주도권은 TARGET(SEX_INITIATIVE(TARGET))
;·조교 대상이 남자의 경우는 발생하지 않는다
;·조교자의 후타나리화, 혹은 페니스밴드장착에 의해 발생한다
;·TEQUIP(삼각목마/페니스밴드/바이브) 장착중에는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·TEQUIP:성교중 == 5(배면좌위)로부터는 발생하지 않는다
;·TFLAG:ACT파생는 전턴으로부터 상태, 및 체위의 참조에 충당된다
;-------------------------------------------------
@KOJO_ACT_K14_30
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속 실행/2=다른Ｖ성교로부터/3=Ａ성교로부터/4=Ｖ성교 봉사로부터/5=Ａ성교 봉사로부터)
;-------------------------------------------------
;·REACT분류가날뛴다였던 경우는 캔슬된다
;·받아들였을 경우, MASTER가 처녀라면 처녀상실, TARGET가 동정이라면 동정상실이 일어난다
;·v1. 010 현재, 거절했을 경우의 SOURCE 리셋트 처리가 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT30, 정상위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;조교 대상이 처녀
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:처녀
		PRINTFORMW 「자, 가요. 준비는 좋을까…」
		PRINTFORMW 「이봐요, 힘을 빼…」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 동정
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:동정
		PRINTFORMW 「… 능숙하게 할 수 있으면 좋지만」
		PRINTFORMW 「아팠으면 말해 줘…」
		RETURN 1
	ENDIF
	
	;Ａ섹스 이외는 함수로 범용 처리
	;=================================================
	;전회질내사정
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 && TFLAG:전턴성교
		;범용 처리
		CALL PRINTTEXT_CATEGORY_3_ACT_1_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 1:연속(정상위로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		;-------------------------------------------------
		;조교자가 갈 것 같다
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
			;범용 처리
			CALL PRINTTEXT_CATEGORY_3_ACT_2_K14
			RETURN 1
		ENDIF
		
		;함수로 범용 처리. 함수내에서 ACT마다 분기 시킨다
		;-------------------------------------------------
		;조교자가 정욕
		;-------------------------------------------------
		IF 구상기입체크(100) && T_COND("정욕")
			;범용 처리
			CALL PRINTTEXT_CATEGORY_3_ACT_3_K14
			RETURN 1
		ENDIF
		
		;---------------------
		;샌 것은 여기서 처리
		;---------------------
		SELECTCASE RANDACT(2, 0)
			CASE 0
				PRINTFORMW 「응쿳! 저기, 안쪽에 해당되고 있는 것 알까…」
				PRINTFORMW 「아라, 이제 대답할 여유는 없는 것 같구나? 후훗」
			CASE 1
				PRINTFORMW 「후후, 다리를 얽히게 되어져 와 버려. 사랑스럽다」
				PRINTFORM 「좋아요…… 좀 더 격렬하게 찔러 준다
				CALL PRINT_SYMBOL_K14
		ENDSELECT
		RETURN 1
	ENDIF
	
	;배면좌위로부터는 파생하지 않는다
	;=================================================
	;파생 2:체위 변경(Ｖ성교로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		SELECTCASE RANDACT(2, 1)
			CASE 0
				PRINTFORMW 「이봐요, 맞는 곳 변하고 있고겠지?」
				PRINTFORM 「후후, 좀 더 마구 찔러 주어요
				CALL PRINT_SYMBOL_K14
			CASE 1
				PRINTFORMW 「부끄러워하지 말고……, 좀 더%CNAME(MASTER, TARGET)%의 얼굴을 보여 줘」
				PRINTFORMW 「키스 하면서 귀여워해 주기 때문에……」
				;페이드 아웃
				CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), KOJO_RAND_K14(("키스"), 1))
				CLEARLINE 1
				PRINTFORMDW %CALLNAME:TARGET%는 수치에 몸부림친다%CALLNAME:MASTER%의 팔을 억눌러, 키스를 하면서 허리를 움직이기 시작했다…
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 3:Ａ성교로부터
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 3
		;No. 2
		SELECTCASE RANDACT(2, 2)
			CASE 0
				PRINTFORMW 「장액과 애액이 서로 섞여, 굉장한 일이 되어 있어요…」
				PRINTFORM 「그렇지만, 후후……%CNAME(MASTER, TARGET)%의 질내, 매우 좋아요
				CALL PRINT_SYMBOL_K14
			CASE 1
				PRINTFORM 「후후, 얼굴이 가까우면 부끄러운 것일까? 사랑스러워요
				CALL PRINT_SYMBOL_K14
				PRINTFORMW 「이봐요, 가요. 좋은 소리로 울어 줘……」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;기본적으로 사정할 때까지는 다른 행동이 되는 일은 없기 때문에
	;페니스밴드에 의한 성교 봉사로부터의 파생, 이라고 하는 안배
	;TFLAG:ACT파생 == 4는 00616판 시점에서, 거의 무의미한 상태라고 생각되고
	;=================================================
	;파생 4/파생 5:Ｖ성교 봉사/Ａ성교 봉사로부터
	;=================================================
	IF 구상기입체크(100) && GROUPMATCH(TFLAG:ACT파생, 4, 5) > 0
		;조교자가 초조하고 있다
		IF TENSION(TARGET, 7) == "높다"
			;범용 처리
			CALL PRINTTEXT_CATEGORY_3_ACT_4_K14
		ELSE
			SELECTCASE RANDACT(2, 3)
				CASE 0
					PRINTFORMW 「…… 공수 교체라는 곳일까」
					PRINTFORM 「좋아해 미치게 해 주기 때문에, 각오 해 줘
					CALL PRINT_SYMBOL_K14
				CASE 1
					PRINTFORMW 「자, 이번은 나의 차례야」
					PRINTFORM 「후후…, 마음껏 찔러 주기 때문에 즐겨 줘
					CALL PRINT_SYMBOL_K14
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;페니스밴드 사용시
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(!PENIS(TARGET), TEQUIP:조교자페니스밴드) > 0
		PRINTFORMW 「자, 준비는 좋을까? 이런 멋없는 것이지만…」
		PRINTFORM 「%CNAME(MASTER, TARGET)%라면 반드시 즐겨 받을 수 있다고 생각해요…
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「자, 나의 것으로 관철해 준다」
	PRINTFORMW 「이것을 갖고 싶었던 것이지요? 후후…, 정직하게 말하세요」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT31, 후배위
;────────────────────────────────────
;·후배위·조교자가 조교 대상으로 삽입한다
;·주도권은 TARGET(SEX_INITIATIVE(TARGET))
;·조교 대상이 남자의 경우는 발생하지 않는다
;·조교자의 후타나리화, 혹은 페니스밴드장착에 의해 발생한다
;·TEQUIP(삼각목마/페니스밴드/바이브) 장착중에는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP:성교중 == 4(대면좌위)로부터는 발생하지 않는다
;·TFLAG:ACT파생는 전턴으로부터 상태, 및 체위의 참조에 충당된다
;-------------------------------------------------
@KOJO_ACT_K14_31
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속 실행/2=다른Ｖ성교로부터/3=Ａ성교로부터/4=Ｖ성교 봉사로부터/5=Ａ성교 봉사로부터)
;-------------------------------------------------
;·REACT분류가날뛴다였던 경우는 캔슬된다
;·받아들였을 경우, MASTER가 처녀라면 처녀상실, TARGET가 동정이라면 동정상실이 일어난다
;·v1. 010 현재, 거절했을 경우의 SOURCE 리셋트 처리가 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT31, 후배위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;파생에 관련되지 않는 것은 상층에 분리한다
	;=================================================
	;조교 대상이 처녀
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:처녀
		PRINTFORMW 「처음이 뒤로부터는, 도착적이라고 생각하지 않아? 후후」
		PRINTFORMW 「자, 힘을 빼 줘…… 차분히 길들여 주기 때문에…」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 동정
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:동정
		PRINTFORMW 「후후…… 드디, 어」
		PRINTFORMW 「능숙하게 할 수 있으면 좋지만…… 얌전하게 하고 있어 줘」
		RETURN 1
	ENDIF
	
	;=================================================
	;전회질내사정
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 && TFLAG:전턴성교
		;범용 처리
		CALL PRINTTEXT_CATEGORY_3_ACT_1_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 1:연속(후배위로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		;-------------------------------------------------
		;조교자가 갈 것 같다
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
			;2/3의 확률로 표시
			IF RAND:3
				PRINTFORMW 「뒤로부터라면 자극이 강하네요……」
				PRINTFORMW 「읏! 이, 이제(벌써) 나…… 가 버릴지도」
			;1/3의 확률로 범용 처리
			ELSE
				CALL PRINTTEXT_CATEGORY_3_ACT_2_K14
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;조교자가 정욕
		;-------------------------------------------------
		IF 구상기입체크(100) && T_COND("정욕")
			;범용 처리
			CALL PRINTTEXT_CATEGORY_3_ACT_3_K14
			RETURN 1
		ENDIF
		
		;-----------------------------------------
		;샌 것은 여기서 처리
		;-----------------------------------------
		SELECTCASE RANDACT(2, 0)
			CASE 0
				PRINTFORMW 「엉덩이의 구멍이 뻔히 보임으로 추잡하네요, 이것……」
				PRINTFORMW 「저기, 개 같은 모습으로 범해지는 것은 어떤 기분일까?」
				IF !RAND:3
					CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「후후후…」")
					CLEARLINE 1
				ENDIF
			CASE 1
				PRINTFORM 「눈앞에서 엉덩이의 구멍이 흠칫흠칫 하고 있어요……
				CALL PRINT_SYMBOL_K14
				PRINTFORM 「이 모습……,%CNAME(MASTER, TARGET)%를 강간하고 있는 것 같아 흥분하는거네요
				CALL PRINT_SYMBOL_K14
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 2:체위 변경(Ｖ성교로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		PRINTFORMW 「후후, 사랑스러운 엉덩이군요? 뒤로부터 충분히 범해 준다」
		PRINTFORMW 「스치는 곳이 달라, 반드시 즐겨 받을 수 있다고 생각해요」
		IF !RAND:3
			CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「후후후…」")
			CLEARLINE 1
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 3:Ａ성교로부터
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 3
		SELECTCASE RANDACT(2, 1)
			CASE 0
				PRINTFORMW 「이봐요, 여기의 구멍이 외로웠던 것이지요?」
				PRINTFORMW 「후후…… 장액과 애액으로 굉장한 미끈미끈」
			CASE 1
				PRINTFORMW 「응, 굉장한 젖고 있어요. 여기에 갖고 싶었다면 말하면 좋았는데?」
				PRINTFORMW 「뭐, 그건 그걸로하고……, 마음껏 가기 때문에, 좋은 소리로 울어 줘……」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 4/파생 5:Ｖ성교 봉사/Ａ성교 봉사로부터
	;=================================================
	IF 구상기입체크(100) && GROUPMATCH(TFLAG:ACT파생, 4, 5) > 0
		;조교자가 초조하고 있다
		IF TENSION(TARGET, 7) == "높다"
			;범용 처리
			CALL PRINTTEXT_CATEGORY_3_ACT_4_K14
		;Ｖ성교 봉사로부터
		ELSEIF TFLAG:ACT파생 == 4
			PRINTFORMW 「이번은 내가 해 주어요. 후후, 자엉덩이를 여기에 향하여?」
			PRINTFORMW 「이봐요, 손을 붙어…… 좀 더 엉덩이를 높게 올리세요」
		;Ａ성교 봉사로부터
		ELSEIF TFLAG:ACT파생 == 5
			PRINTFORMW 「자, 나의 엉덩이를 귀여워해 준 답례야」
			PRINTFORM 「다만…… 나는 앞에서 즐겁게 해 받는 일로 하지만, 말야
			CALL PRINT_SYMBOL_K14
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;페니스밴드 사용시
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(!PENIS(TARGET), TEQUIP:조교자페니스밴드) > 0
		PRINTFORMW 「저기,%CNAME(MASTER, TARGET)%가 지금 느끼고 있는 것은 불안? 그렇지 않으면 기대하고 있을까?」
		PRINTFORMW 「뭐, 어쨌든…… 이 유사품으로 마음껏 울릴 수 있어 준다」
		PRINTFORMW 「망가져 버릴 정도로 찔러 주기 때문에……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE RANDACT(2, 2)
		CASE 0
			PRINTFORMW 「자, 네발로 엎드림이 되어 엉덩이를 여기에 향하여 줘…」
			PRINTFORM 「후후……, 좋은 경치군요. 마음껏 범해 주어요
		CASE 1
			PRINTFORMW 「눈앞에서 엉덩이의 구멍이 흠칫흠칫 하고 있어요」
			PRINTFORM 「후훗, 이 모습……%CNAME(MASTER, TARGET)%를 강간하고 있는 것 같아 흥분하는거네요
	ENDSELECT
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT32, 기승위
;────────────────────────────────────
;·기승위·조교자가 조교 대상으로 삽입한다
;·주도권은 MASTER(SEX_INITIATIVE(MASTER))
;·조교 대상이 남자의 경우는 발생하지 않는다
;·조교자의 후타나리화, 혹은 페니스밴드장착에 의해 발생한다
;·TEQUIP(삼각목마/페니스밴드/바이브) 장착중에는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·TEQUIP:쓰러트리기중에는 발생하지 않는다
;·TFLAG:ACT파생는 전턴으로부터 상태, 및 체위의 참조에 충당된다
;-------------------------------------------------
@KOJO_ACT_K14_32
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속 실행/2=다른Ｖ성교로부터/3=Ａ성교로부터/4=Ｖ성교 봉사로부터/5=Ａ성교 봉사로부터)
;-------------------------------------------------
;·REACT분류가날뛴다였던 경우는 캔슬된다
;·받아들였을 경우, MASTER가 처녀라면 처녀상실, TARGET가 동정이라면 동정상실이 일어난다
;·거절했을 경우, SOURCE:MASTER:쾌Ｖ와 SOURCE:쾌Ｃ는 리셋트 된다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT32, 기승위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;조교 대상이 처녀
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:처녀
		PRINTFORMW 「자, 자신으로부터 바치는거야」
		PRINTFORMW 「당황하지 않아도 괜찮으니까……, 천천히, 느리구나」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 동정
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:동정
		PRINTFORMW 「나의 처음은 이 모습으로 해요」
		PRINTFORMW 「그러면, 아무쪼록 부탁해요」 
		RETURN 1
	ENDIF
	
	;=================================================
	;전회질내사정
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 && TFLAG:전턴성교
		;범용 처리
		CALL PRINTTEXT_CATEGORY_3_ACT_1_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 1:연속(기승위로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		;-------------------------------------------------
		;조교자가 갈 것 같다
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
			IF RAND:3
				SELECTCASE RANDACT(2, 0)
					CASE 0
						PRINTFORMW 「응읏! 그런 허리사용 되면……」
						PRINTFORMW 「나…… 이제(벌써), 가 버릴지도」
					CASE 1
						PRINTFORMW 「읏!  %CNAME(MASTER, TARGET)%의 허리사용이 추잡하고, 슬슬 한계같다…」
						PRINTFORMW 「좋아요…… 좀 더 움직여. 나도 좀 더 밀어올려 주어요…」
				ENDSELECT
			ELSE
				;범용 처리
				CALL PRINTTEXT_CATEGORY_3_ACT_2_K14
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;조교자가 정욕
		;-------------------------------------------------
		IF 구상기입체크(100) && T_COND("정욕")
			;범용 처리
			CALL PRINTTEXT_CATEGORY_3_ACT_3_K14
			RETURN 1
		ENDIF
		
		;---------------------
		;그 외
		;---------------------
		PRINTFORMW 「이봐요, 좀 더 노력해 움직여. 후후」
		PRINTFORMW 「나도 밀어올려 주기 때문에……」
		RETURN 1
	ENDIF
	
	;=================================================
	;파생:2 체위 변경(Ｖ성교로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		PRINTFORMW 「자, 이번은 위를 타 줘」
		PRINTFORMW 「이봐요, 이렇게 해…… 허리를 동글동글 시키는 것과 상하에 움직이는 것을 짜맞추는거야」
		PRINTFORM 「저기? 이렇게 하면 서로 기분 좋아질 수 있어요
		CALL AUTOHEARTCB2_K14
		PRINTFORMW   후후……」
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 3:Ａ성교로부터
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 3
		PRINTFORMW 「자, 이번은 기승위야」
		PRINTFORMW 「…… 기다려. 그쪽이 아니고 나는 앞에서 시 싶은거야」
		PRINTFORMW 「그래, 그것으로 좋아요…… 는, 천천히 앉아」
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 4/파생 5:Ｖ성교 봉사/Ａ성교 봉사로부터
	;=================================================
	IF 구상기입체크(100) && GROUPMATCH(TFLAG:ACT파생, 4, 5) > 0
		;조교자가 초조하고 있다
		IF TENSION(TARGET, 7) == "높다"
			;범용 처리
			CALL PRINTTEXT_CATEGORY_3_ACT_4_K14
		;그 외
		ELSE
			SELECTCASE RANDACT(3, 1)
				CASE 0
					PRINTFORMW 「이번은 내가 시라고 주어요. 자, 위를 타 줘」
					PRINTFORMW 「그래, 좋아요. 그대로 앉아……」
				CASE 1
					PRINTFORMW 「자, 이번은 나의 차례……」
					PRINTFORMW 「그래요……, 나의 위를 타」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;페니스밴드 사용시
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(!PENIS(TARGET), TEQUIP:조교자페니스밴드) > 0
		PRINTFORMW 「이런 멋없는 대용품에서도,%CNAME(MASTER, TARGET)%가 흐트러지는 것을 조망되는 것은 나쁘지 않네요」
		PRINTFORM 「자…, 와. 사양말고 흐트러져 줘…
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「자, 나의 위에 걸쳐 줘」
	PRINTFORM 「%CNAME(MASTER, TARGET)%가 어떤 흐트러지는 방법 하는지, 기다려지구나
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT33, 대면좌위
;────────────────────────────────────
;·대면좌위·조교자가 조교 대상으로 삽입한다
;·주도권은 TARGET(SEX_INITIATIVE(TARGET))
;·조교 대상이 남자의 경우는 발생하지 않는다
;·조교자의 후타나리화, 혹은 페니스밴드장착에 의해 발생한다
;·TEQUIP(삼각목마/페니스밴드/바이브) 장착중에는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·후배위, 대면좌위로부터는 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·TFLAG:ACT파생는 전턴으로부터 상태, 및 체위의 참조에 충당된다
;-------------------------------------------------
@KOJO_ACT_K14_33
;-------------------------------------------------
;TFLAG:ACT파생(0=통상/1=연속 실행/2=다른Ｖ성교로부터/3=Ａ성교로부터/4=Ｖ성교 봉사로부터/5=Ａ성교 봉사로부터)
;-------------------------------------------------
;·REACT분류가날뛴다였던 경우는 캔슬된다
;·받아들였을 경우, MASTER가 처녀라면 처녀상실, TARGET가 동정이라면 동정상실이 일어난다
;·0203판 현재, 거절했을 경우의 SOURCE 리셋트 처리가 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT33, 대면좌위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;조교 대상이 처녀
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:처녀
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 두근두근이 전해져 와요. 이봐요, 긴장하지 않고?」
		PRINTFORMW 「그래……, 나의 목에 손을 써. 괜찮기 때문에……」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 동정
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:동정
		PRINTFORMW 「나의 처음은 이 모습으로 해요」
		PRINTFORM 「그러면, 아무쪼록 부탁해요
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;전회질내사정
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 && TFLAG:전턴성교
		;범용 처리
		CALL PRINTTEXT_CATEGORY_3_ACT_1_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
		;범용 처리
		CALL PRINTTEXT_CATEGORY_3_ACT_2_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 1:연속(대면좌위로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		;-------------------------------------------------
		;조교자가 갈 것 같다
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
			;2/3의 확률로 표시
			IF RAND:3
				PRINTFORMW 「핫, 하…… 응읏!  %KOJO_RAND_K14(("키스"), 1)%」
				PRINTFORMDW 스스로의 한계를 감지했다%CALLNAME:TARGET%는, 키스를 하면서보다 격렬하게 허리를 강탈 세웠다…
			;1/3의 확률로 범용 처리를 부른다
			ELSE
				CALL PRINTTEXT_CATEGORY_3_ACT_2_K14
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;조교자가 정욕
		;-------------------------------------------------
		IF 구상기입체크(100) && T_COND("정욕")
			;범용 처리
			CALL PRINTTEXT_CATEGORY_3_ACT_3_K14
			RETURN 1
		ENDIF
		
		;---------------------
		;그 외
		;---------------------
		SELECTCASE RANDACT(2, 0)
			CASE 0
				PRINTFORM 「후후, 확실히 매달려 버려. 사랑스럽다…
				CALL PRINT_SYMBOL_K14
				PRINTFORMW 「이봐요, 키스 해 준다. 좀 더 입을 열어……」
				;페이드 아웃
				CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), KOJO_RAND_K14(("키스"), 1))
				CLEARLINE 1
			CASE 1
				PRINTFORMW 「%KOJO_RAND_K14(("키스"), 1)%」
				PRINTFORMW 「…… 사랑스러워요. 이대로 부수어 버리고 싶을 정도……」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 2/파생 3:체위 변경(Ｖ성교/Ａ성교로부터)
	;=================================================
	IF 구상기입체크(100) && GROUPMATCH(TFLAG:ACT파생, 2, 3) > 0
		;No. 1
		SELECTCASE RANDACT(2, 1)
			CASE 0
				PRINTFORMW 「얼싸안으면서 하는 것, 기분 좋을 것입니다?」
				PRINTFORMW 「이봐요, 좀 더 강하게 껴안아 주기 때문에…%CNAME(MASTER, TARGET)%도……」
			CASE 1
				PRINTFORMW 「자, 얼싸안으면서 해요」
				PRINTFORMW 「읏, 응츗…… 그렇게, 좀 더 혀를 걸어……」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 4/파생 5:Ｖ성교 봉사/Ａ성교 봉사로부터
	;=================================================
	IF 구상기입체크(100) && GROUPMATCH(TFLAG:ACT파생, 4, 5) > 0
		;조교자가 초조하고 있다
		IF TENSION(TARGET, 7) == "높다"
			;범용 처리
			CALL PRINTTEXT_CATEGORY_3_ACT_4_K14
		;그 외
		ELSE
			SELECTCASE RANDACT(2, 2)
				CASE 0
					PRINTFORM 「네, 공수 교체
					CALL PRINT_SYMBOL_K14
					PRINTFORMW 「포옹하면서 찔러 주어요. 후후후……」
				CASE 1
					PRINTFORMW 「이번은 내가 해 주어요」
					PRINTFORMW 「이봐요, 얼싸안아 키스 하면서…」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;페니스밴드 사용시
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(!PENIS(TARGET), TEQUIP:조교자페니스밴드) > 0
		PRINTFORMW 「이런 완구로도%CNAME(MASTER, TARGET)%안을 감지하는 일은 할 수 있어요」
		PRINTFORMW 「자, 와…… 껴안으면서, 남자같이 찔러 주기 때문에…」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「포옹하면서 찔러 주어요」
	PRINTFORM 「자, 어서오세요. 귀여워해 준다…
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT34, 배면좌위
;────────────────────────────────────
;·배면좌위·조교자가 조교 대상으로 삽입한다
;·주도권은 TARGET(SEX_INITIATIVE(TARGET))
;·조교 대상이 남자의 경우는 발생하지 않는다
;·조교자의 후타나리화, 혹은 페니스밴드장착에 의해 발생한다
;·TEQUIP(삼각목마/페니스밴드/바이브) 장착중에는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·후배위, 대면좌위로부터는 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·정상위, 대면좌위로부터는 발생하지 않는다
;·TFLAG:ACT파생는 전턴으로부터 상태, 및 체위의 참조에 충당된다
;-------------------------------------------------
@KOJO_ACT_K14_34
;-------------------------------------------------
;TFLAG:ACT파생(0=통상/1=연속 실행/2=다른Ｖ성교로부터/3=Ａ성교로부터/4=Ｖ성교 봉사로부터/5=Ａ성교 봉사로부터)
;-------------------------------------------------
;·REACT분류가날뛴다였던 경우는 캔슬된다
;·받아들였을 경우, MASTER가 처녀라면 처녀상실, TARGET가 동정이라면 동정상실이 일어난다
;·v1. 010 현재, 거절했을 경우의 SOURCE 리셋트 처리가 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT34, 배면좌위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;조교 대상이 처녀
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:처녀
		PRINTFORMW 「이봐요, 몸을 맡겨. 그래, 그것으로 좋아요……」
		PRINTFORMW 「걱정하지 않고……, 나에게 맡겨 줘」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 동정
	;=================================================
	IF 구상기입체크(100) && TALENT:동정
		PRINTFORMW 「나의 처음은 이 모습으로 해요」
		PRINTFORMW 「그러면, 아무쪼록 부탁해요」
		RETURN 1
	ENDIF
	
	;=================================================
	;전회질내사정
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 && TFLAG:전턴성교
		;범용 처리
		CALL PRINTTEXT_CATEGORY_3_ACT_1_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 1:연속(대면좌위로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		;-------------------------------------------------
		;조교자가 갈 것 같다
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
			;2/3의 확률로 표시(RANDACT로 좋은 것 같은 생각도 든다)
			IF RAND:3
				PRINTFORMW 「젖가슴 만지작거릴 때에 안이 쿨쿨 해 사랑스럽네요…」
				PRINTFORMW 「응읏! 이래서야 나……, 이제(벌써) 가 버릴 것 같다」
			;1/3의 확률로 범용 처리
			ELSE
				CALL PRINTTEXT_CATEGORY_3_ACT_2_K14
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;조교자가 정욕
		;-------------------------------------------------
		IF 구상기입체크(100) && T_COND("정욕")
			;범용 처리
			CALL PRINTTEXT_CATEGORY_3_ACT_3_K14
			RETURN 1
		ENDIF
		
		;---------------------
		;그 외
		;---------------------
		PRINTFORMW 「연결된 채로, 가득 만지작거려 주어요」
		PRINTFORMW 「이봐요, 귀도 귀여워해 준다……」
		;페이드 아웃
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), KOJO_RAND_K14(("키스"), 1))
		CLEARLINE 1
		PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%를 배후로부터 관철한 채로, 전신을 애무하고 있다…
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 2:체위 변경(Ｖ성교로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		PRINTFORMW 「자, 몸을 맡겨. 뒤로부터 시라고 주기 때문에」
		PRINTFORM 「%CNAME(MASTER, TARGET)%의 키모치이이 곳, 전부 만지작거려 주어요
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 3:체위 변경(Ａ성교로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 3
		PRINTFORM 「이번은 이 몸의 자세야. 이렇게 말해도……, 넣기 전이지만, 말야
		CALL PRINT_SYMBOL_K14
		PRINTFORMW 「이봐요, 유두도 집어 주어요…」
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 4/파생 5:Ｖ성교 봉사/Ａ성교 봉사로부터
	;=================================================
	IF 구상기입체크(100) && GROUPMATCH(TFLAG:ACT파생, 4, 5) > 0
		;조교자가 초조하고 있다
		IF TENSION(TARGET, 7) == "높다"
			;범용 처리
			CALL PRINTTEXT_CATEGORY_3_ACT_4_K14
		;그 외
		ELSE
			SELECTCASE RANDACT(2, 0)
				CASE 0
					PRINTFORMW 「%CNAME(MASTER, TARGET)%보고 있으면……, 나부터 시 싶어져 버렸어요. 후후」
					PRINTFORM 「자, 뒤로부터 껴안으면서 찔러 준다
					CALL PRINT_SYMBOL_K14
				CASE 1
					PRINTFORMW 「이번은 나부터 시라고 준다……」
					PRINTFORMW 「자, 여기에 몸을 맡겨. 그래, 좋아요…… 후후」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;페니스밴드 사용시
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(!PENIS(TARGET), TEQUIP:조교자페니스밴드) > 0
		PRINTFORMW 「이런 완구로도%CNAME(MASTER, TARGET)%안을 감지하는 일은 할 수 있어요」
		PRINTFORM 「뒤로부터 껴안으면서 귀여워해 준다……
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE RANDACT(2, 1)
		CASE 0
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 머리카락, 좋은 향기군요……」
			PRINTFORMW 「후후, 기분 좋은 곳전부 만지작거려 주어요…」
		CASE 1
			PRINTFORMW 「뒤로부터 껴안으면서 시라고 주어요」
			PRINTFORMW 「그래요, 그대로……, 나에게 등을 맡겨」
	ENDSELECT
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT35, 애널섹스
;────────────────────────────────────
;·조교 대상의 성별로 관련되지 않고 조교자의 후타나리화, 페니스밴드장착에 의해 발생한다
;·애널섹스·조교자가 조교 대상의Ａ에 삽입한다
;·주도권은 TARGET(SEX_INITIATIVE(TARGET))
;·조교 대상의 성별로 관련되지 않고 조교자의 후타나리화, 페니스밴드장착에 의해 발생한다
;·TEQUIP(삼각목마/페니스밴드/바이브/관장기＋플러그) 장착중에는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·후배위, 대면좌위로부터는 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·조교 대상의Ａ경험이 2 미만 한편, 기교 2 미만이라면 발생하지 않는다
;·TFLAG:ACT파생는 전턴으로부터 상태, 및 체위의 참조에 충당된다
;-------------------------------------------------
@KOJO_ACT_K14_35
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속 실행/3=Ｖ성교로부터/4=Ｖ성교 봉사로부터/5=Ａ성교 봉사로부터)
;·애널섹스의 경우 TFLAG:ACT파생 == 2는 정의되어 있지 않다
;-------------------------------------------------
;·애널섹스로 동정을 버린 취급은 되지 않는다
;·TEQUIP(애널바이브/애널비즈)는 ACTION_APPLY로 해제된다
;·REACT분류가날뛴다였던 경우는 캔슬된다
;·v1. 010 현재, 거절했을 경우의 SOURCE 리셋트 처리가 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT35, 애널섹스
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;TALENT:작은체형은 꼬마님 취급으로 할 방침
	;어수선한 것이 싫어서 남자와 그 이외에서는 소분기를 나누면서 FIRSTACTMESSAGE와 FIRSTACT를 공용한다
	;=================================================
	;첫회 한편 조교 대상이 남자
	;=================================================
	IF 구상기입체크(100) && (TALENT:MASTER:남자 && FLAG:지문제어 == 2 && FIRSTACT(0))
		;조교 대상이 작은체형 체형
		IF TALENT:MASTER:작은체형
			PRINTFORMW 「\@ TALENT:MASTER:동정 ? 이런 일 # 엉덩이 \@는 처음으로? …… 읏, (들)물을 것도 없네요. 후후」
			PRINTFORMW 「별로 걱정하지 않아도 괜찮아요. 혼내주자는 것이 아니니까」
			PRINTFORMW 「이렇게 말해도……, 너 같은 작은 아이에게는 자극이 너무 강한일지도이지만, 말야」
		;그 외
		ELSE
			PRINTFORMW 「여자에게 이런 일 되는 것, 남자로서는 최고로 비참하다고 생각하지만…」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 경우는 어떨, 까요? 후후」
		ENDIF
		;지문 날조
		PRINTFORMDL %CALLNAME:TARGET%는 그렇게 말하면%CALLNAME:MASTER%의 아누스에\@ TEQUIP:조교자페니스밴드 ? %DHARNESS_DESCRIPTION(TARGET)% # 자신의 페니스 \@를 주어,
		PRINTFORMDW 단번에 꿰뚫으려고 했다…
		;공행
		PRINTL
		;TALENT에 의해 나눈다
		IF TALENT:MASTER:작은체형
			PRINTFORMW 「이봐요, 좀 더 힘을 빼. 아프지 않도록 해 주기 때문에」
			PRINTFORMW 「자…, 여자 아이같이 울릴 수 있어 준다. 좋은 소리로 울어 줘……」
		ELSE
			PRINTFORMW 「이봐요, 좀 더 힘을 빼세요. 아픈 생각은 하고 싶지 않을 것입니다?」
			PRINTFORMW 「자…, 여자같이 울릴 수 있어 주기 때문에, 각오 해……」
		ENDIF
		RETURN 1
	ENDIF
	
	;첫회시는 본체의 지문을 죽여 FIRSTACTMESSAGE를 사용한다. 반환값은 2
	;KOJO_TRAIN_MESSAGE는 구상측에서 날조 하는 경우 반환값을 1
	;그 외의 경우는 반환값을 2로 통일해, 함수내에서 분기 시킬 방침
	;=================================================
	;첫회 한편 조교 대상이 남자 이외
	;=================================================
	IF 구상기입체크(100) && (!TALENT:MASTER:남자 && FLAG:지문제어 == 2 && FIRSTACT(0))
		;조교 대상이 작은체형 체형
		IF TALENT:MASTER:작은체형
			PRINTFORMW 「그렇게 말하면 엉덩이로 하는 것은 처음이군요? …… 읏, 적중전 지났을까. 후후」
			PRINTFORMW 「괜찮아, 무서워하는 일 없어요. 편하게 하고 있으면 자연스럽게 받아들일 수 있기 때문에」
			PRINTFORMW 「뭐, 그런 일 말해도……%CNAME(MASTER, TARGET)%같은 작은 아이에게는 무슨 위로도 안 될지도이지만, 말야」
		;그 외
		ELSE
			;조교 대상이 처녀
			IF TALENT:MASTER:처녀
				PRINTFORMW 「처음이 엉덩이는 조금 어떻게인 것은 생각은 들지만…」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%는 흥미진진이라는 곳이 아니야? 다를까? 후후」
			;그 외
			ELSE
				PRINTFORMW 「응, 그렇구나……\@ EXP:성교경험 < 1 ? %CNAME(MASTER, TARGET)%와의 처음이 # \@아무것도 엉덩이가 아니어도 괜찮은 생각은 들지만」
				PRINTFORMW 「왠지 지금… 후후. 그러한 기분인 것이군요\@ TENSION(TARGET, 8) == "높다" ? # \@. 뭐, 그러한 (뜻)이유이니까…… 얌전하게 단념해 줘」
			ENDIF
		ENDIF
		;지문 날조
		PRINTFORMDL %CALLNAME:TARGET%는 그렇게 말하면%CALLNAME:MASTER%의 아누스에\@ TEQUIP:조교자페니스밴드 ? %DHARNESS_DESCRIPTION(TARGET)% # 자신의 페니스 \@를 주어,
		PRINTFORMDW 단번에 꿰뚫으려고 했다…
		;공행
		PRINTL
		;TALENT에 의해 나눈다
		IF TALENT:MASTER:작은체형
			PRINTFORMW 「이봐요, 숨을 내쉬어 힘을 빼. 조금씩 펼쳐 가 주기 때문에」
			PRINTFORMW 「자…, 귀여워해 준다. 좋은 소리로 울어 줘……」
		ELSE
			PRINTFORMW 「이봐요, 긴장하지 않고 편하게 해. 굳어지지 않으면 자연스럽게 받아들일 수 있어요」
			PRINTFORMW 「자…, 암퇘지같이 울릴 수 있어 주기 때문에, 각오 해……」
		ENDIF
		RETURN 1
	ENDIF
	
	;실제문제로서 현상은 조교자가 성교로 사정한 뒤, 그대로 성교를 계속하는 일은 우선 없는 느낌
	;라고 해도 거동이 바뀔 가능성이나, 테두리를 만들어 두면 구상측에서 비틀어 구부리는 일도 할 수 있기 때문에 아무튼 할 수 밖에라는 곳
	;=================================================
	;전회질내사정
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 && TFLAG:전턴성교
		SELECTCASE TFLAG:전회질내사정
			;전회Ｖ질내사정
			CASE 1
				;전회 COM 파생
				SELECTCASE TRAINNAME:PREVCOM
					CASE "안에내！"
						PRINTFORMW 「그런데, 이번은 여기에 쏟아 줄까? 후후」
						PRINTFORMW 「이봐요, 가요…… 그렇게, 힘을 빼」
					CASE "안에내지마！"
						PRINTFORMW 「저기, 엉덩이의 경우에서도 안에 나오는 것은 싫어?」
						PRINTFORMW 「～응이라고, 지금 듣는 것은 촌스러울까요? 어차피 곧바로 아는 것…」
						;페이드 아웃
						CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「후후후……」")
						CLEARLINE 1
					CASEELSE
						PRINTFORMW 「이봐요, 얌전하게 해. 이번은 엉덩이에 쏟아 주기 때문에」
						PRINTFORM 「%CNAME(MASTER, TARGET)%가 그것을 바랄지 어떨지는 모르지만, 말야
						CALL PRINT_SYMBOL_K14
				ENDSELECT
			;전회 드라이사정
			CASE 3, 7
				PRINTFORMW 「과연 나도 최후 같지만……, 아직%CNAME(MASTER, TARGET)%안을 즐기는 일은 할 수 있을 것 같구나」
				PRINTFORMW 「자, 계속해요. 좋은 소리로 울어 줘……」
			;전회Ａ질내사정
			CASE 5
				;전회 COM 파생
				SELECTCASE TRAINNAME:PREVCOM
					CASE "안에내！"
						PRINTFORMW 「%CNAME(MASTER, TARGET)%의 엉덩이안, 미끈미끈 해 굉장한 일이 되어 있어요. 후후」
						PRINTFORMW 「이대로 질척질척 휘저어 주기 때문에……」
					CASE "안에내지마！"
						PRINTFORMW 「어째서 엉덩이 중(안)에서 나오는 것이 싫은 것인가 모르지만……」
						PRINTFORMW 「뭔가 말한 곳에서 쓸데없는 것은 슬슬 이해해도 되는 것이 아닐까?」
						PRINTFORMW 「뭐, 그런 (뜻)이유로……, 아직도 즐겁게 해 받아요. 나쁘지만, 말야」
					CASEELSE
						PRINTFORMW 「이봐요, 아직 계속해요. 지금의 나, 이런 정도는 만족할 수 없는거야」
						PRINTFORMW 「흘러넘쳐 버릴 정도로 쏟아 주기 때문에 각오 해……」
				ENDSELECT
		ENDSELECT
		RETURN 1
	ENDIF
	
	;ACT파생는 가능한 한 새긴다
	;사실은 TALENT등으로 좀 더 세세하게 나누고 싶지만, 수고를 생각하는 어떤 정도 타협 하지 않을 수 없다
	;아무튼 머지않아 여유가 생기고 오면라는 곳
	;=================================================
	;파생 1:연속(Ａ섹스로부터)
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 1
		;-------------------------------------------------
		;조교자가 갈 것 같다
		;-------------------------------------------------
		IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
			;2/3의 확률로 표시
			IF RAND:3
				PRINTFORMW 「…… 매우 힘들어서 기분 좋아요.%CNAME(MASTER, TARGET)%도 키모치이이겠지? … 라고」
				PRINTFORMW 「정직, 그런 일 말하고 있는 여유는, 이제 없네요…… 응읏!」
			;1/3의 확률로 범용 처리
			ELSE
				;범용 처리
				CALL PRINTTEXT_CATEGORY_3_ACT_2_K14
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;조교자가 정욕
		;-------------------------------------------------
		IF 구상기입체크(100) && T_COND("정욕")
			PRINTFORMW 「…… 근원까지 삼켜 버려 추잡해요. 이봐요, 어때? 엉덩이 키모치이이의?」
			PRINTFORMW 「뭐,%CNAME(MASTER, TARGET)%같은 마조돼지에는 잘 어울리는 모습이야. 망가져 버릴 정도로 찔러 주기 때문에, 마음껏 울어 줘」
			CALL FADEIN(10, "「돼지같이,……」")
			;만족도 50%이상으로 표시
			IF TENSION(TARGET, 8) == "높다"
				WAIT
				;페이드 아웃
				CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「킥킥」")
				CLEARLINE 1
			;TENSION로부터 빠지면 WAIT
			ELSE
				WAIT
			ENDIF
			RETURN 1
		ENDIF
		
		;---------------------
		;그 외
		;---------------------
		SELECTCASE RANDACT(2, 0)
			CASE 0
				PRINTFORMW 「이봐요, 알아?  %CNAME(MASTER, TARGET)%의 엉덩이안이\@ TEQUIP:조교자페니스밴드 ? 이 완구의 # 나의 \@형이 되어 있어…」
				PRINTFORM 「후후……, 자, 좀 더 좋은 소리로 울어 줘. 실금 해 버릴 정도로, 마구 찔러 주기 때문에
				CALL PRINT_SYMBOL_K14
			CASE 1
				PRINTFORMW 「후후,\@ TALENT:MASTER:남자 ? 녀 같은 # 사랑스럽다 \@소리 내 버려……」
				PRINTFORM 「그렇게 좋으면, 좀 더 격렬하게 시라고 주어요…
				CALL PRINT_SYMBOL_K14
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 3:Ｖ성교로부터
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 3
		PRINTFORMW 「이번은 엉덩이로 즐겁게 해 줘」
		PRINTFORMW 「후후…… 사랑스러운 작게 오므린 귀여운 입이군요. 자, 숨을 내쉬어 힘을 빼세요…」
		RETURN 1
	ENDIF
	
	;=================================================
	;파생 4/파생 5:Ｖ성교 봉사/Ａ성교 봉사로부터
	;=================================================
	IF 구상기입체크(100) && GROUPMATCH(TFLAG:ACT파생, 4, 5) > 0
		;조교자가 초조하고 있다
		IF TENSION(TARGET, 7) == "높다"
			;범용 처리
			CALL PRINTTEXT_CATEGORY_3_ACT_4_K14
		;그 외
		ELSE
			;No. 1
			SELECTCASE RANDACT(2, 1)
				CASE 0
					PRINTFORMW 「%WARD_R_K14()%…… 상당히 격렬하게 시라고 주었지 않아…」
					PRINTFORMW 「이봐요, 엉덩이를 여기에 향하세요. 충분히 답례 해 주어요. 후훗」
				CASE 1
					PRINTFORMW 「후후…… 공수 교체라는 곳일까」
					PRINTFORM 「자, 가요. 각오 해 줘…
					CALL PRINT_SYMBOL_K14
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;본체측의 지문의 기술에 맞추어 ABL로 3단계로 나눈다
	;-------------------------
	;그 외
	;-------------------------
	LOCALS = \@ TEQUIP:조교자페니스밴드 ? 이 완구 # 나의 형태 \@
	SELECTCASE ABL:MASTER:Ａ감각
		;A개발제
		CASE IS > 3
			PRINTFORM 「후후,
			SIF TEQUIP:조교자페니스밴드
				PRINTFORM 이런 유사품에서도
			PRINTFORMW %CNAME(MASTER, TARGET)%의 엉덩이가 녹진녹진이 되어 있는 것이 자주(잘) 알아요」
			PRINTFORMW 「시원스럽게 안쪽까지 삼켜 버려, 정말 추잡하다……」
			PRINTFORMW 「\@ TALENT:MASTER:남자 ? 남으로서는 한심한 한? 쿠스 # 아무튼에서도……, 꽤 사랑스러워요. 후후 \@」
			PRINTFORMW 「자, 가요.%CNAME(MASTER, TARGET)%의 엉덩이…, 엉망진창으로 해 준다」
			PRINTFORM 「형태가 바뀌어 버리는 위, 마구 찔러 주기 때문에…
			CALL PRINT_SYMBOL_K14
		;A개발 도중
		CASE IS > 1
			PRINTFORMW 「…… 대단히 순조롭게 들어가게 되었어요. 완전히%LOCALS%에 친숙해 진 것 같아 무엇보다네? 후후」
			PRINTFORMW 「이봐요, 숨을 내쉬어 편하게 해도…… 좋은 아이로 하고 있으면 귀여워해 준다」
			PRINTFORMW 「무엇보다……%CNAME(MASTER, TARGET)%는 괴롭힐 수 있는 것이 기쁜 것일까, 저기?」
			;페이드 아웃
			CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「킥킥」")
			CLEARLINE 1
		;A미개발
		CASEELSE
			PRINTFORMW 「…… 과연 아직 힘든 느낌이군요. 조금 괴로울까? 후후」
			PRINTFORMW 「그렇지만 괜찮아요.%CNAME(MASTER, TARGET)%의 엉덩이……, 조금씩%LOCALS%에 길들여 가 주어요」
	ENDSELECT
	RETURN 1
ENDIF

;*************************************************
;범용 함수 두는 곳
;*************************************************

;-------------------------------------------------
;전회질내사정
;Ａ섹스는 해당 ACT로 별개에 처리한다
;-------------------------------------------------
@PRINTTEXT_CATEGORY_3_ACT_1_K14
SELECTCASE TFLAG:전회질내사정
	;전회Ｖ질내사정
	CASE 1
		;전회 COM 파생
		SELECTCASE TRAINNAME:PREVCOM
			CASE "안에내！"
				PRINTFORMW 「%CNAME(MASTER, TARGET)%안, 미끈미끈 해 굉장히 추잡해요. 후후」
				PRINTFORMW 「이봐요, 아직 계속해요. 소망 대로, 충분히 쏟아 주기 때문에…」
			CASE "안에내지마！"
				PRINTFORMW 「%CNAME(MASTER, TARGET)%에는 아직 모르는 것일까?」
				PRINTFORMW 「나, 안에 내지 말고 뭐라고 말해지면 더욱 더 하고 싶어져 버려」
				PRINTFORMW 「뭐, 무슨 말을 하는 것도%CNAME(MASTER, TARGET)%의 제멋대로이지만, 그래서 이러니 저러니 될 것이 아니니까…」
			CASEELSE
				PRINTFORMW 「모처럼 질내에 낸 것이니까 분명하게 휘젓지 않으면, 는」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%도, 나에게 이렇게 되는 것을 바라고 있겠지요?」
				PRINTFORMW 「정직하게 말할 수 있으면, 좀 더 귀여워해 주어요…」
		ENDSELECT
	;전회 드라이사정
	CASE 3, 7
		PRINTFORMW 「과연 나도 최후 같지만……, 아직%CNAME(MASTER, TARGET)%안을 즐기는 일은 할 수 있을 것 같구나」
		PRINTFORMW 「자, 계속해요. 좋은 소리로 울어 줘……」
	;전회Ａ질내사정
	CASE 5
		PRINTFORMW 「이봐요, 이번은 여기에 쏟아 주어요. 엉덩이 뿐으로는 딱합니다 것이군요? 후후」
		PRINTFORMW 「자, 가요. 준비는 좋을까……」
ENDSELECT

;-------------------------------------------------
;조교자가 갈 것 같다
;해당소분기에서 TCVAR:갈것같다를 조건으로 하고 있으므로 함수에서는 생략한다
;-------------------------------------------------
@PRINTTEXT_CATEGORY_3_ACT_2_K14
;조교 대상도갈것같다
IF (IS_NOWACTNAME("정상위/후배위/기승위/대면좌위/배면좌위") && PALAM:MASTER:쾌Ｖ >= 8500) || (IS_NOWACTNAME("애널섹스") && PALAM:MASTER:쾌Ａ >= 8500) || TCVAR:MASTER:갈것같다
	PRINTFORMW 「%WARD_R_K14()%…… 나, 이제 한계이지만…,%CNAME(MASTER, TARGET)%도 같음 같구나」
	PRINTFORMW 「함께 갈 수 있으면 좋지만, 뒤는%CNAME(MASTER, TARGET)%나름일, 까요」
;쾌Ｖ혹은 쾌Ａ가 5000이상
ELSEIF (IS_NOWACTNAME("정상위/후배위/기승위/대면좌위/배면좌위") && PALAM:MASTER:쾌Ｖ >= 5000) || (IS_NOWACTNAME("애널섹스") && PALAM:MASTER:쾌Ａ >= 5000)
	PRINTFORMW 「%WARD_R_K14()%…… 슬슬, 한계, 군요…」
	PRINTFORMW 「나쁘지만 이대로가게해 받아요……」
;그 외
ELSE
	PRINTFORMW 「읏, 쿳! 나, 이제 한계같다…」
	PRINTFORMW 「저기, 안과 밖……, 어느 쪽을 소망일까? 후후」
ENDIF

;-------------------------------------------------
;조교자가 정욕Ａ섹스는 범용 처리하지 않는다
;-------------------------------------------------
@PRINTTEXT_CATEGORY_3_ACT_3_K14
;2/3의 확률로 표시
IF RAND:3
	PRINTFORM 「%RANDTEXT_K14("학, 하아", "후~, 하아", "하, 하", "하, 하")%…… 사랑스럽다
	CALL HEARTCB(COLOR2_K14("pink"), 1, "나눔고딕")
	PRINTFORMW   나, 멈추지 않게 되어 버린다%RANDTEXT_K14("원", "＿")%…」
	;페이드 아웃
	CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), KOJO_RAND_K14(("키스"), 1))
	CLEARLINE 1
	PRINTFORMD %CALLNAME:TARGET%는%CALLNAME:MASTER%
	;기승위는 연주한다
	IF IS_NOWACTNAME("정상위/후배위/대면좌위/배면좌위")
		PRINTFORMD 의 얼굴을
	ENDIF
	;정상위, 대면좌위
	IF IS_NOWACTNAME("정상위/대면좌위")
		PRINTFORMD 끌어 들여,
	;후배위, 배면좌위
	ELSEIF IS_NOWACTNAME("후배위/배면좌위")
		PRINTFORMD 뒤돌아 보게 해
	;기승위
	ELSEIF IS_NOWACTNAME("기승위")
		PRINTFORMD 를 껴안아,
	ENDIF
	;조교자 만족도 50%이상
	SIF TENSION(TARGET, 8) == "높다"
		PRINTFORMD 정열적인
	PRINTFORMDW 키스를 하면서 허리를 격렬하고\@ IS_NOWACTNAME("기승위") ? 밀어올렸다 # 강탈 세웠다 \@…
;1/3의 확률로 표시
ELSE
	PRINTFORMW 「저기, 키모치이이? 키모치이이원이군요?」
	PRINTFORMW 「읏! 후후……%CNAME(MASTER, TARGET)%, 매우 좋아요. 이봐요, 좀 더 소리를 듣게 해…」
ENDIF

;-------------------------------------------------
;Ｖ성교 봉사/Ａ성교 봉사로부터 한편 조교자가 초조하고 있다
;-------------------------------------------------
@PRINTTEXT_CATEGORY_3_ACT_4_K14
PRINTL
PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 소극적인 자세에 초조하고 있는 것 같다…
SELECTCASE AUTORAND(2, 130)
	CASE 0
		PRINTFORMW 「이제 안돼, 이런 건…… 안타까워서 하고 있을 수 없어요!」
		PRINTFORMW 「자, 각오는 좋을까? 내가 모범을 보여 주기 때문에……」
	CASE 1
		PRINTFORMW 「농담이 아니에요, 이런 건…… 이제(벌써), 정말로 안절부절 해요!」
		PRINTFORMW 「자 각오 해. 어떻게 하면 좋은 것인지, 그 신체에 직접 가르쳐 주기 때문에……」
ENDSELECT
IF IS_NOWACTNAME("애널섹스")
	CALL FADEIN(8, "「다만…… 엉덩이로,. 후후후…」")
	WAIT
ENDIF
