;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	앨리스 구상 콤비 네이션계 액션 구상 파일
;
;	콤비 네이션계 액션 구상을 정리한 파일
;
;Version		Reason
;1. 71			@구상 기입 체크 사양에. 그 외 섬세한 수정
;1. 68 Rel2		일부 개변
;1. 60			단체 파일로서 독립
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;콤비 네이션계 ACT300～312
;========================================================================
;·TFLAG:ACT(300=W자위/301=W페라/302=W키스/303=W가랑이/304=W파이즈리/305=W풋잡
;·306=승마두구멍공격/307=대면두구멍공격/308=변칙혼란모란/309=애널두구멍공격/310=W역강간/311=골짜기를건너감/312=2개빨고강제)
;·조수가 존재하는 경우,50%의 확률로 콤비 네이션에 파생한다
;·TFLAG:ACT가 GET_COMBIACTNUM(TFLAG:ACT)로 덧쓰기되어 함수내에서 ACT 번호가 정의된다
;·콤비 네이션시의 ASSI는 반드시(ASSI:1)된다
;·ACT_ABLE는 기본적으로 파생 바탕으로 준거
;-------------------------------------------------
;좀처럼 손길이 닿지 않는 개소. 초기의 무렵에 써 그대로
;지금은 사용하지 않게 된 조건이라든지 비교적 있구나w
;-------------------------------------------------
;─
;ACT300, W자위
;─
;·조교자와 조수의 2명이 조교 대상으로 손 애무 한다
;·ACT10, 손으로애무로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_300
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT300, W자위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;절정으로 이끌리고
	;=================================================
	;IF 구상 기입 체크(0) && TCVAR:MASTER:초조하게해번 == 1
	;ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 반응을 물으면서 잡은 육봉을 상하시키고
	;절정 직전에 애태우고 있다…
	;=================================================
	;초조하게 해 안
	;=================================================
	;IF 구상 기입 체크(0) && TCVAR:MASTER:초조하게해번 > 1
	;ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 「저기, 우리들의 손으로 희롱해져 어떤 기분일까…?」 
		PRINTFORMW 「후후… 아무것도 말할 수도 없을 정도(수록), 기분 좋아져 버리고 있는 것 같구나」
		RETURN 1
	ENDIF
	
	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		PRINTFORMW 「저기%CNAME(ASSI)%, 봐 줘」
		PRINTFORMW 「이 변태씨는 우리들에게 훑기를 원해서, 기다릴 수 없는 것 같아요?」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMW 「이봐요, 나와%CNAME(ASSI)%로 훑어 준다」
		PRINTFORM 「마지막 한 방울까지 짜내 주어요… 
	ELSE
		PRINTFORMW 「저기,%CNAME(ASSI)%도 도와 줘」
		PRINTFORM 「우리들의 손으로 이키 미치게 해 줍시다…
	ENDIF
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;─
;ACT301, W페라
;─
;·조교자와 조수의 2명이 조교 대상으로 펠라치오를 실시한다
;·ACT12, 입으로애무로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_301
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT301, W페라
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 귀두를 사이에 두어 혀를 얽히게 해라
	;사정을 재촉했다…
	;=================================================
	;절정으로 이끌리고
	;=================================================
	;IF 구상 기입 체크(0) && TCVAR:MASTER:초조하게해번 == 1
	;ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 %CALLNAME:MASTER%의 귀두를 중심으로 졸졸 혀를 기 조생
	;절정 직전에 애태우고 있다…
	;=================================================
	;초조하게 해 안
	;=================================================
	;IF 구상 기입 체크(0) && TCVAR:MASTER:초조하게해번 > 1
	;ENDIF
	
	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		PRINTFORMW 「아라, 그렇게 해 버려… 물었으면 좋은거야?」 
		PRINTFORMW 「%CNAME(MASTER, TARGET)%는 누구에게 시라고 받고 싶은 것일까」
		PRINTFORMW 「나?  %CNAME((ASSI:1), TARGET)%? 그렇지 않으면……」
		PRINTFORM 「두 명 동시가 소망일까…
		CALL PRINT_SYMBOL_K14 
		RETURN 1
	ENDIF
	
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 서로 경쟁하도록(듯이) %CALLNAME:MASTER%의 남성기에 혀를 기게 했다…
	;-------------------------
	;그 외
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMW 「나와%CNAME(ASSI)%로 빨아 준다♪」 
		PRINTFORMW 「사양하지 않고 좋아할 뿐(만큼) 가 버려도 좋아요?」 
	ELSE
		PRINTFORMW 「자,%CNAME(ASSI)%와 둘이서 뽑아 주어요」 
		PRINTFORMW 「우리들의 혀 기술을 즐겨 줘♪」 
	ENDIF
	RETURN 1
ENDIF

;─
;ACT302, W키스
;─
;·조교자와 조수의 2명이 조교 대상으로 키스를 실시한다
;·ACT15, 키스로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_302
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT302, W키스
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;조교 대상 삽입중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「좀, 언제까지%CNAME(ASSI)%와 키스 하고 있는거야!」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%는 지금 나와 시 비치기 때문에, 조금은 생각하세요!」
				PRINTFORMW 「저기%CNAME(ASSI)%, 당신도 일부러 하고 있는 것이 아닌거야?」
			CASEELSE
				PRINTFORMW 「읏…… 츄웃…… 이봐요, 좀 더 추잡하게 혀를 걸어…」
				PRINTFORMW (%CNAME(ASSI)%에 과시해 주어요. 후후…)
				PRINTFORML 「아라,%CNAME(ASSI)%. 싫증시켜 버렸을까…?」
				PRINTFORMW 「나쁘지만, 조금 기다리고 있어 줘. 우후후…」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;위로돌리고
	;=================================================
	IF 구상기입체크(100) && TFLAG:위로돌리고
		PRINTFORMW 「%KOJO_RAND_K14(("키스"), 1)%」
		CALL PRINT_KOJO_COLORW(ASSI, "『츗… 응츗… 츄웃…』", TARGET)
		PRINTFORMW 「즐겁네요♪」
		CALL PRINT_KOJO_COLORW(ASSI, "『에에, 정말로♪』", TARGET)
		PRINTL
		PRINTFORMW 「우후후…」
		CALL PRINT_KOJO_COLORW(ASSI, "『우후후…』", TARGET)
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMW 「자, 나와%CNAME(ASSI)%로 키스 해 준다♪」 
		PRINTFORMW 「모두에게 사랑 받아%CNAME(MASTER, TARGET)%행운아네? 후훗…」
	ELSE
		PRINTFORMW 「저기,%CNAME(ASSI)%도 키스 해 주어?」
		PRINTFORM 「우리들의 키스로 녹여 줍시다…
		CALL PRINT_SYMBOL_K14 
	ENDIF
	RETURN 1
ENDIF

[SKIPSTART]
;─
;ACT303, W가랑이
;─
;·조교자와 조수의 2명이 조교 대상으로 가랑이를 실시한다
;·조교 대상으로 페니스가 있는 것이 전제
;·ACT16, 성기를서로켠다로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_303
;-------------------------------------------------
;·지문은 조교 대상이 위로돌리고지 아닌지로 분기.
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT303, W가랑이
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0)
	;%CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 음순으로 %CALLNAME:MASTER%의 남성기를 끼워 넣어 켜기 시작했다…
	;=================================================
	;조교 대상이 위로돌리고
	;=================================================
	;IF 구상 기입 체크(0) && TFLAG:위로돌리고
	;ENDIF
ENDIF
[SKIPEND]

;─
;ACT304, W파이즈리
;─
;·조교자와 조수의 2명이 조교 대상으로 파이즈리를 실시한다
;·조교 대상으로 페니스가 있는 것이 전제
;·ACT17, 파이즈리한다로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_304
;-------------------------------------------------
;·지문은 초조하게해번에 의해 분기. 각 분기내에서 조교자＆조수의 가슴의 사이즈에 의한 파생
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT304, W파이즈리
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생원의 파이즈리 자체가 발생하지 않기 때문에 사실상의 죽음에 구상이 되어 있다고 한다
;앨리스씨손과 다리 이외를 사용하는 것은 싫어하기 때문에w
;------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;위로돌리고
		IF TFLAG:위로돌리고
			;갈 것 같은 것을 참고 있다		지금은 그다지 사용하지 않게 된 조건
			IF TCVAR:MASTER:갈것같다 > 5
				PRINTFORMW 「의외로 노력해요? 그렇지만 쓸데없는 발버둥질이지만♪」
				PRINTFORMW 「저기%CNAME(ASSI, TARGET)%. 좀 더 격렬하게 시라고 줘」
				PRINTFORMW 「우리들 상대에게 인내는, 멋없는 변태에 깨닫게 해 주어요♪」 
			;갈 것 같다
			ELSE
				PRINTFORMW 「슬슬 한계같구나?」 
				PRINTFORMW 「저기,%CNAME(MASTER, TARGET)%는 어느 쪽의 가슴을 더럽히고 싶은 것일까?」
				PRINTFORMW 「나? 그렇지 않으면%CNAME(ASSI, TARGET)%?」
				PRINTFORMW 「뭐 좋아요. 우선 편하게 시켜 준다♪」
			ENDIF
		;통상
		ELSE
			;갈 것 같은 것을 참고 있다
			IF TCVAR:MASTER:갈것같다 > 5
				PRINTFORMW 「필사적으로 참고 있는 곳 나쁘지만…」
				PRINTFORMW 「우리들은%CNAME(MASTER, TARGET)%가 가는 곳이 빨리 보고 싶은거야」
				PRINTFORMW 「그러면%CNAME(ASSI, TARGET)%, 빨리 짜내 주어요♪」
			;갈 것 같다
			ELSE
				PRINTFORMW 「두 명에게 탓해져, 이제 인내 할 수 없는 것 같구나?」 
				PRINTFORMW 「저기%CNAME(ASSI, TARGET)%. 어떻게 해?」
				PRINTFORMW 「이대로 가게 하는지, 그렇지 않으면 초조하게 해 줄까…」
				CALL PRINT_KOJO_COLORW(ASSI, "『우후후…』", TARGET)
				PRINTFORMW 「우후후…」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 「이봐요, 좀 더 허리를 흔드세요!」  
		PRINTFORMW 「%CNAME(ASSI, TARGET)%도 응석부리게 하면 안 돼요? 우후후…」
		RETURN 1
	ENDIF
	
	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		PRINTFORMW 「아라아라, 그렇게 세워 버려…」
		PRINTFORMW 「그렇게 우리들의 젖가슴을 범하고 싶은 것일까?」
		PRINTFORMW 「저기%CNAME(ASSI, TARGET)%, 어떻게 해? 이것」
		CALL PRINT_KOJO_COLORW(ASSI, "『우후후…』", TARGET)
		PRINTFORMW 「우후후…」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMW 「나와%CNAME(ASSI)%의 가슴으로 샌드위치 해 주어요」 
		PRINTFORMW 「%CNAME(MASTER, TARGET)%는 어느 쪽의 가슴이 마음에 들까요? 우훗…」
	ELSE
		PRINTFORMW 「저기, 나와%CNAME(ASSI)%와 어느 쪽의 가슴으로 사이에 두었으면 좋아?」
		PRINTFORMW 「아라, 양쪽 모두 한 번이 좋은거야…? 그것도 나쁘지 않네요♪」 
	ENDIF
	RETURN 1
ENDIF

;─
;ACT305, W풋잡
;─
;·조교자와 조수의 2명이 조교 대상으로 풋잡한다
;·조교 대상으로 페니스가 있는 것이 전제
;·ACT18, 풋잡한다로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_305
;-------------------------------------------------
;·지문에 분기 없음. 내용은 이하와 같다
;  %CALLNAME:TARGET%과 %CALLNAME:(ASSI:1)%는 협력해 %CALLNAME:MASTER%의 남성기를 다리로 훑어 냈다…
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT305, W풋잡
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;위로돌리고
		IF TFLAG:위로돌리고
			;갈 것 같은 것을 참고 있다
			IF TCVAR:MASTER:갈것같다 > 5
				SELECTCASE AUTORAND(2, 650)
					CASE 0
						PRINTFORMW 「아라아라, 노력해요. 참아도 어차피 곧 가 버리는 주제에」
						PRINTFORMW 「저기, 아직 내고 싶지 않은거야? 우리들의 다리를 정액으로 질척질척하고 싶지 않은거야?」 
						PRINTFORMW 「후후, 좋아요. 자%CNAME(ASSI)%, 슬슬 이 변태로 결정타를 찔러요♪」
					CASE 1
						PRINTFORMW 「의외로 달라붙어요? 이제(벌써) 쿠퍼액으로 질척질척이 되어 있는데」
						PRINTFORMW 「저기%CNAME(ASSI)%. 좀 더 강하게 시라고 줘」
						PRINTFORMW 「그 쪽이 이 변태는 기뻐하는거야♪우훗…」
				ENDSELECT
			;갈 것 같다
			ELSE
				SELECTCASE AUTORAND(2, 651)
					CASE 0
						PRINTFORMW 「%CNAME(MASTER, TARGET)%, 자신의 허리가 움직이고 있는데 깨닫고 있을까?」 
						PRINTFORML 「저기%CNAME(ASSI)%, 다리 움직이는 것 멈추어. 이 변태에 스스로 시키는거야」
						PRINTFORMW 「이봐요, 스스로 허리 흔드세요! 우리들의 다리와 섹스 하고 있을 생각으로 하세요」
					CASE 1
						PRINTFORMW 「저기, 어쩐지 평소보다 딱딱한 것 같은 생각이 들지만?」
						PRINTFORMW 「설마%CNAME(ASSI)%의 다리가 좋다든가 말하는 것이 아니겠지요…」
						PRINTFORMW 「이봐요, 어때? 분명하게 말하지 않으면가게해 주지 않아요?」
				ENDSELECT
			ENDIF
		;통상
		ELSE
			;갈 것 같은 것을 참고 있다
			IF TCVAR:MASTER:갈것같다 > 5
				SELECTCASE AUTORAND(2, 652)
					CASE 0
						PRINTFORMW 「참고 있는 것을 보면 더욱 더 괴롭히고 싶어져요…」
						PRINTFORMW 「저기%CNAME(ASSI)%. 조금 격렬하게 해 줍시다♪」
					CASE 1
						PRINTFORMW 「저기,%CNAME(ASSI)%, 자주(잘) 보고 있어. 이렇게 하는 곳의 변태는 기뻐해」
						PRINTFORMW 「이봐요, 빅쿤빅쿤 해 이제(벌써) 폭발할 것 같겠지?」
						PRINTFORMW 「자, 당신도 똑같이 해 봐. 슬슬 결정타를 찔러요♪」
				ENDSELECT
			;갈 것 같다
			ELSE
				SELECTCASE AUTORAND(2, 653)
					CASE 0
						PRINTFORMW 「저기, 나의 다리와%CNAME(ASSI)%의 다리, 어느 쪽이 기분이 좋아?」
						PRINTFORMW 「정직하게 말하자 마자가게해 주어도 괜찮아요…?」
						PRINTFORMW 「대답 하기에 따라 벌이 필요하게 될지도지만!」
					CASE 1
						PRINTFORMW 「조금 전부터 쭉%CNAME(ASSI)%의 사타구니만 보지 않아?」
						PRINTFORMW 「저기, (듣)묻고 있는 거야? 이봐요, 여기 향하세요 이 변태!」
				ENDSELECT
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;밀어넘어뜨리기 상태
	;=================================================
	IF 구상기입체크(100) && TEQUIP:밀어넘어뜨리기중
		;연속 실행
		IF TFLAG:ACT == TFLAG:전ACT
			PRINTFORMW 「밀어 넘어뜨려져 우리들에게 장난감으로 되어, 더욱 더 흥분하고 있는 것 같구나?」
			PRINTFORMW 「후후… 이것은 간단하게가게해는 재미있지 않을까」
			PRINTFORMW 「저기%CNAME(ASSI)%. 차분히 희롱해 줍시다♪」
		;통상
		ELSE
			PRINTFORMW 「우훗, 움직일 수 있는 없앤 곳에서…」
			PRINTFORMW 「나와%CNAME(ASSI)%로 철저하게 짜내 주어요♪」 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		SELECTCASE AUTORAND(3, 654)
			CASE 0
				PRINTFORMW 「여자 두 명에게 발길질로 되어 흥분해 버리고 있는 거야?」
				PRINTFORMW 「완전히 질린 변태군요!」
				PRINTFORMW 「저기%CNAME(ASSI)%. 이 변태씨는 당신의 다리도 마음에 든 것 같아요?」
				PRINTFORMW 「우후후…」
				CALL PRINT_KOJO_COLORW(ASSI, "『킥킥…』", TARGET)
			CASE 1
				PRINTFORMW 「그래요, 풋잡 정말 좋아하는 변태에는 견딜 수 없네요? 이것」
				PRINTFORMW 「아라아라, 솔직하게 수긍해 버려. 이제(벌써), 어쩔 수 없네요♪」
				PRINTFORMW 「이봐요, 나와%CNAME(ASSI)%의 다리로 좀 더 즐겁게 해 주어요. 우후후…」
			CASE 2
				PRINTFORMW 「아라아라, 두 명에게 훑어져 매우 행복한 것 같구나?」
				PRINTFORMW 「이봐요,%CNAME(ASSI)%도 그 한심한 얼굴을 잘 봐줘♪」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		PRINTFORMW 「저기%CNAME(ASSI)%, 저것을 봐」 
		PRINTFORMW 「우리들의 다리로 훑기를 원해서 기다릴 수 없는 것 같아요?」
		PRINTFORMW 「우후후…」
		CALL PRINT_KOJO_COLORW(ASSI, "『우후후…』", TARGET)
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE AUTORAND(2, 659)
		CASE 0
			PRINTFORMW 「자, 나와%CNAME(ASSI)%의 다리로 훑어 주어요」
			PRINTFORMW 「풋잡 너무 좋아 변태의%CNAME(MASTER, TARGET)%에는 견딜 수 없지요? 우후후…」
		CASE 1
			PRINTFORMW 「저기,%CNAME(ASSI)%도 도와 줘」
			PRINTFORMW 「이 변태를 우리들의 다리로 승천 시켜 주는거야」
			PRINTFORMW 「자, 각오 해. 나와%CNAME(ASSI)%로 한계까지 짜내 주기 때문에♪」 
	ENDSELECT 
	RETURN 1
ENDIF

[SKIPSTART]
in Mothball v1. 71
;─
;ACT306, 승마 두 구멍 고문
;─
;·승마위중의 조교 대상으로 조수가 A삽입한다
;·ACT32, 승마위로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_306
;-------------------------------------------------
;ACT파생에의 대입은 TEQUIP를 참조해 행해지고 있다
;예를 들면 ACT파생 1등의 경우, 전회 ACT가 승마위라고는 할 수 없기 때문에 주의
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT306, 승마 두 구멍 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0)
	;=================================================
	;연속(승마위로부터)
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
	ENDIF
	;=================================================
	;체위 변경(V성교계로부터)			페니스밴드에서의 A성교중의 경우에게만 파생한다
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
	ENDIF
	;=================================================
	;A성교로부터						페니스밴드에서의 A성교중의 경우에게만 파생한다
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 3
	ENDIF
	;=================================================
	;V성교 봉사계로부터					0203판 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 4
	ENDIF
	;=================================================
	;A성교 봉사로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 5
	ENDIF
	;그 외·조건 없음
ENDIF

;─
;ACT307, 대면 두 구멍 고문
;─
;·조교자가 조교 대상의 V에 삽입한 채로, 조수가 조교 대상의 A에 삽입한다
;·ACT32, 대면좌위로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_307
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT307, 대면 두 구멍 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0)
	;=================================================
	;연속(대면좌위로부터)
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
	ENDIF
	;=================================================
	;체위 변경(V성교계로부터)
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
	ENDIF
	;=================================================
	;A성교로부터						페니스밴드에서의 A성교중의 경우에게만 파생한다
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 3
	ENDIF
	;=================================================
	;V성교 봉사계로부터				v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 4
	ENDIF
	;=================================================
	;A성교 봉사로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 5
	ENDIF
	;그 외·조건 없음
ENDIF

;─
;ACT308, 변칙혼란모란
;─
;·조교 대상의 V에 배면좌위로 삽입하면서, 조수에 결합부의 상황을 실황시킨다
;·ACT33, 배면좌위로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_308
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT308, 변칙혼란모란
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0)
	;=================================================
	;연속(배면좌위로부터)
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
	ENDIF
	;=================================================
	;체위 변경(V성교계로부터)
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
	ENDIF
	;=================================================
	;A성교로부터						페니스밴드에서의 A성교중의 경우에게만 파생한다
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 3
	ENDIF
	;=================================================
	;V성교 봉사계로부터				v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 4
	ENDIF
	;=================================================
	;A성교 봉사로부터					v1. 010 현재, ACT_ABLE로 튕겨지고 있다
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT파생 == 5
	ENDIF
	;그 외·조건 없음
ENDIF

;─
;ACT309, 애널 두 구멍 고문
;─
;·조교자가 조교 대상의 A에 삽입한 채로, 조수가 조교 대상의 V에 삽입한다
;·조교 대상이 남자의 경우는 발생하지 않는다
;·ACT33, 애널섹스로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_309
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT309, 애널 두 구멍 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0)
ENDIF
[SKIPEND]

;─
;ACT310, W역강간
;─
;·조교자와 조수로, 조교 대상의 페니스를 교대로 삽입하고 즐긴다
;·ACT103, 역강간으로부터의 파생
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=V성교 봉사계로부터/3=A성교계로부터/4=V성교계로부터/5=A성교 봉사계로부터)
;·v1. 010 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
@KOJO_ACT_K14_310
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT310, W역강간
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;연속(역강간으로부터)
	;=================================================
	;IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
	;ENDIF
	;=================================================
	;체위 변경(V성교 봉사로부터)
	;=================================================
	;IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
	;ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE AUTORAND(2, 670)
		CASE 0
			PRINTFORMW 「자,%CNAME(ASSI)%도 계(오)세요」 
			PRINTFORMW 「둘이서 범해 주는거야♪우훗…」 
		CASE 1
			PRINTFORMW 「이봐요, 나와%CNAME(ASSI)%로 범해 주어요」 
			PRINTFORM 「둘이서 한계까지 짜내 주기 때문에…
			CALL PRINT_SYMBOL_K14
	ENDSELECT
	RETURN 1
ENDIF

;─
;ACT311, 골짜기를건너감
;─
;·3 P의 일종
;·조교자와 조수가 상하과 겹쳐져, 조교 대상이 두 명의 바기나를 교대로 범한다
;·ACT95, 정상위시킨다로부터의 파생
;-------------------------------------------------
;·TFLAG:ACT파생(0=통상/1=연속/2=V성교 봉사계로부터/3=A성교계로부터/4=V성교계로부터/5=A성교 봉사계로부터)
;·0203판 현재, ACT파생 3/4/5는 ACT_ABLE로 규제되고 있으므로 기능하지 않는다
;-------------------------------------------------
@KOJO_ACT_K14_311
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT311, 골짜기를건너감
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;뽑지않고에 속행
	;=================================================
	IF 구상기입체크(100) && TFLAG:뽑지않고
		CALL PRINT_KOJO_COLORW(ASSI, "『아읏…… 앗… 안됏! 그것, 안돼엣…!』", TARGET)
		PRINTFORMW 「%CNAME(ASSI)%의 소리… 는 추잡한거야……」
		PRINTFORMW 「저기, 조금! 언제까지 둘이서 하고 있는거야!」
		PRINTFORMW 「나도, 조금 전부터 쭉… 아…! … 아읏… 그런… 갑자기 되면……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF TCVAR:MASTER:갈것같다
		SELECTCASE TCVAR:MASTER:갈것같다
			;조교 대상이 갈 것 같은 것을 참고 있다
			CASE IS > 5
				PRINTFORMW 「이봐요, 좀 더 허리 흔들어 주지 않으면,%CNAME(ASSI)%가 불쌍해요?」
				PRINTFORMW 「아라, 갈 것 같아? 라고 말하는 일인것 같아요,%CNAME(ASSI)%」
				PRINTFORMW 「마음껏 단단히 조여 짜내 주며♪」
			;조교 대상이 갈 것 같다
			CASE IS > 0
				PRINTFORM 「아… 아읏… 격렬하닷, 야, 응아앗…
				CALL PRINT_SYMBOL2_K14
				PRINTFORMW 「갈것같다… 인 거네? 저기,%CNAME(MASTER, TARGET)%는 어느 쪽으로 따라 줄까?」
			;통상
			CASEELSE
				PRINTFORMW 「읏… 앗…! … 좋은 곳에서 뽑아지면 괴로워요, 이것…」
				PRINTFORMW 「그렇지만…… 앙앙 울고 있다%CNAME(ASSI)%도 사랑스러워서……」
				PRINTFORMW 「이봐요%CNAME(ASSI)%, 키스 합시다. 츄웃… 응……」
		ENDSELECT
	ELSE
		IF AFFECTION("상사상애")
			PRINTFORMW 「자, 좋아요. 나와%CNAME(ASSI)%를 한 번에 범해 줘」
			PRINTFORMW 「그렇지만, 처음은 나부터가 아니면 안 되는 것이니까!」
			PRINTFORMW 「%CNAME(ASSI)%에는 나쁘지만, 조금 기다려 받아요. 우훗…」
		ELSE
			PRINTFORMW 「이봐요, 좋아하는 (분)편으로부터 찔러도 좋아요…」 
			PRINTFORMW 「%CNAME(MASTER, TARGET)%는 어느 쪽으로부터 범해 줄까? 우후후…」 
		ENDIF
	ENDIF
	RETURN 1
ENDIF

[SKIPSTART]
;─
;ACT312, 2개빨고강제
;─
;·조교자와 조수의 페니스에 조교 대상이 봉사하는 플레이
;·이 단계에서는 명령만
;·ACT51, 입으로애무를강제로부터의 파생
;-------------------------------------------------
@KOJO_ACT_K14_312
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT312, 2개빨고강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0)
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상 기입 체크(0) && TCVAR:TARGET:갈것같다
	ENDIF
	;=================================================
	;연속 실행
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:ACT == TFLAG:전ACT
	ENDIF
	;그 외·조건 없음
ENDIF
[SKIPEND]