;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	앨리스 구상 회화계 액션 구상 파일
;
;	회화계 액션 구상을 정리한 파일
;
;Version		Reason
;1. 71			@구상 기입 체크 사양에. ACT1/2/9에 가필. 그 외 섬세한 수정
;1. 62			일부 조건의 재검토. 오자, 탈자 수정. 가필등
;1. 60			ACT분류단체 파일로서 독립
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 ACT0～9
;========================================================================
;·TFLAG:ACT(0=지금의기분을얘기한다/1=자신에게로의기분을얘기한다/2=성에대한이야기를한다/3=상냥하게위로한다/4=엄격하게위협한다/5=의상의변경을요구한다/6=합의를요구한다/7=벌이라고선언한다/8=고함친다/9=기분나쁘게웃는다)
;·TFLAG:ACT분류 == 0에 해당
;·ACT9, 기분나쁘게웃는다는 이상계 ACT분류로부터 파생하는 경우가 있다
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------
;────────────────────────────────────
;ACT0, 지금의기분을얘기한다
;────────────────────────────────────
;·지금의 기분에 대해 들어 온다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K14_0
;-------------------------------------------------
;APPLY2로 대입을 하는 것이 ACT파생라고 하는 것도 뭔가감
;머지않아 정리되어야 할 종류의 대용품
;-------------------------------------------------
;회화계 ACT와 저것 하지 않으면이다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && FIRSTACT(0)
	PRINTFORMW 「아라, 기분 나쁜 것 같구나」
	PRINTFORMW 「응… 당연? 뭐, 그래요. 후후후……」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;그 조교의 최초의 액션
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:경과시간 == 1
	IF AFFECTION("상사상애")
		PRINTFORMW 「저기, 기분은 어때? 라고 말할까…」
		PRINTFORMW 「내가 말을 걸고 있기 때문에, 좀 더 기쁜 듯한 얼굴 하세요!」
	ELSE
		PRINTFORMW 「정직,%CNAME(MASTER, TARGET)%의 지금의 기분이라든지에 흥미없는거야%RAND_SPLIT("군요/…")%」
		PRINTFORMW 「나의 하고 싶은대로 하도록 해 받아요. 우후후…」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 자위중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GETBIT(TEQUIP:자위중, 0)
	LOCALS:0 = \@ GET_KFLAG_K14("상사상애") ? %RAND_SPLIT("후후후/후후/후후/킥킥")% # %RAND_SPLIT("후후후/후후/후훅")% \@
	SELECTCASE RANDACT(20)
		CASE 0
			PRINTFORMW 「어때? 지금의 기분은. 라고……」
			PRINTFORMW 「즐거움중에 이런 일 (듣)묻는 것은 멋없었을까. 저기?」
		CASE 1
			PRINTFORMW 「아라아라, 힘써요? 라고……」
			PRINTFORMW 「이 경우의 말의 의미,%CNAME(MASTER, TARGET)%는 제대로 이해할 수 있을까」
		CASE 2
			PRINTFORMW 「\@ GET_KFLAG_K14("상사상애") ? 후후 # 아라아라 \@, 즐거운 듯 해 무엇보다네\@ !GET_KFLAG_K14("상사상애") ??  # \@」
			PRINTFORMW 「뭐, 정직……, 지금 나에게 참견 나오는 것은 폐인 것이겠지만」
			PRINTFORMW 「그렇지만, 입다물고 보고 있는 것도 저것이고……, 무심코 만지작거리고 싶어져 버리는거네요」
	ENDSELECT
	CALL FADEOUT(10, @"「%LOCALS:0%……」")
	CALL FADE_CLINE(1)
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교 봉사계로 질내사정의 뒤
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
	PRINTFORMW 「뭐야, 완수한 것 같은 얼굴 해 버려…」
	PRINTFORMW 「그렇지만…, 후후. 나의 질내는 좋았던 것 같구나」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교계로 질내사정의 뒤
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
	PRINTFORMW 「%CNAME(MASTER, TARGET)%에 질내사정… 최고군요♪」
	PRINTFORMW 「저기, 나에게 정복 된 기분은 어떨까?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;갈 것 같다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
	;=================================================
	;갈 것 같은 것을 참고 있다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:인내
		PRINTFORMW 「아라, 노력해요?」
		PRINTFORM 「%CNAME(MASTER, TARGET)%를 언제까지 견딜 수 있는지, 시험해 보는 것도 나쁘지 않네요
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;애태움중
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:애태움수 > 1
		PRINTFORMW 「저기, 초조해 해지고 있는 기분은 어때?」
		PRINTFORMW 「간단하게는 가게 하지 않아요, 나쁘지만. 후후…」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「\@ GET_KFLAG_K14("상사상애") ? 후후 # 후후 \@, 갈 것 같은거네?」
	PRINTFORM 「그렇지만, 간단하게가게해는 재미있지 않네요
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;볼개그 장착중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TEQUIP:볼개그
	PRINTFORMW 「후후, 좋은 모습이군요. 지금의 기분은 어떨까?」
	PRINTFORMW 「이렇게 말해도, 지금의%CNAME(MASTER, TARGET)%에는 대답할 수 없겠지만 말이죠,♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;아이마스크 장착중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TEQUIP:아이마스크
	PRINTFORMW 「시야를 빼앗긴 기분은 어떨까? 후후…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;안면승마중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
	PRINTFORMW 「저기……, 나에게 이런 일 되고 있는 기분은 어떨까?」
	PRINTFORMW 「그렇지만 이 상태에서는 대답할 수 없을, 까요? 후후후…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;삼각목마 사용중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TEQUIP:삼각목마
	PRINTFORMW 「나라도 생각하는거야? 이런 상황으로 기분 같은거 (들)물어서는 안된다고」
	PRINTFORMW 「후후후…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;발기
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && PENIS(MASTER) && TCVAR:MASTER:발기도 >= 1000
	IF AFFECTION("상사상애")
		PRINTFORMW 「이제(벌써) 끙끙이 되어 있어요…」
		PRINTFORMDW 앨리스는%CALLNAME:MASTER%의 페니스를 손가락으로 쿡쿡 찌르기 시작했다…
		PRINTFORMW 「움찔움찔 해 버려…  후후후…」
	ELSE
		PRINTFORMW 「다소는 사마가 되어도 곳일까」
		PRINTFORMW 「쇠약해지고 자른 정도, 흥을 깸 하는 것은 없고. 저기?」
		PRINTFORMW 「후후후…」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쇠약해졌다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && PENIS(MASTER) && (TCVAR:MASTER:발기도 < TCVAR:MASTER:전발기도)
	IF AFFECTION("상사상애")
		PRINTFORMW 「나의 앞에서 쇠약해진다든가, 허락하지 않아요…」
	ELSE
		SELECTCASE RANDACT(2, 110)
			CASE 0
				PRINTFORMW 「그렇지 않아도 궁상스러운데, 그렇게 쇠약해지게 해 버려…」
				PRINTFORMW 「정말, 볼품이야는 듯 없네요」
			CASE 1
				PRINTFORMW 「… 완전히. 너무 허술해 슬퍼져요?」
				PRINTFORMW 「사실, 사용할 수 없어요」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 그 외·조건 없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTFORMW 「저기,%RAND_SPLIT("상태는 어떻게든 해들/기분은 어떠세요?/지금의 기분은 어때?")%」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT1, 자신에게로의기분을얘기한다
;────────────────────────────────────
;·자신의 일을 어떻게 생각할까 얘기한다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일 조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K14_1
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && FIRSTACT(0)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;그 조교의 최초의 액션
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:경과시간 == 1 && !GET_KFLAG_K14("상사상애")
	PRINTFORMW 「저기…, 조금 들려주었으면 좋지만」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%, 나의 일 어떻게 생각하고 있을까?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;상사상애 또한 이벤트로 플래그가 서 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;앨리스씨는 이런 것이 너무 어울려 곤란함
;여기는 상황 묘사를 날조 해도 될지도 시련
;-------------------------------------------------
IF 구상기입체크(100) && GET_KFLAG_K14("상사상애")
	LOCALS = \@ TENSION(TARGET, 8) == "높다" ?!  # \@
	;kawaii
	SELECTCASE RANDACT(4, 0)
		CASE 0
			PRINTFORMW 「이제 와서 대답은 (들)물을 것도 없지만, 일단 (들)물어 두어요」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%…, 나의 일 어떻게 생각하고 있어?」
		CASE 1
			PRINTFORMW 「다 안 일에서도, 가끔 말로 하는 것은 중요한 것이군요」
			PRINTFORMW 「이봐요, (들)물어 주기 때문에 빨리 말하세요%LOCALS%」
		CASE 2
			PRINTFORMW 「무엇으로 지금쯤 그런 일이라든지, 시시한 것을 말%RAND_SPLIT("우노는 그만두군요/도 참 때려요")%…」
			PRINTFORMW 「이런 것은, 한 번 말하면 끝난다는 이야기가 아니야. 알아?」
		CASE 3
			PRINTFORMW 「이봐요! 멍청히 하지 않고, 빨리 대답하세요」
			PRINTFORMW 「어차피…, 대답은 다 알고 있지만 말야%LOCALS%」
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외·조건 없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTFORMW 「%CNAME(MASTER, TARGET)%는 나의 일 어떻게 생각하고 있을까?」
	PRINTFORMW 「저기, 들려줘…」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT2, 성에대한이야기를한다
;────────────────────────────────────
;·조교자가 성에 대한 지식을 말하기 시작한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·조교자에게 성지식이 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K14_2
;-------------------------------------------------
;정직, 미묘한 커맨드다라고 생각한다w
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && FIRSTTIME(0)
	PRINTFORMW 「저기,%CNAME(MASTER, TARGET)%는 이런 이야기에 대해 들은 일 있을까?」
	PRINTFORMW 「나도 잘 알고 있는 것이 아니지만――」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;전회 COM가부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_PREVCOMNAME("부끄러워한다")
	PRINTFORMW 「%CNAME(MASTER, TARGET)%같은 부끄럼쟁이\@ TALENT:MASTER:작은체형 ? 의 아이 # \@에, 이런 것은 어떨까 라는 생각도 들지만」
	PRINTFORMW 「하? 무슨이야기인가는? 정해져 있겠죠」
	CALL FADEIN(10, "「섹스의 이야기야!」")
	WAIT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 작은체형		구상적으로는 아이 취급
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(80) && TALENT:MASTER:작은체형
	SELECTCASE RANDACT(3, 2)
		CASE 0
			PRINTFORMW 「정직,%CNAME(MASTER, TARGET)%와 같은 아이에게 들려주어야 할 것인가 괴롭지만…」
			PRINTFORMW 「그렇지만, 엣치한 이야기에 흥미없을 것이 아닐 것입니다?」
		CASE 1
			PRINTFORMW 「이런 것은, 꼬마님에게는 조금 자극이 너무 강한일까…」
			PRINTFORMW 「라는 아무튼, 이제 와서 지나는 이야기일지도. 저기」
		CASE 2
			PRINTFORMW 「저기,%CNAME(MASTER, TARGET)%도 (듣)묻고 싶을 것입니다? 누나의 엣치한 이야기…」
			PRINTFORMW 「후후, 시치미를 떼어도 안돼. 사실은 그렇지만도 않은 주제에…」
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외·조건 없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;전회 REACT분류로 파생
	SELECTCASE PREVCOMGRONAME()
		CASE "적극적으로 한다", "받아들인다", "태연하게 한다"
			PRINTFORMW 「%CNAME(MASTER, TARGET)%라면, 이런 이야기도 상당히 갈 수 있을 것 같다고 생각하지만」
			PRINTFORMW 「예를 들어, 예를 들어――」
		CASE "소극적으로 한다"
			PRINTFORMW 「%CNAME(MASTER, TARGET)%에는 정직, 좀 더 적극적으로 되어 받고 싶네요」
			PRINTFORMW 「그러면, 이런 이야기를 들려줄까요――」
		CASE "용서를빈다"
			PRINTFORMW 「%CNAME(MASTER, TARGET)%를 보고 있으면, 조금 곤란하게 해 주고 싶어지는거네요」
			PRINTFORMW 「뭐, 그런 (뜻)이유이니까 체념해 (듣)묻는거야. 나의 추잡한 이야기――」
		CASEELSE
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 년경이라면, 이런 이야기에 흥미없을 리가 없지만」
			PRINTFORMW 「아라, 시치미를 뗄 생각? 단념하지 못하네요」
			PRINTFORMW 「뭐 좋아요, 들려준다. 조금 엣치한 이야기를, 이군요――」
	ENDSELECT
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT3, 상냥하게위로한다
;────────────────────────────────────
;·조교자가 상냥한 말로 위로해 온다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K14_3
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 성교 봉사계로 질내사정의 뒤
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
	PRINTFORMW 「아라, 낙담하지 않아서 있고 것」
	PRINTFORMW 「나의 질내에 이렇게 내 버려서… 좋았던 것이군요?」
	PRINTFORMW 「저기, 좀 더 솔직해져… 후후후」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교계로 질내사정의 뒤
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
	PRINTFORMW 「걱정하지 말아줘…」
	PRINTFORMW 「데 와 버리면, 분명하게 책임 취해 주기 때문에♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;갈 것 같다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
	;=================================================
	;갈 것 같은 것을 참고 있다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다 > 5
		PRINTFORMW 「인내 같은거 하지 않고 가 버리세요」
		PRINTFORMW 「그렇지만, 참고 있는 모습은 조금 사랑스러울지도… 후후후」
		RETURN 1
	ENDIF
	
	;=================================================
	;애태움중
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:애태움수 > 1
		PRINTFORMW 「어때? 초조해 해지는 것은 괴로울까?」
		PRINTFORML 「나의 기분이 풀리면, 가게해 주어도 괜찮아요」
		PRINTFORMW 「후후후……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「갈 것 같은거네?」
	PRINTFORMW 「기다리고 있어… 곧바로가게해 주기 때문에♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;발기
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
	PRINTFORMW 「후훗… 부끄러워하지 않아도 괜찮은거야」
	PRINTFORMW 「나를 앞으로 하고 있는 것 같은건 끝내는 것은…」
	PRINTFORMW 「자연스러운 일인 것이니까♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 그 외·조건 없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	IF GET_KFLAG_K14("상사상애")
		PRINTFORMDW 앨리스는%CALLNAME:MASTER%의 머리를 상냥하게 어루만졌다…
		PRINTFORM 「후후, 좋아 좋아…
		CALL PRINT_SYMBOL_K14
	ELSE
		PRINTFORMW 「따로 취해 먹자는 것이 아니기 때문에」
		PRINTFORMW 「안심해도 좋아요…」
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT4, 엄격하게위협한다
;────────────────────────────────────
;·어려운 말로 위협한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K14_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && FIRSTACT(0)
	PRINTFORMW 「기억해 둬 줘」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%를 어떻게 하는지, 모두 나나름이라는 일」
	PRINTFORMW 「이 의미를 모를 정도(수록), 바보가 아닌 것을 기대하고 있어요…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교 봉사계로 질내사정의 뒤
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
	PRINTFORMW 「아직, 아직 부족해요!」
	PRINTFORMW 「마지막 한 방울까지 짜내 주기 때문에, 각오 해 줘」
	CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「후후……」")
	CLEARLINE 1
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교계로 질내사정의 뒤
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
	PRINTFORMW 「\@ IS_PREVCOMNAME("안에내지마！") ? 억지로 # %CNAME(MASTER, TARGET)%에 \@질내사정, 최고군요. 후후」
	PRINTFORM 「%CNAME(MASTER, TARGET)%가 분명하게 임신할 때까지, 매일질내에 흘러 들어가 주어요
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;갈 것 같다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
	;=================================================
	;갈 것 같은 것을 참고 있다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다 > 5
		PRINTFORMW 「쓸데없는 저항은 그만두면 어때…?」
		PRINTFORMW 「어차피 곧 가 버리기 때문에」
		RETURN 1
	ENDIF
	
	;=================================================
	;애태움중
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:애태움수 > 1
		SELECTCASE RANDACT(2, 0)
			CASE 0
				PRINTFORMW 「아라, 갈 것 같아?」
				PRINTFORMW 「그렇지만 안 돼요, 좀 더 인내 하세요」
			CASE 1
				PRINTFORMW 「가고 싶어서 견딜 수 없는, 은 얼굴이군요?」
				PRINTFORMW 「이대로 쭉 애태움등%CNAME(MASTER, TARGET)%, 어떻게 되어 버릴까. 저기?」
		ENDSELECT
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), "「후후……」")
		CLEARLINE 1
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;발기
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
	PRINTFORMW 「무엇 마음대로 세우고 있을까」
	PRINTFORMW 「사실…, 신통치 않은 자지, 군요…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;아이마스크 장착중
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TEQUIP:아이마스크
	PRINTFORMW 「이대로%CNAME(MASTER, TARGET)%를 방치하면 어떻게 될까?」
	PRINTFORMW 「후후, 조금 재미있을 것 같구나」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 그 외·조건 없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTFORMW 「얌전하게 하고 있으면, 나쁘게는 하지 않아요」
	PRINTFORMW 「쓸데없는 저항은 그만두는 것이 현명해요」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT5, 의상의변경을요구한다
;────────────────────────────────────
;·조교자가 의상의 변경을 요구해 온다
;·조교 대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:쓰러트리기중에는 발생하지 않는다
;·TEQUIP:줄이 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K14_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「자, 옷을 벗어 줘」 
		PRINTFORM 「%CNAME(MASTER, TARGET)%의 모두, 보여 받아요
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		PRINTFORMW 「뭐야, 벌써 부풀려 버리고 있는 것? 성급한 일이군요」
		PRINTFORMW 「뭐, 그것은 좋기 때문에… 빨리 옷을 탈%RAND_SPLIT("없음 있고/있고로")%」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 조교 대상을 몹시 사랑함
	;=================================================
	IF 구상기입체크(50) && AFFECTION("몹시 사랑함")
		PRINTFORMW 「자, 벗어…, 나에게 전부 보여 줘」
		PRINTFORM 「후훗, 흥분해 버려요
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF

	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「그러면, 벗어 받을까」
	PRINTFORMW 「부끄러워한다일 같은거 없어요」
	PRINTFORM 「천천히…, 나에게 잘 보이도록. 저기
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	PRINTFORMW 「그런데, 옷을 입어 받을까?」
	PRINTFORMW 「별로 이유 같은거 없어요. 다만, 그러한 기분인 것이군요」
	PRINTFORMW 「알면 빨리 해 줘…」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT6, 합의를요구한다
;────────────────────────────────────
;·처음을 받아도 괜찮을까 얘기한다
;·처음=처녀 or동정(양쪽 모두의 경우도 있다)
;·이미 합의를 얻고 있는 경우는 발생하지 않는다
;·조교 대상이 처녀도 동정도 아닌 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
;MASTER의 성별마다 ACT파생가 있거나는 하지 않아서
;! PENISI(MASTER) 되어 PENIS(MASTER) 분기내에서 TALENT로 구별 차든지
;TALENT마다 조건을 나누어 쓰든지 할 필요가 있다
;그렇다고 할까 아무튼, 귀찮으면 별로 나누지 않아도 좋다
;-------------------------------------------------
@KOJO_ACT_K14_6
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;귀찮아서 최상층에서 정리해 버린다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && FIRSTACT(0)
	PRINTFORMW 「저기, 하나 부탁이 있는 것이지만…」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%의 처음으로, 나에게 주지 않을까?」
	SIF TALENT:MASTER:작은체형
		PRINTFORMW 「이렇게 말해도…, 아이에게 이해할 수 있을까 미묘한 곳일, 까요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 조교 대상이 남자의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TALENT:MASTER:남자
	;=================================================
	;조교 대상에의 호감도 1000이상
	;=================================================
	IF 구상기입체크(100) && AFFECTION("권애")
		PRINTFORMW 「저기…, 부탁」
		PRINTFORMW 「나,%CNAME(MASTER, TARGET)%의 처음의 여자가 되고 싶은거야…」
		PRINTFORMDW 앨리스는 뺨을 홍조시켜, 열정적인 시선으로%CALLNAME:MASTER%에 강요해 왔다…
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「,%CNAME(MASTER, TARGET)%는 아직 이른바, 그… 동정이었, 네요?」
	PRINTFORMW 「그렇다면, 저, 나와…… 는. 적당, 헤아려 주었으면 하는 것이지만」
	SIF TALENT:MASTER:작은체형
		PRINTFORMW 「%CNAME(MASTER, TARGET)%같은 꼬마님에게는, 무리한 상담이었을, 까요…」
	SIF !TALENT:MASTER:작은체형
		PRINTFORMW 「너무 창피를 주지 말아줘…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 조교 대상이 후타나리의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TALENT:MASTER:후타나리
	;=================================================
	;조교 대상에의 호감도 3000이상
	;=================================================
	IF 구상기입체크(100) && AFFECTION("맹목적으로 사랑")
		;조교 대상이 처녀 또한 동정
		IF ALLSAMES(1, TALENT:MASTER:처녀, TALENT:MASTER:동정)
			PRINTFORMW 「저기…, 부탁」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 처음으로, 나에게 줘…」
			PRINTFORMW 「응, 그렇구나. 어느 쪽의 일이라고 생각해…?   후후…」
		;그 외
		ELSEIF TRUECHECK(TALENT:MASTER:처녀, TALENT:MASTER:동정) > 0
			PRINTFORMW 「저기… 부탁이야」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 처음으로, 나에게 줘…」
			PRINTFORMDW 앨리스는 뺨을 홍조시켜, 열정적인 시선으로%CALLNAME:MASTER%에 강요해 왔다…
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 처녀 혹은 동정
	;=================================================
	IF 구상기입체크(100) && TALENT:MASTER:처녀
		;처녀 또한 동정
		IF TALENT:MASTER:동정
			PRINTFORMW 「저기, 하나 부탁이 있는 것이지만」
			PRINTFORMW 「%CALLNAME:MASTER%의 처음으로, 나에게 주지 않을까?」
		;처녀
		ELSE
			PRINTFORMW 「저기, 하나 부탁이 있는 것이지만」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 처음으로, 나에게 주지 않을까?」
		ENDIF
		SIF TALENT:MASTER:작은체형
			PRINTFORMW 「… 은.%CNAME(MASTER, TARGET)%같은 꼬마님에게는 이해 할 수 없을, 까요…」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「저기, 받아 버려도 괜찮을까?」
	PRINTFORMW 「무엇을 사? …… 아휴, 완전히」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%, 의외로 둔한거네……」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 조교 대상이 여자 아이의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !PENIS(MASTER)
	;=================================================
	;조교 대상에의 호감도 3000이상
	;=================================================
	IF 구상기입체크(100) && AFFECTION("맹목적으로 사랑")
		PRINTFORMW 「저기…, 부탁」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 처음으로, 나에게 줘…」
		PRINTFORMDW 앨리스는 뺨을 홍조시켜, 열정적인 시선으로%CALLNAME:MASTER%에 강요해 왔다…
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORMW 「저기, 받아 버려도 괜찮을까?」
	PRINTFORMW 「무엇을 사? …… 아휴, 완전히」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%, 의외로 둔한거네……」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT7, 벌이라고선언한다
;────────────────────────────────────
;·지금부터 벌 한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:벌플래그는 서 있다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP:쓰러트리기중시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K14_7
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;여기는 KOJO_TRAIN_MESSAGE로 지문과 제휴시켜야 함
;그것과 행동예약으로부터 발생하는 것에 관해서는 KFLAG범위를 마련해야 함
;아무튼 다음번 이후에…
;-------------------------------------------------

;말로만이라고 하는 것은 싫어서 선언하는 이상에는 실행한다!
TCVAR:행동예약 = 60

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, 전COM가 「무뚝뚝하게대답한다/강하게대답한다/싫어한다/날뛴다/거부한다」의 언젠가
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_PREVCOMNAME("무뚝뚝하게대답한다/강하게대답한다/싫어한다/날뛴다/거부한다")
	PRINTFORMW 「아직 자신의 입장이 모르는 것 같네? 완전히, 아휴예요」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%의 경우 말로 전하는 것보다…, 좀 더 직접적인 방법이 좋은 것일까. 저기?」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;전COM가 「기분좋게해줘」（이 철면피! ）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_PREVCOMNAME("기분좋게해줘")
	PRINTFORMW 「철면피로 야비하네요…」
	PRINTFORMW 「이것은 조금 다시 예의범절을 가르칠 필요가 있을까」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;전COM가 「더하드하게」（이 돼지째! ）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_PREVCOMNAME("더하드하게")
	PRINTFORMW 「기가 막혔다…」
	PRINTFORMW 「이 변태 마조돼지에는, 상응하는 처사가 필요같구나…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;전COM가 「용서를빈다」（허락해 받을 수 없었습니다）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_PREVCOMNAME("용서를빈다")
	PRINTFORMW 「분수를 분별하세요…」
	PRINTFORMW 「지금의%CNAME(MASTER, TARGET)%에는, 용서를빈다권리 따위 없는거야」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;전COM가 「자위하기시작한다」（인의 이야기를 들어라! ）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_PREVCOMNAME("자위하기시작한다")
	PRINTFORMW 「무엇을 마음대로 시작해 버리고 있을까」
	PRINTFORMW 「이것은 벌이 필요할까요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외·조건 없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTFORMW 「분수를 분별하지 않는 인형에게는…」
	PRINTFORMW 「아무래도 벌이 필요같, 구나」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT8, 고함친다
;────────────────────────────────────
;·큰 소리로고함친다
;·턴방침이 "소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:벌플래그에 관련되지 않고, 발생할 가능성이 있다（벌중에 한층 더고함친다가능성등 ）
;-------------------------------------------------
@KOJO_ACT_K14_8
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 전COM가 「무뚝뚝하게대답한다/강하게대답한다/싫어한다/날뛴다/거부한다」의 언젠가
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_PREVCOMNAME("무뚝뚝하게대답한다/강하게대답한다/싫어한다/날뛴다/거부한다")
	PRINTFORMW 「정말, 거역하고 있을 뿐으로 안절부절 해요…」
	PRINTFORMW 「적당, 화가 나는거야! 그러한 것!」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 그 외·조건 없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTFORMW 「사실…, 사랑스럽지 않은거야%CNAME(MASTER, TARGET)%」
	PRINTFORMW 「그 얼간이인 얼굴도 안절부절 해요!」 
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT9, 기분나쁘게웃는다
;────────────────────────────────────
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·화제 『비웃는다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K14_9
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 기분나쁘게웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	LOCALS = %RAND_SPLIT("우후후후후/후후후후후")%
	;=================================================
	;아이마스크 장착중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:아이마스크
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%LOCALS%…」")
		SIF ANIME_CONFIG("FADE")
			CLEARLINE 1
		PRINTFORMDW 시야를 빼앗겼다%CALLNAME:MASTER%의 귀에,%CALLNAME%의 기분 나쁜 치 있고 소리가 닿는다…
		RETURN 1
	ENDIF
	
	;=================================================
	;이상계로부터 발생
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT분류 == 7
		CALL FADE(20, COLOR2_K14("ALICE"), COLOR_K14("black"), @"「%LOCALS%…」")
		SIF ANIME_CONFIG("FADE")
			CLEARLINE 1
		PRINTFORMDW 앨리스의 기분 나쁜 치 있고 소리가 방에 울리고 있다…
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE RANDACT(2, 10)
		CASE 0
			PRINTFORMDW %CALLNAME%는 그 표정에 요염한 웃음을 띠면서,
			PRINTFORMDW 때때로 킥킥 비웃으면서,%CALLNAME:MASTER%에 휘감기는 것 같은 시선을 던지고 있다…
		CASE 1
			PRINTFORMDW %CALLNAME%의 기분 나쁜 치 있고 소리가 방안에 울려 퍼진다…
			PRINTFORMDW 그렇게, 가련한 소녀의 모습을 하고 있어도 그녀는 마녀다
			PRINTFORMDW 재차 깨닫게 되어져,%CALLNAME:MASTER%는 전율 했다…
	ENDSELECT
	RETURN 1
ENDIF