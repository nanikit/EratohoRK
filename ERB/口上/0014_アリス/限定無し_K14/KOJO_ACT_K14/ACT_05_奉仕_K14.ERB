;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	앨리스 구상 봉사계 액션 구상 파일
;
;	봉사계 액션 구상을 정리한 파일
;
;Version		Reason
;1. 71 Rel3			408, 412행째의%%누락을 수정
;1. 71			@구상 기입 체크 사양에. 일부 가필. 그 외 섬세한 수정
;1. 60			ACT 분류 단체 파일로서 독립
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;봉사계 ACT50~57
;========================================================================
;·TFLAG:ACT(50=손으로 애무를 강제/51=입으로 애무를 강제/52=파이즈리/53=가랑이/55=다리 빨고 강제/56=이라마치오/57=가슴 애무 강제)
;·TFLAG:ACT 분류 == 5에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "봉사")로 줍는 것이 가능
;·조교 대상으로, 조교자의 신체를 애무하도록(듯이) 명령한다
;·기본적으로는 명령인 것으로, 이 단계에서는 따를지 어떨지 확정하고 있지 않다(ACT56, 이라마치오는 거부 할 수 없다)
;-------------------------------------------------
;·ACT54, 풋잡은 12/04/08판으로 숙청
;-------------------------------------------------

;─
;ACT50, 손으로 애무를 강제
;─
;·애무 강제/자위 강제/페니스 밴드 자위 강제
;·조교 대상(MASTER)으로, 손에 의한 애무를 강제한다
;·조교자에게 페니스가 있으면 반드시 자위 강제가 된다(페니스가 있으면 통상의 애무 강제는 일어나지 않는다)
;·조교자가 페니스 밴드 장착중은 페니스 밴드 자위 강제가 된다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "하드", "이상"이라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(줄/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교 대상이 따르는지 아닌지는 이 시점에서는 미정
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_ACT_K14_50
;-------------------------------------------------
;·TFLAG:ACT 파생(0=애무 강제/1=자위 강제/2=페니스 밴드 자위 강제)
;-------------------------------------------------
;·지문은 ACT 파생마다 분기. 각 ACT 파생내에서는 분기 없음
;·ACT 파생 0, 애무때만 쾌B/쾌ASOURCE 증가
;·그 외의 경우는 쾌C한정
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_0, 애무 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;ACT50, 첫회
	;=================================================
	IF 구상기입체크(100) && !PENIS(TARGET) && FIRSTACT(0)
		;조교 대상이 남자
		IF TALENT:MASTER:남자
			PRINTFORMW 「\@ TALENT:MASTER:작은체형 ? 이봐요…, 너# …,%CNAME(MASTER, TARGET)% \@가 하는거야」
			;조교 대상이 동정
			IF TALENT:MASTER:동정
				PRINTFORMW 「여자 아이에게 있어 중요해, 섬세한 곳이니까 정중하게 부탁해요」
			ELSE
				PRINTFORMW 「다소 여자의 취급은 알고 있는 것 같으니까, 괜찮다고는 생각하지만」
			ENDIF
		;조교 대상이 남자가 아니다
		ELSE
			PRINTFORMW 「이봐요,%CNAME(MASTER, TARGET)%가 하는거야」
			PRINTFORMW 「같은 여자끼리…, 어디를 어떻게 하면 좋을까 같은거 안 것이지요?」
			;조교 대상 캔버스 선반 리카개 작은체형이 아니다
			SIF TALENT:MASTER:후타나리 && !TALENT:MASTER:작은체형
				PRINTFORMW 「뭐,%CNAME(MASTER, TARGET)%의 경우는 조금 다른 것 같지만. 후후」
			;조교 대상이 작은체형
			SIF TALENT:MASTER:작은체형
				PRINTFORMW 「읏, 작은 여자 아이에게 말하는 것은 너무 엉뚱할지도 모르지만. 킥킥」
		ENDIF
		PRINTFORMW 「그러면, 좋을까. 우선은 내가 말하는 곳으로부터 손대어 줘……」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
		PRINTFORMW 「저기, 나 이제(벌써) 갈 것 같아…」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 손으로 가게 해 줘……」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF RAND:3
		;범용 처리
		CALL PRINT_ACT_50_0_K14
	ELSE
		IF AFFECTION("상사상애")
			PRINTFORMW 「저기, 알까  나의 몸, 손대기를 원하고 뜨거워지고 있어…」
			PRINTFORMW 「빠르게 나에게 손대어…… 좋을대로 만지작거려 돌려…」
		ELSE
			PRINTFORMW 「자, 손으로 기분 좋게 시켜 받을까」
			PRINTFORMW 「좋을대로 만지작거려도 좋아요…」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_1, 자위 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(1)
		IF PENIS(MASTER)
			PRINTFORMW 「이봐요, 어떻게 하면 좋은가 라니%CNAME(MASTER, TARGET)%가 잘 알고 있을까요?」
			PRINTFORMW 「\@ TALENT:MASTER:작은체형 ? 는, 작은 사내 아이에게 말하는 것도 부디 라는 생각도 들지만# 뭐, 인의 것이라고 사정이 다를지도 모르지만\@」
		ELSE
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 경우,\@ TALENT:MASTER:처녀 ? 이런 일 하는 것은, 이것이 처음일까?# 이것이 처음이라는 것도 아닐 것입니다?\@」
			PRINTFORMW 「라는 아무튼, 억측으로 제멋대로인 일 말해 본 (뜻)이유이지만. 후후」
		ENDIF
		PRINTFORMW 「나도 이런 것 되는 측이라는 것은 경험이 없으니까. 흥미 반, 불안 반이라는 곳이지만」
		PRINTFORMW 「뭐, 뒤는%CNAME(MASTER, TARGET)%나름이라는 것…」
		RETURN 1
	ENDIF
	
	;================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
		PRINTFORMW 「읏, 하…! 이제 한계…」
		PRINTFORMW 「저기, 좀 더 강하게 훑어 내…… 망가질 정도로, 마음껏…」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF RAND:3
		;범용 처리
		CALL PRINT_ACT_50_0_K14
	ELSE
		PRINTFORMW 「저기, 알까…?%CNAME(MASTER, TARGET)%로 되는 것 기대해, 이렇게 되어 있는거야…」
		PRINTFORMW 「기대하고 있어요…♪  후후후…」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_2, 페니스 밴드 자위 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(2)
		PRINTFORMW 「정직, 이것에 의미가 있는지 어떤지. 조금 의문으로 생각해요?」
		PRINTFORMW 「뭐 그렇지만…, 해 보면 뭔가 새로운 발견이 있을지도. 후후」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF RAND:3
		;범용 처리
		CALL PRINT_ACT_50_0_K14
	ELSE
		{
		CALL HTMLPRINTW(@"「그런데,%HTMLFONT("<b>이것</b>", , COLOR2_K14("흰색"))%" + 
                         "를 훑어 받을까?」")
		}
		IF TENSION(TARGET, 8) == "높다"
			CALL FADEOUT(10, "「후후…」")
			SIF ANIME_CONFIG("FADE")
				CLEARLINE 1
		ENDIF
		PRINTFORMW 「라도 좋은 일? 모조품이니까 라고, 부실은 허락하지 않으니까…」
	ENDIF
	RETURN 1
ENDIF

;─
;ACT51, 입으로 애무를 강제
;─
;·쿤닐링구스/펠라치오/안면 승마/안면 승마 애널/
;·조교 대상으로, 입에 의한 애무를 강제한다
;·조교자가 후타나리의 경우, 조교 대상이 위로 돌리고라면 1/2으로 안면 승마, 그렇지 않으면 페라 강제가 된다
;·조교자에게 페니스가 없고, 조교 대상이 위로 돌리고라면 안면 승마가 된다
;·안면 승마가 선택되고 있었을 때, 안면 승마 애널에 변화하는 일이 있다
;·모든 조건으로부터 빠졌을 경우는 통상의 쿤닐링구스가 된다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각 목마) 장착중은 발생하지 않는다
;·조교자가 페니스 밴드를 장착하고 있는 경우는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_ACT_K14_51
;-------------------------------------------------
;·TFLAG:ACT 파생(0=쿤닐링구스/1=페라/2=안면 승마/3=안면 승마 애널)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_0, 쿤닐링구스 FIRST/RANDACT(0)-(10)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0) && !TEQUIP:조교대상C사용
		PRINTFORMW 「자… 무릎 꿇어, 입과 혀를 사용해 나에게 봉사하는거야」
		PRINTFORMW 「아직 알지 않아? 빠세요라고 말하고 있는거야」
		PRINTFORMW 「주인님의 소중한 곳에 입으로 봉사할 수 있다니 개에 있어서는 행복하겠지요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		IF AFFECTION("상사상애")
			PRINTFORMW 「앗… 응아앗…… 핫… 아… 아…」
			PRINTFORMW 「좀 더…… 좀 더 혀로 휘저어……!」
		ELSE
			PRINTFORMW 「핫… 앗…… 그렇지 않아… 좀 더… 이렇게…」
			PRINTFORMW 「완전히, 안타깝네요……」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;정액 빨고 강제
	;=================================================
	IF 구상기입체크(100) && INRANGE(TCVAR:MASTER:질내사정후경과턴, 1, 2)
		SELECTCASE RANDACT(3, 3)
			CASE 0
				PRINTFORMW 「상당히 쏟아 주었군요?」
				PRINTFORMW 「자, 자신의 정액을 빨아 예쁘게 해 줘」
			CASE 1
				PRINTFORMW 「이봐요, 정액이 넘쳐 와 버리고 있는 것… 알까」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%가 더럽힌 것이니까, 분명하게 깨끗이 해 받아요」
			CASE 2
				PRINTFORMW 「\@ !TEQUIP:아이마스크 ? 봐# 이봐요\@…、%CNAME(MASTER, TARGET)%의 정액이 넘쳐 버리고 있어…」
				PRINTFORMW 「이것을 어떻게 하면 좋은가. 말하지 않아도 아네요?」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;정액 빨고 강제 2
	;=================================================
	IF 구상기입체크(100) && IS_PREVACTNAME("풋잡 한다") && GET_STAIN("바기나", "정액", TARGET)
		PRINTFORMDL 앨리스는 정액 투성이가 된 스스로의 비밀스런 장소를%CALLNAME:MASTER%에 빨라고 명했다…
		PRINTFORMW 「자, 책임을 져 예쁘게 해 받을까?」
		PRINTFORMW 「일적 남김없이 예쁘게 빠세요. 조금이라도 남기면 용서하지 않으니까…」
		RETURN 1
	ENDIF
	
	;-------------------------
	;샌 것은 여기서 처리
	;-------------------------
	IF RAND:3
		SELECTCASE RANDACT(3, 10)
			CASE 0
				PRINTFORMW 「자, 무릎 꿇어 빠는거야」
				PRINTFORMW 「분명하게 봉사할 수 있었다면, 뭔가 포상을 생각해 주지 않는 것도 없어요. 후후」
			CASE 1
				PRINTFORMW 「언제까지나 바라보고 있는 것은 좋지만, 빨리 시작해 주지 않을까」
				PRINTFORMW 「속상해.%CNAME(MASTER, TARGET)%…」
			CASE 2
				PRINTFORMW 「빠세요. 이 『앨리스님』에, 입으로 봉사할 수 있다니 행복하겠지요?」
				PRINTFORMW 「이것,%CNAME(MASTER, TARGET)%만의 특권인 것이니까…, 그 의미를 자주(잘) 악물어 주었으면 하는 거네」
		ENDSELECT
	ELSE
		IF AFFECTION("상사상애")
			PRINTFORMW 「저기… 나, 이제 인내 할 수 없는거야……」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 혀로…… 휘저어…」
		ELSE
			PRINTFORMW 「자, 나의 코코를 빠세요」
			PRINTFORMW 「정중하게, 상냥하게 부탁해요…」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_1, 펠라치오 FIRST/RANDACT(11)-(19)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 혀사용이 추잡하고…… 이제 한계…」
		PRINTFORMW 「응읏… 그래요, 좀 더 입을 오므라질 수 있어…」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 위로 돌리고
	;=================================================
	IF 구상기입체크(100) && TFLAG:위로돌리고
		PRINTFORMW 「이 몸의 자세로 물어 받는 것, 뭔가 흥분해요……」
		PRINTFORMW 「이봐요, 좀 더 혀 사용해. 성기 뒷부분도 부탁해요…♪」
		RETURN 1
	ENDIF
	
	IF AFFECTION("상사상애")
		PRINTFORMW 「%CNAME(MASTER, TARGET)%로 해 받는 것…… 뭔가 굉장한 행복한 기분이 되어요…」
		PRINTFORMW 「부탁해요, 빨리%CNAME(MASTER, TARGET)%의 입으로 기분 좋게 해…」
	ELSE
		PRINTFORMW 「자, 나의 물어 줘」
		PRINTFORMW 「이빨을 세워서는 안 돼요?」
 	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_2, 안면 승마 FIRST/RANDACT(20)-(29)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(20)
		;-------------------------------------------------
		;조교 대상 C사용중은 정면으로 맞서지 않았다고 간주한다
		;-------------------------------------------------
		IF 구상기입체크(100) && GROUPMATCH(TEQUIP:조교대상C사용, 0, 1) > 0
			PRINTFORMW 「자, 이번은%CNAME(MASTER, TARGET)%의 차례야」
			PRINTFORMW 「제대로 할 수 없다면, 질식할 때까지 강압해, 마음대로 즐겁게 해 받는 일이 되지만」
			RETURN 1
		ENDIF
		
		;---------------------
		;그 외
		;---------------------
		PRINTFORMW 「아라, 좋은 경치 없다.%CNAME(MASTER, TARGET)%에 이런 일을 강제하는 것은, 흥분해 버려요…」
		PRINTFORMW 「싫어한다면, 숨을 할 수 없을 정도 강압해… 좋을대로 즐기면 좋고. 저기?」
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		IF AFFECTION("상사상애")
			PRINTFORMW 「응읏… 하웃… 아…… 아읏…」
			PRINTFORMW 「\@ IS_PREVCOMNAME("적극적 한다/애정을 담아 준다") ? 혀의 움직임이 추잡해서…! 녹아 버린닷!# 좀 더, 좀 더 분명하게 하세요!\@」
		ELSE
			PRINTFORMW 「후우읏…… 응읏…… 앗…」
			PRINTFORMW 「응핫……\@ IS_PREVCOMNAME("적극적 한다/애정을 담아 준다") ? 꽤 능숙함,.# 안타깝네요.\@이봐요, 좀 더 혀로 후비도록(듯이)…!」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;정액 빨고 강제
	;=================================================
	IF 구상기입체크(100) && INRANGE(TCVAR:MASTER:질내사정후경과턴, 1, 2)
		SELECTCASE RANDACT(3, 22)
			CASE 0
				PRINTFORMW 「%CNAME(MASTER, TARGET)%가 더럽힌 곳, 혀로 예쁘게 해 받을까」
				PRINTFORMW 「이봐요, 남기지 않고 예쁘게 빨지 않으면 안 돼요? 우후후…」
			CASE 1
				PRINTFORMW 「이봐요, 자신이 낸 정액을 맛보세요」
				PRINTFORMW 「전부 깨끗이 빨아내 받아요?」
			CASE 2
				PRINTFORMW 「넘친 정액이%CNAME(MASTER, TARGET)%의 얼굴에 늘어져 버리고 있어요. 후후」
				PRINTFORMW 「이봐요,%CNAME(MASTER, TARGET)%의 입으로 예쁘게 해 줘…」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;정액 빨고 강제 2
	;=================================================
	IF 구상기입체크(100) && IS_PREVACTNAME("풋잡 한다") && GET_STAIN("바기나", "정액", TARGET)
		PRINTFORMDL 앨리스는 정액 투성이가 된 스스로의 비밀스런 장소를%CALLNAME:MASTER%에 빨라고 명했다…
		PRINTFORMW 「자, 자신이 한 일의 책임은 취해 받아요」
		PRINTFORMW 「일적 남김없이 예쁘게 빠세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 정욕
	;=================================================
	IF 구상기입체크(100) && T_COND("정욕")
		PRINTFORMDW %CALLNAME%는 흥분한 모습으로%CALLNAME:MASTER%의 안면에 걸치면,
		PRINTFORMDW 다 고조된 관능이 향하는 대로, 흠뻑 젖은 비순을%CALLNAME:MASTER%의 얼굴에 강압했다…
		SELECTCASE RANDACT(3, 24)
			CASE 0
				PRINTFORMW 「이제(벌써)…, 인내 할 수 없어요. 갖고 싶고, 갖고 싶고…, 이상하게 되어 버릴 것 같다!」
				PRINTFORMW 「응 이제(벌써), 속상해! 이봐요, 빠르게! 빨리!」
			CASE 1
				PRINTFORMW 「%RAND_SPLIT("후~, 하아, 후~/하, 하, 하")%……. 이제 안돼. 인내…, 할 수 없다!」
			CASE 2
				PRINTFORMW 「쑤셔 버려…, 이제(벌써) 이상해질 것 같다……. 이봐요 빠르게! 빨리 하세요!」
		ENDSELECT
		PRINTL
		PRINTFORMD %CALLNAME%는
		;조교 대상 C사용에 대해 정면으로 맞서지는 않는 상황
		IF GROUPMATCH(TEQUIP:조교대상C사용, 0, 1) > 0
			PRINTFORMD %CALLNAME:MASTER%의
			IF TEQUIP:페니스밴드
				PRINTFORMD 의욕형
			ELSEIF PENIS(MASTER)
				PRINTFORMD 페니스
			ELSE
				PRINTFORMD 클리토리스
			ENDIF
			PRINTFORMDL 를 애무하면서 허리를 벌름거려,
			PRINTFORMDW 숨을 할 수 없을만큼 그%VAGINA_DESCRIPTION2(TARGET)%를 칠해 왔다…
		;정면으로 맞서, 걸치고 있는 상황
		ELSE
			PRINTFORMDW %CALLNAME:MASTER%의 머리를 잡으면 허리를 구불거리게 해,
			PRINTFORMDW 마치 완구를 취급하는것 같이, 그%VAGINA_DESCRIPTION2(TARGET)%를 칠하고 있다…
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상 C사용중
	;=================================================
	IF 구상기입체크(100) && GROUPMATCH(TEQUIP:조교대상C사용, 0, 1) > 0
		IF RAND:3
			SELECTCASE RANDACT(3, 25)
				CASE 0
					PRINTFORMW 「%CNAME(MASTER, TARGET)%만 기분 좋아져 버린다니 불공평해요?」
					PRINTFORMW 「이봐요…. 나의 일도 기분 좋게 시켜 줘…」
				CASE 1
					PRINTFORMW 「이봐요, 빨아 줘. 제대로 할 수 있었다면,%CNAME(MASTER, TARGET)%도 귀여워해 주어요」
					PRINTFORMW 「만약 거절한다면, 이대로 숨을 할 수 없을 정도 강압해 주기 때문에…」
				CASE 2
					PRINTFORMW 「어떻게 하면 좋은가 라니, 말하지 않아도 아네요?」
					PRINTFORMW 「여기는 여기대로 분명하게 보살펴 주기 때문에,%CNAME(MASTER, TARGET)%도 제대로 빨아 줘」
			ENDSELECT
		ELSE
			IF AFFECTION("상사상애")
				PRINTFORMW 「저기,\@ !TEQUIP:아이마스크 ? 봐.# 알까?    \@%CNAME(MASTER, TARGET)%에 빨기를 원해서 흘러넘칠 것 같게 되어 있어…」
				PRINTFORMW 「주름의 구석구석까지 확실히 맛봐…… 녹을 때까지 없는 돌려…」
			ELSE
				PRINTFORMW 「이대로 질식할 때까지 강압하면, 어떻게 되어 버릴까요…?」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%의 경우…, 이런 것도 좋아하는 것 같다지만. 후후…」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------
	;새어 온 것은 여기서 처리
	;-------------------------
	IF AFFECTION("상사상애")
		SELECTCASE RANDACT(3, 28)
			CASE 0
				PRINTFORMW 「아라, 그렇지만도 않은 것 같은 얼굴이군요? 라고 할까, 오히려 기쁜 것일까」
				PRINTFORMW 「후후, 좋아요. 마음껏 맛보게 해 주어요」
			CASE 1
				PRINTFORMW 「보통으로 생각하면, 사람 해 굉장히 한심한 적당한 (뜻)이유이지만…」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%는 그런 일 신경이 쓰이지 않는 것 같네?」
				PRINTFORMW 「뭐, 정직…. 나도 좋아하는 것이지만, 이런 것……」
			CASE 2
				PRINTFORMW 「역시 좋네요…, 이것. 굉장히 엣치한 기분이 되어 버려요…」
				PRINTFORMW 「%CNAME(MASTER, TARGET)%도, 이렇게 되는 일을 바라고 있던 것이지요?」
				PRINTFORMW 「이봐요, 좋아요. 나의 소중한 곳, 좋아할 뿐(만큼) 빨게 해 준다…」
		ENDSELECT
	ELSE
		SELECTCASE RANDACT(3, 29)
			CASE 0
				PRINTFORMW 「이렇게 해%CNAME(MASTER, TARGET)%를 내려다 보는 것도 좋은 거네」
				PRINTFORMW 「봉사를 강제하는 것은, 굉장히 흥분해 버려요…」
			CASE 1
				PRINTFORMW 「%CNAME(MASTER, TARGET)%…, 실로 보기 흉하고 한심한 모습이군요」
				PRINTFORMW 「뭐, 이런 일강제하고 있는 내가 말하는 것도 무엇인 것이지만. 후후」
			CASE 2
				PRINTFORMW 「이봐요, 마음껏 날름날름 해도 좋아요」
				PRINTFORMW 「개에 있어서는, 그것이 제일의 행복하겠지요? 후후」
		ENDSELECT
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_3, 안면 승마 애널 FIRST/RANDACT(30)-
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		IF AFFECTION("상사상애")
			PRINTFORMW 「저기, 부탁…… 혀로 휘저어…」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%전용의 구멍…… 귀여워해…」
		ELSE
			PRINTFORMW 「이봐요, 좀 더%CNAME(MASTER, TARGET)%의 얼굴에 칠해 주어요」
			PRINTFORMW 「읏… 그래요…  좀 더 혀를 내밀어…」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;정액 빨고 강제
	;=================================================
	IF 구상기입체크(100) && INRANGE(TCVAR:MASTER:장내사정후경과턴, 1, 2)
		SELECTCASE RANDACT(3, 30)
			CASE 0
				PRINTFORMW 「%CNAME(MASTER, TARGET)%가 더럽힌 나의 엉덩이, 혀로 예쁘게 해 받을까」
				PRINTFORMW 「이봐요, 남기지 않고 예쁘게 빨지 않으면 안 돼요? 우후후…」
			CASE 1
				PRINTFORMW 「이봐요, 자신이 낸 정액을 맛보세요」
				PRINTFORMW 「전부 깨끗이 빨아내 받아요, 나쁘지만. 후후…」
			CASE 2
				PRINTFORMW 「넘친 정액이%CNAME(MASTER, TARGET)%의 얼굴에 늘어져 버리고 있어요. 후후」
				PRINTFORMW 「이봐요,%CNAME(MASTER, TARGET)%의 입으로 예쁘게 하는거야…」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;-------------------------
	;새어 온 것은 여기서 처리
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMW 「네 받아 주세요」
		PRINTFORM 「나의 냄새와 맛을 충분히 능숙%RANDTEXT_K14("해도 좋아요", "시켜 주어요")%
		CALL PRINT_SYMBOL_K14
	ELSE
		PRINTFORMW 「제대로 하지 않으면 엉덩이로 질식시켜요…?」
		PRINTFORMW 「이봐요, 나의 부정의 구멍에 키스 하세요」
	ENDIF
	RETURN 1
ENDIF

;─
;ACT52, 파이즈리
;─
;·조교 대상으로 파이즈리에서의 봉사를 강제한다
;·조교 대상이 남자의 경우는 발생하지 않는다
;·조교자에게 페니스가 없는 경우는 발생하지 않는다
;·턴 방침이 "이상"의 경우는 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(줄/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_ACT_K14_52
;-------------------------------------------------
;·ACT 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT52, 파이즈리
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
		PRINTFORMW 「읏쿠…… 좋아요… 좀 더 규는 사이에 둬 줘」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 젖가슴…… 굉장히 추잡하다…」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 위로 돌리고
	;=================================================
	IF 구상기입체크(100) && TFLAG:위로돌리고
		PRINTFORMW 「후후… 좋은 모습이군요」
		PRINTFORMW 「자,%CNAME(MASTER, TARGET)%의 가슴으로 즐겁게 해 받아요♪」
		RETURN 1
	ENDIF
	
	;-------------------------
	;샌 것은 여기서 처리
	;-------------------------
	PRINTFORMW 「… 가슴으로 사이에 두어 받을까」
	PRINTFORMW 「첨단도 분명하게 빨아 줘」
	RETURN 1
ENDIF

;─
;ACT53, 가랑이
;─
;·조교 대상으로 페니스 밴드, 혹은 페니스를 가랑이에 끼워 스치고라고 명령한다
;·조교자에게 봉이 없다(페니스/페니스 밴드) 경우는 발생하지 않는다
;·조교 대상으로 페니스가 있는 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "이상"의 경우는 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:삼각 목마 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교 대상으로 다리가 없으면 발생하지 않는다(현재 상태로서는도자고가 조교 대상인 경우만이라고 생각된다)
;·REACT 분류가 거부/날뛴다/도망치는인 경우는 거절 취급
;-------------------------------------------------
@KOJO_ACT_K14_53
;-------------------------------------------------
;·TFLAG:ACT 파생(0=가랑이/1=페니스 밴드 가랑이)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_0, 가랑이 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자에게 페니스가 있는 경우
;------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
		PRINTFORMW 「응읏…… 슬슬 한계군요…」
		PRINTFORMW 「최후는%CNAME(MASTER, TARGET)%의 넓적다리로 가게 해 받을까♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 위로 돌리고
	;=================================================
	IF 구상기입체크(100) && TFLAG:위로돌리고
		PRINTFORMW 「자, 다리를 확실히 닫아 줘」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 넓적다리, 즐겁게 해 받아요…♪」
		RETURN 1
	ENDIF
	
	;-------------------------
	;샌 것은 여기서 처리
	;-------------------------
	PRINTFORMW 「이봐요, 넓적다리로 내가 사이에 둬 줘」
	PRINTFORMW 「%CNAME(MASTER, TARGET)%도 기분 좋아지도록(듯이), 비비어 주어요♪」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_1, 페니스 밴드 가랑이 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 페니스 밴드 장착중의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	PRINTFORMW 「그런데, 이것을 사이에 두어 받을까?」
	PRINTFORMW 「이런 유사품으로 얼마나 즐길 수 있을까 모르지만, 말야. 후훗」
	RETURN 1
ENDIF

;─
;ACT55, 다리 빨고 강제
;─
;·조교자가 다리를 빨라고 명령해 온다
;·이상계로부터 발생하는 경우 있어
;·턴 방침이 "소프트"라면 발생하지 않는다
;·오늘의 방침이 "러브러브"라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교자에게 다리가 없는 경우는 발생하지 않는다
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급
;-------------------------------------------------
@KOJO_ACT_K14_55
;-------------------------------------------------
;·지문에 분기 없음@TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT55, 다리 빨고 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	;IF 구상 기입 체크(0) && FIRSTACT(0)
	;ENDIF
	
	;전회 COM와 전회 REACT 분류를 참조한다
	;=================================================
	;허가를 청했다
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(IS_PREVCOMNAME("허가를 청한다"), IS_PREVCOMGRONAME("허가를 청한다")) > 0 && !GET_STAIN("다리", "정액", TARGET)
		SELECTCASE RANDACT(2, 0)
			CASE 0
				PRINTFORMW 「허락해, 저기? 뭐, 그렇다면… 나의 다리를 빨아 받을까」
				PRINTFORM 「제대로 할 수 있었다면, 허락해 주어도 괜찮아요…
				CALL PRINT_SYMBOL_K14
			CASE 1
				PRINTFORMW 「허락했으면 좋다면, 우선 태도로 가리켜 받아요」
				PRINTFORMW 「자, 나의 다리를 빠세요…」
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;조루으로의 벌
	;=================================================
	IF 구상기입체크(100) && GETBIT(KFLAG:TARGET:201, 3)
		PRINTFORMW 「누군가씨의 덕분으로 완전히 더러워져 버렸어요」
		PRINTFORMDW %CALLNAME:TARGET%는 그렇게 말하면%CALLNAME:MASTER%의 코끝에 다리를 내밀었다…
		PRINTFORMW 「빠세요」
		PRINTFORMW 「원래는이라고 말하면%CNAME(MASTER, TARGET)%가 나쁘기 때문에, 자신이 한 일의 책임을 져 받아요…」
		RETURN 1
	ENDIF
	
	;=================================================
	;정액 빨고 강제
	;=================================================
	IF 구상기입체크(100) && GET_STAIN("다리", "정액", TARGET) && IS_PREVACTNAME("풋잡 한다")
		PRINTFORMDW 앨리스는 정액으로 더러워진 스스로의 다리를 빨라고%CALLNAME:MASTER%에 명했다…
		PRINTFORMW 「방법 없네요?%CNAME(MASTER, TARGET)%가 더럽힌 것이니까」
		PRINTFORMW 「이봐요, 한 방울 남김없이 예쁘게 빨아내는거야. 남기면 허락하지 않으니까…」
		RETURN 1
	ENDIF
	
	;=================================================
	;벌모드
	;=================================================
	IF 구상기입체크(100) && TFLAG:벌플래그 && ONCEACT(6)
		IF AFFECTION("상사상애")
			PRINTFORMW 「이것, 일단 벌이지만」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%에는 포상 밖에 되지 않네요…」
		ELSE
			PRINTFORMW 「자,%CNAME(MASTER, TARGET)%의 입으로 청소 하세요」
			PRINTFORMW 「구석구석까지 예쁘게 빨아 줘」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 위로 돌리고
	;=================================================
	IF 구상기입체크(100) && TFLAG:위로돌리고 && ONCEACT(7)
		IF AFFECTION("상사상애")
			PRINTFORMW 「자,%CNAME(MASTER, TARGET)%를 아주 좋아하는 나의 다리를 빨게 해 주어요」
			PRINTFORMW 「이봐요, 이미 흥분하고 있는거죠? 우훗…」
		ELSE
			PRINTFORMW 「이봐요, 분명하게 하세요」
			PRINTFORMW 「이대로 체중 걸쳐요…?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 양말을 신고 있다
	;(디폴트로 양말 신지 않은 캐릭터는 표시되지 않는다)
	;=================================================
	IF 구상기입체크(100) && TEQUIP:양말 && ONCEACT(8)
		PRINTFORMW 「자, 양말마다 빨아 받을까」
		PRINTFORMW 「나에게 이런 일 할 수 있는 것은%CNAME(MASTER, TARGET)%만여? 영광으로 생각하세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;-------------------------------------------------
	IF 구상기입체크(100) && ONCEACT(9)
		IF AFFECTION("상사상애")
			PRINTFORML 「%CNAME(MASTER, TARGET)%같은 것을 다리 페티시즘이라고 말하는 것 같아요」
			PRINTFORMW 「완전히, 어쩔 수 없는 변태군요!」
			PRINTFORMW 「나, 나의 다리이니까 좋다는…?」
			PRINTFORMW 「이제(벌써), 뭐 말하고 있는거야! 바보……」
		ELSE
			PRINTFORMW 「자, 나의 다리에 혀로 봉사 하세요」
			PRINTFORMW 「변태의%CNAME(MASTER, TARGET)%에 있어서는 오히려 포상%RAND_SPLIT("일지도 모르지만/그럴까요…?")%」
		ENDIF
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	SELECTCASE RANDACT(3, 10)
		CASE 0
			PRINT 「
			CALL PRINTFONTSTYLE(COLOR2_K14("ALICE"), "빠세요", 1)
			PRINTW 」
			PRINTFORMDW %CALLNAME%는%CALLNAME:MASTER%의 눈앞에%RAND_SPLIT("날씬했다/훌륭한")%각선을 쬐면서 말했다…
			PRINTL
			PRINTFORMW 「손가락의 끝으로부터 허벅지까지 제대로 빠는거야」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%와 같은\@ TALENT:MASTER:남자 ? 개# 암캐\@에는 이것이 적당하다고 생각해?」
			PRINTFORMW 「제대로 할 수 있었다면, 뭔가 포상을 생각해 주지 않는 것도 없어요」
			PRINTFORMW 「어떤 보증도 할 수 없지만, 말야. 후후…」
		CASE 1
			PRINTFORMW 「자, 주인님의 다리에 봉사 하세요」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%와 같은 개에 있어서는, 바라도 없는 일이 아닐까?」
			PRINTFORM 「뭐, 그렇지 않았다고 해도, 나의 알 바가 아니지만… 말야
			CALL PRINT_SYMBOL_K14
		CASE 2
			PRINTFORMW 「이봐요, 나의 다리를 빠세요. 발끝으로부터 허벅지까지%RAND_SPLIT("정중/언제나의")%에, 개같이」
			PRINTFORM 「이 『
			CALL PRINTFONTSTYLE(COLOR2_K14("ALICE"), "앨리스님", 1)
			PRINTFORM 』에 혀로 봉사할 수 있는 행복을 음미하면서. 저기…
			CALL PRINT_SYMBOL_K14
	ENDSELECT
	RETURN 1
ENDIF

;─
;ACT56, 이라마치오
;─
;·명령하는 것은 아니고, 억지로 페니스를 입에 돌진한다
;·조교 대상의 반응은 미정이지만, 거절하는 일은 할 수 없다
;·가학계로부터 발생하는 경우 있어
;·턴 방침이 "노멀", "소프트"라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교자에게 페니스가 없는 경우는 발생하지 않는다(페니스 밴드에 의한 이라마치오는 없다)
;-------------------------------------------------
@KOJO_ACT_K14_56
;-------------------------------------------------
;·자위중이면 해제된다
;·지문에 분기 없음. 지문은↓
;·%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 머리를 잡아, 난폭하게 %CALLNAME:MASTER%의 목까지 찔러 넣었다…
;-------------------------------------------------
;·조교자의 발기 정도는 TCVAR:발기로 참조할 수 있다
;·TCVAR:발기(0= 아직 부드럽다/1=서서히 경도를 더하고 있다/2=발기한/2이상=뒤로 젖혔다)적인 느낌으로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT56, 이라마치오
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
		PRINTFORMW 「좋아요, 그대로……\@ !TEQUIP:아이마스크 ? 나의 눈을 보고 있어# 그대로 있는거야\@…」
		PRINTFORMW 「후후…… 목 안에서 쳐 뿌릴 수 있어 주어요」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 위로 돌리고
	;=================================================
	IF 구상기입체크(100) && TFLAG:위로돌리고
		PRINTFORMW 「저기, 이 모습…  굉장한 굴욕적이지 않아…?」
		PRINTFORM 「후후… 좋은 표정이군요.%CNAME(MASTER, TARGET)%의 입, 좀 더 범해 준다…
		CALL PRINT_SYMBOL_K14
		RETURN 1
	ENDIF
	
	;-------------------------
	;샌 것은 여기서 처리
	;-------------------------
	PRINTFORMW 「%CNAME(MASTER, TARGET)%의 입을 범해 주어요」
	PRINTFORMW 「호흡도 할 수 없을 정도, 격렬하게 하기 때문에. 각오 해 줘…」
	RETURN 1
ENDIF

;─
;ACT57, 가슴 애무 강제
;─
;·조교 대상으로, 조교자의 가슴을 애무하도록(듯이) 강제한다
;·조교자가 남자의 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "이상"이라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:줄장착중은 입으로 가슴 애무 강제에 파생한다. 다만 볼개그를 장착중의 경우는 튕겨진다
;·볼개그가 없으면,1/2의 확률로 입으로 애무가 된다
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급
;·PALAM가 치정선행이나 반항 선행인가로"부끄러워서 봉사 할 수 없었다/싫어해 요구에 응하지 않았다"에 파생한다
;-------------------------------------------------
@KOJO_ACT_K14_57
;-------------------------------------------------
;·TFLAG:ACT 파생(0=손으로 가슴 애무 강제/2=입으로 가슴 애무 강제)
;-------------------------------------------------
;·지문은 ACT 파생으로 분기. 파생내에서는 분기 없음
;·@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;·안면 승마는 해제된다
;·REACT에 대해, 위로 돌리고가 아니고, ACT 파생이 입으로 애무가 아닌 경우,1/2그리고 뒤로부터 애무에 파생한다
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_0, 손으로 가슴 애무를 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;조교자 삽입중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교중
		PRINTFORMW 「아…… 읏…… 좋아요, 좀 더 손대어 줘」
		PRINTFORMW 「나도 마음껏 찔러 주기 때문에…」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상 삽입중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		PRINTFORMW 「저기, 알까…?」
		PRINTFORMW 「젖가슴 만지작거려질 때에, 나의 질내가 쿨쿨 하고 있어…」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && PALAM:쾌B > 8500
		PRINTFORMW 「하……… 읏… 응읏… 이… 좋을 것 같다」
		PRINTFORMW 「좀 더, 강하게… 좀 더 아프게 해…」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	;거유
	IF TALENT:TARGET:거유
		IF AFFECTION("상사상애")
			PRINTFORMW 「이봐요, 큰 젖가슴은 좋아하는 것이지요?」
			PRINTFORMW 「좋아할 뿐(만큼) 손대어도 좋은거야? 우후후……」
		ELSE
			PRINTFORMW 「역시 가슴은 큰 것이 좋은 것일까요?」
			PRINTFORMW 「자, 좋아할 뿐(만큼) 손대어도 좋아요」
		ENDIF
	;빈유
	ELSEIF TALENT:TARGET:빈유
		IF AFFECTION("상사상애")
			PRINTFORMW 「작은 가슴을 좋아한다니, 정말 변하는 거네」
			PRINTFORMW 「나에게는 이해 할 수 없지만,%CNAME(MASTER, TARGET)%가 바란다면 어쩔 수 없어요」
			PRINTFORMW 「자, 좋아할 뿐(만큼) 즐겨? 이 변태♪」
		ELSE
			PRINTFORMW 「이런 아이같은 가슴을 좋아한다니, 추잡한거네…」
			PRINTFORMW 「이봐요, 손대어도 좋아요? 최대한 즐겨 줘」
		ENDIF
	;보유
	ELSE
		IF AFFECTION("상사상애")
			PRINTFORMW 「저기, 젖가슴이 안타까운거야…」
			PRINTFORMW 「빨리 손대어… 이제 기다릴 수 없다…」
		ELSE
			PRINTFORMW 「스스로 말하는 것도 뭐 하지만…… 상당한 미유겠지…?」
			PRINTFORMW 「자, 손대어도 좋아요. 좋을대로 반죽해 돌리며…♪」
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_2, 입으로 가슴 애무를 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;=================================================
	;조교자 삽입중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교중
		PRINTFORMW 「응읏… 하웃… 뭐라고 할까, 이것…」
		PRINTFORMW 「내가 시 비치는지, 되고 있는지 잘 모르네요♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상 삽입중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		PRINTFORMW 「응아앗… 야…… 아… 앙… 후아앗……」
		PRINTFORMW 「그, 그렇게 찌르면서…… 젖가슴 들이마셔지면… 나……」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && PALAM:쾌B > 8500
		PRINTFORMW 「응쿳… 응읏… 앙… 아앗…」
		PRINTFORMW 「민감하게 되어 버리고 있는데…… 그렇게… 들이마셔졌닷… 들」
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	IF AFFECTION("상사상애")
		PRINTFORMW 「자, 젖가슴의 시간이야. 어서오세요♪」
		PRINTFORMW 「이봐요 이봐요, 어떻습니까―?  젖가슴 맛있습니까―?」
		PRINTFORM 「후후후… 이봐요,%CNAME(MASTER, TARGET)%도 말하는거야. 젖가슴 매우 나무라고 말하세요
	ELSE
		PRINTFORMW 「이봐요, 나의 가슴 어떨까…?」
		PRINTFORM 「후후… 불끈불끈 할까요? 좋은 것… 들이마셔도
	ENDIF
	CALL PRINT_SYMBOL_K14
	RETURN 1
ENDIF

;-------------------------------------------------
;ACT50용 범용 처리
;-------------------------------------------------
@PRINT_ACT_50_0_K14
IF ABL:MASTER:애무 > 4
	SELECTCASE AUTORAND(3, 50)
		CASE 0
			PRINTFORMW 「이러니 저러니 말해 나,%CNAME(MASTER, TARGET)%에 만지작거려 받는 것이 기다릴 수 없네요」
			PRINTFORMW 「조금 아니꼽지만……」
		CASE 1
			PRINTFORMW 「뭐 확실히,%CNAME(MASTER, TARGET)%가 능숙한 것은 인정하지 않을 수 없지만」
			PRINTFORMW 「거기에 시원스럽게 잘 되어 버리는 나도, 뭐라고 할까, 대개군요…」
		CASE 2
			PRINTFORMW 「이제 와서, 내가 뭔가 말할 필요도 없다는 느낌이지요」
			PRINTFORMW 「이봐요…, 좋아요.%CNAME(MASTER, TARGET)%가 좋아하는 것 같게 만지작거려……」
	ENDSELECT
ELSE
	SELECTCASE AUTORAND(3, 51)
		CASE 0
			PRINTFORMW 「정직,%CNAME(MASTER, TARGET)%에는 입에서의 봉사를 강요하는 것이 기호이지만…」
			PRINTFORMW 「이것은 이것대로, 싫지 않네요」
		CASE 1
			PRINTFORMW 「\@ PENIS(TARGET) ? 보통으로 되는 것보다, 다른 곳과 함께 만지작거려졌다# 직접 손대어지는 것보다, 주위를 상냥하게 꾸짖을 수 있다\@(분)편이 느껴 버리지만」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%…, 그근처 알고 있을까. 저기?」
		CASE 2
			IF TEQUIP:아이마스크
				PRINTFORMW 「이 정도의 일, 안보여도 할 수 있네요?」
				PRINTFORMW 「뭐, 슬쩍 당치 않음 말한 것 같지 않는 것도 아니지만. 후후」
			ELSE
				PRINTFORMW 「과연 나라도, 그렇게 초롱초롱 볼 수 있는 것은 부끄럽습니다만?」
				PRINTFORMW 「그렇지만 이 느낌, 조금 버릇이 될지도 몰라요…」
			ENDIF
	ENDSELECT
ENDIF