;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코 구상 조교자 행동 전이벤트 구상 파일
;
;	조교자 행동 전이벤트 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV12, 조교자 행동 서두
;========================================================================
;·TRAIN_PROCCES.ERB내, @TRAIN_CANCEL로부터 불려 간다
;·구상주의 파라미터 표시의 직후에 표시. 조교 지시는 이미 결정되고 있다
;·조교중지선언의 교환이나, 조교 턴 개시전에 주인님으로부터 한 마디, 와 같이 쓰는 일도 가능
;·오전, 오후의 조교 개시는 아니고, 조교 턴의 개시전에는 매회 불려 가는 개소
;·그 때문에도 있어, 조교중지시와 경과시간 0이외의 개소는 IF 구상 기입 체크(25) 등으로서 확률 표시하는 일을 추천
;·조교중지시에게만 지문이 존재한다
;·문장 표시 후에 본체측의 DRAWLINE 처리 있어
;-------------------------------------------------
;FIRSTEVENT(0)～나, FIRSTEVENT(10)～등으로 해서 테두리마다 할당하고 있습니다만
;FIRSTEVENT/ONCEEVENT/RANDEVENT/ORDEREVENT는 사실상 무한하게 이벤트번호를 늘릴 수 있으므로
;필요에 따라서 임의에 할당해 주세요
;-------------------------------------------------
@KOJO_MALE_EVENT_K44_12(ARG)
;-------------------------------------------------
;인수 내역…
;ARG(0=조교가능/1=구상주의 피로로 중지/2=조교 대상의 피로로 중지/3= 포상에 의한 중지)
;-------------------------------------------------

;─
;조교중지 ARG:1 ARG:2 ARG:3 FIRSTEVENT(0)～
;─
;·피로, 혹은 포상에 의한 조교중지의 경우의 구상
;·기입 체크=1의 경우는 구상 표시 후 본체측에서 PRINTL의 공행이 들어간다
;·기입 체크를=2로 하면, 지문을 표시하지 않는다
;·이라고 할까 RESULT > 2로 판정되므로 2이상이라면 같은 거동이 됩니다만 일단 비추천이라고 하는 일로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;포상에 의한 중지
;RESULT치(1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 3
	PRINTFORMW 「그러고 보면 쉬고 싶다고 말했던가」
	PRINTFORMW 「천천히 영기를 길러 다음에 대비하는거네♪」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자의 피로에 의한 중지
;RESULT치(1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	PRINTFORMW 「응─조금 나른하기 때문에 이번 패스군요」
	PRINTFORMW 「거기의 찬장에 오레오 들어가 있기 때문이라고 귀로─에 가져 가도 좋아」
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상의 피로에 의한 중지
;RESULT치(1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 2
	PRINTFORMW 「뭔가 컨디션 위험한 것 같지 않아 괜찮아?」
	PRINTFORMW 「오늘은 그만두는 것이 좋을지도. 너무 일방적인 것도 시시하고」
	RETURN 2
ENDIF

;-------------------------------------------------
;·이하, ARG:0(조교가능) 상당
;·TRAIN_PROCCES.ERB내, @BEFORE_ACT로부터 불려 간다
;·이 시점에서 ACT는 벌써 결정하고 있지만 ACT파생의 결정은 이루어지지 않았다
;·NOWEX의 리셋트는 되지 않았다
;·이것들의 개소를 빠진 뒤로 ACT 개시가 된다
;·분기의 증감은 자유롭게 할 수 있습니다만, 우선도에는 주의합시다
;-------------------------------------------------

;─
;조교 개시 직후 FIRSTEVENT(10)～
;─
;·경과시간이 0인 경우에 표시된다
;·그 회의 조교 개시 직후에 한 번만으로, 이후 그 조교중에는 표시되지 않는다
;·이 개소를 빠진 뒤로 ACT 개시가 된다
;-------------------------------------------------
;  여기서 IS_POLICY("오늘", "벌") 등으로오늘의방침를 말하게 하는 일도 가능했다거나 합니다만
;  EVENT17, 오늘의 방침 구상과 입을 가능성이 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 개시 직후
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:경과시간
	;=================================================
	;첫회
	;=================================================
	IF FIRSTEVENT(10)
		PRINTFORMW 「뭐심의 준비가는? 남자인 것이니까 따악해 하세요」
		PRINTFORMW 「에? 과연 범죄 냄새가 한다고? 무슨 말하고 있는 것 여기는 환상향이야?」
		PRINTFORMW 「곧바로 그런 일 말할 수 있을 여유 같은거 없애 주기 때문에♪」
		RETURN 1
	ENDIF

	;=================================================
	;조교 대상에의 호감도 1000이상
	;=================================================
	IF AFFECTION("권애")
		PRINTFORMW 「-두는 어떻게 할까나♪」
		PRINTFORMW 「매우 기분이 좋아?   기분탓 기분탓」
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그런데, 오늘은 어째서 할까」
		PRINTFORMW 「어차피라면 서로 즐깁시다♪」
		RETURN 1
	ENDIF
ENDIF

;─
;벌FIRSTEVENT(20)～
;─
;·TFLAG:벌플래그가 서 있는 경우에 표시되는 개소
;·벌플래그가 서 있어도, 가학계 이외의 ACT가 되는 경우가 있다
;-------------------------------------------------
;가학계 ACT, 파생하는 일에 의해 가학계에 포함될 가능성이 있는 이라마치오
;의상의변경을요구한다/기분나쁘게웃는다/애널바이브/후배위/애널섹스/자위/야외플레이/방뇨/다리빨고강제
;이상계 ACT/성교 봉사계 ACT
;상기의 ACT 이외는 TFLAG:벌플래그에 의해 ACT_ABLE로 규제된다
;-------------------------------------------------
;-------------------------------------------------
;·1회의 조교로 1회만 표시된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;벌개시
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:벌플래그 && ONCEEVENT(20)
	PRINTFORMW 「조금 바보해 머리 차게 할까…」
	PRINTFORMDW 스와코의 배후로부터 눈에 보여 검은 오라가 나와 있는 것을 확인해, 당신은 생각하는 것을 그만두었다.
	RETURN 1
ENDIF

;-------------------------------------------------
;벌개시 이외를 처리하는 개소
;━━━━━━━━━━━━━━━━━━━━━━━━
;벌중
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:벌플래그
;ENDIF

;─
;질내, 장내 사정 직후 FIRSTEVENT(30)～
;─
;NOWEX:사정내지 NOWEX:MASTER:사정으로 사정종의 참조도 가능했다거나 합니다만
;EVENT20, 절정 구상에 동종의 분기와 입을 가능성도 있습니다
;-------------------------------------------------
;관련 변수…
;
;TFLAG:전회질내사정(1=V성교/2=V성교 봉사/3=V성교(하늘 쏘아 맞히고)/4=V성교 봉사(하늘 쏘아 맞히고)/5=A성교/6=A성교 봉사/7=A성교(하늘 쏘아 맞히고)/8=A성교 봉사(하늘 쏘아 맞히고)
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정 직후(조교 대상→조교자)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GROUPMATCH(TFLAG:전회질내사정, 2, 4)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크(40) && TFLAG:전회질내사정 == 4
		PRINTFORMW 「이제(벌써) 텅 비어? 어쩔 수 없구나…」
		PRINTFORMW 「이봐요, 빨리 그 불알로 부지런히 정자 만드세요♪」
		RETURN 1
	ENDIF

	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(40)
		CALL HTMLPRINTW(@"「읏%HTML_HEARTB()%  이제(벌써), 이렇게 내…」")
		CALL HTMLPRINTW(@"「신님잉다툴 생각일까…?   업이 깊네요, 당신도%HTML_HEARTB()%」")
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내 사정 직후(조교 대상→조교자)
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GROUPMATCH(TFLAG:전회질내사정, 6, 8)
	;=================================================
	;드라이사정
	;=================================================
	IF 구상기입체크(40) && TFLAG:전회질내사정 == 8
		CALL HTMLPRINTW(@"「엉덩이로 가득 흠칫거리게 해 버려 이 녀석은…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「이제 낼 수 없다니 한심한 것 말하지 않는거야%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(40)
		CALL HTMLPRINTW(@"「이봐요, 엉덩이로부터 가득 정액 새어 왔어…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「너무 사양말고 내요 이 바보…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정 직후(조교자→조교 대상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && GROUPMATCH(TFLAG:전회질내사정, 1, 3)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내 사정 직후(조교자→조교 대상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && GROUPMATCH(TFLAG:전회질내사정, 5, 7)
ENDIF

;─
;성교중·성교봉사중 FIRSTEVENT(40)～
;─
;관련 변수…
;TEQUIP:성교중(조교자가 조교 대상으로 삽입중) (1=정상위/2=후배위/3=승마위/4=대면좌위/5=배면좌위/6=애널섹스)
;TEQUIP:성교봉사중(조교 대상이 조교자에게 삽입중) 내용은 상기의 것과 같음
;
;관련 함수…
;V_SEX (=TARGET, TEQUIP:성교중 < 6/=MASTER, TEQUIP:성교봉사중 < 6)와 동의
;A_SEX (=TARGET, TEQUIP:성교중 == 6/MASTER, TEQUIP:성교봉사중 == 6)와 동의
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;성교중(조교자→조교 대상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TEQUIP:성교중
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교봉사중(조교 대상→조교자)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TEQUIP:성교봉사중
ENDIF

;─
;절정 한 직후 FIRSTEVENT(50)～
;─
;·조교자, 조교 대상이 절정 했을 경우에 표시되는 구상
;·개별의 부위별 절정을 줍고 싶으면, IF 구상 기입 체크(0) && NOWEX:C절정등의 분기를 추가해 주세요
;·사정종은 NOWEX:사정 /NOWEX:MASTER:사정으로 참조하는 것이 가능합니다
;-------------------------------------------------
;관련 변수…
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;
;관련 함수…
;TRUECHECK 실로 되는 것이 몇개 있을까를 돌려준다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 절정 한 직후
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TRUECHECK(NOWEX:C절정, NOWEX:V절정, NOWEX:A절정, NOWEX:B절정) > 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 절정 한 직후
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TRUECHECK(NOWEX:MASTER:C절정, NOWEX:MASTER:V절정, NOWEX:MASTER:A절정, NOWEX:MASTER:B절정) > 0
ENDIF
[SKIPEND]

;─
;조교 방침 변경 FIRSTEVENT(60)
;─
;·조교 방침이 바뀌었을 때에 표시한다
;·어디까지나 방침인 것으로 행동을 세세하게 결정하는 것은 아니다
;-------------------------------------------------
;관련 변수…
;TFLAG:턴방침		이 턴의 조교 방침(1=휴게/2=소프트/3=노멀/4=하드/5=이상)
;TFLAG:전턴방침		전턴의 조교 방침(내용은 턴방침과 같음)
;
;관련 함수…
;IS_POLICY	조교 방침 명칭과 합치하면 진을 돌려준다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 방침 변경
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:턴방침 != TFLAG:전턴방침
	;=================================================
	;턴방침 1:휴게(확실히 체력 회복 행동이 된다)
	;=================================================
	IF 구상기입체크(30) && IS_POLICY("지금", "휴게")
		PRINTFORMW 「조금 휴게로 할까」
		PRINTFORMW 「너무 노력해 녹초가 되어져도 곤란하고」
		RETURN 1
	ENDIF

	;=================================================
	;턴방침 2:소프트
	;=================================================
	IF 구상기입체크(30) && IS_POLICY("지금", "소프트")
		PRINTFORMW 「조금 녹초가 되고 있기 때문에 소프트에 갑시다」
		PRINTFORMW 「당황해도 괜찮은 일 없어?」
		RETURN 1
	ENDIF

	;=================================================
	;턴방침 3:노멀
	;=================================================
	IF 구상기입체크(30) && IS_POLICY("지금", "노멀")
		PRINTFORMW 「응─」
		PRINTFORMW 「역시 보통이 제일일지도보통이」
		RETURN 1
	ENDIF

	;=================================================
	;턴방침 4:하드
	;=================================================
	IF 구상기입체크(30) && IS_POLICY("지금", "하드")
		PRINTFORMW 「뭔가 따분하네요…」
		PRINTFORMW 「도 조금 하드하게 말해 볼까」
		RETURN 1
	ENDIF

	;=================================================
	;턴방침 5:이상
	;=================================================
	;IF 구상 기입 체크(0) && IS_POLICY("지금", "이상")
	;ENDIF

[SKIPSTART]
	//현재는 실장되어 있지 않은 턴방침
	;=================================================
	;SM
	;=================================================
	IF 구상 기입 체크(0) && TFLAG:턴방침 == 6
		PRINTFORMW 「뭔가 공연히 너를 괴롭히고 싶어져 왔어요」
		PRINTFORMW 「우선 엉덩이전용 없음 있고 엉덩이, 이야기는 그리고」
		RETURN 1
	ENDIF
[SKIPEND]
ENDIF

;─
;조건 없음 FIRSTEVENT(70)～
;─
;·여기까지의 조건에 히트 하지 않았던 경우에 표시되는 개소
;·BASE로 소분기를 마련해 있습니다만, 임의의 조건을 추가하는 일도 할 수 있습니다
;------------------------------------------------
;관련 변수…
;BASE		(0, 체력/1, 기력/2, 사정/3, 모유/4, 소변욕/5, 이성/6, 흥미/7, 초조/8, 만족)
;
;관련 함수…
;TENSION	인수로 지정한 BASE치를 나타낸다(50%이상은 높은,25%이상은 일반,25% 미만은 낮다)
;------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조건 없음
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF