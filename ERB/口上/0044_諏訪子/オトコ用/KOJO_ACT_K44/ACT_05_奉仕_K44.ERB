;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와자 구상 봉사계 액션 구상 파일
;
;	봉사계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;봉사계 ACT50~57
;========================================================================
;·TFLAG:ACT(50=손으로 애무를 강제/51=입으로 애무를 강제/52=파이즈리/53=가랑이/55=다리 빨고 강제/56=이라마치오/57=가슴 애무 강제)
;·TFLAG:ACT 분류 == 5에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "봉사")로 줍는 것이 가능
;·조교 대상으로, 조교자의 신체를 애무하도록(듯이) 명령한다
;·기본적으로는 명령인 것으로, 이 단계에서는 따를지 어떨지 확정하고 있지 않다(ACT56, 이라마치오는 거부 할 수 없다)
;-------------------------------------------------
;·ACT54, 풋잡은 12/04/08판으로 숙청
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;─
;ACT50, 손으로 애무를 강제
;─
;·애무 강제/자위 강제/페니스 밴드 자위 강제
;·조교 대상(MASTER)으로, 손에 의한 애무를 강제한다
;·조교자에게 페니스가 있으면 반드시 자위 강제가 된다(페니스가 있으면 통상의 애무 강제는 일어나지 않는다)
;·조교자가 페니스 밴드 장착중은 페니스 밴드 자위 강제가 된다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "하드", "이상"이라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(줄/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교 대상이 따르는지 아닌지는 이 시점에서는 미정
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_50
;-------------------------------------------------
;·TFLAG:ACT 파생(0=애무 강제/1=자위 강제/2=페니스 밴드 자위 강제)
;-------------------------------------------------
;·지문은 ACT 파생마다 분기. 각 ACT 파생내에서는 분기 없음
;·ACT 파생 0, 애무때만 쾌B/쾌ASOURCE 증가
;·그 외의 경우는 쾌C한정
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_0, 애무 강제 「자신의 성기를 손으로 애무하라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:TARGET:갈것같다
		CALL HTMLPRINTW(@"「응하아… 야… 이제(벌써) 갈 것 같다…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「좀 더 가득함 깨어…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(3, 10)
			CASE 0
				PRINTFORMW 「나의 몸… 손대어 줄래?」
				CALL HTMLPRINTW(@"「좋을대로해도 좋으니까%HTML_HEARTB()%」")
			CASE 1
				PRINTFORMW 「요철 없기 때문에 재미없을지도 모르지만…」
				CALL HTMLPRINTW(@"「가득함 깨어… 기분 좋게 해%HTML_HEARTB()%」")
			CASE 2
				CALL HTMLPRINTW(@"「이봐요, 좋아하게 손대어%HTML_HEARTB()%」")
				PRINTFORMW 「기분 좋게 할 수 있을까나?」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_1, 자위 강제 「자신의 남성기를 손으로 잡아당기라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT 파생 == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_2, 페니스 밴드 자위 강제 「페니스 밴드를 손으로 잡아 움직이라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT 파생 == 2
ENDIF
[SKIPEND]

;─
;ACT51, 입으로 애무를 강제
;─
;·쿤닐링구스/펠라치오/안면 승마/안면 승마 애널/
;·조교 대상으로, 입에 의한 애무를 강제한다
;·조교자가 후타나리의 경우, 조교 대상이 위로 돌리고라면 1/2으로 안면 승마, 그렇지 않으면 페라 강제가 된다
;·조교자에게 페니스가 없고, 조교 대상이 위로 돌리고라면 안면 승마가 된다
;·안면 승마가 선택되고 있었을 때, 안면 승마 애널에 변화하는 일이 있다
;·모든 조건으로부터 빠졌을 경우는 통상의 쿤닐링구스가 된다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각 목마) 장착중은 발생하지 않는다
;·조교자가 페니스 밴드를 장착하고 있는 경우는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_51
;-------------------------------------------------
;·TFLAG:ACT 파생(0=쿤닐링구스/1=페라/2=안면 승마/3=안면 승마 애널)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_0, 쿤닐링구스 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	SELECTCASE RANDACT(3, 0)
		CASE 0
			PRINTFORMW 「나의 여기… 입으로 하고 주어?」
			CALL HTMLPRINTW(@"「이봐요, 빨리%HTML_HEARTB()%」")
		CASE 1
			PRINTFORMW 「무릎 꿇어 날름날름 해 주지 않을래?」
			CALL HTMLPRINTW(@"「할 수 있겠죠?   하고 싶지요 사실은%HTML_HEARTB()%」")
		CASE 2
			CALL HTMLPRINTW(@"「보지 쑤셔 어쩔 수 없는거야…%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「저기… 빨아%HTML_HEARTB()%」")
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_1, 펠라치오
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:ACT 파생 == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_2, 안면 승마
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	PRINTFORMW 「이봐요, 마음껏 날름날름 해도 좋아」
	CALL HTMLPRINTW(@"「응우…%HTML_HEARTB()%  코가 맞고 있다…%HTML_HEARTB()%」")
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_3, 안면 승마 애널
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	PRINTFORMW 「엉덩이의 구멍도 빨기를 원하는거야」
	CALL HTMLPRINTW(@"「혐 같은거 말하게 하지 않아?   이봐요 이봐요%HTML_HEARTB()%」")
	RETURN 1
ENDIF

;─
;ACT52, 파이즈리
;─
;·조교 대상으로 파이즈리에서의 봉사를 강제한다
;·조교 대상이 남자의 경우는 발생하지 않는다
;·조교자에게 페니스가 없는 경우는 발생하지 않는다
;·턴 방침이 "이상"의 경우는 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(줄/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
;@KOJO_MALE_ACT_K44_52
;-------------------------------------------------
;남자 한정 구상에서는 불요
;RETURN 0

;─
;ACT53, 가랑이
;─
;·조교 대상으로 페니스 밴드, 혹은 페니스를 가랑이에 끼워 스치고라고 명령한다
;·조교자에게 봉이 없다(페니스/페니스 밴드) 경우는 발생하지 않는다
;·조교 대상으로 페니스가 있는 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "이상"의 경우는 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:삼각 목마 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교 대상으로 다리가 없으면 발생하지 않는다(현재 상태로서는도자고가 조교 대상인 경우만이라고 생각된다)
;·REACT 분류가 거부/날뛴다/도망치는인 경우는 거절 취급
;-------------------------------------------------
;@KOJO_MALE_ACT_K44_53
;-------------------------------------------------
;·TFLAG:ACT 파생(0=가랑이/1=페니스 밴드 가랑이)
;-------------------------------------------------
;남자 한정 구상에서는 불요
;RETURN 0

;─
;ACT55, 다리 빨고 강제
;─
;·조교자가 다리를 빨라고 명령해 온다
;·이상계로부터 발생하는 경우 있어
;·턴 방침이 "소프트"라면 발생하지 않는다
;·오늘의 방침이 "러브러브"라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교자에게 다리가 없는 경우는 발생하지 않는다
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급
;-------------------------------------------------
@KOJO_MALE_ACT_K44_55
;-------------------------------------------------
;·ACT 파생 없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT55, 다리 빨고 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	PRINTFORMW 「네. … 이봐요 뭐 하고 있는거야」
	;오리지날 =>「빨리 다리 빠세요♪」설명 수상한 감이 있으므로 『다리』는 삭제
	;시추에이션적으로 『다리』를 빠는 것이 전제가 되어 있으므로
	PRINTFORMW 「빨리 빠세요♪」
	RETURN 1
ENDIF

;─
;ACT56, 이라마치오
;─
;·명령하는 것은 아니고, 억지로 페니스를 입에 돌진한다
;·조교 대상의 반응은 미정이지만, 거절하는 일은 할 수 없다
;·가학계로부터 발생하는 경우 있어
;·턴 방침이 "노멀", "소프트"라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각 목마) 장착중에는 발생하지 않는다
;·성교중/성교 봉사중에는 발생하지 않는다
;·조교자에게 페니스가 없는 경우는 발생하지 않는다(페니스 밴드에 의한 이라마치오는 없다)
;-------------------------------------------------
@KOJO_MALE_ACT_K44_56
;-------------------------------------------------
RETURN 0

;─
;ACT57, 가슴 애무 강제
;─
;·조교 대상으로, 조교자의 가슴을 애무하도록(듯이) 강제한다
;·조교자가 남자의 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴 방침이 "이상"이라면 발생하지 않는다
;·밀어 넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:줄장착중은 입으로 가슴 애무 강제에 파생한다. 다만 볼개그를 장착중의 경우는 튕겨진다
;·볼개그가 없으면,1/2의 확률로 입으로 애무가 된다
;·REACT 분류가 거부/날뛴다/도망치는 것 경우는 거절 취급
;·PALAM가 치정선행이나 반항 선행인가로"부끄러워서 봉사 할 수 없었다/싫어해 요구에 응하지 않았다"에 파생한다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_57
;-------------------------------------------------
;·TFLAG:ACT 파생(0=손으로 가슴 애무 강제/2=입으로 가슴 애무 강제)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_0, 손으로 가슴 애무를 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생

	;=================================================
	;조교 대상 삽입중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		CALL HTMLPRINTW(@"「얏…%HTML_HEARTB()%  하%HTML_HEART()%  젖가슴…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「젖가슴도 만지작거려…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && PALAM:쾌B > 8500
		CALL HTMLPRINTW(@"「…%HTML_HEARTB()%  응…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「유두… 오돌오돌 하고 가게 해…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「조금 전부터 가슴뿐 봐…」
		PRINTFORMW 「그렇게 신경이 쓰인다면 손대어 줄래?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_2, 입으로 가슴 애무를 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;=================================================
	;조교 대상 삽입중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		CALL HTMLPRINTW(@"「읏…%HTML_HEARTB()%  젖가슴 들이마셔%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「츄우츄우 하면서 푹푹 해엣%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && PALAM:쾌B > 8500
		CALL HTMLPRINTW(@"「하앗…%HTML_HEARTB()%  후~아%HTML_HEARTB()%  이제 안돼에%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「젖가슴 들이마셔…%HTML_HEARTB()%  츄우츄우 하고 가게 해%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「이런\@ TALENT:TARGET:빈유 ? 작다# \@젖가슴에서도 들이마시고 싶은거야?」
		CALL HTMLPRINTW(@"「어쩔 수 없네요… 이봐요%HTML_HEARTB()%」")
		RETURN 1
	ENDIF
ENDIF