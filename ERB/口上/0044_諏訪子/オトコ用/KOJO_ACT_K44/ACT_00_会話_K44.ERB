;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	諏訪子口上 会話系アクション口上ファイル
;
;	会話系アクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;会話系 ACT0～9
;========================================================================
;・TFLAG:ACT(0=今の気分を聞く/1=自分への気持ちを聞く/2=性についての話をする/3=優しく慰める/4=厳しく脅す/5=衣装の変更を要求する/6=合意を求める/7=お仕置きと宣言する/8=怒鳴る/9=不気味に笑う)
;・TFLAG:ACT分類 == 0に該当
;・ACT9, 不気味に笑うは異常系ACT分類から派生する場合がある
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "会話")で拾う事が可能
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では口上側のRESULT(終端のRETURN 1など)によって口上が表示されたのか否か、
;・また箇所毎の特有の挙動(媚薬等口上をスキップなど)を制御しています
;・@口上記入チェックの場合代入処理を省略できますが、RESULT値は口上側で決めうちする必要があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------
;────────────────────────────────────
;ACT0, 今の気分を聞く
;────────────────────────────────────
;・今の気分について聞いてくる
;・調教対象の反応に応じてREACT以降でACT派生への代入が行われる
;・ターン方針が"異常"なら発生しない
;-------------------------------------------------
@KOJO_MALE_ACT_K44_0
;-------------------------------------------------
;関数@TARGET_DESCRIPTIONによってTEQUIP(性交中/調教対象Ｃ使用/性交奉仕中/自慰中 & 2)
;@EQUIPACTION_DESCRIPTIONによってTEQUIP(顔面騎乗/顔面騎乗アナル/自慰中)で地の文は変化する可能性がある
;全て埋める必要はないので、その他・条件なしを最初に埋めた上で、あとは好みで分岐を増減してください
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 今の気分を聞く
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「さてと」
		PRINTFORMW 「これから始めるわけだけど、今の気分でも教えて貰おうかしら？」
		RETURN 1
	ENDIF

	;=================================================
	;調教内初回
	;=================================================
	IF 口上記入チェック(100) && ONCEACT(0)
		PRINTFORMW 「さーて、始めるに当たって今の気分はどう？」
		PRINTFORMW 「正直に言ってみ？ん？」
		RETURN 1
	ENDIF

	;=================================================
	;性交奉仕系で膣内射精の後
	;=================================================
	IF 口上記入チェック(100) && TFLAG:前回中出し == 2
		CALL HTMLPRINTW(@"「何の躊躇もなくこんなに射精しちゃってぇ…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「気分はどうなの？このスケベ%HTML_HEART()%」")
		RETURN 1
	ENDIF

	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		;イきそうなのを我慢している
		IF TCVAR:MASTER:我慢
			CALL HTMLPRINTW(@"「健気にガマンしちゃって…%HTML_HEARTB()%」")
			PRINTFORMW 「今どういう気分？出したいの？」
		ELSE
			CALL HTMLPRINTW(@"「キンタマ上がってもう射精ちゃいそうじゃないの%HTML_HEART()%」")
			PRINTFORMW 「ねぇ今どんな気分？」
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;勃起
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:勃起度 >= 1000
		CALL HTMLPRINTW(@"「こんなにカタくチンポ勃起させちゃって…%HTML_HEARTB()%」")
		PRINTFORMW 「ねぇどんな気分なのよ？」
		RETURN LOCAL
	ENDIF

	;=================================================
	;萎えた
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:勃起度 < TCVAR:MASTER:前の勃起度
		PRINTFORMW 「えっ嘘っ」
		PRINTFORMW 「何か気に障ったかな？」
		RETURN 1
	ENDIF

	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「そーねぇ、じゃあ…」
		PRINTFORMW 「今の気分はどうなの？」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT1, 自分への気持ちを聞く
;────────────────────────────────────
;・自分のことをどう思うか聞く
;・調教対象の反応に応じてREACT以降でACT派生への代入が行われる
;・ターン方針が"異常"なら発生しない
;・話題『好意』が既出となり、以後、同一調教中では抑制される
;-------------------------------------------------
@KOJO_MALE_ACT_K44_1
;-------------------------------------------------
;関数@TARGET_DESCRIPTIONによってTEQUIP(性交中/調教対象Ｃ使用/性交奉仕中/自慰中 & 2)
;@EQUIPACTION_DESCRIPTIONによってTEQUIP(顔面騎乗/顔面騎乗アナル/自慰中)で地の文は変化する可能性がある
;全て埋める必要はないので、その他・条件なしを最初に埋めた上で、あとは好みで分岐を増減してください
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 自分への気持ちを聞く
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTACT(0)
		PRINTFORMW 「じゃあまず手始めに…」
		PRINTFORMW 「私のことどう思う？正直に言ってね？」
		RETURN 1
	ENDIF

	;=================================================
	;その調教の最初のアクション
	;=================================================
	IF 口上記入チェック(100) && TFLAG:経過時間 == 1
		PRINTFORMW 「そうねぇまずは…」
		PRINTFORMW 「私のことどう思うか教えて貰おうかな♪」
		RETURN 1
	ENDIF

	;=================================================
	;性交奉仕系で膣内射精の後
	;=================================================
	IF 口上記入チェック(100) && TFLAG:前回中出し == 2
		CALL HTMLPRINTW(@"「もーこんなにぃ…%HTML_HEART()%」")
		PRINTFORMW 「私のはどうだった？」
		RETURN 1
	ENDIF

	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		;イきそうなのを我慢している
		IF TCVAR:MASTER:我慢
			CALL HTMLPRINTW(@"「ガマンしちゃってるねぇ…%HTML_HEARTB()%」")
			PRINTFORMW 「私で興奮しちゃってるの？」
		ELSE
			PRINTFORMW 「あは、もうイきそう？」
			PRINTFORMW 「私で興奮しちゃった？」
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;勃起
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:勃起度 >= 1000
		CALL HTMLPRINTW(@"「こーんなチンポおっ勃てちゃってぇ%HTML_HEARTB()%」")
		PRINTFORMW 「ちっちゃな体でも興奮しちゃったの？」
		RETURN 1
	ENDIF

	;=================================================
	;萎えた
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:勃起度 < TCVAR:MASTER:前の勃起度
		PRINTFORMW 「萎えた…だって…？」
		PRINTFORMW 「やっぱし早苗みたいなおっぱいがないとダメなの？」
		RETURN 1
	ENDIF

	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「早苗や神奈子みたいなおっぱいは無いけど」
		PRINTFORMW 「私のことどう思うかな？」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT2, 性についての話をする
;────────────────────────────────────
;・調教者が性についての知識を語り始める
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"異常"だと発生しない
;・調教者に性知識が無いと発生しない
;・話題『知識』が既出となり、以後、同一調教内では抑制される
;-------------------------------------------------
@KOJO_MALE_ACT_K44_2
;-------------------------------------------------
;関数@TARGET_DESCRIPTIONによってTEQUIP(性交中/調教対象Ｃ使用/性交奉仕中/自慰中 & 2)
;@EQUIPACTION_DESCRIPTIONによってTEQUIP(顔面騎乗/顔面騎乗アナル/自慰中)で地の文は変化する可能性がある
;全て埋める必要はないので、その他・条件なしを最初に埋めた上で、あとは好みで分岐を増減してください
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT2, 性についての話をする
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	PRINTFORMW 「イイこと教えてあげよっか♪」
	PRINTFORMW 「ちょっと耳貸してみ」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT3, 優しく慰める
;────────────────────────────────────
;・調教者が優しい言葉で慰めてくる
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"ハード", "異常"だと発生しない
;・話題『慰め』が既出となり、以後、同一調教内では抑制される
;-------------------------------------------------
@KOJO_MALE_ACT_K44_3
;-------------------------------------------------
;関数@TARGET_DESCRIPTIONによってTEQUIP(性交中/調教対象Ｃ使用/性交奉仕中/自慰中 & 2)
;@EQUIPACTION_DESCRIPTIONによってTEQUIP(顔面騎乗/顔面騎乗アナル/自慰中)で地の文は変化する可能性がある
;全て埋める必要はないので、その他・条件なしを最初に埋めた上で、あとは好みで分岐を増減してください
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 優しく慰める
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	;=================================================
	;その調教の最初のアクション
	;=================================================
	IF 口上記入チェック(100) && TFLAG:経過時間 == 1
		PRINTFORMW 「たまには優しく接してあげようかな」
		PRINTFORMW 「この薄い胸で甘えてもいいのよ？」
		RETURN 1
	ENDIF

	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「おおよしよし」
		PRINTFORMW 「ちょっとだけ甘えさせてあげるよ♪」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT4, 厳しく脅す
;────────────────────────────────────
;・厳しい言葉で脅す
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"ソフト", "異常"だと発生しない
;・話題『脅す』が既出となり、以後、同一調教内では抑制される
;-------------------------------------------------
@KOJO_MALE_ACT_K44_4
;-------------------------------------------------
;関数@TARGET_DESCRIPTIONによってTEQUIP(性交中/調教対象Ｃ使用/性交奉仕中/自慰中 & 2)で地の文は変化する可能性がある
;その他・条件なしを最初に埋めた上で、あとは好みで分岐を増減してください
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 厳しく脅す
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	PRINTFORMW 「そんな調子じゃ早苗は諦めてもらうよ？」
	PRINTFORMW 「お遊戯的な事なら他所でやって貰える？」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT5, 衣装の変更を要求する
;────────────────────────────────────
;・調教者が衣装の変更を要求してくる
;・調教対象の反応に応じて、何枚脱ぐか、あるいは脱がないのかにREACT以降で派生する
;・TFLAG:ACT派生(0=服を脱ぐように命令/1=服を着るように命令)
;・TEQUIP:押し倒し中には発生しない
;・TEQUIP:縄が真になっていると発生しない
;-------------------------------------------------
@KOJO_MALE_ACT_K44_5
;-------------------------------------------------
;地の文は"%CALLNAME:TARGET%は%CALLNAME:MASTER%に服を脱いでと命令した…"となっており、これが変化する事はない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 服を脱ぐように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;その調教の最初のアクション
	;=================================================
	IF 口上記入チェック(100) && TFLAG:経過時間 == 1
		PRINTFORMW 「いきなりだけど脱いじゃいなさい♪」
		PRINTFORMW 「どーせ後で脱ぐんだし」
		RETURN 1
	ENDIF

	;=================================================
	;勃起
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:勃起度 >= 1000
		CALL HTMLPRINTW(@"「もーこんなに勃起させちゃってるじゃないのよ…%HTML_HEARTB()%」")
		PRINTFORMW 「余計な物脱いじゃおうよ」
		RETURN 1
	ENDIF

	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「服なんて着ててもジャマでしょ？」
		PRINTFORMW 「さっさと脱いじゃいなさいよ♪」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT5_1, 服を着るように命令
;────────────────────────────────────
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 服を着るように命令
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
	PRINTFORMW 「ちょっと肌寒いわね…」
	PRINTFORMW 「着ても構わないよ？」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT6, 合意を求める
;────────────────────────────────────
;・初めてを貰ってもいいかと聞く
;・初めて＝処女or童貞(両方の場合もある)
;・既に合意を得ている場合は発生しない
;・調教対象が処女でも童貞でもない場合は発生しない
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"異常"だと発生しない
;・話題『合意』が既出となり、以後、同一調教内では抑制される
;-------------------------------------------------
@KOJO_MALE_ACT_K44_6
;-------------------------------------------------
;関数@TARGET_DESCRIPTIONでTEQUIP(性交中/調教対象Ｃ使用/性交奉仕中/自慰中 & 2)で地の文は変化する
;あとは好みで分岐を増減してください
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 合意を求める
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	;=================================================
	;調教対象がオトコの場合
	;=================================================
	IF 口上記入チェック(100) && TALENT:MASTER:オトコ
		PRINTFORMW 「え、マジ童貞だったの！？」
		PRINTFORMW 「いや別に構わないけど、それなら一応聞いておくよ」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT7, お仕置きと宣言する
;────────────────────────────────────
;・これからお仕置きすると言い出す
;・このACTが発生している時点でTFLAG:お仕置きフラグは立っている
;・ターン方針が"ソフト", "異常"だと発生しない
;・TEQUIP:押し倒し中時には発生しない
;-------------------------------------------------
@KOJO_MALE_ACT_K44_7
;-------------------------------------------------
;ここでは地の文の変化はない。分岐の増減はお好みで
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, お仕置きと宣言する
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	;=================================================
	;前COMが「無愛想に応答する/強気に応答する/嫌がる/暴れる/拒否する」のいずれか
	;=================================================
	;IF 口上記入チェック(0) && IS_PREVCOMNAME("無愛想に応答する/強気に応答する/嫌がる/暴れる/拒否する")
	;ENDIF

	;=================================================
	;前COMが「気持ち良くして」（この恥知らず！）
	;=================================================
	IF 口上記入チェック(100) && IS_PREVCOMNAME("気持ち良くして")
		PRINTFORMW 「目先のエロい事に踊らされて簡単に目的見失うヤワな男にゃあ…」
		PRINTFORMW 「少し…現実見せてやらないとねぇ…」
		RETURN 1
	ENDIF

	;=================================================
	;前COMが「もっとハードにして」（この豚め！）
	;=================================================
	IF 口上記入チェック(100) && IS_PREVCOMNAME("もっとハードにして")
		PRINTFORMW 「言ったら悦ぶと思ってあえて言わなかったけど言ってやるよ」
		PRINTFORMW 「この豚野郎が！！」
		RETURN 1
	ENDIF

	;=================================================
	;前COMが「許しを乞う」（許してもらえませんでした）
	;=================================================
	IF 口上記入チェック(100) && IS_PREVCOMNAME("許しを乞う")
		PRINTFORMW 「世の中謝って許してもらえる事ばかりじゃあないんだよ」
		PRINTFORMW 「態度で示して貰おうかな…」
		RETURN 1
	ENDIF

	;=================================================
	;前COMが「自慰し始める」（人の話を聞け！）
	;=================================================
	IF 口上記入チェック(100) && IS_PREVCOMNAME("自慰し始める")
		PRINTFORMW 「言うこと聞かず勝手にシゴき始める猿にゃあ…」
		PRINTFORMW 「ちょっぴり躾が必要かもねぇ…」
		RETURN 1
	ENDIF

	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		PRINTFORMW 「テメーは私を怒らせた」
		PRINTFORMW 「ちょっとばかし痛い目にもあって貰おうかしら。何事も経験だよね」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT8, 怒鳴る
;────────────────────────────────────
;・大声で怒鳴る
;・ターン方針が"ソフト", "ノーマル"では発生しない
;・TFLAG:お仕置きフラグに関わらず、発生する可能性がある（お仕置き中にさらに怒鳴る可能性等）
;-------------------------------------------------
@KOJO_MALE_ACT_K44_8
;-------------------------------------------------
;地の文は"%CALLNAME:TARGET%は%CALLNAME:MASTER%を大声で怒鳴った…"のみで派生はない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 怒鳴る
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	PRINTFORMW 「このド畜生がッ！！」
	PRINTFORMW 「そんなんで早苗と釣り合うとでも思ってるの！？」
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT9, 不気味に笑う
;────────────────────────────────────
;・不気味な微笑を浮かべる
;・異常系から発生する場合もある
;・今日の方針が"イチャイチャ"だと発生しない
;・話題『嗤う』が既出となり、以後、同一調教内では抑制される
;-------------------------------------------------
@KOJO_MALE_ACT_K44_9
;-------------------------------------------------
;関数@TARGET_DESCRIPTIONによってTEQUIP(性交中/調教対象Ｃ使用/性交奉仕中/自慰中 & 2)で地の文は変化する
;その他・条件なしを最初に埋めた上で、あとは好みで分岐を増減してください
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 不気味に笑う
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100)
	PRINTFORMW 「フ、ウフフフ…なんか昔を思い出してきちゃった…」
	PRINTFORMW 「あの頃は色々やったっけなぁ…ウへへへヘヘ…♪」
	RETURN 1
ENDIF


[SKIPSTART]
R#私家版に実装されていたが、現行版では存在しないコマンドなのでコメントアウト
上手い具合に回収できそうならそちらに移したい意向@/L
;-------------------------------------------------
;ACT110, 愛を告げる FIRSTTIME(310)
;-------------------------------------------------
@KOJO_MALE_ACT_K32_110
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;-------------------------------------------------
	;小分岐
	;-------------------------------------------------
	;相思相愛
	;記入チェック
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && FAVOR(3000, 3000)
		PRINTFORMW 「こんな時言うのもなんだけどとっとと言っとくわ」
		PRINTFORMW 「愛してるわ貴方…冗談でも何でもないよ」
		CALL HTMLPRINTW(@"「早苗にだって渡したくないくらいに…ね%HTML_HEARTB()%」")
		PRINTFORMW 「ホントは貴方だって気づいてたんでしょ？…私のこの気持ちをさ」
		RETURN LOCAL
	ENDIF

;一般
	PRINTFORMW 「ねぇ…貴方は早苗に入れ込んでる訳だけど…」
	PRINTFORMW 「私のことはどう思ってるの…？」
	PRINTFORMW 「いや、師匠とかじゃなくて一人の女としてよ…」
	PRINTFORMW 「わ、私は…結構貴方のこと…す、スキよ…？」
	PRINTFORMW 「いや隙だらけのスキじゃないわよ空気読めこのスカポンタン」
	RETURN LOCAL
ENDIF
[SKIPEND]