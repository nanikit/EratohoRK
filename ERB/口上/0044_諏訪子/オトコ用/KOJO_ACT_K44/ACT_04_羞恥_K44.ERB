;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코 구상 수치계 액션 구상 파일
;
;	수치계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;수치계 ACT40～45
;========================================================================
;·TFLAG:ACT(40=자위/41=비패개장/42=자위과시하고/43=수치플레이/44=야외플레이/45=방뇨/46=코스프레(미실장))
;·TFLAG:ACT분류 == 4에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "수치")로 줍는 것이 가능
;·ACT에 따라서는 거부가 가능하고 있거나 않기도 하고
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;─
;ACT40, 자위
;─
;·자위 해 보여라라고 명령한다
;·이 단계에서 명령에 따르는지 아닌지는 미정
;·REACT분류가 소극적 따른다/태연하게 따른다/적극적 따른다/받아들이는 이외의 경우는, 모두 REACT분류 "거부"취급
;·턴방침이 "이상"이라면 발생하지 않는다
;·밀어넘어뜨리기중인 경우는 발생하지 않는다
;·벌써 자위중인 경우는 발생하지 않는다
;·TEQUIP(줄/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·이 ACT에 파생은 없다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_40
;-------------------------------------------------
;·지문에 분기는 없음
;·@TARGET_DESCRIPTION로 상황 묘사가 들어가는 경우 있어
;·조교 대상의 페니스의 유무, 페니스 없음의 경우는 페니스밴드의 유무에 의한 나누어 쓰고가 생각된다
;·조교 대상이 남자이면 쾌B는 발생하지 않는다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상으로 페니스가 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && PENIS(MASTER)
	;=================================================
	;벌모드
	;=================================================
	IF 구상기입체크(100) && TFLAG:벌플래그 && ONCEACT(0) ;//ONCE로 조교내 한 번만 표시
		PRINTFORMW 「혼자서 자지 훑어 내기 시작하세요」
		PRINTFORMW 「원숭이에는 잘 어울리는 모습이예요…」
		RETURN 1
	ENDIF

	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		PRINTFORMW 「사정하는 거야?   스스로 자지 마구 훑어 내 사정해 버리는 거야?」
		CALL HTMLPRINTW(@"「이봐요 이봐요 어떻게든 말하세요…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;조교자도 자위중
	;=================================================
	IF 구상기입체크(100) && GETBIT(TEQUIP:자위중, 1)
		CALL HTMLPRINTW(@"「아우읏%HTML_HEARTB()%… 쿠츗… 엣취%HTML_HEART()%」")
		CALL HTMLPRINTW(@"「너도… 함께 자위 해?   나의 일반찬으로 해도 좋으니까%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(3, 10)
			CASE 0
				PRINTFORMW 「그러면 스스로 훑고 있는 곳 보여 줄래?」
				CALL HTMLPRINTW(@"「언제나 하고 있는 것 같게로 좋으니까%HTML_HEARTB()%」")
			CASE 1
				PRINTFORMW 「혼자 엣치해 보고?」
				CALL HTMLPRINTW(@"「여기서 보고 있어 주기 때문에%HTML_HEARTB()%  응 빨리♪」")
			CASE 2
				PRINTFORMW 「창가 가 자위하세요」
				PRINTFORMW 「조금 전부터 자지 만지작거리고 싶지요?」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 페니스밴드를 장착하고 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TEQUIP:페니스밴드
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상으로 페니스가 없는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ! PENIS(MASTER)
ENDIF
[SKIPEND]

;─
;ACT41, 비패개장
;─
;·조교자가 조교 대상의 비패를 개장 한다. 소위 「쿠빠아」
;·조교 대상이 남자의 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"의 경우는 발생하지 않는다
;·TEQUIP:삼각목마 장착중은 발생하지 않는다
;·밀어넘어뜨리기중은 발생하지 않는다
;·성교중/성교봉사중은 발생하지 않는다
;·이 ACT에 파생은 없다
;·현재 리액션 후의@ACTION_APPLY2에 처리 없음
;-------------------------------------------------
;@KOJO_MALE_ACT_K44_41
;-------------------------------------------------
;·TEQUIP(안면승마/자위중)은 해제된다
;·하반신 전부 강제탈의의 처리 있어
;-------------------------------------------------
;RETURN 0

;─
;ACT42, 자위과시하고
;─
;·조교자가 과시하도록(듯이) 자위를 시작한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·조교자가 벌써 자위중인 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_42
;-------------------------------------------------
;조교자 욕망, 기교, 조교레벨, 조교자 기교, 기묘한손가락소질에 응해 쾌C/쾌BSOURCE 증가
;바이브 소지의 경우의 처리는 1.020판 시점에서는 봉인되고 있다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자에게 페니스가 없는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !PENIS(TARGET)
	;=================================================
	;조교 대상도 자위중
	;=================================================
	IF 구상기입체크(100) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 「그런 기분 좋은 것 같이 틴포시고 있는 것 보여지면…」
		CALL HTMLPRINTW(@"「읏%HTML_HEARTB()%  … 츄쿳…… 나도 하고 싶어져 버리지 않아…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(3, 10)
			CASE 0
				CALL HTMLPRINTW(@"「저기… 이봐요 봐아…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「내가 자위 하고 있다…%HTML_HEARTB()%」")
			CASE 1
				CALL HTMLPRINTW(@"「어때…?   내가 이런 일 하고 느껴 버리고 있는 곳…%HTML_HEARTB()%」")
				PRINTFORMW 「흥분해 버려…?」
			CASE 2
				PRINTFORMW 「뭔가 불끈불끈 해 와 버렸다…」
				CALL HTMLPRINTW(@"「읏%HTML_HEARTB()%  잘 봐 둬…%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;─
;ACT43, 수치플레이
;─
;·조교 대상을 대거울의 전에 데리고 간다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:삼각목마 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·이미 수치플레이 안이라면, 수치플레이 종료가 된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_43
;-------------------------------------------------
;·TFLAG:ACT파생(0=개시/1=종료)
;-------------------------------------------------
RETURN 0


;─
;ACT44, 야외플레이
;─
;·조교 대상으로 목걸이를 입어 밖에 데리고 나간다
;·턴방침이 "소프트", "노멀"이라면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(줄/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·이미 야외플레이 안이라면, 야외플레이 종료가 된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_44
;-------------------------------------------------
;·TFLAG:ACT파생(0=개시/1=종료)
;-------------------------------------------------
RETURN 0

;─
;ACT45, 방뇨
;─
;·오줌누어 보여라라고 명령한다
;·이 단계에서는 명령에 따를지 어떨지 확정하고 있지 않다
;·REACT분류가 소극적 따른다/태연하게 따른다/적극적 따른다/받아들이는 이외의 경우는, 모두 REACT분류 "거부"취급
;·턴방침이 "소프트"라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드) 장착중에는 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·소변욕(BASE:MASTER:소변욕)이 5000 이하라면 발생하지 않는다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_45
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 남자의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TALENT:MASTER:남자
	;=================================================
	;벌모드
	;=================================================
	IF 구상기입체크(100) && TFLAG:벌플래그 && ONCEACT(0)
		PRINTFORMW 「금방 소변 하고 있는 곳 보이세요」
		PRINTFORMW 「빨리 해. 나오지 않아도 근성으로 내」
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「금방 소변 하고 있는 곳 보여 줘?」
		CALL HTMLPRINTW(@"「물론 여기서%HTML_HEARTB()%」")
		RETURN 1
	ENDIF
ENDIF