;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와코 구상 애무계 액션 구상 파일
;
;	애무계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;애무계 ACT10～19 & 27
;========================================================================
;·TFLAG:ACT(10=손으로애무/11=가슴애무/12=입으로애무/13=애널애무/14=애널핥기/15=키스/16=성기를서로켠다/17=파이즈리한다/18=풋잡한다/19=손가락삽입해라/27=쓰러트린다)
;·TFLAG:ACT분류 == 1에 해당
;·ACT18, 풋잡은 이상계로부터 파생하는 경우 있어
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "애무")로 줍는 것이 가능
;·조교자가 조교 대상을 애무한다. 이 구상의 시점에서 조교자는 벌써 손을 대고 있다
;·미약을 사용하는 케이스가 있다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;────────────────────────────────────
;ACT10, 손으로애무(ACT10_0, 애무하는/ACT10_1, 자위 하는/ACT10_2, 페니스밴드 자위)
;────────────────────────────────────
;·Ｃ중심으로 애무한다
;·조교 대상으로 페니스가 있는 경우, 및 페니스밴드장착 때에는 파생 커맨드가 된다
;·조교 대상이 자위중의 경우는 해제된다
;·조교자의 손에 더러움이 있는 경우 불쾌 SOURCE가 변동한다
;·조교자 기교, 애무, 기묘한손가락소질에 응해 발기도 상승
;-------------------------------------------------
@KOJO_MALE_ACT_K44_10
;-------------------------------------------------
;·TFLAG:ACT파생(0=애무/1=자위/2=페니스밴드 자위)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_1, 자위 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·남성기에의 애무를 실시한다, 즉 손 애무
;·다른 파생과 비교해 SOURCE:MASTER:쾌Ｃ는+200%
;·조교자 기교, 애무, 기묘한손가락소질에 응해 발기도 상승
;-------------------------------------------------
;필요에 따라서, TEQUIP:오나홀이나 TCVAR:MASTER:발기도
;PANTSCHECK(1) 등으로 분기를 마련하는 일도 할 수 있습니다.
;온전히 하면 수고가 심상치 않아서, 효율 좋게 기술하는 일을 추천합니다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;위로돌리고
		IF TFLAG:위로돌리고
			;갈것같다인 것을 참고 있다
			IF TCVAR:MASTER:인내
				CALL HTMLPRINTW(@"「그렇게 빠르고는 한심해%HTML_HEARTB()%」")
				PRINTFORMW 「여기는 어떨까?   날름날름」
				PRINTFORMDW 스와코는 옥대를 포함해 혀로 굴리면서 초조하게 하도록(듯이) 훑고 있다…
			;그 외
			ELSE
				PRINTFORMW 「쫑긋쫑긋 시켜 버려 이제(벌써)갈것같다야?」
				PRINTFORMW 「조금 인내가 부족한 것이 아니야?」
				PRINTFORMDW 스와코는 한층 더 비틂을 더하면서 사정을 재촉하고 있다…
			ENDIF
		;통상
		ELSE
			;갈것같다인 것을 참고 있다
			IF TCVAR:MASTER:인내
				CALL HTMLPRINTW(@"「그렇게 안타까운 얼굴 되어 버린 만 없어%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「저기… 그 표정 좀 더 잘 보여 줘%HTML_HEARTB()%」")
				PRINTFORMDW 스와코는 귓전으로 요염하게 속삭이면서 초조하게 하도록(듯이) 훑고 있다…
			;그 외
			ELSE
				PRINTFORMW 「쫑긋쫑긋 시켜 버려 이제(벌써)갈것같다야?」
				PRINTFORMW 「조금 인내가 부족한 것이 아니야?」
				PRINTFORMDW 스와코는 한층 더 비틂을 더하면서 사정을 재촉하고 있다…
			ENDIF
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;쓰러트리기 상태
	;=================================================
	IF 구상기입체크(100) && TEQUIP:쓰러트리기중
		PRINTFORMW 「어때? 움직일 수 없지요♪」
		PRINTFORMW 「이런 상태로 쫄깃쫄깃 되고 있는 기분은 어때?」
		CALL HTMLPRINTW(@"「후후…%HTML_HEARTB()%  자꾸자꾸 딱딱해지고 있다%HTML_HEART()%」")
		RETURN 1
	ENDIF

	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		CALL HTMLPRINTW(@"「이제 이렇게 자지 펄떡펄떡해 버려%HTML_HEART()%」")
		CALL HTMLPRINTW(@"「쿠퍼액으로 이제 질척질척이 아니다%HTML_HEARTB()%」")
		PRINTFORMDW 스와코는 혀로 귀두의 첨단을 빨면서 일부러 소리를 내면서 훑어 내고 있다…
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(3, 10)
			CASE 0
				PRINTFORMW 「그러면 손으로 시라고 줄까」
				PRINTFORMW 「종류에, 종류에… 기분이 좋아?」」
			CASE 1
				PRINTFORMW 「이런 식으로 손대어져…」
				CALL HTMLPRINTW(@"「응, 좋은 반응%HTML_HEARTB()%」")
			CASE 2
				CALL HTMLPRINTW(@"「이봐요, 도망치지 않는거야%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「쫄깃쫄깃 해 와… 사랑스러운 자지다 일%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
남자 한정 구상인 것으로 comment out
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_2, 페니스밴드 자위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_0, 애무한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ! TFLAG:ACT파생
ENDIF
[SKIPEND]

;────────────────────────────────────
;ACT11, 가슴애무
;────────────────────────────────────
;·가슴애무/유두 들이마셔/젖의 몸싸움/유두 고문
;·특정의 조건으로 「유두 들이마셔」 「젖의 몸싸움」 「유두 고문」에 파생한다
;·조교 대상이 남자의 경우는 반드시 「유두 고문」(이)가 되어, 그 밖에 파생하는 일은 없다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_11
;-------------------------------------------------
;·TFLAG:ACT파생(0=가슴애무/1=유두 들이마셔/2=몸싸움/3=유두 고문)
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_0, 가슴애무
;━━━━━━━━━━━━━━━━━━━━━━━━
;·모든 파생 조건으로부터 빠지면 통상의 가슴애무가 된다
;·조교자의 욕망+Ｂ감각이 3이상의 경우 1/2의 확률로 몸싸움에 파생
;·그리고 새었을 경우에게만, 통상의 가슴애무가 된다
;-------------------------------------------------
IF 구상 기입 체크(0) && ! TFLAG:ACT파생
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_1, 유두 들이마셔
;━━━━━━━━━━━━━━━━━━━━━━━━
;·전회 ACT가 가슴애무의 경우 유두계가 된다
;·그 때, 조교자에게 유치한가 혀사용이 있으면 3/4, 없으면 1/4으로 유두흡실 된다
;-------------------------------------------------
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_2, 젖의 몸싸움
;━━━━━━━━━━━━━━━━━━━━━━━━
;·가슴의 몸싸움을 요구한다. 이 시점에서의 조교 대상의 반응은 미정
;·유두계의 조건으로부터 빠졌을 경우, 조교자의 욕망+Ｂ감각 3이상이라면,1/2그리고 가슴의 몸싸움이 된다
;-------------------------------------------------
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_3, 유두 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
;·전회 ACT가 가슴애무의 경우 유두계가 된다
;·그 때, 조교자에게 유치한가 혀사용이 있으면 1/4, 없으면 3/4으로 유두 고문이 된다
;·조교 대상이 남자의 경우는 유두 꾸짖어 밖에 되지 않는다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	SELECTCASE RANDACT(3, 30)
		CASE 0
			PRINTFORMW 「유두도 만지작거려 준다」
			PRINTFORMW 「가득 느껴도 괜찮아요?」
		CASE 1
			CALL HTMLPRINTW(@"「하는 김에 가슴도 개발 해 준다…%HTML_HEARTB()%」")
			CALL HTMLPRINTW(@"「좀 더 기분 좋게 될 수 있다%HTML_HEART()%」")
		CASE 2
			PRINTFORMW 「이런 식으로군요인 체해지면…」
			CALL HTMLPRINTW(@"「어떤 반응할까%HTML_HEARTB()%」")
	ENDSELECT
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT12, 입으로애무
;────────────────────────────────────
;·쿤닐링구스한다/펠라치오 한다
;·하반신의 의상이 남아 있어도 발생하는 일이 있다. 조교자에게 TALENT:벽통과가 없는 경우는 비켜 놓아 실행한다
;·턴방침이 "이상"이라면 발생하지 않는다
;·TEQUIP(바이브/오나홀/페니스밴드/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교 대상으로 페니스가 있으면, 반드시 페라가 된다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_12
;-------------------------------------------------
;·TFLAG:ACT파생(0=쿤닐링구스/1=페라)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT12_0, 쿤닐링구스한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ! TFLAG:ACT파생
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT12_1, 펠라치오 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;가게해！선택시
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("가게해！")
		SELECTCASE TCVAR:MASTER:애태움수
			;애태움중 1
			CASE IS > 1
				CALL HTMLPRINTW(@"「이제(벌써)… 어쩔 수 없네요…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「좀 더 인내를 기억하세요 이 조루%HTML_HEARTB()%」")
			;애태움 종료
			CASE 1
				CALL HTMLPRINTW(@"「슬슬 좋네요…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「들 해%HTML_HEART()%  자지로부터 가득 정액 사정해엣%HTML_HEARTB()%」")
				PRINTFORMDW 스와코는 령구의 첨단에 혀를 붙이면서 격렬하게 잡아당기기 시작했다…
			;초조하게함 없음
			CASEELSE
				PRINTFORMW 「자지 쫑긋쫑긋 말하게 해 보기 흉하다고 생각하지 않는거야?」
				CALL HTMLPRINTW(@"「자신으로부터 허리 움직여 버려…%HTML_HEARTB()%  」")
				PRINTFORMDW 스와코는 육봉의 근원을 억압째면서 성기 뒷부분을 중점적으로 핥아대고 있다…
		ENDSELECT
		RETURN 1
	ENDIF

	;=================================================
	;조교 대상이갈것같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;위로돌리고 상태
		IF TFLAG:위로돌리고
			;갈것같다인 것을 참고 있다
			IF TCVAR:MASTER:인내
				PRINTFORMW 「참는다면 끝까지 노력하세요♪」
				CALL HTMLPRINTW(@"「사내 아이겠지?   %HTML_HEARTB()%」")
				PRINTFORMDW 스와코는 혀를 날카롭게 할 수 있는 요도구를 보전 반복하도록(듯이) 빨고 있다…
			;그 외
			ELSE
				CALL HTMLPRINTW(@"「아─아 움찔움찔 말하게 해 버려%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「갈것같다? 사정해 버리고 싶어?   후후…%HTML_HEART()%  」")
				PRINTFORMDW 스와코는 추격을 곱하도록(듯이) 머리를 흔들어 달라붙어 왔다…
			ENDIF
		;통상
		ELSE
			;갈것같다인 것을 참고 있다
			IF TCVAR:MASTER:인내
				CALL HTMLPRINTW(@"「날름…%HTML_HEARTB()%  자꾸자꾸 맛 진해져 오고 있다%HTML_HEARTB()%」")
				PRINTFORMW 「노력해 참아 주세요♪」
			;그 외
			ELSE
				CALL HTMLPRINTW(@"「아─아 움찔움찔 말하게 해 버려%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「갈것같다?   사정해 버리고 싶어?   후후%HTML_HEARTB()%  」")
				PRINTFORMDW 스와코는 추격을 곱하도록(듯이) 머리를 흔들어 달라붙어 왔다…
			ENDIF
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;동시에 애널애무
	;=================================================
	IF 구상기입체크(100) && TFLAG:추가행동 == 3
		;일반 추가행동구상 처리필 체크(일반 추가행동의 지문 컷 할 때는 TFLAG:추가행동지문스킵의 코멘트를 해제)
		CLEARBIT TFLAG:추가행동구상, 0
		;CLEARBIT TFLAG:추가행동지문스킵, 0
		CALL HTMLPRINTW(@"「하─는─김─에─개도%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「아핫%HTML_HEARTB()%  어때? 좋지요%HTML_HEART()%」")
		RETURN 1
	ENDIF

	;=================================================
	;쓰러트리기 상태
	;=================================================
	IF 구상기입체크(100) && TEQUIP:쓰러트리기중
		CALL HTMLPRINTW(@"「저항 할 수 없지요%HTML_HEART()%」")
		CALL HTMLPRINTW(@"「이렇게 해 장대도… 츄푸풋…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「타마도… 날름날름…%HTML_HEARTB()%  마음껏 괴롭혀 준다%HTML_HEART()%」")
		RETURN 1
	ENDIF

	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		CALL HTMLPRINTW(@"「이렇게 빠직빠직 딱딱하게 해 버려%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「보람이 있지 않아…%HTML_HEART()%」")
		CALL HTMLPRINTW(@"「아하♪  미끈미끈 너무 시키고%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;그 외
		SELECTCASE RANDACT(3, 10)
			CASE 0
				CALL HTMLPRINTW(@"「조금 전부터 빨기를 원했던 것이겠지…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「날름… 쥬풋… 후후, 사랑스러운거야%HTML_HEART()%」")
			CASE 1
				PRINTFORMW 「빨려지는 것과 들이마셔지는 것 어느 쪽을 좋아해?」
				CALL HTMLPRINTW(@"「아무튼 어느 쪽도 하지만 말야%HTML_HEARTB()%」")
			CASE 2
				PRINTFORMW 「슝슝… 읏, 조금 냄새나요?」
				CALL HTMLPRINTW(@"「뭐 나는 상관없지만 말야%HTML_HEARTB()%… 츄푸」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT13, 애널애무
;────────────────────────────────────
;·애널애무/전립선자극/애널바이브 출납
;·애널바이브 장비중은, 애널바이브 출납에 파생
;·연속 실행의 경우, 전립선자극에 파생
;·갈것같다or쾌Ｃ가 5000이상의 경우, 조교자의 애무 스킬에 응해 전립선자극에 파생
;·발기도가 낮은 경우, 조교자의 애무 스킬에 응해 전립선자극에 파생
;·요점강제발기의 경우, 전립선자극에 파생
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드/애널비즈) 장착중에는 발생하지 않는다
;·성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_13
;-------------------------------------------------
;·TFLAG:ACT파생（0=애널애무/1=전립선자극/2=애널바이브 출납）
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13, 애널애무/전립선자극/애널바이브 출납
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && INRANGE(TFLAG:ACT파생, 0, 2) ;파생을 정리해 처리해도 문제 없다고 판단@/L
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		;첫회 분기에 할당하는 것이 타당하다라고 판단@/L
		CALL HTMLPRINTW(@"「여기의 맛… 한 번 차지하면 푹 빠져요…%HTML_HEARTB()%」")
		PRINTFORMW 「시험해 보지 않을래?」
		RETURN 1
	ENDIF

	;=================================================
	;쓰러트리기 상태
	;=================================================
	IF 구상기입체크(100) && TEQUIP:쓰러트리기중
		CALL HTMLPRINTW(@"「이런 부끄러운 모습으로 애널 만지작거려지고 있다니…%HTML_HEART()%」")
		CALL HTMLPRINTW(@"「부모가 보면 어떤 얼굴 할까요%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		CALL HTMLPRINTW(@"「아하핫♪  엉덩이로 이렇게 펄떡펄떡해 버린 것이다%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「정말 변태인 것이니까%HTML_HEART()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(2, 10)
			CASE 0
				PRINTFORMW 「여기의 구멍도 만지작거려 준다♪」
				CALL HTMLPRINTW(@"「이봐, 도망치지 않는거야%HTML_HEARTB()%」")
			CASE 1
				PRINTFORMW 「뭔가 물건 갖고 싶은 듯이 하고 있지 않아」
				PRINTFORMW 「너의 코코가♪」
			[SKIPSTART]
			//첫회 분기에 전용
			;CASE 2
			;	CALL HTMLPRINTW(@" 「여기의 맛… 한 번 차지하면 푹 빠져요…%HTML_HEARTB()%」")
			;	PRINTFORMW 「시험해 보지 않을래?」
			[SKIPEND]
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_1, 전립선자극(애널애무)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_2, 애널바이브 출납
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
ENDIF
[SKIPEND]

;────────────────────────────────────
;ACT14, 애널핥기
;────────────────────────────────────
;·애널핥기
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP(애널바이브/애널비즈/관장기＋플러그/페니스밴드/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·TCVAR:MASTER:애태움수가 실로 되어 있는 경우는 발생하지 않는다
;·혀에 의한 전립선자극에의 파생은 「그런 곳까지 혀가 닿을 이유 없지 않은가www」라고 하는 일로 숙청되고 있습니다
;·현시점에서 ACT가 파생하는 일은 없지만, ACT 개변의 가능성에 대비해, TFLAG:ACT파생 == 0으로 지정
;-------------------------------------------------
@KOJO_MALE_ACT_K44_14
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT14_0, 애널핥기
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;동시에 자위
	;=================================================
	IF 구상기입체크(100) && PENIS(MASTER) && TFLAG:추가행동 == 1
		;일반 추가행동구상 처리필 체크(일반 추가행동의 지문 컷 할 때는 TFLAG:추가행동지문스킵의 코멘트를 해제)
		CLEARBIT TFLAG:추가행동구상, 0
		;CLEARBIT TFLAG:추가행동지문스킵, 0
		PRINTFORMW 「애널핥기면서 자지 잡아당겨지는 기분은 어떨까 ♪」
		CALL HTMLPRINTW(@"「날름날름… 이봐요 이봐요%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		PRINTFORMW 「이런 식으로 엉덩이의 구멍 빨려지면서 자지 발기시켜 버려…」
		PRINTFORMW 「최고로 보기 흉하다고 생각하지 않아?」
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「날름… 후후, 깜짝 놀랐어?」
		CALL HTMLPRINTW(@"「애널도 기분 좋게 해 준다…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT15, 키스
;────────────────────────────────────
;·키스/미약키스
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·조교자에게 TALENT:벽통과가 없는 경우, TEQUIP:볼개그 장착중은 튕겨진다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15_1, 미약키스
;━━━━━━━━━━━━━━━━━━━━━━━━
;·조교 대상으로 입으로 옮김으로 미약을 먹인다
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생 && GETBIT(TFLAG:미약등사용, 1)
	;미약사용 구상 처리필 체크(미약사용의 지문 컷 할 때는 TFLAG:미약등사용지문스킵의 코멘트를 해제)
	CLEARBIT TFLAG:미약등사용구상, 1
	;CLEARBIT TFLAG:미약등사용지문스킵, 1
	CALL HTMLPRINTW(@"「츄웃%HTML_HEARTB()%  쿨렁쿨렁 푼다…」")
	CALL HTMLPRINTW(@"「푸앗%HTML_HEART()%  무엇 먹여 모여들어…?   곧바로 안다%HTML_HEARTB()%」")
	;RESULT2를 돌려준다
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15_0, 키스
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생 && !GETBIT(TFLAG:미약등사용, 1)
	;=================================================
	;성교봉사중（조교 대상 삽입）
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		CALL HTMLPRINTW(@"「이봐요…%HTML_HEART()%  키스도…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「… 읏%HTML_HEARTB()%  응%HTML_HEART()%」")
		RETURN 1
	ENDIF

	;=================================================
	;위로돌리고
	;=================================================
	IF 구상기입체크(100) && TFLAG:위로돌리고
		PRINTFORMW 「이 상태는 도망칠 수 없네요…♪」
		CALL HTMLPRINTW(@"「입술 받아요… 츄…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(3, 10)
			CASE 0
				PRINTFORMW 「이봐요, 키스 해요…?」
				CALL HTMLPRINTW(@"「큰 일인 것이니까 성실하게 말야…%HTML_HEARTB()%」")
			CASE 1
				PRINTFORML 「응…」
				PRINTFORMW 「뭐 하고 있는거야, 키스로 정해져 있지 않아 설유하세요」
			CASE 2
				CALL HTMLPRINTW(@"「…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「이봐요, 분명하게 혀도 내…%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT16, 성기를서로켠다
;────────────────────────────────────
;·짝 맞추기 놀이/가랑이 한다
;·조교 대상으로 페니스가 있는 경우는 ACT파생 == 1 「가랑이 한다」에 파생한다
;·조교 대상이 페니스밴드장착 안은, ACT파생 == 2 「페니스밴드 가랑이」가 된다
;·턴방침이 "하드", "이상하다면 발생하지 않는다
;·조교 대상이 여자 아이로 바이브 장착중이라면 발생하지 않는다
;·TEQUIP(오나홀/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교자에게 페니스가 있으면 발생하지 않는다（봉사계의 가랑이 강제가 된다）
;-------------------------------------------------
@KOJO_MALE_ACT_K44_16
;-------------------------------------------------
;·TFLAG:ACT파생（=0, 조개 우려라/=1, 가랑이/=2, 페니스밴드 가랑이）
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_1, 가랑이 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·여성기와 넓적다리로 남성기를 사이에 두어 애무한다
;·조교 대상으로 페니스가 있으면, 반드시 가랑이 하는게 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;조교 대상이갈것같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;위로돌리고
		IF TFLAG:위로돌리고
			;갈것같다인 것을 참고 있다
			IF TCVAR:MASTER:인내
				CALL HTMLPRINTW(@"「위험햇, 클리 비비어져 멈추지 않는닷…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「이봐요, 힘내%HTML_HEART()%  좀 더 비비어%HTML_HEARTB()%」")
			;그 외
			ELSE
				CALL HTMLPRINTW(@"「조금 전부터 움찔움찔 시키고 있을 뿐이지 않아…%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「사정하는 거야?   나무와 나무 사이는 사정해 버리는 거야?」")
			ENDIF
		;통상
		ELSE
			;갈것같다인 것을 참고 있다
			IF TCVAR:MASTER:인내
				CALL HTMLPRINTW(@"「기분이 좋아?   응 기분이 좋은거야?」")
				CALL HTMLPRINTW(@"「이제 팡팡해 괴로운 듯하지 않아%HTML_HEARTB()%」")
			;그 외
			ELSE
				CALL HTMLPRINTW(@"「조금 전부터 움찔움찔 시키고 있을 뿐이지 않아…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「사정하는 거야?   나무와 나무 사이는 사정해 버리는 거야?」")
			ENDIF
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;쓰러트리기 상태
	;=================================================
	IF 구상기입체크(100) && TEQUIP:쓰러트리기중
		CALL HTMLPRINTW(@"「자지 뜨겁다…%HTML_HEART()%  이대로 이렇게 해…」")
		CALL HTMLPRINTW(@"「입구에서 비비는 것라고 -…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		CALL HTMLPRINTW(@"「후~굉장하다…%HTML_HEART()%  뜨겁고 딱딱의 자지 좋다…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「좀 더 문질러…%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(3, 10)
			CASE 0
				CALL HTMLPRINTW(@"「읏%HTML_HEART()%  삽입할 수 있다고 생각했어어?」")
				CALL HTMLPRINTW(@"「이런 것도 가끔 씩은 좋지요…%HTML_HEARTB()%  후~%HTML_HEART()%」")
			CASE 1
				CALL HTMLPRINTW(@"「오─들 이봐요%HTML_HEARTB()%  부드럽고 좋지요…♪」")
				CALL HTMLPRINTW(@"「자지도 이봐요… 자꾸자꾸 딱딱해지고 있다%HTML_HEARTB()%」")
			CASE 2
				CALL HTMLPRINTW(@"「하앙…%HTML_HEART()%  끝 비비어지는 것도 견딜 수 없지요?」")
				CALL HTMLPRINTW(@"「미끄덩미끄덩해 준닷%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_2, 페니스밴드 가랑이
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_0, 짝 맞추기 놀이
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ! TFLAG:ACT파생
ENDIF
[SKIPEND]

;────────────────────────────────────
;ACT17, 파이즈리한다
;────────────────────────────────────
;·TFLAG:벌플래그가 끊고 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"의 경우는 발생하지 않는다
;·TEQUIP(오나홀/삼각목마) 장착중에는 발생하지 않는다
;·조교 대상으로 페니스가 없는 경우는 발생하지 않는다
;·조교자가 남자의 경우는 발생하지 않는다
;·현재 ACT파생는 존재하지 않지만 장래의 개변에 대비해, ACT파생 == 0으로 한다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_17
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT17_0, 파이즈리한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;위로돌리고
		IF TFLAG:위로돌리고
			;갈것같다인 것을 참고 있다
			IF TCVAR:MASTER:인내
				CALL HTMLPRINTW(@"「아읏%HTML_HEARTB()%  끝 쪽 빠끔빠끔 말하고 있다…%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「미끈미끈해 유두 스쳐도 좋다…%HTML_HEARTB()%  이봐요 이제 한계?   이봐요%HTML_HEART()%」")
			ELSE
				CALL HTMLPRINTW(@"「쿨렁쿨렁 쿠퍼액 나와 있지만…%HTML_HEARTB()%」")
				PRINTFORMW 「이대로 젖가슴에 걸치고 싶어?」
			ENDIF
		;통상
		ELSE
			;갈것같다인 것을 참고 있다
			IF TCVAR:MASTER:인내
				CALL HTMLPRINTW(@"「후훗%HTML_HEARTB()%  사정해 버리는 거야?」")
				CALL HTMLPRINTW(@"「이런 가슴에 개조차 는 해 버린다아%HTML_HEARTB()%」")
			ELSE
				CALL HTMLPRINTW(@"「쿨렁쿨렁 쿠퍼액 나와 있지만…%HTML_HEARTB()%」")
				PRINTFORMW 「이대로 젖가슴에 걸치고 싶어?」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;쓰러트리기 상태
	;=================================================
	IF 구상기입체크(100) && TEQUIP:쓰러트리기중 && !TALENT:TARGET:거유 ;//거유의 경우는 이치가 맞지 않기 때문에 연주한다
		CALL HTMLPRINTW(@"「이렇게 해 자지를 유두로 비비어…%HTML_HEARTB()%」")
		PRINTFORMW 「이것도 일종의 파이즈리군요…♪  무엇, 나이즈리?   빌려주어라」
		RETURN 1
	ENDIF

	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000 && !TALENT:TARGET:거유 ;//거유의 경우는 이치가 맞지 않기 때문에 연주한다
		PRINTFORMW 「이런 작은 젖가슴으로 자지 발기시키다니…」
		PRINTFORMW 「너 역시 로리콘의 기분 있는 것이 아닌거야?」
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:빈유 ;//거유의 경우는 이치가 맞지 않기 때문에 연주한다
		SELECTCASE RANDACT(3, 10)
			CASE 0
				PRINTFORMW 「이런 가슴은 파이즈리는 할 수 없다고 생각했어?」
				CALL HTMLPRINTW(@"「사이에 두지 않아도 칠하는 것 정도는 할 수 있어요%HTML_HEARTB()%」")
			CASE 1
				CALL HTMLPRINTW(@"「이봐요%HTML_HEARTB()%  귀두에 유두가 맞아도 좋지요?」")
				CALL HTMLPRINTW(@"「이대로 기분 좋게 해 준다%HTML_HEART()%」")
			CASE 2
				PRINTFORMW 「무리하게 사이에 둘 수 없는 것도 아닌거야?」」
				PRINTFORMW 「응, 응읏… 뭐야, 웃고 있는 것이 아니에요」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT18, 풋잡한다
;────────────────────────────────────
;·풋잡한다
;·조교 대상으로 페니스가 없는 경우는 발생하지 않는다
;·이상계로부터 파생하는 경우도 있다
;·TFLAG:벌플래그가 끊고 있으면 발생하지 않는다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·TEQUIP(오나홀/삼각목마) 장착중은 발생하지 않는다
;·성교중/성교봉사중은 발생하지 않는다
;·조교자에게 다리가 없으면 발생하지 않는다
;·조교 대상이 여자 아이라면 발생하지 않는다
;·현재 ACT파생는 존재하지 않지만 장래의 개변에 대비해, ACT파생 == 0으로 한다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_18
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT18, 풋잡한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;위로돌리고
		IF TFLAG:위로돌리고
			;갈것같다인 것을 참고 있다
			IF TCVAR:MASTER:인내
				CALL HTMLPRINTW(@"「발바닥으로 자지 날뛰고 있닷%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「아직 안 돼요…%HTML_HEARTB()%  좋다고 말할 때까지 사정하지 않는거야!」")
			;그 외
			ELSE
				PRINTFORMW 「다리이지만… 어떻게 해 주는 거야?」
				PRINTFORMW 「이제(벌써) 사정한 듯하다?   그렇게 보기 흉하게 사정하고 싶은거야?」
			ENDIF
		;통상
		ELSE
			;갈것같다인 것을 참고 있다
			IF TCVAR:MASTER:인내
				CALL HTMLPRINTW(@"「굉장한 움찔움찔 전해져 온다…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「무엇보다─와 가득 참아%HTML_HEART()%」")
			;그 외
			ELSE
				PRINTFORMW 「다리이지만… 어떻게 해 주는 거야?」
				PRINTFORMW 「이제(벌써) 사정한 듯하다?   그렇게 보기 흉하게 사정하고 싶은거야?」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;쓰러트리기 상태
	;=================================================
	IF 구상기입체크(100) && TEQUIP:쓰러트리기중
		CALL HTMLPRINTW(@"「밀어 넘어뜨려지면서 자지 발길질로 되어 앙앙 말하고 있다…%HTML_HEARTB()%」")
		CALL HTMLPRINTW(@"「정말, 좋은 광경이예요…%HTML_HEART()%」")
		RETURN 1
	ENDIF

	;=================================================
	;발기
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		PRINTFORMW 「아─아, 다리로 드디어 발기해 버렸군요」
		CALL HTMLPRINTW(@"「마조끼도 있을까?   충분히 귀여워해 준다%HTML_HEARTB()%」")
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(3, 10)
			CASE 0
				PRINTFORMW 「다리로 자지 비비어지고 느끼고 있는 거야?」
				PRINTFORMW 「좋은 취미 하고 있지 않은 너도♪」
			CASE 1
				CALL HTMLPRINTW(@"「이봐요, 슛슛%HTML_HEARTB()%」")
				PRINTFORMW 「어색한 것이 좋지요…」
			CASE 2
				PRINTFORMW 「밟히면서 커져 와 있지만…?」
				CALL HTMLPRINTW(@"「얼마나 변태인 것이야%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT19, 손가락삽입해라(ACT19_0, 손가락삽입해라/ACT19_1, Ｇ스팟 자극/ACT19_2, 바이브 출납)
;────────────────────────────────────
;·조교 대상이 후타나리 혹은 여자의 경우에 발생
;·조교 대상이 처녀의 경우Ｇ스팟 자극등에 파생하지 않는＆쾌Ｖ에 마이나스 보정
;·바이브 장비중은 바이브 출납에 파생
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교 대상이 남자라면 발생하지 않는다
;·조교 대상이 후타나리의 경우, 애태움중에는 발생하지 않는다
;-------------------------------------------------
;※조교 대상이 처녀는 아니다
;※연속 실행의 경우
;※갈것같다or쾌Ｃ가 5000이상의 경우, 조교자의 애무 스킬에 응해
;※발기도가 낮은 경우, 조교자의 애무 스킬에 응해
;※요점강제발기의 경우
;·상기 조건의 경우Ｇ스팟 자극에 파생한다
;-------------------------------------------------
;@KOJO_MALE_ACT_K44_19
;-------------------------------------------------
;·TFLAG:ACT파생(0=손가락삽입해라/1=Ｇ스팟 자극/2=바이브 출납)
;-------------------------------------------------
;남자 한정 구상에서는 불요
;RETURN 0

;────────────────────────────────────
;ACT27, 쓰러트린다
;────────────────────────────────────
;·쓰러트린다/쓰러트리기 종료
;·이미 쓰러트리기라고 있는 경우, 해방이 된다
;·TFLAG:벌플래그가 끊고 있으면 발생하지 않는다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·TEQUIP:페니스밴드장착 안에는 발생하지 않는다
;·애태움중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_MALE_ACT_K44_27
;-------------------------------------------------
;TFLAG:ACT파생（=0, 쓰러트린다/=1, 쓰러트리기 종료）
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT27_0, 쓰러트린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;발기중
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 >= 1000
		PRINTFORMW 「허가없이 이렇게 딱딱한 자지에는…」
		PRINTFORMW 「조금 예의 범절이 필요하네요♪」
		RETURN 1
	ENDIF

	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDACT(3, 10)
			CASE 0
				PRINTFORMW 「조─금 얌전하게 해 받아?」
			CASE 1
				PRINTFORMW 「영차와」
				CALL HTMLPRINTW(@"「후후, 아주 쉬운 아주 쉬운♪  그런데 어떻게 하지%HTML_HEARTB()%」")
			CASE 2
				PRINTFORMW 「저항하지 말아 줄래?」
				CALL HTMLPRINTW(@"「뭐, 분명하게 기분 좋게 해 주어요%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT27_1, 쓰러트리기 종료
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
;ENDIF