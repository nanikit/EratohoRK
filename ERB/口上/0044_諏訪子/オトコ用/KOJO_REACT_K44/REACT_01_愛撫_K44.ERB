;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스와자 구상 애무계 개별 리액션 구상 파일
;
;	애무계 리액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;애무계 REACT10～19 & REACT27
;========================================================================
;· 각 REACT내의 나누는 방법으로서 TFLAG:REACT분류(IS_COMGRONAME)나 SELECTCOM(IS_NOWCOMNAME)가 생각됩니다
;·다만, 범위의 넓은 조건으로 주워 버리는 것은 기본적으로 추천하지 않습니다
;·ACT분류:애무계의 경우, 기본적으로 애무계 COM(COM10~COM16)가 선택지가 됩니다만
;·상황에 따라서는 그 회화계, 고통계, 그 외의 것이 선택지에 포함될 가능성도 있습니다
;·리액션(REACT)으로 처리되지 않았던 것은 범용 리액션(COM)으로 보완되므로
;·무리하게 모두 묻을 필요는 없습니다
;·필요라고 생각하는 것을 좁혀, COM와의 제휴를 생각해 효율 좋게 구축해야 합니다
;-------------------------------------------------
;· 각 REACT 및 ACT파생내는,&& IS_NOWCOMNAME든지,&& IS_COMGRONAME등의 조건으로 결정하고 쳐 합시다
;·여기에 조건 없음 분기가 있으면, 리액션에 관련되지 않고, 모두 주워 버려 범용 리액션에 흐르지 않습니다
;·요컨데, 상정된 시스템의 파괴에 연결됩니다
;-------------------------------------------------
;  기본적으로 지문은 TFLAG:REACT분류(받아들인다/적극적 따른다/태연하게 따른다/허가를청한다/소극적 따른다)로 분기 해,
;  각 REACT분류내에서 TFLAG:REACT부하(자연스러운 행동/무리가 없는 행동/무리를 한 행동)으로 파생
;  ACT_MESSAGE.ERB를 참조하면서의 작업을 추천합니다
;-------------------------------------------------
;  우선 ACT파생 마다 테두리를 분리해 있습니다만, 1개에 모은 다음 범용적인 기술로 수고를 줄인다고 하는 방법도 있습니다
;  정직, 방식은 얼마든지 있으므로, 이하에 나타내는 방법은 하나의 예라고 생각해 주세요
;-------------------------------------------------

;─
;REACT10, 손으로애무·개별 리액션
;─
;·TFLAG:ACT파생(0=애무/1=자위/2=페니스밴드 자위)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:좀더하드하게해
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_10
;-------------------------------------------------
;·ACTTION_APPLY2의 처리 없음
;·지문은 ACT파생 마다 REACT분류(받아들인다/적극적 따른다/태연하게 따른다/허가를청한다/소극적 따른다)로 분기
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 자위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(70) && TFLAG:ACT파생 == 1
	IF IS_COMGRONAME("소극적 따른다/태연하게 따른다/적극적 따른다/받아들인다/허가를청한다/")
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				PRINTFORMW 「기분이 좋아? 이봐요」
				CALL HTMLPRINTW(@"「이봐요 이봐요 이봐요%HTML_HEARTB()%」")
			CASE 1
				CALL HTMLPRINTW(@"「쿠퍼액 너무 내는 것이 아니다…%HTML_HEARTB()%」")
			CASE 2
				PRINTFORMW 「자신으로부터 허리 움직여 버리고 있어요?」
				CALL HTMLPRINTW(@"「어쩔 수 없는 녀석%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0

[SKIPSTART]
남자 한정 구상인 것으로 comment out
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 페니스밴드 자위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
ENDIF
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 애무
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ! TFLAG:ACT파생
ENDIF
[SKIPEND]

;─
;REACT11, 가슴애무·개별 리액션
;─
;·TFLAG:ACT파생(0=가슴애무/1=유두 들이마셔/2=몸싸움/3=유두 고문)
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:좀더하드하게해
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
;·지문은 ACT파생 2, 몸싸움과 그 외로 분기
;·ACTION_APPLY2에 있어서, ACT파생 2의 경우는 수용, 거부의 판정 처리 있어
;·ACT파생 2, 몸싸움에 대해 REACT분류(소극적 따른다/태연하게 따른다/적극적 따른다/받아들인다) 이외는 "거부"취급
;-------------------------------------------------
;  파생마다 온전히 하면 수고가 심상치 않기 때문에, ACT파생분기의 상층에서 범용 처리하거나
;  각 ACT파생내의 COM 분기등은 일부의 것에 좁혀 버리는 것이 좋다고 생각합니다
;  또 ACT파생로 단락짓지 않고, 상당히기로 정리해 버리는 방법도 있습니다만, 공이 많이 든 일을 하기에는 적합하지 않다라고 생각합니다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_11
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 가슴애무
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ! TFLAG:ACT파생
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 유두 들이마셔
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자에게 소질(유치/혀사용)이 없는 경우는 발생하는 확립은 낮다
;-------------------------------------------------
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 몸싸움
;━━━━━━━━━━━━━━━━━━━━━━━━
;봉사계 COM가 선택지가 되는 경우 있어
;-------------------------------------------------
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 3, 유두 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자에게 소질(유치/혀사용)이 없는 경우는 발생하는 확립이 높다
;조교 대상이 남자의 경우는 여기에 밖에 떨어지지 않는다
;-------------------------------------------------
IF 구상기입체크(70) && TFLAG:ACT파생 == 3
	IF IS_COMGRONAME("소극적 따른다/태연하게 따른다/적극적 따른다/받아들인다/허가를청한다")
		SELECTCASE RANDREACT(3, 30)
			CASE 0
				PRINTFORMW 「낯간지럽다는?」
				CALL HTMLPRINTW(@"「곧 기분 좋아져요%HTML_HEARTB()%」")
			CASE 1
				CALL HTMLPRINTW(@"「좋은 얼굴 하지 않아…%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「지금 여자 아이같아요 너%HTML_HEARTB()%」")
			CASE 2
				PRINTFORMW 「유두 오돌오돌해 버려…」
				PRINTFORMW 「점점 느껴 왔는지?」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0

;─
;REACT12, 입으로애무·개별 리액션
;─
;·TFLAG:ACT파생(0=쿤닐링구스/1=페라)
;-------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:좀더하드하게해
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_12
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 페라
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(80) && TFLAG:ACT파생 == 1
	IF IS_COMGRONAME("소극적 따른다/태연하게 따른다/적극적 따른다/받아들인다/허가를청한다")
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				PRINTFORMW 「끝 쪽이 좋은거야?   그렇지 않으면 성기 뒷부분?」
				CALL HTMLPRINTW(@"「레로레로레로… 키득, 어느 쪽도 반응하지 않아%HTML_HEARTB()%」")
			CASE 1
				CALL HTMLPRINTW(@"「… 푸핫%HTML_HEART()%」")
				PRINTFORMW 「입의 안에서 크게 하고 있는 것 들키고 들키고야?」
			CASE 2
				CALL HTMLPRINTW(@"「조금 전부터 쿠퍼액 내고 있을 뿐%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「그렇게 먹이고 싶은 것인지…%HTML_HEART()%  킥킥」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 쿤닐링구스
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ! TFLAG:ACT파생
;ENDIF


;─
;REACT13, 애널애무·개별 리액션
;─
;·TFLAG:ACT파생(0=애널애무/1=전립선자극/2=애널바이브 출납)
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:허가를청한다
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_13
;-------------------------------------------------
;·지문은 ACT파생로 분기. ACT파생 1, 2 분기내의 파생 없음
;·ACT파생 0, 애널애무 마셔 REACT분류(받아들인다/적극적 따르는, 태연하게 따른다/허가를청한다/소극적 따른다)로 파생 처리된다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT13, 애널애무/전립선자극/애널바이브 출납
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0에 대해서는 가능성이 있는 COM를 망라해 둡니다
;필요에 따라서 분기를 증감, 없고 구조를 개변해 사용해 주세요
;-------------------------------------------------
IF 구상기입체크(70) && INRANGE(TFLAG:ACT파생, 0, 2)
	IF IS_COMGRONAME("소극적 따른다/태연하게 따른다/적극적 따른다/받아들인다/허가를청한다")
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				CALL HTMLPRINTW(@"「흠칫흠칫 시켜 버려%HTML_HEARTB()%」")
				PRINTFORMW 「좀 더 갖고 싶은거야?   うりうり♪」
			CASE 1
				PRINTFORMW 「아직 익숙해지지 않아?」
				CALL HTMLPRINTW(@"「괜찮아요, 그 중 좋아지기 때문에%HTML_HEARTB()%」")
			CASE 2
				CALL HTMLPRINTW(@"「좋아…%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「좀 더 그 얼굴 좀 보여줘%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 전립선자극
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 애널바이브 출납
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
ENDIF
[SKIPEND]

;─
;REACT14, 애널빨고·개별 리액션
;─
;·ACT파생없음. ACTION_APPLY2의 처리 없음
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:허가를청한다
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_14
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT14, 애널빨고
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(70) && !TFLAG:ACT파생
	IF IS_COMGRONAME("소극적 따른다/태연하게 따른다/적극적 따른다/받아들인다/허가를청한다")
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				PRINTFORMW 「더럽기 때문에 그만두어라고?」
				PRINTFORMW 「뭐 말하고 있는 거야. 자지도 같은 것이야♪」
			CASE 1
				CALL HTMLPRINTW(@"「지에 흠칫흠칫 하고 있다…%HTML_HEART()%」")
				PRINTFORMW 「슬슬 느껴 왔지 않아?」
			CASE 2
				PRINTFORMW 「무서운거야?   괜찮아♪」
				CALL HTMLPRINTW(@"「전부 나에게 맡기세요%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0

;─
;REACT15, 키스·개별 리액션
;─
;·ACT파생없음. ACTION_APPLY2의 처리 없음
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:좀더하드하게해
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;그 외 반응
;	COM:자위하기시작한다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT15, 키스
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(70) && !TFLAG:ACT파생
	IF IS_COMGRONAME("소극적 따른다/태연하게 따른다/적극적 따른다/받아들인다/허가를청한다")
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				CALL HTMLPRINTW(@"「응츗… 날름날름 … 아하아%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「꽤 능숙하지 않아%HTML_HEARTB()%」")
			CASE 1
				CALL HTMLPRINTW(@"「응응…%HTML_HEARTB()%  좀 더 혀 관련되어…%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「… 츄풋… 츗%HTML_HEARTB()%」")
			CASE 2
				CALL HTMLPRINTW(@"「부끄러워하지 않는거야%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「… 엣취…%HTML_HEART()%  」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0


;─
;REACT16, 성기를서로켠다·개별 리액션
;─
;·TFLAG:ACT파생(=0, 조개 우려라/=1, 가랑이/=2, 페니스밴드 가랑이)
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:좀더하드하게해
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_16
;-------------------------------------------------
;·지문은 ACT파생 마다 REACT분류(받아들인다/적극적 따른다/태연하게 따른다/허가를청한다, 소극적 따른다)로 분기
;·필요에 따라서 분기를 증감해 사용해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 가랑이
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	IF IS_COMGRONAME("태연하게 따른다/적극적 따른다/받아들인다") ;//적극적 수용인 경우에 한정
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				CALL HTMLPRINTW(@"「아핫%HTML_HEART()%  기분이 좋아?」")
				CALL HTMLPRINTW(@"「굉장한 자지 뒤로 젖히고 있다아%HTML_HEARTB()%」")
			CASE 1
				CALL HTMLPRINTW(@"「응… 하앗%HTML_HEARTB()%  안됏%HTML_HEART()%」")
				CALL HTMLPRINTW(@"「허리 너무 움직이고…%HTML_HEART()%  클리에 해당되고 있다%HTML_HEARTB()%」")
			CASE 2
				CALL HTMLPRINTW(@"「자신으로부터 문질러 오다니…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「정말 에로한 것이니까%HTML_HEART()%  후~♪  미끈미끈 좋다…%HTML_HEARTB()%」")
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 페니스밴드 가랑이
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 조개 우려라
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ! TFLAG:ACT파생
ENDIF
[SKIPEND]

;─
;REACT17, 파이즈리한다·개별 리액션
;─
;·ACT파생없음
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:좀더하드하게해
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_17
;-------------------------------------------------
;·지문은 COM:허리를흔드는, REACT분류(받아들인다/적극적 따른다/태연하게 따른다/허가를청한다/소극적 따른다)로 분기
;·애무계 COM를 망라. 필요에 따라서 분기를 증감해 사용해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT17, 파이즈리한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	IF IS_COMGRONAME("태연하게 따른다/적극적 따른다/받아들인다") ;//적극적 수용인 경우로 한정한다
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				PRINTFORMW 「질척… 쿠츗…」
				CALL HTMLPRINTW(@"「앙…%HTML_HEARTB()%  응 기분이 좋아?」")
			CASE 1
				CALL HTMLPRINTW(@"「허리 움직여 버리고 있지 않아…%HTML_HEARTB()%」")
				CALL HTMLPRINTW(@"「좀더 좀더, 유두에 비비어%HTML_HEARTB()%」")
			CASE 2
				CALL HTMLPRINTW(@"「날름─… 질척질척%HTML_HEARTB()%」")
				PRINTFORMW 「침으로 좀 더 미끈미끈이 된 것이겠지?」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0

;─
;REACT18, 풋잡한다·개별 리액션
;─
;·ACT파생없음
;-------------------------------------------------
;상정되는 COM
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:날뛴다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_18
;-------------------------------------------------
;·지문은 REACT분류(받아들인다/적극적 따르는, 태연하게 따른다/허가를청한다/소극적 따른다)로 분기
;·필요에 따라서 분기를 증감해 사용해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT18, 풋잡한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(70) && !TFLAG:ACT파생
	IF IS_COMGRONAME("태연하게 따른다/적극적 따른다/받아들인다") ;//적극적 수용인 경우로 한정한다
		SELECTCASE RANDREACT(3, 0)
			CASE 0
				PRINTFORMW 「자꾸자꾸 미끈미끈 나와 있지 않아…」
				CALL HTMLPRINTW(@"「이런 것이 좋아했던 것이구나%HTML_HEARTB()%」")
			CASE 1
				PRINTFORMW 「이봐요, 양 다리로 사이에 두어 쫄깃쫄깃이라고」
				CALL HTMLPRINTW(@"「손과 어느 쪽이 기분이 좋은 것일까요%HTML_HEARTB()%」")
			CASE 2
				CALL HTMLPRINTW(@"「타마도 밟아 버릴까…%HTML_HEARTB()%」")
				PRINTFORMW 「거짓말 거짓말, 오슬오슬 오겠죠?」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;조건에 히트 하지 않으면 범용 리액션에
RETURN 0

;─
;REACT19, 손가락삽입해라
;─
;·TFLAG:ACT파생(0=손가락삽입해라/1=G스팟 자극/2=바이브 출납)
;-------------------------------------------------
;상정되는 COM
;애무계 반응
;	COM:부끄러워한다/싫어한다/서투르다고매도한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;고통계 반응
;	COM:날뛴다
;-------------------------------------------------
;@KOJO_MALE_REACT_K44_19
;-------------------------------------------------
;지문은 ACT파생 0만 REACT분류로 분기
;그 외는 TALENT:MASTER:후타나리인지 아닌지로 파생할 뿐
;-------------------------------------------------
;남자 한정 구상에서는 불요
;RETURN 0

;─
;REACT27, 밀어넘어뜨린다·개별 리액션
;─
;TFLAG:ACT파생(=0, 밀어넘어뜨린다/=1, 밀어넘어뜨리기 종료)
;-------------------------------------------------
;상정되는 COM
;회화계 반응
;	COM:심약구응답한다/무뚝뚝하게응답한다/붙임성좋게응답한다/강하게응답한다/허가를청한다/기분좋게해
;고통계 반응
;	COM:무서워한다
;-------------------------------------------------
@KOJO_MALE_REACT_K44_27
;-------------------------------------------------
;지문은 REACT분류(받아들인다/적극적 따르는, 태연하게 따른다/허가를청한다/소극적 따른다)로 분기
;ACT파생 마다의 분기는 없음 한편, 내용적으로는 ACT파생 0의 것
;ACT파생 1의 경우는 정합성이 무너진다고 생각된다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 밀어넘어뜨린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;내용적으로 조건 없음 RAND로 나누어야 할 것은 아니기 때문에, 개별적으로 분리한다
	SELECTCASE COMGRONAME(TFLAG:REACT분류)
		CASE "태연하게 따른다", "적극적 따른다", "받아들인다"
			PRINTFORMW 「어머나 순조롭게」
			CALL HTMLPRINTW(@"「벌써, 기대해 버리고 있지 않아%HTML_HEARTB()%」")
		CASE "날뛴다", "거부", "도망친다"
			PRINTFORMW 「이봐, 날뛰지 않는거야」
			CALL HTMLPRINTW(@"「지금부터 기분 좋게 하기 때문에%HTML_HEARTB()%」")
		CASEELSE
			PRINTFORMW 「하아하아 말해 버려…」
			CALL HTMLPRINTW(@"「너에게는 당하는 편이 어울리구나%HTML_HEARTB()%」")
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 밀어넘어뜨리기 종료
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
;ENDIF