;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	유유코 구상 숍 이벤트 구상 파일
;
;	숍으로부터 불려 가는 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;숍 이벤트 구상
;========================================================================

;─
;EV101, 의상 변경
;─
;·숍으로부터 의상 변경을 실시했을 경우에 표시
;-------------------------------------------------
@KOJO_MALE_EVENT_K20_101(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=
;  1의 위…(0, 이제(벌써) 입고 있다, 1, 갈아입은, 2, 부끄러워서 입을 수 없는, 3, 싫어해 입지 않는, 9, 가지고 있지 않다)
;  10의 위…(0, 통상, 10, 반나체, 20, 전라, 30, 속옷 없음)
;  백의 위… 코스프레를 위해서(때문에) 보류
;  천의 위…(0, 조교자, 1000, 조교 대상)
;  예(조교 대상이 부끄러워해전라로 할 수 없었다=1022)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 갈아입었다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG < 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;통상이 되었다
		CASE 1
			PRINTFORMW 「역시 평소의 옷이 제일 침착해요」
			PRINTFORMW 「다음에 벗어버리면 좋은 거네」
		;반나체가 되었다
		CASE 11
			PRINTFORMW 「반나체가 에로하다고 말하지만…」
			PRINTFORMW 「～응, 아직 브라 작아지고 있다」
		;전라가 되었다
		CASE 21
			PRINTFORMW 「～응, 조금 살쪘을까?」
			PRINTFORMW 「… 오늘도 가득 저 녀석을 괴롭히면 되네요」
		;본디지가 되었다			현행 사양에서는 실장되어 있지 않기 때문에 comment out@/L
		;CASE 31
		;	PRINTFORMW 「조금 악취미일까…」
		;	PRINTFORMW 「유카리나 꽃요괴가 이런 것은 어울릴 것 같구나」
	ENDSELECT
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 부끄러워해 갈아입을 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG < 1000 && (ARG % 10) == 3
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 싫어해 갈아입을 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG < 1000 && (ARG % 10) == 2
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 갈아입는 것을 그만둔	FIRSTEVENT(30)
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%의 의상은 벌써[　　]로 설정되어 있습니다
;------------------------------------------------
;IF 구상 기입 체크(0) && ARG < 1000 && (ARG % 10) == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상을 갈아입게 했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG >= 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;통상이 되었다
		CASE 1001
			PRINTFORMW 「옷을 돌려주어요, 감기 걸리지 않도록요」
			PRINTFORMW 「그렇지만 말해지자마자 벗는거야?」
		;반나체가 되었다
		CASE 1011
			PRINTFORMW 「네, 옷을 몰수합니다」
			PRINTFORMW 「아라, 속옷만이라면 추잡하네요」
		;전라가 되었다
		CASE 1021
			PRINTFORMW 「입는 것은 모두 벗으며♪」
			PRINTFORMW 「엉덩이도 뻔히 보임이야, 우후후」
		;본디지가 되었다			현행 사양에서는 실장되어 있지 않기 때문에 comment out@/L
		;CASE 1031
		;	PRINTFORMW 「이것을 입어 줘♪」
		;	PRINTFORMW 「본테이지는 구속도구라는 의미인 것, 알고 있었어?」
	ENDSELECT
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 부끄러워해 갈아입을 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG >= 1000 && (ARG & 10) == 2
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 싫어해 갈아입을 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG >= 1000 && (ARG & 10) == 3
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상을 갈아입게 하는 것을 그만둔 FIRSTEVENT(70)
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:MASTER%의 의상은 벌써[　　]로 설정되어 있습니다
;------------------------------------------------
;IF 구상 기입 체크(0) && ARG >= 1000 && (ARG % 10) == 0
;ENDIF

;─
;EV102, 조교자 변경
;─
;·조교자를 교대시켰을 때에 표시
;-------------------------------------------------
@KOJO_MALE_EVENT_K20_102(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 구상주가 다른 캐릭터에게 맡긴, 1, 구상주가 조교자가 되었다)
;구조교자는 「%CALLNAME:SHOP_OLDTARGET%」, 신조교자는 「%CALLNAME:SHOP_NEWTARGET%」로 참조할 수 있다
;구상주는 TARGET로 참조
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;다른 조교자에게 교대
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 0
	;조교 대상의 반항각인 Lv5 이상
	IF MARK:반항각인 >= 5
		PRINTFORMW 「조금 맡겨요」
		PRINTFORMW 「나의 대신에 질척로 해 줘」
	;조교 대상의 반항각인 Lv4 이하
	ELSE
		PRINTFORMW 「조금 2, 3일 쉬기로 해요」
		PRINTFORMW 「그 사이 그 아이의 일 아무쪼록 부탁해요, 죽여서는 안 돼요」
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;유유코님이 조교자가 되었다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	;조교 대상의 반항각인 Lv5 이상
	IF MARK:반항각인 >= 5
		PRINTFORMW 「아라아라… 상당히 미움받고 있어요」
		PRINTFORMW 「북풍과 태양이라고 말하고, 쾌락으로 녹진녹진해 주지 않으면」
	;조교 대상의 반항각인 Lv4 이하
	ELSE
		PRINTFORMW 「응─…, 한가하구나」
		PRINTFORMW 「이런 때는 노예와 노는 것도 풍류군요」
	ENDIF
	RETURN 2
ENDIF

;─
;EV110, 마력 사용
;─
;·마력을 소비해 어떠한 특전을 구입했을 때에 표시
;-------------------------------------------------
@KOJO_MALE_EVENT_K20_110(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 마력 사용전, 1, 조교 기교, 2, 지식을 얻기 전, 3, 약학지식 취득, 4, 매혹 취득, 5, 금단의지식 취득, 6, 도구사용 취득,
;	7, 기술을 얻기 전, 8, 혀사용 취득, 9, 기묘한손가락취득, 10, 결박능숙, 11, 새디즘 소거/새디즘 취득, 12, 불결무시 취득, 38, 착의플레이선호, 39, 허리사용)
;이벤트 구상: 아이템
;인수 내역…
;ARG=(13, 아이템 연성전, 14, 로터 연성, 15, 바이브 연성, 16, 애널비즈 연성, 17, 페니스밴드 연성, 18, 애널바이브 연성
; 19, 클리캡 연성, 20, 니플캡 연성, 21, 오나홀 연성)
;이벤트 구상:고문도구 연성
;인수 내역…
;ARG=(22, 고문도구 연성전, 23, 아이마스크 연성, 24, 채찍 연성, 25, 줄연성, 26, 볼개그 연성, 27,   바늘 연성, 28, 관장세트 연성
; 29, 삼각목마 연성, 30, 대거울 연성, 31, 목걸이 연성, 32, 도구를 커스터마이즈 했다)
;그 외 이벤트
;인수 내역…
;ARG=(33, 후타나리화/되돌리는, 34, 거유화/되돌리는, 35, 빈유화/되돌리는, 36, 정력의 한계 가격인상승)
;-------------------------------------------------

;0. 60시대의 EV103, 보너스 포인트 사용에 해당하는 개소
;현행 사양에 있어서 조교자를 고용한, 조교자로서 가입했다는 개별의 이벤트로서 분리할 수 있었습니다@/L

;━━━━━━━━━━━━━━━━━━━━━━━━
;유유코님마력 사용전
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;유유코님의 조교 기교가 업
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;유유코님SM상품 구입
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && INRANGE(ARG, 23, 31)
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「엿과 채찍이군요, 이번 정말 엿이라도 사 와 줄까」
		PRINTFORMDW 유유코는 넋을 잃고도구를 바라보고 있다
	;아라이먼트부
	ELSE
		PRINTFORMW 「우후후, 사 버린 사 버렸다♪」
		PRINTFORMW 「이것도 저것도 대단한 것 같구나」
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;유유코님후타나리화 or후타나리 소거
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 33 && FLAG:모드 != 1
	;후타나리화
	IF TALENT:TARGET:후타나리
		;조교 대상이 남자
		IF TALENT:MASTER:남자
			PRINTFORMW 「우후후, 나 버린 나 버렸다♪」
			PRINTFORMW 「모처럼 난 것이니까 즐기지 않으면 말야」
		;조교 대상이 처녀
		ELSEIF TALENT:MASTER:처녀
			PRINTFORMW 「… 그 아이의 처음은 이것으로 받는 것이 예의군요」
			PRINTFORMW 「바이브나 페니스밴드는 불쌍하고, 임신하게 되어지지 않는 거네♪」
		;조건 없음
		ELSE
			PRINTFORMW 「역시 조교는, 범하지 않으면 시작되지 않네요♪」
			PRINTFORMW 「아라, 생각했던 것보다 큰거네」
		ENDIF
	;후타나리 소거
	ELSE
	ENDIF
	RETURN 2
ENDIF

;─
;EV111, 주인님으로서 새로운 조교자를 고용했다
;─
@KOJO_EVENT_K20_111(ARG)
;-------------------------------------------------
;·새로운 조교자가 TARGET가 되어 있기 (위해)때문에, 구상주는 TARGET는 아니기 때문에 주의
;·구상주를 참조하고 싶은 경우는 FLAG:주인님으로
;-------------------------------------------------
;반환값(1, 표시, 2, 표시해 가입한 캐릭터의 가입 구상을 스킵)
;IF 구상 기입 체크(0)
	;CSTR:(FLAG:주인님):호칭목록 = 전의 이름_부르게 하고 싶은 이름 /%CSTR:(FLAG:주인님):호칭목록%
;ENDIF

;─
;EV112, 유유코님이 조교자로서 가입했다
;─
@KOJO_EVENT_K20_112(ARG)
;-------------------------------------------------
;·주인님의 이름은 「FLAG:주인님」로 참조할 수 있다
;·주인님이 고용 구상으로 반환값 2를 돌려주고 있으면 표시되지 않는다
;-------------------------------------------------
IF 구상기입체크(100)
	;조교 대상의 반항각인 Lv5 이상
	IF MARK:(FLAG:주인님):반항각인 >= 5
		PRINTFORMW 「말하는 일 (듣)묻지 않는 노예가 있다 라고 (들)물었지만」
		PRINTFORMW 「잘 부탁해, 함께 녹진녹진 녹여 줍시다」
	;조교 대상의 반항각인 Lv4 이하
	ELSE
		PRINTFORMW 「사랑스러운 노예가 있다 라고 (들)물었지만」
		PRINTFORMW 「너무 기다려져 신체가 쑤셔요?」
	ENDIF
	RETURN 1
ENDIF