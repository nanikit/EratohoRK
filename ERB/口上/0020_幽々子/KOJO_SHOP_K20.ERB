;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	幽々子口上 ショップイベント口上ファイル
;
;	ショップから呼び出される口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;ショップイベント口上
;========================================================================

;────────────────────────────────────
;EV101, 衣装変更
;────────────────────────────────────
;・ショップから衣装変更を行った場合に表示
;-------------------------------------------------
@KOJO_MALE_EVENT_K20_101(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=
;　一の位…（0, もう着てる、1, 着替えた、2, 恥ずかしくて着られない、3, 嫌がって着ない、9, 持ってない）
;　十の位…（0, 通常、10, 半裸、20, 全裸、30, 下着無し）
;　百の位…コスプレのために保留
;　千の位…（0, 調教者, 1000, 調教対象）
;　例（調教対象が恥ずかしがって全裸にできなかった＝1022）
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が着替えた
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG < 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;通常になった
		CASE 1
			PRINTFORMW 「やっぱりいつもの服が一番落ち着くわね」
			PRINTFORMW 「後で脱いじゃえばいいもんね」
		;半裸になった
		CASE 11
			PRINTFORMW 「半脱ぎの方がエロいって言うけど…」
			PRINTFORMW 「う～ん、まだブラちっちゃくなってる」
		;全裸になった
		CASE 21
			PRINTFORMW 「う～ん、ちょっと太ったかしら？」
			PRINTFORMW 「…今日もいっぱいあいつを虐めればいいのよね」
		;ボンデージになった			現行仕様では実装されていないのでコメントアウト@/L
		;CASE 31
		;	PRINTFORMW 「ちょっと悪趣味かしら…」
		;	PRINTFORMW 「紫や花妖怪の方がこういうのは似合いそうね」
	ENDSELECT
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が恥ずかしがって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && ARG < 1000 && (ARG % 10) == 3
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が嫌がって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && ARG < 1000 && (ARG % 10) == 2
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が着替えるのをやめた	FIRSTEVENT(30)
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%の衣装はすでに[　　]に設定されています
;------------------------------------------------
;IF 口上記入チェック(0) && ARG < 1000 && (ARG % 10) == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象を着替えさせた
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG >= 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;通常になった
		CASE 1001
			PRINTFORMW 「服を返してあげるわ、風邪ひかないようにね」
			PRINTFORMW 「でも言われたらすぐ脱ぐのよ？」
		;半裸になった
		CASE 1011
			PRINTFORMW 「はーい、服を没収しまーす」
			PRINTFORMW 「あら、下着だけだとイヤらしいわね」
		;全裸になった
		CASE 1021
			PRINTFORMW 「着るものはすべて脱いで♪」
			PRINTFORMW 「お尻も丸見えよ、うふふ」
		;ボンデージになった			現行仕様では実装されていないのでコメントアウト@/L
		;CASE 1031
		;	PRINTFORMW 「これを着て頂戴♪」
		;	PRINTFORMW 「ボンテージって拘束具って意味なの、知ってた？」
	ENDSELECT
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が恥ずかしがって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && ARG >= 1000 && (ARG & 10) == 2
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が嫌がって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && ARG >= 1000 && (ARG & 10) == 3
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象を着替えさせるのをやめた FIRSTEVENT(70)
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:MASTER%の衣装はすでに[　　]に設定されています
;------------------------------------------------
;IF 口上記入チェック(0) && ARG >= 1000 && (ARG % 10) == 0
;ENDIF

;────────────────────────────────────
;EV102, 調教者変更
;────────────────────────────────────
;・調教者を交代させたときに表示
;-------------------------------------------------
@KOJO_MALE_EVENT_K20_102(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 口上主が他のキャラに任せた、1, 口上主が調教者になった)
;旧調教者は「%CALLNAME:SHOP_OLDTARGET%」、新調教者は「%CALLNAME:SHOP_NEWTARGET%」で参照できる
;口上主はTARGETで参照
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;他の調教者に交代
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 0
	;調教対象の反抗刻印Lv5以上
	IF MARK:反抗刻印 >= 5
		PRINTFORMW 「ちょっと任せるわね」
		PRINTFORMW 「私の代わりにとろっとろにしてあげて」
	;調教対象の反抗刻印Lv4以下
	ELSE
		PRINTFORMW 「ちょっと２、３日休憩することにするわ」
		PRINTFORMW 「その間あの子のことよろしく頼むわね、殺しちゃだめよ」
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;幽々子さまが調教者になった
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 1
	;調教対象の反抗刻印Lv5以上
	IF MARK:反抗刻印 >= 5
		PRINTFORMW 「あらあら…相当嫌われてるわね」
		PRINTFORMW 「北風と太陽って言うし、快楽でとろとろにしてあげないとね」
	;調教対象の反抗刻印Lv4以下
	ELSE
		PRINTFORMW 「んー…っ、ひまね」
		PRINTFORMW 「こういうときは奴隷とあそぶのも風流ね」
	ENDIF
	RETURN 2
ENDIF

;────────────────────────────────────
;EV110, 魔力使用
;────────────────────────────────────
;・魔力を消費して何らかの特典を購入したときに表示
;-------------------------------------------------
@KOJO_MALE_EVENT_K20_110(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 魔力使用前、1, 調教技巧、2, 知識を得る前、3, 調合知識取得、4, 魅惑取得、5, 禁断の知識取得、6, 道具使い取得、
;	7, 技術を得る前、8, 舌使い取得、9, 器用な指先取得、10, 縛り上手、11, サド消去/サド取得、12, 汚れ無視取得、 38, 着衣プレイ好き、39, 腰使い)
;イベント口上：アイテム
;引数内訳…
;ARG=(13, アイテム練成前、14, ローター練成、15,バイブ練成、16, アナルビーズ練成、 17, ペニスバンド練成、 18, アナルバイブ練成
; 19, クリキャップ練成、 20, ニプルキャップ練成、21, オナホール練成)
;イベント口上：責め具錬成
;引数内訳…
;ARG=(22, 責め具練成前、23, アイマスク練成、24, 鞭練成、 25, 縄練成、 26, ボールギャグ練成、27,  針練成、28, 浣腸セット練成
; 29, 三角木馬練成、 30, 大鏡練成、 31, 首輪練成、 32, 道具をカスタマイズした)
;その他イベント
;引数内訳…
;ARG=(33, ふたなり化/戻す、34, 巨乳化/戻す、35, 貧乳化/戻す、36, 精力の限界値上昇)
;-------------------------------------------------

;0.60時代のEV103, ボーナスポイント使用に該当する箇所
;現行仕様に於いて調教者を雇った、調教者として加入したは個別のイベントとして切り分けられました@/L

;━━━━━━━━━━━━━━━━━━━━━━━━
;幽々子さま魔力使用前
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;幽々子さまの調教技巧がアップ
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && ARG == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;幽々子さまＳＭグッズ購入
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && INRANGE(ARG, 23, 31)
	;アライメント正
	IF ALI(0)
		PRINTFORMW 「飴と鞭ね、こんどほんとに飴でも買って来てあげようかしら」
		PRINTFORMDW 幽々子はうっとりと道具を眺めている
	;アライメント負
	ELSE
		PRINTFORMW 「うふふ、買っちゃった買っちゃった♪」
		PRINTFORMW 「どれもこれもすごそうねぇ」
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;幽々子さまふたなり化orふたなり消去
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 33 && FLAG:モード != 1
	;ふたなり化
	IF TALENT:TARGET:ふたなり
		;調教対象がオトコ
		IF TALENT:MASTER:オトコ
			PRINTFORMW 「うふふ、生えちゃった生えちゃった♪」
			PRINTFORMW 「せっかく生えたんだから楽しまなくちゃね」
		;調教対象が処女
		ELSEIF TALENT:MASTER:処女
			PRINTFORMW 「…あの子の初めてはこれでいただくのが礼儀よね」
			PRINTFORMW 「バイブやペニスバンドじゃかわいそうだし、孕ませられないものね♪」
		;条件なし
		ELSE
			PRINTFORMW 「やっぱり調教は、犯さなきゃ始まらないわよね♪」
			PRINTFORMW 「あら、思ったより大きいのね」
		ENDIF
	;ふたなり消去
	ELSE
	ENDIF
	RETURN 2
ENDIF

;────────────────────────────────────
;EV111, ご主人様として新しい調教者を雇った
;────────────────────────────────────
@KOJO_EVENT_K20_111(ARG)
;-------------------------------------------------
;・新しい調教者がTARGETになっているため、口上主はTARGETではないので注意
;・口上主を参照したい場合はFLAG:ご主人様で
;-------------------------------------------------
;戻り値（1,表示、2,表示して加入したキャラの加入口上をスキップ）
;IF 口上記入チェック(0)
	;CSTR:(FLAG:ご主人様):呼び方リスト = 元の名前_呼ばせたい名前/%CSTR:(FLAG:ご主人様):呼び方リスト%
;ENDIF

;────────────────────────────────────
;EV112, 幽々子さまが調教者として加入した
;────────────────────────────────────
@KOJO_EVENT_K20_112(ARG)
;-------------------------------------------------
;・ご主人様の名前は「FLAG:ご主人様」で参照できる
;・ご主人様が雇用口上で戻り値2を返していると表示されない
;-------------------------------------------------
IF 口上記入チェック(100)
	;調教対象の反抗刻印Lv5以上
	IF MARK:(FLAG:ご主人様):反抗刻印 >= 5
		PRINTFORMW 「言うこと聞かない奴隷がいるって聞いたけど」
		PRINTFORMW 「よろしくね、一緒にとろとろに溶かしてあげましょ」
	;調教対象の反抗刻印Lv4以下
	ELSE
		PRINTFORMW 「可愛い奴隷がいるって聞いたけど」
		PRINTFORMW 「楽しみすぎて身体がうずくわぁ？」
	ENDIF
	RETURN 1
ENDIF