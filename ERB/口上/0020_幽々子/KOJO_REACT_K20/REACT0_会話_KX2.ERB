;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	유들자 구상 회화계 개별 리액션 구상 파일
;
;	회화계 리액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 REACT0～9
;========================================================================
;·1231판보다 TFLAG:REACT부하가 도입되어 선택한 COM가 자연스러운 행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운 행동/1=무리가 없는 행동/2=무리를 한 행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;─
;REACT0, 지금의기분을얘기한다·개별 리액션
;─
;·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K20_0
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일 하고 싶다)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따르는 것으로, 리액션이 빈정거리는 이외의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「아라아라 긴장해 버리고 있는 거야? 라면 풀어 주지 않으면」
		PRINTFORMDW 유들자는 당신의 신체를 추잡하게 어루만지기 시작했다
	;아라이먼트부
	ELSE
		PRINTFORMW 「아라아라 무서워해 버리고 있는 거야? 라면 풀어 주지 않으면」
		PRINTFORMDW 유들자는 당신의 신체를 추잡하게 어루만지기 시작했다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이 강하게응답한다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「～응, 아직 그런 일 생각하고 있다」
		PRINTFORMW 「사실인가 어떤가 신체에 들어 주네요」
	;아라이먼트부
	ELSE
		PRINTFORMW 「～응, 아직 그런 일 생각하고 있다」
		PRINTFORMW 「조금 뜸을 떠 주네요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이 빈정거리는 것 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「입에서는 그렇게 말하고 있지만」
		PRINTFORMW 「신체는 어떨까요, 기다려지구나」
	;아라이먼트부
	ELSE
		PRINTFORMW 「힘껏의 허세라는 것일까?」
		PRINTFORMW 「좋아요, 그런 것 산산히 부숴 준다」
	ENDIF
	RETURN 1
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 태연하게 따르는 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 4
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일 하고 싶다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 5
ENDIF
[SKIPEND]

;─
;REACT1, 자신에게로의기분을얘기한다·개별 리액션
;─
;·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K20_1
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다! /2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「그러면 좋아하지도 않는거네, 유감」
		PRINTFORMW 「오늘은 나를 아주 좋아하게 되도록(듯이) 괴롭혀 준다」
	;아라이먼트부
	ELSE
		PRINTFORMW 「싫지 않아?   그 거 시시하네요」
		PRINTFORMW 「좀 더 괴롭히면 미움받아 버리는 거야?   그렇지 않으면 좋아하게 되어?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이 강하게응답한다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「그래? 그러면, 좋아하게 될 때까지 괴롭혀 준다♪」
		PRINTFORMW 
	;아라이먼트부
	ELSE
		PRINTFORMW 「, 그러면 싫은 여자에게 무엇 되어도 어쩔 수 없네요」
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이 빈정거리는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「화내거나 무엇을 하지 않아요. 좋아한다고 말할 때까지 괴롭혀 버릴 뿐이야♪」
		PRINTFORMW 
	;아라이먼트부
	ELSE
		PRINTFORMW 「어째서?   이렇게 즐거운데 화내다니」
		PRINTFORMW 「그러면… 구슬을 부수면, 좋아하게 되어 줄래?
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 적은지, REACT분류가 태연하게 따른다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「그것은 거짓말이군요. 심한 일 되어 좋아하게 된 것이지요, 변태씨」
		PRINTFORMW 
	;아라이먼트부
	ELSE
		PRINTFORMW 「뭐야 그것, 기분 나쁘다」
		PRINTFORMW 「이런 것 사랑받고 싶지 않기 때문에, 가득 심한 일 해요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 1000이상
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 4
	;부하 2는 연주한 (분)편이 뭔가 형편이 좋은 것 같아 우선
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(4) && TFLAG:REACT부하 < 2
		PRINTFORMW 「…… 아라, 좋아하게 되어 버렸어?」
		PRINTFORMW 「거짓말인가 정말인가 확인해 준다」
		RETURN 1
	ENDIF
	
	;=================================================
	;역강간으로 질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
		PRINTFORMW 「나의 일을 좋아하는가, 나의 신체를 좋아하는가. 이렇게 내 둬…」
		PRINTFORMW 「뭐 좋은 것, 가득, 가득 범해 주기 때문에」
		RETURN 1
	ENDIF
	
	;=================================================
	;섹스로 질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
		PRINTFORMW 「아라아라. 여자의 신체는 그러한 식으로 되어있다는 것일까」
		PRINTFORMW 「그렇지 않으면 보라색이 말하고 있었던 스톡홀름 증후군이라는 녀석일까, 우후후」
		RETURN 1
	ENDIF
	
	;오리지날에서는 FAVOR(0, 1000)의 개소입니다만,
	;TFLAG:ACT파생 == 4가 성립하는 시점에서 조건은 클리어 하고 있다고 생각되므로 일반 분기 취급으로 합니다@/L
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「고마워요, 나도 당신의 일을 아주 좋아해요」
		PRINTFORMW 「그러니까 오늘은 충분히 귀여워해 준다. 기쁘지요?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 5
	;=================================================
	;첫회
	;=================================================
	;IF 구상 기입 체크(0) && FIRSTREACT(5)
	;ENDIF
	
	;=================================================
	;역강간으로 질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
		PRINTFORMW 「범해지는 것이 버릇이 되어 버렸을까, 우후후」
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;섹스로 질내사정의 후
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
		PRINTFORMW 「요컨데 임신하고 싶은거네, 자궁으로 게다가의를 생각할 수 없는거네」
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「굉장한 일… 너무 조금 했는지 라고 생각하기도 했지만」
		PRINTFORMW 「다음은 어떤 일 해 줄까, 킥킥」
	;아라이먼트부
	ELSE
		PRINTFORMW 「그러면, 얼마나 굉장한 일 하면 싫게 되어 받을 수 있을까」
		PRINTFORMW 「설사약 먹여 엉덩이에 마개에서도 해 줄까?」
	ENDIF
	RETURN 1
ENDIF

;─
;REACT2, 성에대한이야기를한다·개별 리액션
;─
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K20_2
;-------------------------------------------------
RETURN 0

;─
;REACT3, 상냥하게위로한다·개별 리액션
;─
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K20_3
;-------------------------------------------------
RETURN 0

;─
;REACT4, 엄격하게위협한다·개별 리액션
;─
;·TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만
;-------------------------------------------------
@KOJO_REACT_K20_4
;-------------------------------------------------
RETURN 0

;─
;REACT5, 의상의변경을요구한다·개별 리액션
;─
;·조교 대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가 "소극적 따른다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가 "적극적 따른다", "태연하게 따른다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
@KOJO_REACT_K20_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 0
	;=================================================
	;한 장 벗었다(소극적 반응)
	;=================================================
	IF 구상 기입 체크(0) && IS_COMGRONAME("소극적 따른다/받아들인다")
	ENDIF
	
	;=================================================
	;2매 벗었다(적극적 반응)
	;=================================================
	IF 구상 기입 체크(0) && IS_COMGRONAME("적극적 따른다/태연하게 따른다")
	ENDIF
	
	;=================================================
	;벗지 않았다
	;=================================================
	IF 구상 기입 체크(0) && IS_COMGRONAME("거부")
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;한 장 입었다(소극적 반응)
	;=================================================
	IF 구상 기입 체크(0) && IS_COMGRONAME("소극적 따른다/받아들인다")
	ENDIF
	
	;=================================================
	;2매 입었다(적극적 반응)
	;=================================================
	IF 구상 기입 체크(0) && IS_COMGRONAME("적극적 따른다/태연하게 따른다")
	ENDIF
	
	;지문에서는 "싫어해"라고 하는 취급
	;=================================================
	;입지 않았다
	;=================================================
	IF 구상 기입 체크(0) && IS_COMGRONAME("거부")
	ENDIF
ENDIF
[SKIPEND]

;─
;REACT6, 합의를요구한다·개별 리액션
;─
;·조교 대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
@KOJO_REACT_K20_6
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따르는 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && IS_COMGRONAME("소극적 따른다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 마음대로 하면 좋다고 대답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 태연하게 따르는 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && IS_COMGRONAME("태연하게 따른다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && IS_COMGRONAME("적극적 따른다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게해 주는 것이라면 처음을 주어도 괜찮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들이는 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && IS_COMGRONAME("받아들인다")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서 "거부"에 세트 된다)
;-------------------------------------------------
IF 구상 기입 체크(0) && IS_COMGRONAME("거부")
ENDIF
[SKIPEND]

;─
;REACT7, 벌이라고선언한다·개별 리액션
;─
;·선언해도 벌이 발생한다고는 할 수 없다
;-------------------------------------------------
@KOJO_REACT_K20_7
;-------------------------------------------------
RETURN 0

;─
;REACT8, 고함치는·개별 리액션
;─
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K20_8
;-------------------------------------------------
RETURN 0

;─
;REACT9, 기분나쁘게웃는다·개별 리액션
;─
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K20_9
;-------------------------------------------------
RETURN 0