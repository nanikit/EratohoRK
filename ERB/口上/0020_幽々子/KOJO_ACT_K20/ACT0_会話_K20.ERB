;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	유유코 구상 회화계 액션 구상 파일
;
;	회화계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 ACT0～9
;========================================================================
;·TFLAG:ACT(0=지금의기분을얘기한다/1=자신에게로의기분을얘기한다/2=성에대한이야기를한다/3=상냥하게위로한다/4=엄격하게위협한다/5=의상의변경을요구한다/6=합의를요구한다/7=벌이라고선언한다/8=고함친다/9=기분나쁘게웃는다)
;·TFLAG:ACT분류 == 0에 해당
;·ACT9, 기분나쁘게웃는다는 이상계 ACT분류로부터 파생하는 경우가 있다
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------
;────────────────────────────────────
;ACT0, 지금의기분을얘기한다
;────────────────────────────────────
;·지금의 기분에 대해 들어 온다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K20_0
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 지금의기분을얘기한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「오늘의 상태는 어때?」
		PRINTFORMW 「… 정직하게 말하지 않으면 부수어져 버릴지도?」
		RETURN 1
	ENDIF
	
	;=================================================
	;역강간으로 질내사정의 뒤(조교 대상이 조교자에게 짜내진 후)
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「우후후, 잘 먹었어요」
			PRINTFORMW 「기분 좋았어?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「우후후, 좋았어요. 필사적으로 참는 얼굴도」
			PRINTFORMW 「쾌락에 져 보기 흉하게 내 버리는 곳도」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 조교의 최초의 액션
	;=================================================
	IF 구상기입체크(100) && TFLAG:경과시간 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「어때? 오늘의 기분은? 행복? 기뻐?」
			PRINTFORMW 
		;아라이먼트부
		ELSE
			PRINTFORMW 「자자 즐거운 조교의 시작이야」
			PRINTFORMW 「응 어떤 기분? 어떤 기분?」
		ENDIF
		RETURN 1
	ENDIF
	
	;아라이먼트정		구상주가 반드시 『주인님』이다고는 할 수 없기 때문에, 대책 해 둡니다@/L
	IF ALI(0)
		PRINTFORMW 「저기,\@ TALENT:TARGET:주인님 ? 주인님 # 나 \@에 이런 일 당해 어떤 기분?   행복?」
	;아라이먼트부
	ELSE
		PRINTFORMW 「… 대단히 비참하구나,%CNAME(MASTER, TARGET)%」
		PRINTFORMW 「어떤 기분?   」
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT1, 자신에게로의기분을얘기한다
;────────────────────────────────────
;·자신의 일을 어떻게 생각할까 얘기한다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일 조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K20_1
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 자신에게로의기분을얘기한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「오늘무엇을 할까 결정하기 전에, 그렇구나…」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%, 나의 일… 좋아하네요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 조교의 최초의 액션
	;=================================================
	IF 구상기입체크(100) && TFLAG:경과시간 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「%CNAME(MASTER, TARGET)%, 나 어떻게 생각해?   를 좋아하는 사람?   싫어?」
			PRINTFORMW 
		;아라이먼트부
		ELSE
			PRINTFORMW 「나는 역시,%CNAME(MASTER, TARGET)%로부터 보면 무섭네요?」
			PRINTFORMW 「그렇지만 그것치고 그렇지만도 않은 것 같아… 이런 것 좋아하는 것이지요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	;IF 구상 기입 체크(0)
	;ENDIF
ENDIF

;────────────────────────────────────
;ACT2, 성에대한이야기를한다
;────────────────────────────────────
;·조교자가 성에 대한 지식을 말하기 시작한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·조교자에게 성지식이 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K20_2
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT2, 성에대한이야기를한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT3, 상냥하게위로한다
;────────────────────────────────────
;·조교자가 상냥한 말로 위로해 온다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K20_3
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 상냥하게위로한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT4, 엄격하게위협한다
;────────────────────────────────────
;·어려운 말로 위협한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K20_4
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화할 가능성이 있다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 엄격하게위협한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT5, 의상의변경을요구한다
;────────────────────────────────────
;·조교자가 의상의 변경을 요구해 온다
;·조교 대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:쓰러트리기중에는 발생하지 않는다
;·TEQUIP:줄이 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K20_5
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%에 옷을 벗어라고 명령했다…"되고 있어 이것이 변화하는 일은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;오리지날에서는 첫회 분기를 포함한, 각 소분기의 체크가 off였던 것입니다만
	;표시해도 문제 없는 것이라고 판단 했습니다@/L
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「자, 옷 벗어?」
			PRINTFORMW 「찢어도 상관없지만 새롭게 사 주지 않아요?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「자, 옷 벗어?」
			PRINTFORMW 「찢어도 상관없지만 새롭게 사 주지 않아요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 조교의 최초의 액션
	;=================================================
	IF 구상기입체크(100) && TFLAG:경과시간 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「자 벗어 해요～」
			PRINTFORMW 「기분 좋게 해 주니, 까요?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「이봐요, 벗어 해요」
			PRINTFORMDW 말은 부드럽지만 뭐라고도 말할 수 없는 위압감이 있다
		ENDIF
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「점점 기분 좋아져 온 것이겠지」
		PRINTFORMW 「자, 벗어 할까」
	;아라이먼트부
	ELSE
		PRINTFORMW 「아─, 방해군요, 이 헝겊」
		PRINTFORMW 「벗으세요, 아픈 생각 하고 싶지 않으면 말야」
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT5_1, 옷을 입도록(듯이) 명령
;────────────────────────────────────
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:ACT파생 == 1
;ENDIF

;────────────────────────────────────
;ACT6, 합의를요구한다
;────────────────────────────────────
;·처음을 받아도 괜찮을까 얘기한다
;·처음=처녀 or동정(양쪽 모두의 경우도 있다)
;·이미 합의를 얻고 있는 경우는 발생하지 않는다
;·조교 대상이 처녀도 동정도 아닌 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K20_6
;-------------------------------------------------
;함수@TARGET_DESCRIPTION로 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;후는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 합의를요구한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT7, 벌이라고선언한다
;────────────────────────────────────
;·지금부터 벌 한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:벌플래그는 서 있다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP:쓰러트리기중시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K20_7
;-------------------------------------------------
;여기에서는 지문의 변화는 없다. 분기의 증감은 기호로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, 벌이라고선언한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT8, 고함친다
;────────────────────────────────────
;·큰 소리로고함친다
;·턴방침이 "소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:벌플래그에 관련되지 않고, 발생할 가능성이 있다(벌중에 한층 더고함친다가능성등)
;-------------------------------------------------
@KOJO_ACT_K20_8
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 큰 소리로 고함쳤다…"만으로 파생은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 고함친다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;ACT9, 기분나쁘게웃는다
;────────────────────────────────────
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·화제 『비웃는다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K20_9
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 기분나쁘게웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF