;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	모미지 구상 회화계 액션 구상 파일
;
;	회화계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 ACT0～9
;========================================================================
;·TFLAG:ACT(0=지금의기분을얘기한다/1=자신에게로의기분을얘기한다/2=성에대한이야기를한다/3=상냥하게위로한다/4=엄격하게위협한다/5=의상의변경을요구한다/6=합의를요구한다/7=벌이라고선언한다/8=고함친다/9=기분나쁘게웃는다)
;·TFLAG:ACT분류 == 0에 해당
;·ACT9, 기분나쁘게웃는다는 이상계 ACT분류로부터 파생하는 경우가 있다
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "회화")로 줍는 것이 가능
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------
;────────────────────────────────────
;ACT0, 지금의기분을얘기한다
;────────────────────────────────────
;·지금의 기분에 대해 들어 온다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K41_0
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT0, 지금의기분을얘기한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「참고까지 (들)물어 두고 싶습니다만」
		PRINTFORMW 「조교되고 있는 입장으로서 지금 어떤 기분인 것입니까?」
		RETURN 1
	ENDIF
	
	;=================================================
	;성교 봉사계로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「저…… 나의 안에 내 버렸습니다만」
			PRINTFORMW 「어땠습니다?   기분 좋았던 것입니까?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「저…… 나의 안에 내 버렸습니다만」
			PRINTFORMW 「어떻게 합니까?   만약 임신해 버리면 큰 일이에요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;성교계로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「미, 미안합니다!   기분 좋아서 무심코 전부 내 버렸습니다!」
			PRINTFORMW 「저…… 혹시 화내 있거나 합니까?」
		;아라이먼트부
		;응 어떤 기분? 질내사정되어 어떤 기분?
		ELSE
			PRINTFORMW 「잘 먹었습니다. 굉장히 좋았던 것입니다」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%는 어땠습니까?」
		ENDIF
		RETURN LOCAL
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 기분, 들려주세요」
			PRINTFORMW 「나는 너무, 사람의 기분을 알 수 있는 요령 있는 성격은 아니기 때문에」
			PRINTFORMW 「입에 내 들려주면 기쁩니다」
		;아라이먼트부
		ELSE
			PRINTFORMW 「어떻습니까?   지금의 기분은」
			PRINTFORMW 「말하고 싶은 것이 있다면 (들)물어 줍니다」
			PRINTFORMW 「나도 너무 요령 있는 (분)편은 아니므로」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT1, 자신에게로의기분을얘기한다
;────────────────────────────────────
;·자신의 일을 어떻게 생각할까 얘기한다
;·조교 대상의 반응에 응해 REACT 이후에 ACT파생에의 대입을 한다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『호의』가 기출이 되어, 이후, 동일 조교중에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K41_1
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT1, 자신에게로의기분을얘기한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;성교 봉사계로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「안에…… 나와 버렸습니다」
			PRINTFORMW 「이런 일 해 두어 이제 와서일지도 모릅니다만」
			PRINTFORMW 「자신으로부터 남성을 덮쳐 안에 내게 하는 것 같은 나의 일, 어떻게 생각하고 있습니까?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「전부 내 버린 것이군요」
			PRINTFORMW 「후후……%CNAME(MASTER, TARGET)%에 있어, 나라는건 무엇이군요?」
			PRINTFORMW 「움직일 수 없는 자신을 조교하는 사람이(가) 아님입니까?」
			PRINTFORMW 「그렇지 않으면, 성욕을 해소시켜 주는 적당한 소녀입니까?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「저, 할 수 있으면, 정직하게 대답하기를 원합니다」
			PRINTFORMW 「나의 일, 좋아합니까?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「거짓말은 말하지 않고, 정직하게 대답해 주세요」
			PRINTFORMW 「나의 일, 좋아합니까?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT2, 성에대한이야기를한다
;────────────────────────────────────
;·조교자가 성에 대한 지식을 말하기 시작한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·조교자에게 성지식이 없으면 발생하지 않는다
;·화제 『지식』이 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K41_2
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT2, 성에대한이야기를한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「그럼, 조금 엣치한 이야기라도 할까요」
		PRINTFORMDW 모미지는%CALLNAME:MASTER%와 마주보면, 우쭐해하는 표정을 띄워 말했다
		PRINTFORMW 「………… 엣또, 엣또…… 다, 다음 올 때까지 생각해 옵니다」
		PRINTFORMDW 자주(잘) 생각하지 않아도 성 경험 따위 압도적으로 적은 것 같은 이 소녀의 일이다
		PRINTFORMDW 조금 전의 우쭐해하는 표정이 단순한 기세 뿐이었다고 하는 일은 알았다……
		RETURN 1
	ENDIF
	
	;=================================================
	;성교 봉사계로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「지금,%CNAME(MASTER, TARGET)%의 정자가 나의 안에 있습니다」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%와는 종족은 다릅니다만…… 아기는 할 수 있는 것일까요」
		;아라이먼트부
		ELSE
			PRINTFORMW 「압니까?   지금,%CNAME(MASTER, TARGET)%가 사양없이 낸 정자가 나의 중을 헤엄치고 있습니다」
			PRINTFORMW 「지금쯤, 나를 임신시키려고 노력하고 있는 것일까요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;성교계로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「덧붙여서 알고 있습니까?   개의 사정은, 굉장히 길다고 합니다」
			PRINTFORMDW …… 그러면, 이리는?
			PRINTFORMW 「에?   이리는……………… 에엣또……」
			PRINTFORMDW 이 소녀는 정말로 이리인 것이나 개인 것인가……
		;아라이먼트부
		;ELSE
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그런데, 나는 아시다시피 흰색이리 텐구입니다만」
		PRINTFORMW 「발정기, 는 있다고 생각합니까?」
		PRINTFORMW 「대답은 비밀입니다. 신경이 쓰입니까?」
		PRINTFORMW 「어째서는……\@ ALI(0) ? 다, 왜냐하면 알려지는 것 부끄러우며…… # 기대하고 있습니까?   내가 발정기가 되는 것 \@」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT3, 상냥하게위로한다
;────────────────────────────────────
;·조교자가 상냥한 말로 위로해 온다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·화제 『위로하고』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K41_3
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)
;@EQUIPACTION_DESCRIPTION에 의해 TEQUIP(안면승마/안면승마애널/자위중)으로 지문은 변화할 가능성이 있다
;모두 묻을 필요는 없기 때문에, 그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT3, 상냥하게위로한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && ALI(0) && FIRSTACT(0)
		PRINTFORMW 「조금…… 천천히 할까요」
		PRINTFORMDW 모미지는%CALLNAME:MASTER%의 머리에 손을 두면, 아이를 어르도록(듯이) 머리를 어루만졌다
		PRINTFORMW 「이런 일을 해도,%CNAME(MASTER, TARGET)%에 있어 나는 허락할 수 없는 상대일지도 모릅니다」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%가 지금, 이렇게 해 자유를 빼앗겨 갇히고 있는 원인은 나에게 있고」
		PRINTFORMW 「실제로 조교를 해, 심한 일을 하고 있는 것도 나이기 때문에」
		PRINTFORMW 「위의 명령이니까 어쩔 수 없는, 등이라고 말하는 것은 나 자신을 정당화 할 뿐(만큼)의 이유이며」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%에 대해서는 무슨변명으로도 되지 않습니다」
		PRINTFORMDW 모미지의 털의 결이 좋은 꼬리가%CALLNAME:MASTER%의 허리에 돌려진다
		PRINTFORMDW 살그머니 비어 있는 손으로 손댄 감촉은 마음 좋게, 마음이 휴식으로 간다……
		PRINTFORMW 「미안해요…… 사과해서 되는 문제도 아니며, 사과한 곳에서 이 현상은 아무것도 변함없습니다만」
		PRINTFORMW 「그런데도 미안해요…… 적어도,%CNAME(MASTER, TARGET)%가 망가져 버리지 않게, 노력할테니까」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「좋아 좋아…… 괜찮기 때문에」
			PRINTFORMDW 모미지는%CALLNAME:MASTER%의 머리를 위로하도록(듯이) 어루만진다
			PRINTFORMW 「나도 선배로부터, 이따금 이렇게 해 위로해 받습니다」
		;아라이먼트부
		ELSE
			PRINTFORMW 「좋아 좋아…… 괜찮기 때문에」
			PRINTFORMDW 모미지는%CALLNAME:MASTER%의 머리를 위로하도록(듯이) 어루만진다
			PRINTFORMW 「나도 선배로부터, 이따금 이렇게 해 위로해 받습니다」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT4, 엄격하게위협한다
;────────────────────────────────────
;·어려운 말로 위협한다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·화제 『위협한다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K41_4
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화할 가능성이 있다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT4, 엄격하게위협한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	;IF 구상 기입 체크(0) && FIRSTACT(0)
	;ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「훗!!」
			PRINTFORMDW 송곳니를 노출로 해, 모미지가 위협의 신음소리를 올린다.
			PRINTFORMDW 말이야말로 사랑스럽지만, 그 눈초리는 이리에 적당한 위압감을 휴대하고 있었다
		;아라이먼트부
		ELSE
			PRINTFORMW 「훗!!」
			PRINTFORMDW 송곳니를 노출로 해, 모미지가 위협의 신음소리를 올린다.
			PRINTFORMDW 말이야말로 사랑스럽지만, 그 눈초리는 이리에 적당한 살기를 휴대하고 있었다
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT5, 의상의변경을요구한다
;────────────────────────────────────
;·조교자가 의상의 변경을 요구해 온다
;·조교 대상의 반응에 응해, 몇매 벗든가, 혹은 벗지 않는 것인지에 REACT 이후에 파생한다
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;·TEQUIP:쓰러트리기중에는 발생하지 않는다
;·TEQUIP:줄이 실로 되어 있으면 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K41_5
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%에 옷을 벗어라고 명령했다…"되고 있어 이것이 변화하는 일은 없다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「그, 그, 그럼, 이군요」
			PRINTFORMW 「오, 옷을…… 벗으세요, 입니다」
			PRINTFORMDW 상당히 남자에게 옷을 벗게 하는데 익숙해지지 않았던 것일까, 모미지는 매우 긴장한 표정으로 명령을 내렸다
		;아라이먼트부
		ELSE
			PRINTFORMW 「그, 그, 그럼, 이군요」
			PRINTFORMW 「오, 옷을…… 벗으세요, 입니다」
			PRINTFORMDW 상당히 남자에게 옷을 벗게 하는데 익숙해지지 않았던 것일까, 모미지는 매우 긴장한 표정으로 명령을 내렸다
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「그, 그, 그럼, 이군요」
			PRINTFORMW 「오, 옷을…… 벗으세요, 입니다」
			PRINTFORMDW 남의 옷을 벗게 하는 것이 처음은 아니라고 말하는데, 모미지는 변함 없이 긴장한 표정으로 명령을 내렸다
		;아라이먼트부
		ELSE
			PRINTFORMW 「그럼, 옷을 벗어 주세요」
			PRINTFORMDW 처음의 때에 비하면 과연 익숙해졌는지, 모미지는 보통으로 명령을 내려 왔다
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「그럼…… 다음은 옷을 입어 주세요」
		PRINTFORMW 「이것으로 끝나는지, 입니까?」
		PRINTFORMW 「미안해요, 아직 끝은 아닙니다」
	;아라이먼트부
	ELSE
		PRINTFORMW 「그럼…… 다음은 옷을 입어 주세요」
		PRINTFORMW 「이것으로 끝나는지, 입니까?」
		PRINTFORMW 「아니오, 아직도 끝나지 않아요」
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT6, 합의를요구한다
;────────────────────────────────────
;·처음을 받아도 괜찮을까 얘기한다
;·처음=처녀 or동정(양쪽 모두의 경우도 있다)
;·이미 합의를 얻고 있는 경우는 발생하지 않는다
;·조교 대상이 처녀도 동정도 아닌 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·화제 『합의』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K41_6
;-------------------------------------------------
;함수@TARGET_DESCRIPTION로 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;후는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT6, 합의를요구한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「미안합니다…… 여기까지 할 생각은 없었던 것입니다만」
		PRINTFORMDW 모미지는%CALLNAME:MASTER%를 「덮고」상태로 하면,
		PRINTFORMDW 배후로 돌아%CALLNAME:MASTER%를 위로부터 억눌렀다
		PRINTFORMW 「%CNAME(MASTER, TARGET)%를 조교하고 있으면, 이제(벌써) 본능이 억제할 수 없어서」
		PRINTFORMDW 억누르는 모미지의 손은, 소녀의 것이라고는 생각되지 않을 정도 강력함
		PRINTFORMDW 등에 외곬의 식은 땀이 흐른다……
		PRINTFORMDW 전혀 안보이는 송곳니를 목덜미에 꽂을 수 있어 있는 것과 같다
		PRINTFORMDW %CALLNAME:MASTER%는 본능적으로, 사냥해지는 입장의 동물의 기분을 피부로 느꼈다
		PRINTFORMDW 즉, 지금의 모미지는 완전한 수렵자로서의 본능이 전면에 나와 있다고 하는 일이다
		PRINTFORMW 「어쩔 수 없고, 지금,%CNAME(MASTER, TARGET)%를\@ ALI(0) ? 사냥 # 유린 \@싶습니다」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%의 처음으로, 받아도 상관하지 않습니까?」
		RETURN 1
	ENDIF
	
	;=================================================
	;호감도 1000이상
	;=================================================
	IF 구상기입체크(100) && AFFECTION("권애")
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「%CNAME(MASTER, TARGET)%를 좀 더 기분 좋게 해 주고 싶습니다」
			PRINTFORMW 「사냥감은 아니고, 한사람의 대등한 상대로서」
			PRINTFORMW 「송곳니나 손톱은 아니고, 나의 전신을 사용해」
			PRINTFORMW 「그러니까…… 나를 받아들여 주겠습니까?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「좀 더%CNAME(MASTER, TARGET)%의 소리를 듣고 싶습니다」
			PRINTFORMW 「기분 좋은 것 같은 소리도, 아픈 것 같은 비명도」
			PRINTFORMW 「나의 모른다%CNAME(MASTER, TARGET)%의 소리, 좀 더 들려주세요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMDW 모미지는 강력한 손으로%CALLNAME:MASTER%를 억누르면, 배후로 돈다
		PRINTFORMW 「죄송합니다만, 슬슬 인내 할 수 없게졌습니다」
		PRINTFORMW 「\@ ALI(0) ? 또, 받아도 괜찮습니까?  # 조금의 사이, 나의 사냥감이 되어 주세요 \@」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT7, 벌이라고선언한다
;────────────────────────────────────
;·지금부터 벌 한다고 말하기 시작한다
;·이 ACT가 발생하고 있는 시점에서 TFLAG:벌플래그는 서 있다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP:쓰러트리기중시에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K41_7
;-------------------------------------------------
;여기에서는 지문의 변화는 없다. 분기의 증감은 기호로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT7, 벌이라고선언한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;전COM가 「기분좋게해줘」（이 철면피! ）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("기분좋게해줘")
		PRINTFORMW 「잊고는 있지 않습니까?   원래 이것은, 텐구가 부과한 벌인 것이에요」
		PRINTFORMW 「사물의 가장 요점인 본질만은 잊어서는 안됩니다」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「더하드하게」（이 돼지째! ）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("더하드하게")
		PRINTFORMW 「좀 더 강하게 해, 뭐라고 말해져야」
		PRINTFORMW 「나도, 조금 짐승의 본능이 다 억제할 수 있지 않게 되어 버려요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「용서를빈다」（허락해 받을 수 없었습니다）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("용서를빈다")
		PRINTFORMW 「개인의 심정으로서는 허락해 주고 싶은 기분도 있습니다만」
		PRINTFORMW 「룰상은, 허락해 줄 수 없는 상황이므로」
		PRINTFORMW 「죄송합니다만, 벌이 됩니다」
		RETURN 1
	ENDIF
	
	;=================================================
	;전COM가 「자위하기시작한다」（인의 이야기를 들어라! ）
	;=================================================
	IF 구상기입체크(100) && IS_PREVCOMNAME("자위하기시작한다")
		PRINTFORMW 「사람의 이야기를 듣지 않는다고 하는 것은」
		PRINTFORMW 「보통, 어떤 상대라도 화낸다고 생각하지 않습니까?」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMDW 모미지는 혀를 내밀면, 마른 자신의 입술을 낼름 빨았다
			PRINTFORMW 「너무 난폭한 일은 하고 싶지 않았던 것입니다만, 어쩔 수 없습니다」
			PRINTFORMW 「벌도 불가피, 으로 하도록 해 받습니다」
		;아라이먼트부
		ELSE
			PRINTFORMW 「여기로부터는 벌의 시간이 됩니다. 각오 해 주세요」
			PRINTFORMDW 모미지는 흥분을 숨길려고도 하지 않고, 송곳니를 드러내로 해 혀 핥음을 했다……
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT8, 고함친다
;────────────────────────────────────
;·큰 소리로고함친다
;·턴방침이 "소프트", "노멀"에서는 발생하지 않는다
;·TFLAG:벌플래그에 관련되지 않고, 발생할 가능성이 있다（벌중에 한층 더고함친다가능성등 ）
;-------------------------------------------------
@KOJO_ACT_K41_8
;-------------------------------------------------
;지문은"%CALLNAME:TARGET%는 %CALLNAME:MASTER%를 큰 소리로 고함쳤다…"만으로 파생은 없다
;-------------------------------------------------

;분기 마다의 내용에 차이가 없기 때문에 정리해 버립니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT8, 고함친다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	PRINTFORMW 「우─!   !   !!」
	PRINTFORMDW 모미지에 있어서는 고함치고 있을 생각 같다……
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT9, 기분나쁘게웃는다
;────────────────────────────────────
;·기분 나쁜 미소를 띄운다
;·이상계로부터 발생하는 경우도 있다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·화제 『비웃는다』가 기출이 되어, 이후, 동일 조교내에서는 억제된다
;-------------------------------------------------
@KOJO_ACT_K41_9
;-------------------------------------------------
;함수@TARGET_DESCRIPTION에 의해 TEQUIP(성교중/조교대상Ｃ사용/성교봉사중/자위중 & 2)로 지문은 변화한다
;그 외·조건 없음을 최초로 묻은 다음, 뒤는 기호로 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT9, 기분나쁘게웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;분기 마다의 내용에 차이가 없기 때문에 압축합니다@/L
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「왓훗훗훗……」
		PRINTFORMDW 는 잘 모르지만 무섭다……
		RETURN 1
	ENDIF
ENDIF