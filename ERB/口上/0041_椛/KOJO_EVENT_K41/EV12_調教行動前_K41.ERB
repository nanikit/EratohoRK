;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	모미지 구상 조교자 행동 전이벤트 구상 파일
;
;	조교자 행동 전이벤트 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV12, 조교자 행동 서두
;========================================================================
;·TRAIN_PROCCES.ERB내, @TRAIN_CANCEL로부터 불려 간다
;·구상주의 파라미터 표시의 직후에 표시. 조교 지시는 이미 결정되고 있다
;·조교중지선언의 교환이나, 조교 턴 개시전에 주인님으로부터 한 마디, 와 같이 쓰는 일도 가능
;·오전, 오후의 조교 개시는 아니고, 조교 턴의 개시전에는 매회 불려 가는 개소
;·그 때문에도 있어, 조교중지시와 경과시간 0이외의 개소는 IF 구상 기입 체크(25) 등으로서 확률 표시하는 일을 추천
;·조교중지시에게만 지문이 존재한다
;·문장 표시 후에 본체측의 DRAWLINE 처리 있어
;-------------------------------------------------
;FIRSTEVENT(0)～나, FIRSTEVENT(10)～등으로 해서 테두리마다 할당하고 있습니다만
;FIRSTEVENT/ONCEEVENT/RANDEVENT/ORDEREVENT는 사실상 무한하게 이벤트번호를 늘릴 수 있으므로
;필요에 따라서 임의에 할당해 주세요
;-------------------------------------------------
@KOJO_EVENT_K41_12(ARG)
;-------------------------------------------------
;인수 내역…
;ARG(0=조교가능/1=구상주의 피로로 중지/2=조교 대상의 피로로 중지/3= 포상에 의한 중지)
;-------------------------------------------------

;────────────────────────────────────
;조교중지 ARG:1 ARG:2 ARG:3 FIRSTEVENT(0)～
;────────────────────────────────────
;·피로, 혹은 포상에 의한 조교중지의 경우의 구상
;·기입 체크=1의 경우는 구상 표시 후 본체측에서 PRINTL의 공행이 들어간다
;·기입 체크를=2로 하면, 지문을 표시하지 않는다
;·이라고 할까 RESULT < 2로 판정되므로 2이상이라면 같은 거동이 됩니다만 일단 비추천이라고 하는 일로
;-------------------------------------------------

;오리지날은 RESULT치 1을 돌려주고 있습니다만, 구상측의 지문과 내용이 입으므로, RESULT치 2로 해 둡니다@/L

;━━━━━━━━━━━━━━━━━━━━━━━━
;포상에 의한 중지
;RESULT치（1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 3
	;기입 체크를 2 이하로 했을 경우는 이하의 지문이 표시된다. 표시 타이밍은 구상 표시의 뒤
	;%CALLNAME:TARGET%는 오늘의 조교를 중지해,
	;%CALLNAME:MASTER%멈추거나로 한 시간을 보냈다…
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자의 피로에 의한 중지
;RESULT치（1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	;기입 체크를 2 이하로 했을 경우는 이하의 지문이 표시된다. 표시 타이밍은 구상 표시의 뒤
	;조교의 시간이 왔지만, 들어 온 %CALLNAME:TARGET%는 어쩐지 피곤한 모습으로,
	;이번은 휴일이라고 말해 나갔다…
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(1)
		PRINTFORMW 「…… 오래 기다리셨습니다」
		PRINTFORMDW 방에 들어 온 모미지는, 분명하게 피곤한 것 같다
		PRINTFORMDW 꼬리는 힘 없게 처져, 후사후사로 하고 있던 꼬리의 털의 결도 흐트러지고 있다
		PRINTFORMW 「미안합니다…… 이번은 휴가로 합니다」
		PRINTFORMW 「상사에게도, 허가는 취해 있기 때문에……」
		PRINTFORMDW 휘청휘청한 발걸음으로, 모미지는 방을 나갔다……
		;오리지날은 RESULT치 1을 돌려주고 있습니다만, 구상측의 지문과 내용이 입으므로, RESULT치 2로 해 둡니다@/L
		RETURN 2
	ENDIF
	
	;-------------------------
	;첫회 이외
	;-------------------------
	PRINTFORMW 「오늘은 휴가로 합니다……」
	PRINTFORMDW 평상시의 일과 조교의 2켤레의 짚신으로 피곤할 것이다.
	PRINTFORMDW 휘청휘청한 발걸음으로, 모미지는 방을 나갔다……
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상의 피로에 의한 중지
;RESULT치（1, 구상의 뒤로 지문을 표시(비추천), 2, 구상만 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 2
	;기입 체크를 2 이하로 했을 경우는 이하의 지문이 표시된다. 표시 타이밍은 구상 표시의 뒤
	;조교의 시간이 왔지만, %CALLNAME:TARGET%는 심한 모습의 %CALLNAME:MASTER%를 봐 당분간 생각하면,
	;이번은 쉬어도 좋다고 말해 나갔다…
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(2)
		PRINTFORMW 「와…… 꽤 지친 것 같네요」
		PRINTFORMDW %CALLNAME:MASTER%를 한번 봐, 모미지는%CALLNAME:MASTER%의 피로의 색을 간파했다
		PRINTFORMW 「오늘은 휴가로 하기 때문에, 신체를 쉬게 해 주세요」
		PRINTFORMDW 벌일 것인데, 그런 달콤한 말로 좋은 것인지라고 물어 보면, 모미지는 성실한 얼굴을 했다
		PRINTFORMW 「우리의 목적은 벌을 주는 것이어,%CNAME(MASTER, TARGET)%를 부수는 것이 아니기 때문에」
		PRINTFORMDW 모미지는 다음에 물과 약을 가져올 것을 약속해, 방을 나갔다……
		RETURN 2
	ENDIF
	
	;-------------------------
	;첫회 이외
	;-------------------------
	PRINTFORMW 「더 이상은 무리인 것 같네요. 오늘은 휴가로 합니다」
	PRINTFORMDW 미안하다, 라고 일의 사이를 꿰매어 일부러 온 모미지에 사과하면, 모미지는 신경쓰지마 라고 하고 싶은 듯이 목을 옆에 흔들었다
	PRINTFORMW 「너무 해 버린 나에게도 비는 있을테니까」
	;여기는 RESULT치 1으로 해 둡니다@/L
	RETURN 1
ENDIF

;-------------------------------------------------
;·이하, ARG:0(조교가능) 상당
;·TRAIN_PROCCES.ERB내, @BEFORE_ACT로부터 불려 간다
;·이 시점에서 ACT는 벌써 결정하고 있지만 ACT파생의 결정은 이루어지지 않았다
;·NOWEX의 리셋트는 되지 않았다
;·이것들의 개소를 빠진 뒤로 ACT 개시가 된다
;·분기의 증감은 자유롭게 할 수 있습니다만, 우선도에는 주의합시다
;-------------------------------------------------

;────────────────────────────────────
;조교 개시 직후 FIRSTEVENT(10)～
;────────────────────────────────────
;·경과시간이 0인 경우에 표시된다
;·그 회의 조교 개시 직후에 한 번만으로, 이후 그 조교중에는 표시되지 않는다
;·이 개소를 빠진 뒤로 ACT 개시가 된다
;-------------------------------------------------
;  여기서 IS_POLICY("오늘", "벌") 등으로오늘의방침를 말하게 하는 일도 가능했다거나 합니다만
;  EVENT17, 오늘의 방침 구상과 입을 가능성이 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 개시 직후
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ! TFLAG:경과시간
;ENDIF

;────────────────────────────────────
;벌FIRSTEVENT(20)～
;────────────────────────────────────
;·TFLAG:벌플래그가 서 있는 경우에 표시되는 개소
;·벌플래그가 서 있어도, 가학계 이외의 ACT가 되는 경우가 있다
;-------------------------------------------------
;가학계 ACT, 파생하는 일에 의해 가학계에 포함될 가능성이 있는 이라마치오
;의상의변경을요구한다/기분나쁘게웃는다/애널바이브/후배위/애널섹스/자위/야외플레이/방뇨/밟핥기강제
;이상계 ACT/성교 봉사계 ACT
;상기의 ACT 이외는 TFLAG:벌플래그에 의해 ACT_ABLE로 규제된다(0616판 시점)
;-------------------------------------------------
;-------------------------------------------------
;·1회의 조교로 1회만 표시된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;벌개시 
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:벌플래그 && ONCEEVENT(20)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(20)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「죄송합니다만, 이대로는 날이 열리지 않으므로」
			PRINTFORMW 「벌을 개시하도록 해 받습니다」
		;아라이먼트부
		ELSE
			PRINTFORMW 「아직 이쪽을 얕잡아 보고 있는 것 같으므로」
			PRINTFORMW 「벌이 수단을 취하도록 해 받습니다」
			PRINTFORMW 「후후후, 실은 한 번은 해 보고 싶었던 것이군요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;전회 ACT가 벌선언
	;=================================================
	;IF 구상 기입 체크(0) && IS_PREVACTNAME("벌이라고선언한다")
	;ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「%CNAME(MASTER, TARGET)%도 질리지 않는 사람이군요」
			PRINTFORMW 모미지는 작게 한숨을 토하면서,%CALLNAME:MASTER%를 억눌렀다……
		;아라이먼트부
		ELSE
			PRINTFORMW 「설마 일부러 사람을 화나게 하거나 든지 하지 않지요?」
			PRINTFORMW 「뭐, 나도 점점 즐거워져 왔기 때문에 상관없지만」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;벌개시 이외를 처리하는 개소
;━━━━━━━━━━━━━━━━━━━━━━━━
;벌중
;구상 기입 체크(확률％, 입력한 숫자의 확률로 구상을 표시)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(50) && TFLAG:벌플래그
	;=================================================
	;전회 ACT가 벌선언 한편 이번 ACT가 가학계
	;=================================================
	;IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "가학") && IS_PREVACTNAME("벌이라고선언한다")
	;ENDIF
	
	;=================================================
	;전회 ACT가 벌선언
	;=================================================
	;IF 구상 기입 체크(0) && IS_PREVACTNAME("벌이라고선언한다")
	;ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「조금은 솔직해져 주세요」
			PRINTFORMW 「나도, 이런 아픈 것은 그다지 하고 싶지 않습니다……」
		;아라이먼트부
		ELSE
			PRINTFORMW 「벌 되는 기분은 어떻습니까?」
			PRINTFORMW 「기르는 개와 같습니다. 말하는 일을 (듣)묻지 않으면, 벌 되어도 어쩔 수 없어요?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;질내, 장내 사정 직후 FIRSTEVENT(30)～
;────────────────────────────────────
;NOWEX:사정내지 NOWEX:MASTER:사정으로 사정종의 참조도 가능했다거나 합니다만
;EVENT20, 절정 구상에 동종의 분기와 입을 가능성도 있습니다
;-------------------------------------------------
;관련 변수…
;
;TFLAG:전회질내사정(1=Ｖ성교/2=Ｖ성교 봉사/3=Ｖ성교(하늘 쏘아 맞히고)/4=Ｖ성교 봉사(하늘 쏘아 맞히고)/5=Ａ성교/6=Ａ성교 봉사/7=Ａ성교(하늘 쏘아 맞히고)/8=Ａ성교 봉사(하늘 쏘아 맞히고)
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정 직후(조교 대상→조교자)
;구상 기입 체크（확률％, 입력한 숫자의 확률로 구상을 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GROUPMATCH(TFLAG:전회질내사정, 2, 4) > 0
	;=================================================
	;드라이사정
	;=================================================
	;IF 구상 기입 체크(0) && TFLAG:전회질내사정 == 4
	;ENDIF
	
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(50) && TFLAG:전회질내사정 == 2
		PRINTFORMW 「앗…… 하아…… 하아……」
		PRINTFORMDW 모미지는 질내에 나온 정액의 뜨거움을 느끼는것 같이, 하복부에 손을 댄다
		PRINTFORMW 「안에, 나와 버린 것이군요……」
		PRINTFORMDW 참지 못하고 안에 내 버린 것을 사과하면, 모미지는 상기 한 표정으로 목을 옆에 흔들었다
		PRINTFORMW 「내가 스스로 한 것이기 때문에,%CNAME(MASTER, TARGET)%가 사과할 필요는 없습니다」
		PRINTFORMW 「그 대신…… 조금으로 좋으니까, 조금 전보다 약간, 나에게 빠져 주세요……」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내 사정 직후(조교 대상→조교자)
;구상 기입 체크（확률％, 입력한 숫자의 확률로 구상을 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && GROUPMATCH(TFLAG:전회질내사정, 6, 8)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;질내사정 직후(조교자→조교 대상)
;구상 기입 체크（확률％, 입력한 숫자의 확률로 구상을 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GROUPMATCH(TFLAG:전회질내사정, 1, 3)
	;=================================================
	;드라이사정
	;=================================================
	;IF 구상 기입 체크(0) && TFLAG:전회질내사정 == 3
	;ENDIF
	
	;=================================================
	;드라이사정 이외
	;=================================================
	IF 구상기입체크(50) && TFLAG:전회질내사정 == 1
		PRINTFORMW 「하아…… 후우……」
		PRINTFORMDW 숨을 거칠게 하면서, 모미지는 페니스를 뽑아 낸다
		PRINTFORMDW 모미지는 마음껏 질내에서 사정한 여운에 잠겨 있는 것 같다
		PRINTFORMW 「이것은…… 아기, 할 수 있거나 하는 것입니까?」
		PRINTFORMDW %CALLNAME:MASTER%는, 모르는, 으로 밖에 대답할 길이 없었다……
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;장내 사정 직후(조교자→조교 대상)
;구상 기입 체크（확률％, 입력한 숫자의 확률로 구상을 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && GROUPMATCH(TFLAG:전회질내사정, 5, 7)
;ENDIF

;────────────────────────────────────
;성교중·성교봉사중 FIRSTEVENT(40)～
;────────────────────────────────────
;관련 변수…
;TEQUIP:성교중(조교자가 조교 대상으로 삽입중) (1=정상위/2=후배위/3=기승위/4=대면좌위/5=배면좌위/6=애널섹스)
;TEQUIP:성교봉사중(조교 대상이 조교자에게 삽입중) 내용은 상기의 것과 같음
;
;관련 함수…
;V_SEX (=TARGET, TEQUIP:성교중 < 6/=MASTER, TEQUIP:성교봉사중 < 6)와 동의
;A_SEX (=TARGET, TEQUIP:성교중 == 6/MASTER, TEQUIP:성교봉사중 == 6)와 동의
;-------------------------------------------------
;오리지날로 확립 1%가 되어 있었던 것이 작자의 의도라고도 생각되지 않기 때문에, 독단과 편견으로 이쪽에서 결정해 버립니다@/L
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;성교중(조교자→조교 대상)
;구상 기입 체크（확률％, 입력한 숫자의 확률로 구상을 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TEQUIP:성교중
	;=================================================
	;페니스밴드 사용
	;=================================================
	IF 구상기입체크(50) && !PENIS(TARGET) && TEQUIP:조교자페니스밴드
		;성교계 ACT에 계속되는 경우
		IF MENUMATCH(TFLAG:ACT, "성교")
			PRINTFORMW 「아, 안됩니닷, 진짜의 자지가 아닌데」
			PRINTFORMW 「나도, 기분 좋아져, 허리가 멈추지 않습니닷」
		;그 이외
		ELSE
			PRINTFORMW 「자꾸자꾸, 들어가 버립니닷」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%안, 좀 더 안쪽까지……!」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;애널섹스(Ａ성교)
	;=================================================
	;IF 구상 기입 체크(0) && A_SEX()
	;ENDIF
	
	;=================================================
	;그 외의 성교계(Ｖ성교)
	;=================================================
	;IF 구상 기입 체크(0) && V_SEX()
	;ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;성교봉사중(조교 대상→조교자)
;구상 기입 체크（확률％, 입력한 숫자의 확률로 구상을 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TEQUIP:성교봉사중
	;=================================================
	;페니스밴드 사용
	;=================================================
	;IF 구상 기입 체크(0) && ! PENIS(MASTER) && TEQUIP:페니스밴드
	;ENDIF
	
	;=================================================
	;애널섹스(Ａ성교 봉사)
	;=================================================
	;IF 구상 기입 체크(0) && A_SEX(MASTER)
	;ENDIF
	
	;=================================================
	;그 외 성교 봉사계(Ｖ성교 봉사)
	;=================================================
	IF 구상기입체크(50) && V_SEX(MASTER)
		;성교 봉사계 ACT에 계속되는 경우
		IF MENUMATCH(TFLAG:ACT, "성적봉사")
			PRINTFORMW 「아웃, 기분, 좋앗, 입니닷」
			PRINTFORMDW 모미지는\@ GROUPMATCH(TEQUIP:성교봉사중, 3, 4) ? 하로부터 밀어올릴 수 있다 # \@쾌락에 당황하면서도 받아들여, 스스로 허리를 움직이고 있다
			PRINTFORMW 「아,%CNAME(MASTER, TARGET)%도, 좀 더, 움직여, 주세욧」
			PRINTFORMDW 그 모습은 마치, 사냥감을 탐내 먹는 진짜의 이리를 연상시켰다
		;그 외의 경우
		ELSE
			PRINTFORMW 「읏, 기분이 좋은, 입니까, 나의, 안은」
			;역레, 혹은대면좌위시킨다의 경우
			SIF GROUPMATCH(TEQUIP:성교봉사중, 3, 4)
				PRINTFORMDW 모미지가%CALLNAME:MASTER% 위에 걸쳐, 억누르도록(듯이)해 페니스를 문다
			PRINTFORMW 「이리, 머, 먹혀진닷, 기분은, 어떻습니까」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;절정 한 직후 FIRSTEVENT(50)～
;────────────────────────────────────
;·조교자, 조교 대상이 절정 했을 경우에 표시되는 구상
;·개별의 부위별 절정을 줍고 싶으면, IF 구상 기입 체크(0) && NOWEX:Ｃ절정등의 분기를 추가해 주세요
;·사정종은 NOWEX:사정/NOWEX:MASTER:사정으로 참조하는 것이 가능합니다
;-------------------------------------------------
;관련 변수…
;NOWEX:사정(1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;
;관련 함수…
;TRUECHECK 실로 되는 것이 몇개 있을까를 돌려준다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 절정 한 직후
;구상 기입 체크（확률％, 입력한 숫자의 확률로 구상을 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정) > 0
	;=================================================
	;조교자 사정
	;=================================================
	;IF 구상 기입 체크(0) && NOWEX:사정
	;ENDIF
	
	;각 부위의 절정이 1개이상의 경우에 실로 되는 개소
	;=================================================
	;다중절정
	;=================================================
	;IF 구상 기입 체크(0) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정) > 1
	;ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(25)
		PRINTFORMW 「아앗, 아아아…… 와 버리는, 와 버립니다…………!」
		PRINTFORMW 「핫, 핫, 하앗, 하앗, 하앗, 하앗…………」
		PRINTFORMW 「이, 갑니닷, 앗, 와웃, 아웃, 와웃, 하웃,」
		PRINTFORMW 「――――!!   후와 아 아!!」
		PRINTFORMDW 모미지는 그 가는 신체를 진동시켜 쾌감을 전신으로 받아 들이면서 달했다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 절정 한 직후
;구상 기입 체크（확률％, 입력한 숫자의 확률로 구상을 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정) > 0
	;=================================================
	;사정
	;=================================================
	IF 구상기입체크(25) && NOWEX:MASTER:사정
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「우와─, 우와─」
			PRINTFORMDW 토해내진 정액을, 모미지는 흠칫흠칫 손가락끝으로 건져 올려 바라본다
			PRINTFORMW 「이상한 냄새…… 정액은, 이상하네요」
			PRINTFORMDW 과연 입에 넣는 것은 저항이 있는지, 모미지는 손가락끝으로 실을 끌게 할 수 있도록(듯이) 정액을 다만 만져 놀고 있다
		;아라이먼트부
		ELSE
			PRINTFORMW 「우와아…… 많이 냈어요」
			PRINTFORMDW 흩날린 정액을, 모미지는 손가락끝으로 건져 올리면 입에 넣었다
			PRINTFORMW 「…… 맛있지는 않네요」
			PRINTFORMDW 눈썹을 찡그려, 모미지는 입가를 닦았다
		ENDIF
		RETURN 1
	ENDIF
	
	;각 부위의 절정이 1개이상의 경우에 실로 되는 개소
	;=================================================
	;다중절정
	;=================================================
	IF 구상기입체크(35) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정) > 1
		IF RAND:2
			PRINTFORMW 「지금의%CNAME(MASTER, TARGET)%, 굉장한 달하는 방법이었어요」
			PRINTFORMW 「참고까지 묻습니다만, 기분 좋았던 것은 어디서 했어?」
			PRINTFORMW 「가슴입니까?   엉덩이입니까?   그렇지 않으면 제일 민감한 곳입니까?」
			PRINTFORMW 「설마, 전부였다거나 합니까?」
		ELSE
			PRINTFORMW 「사물을 다한다, 라고는 이런 일을 말하는 것일까요」
			PRINTFORMW 「신체를 만지는 것으로 저기까지 격렬하게 절정 하다니……」
			PRINTFORMW 「참고로는 됩니다만, 자신이 되면 좋겠다고 생각하면 조금 무서울지도입니다」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(25)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「달했어요?」
			PRINTFORMDW 모미지는 돌보도록(듯이), 살그머니%CALLNAME:MASTER%의 손을 잡았다……
		;아라이먼트부
		ELSE
			PRINTFORMW 「가 버렸어요?」
			PRINTFORMDW 모미지는 추격을 걸치도록(듯이),%CALLNAME:MASTER%의 민감한 부분을 손가락끝으로 훑었다……
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;조교 방침 변경 FIRSTEVENT(60)
;────────────────────────────────────
;·조교 방침이 바뀌었을 때에 표시한다
;·어디까지나 방침인 것으로 행동을 세세하게 결정하는 것은 아니다
;-------------------------------------------------
;관련 변수…
;TFLAG:턴방침		이 턴의 조교 방침(1=휴식/2=소프트/3=노멀/4=하드/5=이상)
;TFLAG:전턴방침		전턴의 조교 방침(내용은 턴방침과 같음)
;
;관련 함수…
;IS_POLICY	조교 방침 명칭과 합치하면 진을 돌려준다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 방침 변경
;구상 기입 체크（확률％, 입력한 숫자의 확률로 구상을 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:턴방침 != TFLAG:전턴방침
	;=================================================
	;턴방침 1:휴식（확실히 체력 회복 행동이 된다）
	;=================================================
	IF 구상기입체크(25) && IS_POLICY("지금", "휴식")
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「조금 휴식으로 합시다」
			PRINTFORMW 「끝까지 이 페이스에서는, 서로 신체가 가지지 않기 때문에」
		;아라이먼트부
		ELSE
			PRINTFORMW 「조금 휴식으로 합니다」
			PRINTFORMW 「회복하면 또 재개하기 때문에, 지금 천천히 쉬어 주세요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 2:소프트
	;=================================================
	IF 구상기입체크(25) && IS_POLICY("지금", "소프트")
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「그럼, 좀 더 가볍게 갑시다」
			PRINTFORMW 「이 정도라면, 그다지 부담도 걸리지 않는다고 생각합니다」
		;아라이먼트부
		ELSE
			PRINTFORMW 「그럼, 가벼운 조교에 되돌립시다」
			PRINTFORMW 「가벼운 조교이기 때문에, 제대로 따르게 하는군요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 3:노멀
	;=================================================
	IF 구상기입체크(25) && IS_POLICY("지금", "노멀")
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「그럼, 보통으로 할까요」
			PRINTFORMW 「역시, 보통이 제일 기본이군요」
			PRINTFORMW 「…… 수수한 것은…… 조금 슬프지만……」
			PRINTFORMDW 보통인 일에 뭔가 트라우마이기도 할까……
			PRINTFORMDW 어차피 나에게는 서 그림도 회화도 스펠 카드도…… 라든지, 작은 소리로 들린 것 같은 생각이 들었다
			;스펠 카드는 할 수 있었습니다만 뭐 이대로 해 둡시다
		;아라이먼트부
		ELSE
			PRINTFORMW 「그럼, 보통으로 합시다」
			PRINTFORMW 「조금 지루할지도 모릅니다만, 무슨 일도 밸런스가 큰 일이니까요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 4:하드
	;=================================================
	IF 구상기입체크(100) && IS_POLICY("지금", "하드")
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「그러면, 조금 조금 강하게 가네요」
			PRINTFORMW 「괴로울지도 모릅니다만, 미안합니다……」
		;아라이먼트부
		ELSE
			PRINTFORMW 「그러면, 좀 더 강하게 하네요」
			PRINTFORMW 「조금 하드하게 가지 않으면, 조교라고는 말할 수 없고」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;턴방침 5:이상
	;=================================================
	IF 구상기입체크(100) && IS_POLICY("지금", "이상")
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「응…… 너무 기분은 타지 않지만……」
			PRINTFORMW 「필요하면, 할 수 밖에 없습니다」
		;아라이먼트부
		ELSE
			PRINTFORMW 「나도 너무 자신을 이상하다고 생각한 일은 없습니다만」
			PRINTFORMW 「하는 이상에는, 결론지어 시켜 받습니다」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;조건 없음 FIRSTEVENT(70)～
;────────────────────────────────────
;·여기까지의 조건에 히트 하지 않았던 경우에 표시되는 개소
;·BASE로 소분기를 마련해 있습니다만, 임의의 조건을 추가하는 일도 할 수 있습니다
;------------------------------------------------
;관련 변수…
;BASE		(0, 체력/1, 기력/2, 사정/3, 모유/4, 소변욕/5, 이성/6, 흥미/7, 초조/8, 만족)
;
;관련 함수…
;TENSION	인수로 지정한 BASE치를 나타낸다(50%이상은 높은,25%이상은 일반,25% 미만은 낮다)
;------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;조건 없음
;구상 기입 체크（확률％, 입력한 숫자의 확률로 구상을 표시）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0)
	;=================================================
	;만족 50％이상
	;=================================================
	IF 구상 기입 체크(0) && TENSION(TARGET, 8) == "높다"
	ENDIF
	
	;=================================================
	;이성 25％이하
	;=================================================
	IF 구상 기입 체크(0) && TENSION(TARGET, 5) == "낮다"
	ENDIF
	
	;=================================================
	;흥미 25％이하
	;=================================================
	IF 구상 기입 체크(0) && TENSION(TARGET, 6) == "낮다"
	ENDIF
	
	;=================================================
	;초조 50％이상
	;=================================================
	IF 구상 기입 체크(0) && TENSION(TARGET, 7) == "높다"
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상 기입 체크(0)
	ENDIF
ENDIF
[SKIPEND]