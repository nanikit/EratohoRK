;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	椛口上 調教者行動前イベント口上ファイル
;
;	調教者行動前イベント口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV12, 調教者行動前口上
;========================================================================
;・TRAIN_PROCCES.ERB内、@TRAIN_CANCELから呼び出される
;・口上主のパラメータ表示の直後に表示。調教指示は既に決定されている
;・調教中止宣言の差し替えや、調教ターン開始前にご主人様から一言、のように書くことも可能
;・午前、午後の調教開始ではなく、調教ターンの開始前には毎回呼び出される箇所
;・その為もあって、調教中止時と経過時間0以外の箇所はIF 口上記入チェック(25)等として確率表示する事を推奨
;・調教中止時にのみ地の文が存在する
;・文章表示後に本体側のDRAWLINE処理あり
;-------------------------------------------------
;FIRSTEVENT(0)～や、FIRSTEVENT(10)～などとして枠毎に割り当てていますが
;FIRSTEVENT/ONCEEVENT/RANDEVENT/ORDEREVENTは事実上無限にイベント番号を増やせるので
;必要に応じて任意に割り当てて下さい
;-------------------------------------------------
@KOJO_EVENT_K41_12(ARG)
;-------------------------------------------------
;引数内訳…
;ARG(0=調教可/1=口上主の疲労で中止/2=調教対象の疲労で中止/3=ご褒美による中止)
;-------------------------------------------------

;────────────────────────────────────
;調教中止 ARG:1 ARG:2 ARG:3 FIRSTEVENT(0)～
;────────────────────────────────────
;・疲労、もしくはご褒美による調教中止の場合の口上
;・記入チェック=1の場合は口上表示後本体側でPRINTLの空行が入る
;・記入チェックを=2にすると、地の文を表示しない
;・というかRESULT < 2で判定されるので2以上なら同じ挙動になりますが一応非推奨ということで
;-------------------------------------------------

;オリジナルはRESULT値1を返しているのですが、口上側の地の文と内容が被るので、RESULT値2にしておきます@/L

;━━━━━━━━━━━━━━━━━━━━━━━━
;ご褒美による中止
;RESULT値（1, 口上の後に地の文を表示(非推奨)、2, 口上のみ表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && ARG == 3
	;記入チェックを2以下にした場合は以下の地の文が表示される。表示タイミングは口上表示の後
	;%CALLNAME:TARGET%は今日の調教を中止し、
	;%CALLNAME:MASTER%とまったりとした時間を過ごした…
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者の疲労による中止
;RESULT値（1, 口上の後に地の文を表示(非推奨)、2, 口上のみ表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 1
	;記入チェックを2以下にした場合は以下の地の文が表示される。表示タイミングは口上表示の後
	;調教の時間が来たが、入ってきた%CALLNAME:TARGET%はなにやら疲れている様子で、
	;今回は休みだと言って出ていった…
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(1)
		PRINTFORMW 「わふぅ……お待たせしました」
		PRINTFORMDW 部屋に入ってきた椛は、明らかに疲れているようだ
		PRINTFORMDW 尻尾は力なく垂れ下がり、フサフサとしていた尻尾の毛並みも乱れている
		PRINTFORMW 「すみません……今回はお休みにします」
		PRINTFORMW 「上司にも、許可は取ってありますので……」
		PRINTFORMDW ふらふらとした足取りで、椛は部屋を出て行った……
		;オリジナルはRESULT値1を返しているのですが、口上側の地の文と内容が被るので、RESULT値2にしておきます@/L
		RETURN 2
	ENDIF
	
	;-------------------------
	;初回以外
	;-------------------------
	PRINTFORMW 「今日はお休みにします……」
	PRINTFORMDW 普段の仕事と調教の二足の草鞋で疲れているのだろう。
	PRINTFORMDW ふらふらとした足取りで、椛は部屋を出て行った……
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象の疲労による中止
;RESULT値（1, 口上の後に地の文を表示(非推奨)、2, 口上のみ表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 2
	;記入チェックを2以下にした場合は以下の地の文が表示される。表示タイミングは口上表示の後
	;調教の時間が来たが、%CALLNAME:TARGET%は酷い様子の%CALLNAME:MASTER%を見てしばらく考えると、
	;今回は休んでいいと言って出ていった…
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(2)
		PRINTFORMW 「わ……かなりお疲れのようですね」
		PRINTFORMDW %CALLNAME:MASTER%を一目見て、椛は%CALLNAME:MASTER%の疲労の色を見て取った
		PRINTFORMW 「今日はお休みにしますから、身体を休めてください」
		PRINTFORMDW 罰のはずなのに、そんな甘いことでいいのかと尋ねてみると、椛は真面目な顔をした
		PRINTFORMW 「私たちの目的は罰を与えることであって、%CNAME(MASTER, TARGET)%を壊すことではありませんから」
		PRINTFORMDW 椛は後で水と薬を持ってくることを約束し、部屋を出て行った……
		RETURN 2
	ENDIF
	
	;-------------------------
	;初回以外
	;-------------------------
	PRINTFORMW 「これ以上は無理そうですね。今日はお休みにします」
	PRINTFORMDW 申し訳ない、と仕事の合間を縫ってわざわざ来た椛に詫びると、椛は気にするなと言いたげに首を横に振った
	PRINTFORMW 「やりすぎてしまった私にも非はありますから」
	;ここはRESULT値1にしておきます@/L
	RETURN 1
ENDIF

;-------------------------------------------------
;・以下、ARG:0(調教可)相当
;・TRAIN_PROCCES.ERB内、@BEFORE_ACTから呼び出される
;・この時点でACTはすでに決定しているがACT派生の決定はなされていない
;・NOWEXのリセットはなされていない
;・これらの箇所を抜けた後にACT開始になる
;・分岐の増減は自由にできますが、優先度には注意しましょう
;-------------------------------------------------

;────────────────────────────────────
;調教開始直後 FIRSTEVENT(10)～
;────────────────────────────────────
;・経過時間が0である場合に表示される
;・その回の調教開始直後に一度きりで、以後その調教中には表示されない
;・この箇所を抜けた後にACT開始になる
;-------------------------------------------------
;　ここでIS_POLICY("今日", "お仕置き")等で今日の方針を喋らせる事も可能だったりしますが
;　EVENT17, 本日の方針口上と被る可能性があります
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教開始直後
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && !TFLAG:経過時間
;ENDIF

;────────────────────────────────────
;お仕置き FIRSTEVENT(20)～
;────────────────────────────────────
;・TFLAG:お仕置きフラグが立っている場合に表示される箇所
;・お仕置きフラグが立っていても、加虐系以外のACTになる場合がある
;-------------------------------------------------
;加虐系ACT、派生する事により加虐系に含まれる可能性のあるイラマチオ
;衣装の変更を要求する/不気味に笑う/アナルバイブ/後背位/アナルセックス/自慰/野外プレイ/放尿/足舐め強制
;異常系ACT/性交奉仕系ACT
;上記のACT以外はTFLAG:お仕置きフラグによってACT_ABLEで規制される(0616版時点)
;-------------------------------------------------
;-------------------------------------------------
;・一回の調教で一回のみ表示される
;━━━━━━━━━━━━━━━━━━━━━━━━
;お仕置き開始 
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:お仕置きフラグ && ONCEEVENT(20)
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(100) && FIRSTEVENT(20)
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「すみませんが、このままでは埒があきませんので」
			PRINTFORMW 「お仕置きを開始させていただきます」
		;アライメント負
		ELSE
			PRINTFORMW 「まだこちらを甘く見ているようですので」
			PRINTFORMW 「お仕置きの手段をとらせていただきます」
			PRINTFORMW 「ふふふ、実は一度はやってみたかったんですよね」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;前回ACTがお仕置き宣言
	;=================================================
	;IF 口上記入チェック(0) && IS_PREVACTNAME("お仕置きと宣言する")
	;ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「%CNAME(MASTER, TARGET)%も懲りない人ですね」
			PRINTFORMW 椛は小さく溜息を吐きながら、%CALLNAME:MASTER%を押さえつけた……
		;アライメント負
		ELSE
			PRINTFORMW 「まさかわざと人を怒らせたりとかしてないですよね？」
			PRINTFORMW 「まぁ、私もだんだん楽しくなってきたから構わないですけど」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;お仕置き開始以外を処理する箇所
;━━━━━━━━━━━━━━━━━━━━━━━━
;お仕置き中
;口上記入チェック(確率％, 入力した数字の確率で口上を表示)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(50) && TFLAG:お仕置きフラグ
	;=================================================
	;前回ACTがお仕置き宣言かつ今回ACTが加虐系
	;=================================================
	;IF 口上記入チェック(0) && MENUMATCH(TFLAG:ACT, "加虐") && IS_PREVACTNAME("お仕置きと宣言する")
	;ENDIF
	
	;=================================================
	;前回ACTがお仕置き宣言
	;=================================================
	;IF 口上記入チェック(0) && IS_PREVACTNAME("お仕置きと宣言する")
	;ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「少しは素直になってください」
			PRINTFORMW 「私だって、こういう痛いことはあまりしたくないです……」
		;アライメント負
		ELSE
			PRINTFORMW 「おしおきされる気分はどうですか？」
			PRINTFORMW 「飼い犬と同じです。言うことを聞かなければ、お仕置きされても仕方ないんですよ？」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;膣内、腸内射精直後 FIRSTEVENT(30)～
;────────────────────────────────────
;NOWEX:射精ないしNOWEX:MASTER:射精で射精種の参照も可能だったりしますが
;EVENT20, 絶頂口上に同種の分岐がると被る可能性もあります
;-------------------------------------------------
;関連変数…
;
;TFLAG:前回中出し(1=Ｖ性交/2=Ｖ性交奉仕/3=Ｖ性交(空射)/4=Ｖ性交奉仕(空射)/5=Ａ性交/6=Ａ性交奉仕/7=Ａ性交(空射)/8=Ａ性交奉仕(空射)
;NOWEX:射精(1=空射精/2=射精/3=大量射精/4=射精(早漏)/5=A責射精)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;膣内射精直後(調教対象→調教者)
;口上記入チェック（確率％, 入力した数字の確率で口上を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && GROUPMATCH(TFLAG:前回中出し, 2, 4) > 0
	;=================================================
	;空射精
	;=================================================
	;IF 口上記入チェック(0) && TFLAG:前回中出し == 4
	;ENDIF
	
	;=================================================
	;空射精以外
	;=================================================
	IF 口上記入チェック(50) && TFLAG:前回中出し == 2
		PRINTFORMW 「あっ……はぁ……はぁ……」
		PRINTFORMDW 椛は膣内に出された精液の熱さを感じるかのように、下腹部に手を当てる
		PRINTFORMW 「中に、出されちゃったんですね……」
		PRINTFORMDW 我慢できずに中に出してしまったことを詫びると、椛は上気した表情で首を横に振った
		PRINTFORMW 「私が自分でしたことですから、%CNAME(MASTER, TARGET)%が謝る必要はありません」
		PRINTFORMW 「そのかわり……少しでいいから、さっきより少しだけ、私に溺れてください……」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;腸内射精直後(調教対象→調教者)
;口上記入チェック（確率％, 入力した数字の確率で口上を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && GROUPMATCH(TFLAG:前回中出し, 6, 8)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;膣内射精直後(調教者→調教対象)
;口上記入チェック（確率％, 入力した数字の確率で口上を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && GROUPMATCH(TFLAG:前回中出し, 1, 3)
	;=================================================
	;空射精
	;=================================================
	;IF 口上記入チェック(0) && TFLAG:前回中出し == 3
	;ENDIF
	
	;=================================================
	;空射精以外
	;=================================================
	IF 口上記入チェック(50) && TFLAG:前回中出し == 1
		PRINTFORMW 「はぁ……ふぅ……」
		PRINTFORMDW 息を荒げながら、椛はペニスを引き抜く
		PRINTFORMDW 椛は思いっきり膣内で射精した余韻に浸っているようだ
		PRINTFORMW 「これって……赤ちゃん、出来たりするんでしょうか？」
		PRINTFORMDW %CALLNAME:MASTER%は、分からない、としか答えようがなかった……
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;腸内射精直後(調教者→調教対象)
;口上記入チェック（確率％, 入力した数字の確率で口上を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && GROUPMATCH(TFLAG:前回中出し, 5, 7)
;ENDIF

;────────────────────────────────────
;性交中・性交奉仕中 FIRSTEVENT(40)～
;────────────────────────────────────
;関連変数…
;TEQUIP:性交中(調教者が調教対象に挿入中) (1=正常位/2=後背位/3=騎乗位/4=対面座位/5=背面座位/6=アナルセックス)
;TEQUIP:性交奉仕中(調教対象が調教者に挿入中) 内容は上記のものと同じ
;
;関連関数…
;V_SEX (=TARGET, TEQUIP:性交中 < 6/=MASTER, TEQUIP:性交奉仕中 < 6)と同義
;A_SEX (=TARGET, TEQUIP:性交中 == 6/MASTER, TEQUIP:性交奉仕中 == 6)と同義
;-------------------------------------------------
;オリジナルで確立1%になっていたのが作者の意図とも思えないので、独断と偏見でこちらで決めてしまいます@/L
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;性交中(調教者→調教対象)
;口上記入チェック（確率％, 入力した数字の確率で口上を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TEQUIP:性交中
	;=================================================
	;ペニスバンド使用
	;=================================================
	IF 口上記入チェック(50) && !PENIS(TARGET) && TEQUIP:調教者ペニスバンド
		;性交系ACTに続く場合
		IF MENUMATCH(TFLAG:ACT, "性交")
			PRINTFORMW 「だ、ダメですっ、本物のおちんちんじゃないのに」
			PRINTFORMW 「私も、気持ちよくなって、腰が止まらないですっ」
		;それ以外
		ELSE
			PRINTFORMW 「どんどん、入っていっちゃいますっ」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%の中っ、もっと奥まで……っ！」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;アナルセックス(Ａ性交)
	;=================================================
	;IF 口上記入チェック(0) && A_SEX()
	;ENDIF
	
	;=================================================
	;その他の性交系(Ｖ性交)
	;=================================================
	;IF 口上記入チェック(0) && V_SEX()
	;ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;性交奉仕中(調教対象→調教者)
;口上記入チェック（確率％, 入力した数字の確率で口上を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TEQUIP:性交奉仕中
	;=================================================
	;ペニスバンド使用
	;=================================================
	;IF 口上記入チェック(0) && !PENIS(MASTER) && TEQUIP:ペニスバンド
	;ENDIF
	
	;=================================================
	;アナルセックス(Ａ性交奉仕)
	;=================================================
	;IF 口上記入チェック(0) && A_SEX(MASTER)
	;ENDIF
	
	;=================================================
	;その他性交奉仕系(Ｖ性交奉仕)
	;=================================================
	IF 口上記入チェック(50) && V_SEX(MASTER)
		;性交奉仕系ACTに続く場合
		IF MENUMATCH(TFLAG:ACT, "性奉")
			PRINTFORMW 「あうっ、気持ちっ、いいっ、ですっ」
			PRINTFORMDW 椛は\@ GROUPMATCH(TEQUIP:性交奉仕中, 3, 4) ? 下から突き上げられる # \@快楽に戸惑いながらも受け入れ、自分で腰を動かしている
			PRINTFORMW 「あ、%CNAME(MASTER, TARGET)%もっ、もっと、動いて、くださいっ」
			PRINTFORMDW その姿はさながら、獲物を貪り食う本物の狼を連想させた
		;その他の場合
		ELSE
			PRINTFORMW 「んっ、気持ちいい、ですかっ、私の、中はっ」
			;逆レ、もしくは対面座位させるの場合
			SIF GROUPMATCH(TEQUIP:性交奉仕中, 3, 4)
				PRINTFORMDW 椛が%CALLNAME:MASTER%の上に跨り、押さえつけるようにしてペニスを咥えこむ
			PRINTFORMW 「狼、にっ、食べられるっ、気分は、どうですかっ」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;絶頂した直後 FIRSTEVENT(50)～
;────────────────────────────────────
;・調教者、調教対象が絶頂した場合に表示される口上
;・個別の部位別絶頂を拾いたいなら、IF 口上記入チェック(0) && NOWEX:Ｃ絶頂等の分岐を追加してください
;・射精種はNOWEX:射精/NOWEX:MASTER:射精で参照する事が可能です
;-------------------------------------------------
;関連変数…
;NOWEX:射精(1=空射精/2=射精/3=大量射精/4=射精(早漏)/5=A責射精)
;
;関連関数…
;TRUECHECK 真になるものがいくつあるかを返す
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が絶頂した直後
;口上記入チェック（確率％, 入力した数字の確率で口上を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TRUECHECK(NOWEX:Ｃ絶頂, NOWEX:Ｖ絶頂, NOWEX:Ａ絶頂, NOWEX:Ｂ絶頂) > 0
	;=================================================
	;調教者射精
	;=================================================
	;IF 口上記入チェック(0) && NOWEX:射精
	;ENDIF
	
	;各部位の絶頂が1つ以上の場合に真になる箇所
	;=================================================
	;多重絶頂
	;=================================================
	;IF 口上記入チェック(0) && TRUECHECK(NOWEX:Ｃ絶頂, NOWEX:Ｖ絶頂, NOWEX:Ａ絶頂, NOWEX:Ｂ絶頂) > 1
	;ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(25)
		PRINTFORMW 「ああっ、あああ……来ちゃう、来ちゃいます…………！」
		PRINTFORMW 「はっ、はっ、はぁっ、はぁっ、はぁっ、はぁっ…………」
		PRINTFORMW 「い、イキますっ、あっ、わうっ、あうっ、わうっ、はうっ、」
		PRINTFORMW 「――――！！　ふわああああぁぁぁぁぁぁぁぁぁぁっ！！」
		PRINTFORMDW 椛はその細い身体を震わせ、快感を全身で受け止めながら達した
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が絶頂した直後
;口上記入チェック（確率％, 入力した数字の確率で口上を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TRUECHECK(NOWEX:MASTER:Ｃ絶頂, NOWEX:MASTER:Ｖ絶頂, NOWEX:MASTER:Ａ絶頂, NOWEX:MASTER:Ｂ絶頂) > 0
	;=================================================
	;射精
	;=================================================
	IF 口上記入チェック(25) && NOWEX:MASTER:射精
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「うわー、うわー」
			PRINTFORMDW 吐き出された精液を、椛は恐る恐る指先で掬い取って眺める
			PRINTFORMW 「変な匂い……精液って、不思議ですね」
			PRINTFORMDW さすがに口に含むのは抵抗があるのか、椛は指先で糸を引かせるように精液をただいじって遊んでいる
		;アライメント負
		ELSE
			PRINTFORMW 「うわぁ……たくさん出しましたね」
			PRINTFORMDW 飛び散った精液を、椛は指先で掬い取ると口に含んだ
			PRINTFORMW 「……美味しくはないですね」
			PRINTFORMDW 眉をしかめて、椛は口元を拭った
		ENDIF
		RETURN 1
	ENDIF
	
	;各部位の絶頂が1つ以上の場合に真になる箇所
	;=================================================
	;多重絶頂
	;=================================================
	IF 口上記入チェック(35) && TRUECHECK(NOWEX:MASTER:Ｃ絶頂, NOWEX:MASTER:Ｖ絶頂, NOWEX:MASTER:Ａ絶頂, NOWEX:MASTER:Ｂ絶頂) > 1
		IF RAND:2
			PRINTFORMW 「今の%CNAME(MASTER, TARGET)%、すごい達し方でしたよ」
			PRINTFORMW 「参考までに尋ねますが、気持ちよかったのはどこでした？」
			PRINTFORMW 「胸ですか？　お尻ですか？　それとも一番敏感なところですか？」
			PRINTFORMW 「まさか、全部だったりしますか？」
		ELSE
			PRINTFORMW 「物事を極める、とはこういうことを言うのでしょうか」
			PRINTFORMW 「身体をいじることであそこまで激しく絶頂するなんて……」
			PRINTFORMW 「参考にはなりますが、自分がされたらと思うとちょっと怖いかもです」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(25)
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「達しましたね？」
			PRINTFORMDW 椛は労わるように、そっと%CALLNAME:MASTER%の手を握った……
		;アライメント負
		ELSE
			PRINTFORMW 「イってしまいましたね？」
			PRINTFORMDW 椛は追い打ちをかけるように、%CALLNAME:MASTER%の敏感な部分を指先でなぞった……
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;調教方針変更 FIRSTEVENT(60)
;────────────────────────────────────
;・調教方針が変わったときに表示する
;・あくまでも方針なので行動を細かく決定するものではない
;-------------------------------------------------
;関連変数…
;TFLAG:ターン方針		このターンの調教方針(1=休憩/2=ソフト/3=ノーマル/4=ハード/5=異常)
;TFLAG:前ターン方針		前ターンの調教方針(内容はターン方針と同じ)
;
;関連関数…
;IS_POLICY	調教方針名称と合致すれば真を返す
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教方針変更
;口上記入チェック（確率％, 入力した数字の確率で口上を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && TFLAG:ターン方針 != TFLAG:前ターン方針
	;=================================================
	;ターン方針1:休憩（確実に体力回復行動になる）
	;=================================================
	IF 口上記入チェック(25) && IS_POLICY("今", "休憩")
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「少し休憩にしましょう」
			PRINTFORMW 「最後までこのペースでは、お互いに身体がもちませんからね」
		;アライメント負
		ELSE
			PRINTFORMW 「少し休憩にします」
			PRINTFORMW 「回復したらまた再開しますから、今のうちにゆっくり休んでください」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ターン方針2:ソフト
	;=================================================
	IF 口上記入チェック(25) && IS_POLICY("今", "ソフト")
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「では、もう少し軽く行きましょう」
			PRINTFORMW 「このくらいなら、あまり負担もかからないと思います」
		;アライメント負
		ELSE
			PRINTFORMW 「では、軽めの調教に戻しましょう」
			PRINTFORMW 「軽めの調教ですから、きちんと従えますよね？」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ターン方針3:ノーマル
	;=================================================
	IF 口上記入チェック(25) && IS_POLICY("今", "ノーマル")
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「では、普通にしましょうか」
			PRINTFORMW 「やっぱり、普通が一番基本ですよね」
			PRINTFORMW 「……地味なのは……ちょっと悲しいですけど……」
			PRINTFORMDW 普通であることに何かトラウマでもあるのだろうか……
			PRINTFORMDW どうせ私には立ち絵も会話もスペルカードも……とか、小声で聞こえたような気がした
			;スペルカードはできましたがまあこのままにしておきましょう
		;アライメント負
		ELSE
			PRINTFORMW 「では、普通にしましょう」
			PRINTFORMW 「ちょっと退屈かもしれませんが、何事もバランスが大事ですからね」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ターン方針4:ハード
	;=================================================
	IF 口上記入チェック(100) && IS_POLICY("今", "ハード")
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「それでは、少し強めに行きますね」
			PRINTFORMW 「辛いかもしれませんが、すみません……」
		;アライメント負
		ELSE
			PRINTFORMW 「それでは、もっと強くしますね」
			PRINTFORMW 「少しハードに行かないと、調教とは言えませんし」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;ターン方針5:異常
	;=================================================
	IF 口上記入チェック(100) && IS_POLICY("今", "異常")
		;アライメント正
		IF ALI(0)
			PRINTFORMW 「うーん……あまり気は乗りませんけど……」
			PRINTFORMW 「必要とあらば、やるしかないのです」
		;アライメント負
		ELSE
			PRINTFORMW 「私もあまり自分を異常だと思ったことはないですが」
			PRINTFORMW 「やるからには、割り切ってやらせてもらいます」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;条件なし FIRSTEVENT(70)～
;────────────────────────────────────
;・ここまでの条件にヒットしなかった場合に表示される箇所
;・BASEで小分岐を設けてありますが、任意の条件を追加する事もできます
;------------------------------------------------
;関連変数…
;BASE		(0, 体力/1, 気力/2, 射精/3, 母乳/4, 尿意/5, 理性/6, 興味/7, 苛立ち/8, 満足)
;
;関連関数…
;TENSION	引数で指定したBASE値を表す(50%以上は高い、25%以上は一般、25%未満は低い)
;------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;条件なし
;口上記入チェック（確率％, 入力した数字の確率で口上を表示）
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0)
	;=================================================
	;満足50％以上
	;=================================================
	IF 口上記入チェック(0) && TENSION(TARGET, 8) == "高い"
	ENDIF
	
	;=================================================
	;理性25％以下
	;=================================================
	IF 口上記入チェック(0) && TENSION(TARGET, 5) == "低い"
	ENDIF
	
	;=================================================
	;興味25％以下
	;=================================================
	IF 口上記入チェック(0) && TENSION(TARGET, 6) == "低い"
	ENDIF
	
	;=================================================
	;苛立ち50％以上
	;=================================================
	IF 口上記入チェック(0) && TENSION(TARGET, 7) == "高い"
	ENDIF
	
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
	ENDIF
ENDIF
[SKIPEND]