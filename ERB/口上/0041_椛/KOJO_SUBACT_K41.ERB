;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	화구상 탈의·추가 액션 구상 파일
;
;	조교자가 조교 지시 이외에 실시하는 행동의 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;─
;탈의 액션 구상
;─
;·조교자 액션의 앞에 강제 탈의나 자주 탈의가 발생했을 때에 표시
;·이 구상의 시점에서는 아직 TEQUIP는 갱신되어 있지 않다
;·첫회 분기는 「처음 보았다/보였다」란 할 수 없다.
;  숍 메뉴로 벗게 했던 적이 있을지도 모르고, 조교의 흐름으로 완전하게 벗게 하지 않고서 노출만 하고 있는 경우도 있다.
;  어쩌면 조수였던 때에 본 적이 있을지도 모른다. 그러한 의미에서는 간이적인 것이다.
;  엄밀한 의미에서의 「초견」플래그를 준비할지 어떨지는 요망하는 대로라고 하는 일로.
;-------------------------------------------------
@KOJO_DATUI_K41
;-------------------------------------------------
;관련 변수…
;강제 탈의:TFLAG:102  자주 탈의:TFLAG:103
;=(-1, 강제 탈의에 저항, 1, 윗도리, 2, 하반신 윗도리, 3, 웃도리, 4, 상반신 전부, 5, 하반신 전부,
;  6, 액세서리, 7, 액세서리 이외 모두, 8, 전라까지, 9, 속옷만)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제 탈의에 저항된 FIRSTDATUI(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 < 0
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(0)
		PRINTFORMW 「옷을 벗어 받는 것만으로 여기까지 저항된다고는 생각하지 않았습니다」
		PRINTFORMW 「전도다난 그렇습니다…… 그런데 어떻게 한 것인가」
		RETURN 1
	ENDIF
	
	;=================================================
	;자신은 벗었다
	;=================================================
	IF 구상기입체크(100) && TFLAG:자주탈의
		PRINTFORMW 「이제(벌써), 나라도 벗고 있으니까」
		PRINTFORMW 「\@ TALENT:MASTER:남자 ? 여성에게# \@그다지 창피를 주지 말아 주세욧」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「이제 와서 부끄러워하는 것도 아닐 것입니다」
			PRINTFORMW 「나도, 언제라도 상냥하게 할 수 있다고는 할 수 없어요?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「싫습니까…… 어쩔 수 없네요」
			PRINTFORMW 「죄송합니다만, 조금, 자신의 입장을 이해해 받습니다」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;두 사람 모두 뭔가 벗은 FIRSTDATUI(10)
;------------------------------------------------
;·주로 섹스계 행동의 예비 동작으로 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;내용적으로 전라 상정 같기 때문에 조건을 짜 버립니다@/L
IF 구상기입체크(100) && TFLAG:강제탈의 > 0 && TFLAG:자주탈의 && ZENRACHECK(0) && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(10)
		PRINTFORMW 「이렇게 해 두 사람 모두 알몸이 되는 것은 처음이군요」
		PRINTFORMW 「서로, 몇번이나 상대의 알몸은 보고 있을 것인데」
		PRINTFORMW 「함께 알몸이 된다고, 왠지 부끄럽습니다」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가[정욕]상태
	;=================================================
	IF 구상기입체크(100) && IS_CONDITION(TARGET, "정욕")
		PRINTFORMW 「와우…… 그렇지 않아도 기분이 고조되고 있던 것입니다만」
		PRINTFORMW 「둘이서 알몸이 되어 버리면, 그」
		PRINTFORMW 「이제(벌써), 억제하지 못할입니다」
		PRINTFORMDW 화는 자신의 몸을 숨길려고도 하지 않고, 사냥감을 노리는 표정으로 눈앞의 인간을 바라본다
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그럼, 어느쪽이나 알몸이 된 것이고」
		PRINTFORMW 「옷이 더러워질 걱정은 없으니까, 사양말고 가요~」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주 탈의·액세서리만 FIRSTDATUI(20)
;------------------------------------------------
;·주로 양말. 그 밖에 장갑이나 소매 따위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 == 6 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;전라가 되었다(벗기 전은 양말 이외 알몸이라든지 그러한 느낌)
	;=================================================
	IF 구상기입체크(100) && TFLAG:자주탈의 && ZENRACHECK(0)
		PRINTFORMDW 화는 마지막에 남아 있던 버선을 벗었다……
		PRINTFORMW 「이것으로, 정말로 전부 벗었습니다……」
		PRINTFORMW 「나의 전부,%CNAME(MASTER, TARGET)%로 보여져 버리고 있습니다……」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「버선은 남겨 두는 것이 좋습니까?」
		PRINTFORMW 「어느 쪽으로 해도, 지금 벗어 버립니다만 말이죠」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주 탈의·만p FIRSTDATUI(30)
;------------------------------------------------
;·이번 탈의로 성기가 보이게 되었을 경우에 표시
;·팬티 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:주인님 && FIRSTDATUI(30)
		PRINTFORMW 「그, 그럼, 갑니다」
		PRINTFORMDW 화는 사타구니를 숨기는 곤의 매듭에 손을 대어, 풀어 간다
		PRINTFORMDW 긴장하고 있는지 손은 떨려, 어색한 손놀림이 되어 있다
		PRINTFORMDW 역시 남자의 앞에서 속옷을 벗는 것은 저항감이 있는지 물으면,
		PRINTFORMW 「아, 당연합니닷. 남자에게 보이게 한다니 처음이니까」
		PRINTFORMDW …… 지금, 터무니 없는 발언을 (들)물은 것 같은 생각이 든다
		PRINTFORMW 「아, 심합니닷. 『사람을 조교 같은거 할 정도로이니까, 이제(벌써) 몇번이나 남자에게 신체를 열어 짓무른 생활을 보내왔을 것이다』든지 절대 지금 생각했어요!」
		PRINTFORMDW 아니, 거기까지는 생각하지 않지만…… 의외였던 것은 사실이다.
		PRINTFORMDW 남에 가랑이를 보이는 것이 처음이라고 하는 일은, 당연……
		PRINTFORMW 「그렇습니닷!   처녀입니닷!   …… 어차피 나는 처녀인데 이런 추잡한 일을 하고 있는 변태흰색이리 텐구이랍니다」
		PRINTFORMDW 는 마음대로 자포자기가 되어 낙담하는 화. 그러면, 따로 무리해 벗지 않아도……
		PRINTFORMW 「아니오 벗습니다!   나에게도 흰색이리 텐구의 고집이 있습니다」
		PRINTFORMDW 고집과 벗는 것이 결합되지 않는 생각도 들지만……
		PRINTFORMDW 화는 단번에 곤의 매듭을 풀었다
		PRINTFORMDW 한다, 라고 길고 흰 옷감이 마루에 떨어져 화의 남자를 모르는 하반신이 완전하게 노출되었다
		PRINTFORMDW 화는 손으로 숨기려고 하고 싶어하는 것을 필사적으로 억제해, 과시하도록(듯이) 허리를 내밀고 있다
		PRINTFORMW 「어, 어떻습니까?   조금은 흥분해 받을 수 있던 것입니까?」
		PRINTFORMDW 그 초심인 행동이라고 희미하게솜털이 나 있을 뿐의 균열이 기묘한 추잡함을 조성하기 시작하고 있었다……
		RETURN 1
	ENDIF
	
	;=================================================
	;응 이렇게 p
	;=================================================
	IF 구상기입체크(100) && PENIS(TARGET)
		PRINTFORMDW 화는 사타구니에 나 있는 페니스를 드러냈다……
		PRINTFORMW 「원래 없었기 때문에, 보여져 부끄러운 것 같은 낯간지러운 것 같은, 이상한 느낌입니다」
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써 가슴이 보이고 있다
	;=================================================
	IF 구상기입체크(100) && B_DATUICHECK(0) == 1
		PRINTFORMW 「가슴의 다음은, 당연 아래…… 군요」
		PRINTFORMDW 화는 하반신을 가리는 천을 모두 벗어 던진다
		PRINTFORMDW 조심스러운 가슴과 미성숙인 하반신, 화의 모두가 공공연하게 되었다……
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「어, 어떻습니까…… 인가. 여, 여자 아이의 저기…… 하」
		PRINTFORMDW 화는 자신의 제일 중요한 곳을 볼 수 있는 수치에 신체를 진동시키면서, 비밀스런 장소를 손가락으로 벌렸다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주 탈의·말하는 p FIRSTDATUI(40)
;------------------------------------------------
;·이번 탈의로 젖가슴이 보이게 되었을 경우에 표시
;·브라 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:주인님 && !TALENT:거유 && FIRSTDATUI(40)
		PRINTFORMDW 아연하게로 한다%CALLNAME:MASTER%의 앞에서, 화는 가슴의 사라시를 풀어 갔다
		SIF ALLSAMES(!TALENT:거유, !TALENT:빈유)
			PRINTFORMDW 크지도 않지만 작은 것도 아닌, 손에 막 들어가는 정도의 형태가 좋은 가슴이 공공연하게 되었다
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써 성기가 보이고 있다
	;=================================================
	IF 구상기입체크(100) && P_DATUICHECK(0) == 1
		PRINTFORMW 「뭔가 순번이 틀리다는 느낌도 듭니다만…… 이것으로 전부 보여 버리고 있군요……」
		PRINTFORMDW 하에 이어 위도 벗은 화는, 지금은 소중한 곳이 모두 뻔히 보임이 되어 있다
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「어떻습니까?   나의 젖가슴」
		PRINTFORMW 「별로\@ !TALENT:거유 ? 크기에는# \@자신 없지만…… 봐 손해는 하지 않는다고 생각하지 않습니까?」
		PRINTFORMW 「…… 아니요이니까는 그렇게 빤히라고 볼 수 있어도 조금 곤란하다고 할까」
		PRINTFORMW 「그, 나도, 정말로는 조금 부끄럽다고 할까」		 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주 탈의·팬티 보이고 FIRSTDATUI(50)
;------------------------------------------------
;·이번 탈의로 팬티(캐릭터에 의해 드로워즈·트렁크스)가 보이게 되었을 경우에 표시
;·하반신 속옷이 없는 캐릭터는 쓰는 필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;첫회와 그 외의 내용적으로 바꾸어 넣고가 타당하다라고 판단했습니다@/L
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(50) && FIRSTDATUI(30, 1)
		PRINTFORMW 「어째서 곤인 것인가, 입니까?」
		PRINTFORMW 「확실히 산기슭의 인간이나 요괴의 여러분은 드로워즈가 많은 것 같지만……」
		PRINTFORMW 「아, 문양도였지요」
		PRINTFORMW 「그렇지만, 하카마를 입으려면 드로워즈는 조금 방해이므로, 우리는 모두곤입니다」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMDW 화는 천천히, 입고 있는 하카마를 내려 갔다
		PRINTFORMDW 흰 곤을 몸에 익힌 화의 하반신이 공공연하게 되었다……
		PRINTFORMDW 화의 사타구니에 흰 옷감이 먹혀들고 있는 눈앞의 광경에, 무심코 군침이 흘러넘쳤다……
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주 탈의·브라 보이고 FIRSTDATUI(60)
;------------------------------------------------
;·이번 탈의로 브라(캐릭터에 의해 스포트브라·사라시)가 보이게 되었을 경우에 표시
;·상반신 속옷이 없는 캐릭터는 쓰는 필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(60) && FIRSTDATUI(40, 1)
		PRINTFORMDW 화는 윗도리를 싶게 완성하면, 단번에 벗어 던졌다
		PRINTFORMDW 그 아래로부터는, 사라시를 감은 가슴이\@ !TALENT:거유 ? 다소곳하게# \@얼굴을 들여다 보게 했다
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그, 색기가 없어서 미안합니다」
		PRINTFORMDW %CALLNAME:MASTER%는 사라시가 잘 어울리고 있다, 라고 정직하게 생각한 것을 대답했다
		PRINTFORMW 「정말입니까?   겉치레말에서도 기쁩니다」
		PRINTFORMDW 그런데도 화는, 약간 누그러진 표정으로 귀를 삑삑 흔들었다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주 탈의·전라에 FIRSTDATUI(70)
;------------------------------------------------
;·「전라까지 벗었다」만이 아니고, 「지금 벗었으므로 전라가 되었다」라고 하는 의미
;·「윗도리 밖에 남지 않은 상태로 윗도리를 벗었다」등도 포함된다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:자주 탈의 && ZENRACHECK(0) && ! IS_NOWASSIACTNAME(, "탈의")
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제 탈의·액세서리만 FIRSTDATUI(80)
;------------------------------------------------
;·주로 양말. 그 밖에 장갑이나 소매 따위
;·벗긴 중에 접어들지 않는다! 되기 십상인 것으로 이것만은 부위별 텐프레를 준비
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 == 6
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(80)
		PRINTFORMW 「그럼, 양말도 벗어요」
		PRINTFORMDW 화는%CALLNAME:MASTER%의 양말을 억지로 벗겨내면, 대단히 멀어진 장소에 두었다
		PRINTFORMDW …… 그렇게 냄새날까.
		PRINTFORMDW %CALLNAME:MASTER%가 물으면, 화는 당황해 목을 옆에 흔들었다
		PRINTFORMW 「그, 그렇지 않습니다. 단지 나의 너무 냄새를 잘 맡기 때문에, 약간의 냄새에서도 신경이 쓰일 뿐입니다」
		PRINTFORMDW 역시 개 같다…… 라고 생각했지만, 입에는 내지 않고 두었다
		RETURN 1
	ENDIF
	
	;=================================================
	;전라가 되었다
	;=================================================
	IF 구상기입체크(100) && ZENRACHECK(1)
		PRINTFORMW 「양말 정도 좋을 것이다, 는?」
		PRINTFORMW 「안됩니다. 전부 벗게 하는 일에 의미가 있습니다」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그럼, 양말은 저쪽에 놓아둡니다」
		PRINTFORMDW 화는 대단히 멀어진 장소에, 벗긴 양말을 정중하게 두었다
		PRINTFORMDW …… 역시 냄새가 신경이 쓰일까
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제 탈의·고추 보여라 FIRSTDATUI(90)
;------------------------------------------------
;·이번 탈의로 남성기가 보이게 되었을 경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && PENIS(MASTER)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(90)
		PRINTFORMW 「와훗!?」
		PRINTFORMDW %CALLNAME:MASTER%의 하반신이 공공연하게 되면, 화는 손으로 자신의 눈을 숨겨 뒤로 물러났다
		PRINTFORMDW 그렇다 치더라도, 지금 것은 조교하는 측이라고는 생각되지 않는 청순한 반응이었지만, 설마……
		PRINTFORMW 「그, 그렇습니다!   남성의 것을 보는 것은 처음입니닷!」
		PRINTFORMW 「왜, 왜냐하면, 이런 커서 그로테스크하다고 생각하지 않았어요……」
		PRINTFORMDW …… 이제 와서이지만, 텐구는 큰 인선 미스를 한 것은 아닐까?
		RETURN 1
	ENDIF
	
	;=================================================
	;상냥하게 벗긴다(호감도 1000이상·아라이먼트정)
	;=================================================
	IF 구상기입체크(100) && AFFECTION("권애") && ALI(0)
		PRINTFORMW 「우와─우와─, 오늘도 또 훌륭한 것이군요……」
		PRINTFORMW 「익숙해져 오면, 뭔가 이것도 사랑스럽게 보여 오기 때문에 이상합니다」
		;후타나리로 표시하면 위화감 있으므로 남자 한정에
		IF TALENT:MASTER:남자
			PRINTFORMDW 아마 화에 타의는 없을 것이지만, 남자로서는
			PRINTFORMDW 「저기가 사랑스럽다」발언은 꽤 다치는 것을 이해 해 주었으면 했다……
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「는─…… 좋아, 마음의 준비는 할 수 있었습니다. 막상」
		PRINTFORMDW 화의 눈앞에 내밀어졌다%CALLNAME:MASTER%를, 화는 진지한 눈으로 응시하고 있다……
		PRINTFORMDW 무사의 결투가 아닌 것이니까, 거기까지 기합을 넣어 마주보지 않아도……
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제 탈의·그대로응 보여라 FIRSTDATUI(100)
;------------------------------------------------
;·이번 탈의로 여성기가 보이게 되었을 경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:강제 탈의 && P_DATUICHECK(1) == 3 && ! TALENT:MASTER:남자
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제 탈의·젖가슴 보여라 FIRSTDATUI(110)
;------------------------------------------------
;·이번 탈의로 젖가슴이 보이게 되었을 경우에 표시
;·브라 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;·조교 대상이 남자라고 표시하지 않기 때문에, 남자 한정 구상에서는 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:강제 탈의 && B_DATUICHECK(1) == 3 && ! TALENT:MASTER:남자
ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제 탈의·팬티 보여라 FIRSTDATUI(120)
;------------------------------------------------
;·이번 탈의로 하반신 속옷이 보이게 되었을 경우에 표시
;·조교 대상이 하반신 속옷이 없는 캐릭터 한정의 구상이면 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 2
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(120) && FIRSTDATUI(90, 1)
		;조교 대상이 남자
		IF TALENT:MASTER:남자
			PRINTFORMW 「과연…… 남성은 이러한 속옷을 입고 있군요」
			PRINTFORMW 「곤과는 전혀 다릅니다. 스으스으 하지 않습니까?」
		;조교 대상이 후타나리
		;ELSEIF TALENT:MASTER:후타나리
		;조교 대상이 여자
		ELSE
			PRINTFORMW 「좋구나…… 멋진 속옷입니다」
			PRINTFORMW 「나는…… 나는……」
		ENDIF
		RETURN 1
	ENDIF
	
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;조교 대상이 남자
		IF TALENT:MASTER:남자
			PRINTFORMW 「저…… 실례이지만, 속옷, 분명하게 씻고 있습니다?」
			PRINTFORMDW 희미하게 눈썹을 찡그리면서 화에 그런 말을 들었다.
			PRINTFORMDW 그렇게 냄새날까, 라고 쇼크를 받았지만,
			PRINTFORMDW 그렇게 말하면 화는 흰색이리 텐구, 즉 이리였던 일을 생각해 냈다.
			PRINTFORMDW 아마 후각은 인간의 몇십배도 있기 때문에, 속옷의 냄새에도 민감한 것일 것이다
		;조교 대상이 후타나리
		;ELSEIF TALENT:MASTER:후타나리
		;조교 대상이 여자
		ELSE
			PRINTFORMW 「그렇게 말하면, 만약 생리의 시기가 되면 말해 주세요」
			PRINTFORMW 「조교의 벌이라고는 해도, 최저한의 존엄과 환경은 이쪽에서도 확보할 생각이므로」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제 탈의·브라 보여라 FIRSTDATUI(130)
;------------------------------------------------
;·이번 탈의로 브라 상반신 속옷이 보이게 되었을 경우에 표시
;·조교 대상이 남자라고 표시하지 않기 때문에, 남자 한정 구상에서는 쓸 필요가 없다
;·조교 대상이 상반신 속옷이 없는 캐릭터 한정의 구상이면 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && B_DATUICHECK(1) == 2 && !TALENT:MASTER:남자
	;=================================================
	;첫회
	;=================================================
	;IF 구상 기입 체크(0) && TALENT:TARGET:주인님 && FIRSTDATUI(13)
	;ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「멋진 속옷이군요」
		PRINTFORMW 「나는 기능성 밖에 중시하고 있지 않기 때문에……」
		PRINTFORMW 「나도 가끔 씩은 당신 같은 사랑스러운 속옷을 입어 보고 싶은 것입니다」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제 탈의·전라에 FIRSTDATUI(140)
;------------------------------------------------
;·「전라까지 벗겼다」만이 아니고, 「지금 벗겼으므로 전라가 되었다」라고 하는 의미
;·「윗도리 밖에 남지 않은 상태로 윗도리를 벗겼다」등도 포함된다
;·「젖가슴 보여라」~ 「팬티 보여라」를 썼을 경우는 기입할 필요는 없다(상층의 분기에 먹혀져 여기까지 떨어져 오지 않기 때문에)
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:강제 탈의 && ZENRACHECK(1)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주 탈의·뭔가 벗은 FIRSTDATUI(150)
;------------------------------------------------
;·자위·봉사계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(150)
		PRINTFORMDW 화는 입고 있는 옷을 스스로 벗기 시작했다
		PRINTFORMW 「저, 그다지 차분히 보지 말아 주세요?」
		PRINTFORMDW 그렇게는 말해져도, 타인의 앞에서 옷을 탈 일에 익숙하지 않은 것인지,
		PRINTFORMDW 때때로 어물어물 벗는데 고전하고 있는 화의 모습은 위태로울지 사랑스러울지로
		PRINTFORMDW %CALLNAME:MASTER%는 화의 탈의로부터 한 눈을 팔 수 없었다……
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMDW 화는 벗은 것을 정중하게 접어, 옆에 두었다
		PRINTFORMW 「그런데, 벗은 곳에서 다음은 무엇을 한다고 생각합니까?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제 탈의·뭔가 벗긴 FIRSTDATUI(160)
;------------------------------------------------
;·애무계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(160)
		PRINTFORMDW 화는%CALLNAME:MASTER% 위에 덮어씌우면, 옷에 손을 대었다
		PRINTFORMDW 화의 완력은 가냘픈 팔의 소녀라고는 도저히 생각되지 않을 정도 강하고,%CALLNAME:MASTER%의 저항은 시원스럽게 억누를 수 있었다
		PRINTFORMW 「너무 저항하면 옷이 찢어져 버려요?」
		PRINTFORMDW 조교가 끝난 뒤로 입는 것조차없이는 과연 견딜 수 없다
		PRINTFORMDW %CALLNAME:MASTER%는 어쩔 수 없이, 저항의 손을 느슨하게했다……
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMDW 화는 벗긴 것을 정중하게 접어, 옆에 두었다
		PRINTFORMW 「그런데, 벗긴 곳에서, 다음은 무엇을 된다고 생각합니까?」
		RETURN 1
	ENDIF
ENDIF

;========================================================================
;추가 액션 구상
;========================================================================
;·조교자가 추가 행동을 취했을 때에 범용적으로 표시
;·쓰는 경우, 액션 구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
;─
;EXTRA1, C애무
;─
;·추가로 애무 또는 자위를 실시한다
;·조교 대상으로 페니스가 없는 경우는 애무, 어느 경우는 자위
;-------------------------------------------------
@KOJO_ACT_EXTRA_K41_1
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_0, 애무한다
;-------------------------------------------------
;·조교 대상으로 페니스가 없는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ! PENIS(MASTER)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_1, 자위 한다
;-------------------------------------------------
;·조교 대상으로 페니스가 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && PENIS(MASTER)
ENDIF
[SKIPEND]
;─
;EXTRA2, V애무
;─
;·추가로 바지 애무를 실시한다
;·조교 대상이 남자의 경우 발생하지 않는다
;·조교 대상이 처녀의 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K41_2
;-------------------------------------------------
;IF 구상 기입 체크(0)
;ENDIF

;─
;EXTRA3, A애무
;─
;·추가로 애널 애무를 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K41_3
;-------------------------------------------------
;IF 구상 기입 체크(0)
;ENDIF

;─
;EXTRA4, B애무
;─
;·추가로 유두 고문을 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K41_4
;-------------------------------------------------
;IF 구상 기입 체크(0)
;ENDIF

;─
;미약등 사용 구상
;─
;·조교자가 미약이나 로션을 사용했을 때에 범용적으로 표시
;·「액션 구상」으로 기입 체크를=2로 했을 경우는 표시되지 않는다
;·쓰는 경우, 액션 구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
@KOJO_USE_K41
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;로션/미약동시 사용
;------------------------------------------------
;·주로 성교계 커맨드로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && GETBIT(TFLAG:미약등 사용 구상, 0) && GETBIT(TFLAG:미약등 사용 구상, 1)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션 사용
;------------------------------------------------
;·완구·성교계 커맨드등으로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GETBIT(TFLAG:미약등사용구상, 0)
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「미끈미끈, 기분이 좋습니까?」
		PRINTFORMW 「실은 나는 조금 서투릅니다」
		PRINTFORMW 「꼬리에 붙어 버리면, 좀처럼 떨어지지 않지요 이것」
	;아라이먼트부
	ELSE
		PRINTFORMW 「그럼, 발라 하네요」
		PRINTFORMW 「전신 샅샅이 미끈미끈으로 해 줍니다. 기분이 좋습니까?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;미약사용
;------------------------------------------------
;·애무계 커맨드등으로부터 발생한다
;·키스로 발생했을 경우는 입으로 옮김이 되기 (위해)때문에, 그 케이스는 키스의 액션 구상내에서 처리하는 편이 좋다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GETBIT(TFLAG:미약등사용구상, 1)
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「무엇을 먹였는지, 입니까?」
		PRINTFORMW 「기분 좋게 될 수 있는 약입니다」
		PRINTFORMW 「괜찮습니다. 인간이 마셔도 해는 없기 때문에」
		PRINTFORMW 「조금이라도 기분 좋게 될 수 있는 편이 좋을까 생각해서」
	;아라이먼트부
	ELSE
		PRINTFORMW 「무엇을 먹였는지, 입니까?」
		PRINTFORMW 「후후, 사실은 알고 있어 (듣)묻고 있지 않습니까?」
		PRINTFORMDW 화는 열을 띤 목덜미에 혀를 기게 해 왔다……
	ENDIF
	RETURN 1
ENDIF