;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	모미지 구상 회화계 개별 리액션 구상 파일
;
;	회화계 리액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;회화계 REACT0～9
;========================================================================
;·1231판보다 TFLAG:REACT부하가 도입되어 선택한 COM가 자연스러운 행동인가 어떤가를 참조할 수 있도록(듯이)
;·0127판 이후의 리액션 후의 지문은, 기본적으로 REACT부하로 분기 하게 되어 있습니다
;·TFLAG:REACT부하(0=자연스러운 행동/1=무리가 없는 행동/2=무리를 한 행동)
;-------------------------------------------------
;  회화계는 그다지 범용 처리에는 향하지 않기 때문에, 범용 리액션(COM)에 흘리지 않고 개별 리액션(REACT)
;  로 완결시켜 버리는 것이 바람직하다고 생각합니다
;-------------------------------------------------

;─
;REACT0, 지금의기분을얘기한다·개별 리액션
;─
;·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_0에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K41_0
;-------------------------------------------------
;·TFLAG:ACT파생(0=말씨 나쁘게 대답한/1=용기를 내 강하게 맞은/2=야유/3=분명히 지금의 기분을 고했다
;·4=그저 나쁘지는 않다/5=기분 좋은 일 하고 싶다)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 0 말씨 나쁘게 질문에 답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따른다 것으로, 리액션이빈정거린다 이외의 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「역시 말하기 어렵습니까?」
		PRINTFORMW 「그렇네요…… 솔직하게 대답하고 싶고 같은거 없지요……」
	;아라이먼트부
	ELSE
		PRINTFORMW 「역시 말하기 어렵습니까?」
		PRINTFORMW 「뭐, 뭐든지 생각한 채로 말하는 것이 언제나 올바르다고는 말할 수 없기 때문에」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 1 용기를 내 강하게 맞았다
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게대답한다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「미, 미안합니다」
		PRINTFORMW 「기분이 나쁜 것은 알고 있습니다」
		PRINTFORMW 「그렇지만…… 그런데도, 그만둘수는 없으므로」
	;아라이먼트부
	ELSE
		PRINTFORMW 「과연, 의사가 확실히 하고 있는 (분)편이라고 하는 일은 알았습니다」
		PRINTFORMW 「그렇지만 때와 경우를 생각하는 것이 좋다, 라고 충고합니다」
		PRINTFORMW 「용기와 만용은 다른, 라는 것을 알아차리고 나서는 늦습니다」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 2 야유 「지금의 기분은, 이라고 (들)묻어도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「응…… 그렇네요. 역시 이런 일 (들)물어도 곤란한군요」
		PRINTFORMDW 야유가 통하지 않았던 것일까, 모미지는 아주 진지하게 반응했다
	;아라이먼트부
	ELSE
		PRINTFORMW 「곤란합니까?   어쩔 수 없네요, 다음까지는 생각해 두어 주세요」
		PRINTFORMDW 야유가 통하지 않았던 것일까, 모미지는 아주 진지하게 반응했다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 3분명히 지금의 기분을 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(30)
		;호의 2000이상
		IF AFFECTION("조교 완료")
			PRINTFORMW 「정말입니까?   기쁩니다」
			PRINTFORMW 「솔직한 사람의 (분)편이, 나도 좋아합니다」
		;그 외
		ELSE
			PRINTFORMW 「정말입니까?   기분은 나쁘지 않습니다」
			PRINTFORMW 「지금은 아직,%CNAME(MASTER, TARGET)%의 기분에는 응할 수 없지만……」
		ENDIF
		RETURN 1
	ENDIF
	
	;CFLAG:M호의 >= 1000혹은 FAVOR(1000)가 파생의 조건의 1개여서
	;오리지날의 FAVOR(500)는 확실히 죽어 분기가 된다. 라는 것으로 만지작거립니다@/L
	;=================================================
	;호의 호의 2000이상
	;=================================================
	IF 구상기입체크(100) && AFFECTION("조교 완료")
		;-------------------------------------------------
		;성교계/성교 봉사계로 질내사정의 뒤
		;-------------------------------------------------
		IF GROUPMATCH(TFLAG:전회질내사정, 1, 2) > 0
			;성교 봉사계로 질내사정의 뒤
			IF TFLAG:전회질내사정 == 2
				PRINTFORMW 「이제(벌써)…… 안에 내고 나서 그런 일 말한다니 반칙입니다」
				PRINTFORMW 「안에 낸 것, 화내고자 해도 화낼 수 없지 않습니까」
			;성교계로 질내사정의 뒤
			ELSE
				PRINTFORMW 「안에 내 버렸는데, 좋았던 것입니까?」
				PRINTFORMW 「아기 생겨 버릴지도 몰라요?」
				PRINTFORMW 「정말로, 정말로 좋습니까?」
			ENDIF
			RETURN 1
		ENDIF
		
		;---------------------
		;그 외
		;---------------------
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「그, 그렇게 분명히 말해지면」
			PRINTFORMW 「매우 부끄럽습니다. 낯간지럽습니다」
			PRINTFORMW 「그렇지만, 전혀 싫지 않습니다」
		;아라이먼트부
		ELSE
			PRINTFORMW 「그, 그런 일 말해지면」
			PRINTFORMW 「%CNAME(MASTER, TARGET)%의 일, 좀 더 괴롭히고 싶어져 버립니다」
			PRINTFORMW 「그리고 자신에게 그런 일면이 있었던 것이 놀라움입니다」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「솔직하게 대답해 주어 감사합니다」
			PRINTFORMW 「나, 너무 말의 뒤의 의미를 알아채는 것이 자신있지 않기 때문에」
			PRINTFORMW 「솔직하게 말해 받을 수 있는 편이 기쁩니다」
		;아라이먼트부
		ELSE
			PRINTFORMW 「솔직하게 대답해 주어 감사합니다」
			PRINTFORMW 「나, 너무 말의 뒤의 의미를 알아채는 것이 자신있지 않기 때문에」
			PRINTFORMW 「솔직하게 말해 받을 수 있는 편이 기쁩니다」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 4그저 나쁘지는 않다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 태연하게 따르는 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 4
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생, 5 기분 좋은 일 하고 싶다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들인다 것 경우
;-------------------------------------------------
IF 구상 기입 체크(0) && TFLAG:ACT파생 == 5
ENDIF
[SKIPEND]
;REACT0, 지금의기분을얘기한다

;─
;REACT1, 자신에게로의기분을얘기한다·개별 리액션
;─
;·ACT파생에의 대입은 조교 대상의 리액션에 응해@ACTION_APPLY2_1에서 행해진다
;-------------------------------------------------
@KOJO_REACT_K41_1
;-------------------------------------------------
;·TFLAG:ACT파생(0=싫지 않은… 일지도 모른다…/1=그런 일이 있을 이유 없다! /2=곤란하네요… 좋아하지 않다고 말하면 화내?
;·3= 이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다/4=분명히 좋아라고 고했어? 5= 좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도)
;-------------------------------------------------
;;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「싫지 않은… 일지도 모른다…」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따른다 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「그렇습니까. 그것만으로 안심했습니다」
		PRINTFORMW 「누구라도, 타인으로부터 미움받고 싶지는 않으니까」
		PRINTFORMW 「물론, 나도 그렇습니다」
	;아라이먼트부
	ELSE
		PRINTFORMW 「그렇습니까. 안심했습니다」
		PRINTFORMW 「그럼, 이대로 계속해도 괜찮아라고 하는 일이군요?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「그런 일이 있을 이유 없다!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이강하게대답한다의 경우
;REACT분류는 "거부"에 세트 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「우우우…… 예상은 하고 있었습니다만, 분명히 말해지면 역시 견딥니다」
		PRINTFORMDW 모미지는 구석에 웅크려, 손가락끝으로 「노」의 글자를 마루에 그리고 있다……
	;아라이먼트부
	ELSE
		PRINTFORMW 「당연한 반응이라고 생각합니다」
		PRINTFORMW 「하지만,%CNAME(MASTER, TARGET)%도 나와 같아 세상살이는 너무 능숙한 (분)편이 아닌 것 같네요」
		PRINTFORMW 「왜냐하면, 그런 반항적인 일 말해지면, 또 심한 일을 할 수 밖에 없어요?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 야유 「곤란하네요… 좋아하지 않다고 말하면 화내?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;리액션이빈정거린다의 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「분노는 하지 않습니다만…… 조금, 곤란할지도 모릅니다」
		PRINTFORMDW 원래 고지식한 성격 고인가, 모미지에게는 그다지 야유는 통하지 않았던 것 같다
	;아라이먼트부
	ELSE
		PRINTFORMW 「괜찮습니다. 심한 일을 하고 있는 자각은 있기 때문에, 당연하다고 생각합니다」
		PRINTFORMDW 원래 고지식한 성격 고인가, 모미지에게는 그다지 야유는 통하지 않았던 것 같다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「이제(벌써) 심한 일 하지 않으면, 좀 더 좋아하게 될 수 있을지도 모른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 적은지, REACT분류가 태연하게 따른다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 3
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「나도 같아요」
		PRINTFORMW 「좀 더 나를 좋아하게 되어 준다면, 나도 이제(벌써) 심한 일을 할 필요는 없습니다」
	;아라이먼트부
	ELSE
		PRINTFORMW 「심한 것은 가능한 한 하지 않을 생각입니다만, 어느 정도까지가 심한 일, 이 되겠지요?」
		PRINTFORMW 「그것을 확인하기 위해서는, 좀 더 여러 가지 시험해 보지 않으면 안 되네요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, 분명히 좋아한다고 고했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것으로, 조교 대상 호의가 1000이상
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 4
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(40)
		PRINTFORMW 「엣?   네……?   저, 그, 좋아?   네……」
		PRINTFORMDW 예상외의 대답이었는가, 모미지는 갑자기 심심하게 시선을 둘러싸게 한 허둥지둥 하기 시작했다……
		PRINTFORMDW 역시 폐였을까?   반응이 곤란하고 있으면, 모미지는 거기에 깨달은 모습으로,
		PRINTFORMW 「아, 아니오, 다릅니닷. 다만, 이렇게 분명히 『좋아』는 정면에서 말해진 적 없어,」
		PRINTFORMW 「기쁜 것 같은…… 부끄러운 것 같은…… 낯간지러운 것 같은……」
		PRINTFORMDW 모미지는 공중에 손가락끝으로 「노」의 글자를 쓰면서, 뺨을 물들여 시선을 유영하게 하고 있다
		PRINTFORMW 「…… 그렇지만, 싫지 않습니다」
		PRINTFORMDW 겨우 들어 잡힐 정도의 말로 마지막에 그렇게 말한 뒤, 모미지는 조금의 사이 시선을 맞추지 않고 침묵하고 있었다……
		RETURN 1
	ENDIF
	
	;=================================================
	;성교 봉사계로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
		PRINTFORMW 「좋아해, 입니까. 감사합니다」
		PRINTFORMW 「그것은 안으로 내게 해 받을 수 있었기 때문에입니까?」
		PRINTFORMDW 그것뿐이지 않아, 라고%CALLNAME:MASTER%는 강한 의지를 담아 대답했다
		PRINTFORMW 「그렇습니까. 그럼…… 그……」
		PRINTFORMDW 모미지는 새빨갛게 되어,%CALLNAME:MASTER%로부터 시선을 피해 버렸다
		RETURN 1
	ENDIF
	
	;=================================================
	;섹스로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
		PRINTFORMW 「억지로 넣을 수 있어, 게다가 안에 나왔는데, 입니까?」
		PRINTFORMW 「기분 좋았다고 말하는 일인 것입니까?」
		PRINTFORMW 「응…… 나에게는 잘 모릅니다」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「에, 엣또…… 저, 감사합니다」
		PRINTFORMDW 몇번 말해져도 익숙해지지 않는 것인지, 모미지는 사춘기의 소녀와 같이 허둥지둥 머뭇머뭇 하면서 답례의 말을 말했다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「좀 더 굉장한 일 해 주면, 좀 더 좋아하게 될 수 있을지도」
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들인다 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 5
	;=================================================
	;성교 봉사계로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
		PRINTFORMW 「좀 더 굉장한 일, 입니까?」
		PRINTFORMW 「저, 나의 지식에서는 더 이상 굉장한 일이 상상 붙지 않습니다만……」
		PRINTFORMW 「서, 설마 엉덩이로 한다든가!?」
		PRINTFORMW 「그, 그렇지 않으면에, 에, 에…… 해…… 라든지입니까―!?」
		PRINTFORMDW 모미지는 다양하게 상상…… 아니 망상을 시작한 것 같다……
		RETURN 1
	ENDIF
	
	;=================================================
	;성교계로 질내사정의 뒤
	;=================================================
	IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
		PRINTFORMW 「더 이상 굉장한 일이라고 말해져도……」
		PRINTFORMW 「나의 지식에서는 이것도 충분히 굉장한 일이라고 생각합니다만」
		PRINTFORMW 「조교의 길은 안쪽이 깊군요……」
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「좀 더 굉장한 일, 입니까?」
			PRINTFORMW 「이제(벌써), 나에게 도대체 무엇을 시킬 생각입니까」
			PRINTFORMDW 모미지는 뺨을 부풀려 노려봐 오지만, 정말로 화나 있는 것은 아닌 것 같다
		;아라이먼트부
		ELSE
			PRINTFORMW 「좀 더 굉장한 일, 입니까? 　%CNAME(MASTER, TARGET)%도 좋아하네요」
			PRINTFORMW 「그런 변태씨에게는, 솔직하게 굉장한 일을 해 줄까 어떤가 결심이 무디어져 옵니다」
			PRINTFORMDW 즐거운 듯이%CALLNAME:MASTER%의 얼굴을 들여다 보는 모미지의 표정은, 일부러 강아지에 보류를 주어 곤란하게 하는 인간인 것 같았다.
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT1, 자신에게로의기분을얘기한다

;─
;REACT2, 성에대한이야기를한다·개별 리액션
;─
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K41_2
;-------------------------------------------------
;  파생할 가능성이 있는 COM는 우선 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는 방법도 있습니다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「쾅!   무시되었습니닷!?」
		PRINTFORMW 「노력해, 서투른 화제를 털었는데……」
		PRINTFORMDW 모미지는 주눅들면서 마루에 「노」의 글자를 쓰고 있다……
	;아라이먼트부
	ELSE
		PRINTFORMW 「심합니닷. 모처럼%CNAME(MASTER, TARGET)%가 흥미가 있을 것 같은 화제로 했는데」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두고라고 말했다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「나라도 부끄럽지만, 그만둘수는 없어요～」
			PRINTFORMDW 모미지의 귀는 곤란한 감정을 표현하도록(듯이) 늘어지고 내리고 있다……
		;아라이먼트부
		ELSE
			PRINTFORMW 「의외이네요. 나는 다만 이야기를 하고 있을 뿐입니다?」
			PRINTFORMW 「조금은 설파하려고 하는 기력은 없습니까?」
		ENDIF
		RETURN 1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 0
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「아니오 저, 그러한 반응을 되면, 오히려 부끄럽다고 말합니까」
			PRINTFORMW 「우우…… 오히려 가볍게 웃어 버려 주세요～」
		;아라이먼트부
		ELSE
			PRINTFORMW 「기운이 없네요─」
			PRINTFORMW 「추잡한 이야기를 반대로 털어, 순진할 것 같은 흰색이리 텐구의 소녀를 희롱해 주는, 정도의 기개는 없는 것입니까?」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게해줘
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("기분좋게해줘")
	;=================================================
	;벌포인트 가산 「이 철면피의 돼지째!」
	;=================================================
	;IF 구상 기입 체크(0) && TFLAG:REACT파생 == 1
	;ENDIF
	
	;내용적으로 파생 0으로 한정하는 것이 타당하다라고 판단했습니다@/L
	;=================================================
	;REACT파생 0, 흐음
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「뭐, 뭔가 이상한 방향으로 스윗치가 들어가 버렸습니다!?」
			PRINTFORMW 「긁어 부스럼과는 이 일입니까」
			PRINTFORMW 「그, 그렇지만, 조교로서는 이것이 올바른 본연의 자세이예요……」
			PRINTFORMW 「그, 그럼 노력해 기분 좋게 해 나갑시다!」
		;아라이먼트부
		ELSE
			PRINTFORMW 「스윗치가 들어가 버렸습니까?」
			PRINTFORMW 「짐승씨군요─. 기분 좋게 해 줄까요 어떻게 할까요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("자위하기시작한다")
	;=================================================
	;REACT파생 1, 「마음대로 자위 하지마」
	;=================================================
	;IF 구상 기입 체크(0) && TFLAG:REACT파생 == 1
	;ENDIF
	
	;=================================================
	;REACT파생 0, 「마음대로 자위 하고 있지만 좋은가」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 0
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「갑자기 무엇을 시작하고 있습니까!?」
			PRINTFORMW 「도와 주세요 선배～」
			PRINTFORMW 「이 (분)편, 나로는 어찌할 도리가 없을지도 모릅니다～」
		;아라이먼트부
		ELSE
			PRINTFORMW 「…………」
			PRINTFORMW 「아…… 미안합니다. 너무나 이해를 넘은 행동에, 머리가 일순간 생각하는 것을 방폐 하고 있었습니다」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT2, 성에대한이야기를한다

;─
;REACT3, 상냥하게위로한다·개별 리액션
;─
;·지문은@REACTION_MESSAGE로 범용 처리되는 분만
;-------------------------------------------------
@KOJO_REACT_K41_3
;-------------------------------------------------
;  파생할 가능성이 있는 COM에는 REACT파생분기를 마련하고 있습니다만
;  && TFLAG:REACT파생 < 1등으로 해서 범용 리액션으로 파생을 줍는 방법도 있습니다
;-------------------------------------------------
;  우선 회화계 COM를 모두 망라되어 있습니다만
;  10 접수대의 애무계 COM, 50 접수대 이후의 그 외 COM등도 상황에 따라서는 선택 가능하게 됩니다
;  필요에 따라서 분기를 증감해 주세요
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「아, 아무리 기분이 나쁘니까 라고, 무시하는 것 없지 않습니까～」
		PRINTFORMDW 모미지는 눈물고인 눈으로 항의하고 있다……
	;아라이먼트부
	ELSE
		PRINTFORMW 「무시입니까 그렇습니까……」
		PRINTFORMW 「위로는 필요없다, 좀 더 강하게 해도 괜찮다, 라고 받아요?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("무뚝뚝하게대답한다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「역시, 나 따위에 위로받아도 아무것도 되지 않습니까?」
		PRINTFORMW 「미안합니다…… 적어도,%CNAME(MASTER, TARGET)%에 부담을 주지 않게 주의를 표해 조교를 하기 때문에」
	;아라이먼트부
	ELSE
		PRINTFORMW 「나에게 위로받을 필요는 없는, 라는 것입니까?」
		PRINTFORMW 「그렇다면 이쪽이라고 해도 그다지 죄악감은 솟아 오르지 않으므로, 살아납니다」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성 있게 응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("붙임성좋게대답한다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「조금은 기운이 생겼습니까?」
		PRINTFORMW 「좋았던 것입니다…… 에헤헤」
	;아라이먼트부
	ELSE
		PRINTFORMW 「그 대답이 본심때문인지는 나에게는 모릅니다만」
		PRINTFORMW 「그러한 반응을 해 주면, 나도 기쁩니다」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("강하게대답한다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「미, 미안합니다」
		PRINTFORMW 「결코%CNAME(MASTER, TARGET)%를 아이 취급했을 것은 아닙니다만」
	;아라이먼트부
	ELSE
		PRINTFORMW 「대단히 강하네요」
		PRINTFORMW 「이쪽이 조금이라도 가감(상태)를 하면, 그렇게 말하는 태도로 온다는 것이라면」
		PRINTFORMW 「그것은 이쪽의 빈틈을 찌른다고는 말하지 않아요?   단순한 반항기와 같습니다」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("빈정거린다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「조교에 위로는 필요 있는지…… 입니까」
		PRINTFORMW 「우우우…… 당연합니다만…… 지당한 것입니다만～」
	;아라이먼트부
	ELSE
		PRINTFORMW 「그러한 태도로 돌려주어지면」
		PRINTFORMW 「다음부터는 상냥하게 하고 싶지 않게 되어요?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더하드하게
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("더하드하게")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「저…… 지금, 위로하고 있는 곳이었네요?   적어도 나는 그렇다고 생각하고 있던 것입니다만」
			PRINTFORMW 「거기서 하드하게 해, 뭐라고 말해지면 나로서도 많이 당황하고 있어서」
			PRINTFORMDW 모미지는 이야기의 상정외의 맥락이 없음에 혼란하고 있는 것 같다……
		;아라이먼트부
		ELSE
			PRINTFORMW 「뭐라고 말합니까…… 뭐,%CNAME(MASTER, TARGET)%가 그렇게 말한다면」
			PRINTFORMDW 모미지의 배후에, 왠지 검은 기색이 감돌고 있다……
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 0
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「조, 좋습니까!?」
			PRINTFORMDW 예상외의 반응에, 모미지는 당황하고 있는 것 같다……
		;아라이먼트부
		ELSE
			PRINTFORMW 「별난 사람이군요……%CNAME(MASTER, TARGET)%」
			PRINTFORMDW 모미지는 묘한 것을 볼 것 같은 눈으로,%CALLNAME:MASTER%를 보고 있다……
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「죄송합니다만, 그것은 할 수 없습니다」
			PRINTFORMDW 모미지의 말로부터는 유언실행의 강한 의지가 느껴졌다……
		;아라이먼트부
		ELSE
			PRINTFORMW 「안됩니다, 허락해 주지 않습니다♪」
			PRINTFORMDW 모미지는 굉장히 좋은 웃는 얼굴로 말했다……
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 흐음
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「죄송합니다……」
			PRINTFORMW 「지금의 나에게는, 사과하는 것으로, 가능한 한 부담을 주지 않게 조교할 수 밖에 없습니다」
		;아라이먼트부
		ELSE
			PRINTFORMW 「죄송합니다만」
			PRINTFORMW 「결과에는 원인이 부착의 것입니다」
			PRINTFORMW 「이번 경우는, 용서되려면 조금……」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 기분좋게해줘
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("기분좋게해줘")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;오리지날에서는 아라이먼트-의 부분이었지만, 이쪽이 보다 적당하다고 생각되므로 개변했습니다@/L
		PRINTFORMW 「많이 솔직해져 왔어요」
		PRINTFORMW 「그렇지만, 기분 좋게 할까 아프게 할까는 내가 결정합니다」
		PRINTFORMW 「어느 쪽이 될까는,%CNAME(MASTER, TARGET)%의 태도 나름이므로 잊지 말아 주세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 흐음
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		PRINTFORMW 「에, 에에에─!?」
		PRINTFORMW 「나, 나에게 기분 좋고 해 주었으면 합니까!?」
		PRINTFORMW 「아니오, 확실히 아픈 것 보다는 그쪽이 좋을 것입니다만…… 능숙하게 할 수 있을까나……」
		RETURN 1
	ENDIF
ENDIF
;REACT3, 상냥하게위로한다

;─
;REACT4, 엄격하게위협한다·개별 리액션
;─
;·TRAIN_MESSAGE2의 처리 없음. SOURCE_MESSAGE에서의 범용 처리만
;-------------------------------------------------
@KOJO_REACT_K41_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「에?   예」
		PRINTFORMW 「꽤 진심으로 위협한 생각…… 이었던 것이지만……」
		PRINTFORMW 「나, 그렇게 박력 없는 걸까나……」
		PRINTFORMDW 모미지는 벽에 「노」의 글자를 쓰면서 낙담하고 있다……
	;아라이먼트부
	ELSE
		PRINTFORMW 「소리도 나오지 않습니까?」
		PRINTFORMW 「설마, 정말로 언제까지나 위협만으로 끝난다고 생각하지 않지요……?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 여리게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("여리게대답한다")
	;REACT분류"용서를빈다"취급
	;=================================================
	;REACT파생 1, 이제(벌써) 이런 일 그만두어
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「미, 미안합니다」
		PRINTFORMW 「조금 너무 무서워하게 했습니다」
		RETURN 1
	ENDIF
	
	;REACT분류 "소극적 따른다"취급
	;=================================================
	;REACT파생 0, 심약구대답했다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 0
		PRINTFORMW 「놀랐습니까?」
		PRINTFORMW 「잊지 말아 주세요. 내가 개는 아니고 이리라고 하는 일을」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("빈정거린다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「므우, 그다지 무서워해지지 않습니다」
		PRINTFORMW 「좀 더 박력을 내는 연습을 하는 것이 좋은 걸까나……」
	;아라이먼트부
	ELSE
		PRINTFORMW 「개의 흉내가 능숙하구나, 입니다 라고?」
		PRINTFORMW 「…… 정말로 화내요?」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를빈다")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(7)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMDW 자칫하면 물어 죽여질 수도 있는, 이라고%CALLNAME:MASTER%의 본능이 경고했다
			PRINTFORMDW %CALLNAME:MASTER%는 솔직하게 고개를 숙여 사과했다
			PRINTFORMDW 눈앞의 소녀는 얼마나 강아지와 같은 성격을 하고 있어도,
			PRINTFORMDW 요괴의산의 흰색이리 텐구이다고 하는 일을 잊어서는 안 된다고 자신에게 타일렀다……
		;아라이먼트부
		ELSE
			PRINTFORMDW 자칫하면 물어 죽여질 수도 있는, 이라고%CALLNAME:MASTER%의 본능이 경고했다
			PRINTFORMDW %CALLNAME:MASTER%는 솔직하게 고개를 숙여 사과했다
			PRINTFORMDW 눈앞의 소녀는 얼마나 강아지와 같은 성격을 하고 있어도,
			PRINTFORMDW 요괴의산의 흰색이리 텐구이다고 하는 일을 잊어서는 안 된다고 자신에게 타일렀다……
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	;IF 구상 기입 체크(0) && TFLAG:REACT파생 == 1
	;ENDIF
	
	;=================================================
	;REACT파생 0, 흐음
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「알아 받을 수 있었어요」
			PRINTFORMW 「정말로, 위협에서는 끝나지 않게 되는 것이라도 있습니다.%CNAME(MASTER, TARGET)%의 모르는 요괴의 사회에서는」
		;아라이먼트부
		ELSE
			PRINTFORMW 「허락해 줄까요, 어떻게 할까요……」
			PRINTFORMW 「무슨, 농담이에요. 지금의%CNAME(MASTER, TARGET)%, 굉장히 좋은 얼굴 하고 있었어요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("비명을올린다")
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;=================================================
	;IF 구상 기입 체크(0) && TFLAG:REACT파생 == 0
	;ENDIF
	
	;내용적으로 파생 1으로 한정하는 것이 타당하다라고 판단했습니다@/L
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「화, 확실히 위협한 것은 나입니다만」
			PRINTFORMW 「그렇게 무서워해지면 그건 그걸로 쇼크입니닷」
		;아라이먼트부
		ELSE
			PRINTFORMW 「그렇게 무서웠던 것입니까?」
			PRINTFORMW 「이것은 이것대로…… 어쩐지 기분이 좋네요」
			PRINTFORMW 「전에 산에 온 인간의 무녀와 마법사는, 나의 위협은 전혀 효과가 없어서」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT4, 엄격하게위협한다

;─
;REACT5, 의상의변경을요구한다·개별 리액션
;─
;·조교 대상의 리액션에 의해 한 장 벗는, 2매 벗는, 벗지 않는으로 나누어진다
;·REACT분류가 "소극적 따른다", "받아들인다"의 경우는 한 장 벗는다
;·REACT분류가 "적극적 따른다", "태연하게 따른다"의 경우는 2매 벗는다
;·그 외의 경우는 벗지 않는다(PALAM치정/반항의 수치에 의해, 부끄러워서 벗지 않는, 싫어해 벗지 않는으로 나누어진다)
;-------------------------------------------------
@KOJO_REACT_K41_5
;-------------------------------------------------
;·TFLAG:ACT파생(0=옷을 벗도록(듯이) 명령/1=옷을 입도록(듯이) 명령)
;-------------------------------------------------

;오리지날의 SELECTCOM에 의한 분기에서는 현재의 사양에 대응 할 수 없기 때문에
;독단과 편견으로 재편성 해 버립니다@/L

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 옷을 벗도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 0
	;=================================================
	;한 장 벗었다(소극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("소극적 따른다/받아들인다")
		;-------------------------------------------------
		;완만한 움직임으로 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF IS_COMGRONAME("받아들인다")
			;아라이먼트정
			IF ALI(0)
				PRINTFORMW 「저, 저. 너무 보통으로 벗어지면, 나로서도 부끄럽다고 할까」
				PRINTFORMDW 모미지는 당황해 눈을 피했다.
				PRINTFORMDW …… 어떻게 하라고.
			;아라이먼트부
			ELSE
				PRINTFORMW 「벗었어요」
				PRINTFORMW 「그러면, 다음은……」
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;어쩔 수 없이 옷을 한 장 벗어 던졌다
		;-------------------------------------------------
		IF IS_COMGRONAME("소극적 따른다")
			;COM10, 부끄러워한다
			IF IS_NOWCOMNAME("부끄러워한다")
				;첫회
				IF FIRSTREACT(0)
					PRINTFORMW 「그, 글쎄요. 옷을 벗는 것만으로 그렇게 부끄러워해지면, 나로서도 어떻게 하면 좋은 것인지」
					PRINTFORMDW 모미지는 얼굴을 새빨갛게 해 시선을 아래에 향한다.
					PRINTFORMDW …… 정말로 인선 미스가 아닐까.
				;조건 없음
				ELSE
					PRINTFORMW 「부, 부끄러워해도 안됩니다!   처음이 아니니까 확실히 해 주세요!」
					PRINTFORMDW 그렇게 말하는 모미지도 부끄러운 듯이 보이지만……
				ENDIF
				RETURN 1
			ENDIF
			
			;그 외
			IF ALI(0)	;아라이먼트+
				PRINTFORMW 「일시 모면적이네요」
				PRINTFORMW 「그렇지만, 벗은 옷은 제대로 접어 주세요?」
				PRINTFORMDW …… 대단히 꼼꼼한 조교자도 있던 것이다.
			ELSE		;아라이먼트-
				PRINTFORMW 「지금의 대답의 방법은 조금 좋지 않다고 생각합니다」
				PRINTFORMW 「옷의 벗는 방법 한데 합쳐도, 상대에게 주는 인상은 크게 바뀝니다?」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;2매 벗었다(적극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("적극적 따른다/태연하게 따른다")
		;-------------------------------------------------
		;적극적으로 벗어 던졌다
		;-------------------------------------------------
		IF IS_COMGRONAME("적극적 따른다")
			;아라이먼트정
			IF ALI(0)
				PRINTFORMW 「대단히 적극적으로 되었어요」
				PRINTFORMW 「아니오, 나로서는 좋은 경향이라고 생각합니다만」
				PRINTFORMW 「너, 너무 당당히 벗어지면 조금 부끄러울까, 라고……」
				PRINTFORMDW 모미지는 옷을 벗었다%CALLNAME:MASTER%의 몸으로부터, 눈을 떼거나 슬쩍 보거나를 반복한다……
			;아라이먼트부
			ELSE
				PRINTFORMW 「대단히 적극적으로 되었어요」
				PRINTFORMW 「옷을 벗으면 기분이 좋은 일을 해 줄 수 있으면 학습했습니까?」
				PRINTFORMW 「뭐라고 말했던 가요 이런 것, 에엣또…… 파블로프의 개, 였던가요?」
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;솜씨 좋게 벗어 던졌다
		;-------------------------------------------------
		IF IS_COMGRONAME("태연하게 따른다")
			;아라이먼트정
			IF ALI(0)
				PRINTFORMDW 좋아!   개의 벗어를 신중히 그 눈에 새기는 것이 좋다!
				PRINTFORMW 「꺄우우우우우우!?   무섭습니닷!?」
			;아라이먼트부
			ELSE
				PRINTFORMDW 좋아!   개의 벗어를 신중히 그 눈에 새기는 것이 좋다!
				PRINTFORMW 「변태씨가 있습니닷!?」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;벗지 않았다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("거부")
		;-------------------------------------------------
		;부끄러워서 벗겨지지 않았다
		;-------------------------------------------------
		IF PALAM:MASTER:치정 > PALAM:MASTER:반항
			PRINTFORMW 「…… 무─」
			PRINTFORMDW 모미지는 불만스러운 듯한 표정으로, 어떻게 하면 부탁을 들어 받을 수 있을까를 생각하고 있는 것 같다……
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;싫어해 벗지 않았다
		;-------------------------------------------------
		IF PALAM:MASTER:반항 > PALAM:MASTER:치정
			;COM11, 싫어한다
			IF IS_NOWCOMNAME("싫어한다")
				;아라이먼트정
				IF ALI(0)
					PRINTFORMW 「그, 벗겨지고와 말 깨지고 누구라도 싫다고는 생각합니다만」
					PRINTFORMW 「거기를 어떻게든 부탁할 수 없습니까?」
				;아라이먼트부
				ELSE
					PRINTFORMW 「벗겨지고라고 말해지면 누구라도 싫다고는 생각합니다만」
					PRINTFORMW 「절대로 『명령』되고 있다고 하는 일을 잊지 말아 주세요?」
				ENDIF
				RETURN 1
			ENDIF
			
			;그 외
			PRINTFORMW 「그렇습니까……」
			PRINTFORMDW 말이야말로 정중하지만, 모미지의 눈에는 분명하게 불만의 표정을 간파할 수 있었다……
			RETURN 1
		ENDIF
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 옷을 입도록(듯이) 명령
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;한 장 입었다(소극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("소극적 따른다/받아들인다")
		;이 개소에서 범용적으로 사용할 수 있는 내용은 아니기 때문에 COM로 결정하고 쳐 합니다@/L
		;-------------------------------------------------
		;COM10, 부끄러워한다
		;-------------------------------------------------
		IF IS_NOWCOMNAME("부끄러워한다")
			;첫회
			IF FIRSTTIME(10)
				PRINTFORMW 「나에게는 잘 모릅니다만」
				PRINTFORMW 「어째서 옷을 입을 때에부끄러워한다의 것이지요?」
			;조건 없음
			ELSE
				PRINTFORMW 「어째서 옷을 입을 때에부끄러워한다의 것이지요?」
				PRINTFORMW 「설마, 알몸으로 있는 (분)편이 좋아했다든지입니까?」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;2매 입었다(적극적 반응)
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("적극적 따른다/태연하게 따른다")
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「그렇게 알몸으로 있는 것이 싫었습니까?」
			PRINTFORMW 「아니오, 무리도 없지만……」
		;아라이먼트부
		ELSE
			PRINTFORMW 「그렇게 알몸으로 있는 것이 싫었습니까?」
			PRINTFORMW 「지금부터라도 명령 취소할까……」
		ENDIF
		RETURN 1
	ENDIF
	
	;지문에서는 "싫어해"라고 하는 취급
	;=================================================
	;입지 않았다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("거부")
		;-------------------------------------------------
		;COM11, 싫어한다
		;-------------------------------------------------
		IF IS_NOWCOMNAME("싫어한다")
			;아라이먼트정
			IF ALI(0)
				PRINTFORMW 「에, 에엣?」
				PRINTFORMDW 모미지는,%CALLNAME:MASTER%가 옷을 입는 것을 싫어하고 있는 것을 이해 할 수 없는 것 같다……
			;아라이먼트부
			ELSE
				PRINTFORMW 「싫으면 상관하지 않습니다」
				PRINTFORMW 「동물과 같이, 그대로 알몸으로 있어 주어도 좋아요?」
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「지, 진심으로 이해 할 수 없습니다만……」
			PRINTFORMW 「저, 설마 조교의 너무 되어 야생에 돌아가 버린,」
			PRINTFORMW 「등이라고 말하는 일은 없지요……?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「그렇게 옷을 입고 싶지 않습니까?」
			PRINTFORMW 「그럼, 거기에 정리해 있다%CNAME(MASTER, TARGET)%의 옷은 태워 버려도 괜찮네요?」
			PRINTFORMDW %CALLNAME:MASTER%는 당황해, 그것만은 그만두어 주고 라고 고개를 숙였다……
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT5, 의상의변경을요구한다

;─
;REACT6, 합의를요구한다·개별 리액션
;─
;·조교 대상의 리액션에 응해 REACT분류 마다 파생한다
;-------------------------------------------------
@KOJO_REACT_K41_6
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 작은 소리로 승낙한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 소극적 따른다 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && IS_COMGRONAME("소극적 따른다")
	PRINTFORMW 「역시, 조금 무섭습니까?」
	PRINTFORMW 「…… 그렇지만, 그런데도 나를 받아들여 줄래?」
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「미안한 것 같은, 기쁜 것 같은, 복잡한 기분입니다」
		PRINTFORMW 「내가 해 줄 수 있는 것은, 조금이라도 상냥하게 해 주는 것 뿐입니다만」
		PRINTFORMW 「%CNAME(MASTER, TARGET)%를, 잘 먹겠습니다」
	;아라이먼트부
	ELSE
		PRINTFORMW 「그런 말을 들으면, 무심코 힘이 들어가 버릴 것 같습니다」
		PRINTFORMW 「내가 해 줄 수 있는 것은, 조금이라도 기분 좋게 해 주는 것 뿐입니다만」
		PRINTFORMW 「도중에 역시 그만두었다고 말해져도 제지당하지 않으므로, 각오 해 주세요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 마음대로 하면 좋다고 대답했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 태연하게 따르는 것 경우
;-------------------------------------------------
;IF 구상 기입 체크(0) && IS_COMGRONAME("태연하게 따른다")
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 뜻을 정해 바쳐도 괜찮다고 고한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 적극적 따르는 것 경우
;-------------------------------------------------
IF 구상기입체크(100) && IS_COMGRONAME("적극적 따른다")
	PRINTFORMW 「정말입니까!?」
	PRINTFORMDW 모미지의 얼굴이, 봐 그것이라고 알 정도로 기쁜듯이 빛났다
	PRINTFORMW 「감사합니다!」
	PRINTFORMW 「정직, 거절되는 것이 아닐까, 내심 불안했습니다」
	IF ALI(0)
		PRINTFORMW 「가능한 한,%CNAME(MASTER, TARGET)%가 괴롭지 않게, 상냥하게 하기 때문에」
	ELSE
		PRINTFORMW 「%CNAME(MASTER, TARGET)%를 기분 좋게 될 수 있도록, 노력하네요」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 기분좋게해줘 주는 것이라면 처음을 주어도 괜찮다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 받아들인다 것 경우
;-------------------------------------------------
;IF 구상 기입 체크(0) && IS_COMGRONAME("받아들인다")
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 합의하지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT분류가 그 외의 경우(그 외의 REACT분류의 경우, @ACTION_APPLY2_6에서 "거부"에 세트 된다)
;-------------------------------------------------
IF 구상기입체크(100) && IS_COMGRONAME("거부")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTREACT(4)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「이것까지의 조교와는 구별을 분명히 하니까요」
			PRINTFORMW 「어쩔 수 없다고 말하면 어쩔 수 없습니다」
		;아라이먼트부
		ELSE
			PRINTFORMW 「역시 무섭습니다?  여기까지 된다는 것은」
			PRINTFORMW 「지금의%CNAME(MASTER, TARGET)%라면 받아들여 줄지도, 라고는 생각하고 있던 것입니다만」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「역시, 억지로 빼앗기는 것은 싫습니까?」
			PRINTFORMW 「아니오…… 신경쓰지 말아 주세요. 나쁜 것은 원래 이쪽입니다」
		;아라이먼트부
		ELSE
			PRINTFORMW 「조금은 좋은 관계를 쌓아 올려져 왔다고 생각하고 있었습니다만」
			PRINTFORMW 「좀 더 시간을 들이지 않으면 안 될지도 모르네요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;REACT6, 합의를요구한다

;─
;REACT7, 벌이라고선언한다·개별 리액션
;─
;·선언해도 벌이 발생한다고는 할 수 없다
;-------------------------------------------------
@KOJO_REACT_K41_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, 더하드하게
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("더하드하게")
	;=================================================
	;REACT파생 1, 「이 철면피의 돼지째!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「완전히 포로가 되어 버렸습니까」
		PRINTFORMW 「그런 소질이 있다고는 예상외였지만」
		PRINTFORMW 「정말로 어쩔 수 없는 사람이군요,%CNAME(MASTER, TARGET)%」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 0
		PRINTFORMW 「에에에에에─!?」
		PRINTFORMDW 모미지는 예상외의 반응에 혼란하고 있는 것 같다……
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를빈다") 
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「허락하지 않습니다」
		PRINTFORMDW 모미지는 단호히 말해 발했다
		PRINTFORMDW 그 강고한 의사는 간단하게는 뒤집을 수 없을 것이다……
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 0, 「흐음」
	;=================================================
	IF 구상기입체크(100) && !TFLAG:REACT파생
		PRINTFORMW 「허락하는, 허락하지 않는 것 문제가 아닙니다」
		PRINTFORMW 「필요하기 때문에 하는, 그것뿐입니다」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;비명을올린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("비명을올린다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「그렇게 벌이 싫습니까?」
		PRINTFORMW 「다음부터는 좀 더 상대의 입장에 서 행동해 보는 것을 추천합니다」
		PRINTFORMW 「이번?   죄송합니다만, 멈추거나는 하지 않으므로」
		;아픔에 의한 비명의 경우
		SIF !TFLAG:REACT파생
			PRINTFORMW 「노력해 참아 주세요」
	;아라이먼트부
	ELSE
		PRINTFORMW 「보기 흉해요」
		;아픔에 의한 비명의 경우
		SIF !TFLAG:REACT파생
			PRINTFORMW 「아픈 것이 싫으면, 최초부터 고집이 되어 저항 같은거 하지 않으면 좋았던 것입니다」
		PRINTFORMW 「나중에 후회해도 되찾을 수 없는 것은, 인간도 요괴도 변함없어요」
	ENDIF
	RETURN 1
ENDIF
;REACT7, 벌이라고선언한다

;─
;REACT8, 고함친다·개별 리액션
;─
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K41_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를빈다")
	PRINTFORMW 「최근, 잊고는 있지 않았습니까?」
	PRINTFORMW 「\@ ALI(0) ? 절대로 내가 이리라고 하는 일에 # 나도 사냥하는 (분)편의 입장이라고 하는 일에 \@」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("비명을올린다")
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「조금은 이해해 받을 수 있었습니까?」
		PRINTFORMW 「겉모습으로 상대를 경시하자, 곧바로 먹을 수 있는 세계도 있습니다」
	;아라이먼트부
	ELSE
		PRINTFORMW 「이 정도로비명을올린다의 것입니까?」
		PRINTFORMW 「산의 텐구에는, 한사람이라도 그런 텐구는 없었습니다?」
	ENDIF
	RETURN 1
ENDIF
;REACT8, 고함친다

;─
;REACT9, 기분나쁘게웃는다·개별 리액션
;─
;·TRAIN_MESSAGE2의 처리 없음
;-------------------------------------------------
@KOJO_REACT_K41_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("침묵한다")
	PRINTFORMW 「\@ ALI(0) ? 가─응!?   무시되었습니닷!?  # 무시하다니 심해요～ \@」
	PRINTFORMDW 도, 저것으로 어떻게 반응해도 좋은 것이든지……
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("용서를빈다")
	;파생마다 분리해 버리는 것이 타당하다라고 판단했습니다@/L
	;=================================================
	;REACT파생 1, 「의견을 내다니 좋은 담력이구나!」
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「차분히 들려주어 받습니다. 왜 나는 웃는 얼굴을 향했는데 허가를 청해지고 있는 것일까요?」
		PRINTFORMDW 얼굴은 힘이 빠지고 있다. 하지만, 소리는 웃지 않았었다.
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0 파생 「흐음」취급
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「웃는 얼굴을 주었는데, 왠지 허가를 청해지고 있습니닷!?」
		PRINTFORMDW 모미지는 쇼크를 받은 모습으로 그 자리에 쭈그려 앉기를 시작했다……
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;TEQUIP:(관장＋애널 플러그/삼각목마) 장착 중등의 경우는 아픔의 SOURCE가 들어가므로
	;가능성으로서는 생각됩니다만, 이 개소에서 수고를 거는 필요성으로서는 미묘한 점
	;=================================================
	;REACT파생 0, 아픔에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 공포에 의한 비명
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「나, 나의 웃어, 그렇게 무서웠던 것입니까!?」
			PRINTFORMDW 낙담할 정도라면 최초부터 하지 않으면……
		;아라이먼트부
		ELSE
			PRINTFORMW 「사람이 웃는 얼굴로 장소를 누그러지게 하려고 했는데,%CNAME(MASTER, TARGET)%는비명을올린다응이군요」
			PRINTFORMDW 아니, 그 웃는 방법은 반대로 무섭단 말야……
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("무서워한다")
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;REACT파생 1 분기를 묻었을 경우는, 사실상 TFLAG:REACT파생 == 0(파생 「흐음」) 취급
	;이 개소만 쓰는 경우는 REACT파생에 관련되지 않고 보완이 책임을 다합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
;REACT9, 기분나쁘게웃는다