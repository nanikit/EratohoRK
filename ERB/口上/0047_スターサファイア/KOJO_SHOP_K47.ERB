;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스타 사파이어 구상 숍 이벤트 구상 파일
;
;	숍으로부터 불려 가는 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;숍 이벤트 구상
;========================================================================

;─
;EV101, 의상 변경
;─
;·숍으로부터 의상 변경을 실시했을 경우에 표시
;-------------------------------------------------
@KOJO_EVENT_K47_101(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=
;  1의 위…(0, 이제(벌써) 입고 있다, 1, 갈아입은, 2, 부끄러워서 입을 수 없는, 3, 싫어해 입지 않는, 9, 가지고 있지 않다)
;  10의 위…(0, 통상, 10, 반나체, 20, 전라, 30, 속옷 없음)
;  백의 위… 코스프레를 위해서(때문에) 보류
;  천의 위…(0, 조교자, 1000, 조교 대상)
;  예(조교 대상이 부끄러워해전라로 할 수 없었다=1022)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 갈아입었다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG < 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;통상이 되었다
		CASE 1
			PRINTFORMW 「역시 이것이 제일 침착해요♪」
			RETURN 2
		;반나체가 되었다
		CASE 11
			PRINTFORMW 「이런 옷매무새가 흐트러짐느낌은 흥분하지 않아?
			PRINTFORMW   물론 하네요? 저기?」
			PRINTFORMDW 웃는 얼굴이 무섭다…
			RETURN 2
		;전라가 되었다
		CASE 21
			PRINTFORMW 「조교 때에 옷은 방해인만인걸♪」
			PRINTFORMDW 눈앞에서 술술 옷이 열려, 마루로 떨어져 간다…
			PRINTFORM 「키득, 이미 흥분하고 있는 거야?   변태씨
			CALL HEARTCB
			PRINTW 」
			RETURN 2
		;…… 스으스으 한다						현재의 Rev에서는 미실장@/L
		[SKIPSTART]
		CASE 31
			PRINTFORMW 「킥킥…♪」
			PRINTFORMDW 스타는 의미심장한 미소를 띄우고 있다…
			RETURN 2
		[SKIPEND]
		;그 외
		CASEELSE
			RETURN 0
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 부끄러워해 갈아입을 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG < 1000 && (ARG % 10) == 3
	SELECTCASE ARG
		;반나체라도 부끄럽다
		CASE 12
			PRINTFORMW 「평상시부터옷을 벗는 것은 조금…」
			RETURN 2
		;평상시부터전라는 조금……
		CASE 22
			PRINTFORMW 「주위에%CNAME(MASTER)%밖에 없어도 부끄러운 것은 부끄러워요」
			RETURN 2
		[SKIPSTART]
		;팬츠가 없기 때문에 부끄러운 걸! 		현재의 Rev에서는 미실장@/L
		CASE 32
			PRINTFORMDW %CALLNAME:TARGET%는 한 번 벗은 팬츠를 한번 더 꼼질꼼질 입기 시작했다.
			PRINTFORMW 「이것은 전부 벗는 것과는 또 다른 부끄러움이 있어요…」
			RETURN 2
		[SKIPEND]
		;그 외
		CASEELSE
			RETURN 0
	ENDSELECT

ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 싫어해 갈아입을 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG < 1000 && (ARG % 10) == 2
	SELECTCASE ARG
		;반나체는 싫어
		CASE 13
			PRINTFORMW 「어째서%CNAME(MASTER)%의 명령으로 내가 벗지 않으면 안 되는 것일까?」
			RETURN 2
		;평상시부터전라는 싫어
		CASE 23
			PRINTFORMW 「어째서%CNAME(MASTER)%의 명령으로 내가 벗지 않으면 안 되는 것일까?」
			RETURN 2
		[SKIPSTART]
		;스으스으 하는 것은 혐						현재의 Rev에서는 미실장@/L
		CASE 33
			PRINTFORMW 「어째서%CNAME(MASTER)%의 명령으로 내가 벗지 않으면 안 되는 것일까?」
			RETURN 2
		[SKIPEND]
		CASEELSE
			RETURN 0
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 갈아입는 것을 그만둔	FIRSTEVENT(30)
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%의 의상은 벌써[　　]로 설정되어 있습니다
;------------------------------------------------
;IF 구상 기입 체크(0) && ARG < 1000 && (ARG % 10) == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상을 갈아입게 했다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG >= 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;통상이 되었다
		CASE 1001
			PRINTFORMW 「응응, 역시 벗기는 것도 즐길 수 없으면」
			RETURN 2
		;반나체가 되었다
		CASE 1011
			PRINTFORMW 「어느 정도는 즐거움이 남아있는 편이 좋네요♪
			PRINTFORM 벗기는 즐거움
			CALL HEARTCB
			PRINTW 」
			RETURN 2
		;전라가 되었다
		CASE 1021
			PRINTFORM 「킥킥
			CALL HEARTCB
			PRINTW 」
			PRINTFORMDW 스타는 싱글벙글 하면서 전신을 핥고 돌리도록(듯이) 보고 있다…
			RETURN 2
		[SKIPSTART]
		;…… 스으스으 한다						현재의 Rev에서는 미실장@/L
		CASE 1031
			PRINTFORMW 「어울리고 있는 어울리고 있다. 매우 팬츠는 있지 않게는 안보여요」
			RETURN 2
		[SKIPEND]
		CASEELSE
			RETURN 0
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 부끄러워해 갈아입을 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG >= 1000 && (ARG & 10) == 2
	SELECTCASE ARG
		;반나체라도 부끄럽다
		CASE 1012
			PRINTFORMW 「이제(벌써), 이 정도로 부끄러운거야?
			PRINTFORMW 아직도 조교가 필요같구나」
			RETURN 2
		;평상시부터전라는 부끄럽다
		CASE 1022
			PRINTFORMW 「이제(벌써), 나 이외에 보여질 것이 아니고 별로 좋지 않아…」
			RETURN 2
		[SKIPSTART]
		;팬츠가 없기 때문에 부끄러운 걸! 		현재의 Rev에서는 미실장@/L
		CASE 1032
			PRINTFORMW 「아라아라, 팬츠를 입지 않은 것은 건강에도 좋은거야?
			PRINTFORMW   노 팬츠 건강법이라는 녀석다워요
			PRINTFORMW … 그렇게 싫을 것 같은 얼굴 하지 말아요」
			RETURN 2
		[SKIPEND]
		CASEELSE
			RETURN 0
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 싫어해 갈아입을 수 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG >= 1000 && (ARG & 10) == 3
	SELECTCASE ARG
		;반나체는 싫어
		CASE 1013
			PRINTFORMW 「이제(벌써), 조교가 부족한 것일까…」
			RETURN 2
		;평상시부터전라는 싫어
		CASE 1023
			PRINTFORMW 「이제(벌써), 조교가 부족한 것일까…」
			RETURN 2
		[SKIPSTART]
		;스으스으 하는 것은 혐						현재의 Rev에서는 미실장@/L
		CASE 1033
			PRINTFORMW 「이제(벌써), 조교가 부족한 것일까…」
			RETURN 2
		[SKIPEND]
		;그 외
		CASEELSE
			RETURN 0
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상을 갈아입게 하는 것을 그만둔 FIRSTEVENT(70)
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:MASTER%의 의상은 벌써[　　]로 설정되어 있습니다
;------------------------------------------------
;IF 구상 기입 체크(0) && ARG >= 1000 && (ARG % 10) == 0
;ENDIF

;─
;EV102, 조교자 변경
;─
;·조교자를 교대시켰을 때에 표시
;-------------------------------------------------
@KOJO_EVENT_K47_102(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 구상주가 다른 캐릭터에게 맡긴, 1, 구상주가 조교자가 되었다)
;구조교자는 「%CALLNAME:SHOP_OLDTARGET%」, 신조교자는 「%CALLNAME:SHOP_NEWTARGET%」로 참조할 수 있다
;구상주는 TARGET로 참조
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;다른 조교자에게 교대
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 0
	;조교 대상의 반항각인 Lv5 이상
	IF MARK:반항각인 >= 5
		PRINTFORMW 「이제(벌써),%CNAME(MASTER)%같은 재미없는 애완동물은 여기로부터 사절이야!」
	;조교 대상의 반항각인 Lv4 이하
	ELSE
		PRINTFORMW 「좀 더 즐기고 싶은 것에 유감이구나.
		PRINTFORMW   버섯의 분재의 손질을 해 와야지」
		PRINTFORMDW 전혀 유감이지 않는 것 같은 분위기다…
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스타님이 조교자가 되었다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	;조교 대상의 반항각인 Lv5 이상
	IF MARK:반항각인 >= 5
		PRINTFORMW 「도대체 어떤 기분의 변화인 것일까?」
		PRINTFORMDW 스타는 룰루랄라해 조교의 준비를 시작하고 있다…
	;조교 대상의 반항각인 Lv4 이하
	ELSE
		PRINTFORMW 「후후훗, 조교 같은거 즐거움♪」
	ENDIF
	RETURN 2
ENDIF

;─
;EV110, 마력 사용
;─
;·마력을 소비해 어떠한 특전을 구입했을 때에 표시
;-------------------------------------------------
@KOJO_EVENT_K47_110(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 마력 사용전, 1, 조교 기교, 2, 지식을 얻기 전, 3, 약학지식 취득, 4, 매혹 취득, 5, 금단의지식 취득, 6, 도구사용 취득,
;	7, 기술을 얻기 전, 8, 혀사용 취득, 9, 기묘한손가락취득, 10, 결박능숙, 11, 새디즘 소거/새디즘 취득, 12, 불결무시 취득, 38, 착의플레이선호, 39, 허리사용)
;이벤트 구상: 아이템
;인수 내역…
;ARG=(13, 아이템 연성전, 14, 로터 연성, 15, 바이브 연성, 16, 애널비즈 연성, 17, 페니스밴드 연성, 18, 애널바이브 연성
; 19, 클리캡 연성, 20, 니플캡 연성, 21, 오나홀 연성)
;이벤트 구상:고문도구 연성
;인수 내역…
;ARG=(22, 고문도구 연성전, 23, 아이마스크 연성, 24, 채찍 연성, 25, 줄연성, 26, 볼개그 연성, 27,   바늘 연성, 28, 관장세트 연성
; 29, 삼각목마 연성, 30, 대거울 연성, 31, 목걸이 연성, 32, 도구를 커스터마이즈 했다)
;그 외 이벤트
;인수 내역…
;ARG=(33, 후타나리화/되돌리는, 34, 거유화/되돌리는, 35, 빈유화/되돌리는, 36, 정력의 한계 가격인상승)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;스타님마력 사용전
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스타님의 조교 기교가 업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 1
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「조금은 조교도 능숙하게 되었을까.
		PRINTFORMW 이것으로 좀 더 이것저것 할 수 있어요♪」
	;아라이먼트부
	ELSE
		PRINTFORMW 「후훗, 아프고, 그렇지만 몸에는 상냥한 절묘한 힘 조절을 알 수 있던 생각이 들어요」
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스타님SM상품 구입
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && INRANGE(ARG, 23, 31)
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「조교라고 하면 역시,?」
		PRINTFORMDW 스타는 싱글벙글 하면서 무서운 도구를 닦고 있다.
	;아라이먼트부
	ELSE
		PRINTFORMW 「빨리 사용해 보고 싶어요♪이것은 어울릴 것 같아」
		PRINTFORMDW 스타는 도구의 설명서를 열심히 읽고 있다…
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;스타님후타나리화 or후타나리 소거
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 33
	;후타나리화
	IF TALENT:TARGET:후타나리
		;조교 대상이 남자
		IF TALENT:MASTER:남자
			PRINTFORMW 「킥킥, 이것으로 전부군요.
			PRINTFORMW   가득 범해 준다♪
			PRINTFORMW   어머나? 말하고 있는 의미를 모를까?
			PRINTFORM   괜찮아요, 곧바로 가르쳐 주기 때문에
			CALL HEARTCB
			PRINTW 」
			PRINTFORMDW 굉장히 싫은 예감이 한다…
		;조교 대상이 처녀
		ELSEIF TALENT:MASTER:처녀
			PRINTFORMDW 스타가%CNAME(MASTER)%의 눈앞에서 스커트를 걷어올리면
			PRINTFORMDW 남성기가 얼굴을 들여다 보게 했다.
			PRINTFORMW 「우후후, 새빨갛게 되어 버려 사랑스럽네요
			PRINTFORMW   무엇을 위해서 이것이 있는지 모를 이유 없네요?」
			PRINTFORMDW 스타는 싱글벙글 하면서 이쪽으로와 다가오면
			PRINTFORMDW %CNAME(MASTER)%를 밀어넘어뜨리기, 귀에 숨결이 닿는 거리로 속삭였다.
			PRINTFORMW 「처녀막을 관철해 아픈 아프다고 외친다%CNAME(MASTER)%의 몸안을 휘저어
			PRINTFORM   아기가 생길 때까지 몇번이나 몇번이나 더럽히기 (위해)때문에
			CALL HEARTCB
			PRINTW 」
			PRINTFORMDW 스타는 깡총%CNAME(MASTER)%의 몸으로부터 뛰어 내리면 싱글벙글 미소지었다.
			PRINTFORMW 「농담이야, 농담♪」
		;그 외
		ELSE
			PRINTFORMDW 스타는%CNAME(MASTER)%의 눈앞에서 스커트를 걷어올리면
			PRINTFORMDW 남성기가 얼굴을 들여다 보게 했다.
			PRINTFORMW 「이것으로 정말로%CNAME(MASTER)%와 하나가 될 수 있어요♪
			PRINTFORMW   건강한 아기를 낳아」
		ENDIF
	;후타나리 소거
	ELSE
		;조교 대상이 남자
		IF TALENT:MASTER:남자
			PRINTFORMW 「어머, 나 있는 것도 즐거웠는데」
			PRINTFORMDW 스타는 조금 유감스러운 듯하다.
		;조교 대상이 처녀
		ELSEIF TALENT:MASTER:처녀
			PRINTFORMW 「아─아, 사라져 버렸다.
			PRINTFORMW   좀 더 힘이라도 범해 버리면 좋았어요」
		;조건 없음
		ELSE
			PRINTFORMW 「모처럼 났는데 사라져 버렸다…
			PRINTFORMW   시시한거야」
		ENDIF
	ENDIF
	RETURN 2
ENDIF

;─
;EV111, 주인님으로서 새로운 조교자를 고용했다
;─
@KOJO_EVENT_K47_111(ARG)
;-------------------------------------------------
;·새로운 조교자가 TARGET가 되어 있기 (위해)때문에, 구상주는 TARGET는 아니기 때문에 주의
;·구상주를 참조하고 싶은 경우는 FLAG:주인님으로
;-------------------------------------------------
;반환값(1, 표시, 2, 표시해 가입한 캐릭터의 가입 구상을 스킵)
;-------------------------------------------------
IF 구상기입체크(100)
	;CSTR:(FLAG:주인님):호칭목록 = 전의 이름_부르게 하고 싶은 이름 /%CSTR:(FLAG:주인님):호칭목록%
	;아라이먼트정
	IF ALI(0, (FLAG:주인님))
		PRINTFORMW 「심한 애완동물. 나라고 하는 것이 있으면서 새로운 사람을 고용한다니」
		PRINTFORMDW 스타는 마치 인사와 같이 말했다.
	;아라이먼트부
	ELSE
		PRINTFORMW 「아라? 기브업 해 버렸을까.
		PRINTFORMW 그런 근성이 없는 애완동물은 벌이 필요해요」
		PRINTFORMDW 스타는 오싹 하는 뭔가가 있는 싱글벙글로 한 웃는 얼굴을 띄운다…
	ENDIF
	RETURN 1
ENDIF

;─
;EV112, 스타님이 조교자로서 가입했다
;─
@KOJO_EVENT_K47_112(ARG)
;-------------------------------------------------
;·주인님의 이름은 「FLAG:2」로 참조할 수 있다
;·주인님이 고용 구상으로 반환값 2를 돌려주고 있으면 표시되지 않는다
;-------------------------------------------------
IF 구상기입체크(100)
	;조교 대상의 반항각인 Lv5 이상
	IF MARK:(FLAG:주인님):반항각인 >= 5
		PRINTFORMW 「아라아라 애완동물이 받을 수 있다 라고 (들)물었는데 상당히 무서운 것 같은 애완동물」
		PRINTFORMW 스타는 곤란한 것처럼 웃고 있다.
	;조교 대상의 반항각인 Lv4 이하
	ELSE
		PRINTFORMW 「애완동물은 인간이었던 것이군요. 어떻게 가르치면 좋은 것일까?」
	ENDIF
	RETURN 2
ENDIF