;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	スターサファイア口上 ショップイベント口上ファイル
;
;	ショップから呼び出される口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;ショップイベント口上
;========================================================================

;────────────────────────────────────
;EV101, 衣装変更
;────────────────────────────────────
;・ショップから衣装変更を行った場合に表示
;-------------------------------------------------
@KOJO_EVENT_K47_101(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=
;　一の位…（0, もう着てる、1, 着替えた、2, 恥ずかしくて着られない、3, 嫌がって着ない、9, 持ってない）
;　十の位…（0, 通常、10, 半裸、20, 全裸、30, 下着無し）
;　百の位…コスプレのために保留
;　千の位…（0, 調教者, 1000, 調教対象）
;　例（調教対象が恥ずかしがって全裸にできなかった＝1022）
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が着替えた
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG < 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;通常になった
		CASE 1
			PRINTFORMW 「やっぱりこれが一番落ち着くわね♪」
			RETURN 2
		;半裸になった
		CASE 11
			PRINTFORMW 「こういう着崩れた感じって興奮しない？
			PRINTFORMW 　もちろんするわよね？ね？」
			PRINTFORMDW 笑顔が怖い…
			RETURN 2
		;全裸になった
		CASE 21
			PRINTFORMW 「調教のときに服は邪魔なだけだものね♪」
			PRINTFORMDW 目の前でスルスルと服がはだけ、床へと落ちてゆく…
			PRINTFORM 「くすっ、もう興奮してるの？　変態さん
			CALL HEARTCB
			PRINTW 」
			RETURN 2
		;……すーすーする						現在のRevでは未実装@/L
		[SKIPSTART]
		CASE 31
			PRINTFORMW 「くすくす…♪」
			PRINTFORMDW スターは意味深な笑みを浮かべている…
			RETURN 2
		[SKIPEND]
		;その他
		CASEELSE
			RETURN 0
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が恥ずかしがって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG < 1000 && (ARG % 10) == 3
	SELECTCASE ARG
		;半裸だって恥ずかしい
		CASE 12
			PRINTFORMW 「普段から服を脱ぐのはちょっと…ね」
			RETURN 2
		;普段から全裸はちょっと……
		CASE 22
			PRINTFORMW 「周りに%CNAME(MASTER)%しかいなくても恥ずかしいものは恥ずかしいわ」
			RETURN 2
		[SKIPSTART]
		;パンツが無いから恥ずかしいもん！		現在のRevでは未実装@/L
		CASE 32
			PRINTFORMDW %CALLNAME:TARGET%は一度脱いだパンツをもう一度もぞもぞと穿きはじめた。
			PRINTFORMW 「これは全部脱ぐのとはまた別の恥ずかしさがあるわね…」
			RETURN 2
		[SKIPEND]
		;その他
		CASEELSE
			RETURN 0
	ENDSELECT

ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が嫌がって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG < 1000 && (ARG % 10) == 2
	SELECTCASE ARG
		;半裸は嫌
		CASE 13
			PRINTFORMW 「どうして%CNAME(MASTER)%の命令で私が脱がないといけないのかしら？」
			RETURN 2
		;普段から全裸なんて嫌
		CASE 23
			PRINTFORMW 「どうして%CNAME(MASTER)%の命令で私が脱がないといけないのかしら？」
			RETURN 2
		[SKIPSTART]
		;すーすーするのは嫌						現在のRevでは未実装@/L
		CASE 33
			PRINTFORMW 「どうして%CNAME(MASTER)%の命令で私が脱がないといけないのかしら？」
			RETURN 2
		[SKIPEND]
		CASEELSE
			RETURN 0
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者が着替えるのをやめた	FIRSTEVENT(30)
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:TARGET%の衣装はすでに[　　]に設定されています
;------------------------------------------------
;IF 口上記入チェック(0) && ARG < 1000 && (ARG % 10) == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象を着替えさせた
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG >= 1000 && (ARG % 10) == 1
	SELECTCASE ARG
		;通常になった
		CASE 1001
			PRINTFORMW 「うんうん、やっぱり脱がすのも楽しめないとね」
			RETURN 2
		;半裸になった
		CASE 1011
			PRINTFORMW 「ある程度は楽しみが残ってるほうがいいわよね♪
			PRINTFORM 脱がす楽しみ
			CALL HEARTCB
			PRINTW 」
			RETURN 2
		;全裸になった
		CASE 1021
			PRINTFORM 「くすくす
			CALL HEARTCB
			PRINTW 」
			PRINTFORMDW スターはニコニコしながら全身を嘗め回すように見ている…
			RETURN 2
		[SKIPSTART]
		;……すーすーする						現在のRevでは未実装@/L
		CASE 1031
			PRINTFORMW 「似合ってる似合ってる。とってもパンツはいてないようには見えないわ」
			RETURN 2
		[SKIPEND]
		CASEELSE
			RETURN 0
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が恥ずかしがって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG >= 1000 && (ARG & 10) == 2
	SELECTCASE ARG
		;半裸だって恥ずかしい
		CASE 1012
			PRINTFORMW 「もう、この程度で恥ずかしいの？
			PRINTFORMW まだまだ調教が必要みたいね」
			RETURN 2
		;普段から全裸なんて恥ずかしい
		CASE 1022
			PRINTFORMW 「もうっ、私以外に見られるわけじゃないし別にいいじゃない…」
			RETURN 2
		[SKIPSTART]
		;パンツがないから恥ずかしいもん！		現在のRevでは未実装@/L
		CASE 1032
			PRINTFORMW 「あらあら、パンツをはかないのは健康にもいいのよ？
			PRINTFORMW 　ノーパン健康法ってやつらしいわ
			PRINTFORMW …そんな嫌そうな顔しないでよ」
			RETURN 2
		[SKIPEND]
		CASEELSE
			RETURN 0
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が嫌がって着替えられない
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG >= 1000 && (ARG & 10) == 3
	SELECTCASE ARG
		;半裸は嫌
		CASE 1013
			PRINTFORMW 「もうっ、調教が足りないのかしら…」
			RETURN 2
		;普段から全裸なんて嫌
		CASE 1023
			PRINTFORMW 「もうっ、調教が足りないのかしら…」
			RETURN 2
		[SKIPSTART]
		;すーすーするのは嫌						現在のRevでは未実装@/L
		CASE 1033
			PRINTFORMW 「もうっ、調教が足りないのかしら…」
			RETURN 2
		[SKIPEND]
		;その他
		CASEELSE
			RETURN 0
	ENDSELECT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象を着替えさせるのをやめた FIRSTEVENT(70)
;━━━━━━━━━━━━━━━━━━━━━━━━
;%CALLNAME:MASTER%の衣装はすでに[　　]に設定されています
;------------------------------------------------
;IF 口上記入チェック(0) && ARG >= 1000 && (ARG % 10) == 0
;ENDIF

;────────────────────────────────────
;EV102, 調教者変更
;────────────────────────────────────
;・調教者を交代させたときに表示
;-------------------------------------------------
@KOJO_EVENT_K47_102(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 口上主が他のキャラに任せた、1, 口上主が調教者になった)
;旧調教者は「%CALLNAME:SHOP_OLDTARGET%」、新調教者は「%CALLNAME:SHOP_NEWTARGET%」で参照できる
;口上主はTARGETで参照
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;他の調教者に交代
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 0
	;調教対象の反抗刻印Lv5以上
	IF MARK:反抗刻印 >= 5
		PRINTFORMW 「もうっ、%CNAME(MASTER)%みたいなつまんないペットはこっちから願い下げよ！」
	;調教対象の反抗刻印Lv4以下
	ELSE
		PRINTFORMW 「もっと楽しみたいのに残念ね。
		PRINTFORMW 　キノコの盆栽の手入れをしてこようっと」
		PRINTFORMDW 全然残念ではなさそうな雰囲気だ…
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スターさまが調教者になった
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 1
	;調教対象の反抗刻印Lv5以上
	IF MARK:反抗刻印 >= 5
		PRINTFORMW 「一体どんな気持ちの変化なのかしら？」
		PRINTFORMDW スターはノリノリで調教の準備をはじめている…
	;調教対象の反抗刻印Lv4以下
	ELSE
		PRINTFORMW 「ふふふっ、調教なんて楽しみ♪」
	ENDIF
	RETURN 2
ENDIF

;────────────────────────────────────
;EV110, 魔力使用
;────────────────────────────────────
;・魔力を消費して何らかの特典を購入したときに表示
;-------------------------------------------------
@KOJO_EVENT_K47_110(ARG)
;-------------------------------------------------
;引数内訳…
;ARG=(0, 魔力使用前、1, 調教技巧、2, 知識を得る前、3, 調合知識取得、4, 魅惑取得、5, 禁断の知識取得、6, 道具使い取得、
;	7, 技術を得る前、8, 舌使い取得、9, 器用な指先取得、10, 縛り上手、11, サド消去/サド取得、12, 汚れ無視取得、 38, 着衣プレイ好き、39, 腰使い)
;イベント口上：アイテム
;引数内訳…
;ARG=(13, アイテム練成前、14, ローター練成、15,バイブ練成、16, アナルビーズ練成、 17, ペニスバンド練成、 18, アナルバイブ練成
; 19, クリキャップ練成、 20, ニプルキャップ練成、21, オナホール練成)
;イベント口上：責め具錬成
;引数内訳…
;ARG=(22, 責め具練成前、23, アイマスク練成、24, 鞭練成、 25, 縄練成、 26, ボールギャグ練成、27,  針練成、28, 浣腸セット練成
; 29, 三角木馬練成、 30, 大鏡練成、 31, 首輪練成、 32, 道具をカスタマイズした)
;その他イベント
;引数内訳…
;ARG=(33, ふたなり化/戻す、34, 巨乳化/戻す、35, 貧乳化/戻す、36, 精力の限界値上昇)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;スターさま魔力使用前
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 口上記入チェック(0) && ARG == 0
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スターさまの調教技巧がアップ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 1
	;アライメント正
	IF ALI(0)
		PRINTFORMW 「ちょっとは調教も上手になったかしら。
		PRINTFORMW これでもっとあれこれできるわね♪」
	;アライメント負
	ELSE
		PRINTFORMW 「ふふっ、痛く、でも体には優しい絶妙な力加減がわかったきがするわ」
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スターさまＳＭグッズ購入
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && INRANGE(ARG, 23, 31)
	;アライメント正
	IF ALI(0)
		PRINTFORMW 「調教っていえばやっぱり、ね？」
		PRINTFORMDW スターはニコニコしながら恐ろしい道具を磨いている。
	;アライメント負
	ELSE
		PRINTFORMW 「早く使ってみたいわ♪これなんて似合いそうよ」
		PRINTFORMDW スターは道具の説明書を熱心に読んでいる…
	ENDIF
	RETURN 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;スターさまふたなり化orふたなり消去
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(100) && ARG == 33
	;ふたなり化
	IF TALENT:TARGET:ふたなり
		;調教対象がオトコ
		IF TALENT:MASTER:オトコ
			PRINTFORMW 「くすくす、これでおそろいね。
			PRINTFORMW 　いっぱい犯してあげる♪
			PRINTFORMW 　あら？言っている意味がわからないかしら？
			PRINTFORM 　大丈夫よ、すぐに教えてあげるから
			CALL HEARTCB
			PRINTW 」
			PRINTFORMDW ものすごく嫌な予感がする…
		;調教対象が処女
		ELSEIF TALENT:MASTER:処女
			PRINTFORMDW スターが%CNAME(MASTER)%の目の前でスカートをたくし上げると
			PRINTFORMDW 男性器が顔を覗かせた。
			PRINTFORMW 「うふふ、真っ赤になっちゃってかわいいわね
			PRINTFORMW 　何のためにこれがあるか分からないわけないわよね？」
			PRINTFORMDW スターはニコニコしながらこちらへと歩み寄ってくると
			PRINTFORMDW %CNAME(MASTER)%を押し倒し、耳に息がかかる距離で囁いた。
			PRINTFORMW 「処女膜を貫いて痛い痛いって叫ぶ%CNAME(MASTER)%の体の中を掻き回して
			PRINTFORM 　赤ちゃんが出来るまで何度も何度も汚すため
			CALL HEARTCB
			PRINTW 」
			PRINTFORMDW スターはぴょこんと%CNAME(MASTER)%の体から飛び降りるとニコニコと笑いかけた。
			PRINTFORMW 「冗談よ、冗談♪」
		;その他
		ELSE
			PRINTFORMDW スターは%CNAME(MASTER)%の目の前でスカートをたくし上げると
			PRINTFORMDW 男性器が顔を覗かせた。
			PRINTFORMW 「これで本当に%CNAME(MASTER)%と一つになれるわ♪
			PRINTFORMW 　元気な赤ちゃんを産んでね」
		ENDIF
	;ふたなり消去
	ELSE
		;調教対象がオトコ
		IF TALENT:MASTER:オトコ
			PRINTFORMW 「あらら、生えてるのも楽しかったのにな」
			PRINTFORMDW スターはちょっと残念そうだ。
		;調教対象が処女
		ELSEIF TALENT:MASTER:処女
			PRINTFORMW 「あーあ、消えちゃった。
			PRINTFORMW 　もっと力ずくにでも犯しちゃえばよかったわ」
		;条件なし
		ELSE
			PRINTFORMW 「折角生えたのにきえちゃった…
			PRINTFORMW 　つまらないの」
		ENDIF
	ENDIF
	RETURN 2
ENDIF

;────────────────────────────────────
;EV111, ご主人様として新しい調教者を雇った
;────────────────────────────────────
@KOJO_EVENT_K47_111(ARG)
;-------------------------------------------------
;・新しい調教者がTARGETになっているため、口上主はTARGETではないので注意
;・口上主を参照したい場合はFLAG:ご主人様で
;-------------------------------------------------
;戻り値（1,表示、2,表示して加入したキャラの加入口上をスキップ）
;-------------------------------------------------
IF 口上記入チェック(100)
	;CSTR:(FLAG:ご主人様):呼び方リスト = 元の名前_呼ばせたい名前/%CSTR:(FLAG:ご主人様):呼び方リスト%
	;アライメント正
	IF ALI(0, (FLAG:ご主人様))
		PRINTFORMW 「ひどいペットね。私と言うものがありながら新しい人を雇うだなんて」
		PRINTFORMDW スターはまるで人事のように言った。
	;アライメント負
	ELSE
		PRINTFORMW 「あら？ギブアップしちゃったのかしら。
		PRINTFORMW そんな根性のないペットはおしおきが必要よね」
		PRINTFORMDW スターはぞっとする何かがあるニコニコとした笑顔を浮かべる…
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;EV112, スターさまが調教者として加入した
;────────────────────────────────────
@KOJO_EVENT_K47_112(ARG)
;-------------------------------------------------
;・ご主人様の名前は「FLAG:2」で参照できる
;・ご主人様が雇用口上で戻り値2を返していると表示されない
;-------------------------------------------------
IF 口上記入チェック(100)
	;調教対象の反抗刻印Lv5以上
	IF MARK:(FLAG:ご主人様):反抗刻印 >= 5
		PRINTFORMW 「あらあらペットが貰えるって聞いたのに随分と怖そうなペットね」
		PRINTFORMW スターは困ったように笑っている。
	;調教対象の反抗刻印Lv4以下
	ELSE
		PRINTFORMW 「ペットって人間だったのね。どうやってしつければいいのかしら？」
	ENDIF
	RETURN 2
ENDIF