;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	스타 사파이어 구상 조교 개시 이벤트 구상 파일
;
;	조교 개시 이벤트 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV11, 조교 개시 구상
;========================================================================
;·대체로의 경우, 구상 작자가 우선 생각하는 것은 "주인"으로서의 구상이라고 생각되므로
;·@KOJO_MS_EVENT로서 TALENT:주인님인 경우로 한정해 버립니다
;·이벤트 관련은 구상주의 입장에 의해 정합성이 손상될 가능성이 있으므로 이러한 처치로 했습니다
;·구상내의 모든 개소에서 구상 함수에 의한 같은 배타적 연산은 가능했다거나 합니다
;-------------------------------------------------
;  소환되어 조교를 맡겨지는 경우의 구상을 쓴다면
;  호출을@KOJO_EVENT_KX1_11으로서 배타를 실시하지 않고, TALENT:주인님과 그 외로 분기를 마련한다
;  EVENT1, OP(주인인 경우에 밖에 처리되지 않는다)로 KFLAG에의 대입을 실시해, 분기를 마련한다
;  혹은, @KOJO_AS_EVENT_KX1_11으로서 별파일에 분리해 버리면 좋을 것입니다
;-------------------------------------------------
;우선 분기를 마련해 있습니다만, EVENT11범위내의 분기의 증감은 자유롭게 할 수 있습니다
;COMMON_GETTER_CHARA.ERB내, @AFFECTION에는 여기에 없는 것도 정의되어 있거나 합니다
;다만, 조건의 느슨한 분기를 상층에 가지고 오면 하위의 분기가 모두 공수상이 되므로
;우선도에는 충분히 주의해 주세요
;-------------------------------------------------
@KOJO_MS_EVENT_K47_11(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 통상/1, 조교자의 피로에 의한 중지/2, 조교 대상의 피로에 의한 중지)
;-------------------------------------------------
;─
;조교 첫회·10회째
;─
;·조교 첫회, 및 10회째에 표시되는 구상
;·CFLAG:조교횟수로 회수는 어떻게라도 참조할 수 있습니다만, 너무 하면 다른 조건을 탕진할 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && CFLAG:조교횟수 == 1
	;=================================================
	;반항각인 Lv5 이상
	;=================================================
	IF 구상기입체크(100) && MARK:반항각인 >= 5
		PRINTFORMW 「아라? 혹시 나의 일을 싫을까?
		PRINTFORMW   싫어 해도 괜찮아요.
		PRINTFORMW   미움받아도 돌려보내 줄 생각은 없는 것」
		PRINTFORMDW 싱글벙글로 하면서 태연하게 그렇게 말한다%CALLNAME:TARGET%이지만,
		PRINTFORMDW 어딘가 기분이 나쁜 것 같게 느꼈다.
		RETURN 1
	ENDIF
	
	;=================================================
	;트라우마 Lv5 이상
	;=================================================
	IF 구상기입체크(100) && MARK:MASTER:트라우마 >= 5
		PRINTFORMDW %CALLNAME:TARGET%는 싱글벙글로 하면서 이쪽으로와 가까워져 온다.
		PRINTFORMDW 무심코 뒷걸음을 한다고 그녀는 무언으로 손을 치켜들었다.
		PRINTFORMDW 작게 비명을 올려 머리를 감싸면 그녀는 즐거운 듯이 웃기 시작했다.
		PRINTFORMW 「이제(벌써), 장난 요정을 너무 무서워했어.
		PRINTFORMW   그런 반응되어 버리면 어쩐지 즐거워져 버려요♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;쾌락각인 Lv7 이상
	;=================================================
	IF 구상기입체크(100) && MARK:MASTER:쾌락각인 >= 7
		PRINTFORMW 「아라아라, 기쁜듯이 해 버려♪
		PRINTFORMW   장난 되는 것이 즐겁다니 변태씨네.
		PRINTFORMW   그런 변태씨에게는 벌 하지 않으면」
		PRINTFORMDW %CALLNAME:TARGET%는 콧노래 섞임에 조교의 준비를 시작했다.
		RETURN 1
	ENDIF
	
	;-------------------------
	;그 외
	;-------------------------
	PRINTFORML 「인간을 길러 보았다는 좋지만, 어떻게 하면 좋은 것일까?」
	PRINTFORMW 「애완동물은 귀여워하는 것이군요」
	PRINTL
	PRINTFORML 「역시, 우선은 스킨십일까」
	PRINTFORMW 「우응, 달라요. 우선은 상하 관계를 확실히 등실」
	;BGM 컷 인  스타보야쥬 2008
	CALL BGM_CUTIN(CSTR:곡2)
	CALL FADEIN(13, "「응, 결정했다! 우선은 조교군요♪」")
	WAIT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 10회째
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && CFLAG:조교횟수 == 10 && !ARG
	PRINTFORMW 「이제(벌써) 장난도 10회째인 거네」
	PRINTFORMW 「슬슬 좀 더 새로운 일이라든지 안개는 보고 싶어요」
	RETURN 1
ENDIF

;─
;조교중지 ARG:1 ARG:2
;─
;CFLAG:소모 > 5혹은 CFLAG:MASTER:소모 > 5를 조건으로서 불려 가는 개소
;이 조건으로부터 빠졌을 경우는 통상의 조교 개시 구상이 불린다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;피로로 조교가 중지가 되는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG > 0
	;=================================================
	;피곤한 것은 구상주(TARGET)
	;=================================================
	IF 구상기입체크(100) && ARG == 1
		SELECTCASE RANDEVENT(3, 100)
			CASE 0
				PRINTFORMW 「하아, 지치게 되었다…」
			CASE 1
				PRINTFORML 「응─……
				PRINTFORMW   충분히 놀았고 슬슬 돌아가려고」
			CASE 2
				PRINTFORMW 「버섯의 분재같이 생각하도록(듯이) 가지 않네요」
				PRINTFORMDW %CALLNAME:TARGET%는 조금 곤란한 것처럼 말해 보였다.
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;피곤한 것은 조교 대상(MASTER)
	;=================================================
	IF 구상기입체크(100) && ARG == 2
		SELECTCASE RANDEVENT(3, 101)
			CASE 0
				PRINTFORML 「아라아라, 벌써 지치게 되었어?
				PRINTFORMW   어쩔 수 없네요. 이번은 여기까지」
			CASE 1
				PRINTFORMW 「이제(벌써), 야무지지 못하네요」
				PRINTFORMDW %CALLNAME:TARGET%는 기가 막힌 것처럼 미소지었다.
			CASE 2
				PRINTFORML 「완전히 녹초가 되어 버리고 있다
				PRINTFORMW   응, 슬슬 연회의 끝일까」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;─
;그 외·호감도등에 의한 분기
;─
;·현재의 ACTABLE나 COMABLE의 사양적으로, 진전도를 측정하려면 (사실상) 호감도를 기준으로 할 수 밖에 없다고 생각된다
;·의존도는@COM로 대입되고 참조할 수 있을 뿐으로, 본체측의 거동에는 아무런 영향을 주지 않았다
;·향후의 손질 여하에 따라서는 용도도 있을지도? 그렇다고 하는 곳
;-------------------------------------------------

;-------------------------------------------------
;·서로 서로 좋아하고 있다
;-------------------------------------------------
;원래는 ED가 실장되어 있지 않은 시대에, 가짜의 ED 범위로서 사용되는 일이 많았던 개소
;현상에서도 이 단계에 이르려면, 그만한 일수와 진전이 필요하게 된다고 생각됩니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;상사상애(호의 3000이상·호감도 3000이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("상사상애")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(0)
		PRINTFORMDW %CALLNAME:TARGET%는 기쁜듯이 타박타박 이쪽으로와 달려들어 왔다.
		PRINTFORMDW 이 작은 주인님의 조교도 이것으로 몇 회째일까?
		PRINTFORMDW 이제(벌써) 그녀에게 조교되는 일에 저항을 느끼기는 커녕,
		PRINTFORMDW 마음속으로부터 기뻐하고 있는 것 부정은 할 수 있을 것 같지는 않다.
		PRINTFORMW 「오늘은 무엇을 할까?
		PRINTFORMW   후후, 버섯의 분재보다 즐거울지도 모르네요」
		PRINTFORMDW 솔직하지 않다고 말하면 어떨까? (와)과 생긋 웃어 돌려주어졌다.
		PRINTFORMDW 그렇게 사소한 교환조차도 즐거워서 어쩔 수 없다.
		PRINTFORMDW 그녀만 있으면 이대로 이 방으로부터 나올 수 없어도 괜찮다.
		PRINTFORMDW 이 붙잡을 데가 없는 요정과 이제 곧 접촉할 수 있다.
		PRINTFORMDW 그렇게 생각하는 것만으로 가슴이 기대로 크게 울렸다.
		PRINTFORMW 「이봐요 이봐요, 정신나가지 말고 시작해요.
		PRINTFORMW   나의 사랑스러운 애완동물씨♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDEVENT(3, 0)
			CASE 0
				PRINTFORMW 「자, 즐거운 조교의 시간이야♪」
			CASE 1
				PRINTFORMW 「오늘도 이상해져 버릴 정도로 기분 좋게 해 준다.
				PRINTFORMW   후후후, 야무지지 못한 얼굴 해 버려」
			CASE 2
				PRINTFORMW 「이제(벌써), 눈초리가 추잡해요」
				PRINTFORMDW 그렇게 말한다%CALLNAME:TARGET%는 어딘가 기쁜 듯했다.
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·조교 대상이 구상 주로 이제(벌써) 헤롱헤롱
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 완료(호의 2000이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("조교 완료")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(10)
		PRINTFORMW 「아라아라, 상당히 좋은 표정이 되었군요」
		PRINTFORMDW 스타에 미소지을 수 있는 무심코 가슴이 크게 울렸다.
		PRINTFORMW 「이미 기대하고 있는 거야? 성급한 애완동물.
		PRINTFORMW 그러면 기대 그대로의 일, 해 주어요%UNICODE(0x2661)%」
		PRINTFORMDW 가까워져 오는 스타의 체온에 가슴은 경종을 계속 칠 뿐이다…
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	;IF 구상 기입 체크(0)
	;ENDIF
ENDIF

[SKIPSTART]
;-------------------------------------------------
;·조교자가 조교 대상에 빠져 버려, 깊기도 하고 도착 한 사랑을 따르고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;몹시 사랑함(호감도 2000이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && AFFECTION("몹시 사랑함")
ENDIF

;-------------------------------------------------
;·조교 대상이 구상 주로 조교되는 것을 행복하게 느끼고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교가 끝난 상태(호의 1000이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && AFFECTION("조교가 끝난 상태")
ENDIF

;-------------------------------------------------
;·조교자에 대해 혐오감을 안고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;혐오(호의-100 미만)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && AFFECTION("혐오")
ENDIF

;-------------------------------------------------
;·마음에 상처를 만들어져 주인님의 모습을 보는 것만으로 무서워해 버린다
;·트라우마의 종은 「%ACTSTR(CFLAG:MASTER:8)%」로 표시할 수 있다
;  예) PRINTFORMW 「이제(벌써)%ACTSTR(CFLAG:MASTER:트라우마)%는 싫습니다 라고?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;공포(호의 200 미만·트라우마 Lv3 이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && AFFECTION("공포")
ENDIF

;-------------------------------------------------
;·마음을 열지 않는 노예에게 초조해지는 주인님
;━━━━━━━━━━━━━━━━━━━━━━━━
;반항(호의 0 이하·반항각인 Lv3 이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && AFFECTION("반항")
ENDIF

;-------------------------------------------------
;·조교되는 것도 오히려 좋아하게 되었는지도 모른다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;눈을 뜸(호의 500이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && AFFECTION("눈을 뜸")
ENDIF

;-------------------------------------------------
;·정중하게 조교를 진행되어, 생각했던 것보다 싫지 않을지도…
;━━━━━━━━━━━━━━━━━━━━━━━━
;순응(호의 100이상·호감도 100이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && AFFECTION("순응")
ENDIF
[SKIPEND]

;-------------------------------------------------
;·요컨데, 다른 조건에 히트 하지 않았던 경우에 떨어지는 개소
;·만일 다른 분기를 모두 방치했을 경우, 이 개소만 써 두면 뭔가는 표시된다든가, 그런 장소입니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;초반(조건 없음)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(90)
		PRINTFORMW 「자, 즐거운 즐거운 조교군요.
		PRINTFORMW %CNAME(MASTER)%는 어떤 애완동물으로 자라는지 지금부터 즐거움이예요」
		PRINTFORMDW 지금부터 정말로 어떻게 되어 버릴까…
		PRINTFORMDW 그다지 강한 요정은 아닌 것 같지만 밖없기 때문에 어쩔 수 없다.
		PRINTFORMDW 당분간은 이 요정의 조교 짓거리에 대해 만날 수 밖에 없는 것 같다.
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RANDEVENT(3, 90)
			CASE 0
				PRINTFORMW 「오늘은 어떤 일을 할까♪」
			CASE 1
				PRINTFORMW 「자신의 이상의 형태로 해 가는 것은 버섯의 분재와 같구나」
				PRINTFORMDW 버섯과 동일한 정도인 것인가…
			CASE 2
				PRINTFORMDW 마음 탓인지 스타의 기분이 좋은 것 같다.
				PRINTFORMDW 뭔가 좋은 일이기도 했을 것인가?
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF