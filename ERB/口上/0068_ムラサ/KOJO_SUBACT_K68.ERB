;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	무라사 구상 추가 액션 구상 파일
;
;	조교자가 조교 지시 이외에 실시하는 행동의 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;조교 액션·커맨드 구상
;========================================================================

;────────────────────────────────────
;탈의 액션 구상
;────────────────────────────────────
;·조교자 액션의 앞에 강제탈의나 자주탈의가 발생했을 때에 표시
;·이 구상의 시점에서는 아직 TEQUIP는 갱신되어 있지 않다
;·첫회 분기는 「처음 보았다/보였다」란 할 수 없다.
;  숍 메뉴로 벗게 했던 적이 있을지도 모르고, 조교의 흐름으로 완전하게 벗게 하지 않고서 노출만 하고 있는 경우도 있다.
;  어쩌면 조수였던 때에 본 적이 있을지도 모른다. 그러한 의미에서는 간이적인 것이다.
;  엄밀한 의미에서의 「초견」플래그를 준비할지 어떨지는 요망하는 대로라고 하는 일로.
;-------------------------------------------------
@KOJO_DATUI_K68
;-------------------------------------------------
;관련 변수…
;강제탈의:TFLAG:102  자주탈의:TFLAG:103
;=(-1, 강제탈의에 저항, 1, 윗도리, 2, 하반신윗도리, 3, 웃도리, 4, 상반신 전부, 5, 하반신 전부,
;  6, 악세사리─, 7, 악세사리─이외 모두, 8, 전라까지, 9, 속옷만)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의에 저항된 FIRSTDATUI(0)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 < 0
	PRINTL
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(0)
		PRINTFORMW 「아라, 그렇게 싫어하지 않아도」
		PRINTFORMW 「그대로라고, 옷이 더러워져 버려요?」
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;자신은 벗었다
	;=================================================
	IF 구상기입체크(100) && TFLAG:자주탈의
		;퍼펙트 서로 사모함
		IF KFLAG:700
			PRINTFORMW 「나는 벗고 있는데 그렇게 싫어하지 않아도～」
			PRINTFORMW 「일부러 애태움이라고 있는 거네? 이 가지 못하고」
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「응, 그렇게 싫어하지 않아도」
			PRINTFORMW 「옷을 벗어 권하고 있는 여성을 앞으로 해」
			PRINTFORMW 「맛있는 상황이다, 라고 하는 식으로는 생각해 주지 않습니까?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「흐음… 뭐, 하는 일은 변함없고」
			PRINTFORMW 「더러워지고 바를 수 있는의 냄새옷을 입어 생활하고 싶다면 멈추지 않아요」
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORMW 「에─? 어째서싫어한다응입니까―?」
		PRINTFORMW 「옷, 벗고 싶지 않은거야? 혹시 입은 채로 하고 싶은… 이라든지?」
		PRINTFORMW 「%CALLNAME:MASTER%를 하고 싶다면, 나는 그런데도 좋지만 말이죠」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「그러니까, 옷이 더러워져 버려요?」
		PRINTFORMW 「%CALLNAME:MASTER%에도 이 환경을 빨리 받아들여 주었으면 하는 것이군요」
	;아라이먼트부
	ELSE
		PRINTFORMW 「흐음… 뭐, 하는 일은 변함없고」
		PRINTFORMW 「더러워지고 바를 수 있는의 냄새옷을 입어 생활하고 싶다면 멈추지 않아요」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;두 사람 모두 뭔가 벗은 FIRSTDATUI(1)
;------------------------------------------------
;·주로 섹스계 행동의 예비 동작으로 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 > 0 && TFLAG:자주탈의 && !IS_NOWASSIACTNAME(, "탈의")
	PRINTL
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(1)
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「네, 빨리%CALLNAME:MASTER%도 벗어」
			PRINTFORMDW 무라사는 자신의 의복을 벗으면서,%CALLNAME:MASTER%에 그렇게 고한다
			PRINTFORMW 「자, 함께 기분 좋아집시다?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「이봐요, 빨리%CALLNAME:MASTER%도 벗으세요」
			PRINTFORMDW 무라사는 자신의 의복을 벗으면서,%CALLNAME:MASTER%에 그렇게 고한다
			PRINTFORMW 「너무 기다리게 하면 괴롭혀 버려요?」
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가[정욕]상태
	;=================================================
	IF 구상기입체크(100) && T_COND("정욕")
		;퍼펙트 서로 사모함
		IF KFLAG:700
			PRINTFORMW 「저기,%CALLNAME:MASTER%도 빨리 벗어, 빨리 빨리♪」
			PRINTFORMDW 미나미츠는 자신의 의복을 기세 좋게 벗어 던지면서,%CALLNAME:MASTER%를 재촉한다
			PRINTFORMW 「빨리 가득 해, 기다릴 수 없어요♪」
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「빨리%CALLNAME:MASTER%도 벗어, 가능한 한 서둘러?」
			PRINTFORMDW 무라사는 재빠르게 자신의 의복을 벗으면서,%CALLNAME:MASTER%에 그렇게 고한다
			PRINTFORMW 「나, 이제 인내 할 수 없어요… 빨리기분좋게해줘군요?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「후후, 욕정해 버렸어요… 이봐요, 빨리%CALLNAME:MASTER%도 벗으세요」
			PRINTFORMDW 무라사는 자신의 의복을 벗으면서,%CALLNAME:MASTER%에 그렇게 고한다
			PRINTFORMW 「만족시켜 주지 않으면 괴롭혀 버려요?」
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORMW 「후훗,%CALLNAME:MASTER%도 빨리 벗으며♪」
		PRINTFORMDW 미나미츠는 자신의 의복을 벗어 던져,%CALLNAME:MASTER%가 옷을 벗는 것을 도왔다
		PRINTFORMW 「기분 좋은 일 가득 하자♪」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「네, 빨리%CALLNAME:MASTER%도 벗어」
		PRINTFORMDW 무라사는 자신의 의복을 벗으면서,%CALLNAME:MASTER%에 그렇게 고한다
		PRINTFORMW 「자, 함께 기분 좋아집시다?」
	;아라이먼트부
	ELSE
		PRINTFORMW 「이봐요, 빨리%CALLNAME:MASTER%도 벗으세요」
		PRINTFORMDW 무라사는 자신의 의복을 벗으면서,%CALLNAME:MASTER%에 그렇게 고한다
		PRINTFORMW 「너무 기다리게 하면 괴롭혀 버려요?」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·악세사리─마셔 FIRSTDATUI(2)
;------------------------------------------------
;·주로 양말. 그 밖에 장갑이나 소매 따위
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 == 6 && !IS_NOWASSIACTNAME(, "탈의")
	PRINTL
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(2)
		PRINTFORMDW 무라사는 그 자리에 주저앉으면, 천천히 양말을 벗어 던졌다…
		PRINTFORMW 「움직이고 있으면 상당히 뜸들어 옵니다」
		PRINTL
		RETURN 1
	ENDIF

	;=================================================
	;전라가 되었다(벗기 전은 양말 이외 알몸이라든지 그러한 느낌)
	;=================================================
	IF 구상기입체크(100) && TFLAG:자주탈의 && ZENRACHECK(0)
		;퍼펙트 서로 사모함
		IF KFLAG:700
			PRINTFORMDW %CALLNAME:MASTER%는 한가닥의 실 감기지 않는 모습이 된 미나미츠를, 구멍이 여는 만큼 응시했다.
			PRINTFORMW 「우… 그, 그렇게 볼 수 있으면 부끄럽어요～」
			PRINTFORMDW … 심하게 부끄러워한다 되어 하지 않았습니까 미나미츠씨?
			;너무 길므로 생략			확률을 짜 회수해 버립니다@/L
			IF PERCENT(15)
				PRINTL
				PRINTFORMW 「아, 우웃… 이제(벌써) 바봇, 심술궂은 것이니까」
				PRINTFORMDW 수줍음 감추기에 이쪽을 따끈따끈 후려갈겨 왔다… 뭐, 힘은 완전히 들어 있지 않습니다만
			ENDIF
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORMDW 무라사는 양말을 벗어 던져, 한가닥의 실 감기지 않는 모습이 되었다.
			PRINTFORMW 「그런데, 이것으로 이쪽은 숨기는 것이 무엇하나 없어요」
			PRINTFORMW 「이런 무방비인 나를, 덮쳐 보고 싶지는 되지 않겠습니까?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「아무것도 몸에 대지 않은 소녀에게 습격당하는 기분이라고 하는 것은 어떨까요」
			PRINTFORMW 「저기,%CALLNAME:MASTER%?   지금 어떤 기분입니까?」
			PRINTFORMDW 무라사는%CALLNAME:MASTER%에 과시하도록(듯이) 양말을 벗어 던지고전라가 되었다…
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORMDW 미나미츠는%CALLNAME:MASTER%에 과시하도록(듯이), 천천히 양말을 벗어 던졌다…
		PRINTFORMW 「후후, 옷이 좋았어? 얼굴이 실망 하고 있어요」
		PRINTFORMW 「이것이 애태움플레이라고 말하는 것이겠지?   … 다, 다른 거야?」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMDW 무라사는%CALLNAME:MASTER%에 과시하도록(듯이), 천천히 양말을 벗어 던졌다…
		PRINTFORMW 「아라, 혹시 옷이 좋았을까?」
	;아라이먼트부
	ELSE
		PRINTFORMDW 무라사는%CALLNAME:MASTER%에 과시하도록(듯이), 천천히 양말을 벗어 던졌다…
		PRINTFORMW 「후훗, 나의 뜸든 다리의 냄새, 냄새 맡게 해 줄까요?」
		PRINTFORMW 「그렇지 않으면 빨아 봅니까? 어디라도%CALLNAME:MASTER%에는 포상이지요?」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·만p FIRSTDATUI(3)
;------------------------------------------------
;·이번 탈의로 성기가 보이게 되었을 경우에 표시
;·팬티 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && P_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	PRINTL
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:주인님 && FIRSTDATUI(3)
		PRINTFORMDW 무라사는 스스로의 속옷을 끌어 내리면,%CALLNAME:MASTER%에 비밀스런 장소를 과시했다.
		PRINTFORMW 「어떻습니까? 슬슬 하고 싶어지지 않습니까?」
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써 가슴이 보이고 있다
	;=================================================
	IF 구상기입체크(100) && B_DATUICHECK(0) == 1
		;퍼펙트 서로 사모함
		IF KFLAG:700
			PRINTFORMW 「그런데, 나머지는 양말 뿐입니다만…  네? 그것은 벗지 않아도 괜찮아?」
			PRINTFORMW 「전라에 양말은 정의? … 응, 잘 모릅니다만 그러한 것입니까?」
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「후후, 슬슬 아래도 보고 싶습니까? 지금 벗어요… 와」
			PRINTFORMW 「%CALLNAME:MASTER%가 솔직하게 해 준다면, 만지작거리거나 해도 되는 것이에요?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「후후, 금방 벗은 배어들어 속옷,%CALLNAME:MASTER%에 씌워 줄까」
			PRINTFORMW 「그러한 것 좋아하겠지요? 사양하지 않아도 괜찮은 것이에요?」
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
	
	;조건을 짜 두지 않으면 조교자의 페니스의 유무에 관련되지 않고 처리되어 버리므로 페니스 없음의 경우로 한정합니다@/L
	;=================================================
	;응 이렇게 p… 가 아닌 경우(조교자에게 페니스 없음)
	;=================================================
	IF 구상기입체크(100) && !PENIS(TARGET)
		IF KFLAG:700
			PRINTFORMDW 미나미츠는 재빠르고 스스로의 속옷을 벗어 던지면, 양손으로 빠끔히비밀스런 장소를 넓혔다.
			PRINTFORMW 「저기%CALLNAME:MASTER%?   나의 질내, 자주(잘) 보입니까?」
			PRINTFORMW 「가끔 씩은 위만 입은 채로, 라고 하는 것도 좋다고 생각하지 않아?」
			PRINTL
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「후후, 아래만 알몸이라고 하는 것도 흥분하지 않습니까?」
			PRINTFORMDW 무라사는%CALLNAME:MASTER%에 과시하도록(듯이), 천천히 속옷을 내려 갔다…
		;아라이먼트부
		ELSE
			PRINTFORMW 「후후, 금방 벗은 배어들어 속옷,%CALLNAME:MASTER%에 씌워 줄까」
			PRINTFORMW 「그러한 것 좋아하겠지요? 사양하지 않아도 괜찮은 것이에요?」
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·말하는 p FIRSTDATUI(4)
;------------------------------------------------
;·이번 탈의로 젖가슴이 보이게 되었을 경우에 표시
;·브라 보이고를 경유하지 않고 단번에 벗는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:자주탈의 && B_DATUICHECK(0) == 3 && !IS_NOWASSIACTNAME(, "탈의")
	PRINTL
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && TALENT:TARGET:주인님 && FIRSTDATUI(4)
		PRINTFORMW 「영차… 와  후후, 어떻습니까 나의 가슴?」
		PRINTFORMW 「%CALLNAME:MASTER%가 좋다면 손대어도 괜찮아요?」
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;벌써 성기가 보이고 있다
	;=================================================
	IF 구상기입체크(100) && P_DATUICHECK(0) == 1
		;퍼펙트 서로 사모함
		IF KFLAG:700
			PRINTFORMW 「에? 역시 젖가슴도 보고 싶어? 완전히 어쩔 수 없는 사람이군요…」
			PRINTFORMW 「그러면, 가슴도 충분히로서 받지 않습니다와?%CALLNAME:MASTER%♪」
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「후후, 아래만으로는 어딘지 부족합니까?」
			PRINTFORMW 「아라, 눈이 못박음이에요? 그렇게 가슴 보고 싶었던 것입니까?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「아래만으로는 아무래도 어딘지 부족한 것 같구나」
			PRINTFORMW 「아라아라, 눈이 못박음이군요? 그렇게 가슴을 보고 싶었을까?」
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORMW 「젖가슴을 보고 싶어? 좋아요, 네♪」
		PRINTFORMDW 미나미츠가 재빠르게 의복을 벗어 던지면, 크지는 않지만 형태의 좋은 유방이 모습을 나타냈다.
		PRINTFORMW 「%CALLNAME:MASTER%에만이에요? 이런 식으로 보이는 것은」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「후후, 슬슬 벗을까요?」
		PRINTFORMW 「아라, 눈이 못박음이에요? 그렇게 나의 가슴을 보고 싶었어?」
	;아라이먼트부
	ELSE
		PRINTFORMW 「후후, 슬슬 벗어 줄까요? 어쩔 수 없네요」
		PRINTFORMW 「아라아라, 그렇게 가슴을 보고 싶었을까?」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·팬티 보이고 FIRSTDATUI(5)
;------------------------------------------------
;·이번 탈의로 팬티(캐릭터에 의해 드로워즈·트렁크스)가 보이게 되었을 경우에 표시
;·하반신속옷이 없는 캐릭터는 쓰는 필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && P_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:주인님 && FIRSTDATUI(5)
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEDATUI(5)
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·브라 보이고 FIRSTDATUI(6)
;------------------------------------------------
;·이번 탈의로 브라(캐릭터에 의해 스포트브라·사라시)가 보이게 되었을 경우에 표시
;·상반신속옷이 없는 캐릭터는 쓰는 필요없음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && B_DATUICHECK(0) == 2 && !IS_NOWASSIACTNAME(, "탈의")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && TALENT:TARGET:주인님 && FIRSTDATUI(6)
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEDATUI(6)
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·전라에 FIRSTDATUI(7)·RANDDATUI(8) ～ (11)
;------------------------------------------------
;·「전라까지 벗었다」만이 아니고, 「지금 벗었으므로전라가 되었다」라고 하는 의미
;·「윗도리 밖에 남지 않은 상태로 윗도리를 벗었다」등도 포함된다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:자주탈의 && ZENRACHECK(0) && !IS_NOWASSIACTNAME(, "탈의")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·악세사리─마셔 FIRSTDATUI(8)
;------------------------------------------------
;·주로 양말. 그 밖에 장갑이나 소매 따위
;·벗긴 중에 접어들지 않는다! 되기 십상인 것으로 이것만은 부위별 텐프레를 준비
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 == 6
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(8)
		PRINTFORMW 「네, 이것도 벗어요」
		PRINTFORMDW 무라사는%CALLNAME:MASTER%의 양말에 손을 대면 단번에 벗겨 갔다.
		RETURN 1
	ENDIF
	
	;=================================================
	;전라가 되었다
	;=================================================
	IF 구상기입체크(100) && ZENRACHECK(1)
		;퍼펙트 서로 사모함
		IF KFLAG:700
			PRINTFORMW 「네, 이것도 벗고 벗으며♪」
			PRINTFORMDW 그렇게 말하면 미나미츠는%CALLNAME:MASTER%의 양말을 단번에 벗겨냈다.
			PRINTFORMW 「그런데… 어디에서 받아 버릴까? 저기,%CALLNAME:MASTER%?」
			RETURN 1
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「자, 이것도 벗어요～」
			PRINTFORMDW 몸에 걸치는 것이 없어졌다%CALLNAME:MASTER%를 봐 무라사는 수긍했다.
			PRINTFORMW 「이것으로%CALLNAME:MASTER%도 체념해 주는군요?」
		;아라이먼트부
		ELSE
			PRINTFORMW 「그런데, 이것으로%CALLNAME:MASTER%가 대고 있던 의복은 최후예요」
			PRINTFORMDW 완전하게전라가 되었다%CALLNAME:MASTER%를 봐 무라사는 웃음을 띄웠다.
			PRINTFORMW 「후후, 어디에서 꾸짖어 줄까요?   즐거움이에요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORMW 「아라, 상당히 더러워져 있네요%CALLNAME:MASTER%의 양말」
		PRINTFORMDW 그렇게 말하면, 미나미츠는%CALLNAME:MASTER%의 양말을 단번에 벗겨내 멀리 내던졌다.
		PRINTFORMW 「다음에 씻어 두기 때문에, 지금은, 말야?」
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「네, 이것도 벗어요」
		PRINTFORMDW 무라사는%CALLNAME:MASTER%의 양말에 손을 대면 단번에 벗겼다.
		PRINTFORMW 「아라, 혹시 옷을 벗겨지는 편이 좋았을까?」
	;아라이먼트부
	ELSE
		PRINTFORMW 「네, 이것도 벗어요」
		PRINTFORMDW 무라사는%CALLNAME:MASTER%의 양말에 손을 대면 단번에 벗겼다.
		PRINTFORMW 「옷이 아니라 안심했습니까?   후후, 메인은 뒤로 취해 두는 것이에요」
	ENDIF
	RETURN 1
ENDIF

;0. 60시대의 「저기 보여라」에 상당. 페니스의 유무로 테두리를 단락짓고 있습니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·고추 보여라 FIRSTDATUI(9)
;------------------------------------------------
;·이번 탈의로 남성기가 보이게 되었을 경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && PENIS(MASTER)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTDATUI(9)
		PRINTFORMW 「그런데, 슬슬%CALLNAME:MASTER%의 것을 보여 받을까요」
		PRINTFORMW 「이런, 조금 딱딱해지고 있네요?   … 좋은 것이에요?   좀 더 솔직해져도」
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORMW 「그럼,%CALLNAME:MASTER%의 자지 보여 주세요」
		PRINTFORMW 「이제(벌써) 준비만단이군요… 입으로 해? 가슴으로 해?   그렇지 않으면… 이제 들어가지고 싶어?」
		RETURN 1
	ENDIF
	
	;=================================================
	;호감도 1000이상으로 아라이먼트정
	;=================================================
	IF 구상기입체크(100) && AFFECTION("권애") && ALI(0)
		PRINTFORMW 「그런데,%CALLNAME:MASTER%의 자지의 모습은… 과」
		PRINTFORMW 「후훗, 커지고 있네요?   슬슬 만지작거려지고 싶지 않습니까?」
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「그런데, 또%CALLNAME:MASTER%의 것을 보여 받습니까」
		PRINTFORMW 「신체는 이렇게 정직한데… 아직 받아들여 받을 수 없습니까?」
	;아라이먼트부
	ELSE
		PRINTFORMW 「네, 자지 보여? … 아라아라, 이렇게 크게 해 버려」
		PRINTFORMW 「참고 싶다면 하고 있어 좋아요?   나는 마음대로 만지작거릴 뿐이니까」
	ENDIF
	RETURN 1
ENDIF

[SKIPSTART]

오리지날로 기술되어 있지 않은 개소는 comment out 해 둡니다@/L

;0. 60시대의 「저기 보여라」에 상당. 페니스의 유무로 테두리를 단락짓고 있습니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·그대로응 보여라 FIRSTDATUI(10)
;------------------------------------------------
;·이번 탈의로 여성기가 보이게 되었을 경우에 표시
;·팬티 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 3 && ! TALENT:MASTER:남자
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·젖가슴 보여라 FIRSTDATUI(11)
;------------------------------------------------
;·이번 탈의로 젖가슴이 보이게 되었을 경우에 표시
;·브라 보여라를 경유하지 않고 단번에 벗게 하는 경우 있어
;·조교 대상이 남자라고 표시하지 않기 때문에, 남자 한정 구상에서는 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 3 && ! TALENT:MASTER:남자
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·팬티 보여라 FIRSTDATUI(12)
;------------------------------------------------
;·이번 탈의로 하반신속옷이 보이게 되었을 경우에 표시
;·조교 대상이 하반신속옷의 없는 캐릭터 한정의 구상이면 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:강제탈의 && P_DATUICHECK(1) == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·브라 보여라 FIRSTDATUI(13)
;------------------------------------------------
;·이번 탈의로 브라 상반신속옷이 보이게 되었을 경우에 표시
;·조교 대상이 남자라고 표시하지 않기 때문에, 남자 한정 구상에서는 쓸 필요가 없다
;·조교 대상이 상반신속옷의 없는 캐릭터 한정의 구상이면 쓸 필요가 없다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:강제탈의 && B_DATUICHECK(1) == 2 && ! TALENT:MASTER:남자
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·전라에 FIRSTDATUI(14)
;------------------------------------------------
;·「전라까지 벗겼다」만이 아니고, 「지금 벗겼으므로전라가 되었다」라고 하는 의미
;·「윗도리 밖에 남지 않은 상태로 윗도리를 벗겼다」등도 포함된다
;·「젖가슴 보여라」～「팬티 보여라」를 썼을 경우는 기입할 필요는 없다(상층의 분기에 먹혀져 여기까지 떨어져 오지 않기 때문에)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:강제탈의 && ZENRACHECK(1)
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;자주탈의·뭔가 벗은 FIRSTDATUI(15)
;------------------------------------------------
;·자위·봉사계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:자주탈의 && ! IS_NOWASSIACTNAME(, "탈의")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;강제탈의·뭔가 벗긴 FIRSTDATUI(16)
;------------------------------------------------
;·애무계의 예비 동작이 되기 쉽다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:강제탈의
ENDIF
[SKIPEND]

;========================================================================
;추가 액션 구상
;========================================================================
;·조교자가 추가행동을 취했을 때에 범용적으로 표시
;·쓰는 경우, 액션 구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
;────────────────────────────────────
;EXTRA1, Ｃ애무
;────────────────────────────────────
;·추가로 애무 또는 자위를 실시한다
;·조교 대상으로 페니스가 없는 경우는 애무, 어느 경우는 자위
;-------------------------------------------------
@KOJO_ACT_EXTRA_K68_1
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_0, 애무한다
;-------------------------------------------------
;·조교 대상으로 페니스가 없는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ! PENIS(MASTER)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;EXTRA1_1, 자위 한다
;-------------------------------------------------
;·조교 대상으로 페니스가 있는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && PENIS(MASTER)
;ENDIF

;────────────────────────────────────
;EXTRA2, Ｖ애무
;────────────────────────────────────
;·추가로 바지 애무를 실시한다
;·조교 대상이 남자의 경우 발생하지 않는다
;·조교 대상이 처녀의 경우는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K68_2
;-------------------------------------------------
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;EXTRA3, Ａ애무
;────────────────────────────────────
;·추가로 애널애무를 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K68_3
;-------------------------------------------------
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;EXTRA4, Ｂ애무
;────────────────────────────────────
;·추가로 유두 고문을 실시한다
;-------------------------------------------------
@KOJO_ACT_EXTRA_K68_4
;-------------------------------------------------
;IF 구상 기입 체크(0)
;ENDIF

;────────────────────────────────────
;미약등사용구상
;────────────────────────────────────
;·조교자가 미약이나 로션을 사용했을 때에 범용적으로 표시
;·「액션 구상」으로 기입 체크를=2로 했을 경우는 표시되지 않는다
;·쓰는 경우, 액션 구상의 뒤로 이어 위화감이 없는 형태로 쓰여지는 것이 바람직함
;-------------------------------------------------
@KOJO_USE_K68
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션/미약동시 사용
;------------------------------------------------
;·주로 성교계 커맨드로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && GETBIT(TFLAG:미약등사용구상, 0) && GETBIT(TFLAG:미약등사용구상, 1)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;로션 사용
;------------------------------------------------
;·완구·성교계 커맨드등으로부터 발생한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && GETBIT(TFLAG:미약등사용구상, 0)
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;미약사용
;------------------------------------------------
;·애무계 커맨드등으로부터 발생한다
;·키스로 발생했을 경우는 입으로 옮김이 되기 (위해)때문에, 그 케이스는 키스의 액션 구상내에서 처리하는 편이 좋다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && GETBIT(TFLAG:미약등사용구상, 1)
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORMW 「네, 평소의 약  … 에? 이런 것 필요 없어?」
		PRINTFORMW 「그런 일 말하지 말고, 나도 사용하기 때문에 이것으로 함께 빠집시다?」
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「네이것, 기분 좋아지는 약 사용해 준다」
		PRINTFORMW 「수채 코로라든지 말하는 약사가 만든 곧 효과가 있는 약이야… 아, 벌써 효과가 있어 온듯 하네」
	;아라이먼트부
	ELSE
		PRINTFORMW 「네이것, 무엇 되어도 좋아지는 약 사용해 주는… 필요없어?」
		PRINTFORMW 「안돼, 이것으로 이성 날고 있다%CALLNAME:MASTER%를 보고 싶은거야」
	ENDIF
	RETURN 1
ENDIF