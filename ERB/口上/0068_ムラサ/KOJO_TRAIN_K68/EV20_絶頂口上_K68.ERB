;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	무라사 구상 조교 이벤트 구상 파일
;
;	턴중 이벤트, 절정 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV20, 절정 구상
;========================================================================
;·조교자나 조교 대상이 절정에 이를 때 표시
;· 각 절정은 개별의 호출을 가지지 않고, 모두 파생으로 처리된다
;-------------------------------------------------
;=================================================
;관련 변수…
;
;TFLAG:전회 질내사정, (1=V성교/2=V성교 봉사/3=V성교(하늘 쏘아 맞히고)/4=V성교 봉사(하늘 쏘아 맞히고)/5=A성교/6=A성교 봉사/7=A성교(하늘 쏘아 맞히고)/8=A성교 봉사(하늘 쏘아 맞히고))
;TFLAG:REACT 분류, (0=없음/1=소극적 따른다/2=적극적 따른다/3=허가를 청한다/4=날뛴다/5=요구에 응하지 않는, 거부/6=도망치는, 발버둥 친다)
;NOWEX:사정, (1=하늘 사정/2=사정/3=대량 사정/4=사정(조루)/5=A책사정)
;TEQUIP:자위중, (bit0=조교 대상/bit1=조교자)
;
;=================================================
;함수류…
;
;MASTER_EX
;인  수:ARG:0… 애무자→1. 조교 대상 자신/2. 조교자/4. 조수 1/8. 조수 2/16. 조수 3
;                      /64. 도구/128. 변소 이용자  생략 하면 애무자를 묻지 않는다
;              비트화에서의 지정이 가능
;       ARG:1…(생략가능) 절정의 종별→1. C절정/2. V절정/3. A절정/4. B절정
;                                  /11. 분유/12. 사정/13. 방뇨
;ARG:0(bit0 =조교 대상 자신 /bit1 =조교자 /bit2 =조수 1/bit3 =조수 2/bit4 =조수 3/bit6 =도구 /bit7 =변소 이용자)
;-------------------------------------------------
;TARGET_EX
;인  수:ARG:0… 애무자→1. 조교 대상/2. 조교자 자신/4. 조수 1/8. 조수 2/16. 조수 3
;              비트화에서의 지정이 가능. 생략 하면 애무자를 묻지 않는다
;       ARG:1…(생략가능) 절정의 종별→1. c절정/2. v절정/3. a절정/4. b절정
;                                  /11. 분유/12. 사정
;ARG:0(bit0 =조교 대상 /bit1 =조교자 자신 /bit2 =조수 1/bit3 =조수 2/bit4 =조수 3)
;-------------------------------------------------
@KOJO_EVENT_K68_20(ARG)
;-------------------------------------------------
;TFLAG:전회 질내사정;(1=V성교/2=V성교 봉사/3=V성교(하늘 쏘아 맞히고)/4=V성교 봉사(하늘 쏘아 맞히고)/5=A성교/6=A성교 봉사/7=A성교(하늘 쏘아 맞히고)/8=A성교 봉사(하늘 쏘아 맞히고))
;-------------------------------------------------

;─
;질내 사정(MASTER→TARGET)
;─
;·V성교 봉사계로 질내 사정하는 경우에 표시
;·TFLAG:106은 뽑지 않는 판정 마다+1된다
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;-------------------------------------------------

;TFLAG:전회 질내사정 == 4(하늘 쏘아 맞히고)는 연주해 둡니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━━
;질내 사정(조교 대상→조교자)
;━━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:전회질내사정 == 2
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		SELECTCASE RANDEVENT(3, 0)
			CASE 0
				PRINTFORMW 「아앗, 안쪽에, 안쪽에 내!!」
				PRINTL 
				PRINTFORMW 「후훗, 자궁의 안쪽까지 가득%CALLNAME:MASTER%의 정액 나와 버렸다…」
				PRINTFORMW 「유령의 나라도… 아기 만들 수 있을까나…?」
				PRINTFORMW 「%CALLNAME:MASTER%의 아기 갖고 싶구나…」
			CASE 1
				PRINTFORMW 「후앗, 질내에 나오고 있닷, 나오고 있다♪」
				PRINTFORMDW 수밀도는 넋을 잃은 표정으로, 자신의 하복부를 문질렀다
				PRINTFORMW 「이렇게 많이 내, 임신할지도 모릅니다? … 나는 상관없지만」
			CASE 2
				PRINTFORMW 「아앗, 나오고 있닷! 나의 질내에서, 쿨렁쿨렁 말하고 있는…!」
				PRINTFORMW 「뱃속…%CALLNAME:MASTER%의 정액으로 가득해♪」
		ENDSELECT
		RETURN 2
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「응읏, 뜨거운… 가득 냈어요」
		PRINTFORMW 「그렇게 나의 질내, 기분 좋았던 것입니까?」
	;아라이먼트부
	ELSE
		PRINTFORMW 「아라아라, 이렇게 질내에 내 버려」
		PRINTFORMW 「이전의 저항은 몰라볼 정도로 초췌하네요, 저기? 원숭이씨」
	ENDIF
	RETURN 2
ENDIF

;─
;애널내 사정(MASTER→TARGET)
;─
;·A성교 봉사계로 장내 사정하는 경우에 표시
;·TFLAG:106, 뽑지 않고는 뽑지 않는 판정 마다+1된다
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;애널내 사정(조교 대상→조교자)
;━━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && GROUPMATCH(TFLAG:전회 질내사정, 6, 8)
;ENDIF

;─
;질내 사정(TARGET→MASTER)
;─
;·V성교계로 질내 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;·현재의 사양에서는 조교자가 하늘 사정하는 것은 매우 희(거의 없어?)
;-------------------------------------------------

;;TFLAG:전회 질내사정 == 3(하늘 쏘아 맞히고)은 연주해 둡니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━━
;질내 사정(조교자→조교 대상)
;━━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:전회질내사정 == 1
	;=================================================
	;COM가 안에 내!
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("중에 내!")
		;퍼펙트 서로 사모함
		IF KFLAG:700
			PRINTFORMW 「이, 이제 한계, 나오는, 나와버린다…!」
			PRINTFORMW 「막아내!%CALLNAME:MASTER%중(안)에서 받아 들여!! 쿠아앗!!」
			PRINTFORMDW 수밀도는 마지막에 한층 더 깊게 허리를 찔러 넣으면,%CALLNAME:MASTER%의 자궁입구에 향해 대량의 정령을 토해냈다…
		ENDIF
		RETURN 2
	ENDIF
	
	;=================================================
	;COM가 안에 내지 마!
	;=================================================
	;IF 구상 기입 체크(0) && IS_NOWCOMNAME("안에 내지 마!")
	;ENDIF
RETURN 0
ENDIF

;─
;애널내 사정(TARGET→MASTER)
;─
;·A성교계로 장내 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;·현재의 사양에서는 조교자가 하늘 사정하는 것은 매우 희(거의 없어?)
;-------------------------------------------------
;IF 구상 기입 체크(0) && GROUPMATCH(TFLAG:전회 질내사정, 5, 7)
;ENDIF

;─
;동시 절정
;─
;·조교자와 조교 대상이 함께 절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(100) && (NOWEX:MASTER:C절정 || NOWEX:MASTER:V절정 || NOWEX:MASTER:A절정 || NOWEX:MASTER:B절정) && (NOWEX:C절정 || NOWEX:V절정 || NOWEX:A절정 || NOWEX:B절정)
	;=================================================
	;조교자가 사정
	;=================================================
	IF 구상기입체크(100) && NOWEX:사정
		;퍼펙트 서로 사모함
		IF KFLAG:700
			PRINTFORMW 「%CALLNAME:MASTER%도 가는, 의? 응, 함께 가자…」
			PRINTFORMW 「쿠앗, 이제(벌써), 나와버리는! 탓네 나무는 나와버린다! 아아앗!!」
			PRINTFORMDW %CALLNAME:MASTER%와 수밀도는 함께 끝까지 올라, 수밀도는%CALLNAME:MASTER%에 욕망의 정령을 토해냈다…
			RETURN 2
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「%CALLNAME:MASTER%도 가군요, 나도 함께… 쿠아앗!」
			PRINTFORMDW %CALLNAME:MASTER%와 무라사는 함께 달하면, 무라사는 힘차게 정령을 토해냈다.
			PRINTFORMDW 무라사가 발한 정액이%CALLNAME:MASTER%에 쏟아져, 신체를 희게 물들여 간다…
		;아라이먼트부
		ELSE
			PRINTFORMW 「…%CALLNAME:MASTER%도 가고 싶은거네? , 나… 나보다 먼저 가면 용서하지 않아요…!」
			PRINTFORMW 「가요!%CALLNAME:MASTER%에 내요, 응아앗!!」
			PRINTFORMDW %CALLNAME:MASTER%와 무라사는 함께 달하면, 무라사는 힘차게 정령을 토해냈다
			PRINTFORMDW 무라사가 낸 정액이%CALLNAME:MASTER%의 신체에 쏟아져, 신체를 희게 물들여 간다…
		ENDIF
		RETURN 2
	ENDIF
	
	;=================================================
	;조교 대상이 사정
	;=================================================
	IF 구상기입체크(100) && NOWEX:MASTER:사정
		;퍼펙트 서로 사모함
		IF KFLAG:700
			SELECTCASE RANDEVENT(3, 40)
				CASE 0
					PRINTFORMW 「앙돼에, 가 버린다! 가 버린다!%CALLNAME:MASTER%、%CALLNAME:MASTER%！！」
					PRINTL 
					PRINTFORMW 「후훗, 가득 나왔어요…  그렇게 기분 좋았어?」
					PRINTFORMW 「나도 기분 좋았어요…  응, 좀 더 합시다?」
					PRINTFORMDW 수밀도는 응석부리면서,%CALLNAME:MASTER%에 신체를 접대어 왔다…
				CASE 1
					PRINTFORMW 「%CALLNAME:MASTER%, 앙됏, 간닷! 가 버린다아…!!」
					PRINTFORMDW %CALLNAME:MASTER%와 수밀도는 함께 끝까지 오르면, 수밀도는 실이 끊어졌는지와 같이%CALLNAME:MASTER%에 기대었다…
					PRINTFORMW 「우, 응… 미안해요, 너무 기분 좋아…  후후, 정액 가득」
					PRINTFORMW 「%CALLNAME:MASTER%도 이렇게 낼 만큼 좋았던 것이군요, 기뻐요…♪」
				CASE 2
					PRINTFORMW 「아앗, 함께! 짚사람 함께 빠졋…!!」
					PRINTFORMDW 동시에 달했다%CALLNAME:MASTER%와 수밀도는, 쾌락의 여운을 즐기고 있었다…
					PRINTFORMW 「후훗… 동시에 끝까지 오르다니 무슨 멋진 것일까」
					PRINTFORMW 「저기, 다시 한번 하지 않아? 함께 기분 좋아지고 싶구나… 안돼?」
			ENDSELECT
			RETURN 2
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORMW 「%CALLNAME:MASTER%도 가군요, 나도 함께… 후아아!」
			PRINTFORMDW %CALLNAME:MASTER%와 무라사는 함께 달하면,%CALLNAME:MASTER%는 힘차게 정령을 토해냈다
			PRINTFORMDW %CALLNAME:MASTER%가 낸 정액이 무라사의 신체에 쏟아져, 무라사의 신체를 희게 물들여 간다…
		;아라이먼트부
		ELSE
			PRINTFORMW 「… 가고 싶은거네? , 나… 나도 가요! 아앗!!」
			PRINTFORMDW %CALLNAME:MASTER%와 무라사는 함께 달하면,%CALLNAME:MASTER%는 힘차게 정령을 토해냈다
			PRINTFORMDW %CALLNAME:MASTER%가 낸 정액이 무라사의 신체에 쏟아져, 무라사의 신체를 희게 물들여 간다…
		ENDIF
		RETURN 2
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORMW 「%CALLNAME:MASTER%! 함께, 함께 빠졋! 후아아…♪」
		PRINTFORMDW %CALLNAME:MASTER%와 수밀도는 함께 끝까지 오르면, 수밀도는 정신나간 얼굴을 해%CALLNAME:MASTER%에 기대었다…
		RETURN 2
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「후훗… 함께 빠져 버렸어요」
		PRINTFORMW 「어떻습니까? 다시 한번, 나와 함께 빠져 보지 않겠습니까?」
	;아라이먼트부
	ELSE
		PRINTFORMW 「후훗… 함께 빠져 버렸군요」
		PRINTFORMW 「자, 다시 한번 할까요. 아아,%CALLNAME:MASTER%의 의사 같은거 (듣)묻지 않으니까」
	ENDIF
	RETURN 2
ENDIF


;─
;조교 대상 사정
;─
;·조교 대상이 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 가감(상태)해 사용하는 것이 바람직함
;-------------------------------------------------
IF 구상기입체크(100) && NOWEX:MASTER:사정
	;퍼펙트 서로 사모함
	IF KFLAG:700
		PRINTFORMW 「후후가득 나왔군요, 그렇게 좋았어?」
		PRINTFORMW 「응…%CALLNAME:MASTER%의 정액 맛있어…♪」
		PRINTFORMDW 수밀도는 국자안에 나왔다%CALLNAME:MASTER%의 정액을 맛있을 것 같게 다 마셔 간다…
		RETURN 2
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「기분 좋았을까? 아직%CALLNAME:MASTER%의 자지, 움찔움찔 하고 있다…」
		PRINTFORMW 「좀 더 빠지게 해 주는 것을 좋아할 뿐(만큼) 사정해 주세요」
	;아라이먼트부
	ELSE
		PRINTFORMW 「이제(벌써),%CALLNAME:MASTER%의 더러운 국물로 신체가 더러워져 버렸어요」
		PRINTFORMW 「물론%CALLNAME:MASTER%가 빨아 예쁘게 해 주는거네요?」
	ENDIF
	RETURN 2
ENDIF

;─
;조교자 사정
;─
;·조교자가 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;·현재의 사양에서는 조교자가 하늘 사정하는 것은 매우 희(거의 없어?)
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:사정
;ENDIF

;─
;조교 대상 절정
;─
;·조교 대상이 절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && (NOWEX:MASTER:C절정 || NOWEX:MASTER:V절정 || NOWEX:MASTER:A절정 || NOWEX:MASTER:B절정)
;ENDIF

;─
;조교자 절정
;─
;·조교자가 절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(100) && (NOWEX:C절정 || NOWEX:V절정 || NOWEX:A절정 || NOWEX:B절정)
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		SELECTCASE RANDEVENT(3, 80)
			CASE 0
				PRINTFORMW 「하아앗, 앙됏, 이런… 히웃」
				PRINTFORMW 「짚사람! 풍부해는 끝낸다! 후아앗…!!」
			CASE 1
				PRINTFORMW 「(이)든지, 빠진닷! 빠져 버린다!」
				PRINTFORMW 「안아 합계, 부탁등으로부터! 들 삐걱거릴 수 있는, 응앗아아앗!!」
			CASE 2
				PRINTFORMW 「하앗, 째의 앞 반짝반짝 해, 아무것도 모르게 되어 버린다!」
				PRINTFORMW 「잇, 가! 히 가 버려! 아아아앗!!」
		ENDSELECT
		RETURN 2
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORMW 「후앗, 하앗, 앗, 쿠앗!」
		PRINTFORMW 「간닷, 갑니닷, 후아앗!」
	;아라이먼트부
	ELSE
		PRINTFORMW 「읏, 앗… 쿠앗, 아앗!」
		PRINTFORMW 「쿠…, 가요, 응아앗!」
	ENDIF
	RETURN 2
ENDIF

;─
;조교 대상 C절정
;─
;·조교 대상이 C절정 하는 경우에 표시
;·여기에서는 「여자 아이의 C절정」을 상정하고 있다
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:MASTER:C절정
;ENDIF

;─
;조교 대상 V절정
;─
;·조교 대상이 V절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:MASTER:V절정
;ENDIF

;─
;조교 대상 A절정
;─
;·조교 대상이 A절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:MASTER:A절정
;ENDIF

;─
;조교 대상 B절정
;─
;·조교 대상이 B절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:MASTER:B절정
;ENDIF

;─
;조교자 C절정
;─
;·조교자가 C절정 하는 경우에 표시
;·여기에서는 「여자 아이의 C절정」을 상정하고 있다
;·사정은 사정 구상의 분기에서 쓰는 것이 바람직함
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:C절정
;ENDIF

;─
;조교자 V절정
;─
;·조교자가 V절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:V절정
;ENDIF

;─
;조교자 A절정
;─
;·조교자가 A절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:A절정
;ENDIF

;─
;조교자 B절정
;─
;·조교자가 B절정 하는 경우에 표시
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:B절정
;ENDIF

;─
;조교 대상 물총
;─
;·조교 대상이 물총의 경우에 표시
;·현상, ACT손가락 삽입할 수 있는 때에 G스팟 고문에 파생했을 경우에게만 일어난다
;·주인님의 물총은 없다
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:MASTER:물총
;ENDIF

;─
;조교 대상 실금
;─
;·조교 대상이 실금 하는 경우에 표시
;·조교 대상으로 소질:소변버릇이 없으면 발생하지 않는다
;·C절정없이 실금만이라고 하는 것은 드물게 있는 케이스
;-------------------------------------------------
;IF 구상 기입 체크(0) && NOWEX:MASTER:방뇨
;ENDIF