;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;		무라사 구상 범용 리액션 구상 파일
;
;	개별 리액션(REACT)으로부터 빠져 온 것을 해당 COM로 처리하는 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;=====================================================================
;범용 리액션 구상
;=====================================================================
;·개별 리액션 구상을 표시하지 않았던 경우에 표시
;·KOJO_REACT_KX1_Y1의 RESULT가 0이었을 경우, 범용 리액션이 불려 간다
;·REACT로 표시한지 아닌지는 RESULT로 판정되므로, REACT로 표시해 RETURN 0으로 한층 더 범용도 표시시키는 일도 가능한 것이긴 함
;·KOJO_COM_KX1_Y1로부터 RESULT 0이 돌아가면 통괄 범용 리액션(KOJO_REACT_KX)이 불려 간다
;---------------------------------------------------------------------
;간이 플로우
;    표시 타이밍·조교 대상의 리액션(커맨드·반응)
;        액션 번호별의, 개별 리액션 구상
;            표시하지 않았던 경우, 커맨드 번호별의 범용 리액션 구상←(지금 여기)
;                표시하지 않았던 경우, 통괄 범용 리액션 구상
;---------------------------------------------------------------------
;관련 변수/함수류…
;
;ACT분류계
;TFLAG:ACT분류 이 턴의 조교 메뉴(0=회화/1=애무/2=도구/3=성교/4=수치/5=봉사/6=가학/7=이상/8=사역/9=휴게/10=성교 봉사)
;GET_ACTM ACT 번호로부터 ACT분류번호를 취득하는 함수
;MENUMATCH 지정한 ACT분류에 포함되어 있으면 진을 돌려준다(회화/애무/도구/성교/수치/봉사/가학/이상/사역/휴게/성적봉사)
;
;COM계
;TFLAG:REACT파생 각 SELECTCOM의 파생. COM마다 파생 내용은 다르다
;TFLAG:REACT부하 선택한 COM의 기력 소비를 판별한다(0=자연스러운 행동/2=무리가 없는 행동/무리를 한 행동)
;COMCOR_POSI 주로 소극적 따르는 계 COM의 기력 소비가 큰 때(TFLAG:REACT부하 == 2인 경우)에, COM 보정치가 호의적인가 어떤가를 판단
;
;그 외
;TFLAG:REACT인상 각 SELECTCOM의 상대에게로의 심상(0=영향없음/1=좋은인상/2=약간좋은인상/3=나쁜인상/4=약간나쁜인상/5=0에서멀어짐/6=0에가까워짐)
;TFLAG:벌포인트( 100모이면 벌개시)
;FIRSTCOM  첫회 범용 리액션 판정 함수  SELECTCOM를 자동으로 참조하므로, 현재 선택한 COM 마다 번호가 독립해 보존된다
;ONCECOM  조교내 첫회 판정용 함수. FIRSTTIME계와 달라 조교전에 초기화된다. 기본적인 사양은 FIRSTCOM와 같이
;RANDCOM(AUTORAND계)  RAND 처리로 이벤트 종류마다의 번호를 돌려주지만, 전종 볼 때까지 같은 숫자는 돌려주지 않는다. 모두 표시했을 때에 초기화.
;
;---------------------------------------------------------------------

;=====================================================================
;※ACT분류와 그 파생에 대해
;
;COM에 따라서는 발생하는 ACT분류가 복수에 걸칩니다
;각 COM내를 TFLAG:ACT분류로 나누어 주는 일에 의해, 보다 정확하게 상황을 참조하는 것이 가능합니다
;다만, ACT분류자체도 파생하는 것이 있어, 이 경우, TFLAG:ACT분류에서는 파생 상황을 참조 할 수 있지 못하고, 약간 이상한 일이 됩니다
;
;예를 들면…, ACT55, 다리빨고강제는 본래 ACT분류:봉사계입니다만, 이상계로부터 발생하는 경우가 있어,
;TFLAG:ACT분류의 단락에서는, 상정 그대로의 구상이 표시되지 않는다고 말하는 것이 생각됩니다
;
;거기서 TFLAG:ACT분류는 아니고, @MENUMATCH든지@GET_ACTM로 ACT분류 마다의 분기를 마련해 주면, 파생처를 참조하는 것이 가능하게 되어
;실제로 행해지고 있는 ACT분류로 처리되게 됩니다
;IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "봉사")와 같은 테두리를 마련해 주면, 위에 올린 것 같은 상황에도 대응이 가능합니다
;MENUMATCH의 분류 마다의 정의에 대해서는 이 파일의 28행째를 참조해 주세요
;
;ACT분류파생 일람…
;(각 수치는 ACT 번호)
;회화(기분 0/호의 1/지식 2/농락해라 3/위협한다 4/의상 5/합의 6/처분 7/고함친다 8/비웃는다 9/매도 66)
;애무(손으로 10/가슴을 11/입으로 12/A손 13/A구 14/키스 15/C찰 16/B찰 17/다리로 18/V손 19/로터 20/압도 27)
;도구(로터 20/바이 21/A바이 22/A비 23/C사랑 24/니풀 25/P대 26/관장 68)
;성교(정상 30/후배 31/승마 32/대면 33/배면 34/AS35)
;수치(자위 40/개장 41/보이고 42/대거울 43/야외 44/방뇨 45/코스(미실장)46/방치 73)
;봉사(손으로 50/입으로 51/가슴으로 52/가랑이 53/족지 55/이라 56/승마 32/가슴을 57)
;가학(손바닥 60/회초리 61/바늘 62/줄 63/눈가림 64/입마개 65/매도 66/목마 67/관장 68/이라 56)
;이상(Fist70/AFi71/양Fi72/방치 73/공중 74/P장 75/비웃는다 9/다리로 18/족지 55/목마 67)
;사역(미실장)
;휴게(휴일 90/복약 91/치료 92)
;성교(봉사) (정상 95/후배 96/승마 103/대면 97/배면 98/AS99)
;
;=====================================================================
[SKIPSTART]
MENUMATCH소분기 견본…

다른 분기와의 우선도에 주의해, 필요에 따라서 각 COM에 추가해 사용해 주세요
	;=================================================
	;ACT분류, 회화
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "회화")
	ENDIF
	;=================================================
	;ACT분류, 애무
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "애무")
	ENDIF
	;=================================================
	;ACT분류, 도구
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "도구")
	ENDIF
	;=================================================
	;ACT분류, 성교
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "성교")
	ENDIF
	;=================================================
	;ACT분류, 수치
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "수치")
	ENDIF
	;=================================================
	;ACT분류, 봉사
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "봉사")
	ENDIF
	;=================================================
	;ACT분류, 가학
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "가학")
	ENDIF
	;=================================================
	;ACT분류, 이상
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "이상")
	ENDIF
	;=================================================
	;ACT분류, 휴게
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "휴게")
	ENDIF
	;=================================================
	;ACT분류, 성적봉사
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "성적봉사")
	ENDIF
[SKIPEND]
;---------------------------------------------------------------------

;※각 분기에는 대응하는 지문을 제시되어 있습니다만, 최종적으로 어떠한 형태로 할까 아직 뭐라고도 말할 수 없는 곳

;=====================================================================
;회화계 반응 COM0 접수대
;=====================================================================
;·주로 ACT분류:회화계/휴게계 시에 발생한다
;·발생은 트리거 이벤트제. 특정의 ACT가 트리거가 된다
;·ACT분류:회화에 대해서는 REACT로 완결시키는 것이 바람직함
;-------------------------------------------------

;─
;COM0, 침묵한다
;─
;·따랐던 것에는 안 된다
;·ACT1, 지금의기분을얘기한다/자신에게로의기분을얘기한다의 경우는 REACT파생 1, "질문에 답하지 않는다"되어 벌포인트 가산
;·REACT인상:나쁜인상
;·의존도 변화(-1, -1)
;-------------------------------------------------
;·ACT가 지금의기분을얘기한다/자신에게로의기분을얘기한다의 경우는 파생 1, 질문에 답하지 않는 취급
;·그 외의 경우는 파생 0, 단순한 침묵
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류 0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류 2:도구
;	ACT:
;ACT분류 4:수치
;	ACT:수치플레이/야외플레이
;ACT분류 6:가학
;	ACT:줄/아이마스크/볼개그/매도/삼각목마
;ACT분류 7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류 9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;--------------------------------------------------
@KOJO_COM_K68_0
;-------------------------------------------------
;·TFLAG:REACT파생(0=단순한 침묵/1=질문에 답하지 않는다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTL
	;ACT, 지금의기분을얘기한다/자신에게로의기분을얘기한다의 경우에 파생한다
	;=================================================
	;REACT파생 1, 질문에 답하지 않는다
	;=================================================
	;IF 구상 기입 체크(0) && TFLAG:REACT파생 == 1
	;ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「%CALLNAME:MASTER%?」
		PRINTFORML 「미안해요, 뭔가 비위에 거슬린 것이라면 사과해요」
		PRINTFORMW 「그렇지만, 대답 정도 돌려주어 주어도 좋다고 생각해」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「응, 침묵입니까?」
		PRINTFORMW 「그렇게 입다물어지고 버리는 곳도 어째서 좋은가 헤맵니다만」
	;아라이먼트부
	ELSE
		PRINTFORML 「나와 이야기할 생각은 없다, 라고」
		PRINTFORMW 「즉 내가 마음대로 해도 좋은, 이라고 하는 일이군요?」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM1, 심약구응답한다
;─
;·REACT분류는 파생에 의해 변화
;·ACT("엄격하게위협한다/벌이라고선언한다/고함친다/밀어넘어뜨린다")로 일정 조건을 채우면 REACT파생 1, 애원에 파생
;·REACT인상:약간좋은인상
;·의존도 변화(0, +1)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다/의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;-------------------------------------------------
@KOJO_COM_K68_1
;-------------------------------------------------
;·TFLAG:REACT파생(0=심약구응답한다/1=애원 한다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 애원 한다(REACT분류:허가를청한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	;COM:허가를청한다를 호출
	CALL KOJO_COM_K68_7
	RETURN RESULT
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 심약구응답한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「무슨 일이야? 어딘가 컨디션에서도 나쁜 것일까…」
		PRINTFORMDW 수밀도는 걱정일 것 같은 얼굴로%CALLNAME:MASTER%의 일을 응시했다.
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「응, 대답해 주어 고마워요」
		PRINTFORMW 「다음은 조금만 더에서도, 적극적으로 되어 받을 수 있으면 기뻐요」
	;아라이먼트부
	ELSE
		PRINTFORML 「에? 뭔가 말했을까」
		PRINTFORMW 「자주(잘) 들리지 않았어요, 미안해요」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM2, 무뚝뚝하게응답한다
;─
;·REACT분류:없음
;·따랐던 것에는 안 된다
;·벌포인트가 가산된다
;·REACT인상:나쁜인상
;·의존도 변화(-2, -2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다/의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K68_2
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「%CALLNAME:MASTER%?」
		PRINTFORML 「미안해요, 나 뭔가 이상한 일을 말했을까?」
		PRINTFORMDW 수밀도는%CALLNAME:MASTER%의 안색을 물으면서, 침착하지 않은 모습으로 주위를 둘러보았다.
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「당신의 기분도 알지만」
		PRINTFORMW 「그렇지만, 그렇게 싫을 것 같은 얼굴 하지 않으면 좋아요」
	;아라이먼트부
	ELSE
		PRINTFORML 「흐음, 그러한 태도를 취하는 거네」
		PRINTFORMW 「어디까지 그 태도를 계속해 있을 수 있을까요」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM3, 붙임성좋게응답한다
;─
;·REACT분류:적극적 따른다
;·REACT인상:좋은인상
;·의존도 변화(+3, +1)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K68_3
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성좋게응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「응, 물론 나도%CALLNAME:MASTER%의 일을 아주 좋아해요!」
		PRINTFORMDW 수밀도는 그렇게 말하면, 밝게%CALLNAME:MASTER%에 향하여 미소지었다.
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「응, 받아들여 받을 수 있어 기뻐요」
		PRINTFORMDW 그렇게 말하면 무라사는%CALLNAME:MASTER%에 향하여 미소지었다.
	;아라이먼트부
	ELSE
		PRINTFORML 「그래, 기뻐요」
		PRINTFORMW 「좀 더, 내가 좋아하는 일을 해도 좋네요?」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM4, 강하게응답한다
;─
;·REACT분류:없음
;·따랐던 것에는 안 된다
;·ACT, 밀어넘어뜨린다의 경우는 REACT파생 1, "밀어넘어뜨리기에 굽히지 않는다"에 파생
;·REACT인상:나쁜인상
;·의존도 변화(-1, -3)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다/의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K68_4
;-------------------------------------------------
;·TFLAG:REACT파생(0=강하게 응답/1=밀어넘어뜨리기에 굽히지 않는다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게응답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTL
	;ACT, 밀어넘어뜨린다의 경우에게만 파생
	;=================================================
	;REACT파생 1, 밀어넘어뜨리기에 굽히지 않는다
	;=================================================
	;IF 구상 기입 체크(0) && TFLAG:REACT파생 == 1
		;%CALLNAME:MASTER%는 이제 그만두라고 강하게 말했다…
	;ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「에, 아, 미안해요%CALLNAME:MASTER%」
		PRINTFORMW 「그렇지만, 굳이 그렇게 강한 어조로 말하지 않아도」
		PRINTL 
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「응」
		PRINTFORMW 「거기까지 미움 받는 일은, 하고 있지 않았다고 생각합니다만 말이죠」
	;아라이먼트부
	ELSE
		PRINTFORML 「아라, 그렇습니까」
		PRINTFORMW 「%CALLNAME:MASTER%는 좀 더, 자신의 입장을 반성하는 것이 좋다고 생각해요?」
	ENDIF
	RETURN 1
ENDIF

;─
;COM5, 빈정거린다
;─
;·ACT, 지금의기분을얘기한다/자신에게로의기분을얘기한다의 경우는 REACT분류:거부
;·그 이외의 경우는 REACT분류:태연하게 따르는이 된다
;·벌포인트 가산
;·REACT인상:나쁜인상
;·의존도 변화(-3, -1)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/합의를요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:줄/아이마스크/볼개그/매도/삼각목마
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K68_5
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------

;REACT분류:거부가 되는 경우는 연주하도록(듯이)해 둡니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생 && !IS_COMGRONAME("거부")
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「그런 일 말하지 않으면 좋어요～」
		PRINTFORMW 「그렇지만 그렇게 말하면서도 해 주는 근처, 사랑스럽지요%CALLNAME:MASTER%는♪」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「응, 그 말투는 싫은 느낌이지만」
		PRINTFORMW 「따라주고 있는 것은 기뻐요?」
	;아라이먼트부
	ELSE
		PRINTFORML 「그렇게 말하면서도 따라서 있군요,%CALLNAME:MASTER%」
		PRINTFORMW 「나를 부추기고 있을까? 라고 하면 거기에 대답해 주는 것이 줄기군요」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM6, 좀더하드하게해
;─
;·REACT분류:받아들인다
;·조교자의 소질, BASE:초조해 등에 의해 REACT파생 1, 「이 철면피의 암퇘지놈!」(이)가 된다
;·REACT파생 1의 경우는 벌포인트 가산
;·REACT인상:0에서멀어짐
;·의존도 변화(0, +3)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/상냥하게위로한다
;		벌이라고선언한다
;ACT분류:1:애무
;	ACT:손으로애무/가슴애무/입으로애무/키스/성기를서로켠다/파이즈리한다
;ACT분류:2:도구
;	ACT:C애무도구/니플캡
;ACT분류:3:성교
;	ACT:정상위
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:스팽킹/줄/아이마스크/볼개그/삼각목마
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K68_6
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=이 철면피의 암퇘지놈!)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 「이 철면피의 암퇘지놈!」
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:REACT파생 == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 「흐음」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「응,%CALLNAME:MASTER%알았어요」
		PRINTFORMW 「어디까지나 깊고, 함께 빠져 갑시다?」
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「후후,%CALLNAME:MASTER%모양이나 구나에게 빠져 줄 마음이 생겼습니까」
		PRINTFORMW 「좋아요,%CALLNAME:MASTER%가 바랄 때까지, 어디까지나 빠지게 해 주어요」
	;아라이먼트부
	ELSE
		PRINTFORML 「아라,%CALLNAME:MASTER%의 입으로부터 그런 말이 나오다니」
		PRINTFORML 「격렬하게 하는 것은 별로 괜찮습니다만」
		PRINTFORMW 「어떻게 되어도 책임을 지지 않아요?%CALLNAME:MASTER%가 바란 일인 것이기 때문에」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM7, 허가를청한다
;─
;·REACT분류:허가를청한다. 파생했을 경우는 REACT분류:없음
;·조교자의 소질, BASE:이성, 아라이먼트에 의해 REACT파생 1, 「의견을 내다니 좋은 담력이군요!」(이)가 된다
;·턴방침이 이상의 경우는 파생 1이 되기 쉽다
;·파생 1의 경우는 벌포인트 가산
;·REACT인상:0에가까워짐
;·의존도 변화(0, +2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류:1:애무
;	ACT:애널애무/애널빨고/밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:애널바이브/애널비즈/페니스밴드
;ACT분류:3:성교
;	ACT:후배위/배면좌위/애널섹스
;ACT분류:4:수치
;	ACT:자위/비패개장/수치플레이/야외플레이/방뇨
;ACT분류:5:봉사
;	ACT:다리빨고강제
;ACT분류:6:가학
;	ACT:스팽킹/채찍/바늘/삼각목마/관장세트
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착/피스트퍽/애널피스트/양구멍피스트/공중육변기플레이
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:역강간
;
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K68_7
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=의견을 내다니 좋은 담력이군요!)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 의견을 낸다니 좋은 담력이군요!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「%CALLNAME:MASTER%의 바봇!」
		PRINTFORML 「내가 좋아해 이런 일 하고 싶다고라도 생각하고 있을까!?」
		PRINTFORMW 「사과할 정도라면, 이상한 태도 취하지 않으면 좋은거야!!」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「나도 나빴다고 생각합니다만도」
		PRINTFORMW 「%CALLNAME:MASTER%의 태도, 눈에 거슬려요 과연」
	;아라이먼트부
	ELSE
		PRINTFORML 「몇번도 이런 일은 통하지 않는다고 했군요?」
		PRINTFORML 「그런데, 어떻게 해 줄까」
		PRINTFORMDW 그렇게 말하면서 무라사는%CALLNAME:MASTER%에 향해, 싫은 것 같은 미소를 띄우고 있었다.
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 흐음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「미안해요, 덩달아 분위기 타기가 지났어요」
		PRINTFORMW 「이제 하지 않기 때문에 그런 얼굴 하지 말아줘%CALLNAME:MASTER%,… 저기?」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「응, 나도 나빴다고 생각하고 허락해 준다」
		PRINTFORMW 「그렇지만, 좀 더%CALLNAME:MASTER%도 받아들여 주면 기쁘구나?」
	;아라이먼트부
	ELSE
		PRINTFORML 「흐음, 뭐 이번은 허락해 주어요」
		PRINTFORMW 「그렇지만, 그렇게 몇번도 통한다고는 생각하지 않는 것이군요」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM8, 기분좋게해
;─
;·REACT분류:받아들인다
;·조교자의 소질, BASE:이성, 아라이먼트에 의해 REACT파생 1, 「이 철면피의 암퇘지놈!」(이)가 된다
;·파생 1의 경우는 벌포인트 가산
;·REACT인상:0에서멀어짐
;·의존도 변화(0, +2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다/합의를요구한다
;ACT분류:1:애무
;	ACT:애널애무/애널빨고/밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:페니스밴드
;ACT분류:3:성교
;	ACT:
;ACT분류:4:수치
;	ACT:자위과시하고/수치플레이/야외플레이/방뇨
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;
;TEQUIP:볼개그는 튕겨진다
;-------------------------------------------------
@KOJO_COM_K68_8
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=이 철면피의 암퇘지놈!)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 이 철면피의 암퇘지놈!
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:REACT파생 == 1
;ENDIF

;파생 1의 경우 위화감이 나오므로 파생 0 성과째 쳐 합니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 흐음
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「예물론,%CALLNAME:MASTER%의 부탁인거야」
		PRINTFORMW 「어디까지나 기분좋게해 주는… 이니까%CALLNAME:MASTER%도 나의 일을 사랑해?」
		PRINTL 
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「저, 정말로? 나에게 그렇게 말해 줄래?」
		PRINTFORML 「알았어요, 충분히 빠지게 해 준다」
		PRINTFORMDW 무라사는 매우 기쁜듯이 준비를 시작했다…
	;아라이먼트부
	ELSE
		PRINTFORML 「아라, 드디어 졸라댈 마음이 생겼습니까」
		PRINTFORMW 「어떤 일을 하면 제일 효과적인 것이군요,%CALLNAME:MASTER%에는」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;=====================================================================
;쾌락계 반응
;=====================================================================
;·쾌SOURCE나 특정의 PALAM가 트리거가 되어 발생한다
;·COM에 따라서는 ACT분류가 트리거가 된다
;-------------------------------------------------

;─
;COM10, 부끄러워한다
;─
;·REACT분류:소극적 따른다
;·벌포인트 가산
;·쾌SOURCE가 일정 이상의 경우는 파생 1, 쾌감에 부끄러워하는이 된다
;·의존도 변화(+1, 0)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:성에대한이야기를한다/의상의변경을요구한다
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:수치
;	ACT:자위/비패개장/자위과시하고/수치플레이/야외플레이/방뇨
;ACT분류:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/가슴애무강제
;ACT분류:이상
;	ACT:방치플레이
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;※SOURCE:쾌C/쾌V/쾌A/쾌B, PALAM:치정이 트리거가 된다
;-------------------------------------------------
@KOJO_COM_K68_10
;-------------------------------------------------
;·TFLAG:REACT파생(0=상황에 부끄러워한다/1=쾌감에 부끄러워한다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 쾌감에 부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「무슨 일이야, 그렇게 부끄러워하다니」
		PRINTFORMW 「지금의 나라면 어떤%CALLNAME:MASTER%도 받아들일 수 있는, 그러니까 안심해」
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「기분 좋아지고 있는 곳을 보이는 것이 싫은 것입니까?」
		PRINTFORMW 「나는 그것을 보고 싶기 때문에, 그렇게 신경쓸 필요도 없는데」
	;아라이먼트부
	ELSE
		PRINTFORML 「도대체 무엇을 부끄러워하고 있을까?」
		PRINTFORMW 「여기에는 나와%CALLNAME:MASTER%, 두 명밖에 없다고 말하는데」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 상황에 부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「그렇게 부끄러워한다니 뭔가 있었을까?」
		PRINTFORMW 「나와%CALLNAME:MASTER%의 사이에서, 이제 와서라고 생각하는 것이지만」
		PRINTL 
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「수줍음이 있는 것은 좋은 일이라고 생각합니다만」
		PRINTFORMW 「여기에 있는 것은 우리들만이기 때문에, 신경쓸 필요 없다고 생각하지 않습니까?」
	;아라이먼트부
	ELSE
		PRINTFORML 「도대체 무엇을 그렇게 신경쓰고 있을까」
		PRINTFORMW 「자신의 신체에 그렇게도 자신이 없는거야?」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM11, 싫어한다
;─
;·REACT분류:소극적 따른다
;·REACT파생 1는 ACT 방해가 되어 REACT분류:날뛰는 취급
;·의존도 변화(-1, -1)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:의상의변경을요구한다
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/풋잡
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;
;※SOURCE:쾌C/쾌V/쾌A/쾌B, PALAM:치정이 트리거가 된다
;-------------------------------------------------
@KOJO_COM_K68_11
;-------------------------------------------------
;·TFLAG:REACT파생(0=싫어한다/1=싫어해 ACT를 방해)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 싫어해 방해한다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:REACT파생 == 1
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 싫어한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「싫어 해, 그런…」
		PRINTFORML 「그렇게 나,%CALLNAME:MASTER%가 싫어하는 일 하고 있을까나」
		PRINTFORMDW 수밀도는 슬픈 듯한 얼굴로%CALLNAME:MASTER%를 응시해 그렇게 작게 중얼거렸다.
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「그래, 이것은 싫은거네?」
		PRINTFORML 「가능한 한 싫어하는 일은 하고 싶지 않지만,
		PRINTFORMW   좀 더%CALLNAME:MASTER%로부터도 다가갔으면 좋구나」
	;아라이먼트부
	ELSE
		PRINTFORML 「그래, 이것은 싫은거네?」
		PRINTFORMW 「벌 해 받고 싶어서 싫어하는 척 하고 있는 것은 아닌거야?%CALLNAME:MASTER%」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM12, 서투르다고매도한다
;─
;·REACT분류:태연하게 따른다
;·REACT파생 1(강한척 한다)는 REACT분류:소극적 따르는 취급
;·벌포인트 가산
;·파생 0, 새겨 내린다(REACT인상:약간나쁜인상)
;·파생 1, 강한척 한다(REACT인상:나쁜인상)
;·의존도 변화(-2, -3)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:성교
;	ACT:정상위/후배위/대면좌위/배면좌위
;
;※TFLAG:ACT분류(1=애무/3=성교)가 트리거
;-------------------------------------------------
@KOJO_COM_K68_12
;-------------------------------------------------
;·TFLAG:REACT파생(0=새겨 내린다/1=강한척 한다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 강한척 해(쾌감을 느끼면서)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「어째서%CALLNAME:MASTER%는 그런 일을 말하는 거야?」
		PRINTFORML 「이봐요, 여기 이렇게 해 기분 좋은 것이 아니야,
		PRINTFORMW   완전히 심술궂은 것이니까%CALLNAME:MASTER%는」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「서투르다는 것은, 그렇게 말해져도…」
		PRINTFORMW 「여기를 이렇게 하면서로는, 설득력의 조각도 없어요?」
	;아라이먼트부
	ELSE
		PRINTFORML 「몹시 서투릅니까, 그렇습니까」
		PRINTFORML 「그럼 (듣)묻습니다만, 그 몹시 서투르게 만지작거려져,
		PRINTFORMW   여기를 이렇게 하고 있다%CALLNAME:MASTER%는 도대체 무엇인 것일까요?」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 새겨 내린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「무─, 확실히 서투른 것은 인정하지만」
		PRINTFORMW 「좀 더 상냥하게 말해 주어도 좋다고 생각하지 않아? … 울어 버려요 나」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「미안해요, 나도 그만큼 경험 없는 것이니까」
		PRINTFORMW 「그렇지만%CALLNAME:MASTER%도, 좀 더 말투를 생각하는 것이 좋아요?」
	;아라이먼트부
	ELSE
		PRINTFORML 「그래, 그런 일로 도발해 온다니 사랑스럽네요」
		PRINTFORMW 「소망 대로 괴롭혀 돌려주어 드릴까요, 그것이 목적이군요?」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM13, 겁없게웃는다
;─
;·REACT분류:태연하게 따른다
;·벌포인트 가산
;·REACT인상:나쁜인상
;·의존도 변화(-2, -3)
;-------------------------------------------------
;·트리거가 되는 행동, 소스, 파라미터
;·SOURCE:쾌C/쾌V/쾌A/쾌B, SOURCE:아픔/구속, ACT분류:가학
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그/매도/삼각목마/관장세트
;-------------------------------------------------
@KOJO_COM_K68_13
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「조금―%CALLNAME:MASTER%, 무엇입니까 그것」
		PRINTFORML 「조금도 재미있지 않으니까 그만두어 주세요… 이제(벌써)」
		PRINTFORMDW 수밀도는%CALLNAME:MASTER%에 대해 불만을 토로하고 있다…
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「응, 여유가 있는 곳을 보여 주고 싶은 것인지도 모릅니다만」
		PRINTFORMW 「결국의 곳은 따르니까, 좀 더 솔직해져 봐서는 어떻습니까?」
	;아라이먼트부
	ELSE
		PRINTFORML 「후후, 반항하는 모습을 하는 것은 괜찮습니다만」
		PRINTFORMW 「끝까지 관철하지 못하고에 따르는 것은 단순한 젊은이에게 밖에 보이지 않아요?」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM14, 쾌감을참는다
;─
;·REACT분류:소극적 따른다
;·일정 조건으로 REACT파생 1 「애원(REACT분류:허가를청한다)」취급
;·파생 1의 경우 REACT인상는 0에가까워짐
;·의존도 변화(+1, -1)
;-------------------------------------------------
;※트리거가 되는 행동, 소스, 파라미터
;·SOURCE:쾌C/쾌V/쾌A/쾌B
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/가랑이
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트
;-------------------------------------------------
@KOJO_COM_K68_14
;-------------------------------------------------
;·TFLAG:REACT파생(0=내구/1=애원)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 애원 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「어째서 그런 일 말하는 거야?」
		PRINTFORML 「오늘은 나와 엣치한 일 하는 것 같은 기분이 아닌거야?」
		PRINTFORMDW 수밀도는%CALLNAME:MASTER%에 대해 불만을 토로하고 있다…
		PRINTL 
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「응, 그렇게 나와 기분 좋아지는 것이 싫습니까?」
		PRINTFORMW 「어떻게 하면 받아들여 받을 수 있겠지요」
	;아라이먼트부
	ELSE
		PRINTFORML 「아라, 허세 부려 버려」
		PRINTFORML 「받아들이면 편해지는데 고집이 있네요」
		PRINTFORMW 「뭐, 얼마나 계속해 있을 수 있는지 즐거움으로 해 짊어진다」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 내구
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「저기, 어째서 인내 같은거 하고 있는 거야?」
		PRINTFORML 「빨리 나와 함께 기분 좋아집시다, 군요?」
		PRINTFORMDW 수밀도는%CALLNAME:MASTER%에 껴안아 응석부려 왔다…
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「인내 같은거 해도 신체에 나쁜 것뿐?」
		PRINTFORML 「차린 밥상 먹지 않으면 뭐라고든지라고 하지만」
		PRINTFORMW 「나는 차린 밥상도 안 될 수록 매력이 없는 것일까」
	;아라이먼트부
	ELSE
		PRINTFORML 「받아들인 (분)편이 편해질 수 있고, 어떤 제약도 없다고 생각하는 것이지만?」
		PRINTFORMW 「%CALLNAME:MASTER%를 거기까지 휘모는 것은 무엇인 것이군요」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM15, 쾌감을받아들인다
;─
;·COM16, 기분좋다！와 거의 같음. 차이는 의존도의 변화만
;·조교 대상이 의존하기 쉽다
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도 변화(+1, +5)
;-------------------------------------------------
;※트리거가 되는 행동, 소스, 파라미터
;·SOURCE:쾌C/쾌V/쾌A/쾌B
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/가랑이
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;-------------------------------------------------
@KOJO_COM_K68_15
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;일행 비운다
	PRINTL
	;0. 60시대와 성교, 성적봉사의 사양이 다르므로 오리지날의 분기 구조를 만지작거렸습니다@/L
	;=================================================
	;성교중의 경우
	;=================================================
	IF 구상기입체크(100) && MENUMATCH(TFLAG:ACT, "성교")
		;아라이먼트정
		IF ALI(0)
			PRINTFORML 「후훗, 이런 일을 해도 받아들여 주는 거네」
			PRINTFORML 「%CALLNAME:MASTER%는 움직이지 않아도 괜찮아요, 내가 빠지게 해 주기 때문에♪」
			PRINTFORMDW 무라사의 허리의 움직임에%CALLNAME:MASTER%의 것은 고조를 더해 온다…
		;아라이먼트부
		ELSE
			PRINTFORML 「아라, 무리하게 되고 있는데 기분 좋은 것 같구나」
			PRINTFORMW 「처음부터 솔직해지고 있으면 좋은데, 정말 바보 같은 사람이네」
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(100) && MENUMATCH(TFLAG:ACT, "성적봉사")
		;-------------------------------------------------
		;퍼펙트 서로 사모함
		;-------------------------------------------------
		IF KFLAG:700
			;역강간의 경우
			IF IS_NOWACTNAME("역강간") && TEQUIP:성교봉사중 == 3
				PRINTFORML 「우읏, 기분 좋은거네%CALLNAME:MASTER%…」
				PRINTFORML 「함께, 함께 가자%CALLNAME:MASTER%, 하앙…♪」
				PRINTFORMDW 수밀도는%CALLNAME:MASTER%의 페니스를 근원까지 물어 떼어 놓으려고 하지 않는다…
			;그 외
			ELSE
				PRINTFORML 「하아…, 응, 기분이 좋아…♪」
				PRINTFORML 「좀 더%CALLNAME:MASTER%가 좋아하는 것 같게 움직여도 좋아,
				PRINTFORML   나도 가능한 한 기분 좋게 하기 때문에」
				PRINTFORMDL %CALLNAME:MASTER%는 수긍하면 수밀도의 허리를 잡아,
				PRINTFORMDW 더욱 움직임을 빨리 해 갔다…
			ENDIF
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORML 「응… 좋아,%CALLNAME:MASTER%」
			PRINTFORML 「나도 움직이기 때문에, 함께 기분 좋아지자?」
			PRINTFORMDW %CALLNAME:MASTER%의 움직임에 맞추어, 무라사도 허리를 움직이기 시작했다…
		;아라이먼트부
		ELSE
			PRINTFORML 「나의 일을 무시해 무엇 마음대로 기분 좋아지고 있는거야」
			PRINTFORML 「만났을 무렵이라면 그런데도 만족이었다고 생각하지만, 이제 와서는
			PRINTFORML   공연히 화가 나요」
			PRINTFORMW 「이봐요, 좀 더 나도 기분 좋게 하세요, 이 원숭이씨」
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
	
	;이하, 성교/성적봉사 이외를 처리
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「%CALLNAME:MASTER%、%CALLNAME:MASTER%、%CALLNAME:MASTER%♪」
		PRINTFORMW 「좀 더 느껴, 좀 더 나와 함께 빠져…」
		PRINTL 
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「응, 겨우 솔직해져 준 거네」
		PRINTFORMW 「이 쾌락을 잊는 것이 할 수 없게 되는 위, 빠지게 해 주네요」
	;아라이먼트부
	ELSE
		PRINTFORML 「드디어 솔직해진 거네」
		PRINTFORMW 「후후, 나의 주는 쾌감 없이는 있을 수 없게 해 준다」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM16, 기분좋다！
;─
;·COM15, 쾌감을받아들인다와와 거의 같음. 차이는 의존도의 변화만
;·주인님의 의존을 권하기 쉽다
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도 변화(+4, +1)
;-------------------------------------------------
;※트리거가 되는 행동, 소스, 파라미터
;·SOURCE:쾌C/쾌V/쾌A/쾌B
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/가랑이
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;-------------------------------------------------
@KOJO_COM_K68_16
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------

;0. 60시대에는 존재하고 있지 않았던 커맨드
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ! TFLAG:REACT파생
;ENDIF

;=====================================================================
;성교계 반응 COM20 접수대
;=====================================================================
;·ACT분류:성교/성적봉사, ACT:파이즈리한다/성기를서로켠다 등이 트리거가 되어 발생
;-------------------------------------------------

;─
;COM20, 허리를흔드는
;─
;·주도권(SEX_INITIATIVE)에 의해 내용이 다른 COM
;-------------------------------------------------
;·ACT분류가 성교 봉사, ACT:승마위의 경우는 주도권은 조교 대상(SEX_INITIATIVE(MASTER))
;·REACT분류:적극적 따른다
;·의존도 변화(+5, +3)
;-------------------------------------------------
;·ACT분류가 성교, ACT:역강간/성기를서로켠다/파이즈리한다의 경우는 주도권은 조교자
;·※성기를서로켠다/파이즈리한다는 SEX_INITIATIVE(TARGET)에서는 참조 할 수 없다
;·REACT분류:받아들인다
;·의존도 변화(+4, +8)
;-------------------------------------------------
;·REACT인상:좋은인상
;-------------------------------------------------
;·초조하게 해 안은 실행 불가
;·ACT:파이즈리한다의 경우, 조교자가 빈유라고 튕겨진다
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:성기를서로켠다/파이즈리한다
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;-------------------------------------------------
@KOJO_COM_K68_20
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;주도권은 조교자
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && SEX_INITIATIVE(TARGET)
	PRINTL
	;역강간시의 주도권은 조교자가 되고 있습니다@/L
	;=================================================
	;퍼펙트 서로 사모함 또한 ACT가 역강간의 경우
	;=================================================
	IF 구상기입체크(100) && KFLAG:700 && IS_NOWACTNAME("역강간") && TEQUIP:성교봉사중 == 3
		PRINTFORML 「아앗, 좋아해요,%CALLNAME:MASTER%、%CALLNAME:MASTER%…!」
		PRINTFORML 「하앗, 좀 더, 좀 더 갖고 싶은 것,%CALLNAME:MASTER%를 갖고 싶은거야!!」
		PRINTFORMDL 수밀도는%CALLNAME:MASTER%의 격렬한 허리의 움직임에 맞추면서,
		PRINTFORMDW %CALLNAME:MASTER%의 모두를 짜내는것 같이 질을 단단히 조여 온다…
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류가 성교
	;=================================================
	IF 구상기입체크(100) && MENUMATCH(TFLAG:ACT, "성교")
		;아라이먼트정
		IF ALI(0)
			PRINTFORML 「어때? 기분, 좋을까…?」
			PRINTFORML 「기분이 좋은거네? 후훗, 좀 더 자주(잘) 해 주니까요」
			PRINTFORMDW 무라사는%CALLNAME:MASTER%의 허리의 움직임에 맞추고 페니스를 졸라 간다…
		;아라이먼트부
		ELSE
			PRINTFORML 「이렇게 열심히 허리를 흔들어」
			PRINTFORMW 「몇시부터%CALLNAME:MASTER%는 습격당하고 느끼는 변태가 된 것일, 까요…?」
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
ENDIF

;오리지날이 상정되어 있는 것은 성교 봉사인 것으로 MENUMATCH 성과째 쳐 합니다
;SEX_INITIATIVE(MASTER)만으로 참조했을 경우는 ACT:승마위등도 걸리므로@/L
;━━━━━━━━━━━━━━━━━━━━━━━━
;주도권은 조교 대상
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && SEX_INITIATIVE(MASTER) && MENUMATCH(TFLAG:ACT, "성적봉사")
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「후앗, 햣, 하웃, 후응…♪」
		PRINTFORML 「%CALLNAME:MASTER%, 좀 더 해, 안쪽까지 찔럿」
		PRINTFORMDW 수밀도는%CALLNAME:MASTER%의 격렬한 허리의 움직임에, 완전하게 몸을 바치고 있다…
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「좋아, 이대로 나의 질내에서 기분 좋아져…!」
		PRINTFORMDL 무라사는%CALLNAME:MASTER%의 허리의 움직임에 맞추어,
		PRINTFORMDW 스스로도 허리를 거절해%CALLNAME:MASTER%의 페니스를 짜냈다…
	;아라이먼트부
	ELSE
		PRINTFORML 「%CALLNAME:MASTER%로부터 나를 범하다니」
		PRINTFORML 「결국 쾌락에는 저항할 수 없었던 것이군요, 무슨 어중간한 노예인 것일까」
		PRINTFORMDL %CALLNAME:MASTER%에 관철해지면서, 그렇게 업신여김의 소리를 높이는 무라사
		PRINTFORMDW 모멸이 깃들인 시선을 느끼면서,%CALLNAME:MASTER%는 미친 것처럼 허리를 계속 흔들었다
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM21, 안에내！
;─
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·조교 대상이 남자의 경우는 튕겨진다
;·의존도 변화(+10, +10)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;-------------------------------------------------
@KOJO_COM_K68_21
;-------------------------------------------------
;·TFLAG:REACT파생(0=V에 내/1=A에 내)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 안에내！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTL
	;=================================================
	;조교자가갈것같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:갈것같다
		;-------------------------------------------------
		;퍼펙트 서로 사모함
		;-------------------------------------------------
		IF KFLAG:700
			PRINTFORML 「응, 알았다… 욧」
			PRINTFORML 「흘러넘칠 정도로,%CALLNAME:MASTER%의 질내에 내 주기 때문에!!」
			PRINTFORMDW 수밀도는%CALLNAME:MASTER%의 허리를 잡아, 사정에 향해 격렬하게 삽입질을 시작했다…
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORML 「좋은거야?%CALLNAME:MASTER%의 질내에 내도 괜찮구나!?」
			PRINTFORMDW 그 소리에%CALLNAME:MASTER%가 수긍하면, 무라사는 허리의 움직임을 빨리 해 갔다…
		;아라이먼트부
		ELSE
			PRINTFORML 「나의 정액을 갖고 싶어?   후후,%CALLNAME:MASTER%도 거기까지 저속해졌습니까」
			PRINTFORML 「그러면, 소망 대로 충분히 내 임신하게 해 주어요…!」
			PRINTFORMDL 무라사는 모멸이 깃들인 시선을%CALLNAME:MASTER%에 향하면서,
			PRINTFORMDW 방출에 향해 격렬하게 허리를 거절해 세웠다…
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「미안해요…, 아직 좀 더, 걸리는 것 같다」
		PRINTFORML 「절대로, 내 주기 때문에, 좀 더 기다리고 있어…!」
		PRINTFORMDW 그렇게 말하면서 수밀도는, 가능한 한 빨리 사정 할 수 있도록(듯이)와 허리의 움직임을 빨리 해 간다…
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「미안해요, 좀 더 걸릴 것 같다…」
		PRINTFORMW 「그렇지만%CALLNAME:MASTER%로부터 그렇게 말해 받을 수 있다니 기뻐요…♪」
	;아라이먼트부
	ELSE
		PRINTFORML 「그렇게 조르지 않아 받을 수 있습니까? 이쪽에도 형편이 있는 거에요」
		PRINTFORMW 「완전히… 자신으로부터 그런 일을 말하다니 터무니 없는 매춘이 된 것이군요%CALLNAME:MASTER%도」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM22, 안에내지마！
;─
;·REACT분류:허가를청한다
;·REACT파생 1(안에 낸다)의 경우는 벌포인트 추가. REACT분류:없음
;·REACT인상:나쁜인상
;·의존도 변화(0, -10)
;·인내 플래그는 소멸한다
;·조교 대상이 남자의 경우는 튕겨진다
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;-------------------------------------------------
@KOJO_COM_K68_22
;-------------------------------------------------
;·TFLAG:REACT파생(0=안에 내지 않는다/1=안에 낸다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 안에 낸다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1 && !TALENT:MASTER:남자
	PRINTL
	;=================================================
	;조교자가갈것같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:갈것같다
		;-------------------------------------------------
		;퍼펙트 서로 사모함
		;-------------------------------------------------
		IF KFLAG:700
			PRINTFORML 「미안, 미안해요!%CALLNAME:MASTER%!」
			PRINTFORML 「이제 나 멈추지 않는 것, 마음대로 허리가 움직여 버려!!」
			PRINTFORML 「… 아기 생겨 버리면 내가 책임 취하기 때문에! 그러니까!」
			PRINTFORMDL 수밀도는%CALLNAME:MASTER%의 허리를 잡아,
			PRINTFORMDW 스스로의 허리를 강압하고 전혀 떨어지려고 하지 않는다…
			PRINTL
			RETURN 1
		ENDIF
		
		IF ALI(0)
			PRINTFORML 「미안해요! 이제(벌써) 나, 멈추지 않는거야!」
			PRINTFORML 「미안해요! 미안해요!」
			PRINTFORMDW 그렇게 사죄의 말을 흘리면서, 무라사는%CALLNAME:MASTER%의 질내에서 기세를 더해 간다…
		;아라이먼트부
		ELSE
			PRINTFORML 「응─,… 그렇구나」
			PRINTFORML 「응 결정했다. 이번은%CALLNAME:MASTER%의 질내에서 내요」
			PRINTFORML 「좋았지요, 언제 되는지 무서워하는 시간도 이것으로 끝이야?」
			PRINTFORMW 「이봐요, 충분히 내 주기 때문에 임신하세요」
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;좀 더
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「그렇네요, 아기 생기면%CALLNAME:MASTER%가 곤란한 거네…」
		PRINTFORML 「그렇지만, 아직 나오기까지는 좀 더 걸릴 것 같아」
		PRINTFORML 「나올 것 같게 되면 뽑기 때문에,
		PRINTFORMW   이니까 그것까지는%CALLNAME:MASTER%의 질내를 느끼게 해…」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「응, 그렇게 말하지 않아도 알고 있을테니까」
		PRINTFORMW 「그 때가 되면 분명하게 뽑기 때문에, 그러니까%CALLNAME:MASTER%안에 좀 더 있게 해…?」
	;아라이먼트부
	ELSE
		PRINTFORML 「그다지 마지못해서 말하지 말아 주세요, 기분이 꺾이지 않습니까」
		PRINTFORML 「… 그런 말을 들으면 반대로 고집이라도%CALLNAME:MASTER%의 자궁에 충분히
		PRINTFORMW   쏟아 주고 싶어지네요」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 안에 내지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생 && !TALENT:MASTER:남자
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「응, 그렇네요…  아기 생기면%CALLNAME:MASTER%가 곤란한 거네…」
		PRINTFORML 「… 그렇지만, 나와%CALLNAME:MASTER%의 아이, 보고 싶구나…」
		PRINTFORML 「… 정말로 안에 내서는, 안돼?   응, 알았다…」
		PRINTFORMDW %CALLNAME:MASTER%가 한번 더 부탁하면, 수밀도는 미련 가득 승낙했다…
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「그렇구나, 질내에서 내면%CALLNAME:MASTER%가 곤란한 거네」
		PRINTFORMW 「그러면, 다른 일로 나를 기분좋게해 줍니까?」
	;아라이먼트부
	ELSE
		PRINTFORML 「그래, 어쩔 수 없네요. 이번은 멈추어 주어요」
		PRINTFORML 「시원스럽게 멈추었으므로 곤혹하고 있습니까?   … 후훗」
		PRINTFORML 「오늘은 질내에 나오는지, 그렇지 않으면 내일인가 그 다음인가」
		PRINTFORML 「충분히 초조해 해진 (분)편이,%CALLNAME:MASTER%도
		PRINTFORMW   그 때가 즐거움으로 어쩔 수 없이 될까요?」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;=====================================================================
;고통계 반응
;=====================================================================
;·가학계 행동이나 삽입을 수반하는 각종 행동으로 볼 수 있다
;·v1. 010 현재, 고통계에 의존도 변화의 처리 없음
;-------------------------------------------------

;─
;COM30, 아픔을참는다
;─
;·REACT분류:소극적 따른다
;·REACT인상:약간좋은인상
;·SOURCE:아픔이 없는 경우는 튕겨진다
;-------------------------------------------------
;·트리거가 되는 행동, 소스, 파라미터
;·SOURCE:아픔/구속
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:가슴애무/입으로애무/애널애무/애널빨고/키스/파이즈리한다
;ACT분류:도구
;	ACT:바이브/애널바이브/애널비즈
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트
;-------------------------------------------------
@KOJO_COM_K68_30
;-------------------------------------------------
;·TFLAG:REACT파생(0=아픔을 인내/1=아프지만기분좋다！)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 아프지만기분좋다！
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:REACT파생 == 1
;ENDIF

;0. 60 당시에는 현재의 파생 0 상당한의 것은 정의되어 있지 않았기 때문에, 파생 0으로 한정해 둡니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 아픔을 인내
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「이제 그만두자%CALLNAME:MASTER%」
		PRINTFORMW 「좋아하는 사람을 이런 식으로 괴롭혀도 기분 좋지 않게」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「아픈이지요? 이런 일 이제 되고 싶지 않네요?」
		PRINTFORML 「그러니까 기분 좋은 일이 절대 좋다고,
		PRINTFORMW   알까요?」
	;아라이먼트부
	ELSE
		PRINTFORML 「어떻게, 아파? 아프지요?」
		PRINTFORMW 「%CALLNAME:MASTER%가 고집이 있기 때문에, 이런 일이 되어 있는 거에요?」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM31, 비명을올린다
;─
;·REACT분류:소극적 따른다
;·REACT인상:0에서멀어짐
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류:애무
;	ACT:가슴애무/입으로애무/애널애무/애널빨고/키스/파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:바이브/애널바이브/애널비즈
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그/매도/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트/공중육변기플레이
;-------------------------------------------------
@KOJO_COM_K68_31
;-------------------------------------------------
;·TFLAG:REACT파생(0=아픔에 의한 비명/1=공포에 의한 비명)
;-------------------------------------------------

;내용적으로 파생 1에서의 한정이 타당하다라고 판단했습니다@/L
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 공포에 의한 비명
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1 && MENUMATCH(TFLAG:ACT, "회화")
	PRINTL
	;퍼펙트 서로 사모함
	IF KFLAG:700
		PRINTFORML 「에엣,%CALLNAME:MASTER%、%CALLNAME:MASTER%?」
		PRINTFORMW 「괜찮아, 괜찮기 때문에 침착해!」
	ELSE
		PRINTFORML 「깜짝 놀랐어요, 이제(벌써)」
		PRINTFORMW 「갑자기 큰 소리를 지르는걸」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 아픔에 의한 비명
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && CFLAG:700 == 1
		PRINTFORML 「그러니까 그만두자고 했지 않아」
		PRINTFORML 「이런 일보다 기분 좋은 일이 좋은 것에 정해져 있어요」
		PRINTFORMW 「… 그렇지만 평상시 (들)물을 수 없다%CALLNAME:MASTER%의 소리를 들을 수 있어 한 치만 만족… 한 치 뿐이니까」
		PRINTL 
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「싫겠지, 쭉 이런 성과가 없는 일을 계속 되는 것은」
		PRINTFORMW 「지금부터라도 늦지 않기 때문에, 나와 기분 좋은 일 하는 것이 좋다고 생각해」
	;아라이먼트부
	ELSE
		PRINTFORML 「%CALLNAME:MASTER%, 의외로 이것으로 기뻐하고 있거나 할까?」
		PRINTFORML 「그것이라면 멈추지 않아요, 쾌락의 정념도 고통의 원한도,
		PRINTFORMW   모두 나의 양식이 되니까」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM32, 날뛴다
;─
;·파생에 의해 날뛰고 성공(REACT분류:날뛴다), 날뛰고 실패(REACT분류:소극적 따른다)로 나누어진다
;·날뛰고가 통과하면 도구계/성교계/성교 봉사계 커맨드를 방해한 취급이 된다
;·벌포인트 추가
;·REACT인상:약간나쁜인상
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스
;		성기를서로켠다/파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그
;ACT분류:휴게
;	ACT:치료한다
;ACT분류:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다
;-------------------------------------------------
@KOJO_COM_K68_32
;-------------------------------------------------
;·TFLAG:REACT파생(0=날뛰고 성공/1=줄구속에 의한 날뛰고 실패/2=제지에 의한 날뛰고 실패)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 2, 제지에 의한 날뛰고 실패(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 2
	PRINTFORML 「어디까지나 저항하는 거네,%CALLNAME:MASTER%는」
	PRINTFORMW 「이대로 해도 결말이 나지 않은 것 같구나, 좀 더 벌 할 수 밖에 없는 것일까」
	PRINTL
	RETURN 1
ENDIF
	
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 줄구속에 의한 날뛰고 실패(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	PRINTFORML 「어째서 거기까지 저항하는 거야?」
	PRINTFORMW 「%CALLNAME:MASTER%를 알 수 있어 주기만 하면, 그런 줄도 곧바로 풀어 주는데」
	PRINTL
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 날뛰고 성공
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「그만두어, 그만두어요%CALLNAME:MASTER%, 이렇게 날뛴다니 심해요」
		PRINTFORML 「내가 하는 일, 싫게 되어 버렸어?」
		PRINTFORMDW 수밀도는 깊은 슬픔의 눈으로 복목 십상으로%CALLNAME:MASTER%의 일을 응시하고 있다…
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「그만두어, 날뛰지 말고, 부탁」
		PRINTFORMW 「어째서? 다만 나는%CALLNAME:MASTER%에 기분 좋아져 받고 싶은 것뿐인데」
	;아라이먼트부
	ELSE
		PRINTFORML 「… 그렇게, 거기까지 해 저항한다는 것이군요」
		PRINTFORML 「… 아무래도%CALLNAME:MASTER%가 나를 받아들이고 싶지 않은 것이라면」
		PRINTFORMW 「힘으로도 받아들여 받을 수 밖에 없겠지요」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM33, 무서워한다
;─
;·REACT분류:허가를청한다
;·일정 조건으로 분노를 사, 벌포인트 추가
;-------------------------------------------------
;·트리거 이벤트제
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류:1:애무
;	ACT:풋잡한다/밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:애널바이브/애널비즈
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:줄/아이마스크/볼개그/매도/관장세트
;ACT분류:7:이상
;	ACT:방치플레이/공중육변기플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;-------------------------------------------------
@KOJO_COM_K68_33
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=두려움에 대한 초조)
;-------------------------------------------------

;Rev0. 60에는 존재하고 있지 않았던 커맨드
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;=====================================================================
;봉사계 반응 COM40 접수대
;=====================================================================
;·조교자에게로의 애무를 포함한 행동으로 볼 수 있다
;·ACT분류:봉사+트리거 이벤트제
;-------------------------------------------------

;─
;COM40, 거부한다
;─
;·REACT분류:거부
;·REACT인상:약간나쁜인상
;·벌포인트 추가
;·의존도 변화(-2, -4)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:영양제를먹인다
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다
;-------------------------------------------------
@KOJO_COM_K68_40
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM40, 거부한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;=================================================
	;구와 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「그, 그런,%CALLNAME:MASTER%?」
		PRINTFORML 「나의 일을 받아들이는 것이 싫게 되어 버린 것입니까?」
		PRINTFORMDW 수밀도는 당장 울 듯한 눈으로%CALLNAME:MASTER%의 일을 응시하고 있다…
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「왜? 어째서 받아들여 주지 않습니까?」
		PRINTFORMW 「부탁이니까 나를 받아들여!」
	;아라이먼트부
	ELSE
		PRINTFORML 「%CALLNAME:MASTER%는 잊고 있을지도 모르지만」
		PRINTFORML 「이런 겉모습에서도 나는 요괴, 본래 인간을 덮치는데 용서는 필요없는 것이에요?」
		PRINTFORML 「… 물론%CALLNAME:MASTER%는 그것을 고려한 다음 행동하고 있는 거에요?」
		PRINTFORMDW 무라사의 눈동자에 이상한 빛이 켜진다…
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM41, 소극적한다
;─
;·REACT분류:소극적 따른다
;·REACT파생 1는 "소극적인의가 마음에 들지 않는다"되어 벌포인트 추가
;·REACT인상:약간좋은인상
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:의상의변경을요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/애널섹스시킨다
;-------------------------------------------------
@KOJO_COM_K68_41
;-------------------------------------------------
;·TFLAG:REACT파생 == 0, 소극적이지만 좋은가
;·의존도 변화(-3, +2)
;-------------------------------------------------
;·TFLAG:REACT파생 == 1, 소극적인의가 마음에 들지 않는다
;·의존도 변화(-5, 0)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 소극적인의가 마음에 들지 않는다(벌포인트 가산)
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && TFLAG:REACT파생 == 1
	;%CALLNAME:MASTER%의 소극적인 자세에,%CALLNAME%는 불만인 것 같다…
;ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 소극적이지만 좋은가
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;정합성이 무너질 것 같은 것은 제외하도록(듯이)했습니다@/L
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700 && (IS_NOWACTNAME("의상의변경을요구한다/승마위") || MENUMATCH(TFLAG:ACT, "봉사") || MENUMATCH(TFLAG:ACT, "성적봉사"))
		PRINTFORML 「응, 어떻게 한 것입니까%CALLNAME:MASTER%, 한 치 속상해요」
		PRINTFORML 「혹시 이것이 이야기에 듣는 『초조하게해플레이』라고 하는 것일까?」
		PRINTFORMW 「%CALLNAME:MASTER%에는 나쁩니다만 나에게는 맞을 것 같게 없네요, 보통으로 합시다」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「응, 따라주어 고마워요」
		PRINTFORMW 「다음은 조금만 더라도 좋으니까, 적극적으로 되어 받을 수 있으면 기뻐요」
	;아라이먼트부
	ELSE
		PRINTFORML 「… 아 이제(벌써), 속상하네요」
		PRINTFORMW 「혹시, 따라서 있는 모습을 해 나를 화나게 하려고 하고 있습니까?」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM42, 적극적한다
;─
;·REACT분류:적극적 따른다
;·REACT인상:좋은인상
;·인내 플래그 해소
;·의존도 변화(+4, +2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:의상의변경을요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/애널섹스시킨다
;-------------------------------------------------
@KOJO_COM_K68_42
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;정합성이 무너질 것 같은 것은 제외하도록(듯이)했습니다@/L
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700 && (IS_NOWACTNAME("승마위") || MENUMATCH(TFLAG:ACT, "봉사") || MENUMATCH(TFLAG:ACT, "성적봉사"))
		PRINTFORML 「언제나 적극적으로 해 주네요」
		PRINTFORML 「정말 좋아해요,%CALLNAME:MASTER%♪」
		PRINTFORMDW 수밀도는%CALLNAME:MASTER%가 더욱 줄 쾌락을 기대하고 있다…
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「그렇게 적극적으로 해 준다니 기뻐요…」
		PRINTFORMW 「나도 좀 더 노력하니까요」
	;아라이먼트부
	ELSE
		PRINTFORML 「대단히 온순하게 되었어요」
		PRINTFORMW 「향후도 그대로의 태도로 있다면, 포상도 생각해 두어요」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM43, 애정을담아준다
;─
;·일정 이상의 호감도, 조교레벨이 필요
;·REACT분류:적극적 따른다
;·REACT인상:좋은인상
;·인내 플래그 해소
;·반발 각인이 있으면 튕겨진다
;·의존도 변화(+6, +10)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/애널섹스시킨다
;-------------------------------------------------
@KOJO_COM_K68_43
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을담아준다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	PRINTL
	;내용적으로 ACT:승마위를 포함해도 좋다고 판단했습니다@/L
	;=================================================
	;ACT분류가 성적봉사, 또는 ACT 승마위
	;=================================================
	IF 구상기입체크(100) && (MENUMATCH(TFLAG:ACT, "성적봉사") || IS_NOWACTNAME("승마위"))
		IF KFLAG:700
			PRINTFORML 「아, 앙됏♪, 후앗, 놈들이야…♪」
			PRINTFORML 「그렇게 감정 담아 되면 갖지 않아…♪  … 후앙♪」
			PRINTFORMDW 수밀도는 완전하게 도중 중단 상태가 되면서,%CALLNAME:MASTER%가 주는 쾌락에 빠지고 있다…
			PRINTL
			RETURN 1
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORML 「아훗, 앗, 하앗, 후앗」
			PRINTFORML 「이, 이렇게 상냥하고, 되면, 날아가 버릴 것 같아요… 히양♪」
			PRINTFORMDW 무라사는%CALLNAME:MASTER%로 되는 대로 되어 있다…
		;아라이먼트부
		ELSE
			PRINTFORML 「이런, 이런 식으로 되어도」
			PRINTFORML 「속여지면, 생각하면, 큰 실수, 그러니까」
			PRINTFORMW 「그렇지만 기분 좋아요… 이봐요, 좀 더 계속하세요…」
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「아앗, 이, 이런, 기분 좋아…♪」
		PRINTFORML 「너무 정중함%CALLNAME:MASTER%! 앗, 앙돼, 앙돼엣」
		PRINTFORMDL %CALLNAME:MASTER%의 정중한 봉사를 계속 받은 수밀도는,
		PRINTFORMDW 말씨가 돌지 않는 소리로 쾌락을 고하고 있다…
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 「하아…, 그런 곳, 까지… 우아」
		PRINTFORML 「어째서 그렇게 상냥한 것… 응읏」
		PRINTFORMDW 무라사는%CALLNAME:MASTER%로 되는 대로 되어 있다…
	;아라이먼트부
	ELSE
		PRINTFORML 「언제의, 에… 이런 일, 기억한 것입니까%CALLNAME:MASTER%는… 읏」
		PRINTFORMW 「뭐, 좋아요… 이봐요, 입다물고 계속하세요…」
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM44, 태연하게한다
;─
;·REACT분류:태연하게 따른다
;·REACT인상:약간좋은인상
;·의존도 변화(+2, -2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:의상의변경을요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/애널섹스시킨다
;-------------------------------------------------
@KOJO_COM_K68_44
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------

;Rev0. 60에는 존재하고 있지 않았던 커맨드
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0) && ! TFLAG:REACT파생
;ENDIF

;=====================================================================
;그 외의 반응 COM50 접수대
;=====================================================================
;·특정의 장면에서 발생하는 이상한 행동
;-------------------------------------------------

;─
;COM50, 자위하기시작한다
;─
;·REACT분류:받아들인다
;·파생 1, 마음대로 자위 한데의 경우는 벌포인트 추가
;·REACT인상:나쁜인상
;·인내 플래그 해소
;-------------------------------------------------
;·트리거 이벤트제
;-------------------------------------------------
;·줄로 속박되고 있으면 안돼
;·벌써 자위하고 있다, 성교중, 성교봉사중은 튕겨진다
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다
;ACT분류:1:애무
;	ACT:키스
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:
;ACT분류:4:수치
;	ACT:자위과시하고/수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/다리빨고강제/가슴애무강제
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이
;ACT분류:9:휴게
;	ACT:쉬게한다
;ACT분류:10:성교 봉사
;	ACT:
;-------------------------------------------------
@KOJO_COM_K68_50
;-------------------------------------------------
;TFLAG:REACT파생 == 0, 마음대로 자위 하고 있지만 좋은가, 의존도 변화(-1, 0)
;TFLAG:REACT파생 == 1, 마음대로 자위 하지마, 의존도 변화(-3, +2)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	PRINTL
	;내용적으로 이 개소에 적당하다고 생각했으므로, 조건 없음 또한 아라이먼트부의 개소로부터 전용
	;아무튼 중복 합니다만, 현행 사양은 한 번 아라이먼트 정말로 되면 이후 그대로인 것으로 별로 좋을까라고 하는 일로@/L
	;=================================================
	;REACT파생 1, 마음대로 자위 하지마
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORML 「사람을 무시해 마음대로 시작하다니 좋은 담력이군요」
		PRINTFORMW 「어떤 벌을 할까요는, 원래 들리고 있는 것일까 이 원숭이씨」
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT분류가 회화계
	;=================================================
	IF 구상기입체크(100) && MENUMATCH(TFLAG:ACT, "회화")
		;퍼펙트 서로 사모함
		IF KFLAG:700 == 1
			PRINTFORML 「무, 무엇? 조금―?%CALLNAME:MASTER%-?」
			PRINTFORML 「… 아무리 뭐라해도 회화중에 그것은 너무 하지 않습니까?」
			PRINTFORMDW 수밀도는%CALLNAME:MASTER%의 치태를 보면서 질려 버리고 있다…
			PRINTL 
			RETURN 1
		ENDIF
		
		;아라이먼트정
		IF ALI(0)
			PRINTFORML 「… 확실히 쾌락에 빠졌으면 좋다고는 말했습니다만」
			PRINTFORMW 「과연 여기까지는 상상의 범주외예요…」
		;아라이먼트부
		ELSE
			PRINTFORML 「엣또, 여보세요」
			PRINTFORMW 「… 조교의 너무 해 망가져 버렸을까」
		ENDIF
		PRINTL
		RETURN 1
	ENDIF
	
	;=================================================
	;퍼펙트 서로 사모함
	;=================================================
	IF 구상기입체크(100) && KFLAG:700
		PRINTFORML 「저기, 인내 할 수 없다면 그렇게 말해 준다면 좋은데」
		PRINTFORML 「그렇지 않으면 나로는 이제 만족 할 수 없다고 말하는 일이야?」
		PRINTFORMW 「…… 무시하지 말아요」
		PRINTL
		RETURN 1
	ENDIF
	
	;아라이먼트정
	IF ALI(0)
		PRINTFORML 
		PRINTFORML 「… 확실히 쾌락에 빠졌으면 좋다고는 말했습니다만」
		PRINTFORMW 「나를 무시해 자신만큼, 이라고 말하는 것은 심한 것이 아닙니까?」
		PRINTFORML 
	;아라이먼트부
	ELSE
		PRINTFORML 
		PRINTFORML 「사람을 무시해 마음대로 시작하다니 좋은 담력이군요」
		PRINTFORMW 「어떤 벌을 할까요는, 원래 들리고 있는 것일까 이 원숭이씨」
		PRINTFORML 
	ENDIF
	PRINTL
	RETURN 1
ENDIF

;─
;COM51, 도구를제외한다
;─
;·REACT분류:날뛴다
;·TEQUIP:(바이브/애널바이브/애널비즈/관장기＋플러그/클리캡/오나홀/니플캡/아이마스크/볼개그/페니스밴드)를 장착하고 있는 것이 조건
;·조교 대상이 자위중인 경우는 중단이 된다
;·REACT인상:약간나쁜인상
;·의존도 변화(-3, -10)
;-------------------------------------------------
;·제외하는 도구는 한 번에 1개까지
;※우선도는 이하와 같다
;(아이마스크>관장기＋플러그>애널바이브>바이브> 오나홀>클리캡>볼개그>니플캡> 애널비즈)
;-------------------------------------------------
;선택 가능한 조건…
;·TEQUIP:(바이브/애널바이브/애널비즈/관장기＋플러그/클리캡/오나홀/니플캡/아이마스크/볼개그/페니스밴드)를 장착하고 있는 것이 조건
;·TEQUIP:줄/성교중은 실행 불가
;-------------------------------------------------
@KOJO_COM_K68_51
;-------------------------------------------------
;·제외한 도구의 TEQUIP 번호가 TFLAG:REACT파생에 들어간다
;-------------------------------------------------

;오리지날에 기술 없음
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM51, 도구를제외한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;IF 구상 기입 체크(0)
;ENDIF

;─
;COM52, 하는대로
;─
;·효과 없음. 실행할 수 있는 반응이 한 개도 없을 때의 구제
;·조교자에게 좋을대로 희롱해지고 한편, 하라는 대로 되는 목표 상황
;·REACT파생 마다 REACT분류, 의존도 변화가 바뀐다
;-------------------------------------------------
@KOJO_COM_K68_52
;-------------------------------------------------
;·TFLAG:REACT파생(0=통상/1=성교계/2=성교 봉사계/3=수치계, 강제계/4=봉사계/5=협박계/6=협박계/7=고통계/8=자위중의 회화/9=쾌감을 받으면서 회화/10=쾌락계/11=회화계/12=한번휴식)
;-------------------------------------------------
[SKIPSTART]

COM52는 현행판으로 크게 사양이 바뀐 개소
COM70는 Rev0. 60에는 존재하고 있지 않았던 COM
그런 (뜻)이유로 오리지날에도 기술이 없기 때문에 정리해 comment out 해 둡니다@/L

;------------------------------------------------
;REACT분류:받아들인다/의존도 변화(+4, +10)
;성교계 ACT·역강간
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 「무저항에 범해진다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 1
ENDIF

;------------------------------------------------
;REACT분류:받아들인다/의존도 변화(+10, +10)
;성교계 봉사계 ACT·승마위
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 2, 「빠진 것처럼허리를흔드는」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 2
ENDIF

;------------------------------------------------
;ACT(의상 변경/자위/방뇨/개장/이라마치오)
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 3, 「말해지는 대로 천천히 따른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 3
ENDIF

;------------------------------------------------
;봉사계 ACT
;REACT분류:받아들인다/의존도 변화(+8, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 4, 「매료되어진 것처럼 봉사를 반복한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 4
ENDIF

;------------------------------------------------
;ACT(위협한다/벌과 선언/고함친다/기분나쁘게웃는다/매도)
;반항적인 소질이 있어, 굴복각인 5 이하의 경우
;REACT분류:소극적 따른다/의존도 변화(0, -2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 5, 「분한 듯이 노려본다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 5
ENDIF

;------------------------------------------------
;ACT(위협한다/벌과 선언/고함친다/기분나쁘게웃는다/매도)
;REACT분류:소극적 따른다/의존도 변화(+2, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 6, 「무서워한 것처럼 응시한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 6
ENDIF

;------------------------------------------------
;가학계 ACT·고통이 큰 경우
;REACT분류:소극적 따른다/의존도 변화(+8, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 7, 「소리가 되지 않는 비명을 올려 몸부림쳐 뒹군다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 7
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT 또한 자위중인 경우
;REACT분류:받아들이는 /REACT인상:나쁜인상/의존도 변화(-2, +2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 8, 「오나니─에 열중해 듣지 않았다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 8
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT
;REACT분류:받아들인다/의존도 변화(+4, +2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 9, 「기분 좋아서 선대답」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 9
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT 이외의 경우
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 10, 「머리 진흰색 황홀 상태」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 10
ENDIF

;------------------------------------------------
;회화계 ACT
;REACT분류:받아들인다/의존도 변화(+8, +4)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 11, 「스스로도 모르는 채 부끄러운 것을 말해버린다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 11
ENDIF

;------------------------------------------------
;조교자, 혹은 조교 대상 상태에 의해 한번휴식이 되었을 경우
;혹은 대만족 보너스로 쉬게 해를 선택했을 경우
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;한번휴식의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:한번휴식 && TFLAG:REACT파생 == -1
	;=================================================
	;대만족 보너스로 쉬게 해를 선택했을 경우
	;=================================================
	IF 구상 기입 체크(0) && IS_POLICY("오늘", "휴게")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상 기입 체크(0)
	ENDIF
ENDIF

;------------------------------------------------
;모든 조건에 해당하지 않는 경우
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 그 외
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && ! TFLAG:REACT파생
ENDIF
[SKIPEND]
;=====================================================================
;특수한 반응 COM70～
;=====================================================================

;─
;COM70, 가게해!
;─
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도 변화(+10, +6)
;-------------------------------------------------
;선택 가능한 조건…
;·초조하게 해 안은 아니다
;·TCVAR:MASTER:갈것같다가 서 있다
;·애무계 ACT(키스/밀어넘어뜨리기 이외) 한편 각 부위의 PALAM가 5000이상이다
;※상기 조건에 들어맞는 경우는 가능
;-------------------------------------------------
;·TFLAG:REACT파생(0=들은체 만체 한다/1=가게해 준다/2=가게해 주지 않는다)
;-------------------------------------------------
;ACT에 있어서 TCVAR:MASTER:초조하게해번 == 1(가게 한다)에서 만나도 COM70의 선택으로 TCVAR:MASTER:초조하게해번 > 1(초조하게 한다)(이)가 되는 경우 있어
;현재의 사양에서는 이 손의 변화를 참조할 방법은 없다. 본체측의 대응이 바람직한다!
;-------------------------------------------------
@KOJO_COM_K68_70
;-------------------------------------------------
;·TFLAG:REACT파생(0=들은체 만체 한다/1=가게해 준다/2=가게해 주지 않는다)
;-------------------------------------------------
[SKIPSTART]
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 2, 가게해 주지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 가게해 준다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 요구를 들은체 만체 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상 기입 체크(0) && TFLAG:REACT파생 == 0
ENDIF
[SKIPEND]