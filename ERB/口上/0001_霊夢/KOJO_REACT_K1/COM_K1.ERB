;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무 구상 범용 리액션 구상 파일
;
;	개별 리액션(REACT)으로부터 빠져 온 것을 해당 COM로 처리하는 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;%CALLNAME:MASTER%의 행동으로부터의 역당겨

;=====================================================================
;범용 리액션 구상
;=====================================================================
;·개별 리액션 구상을 표시하지 않았던 경우에 표시
;·KOJO_REACT_KX1_Y1의 RESULT가 0이었을 경우, 범용 리액션이 불려 간다
;·REACT로 표시한지 아닌지는 RESULT로 판정되므로, REACT로 표시해 RETURN 0으로 한층 더 범용도 표시시키는 일도 가능한 것이긴 함
;·KOJO_COM_KX1_Y1로부터 RESULT 0이 돌아가면 통괄 범용 리액션(KOJO_REACT_KX)이 불려 간다
;---------------------------------------------------------------------
;간이 플로우
;    표시 타이밍·조교 대상의 리액션(커맨드·반응)
;        액션 번호별의, 개별 리액션 구상
;            표시하지 않았던 경우, 커맨드 번호별의 범용 리액션 구상←(지금 여기)
;                표시하지 않았던 경우, 통괄 범용 리액션 구상
;---------------------------------------------------------------------
;관련 변수/함수류…
;
;ACT분류계
;TFLAG:ACT분류 이 턴의 조교 메뉴(0=회화/1=애무/2=도구/3=성교/4=수치/5=봉사/6=가학/7=이상/8=사역/9=휴게/10=성교 봉사)
;GET_ACTM ACT 번호로부터 ACT분류번호를 취득하는 함수
;MENUMATCH 지정한 ACT분류에 포함되어 있으면 진을 돌려준다(회화/애무/도구/성교/수치/봉사/가학/이상/사역/휴게/성적봉사)
;
;COM계
;TFLAG:REACT파생 각 SELECTCOM의 파생. COM마다 파생 내용은 다르다
;TFLAG:REACT부하 선택한 COM의 기력 소비를 판별한다(0=자연스러운 행동/2=무리가 없는 행동/무리를 한 행동)
;COMCOR_POSI 주로 소극적 따르는 계 COM의 기력 소비가 큰 때(TFLAG:REACT부하 == 2인 경우)에, COM 보정치가 호의적인가 어떤가를 판단
;
;그 외
;TFLAG:REACT인상 각 SELECTCOM의 상대에게로의 심상(0=영향없음/1=좋은인상/2=약간좋은인상/3=나쁜인상/4=약간나쁜인상/5=0에서멀어짐/6=0에가까워짐)
;TFLAG:벌포인트( 100모이면 벌개시)
;FIRSTCOM  첫회 범용 리액션 판정 함수  SELECTCOM를 자동으로 참조하므로, 현재 선택한 COM 마다 번호가 독립해 보존된다
;ONCECOM  조교내 첫회 판정용 함수. FIRSTTIME계와 달라 조교전에 초기화된다. 기본적인 사양은 FIRSTCOM와 같이
;RANDCOM(AUTORAND계)  RAND 처리로 이벤트 종류마다의 번호를 돌려주지만, 전종 볼 때까지 같은 숫자는 돌려주지 않는다. 모두 표시했을 때에 초기화.
;
;---------------------------------------------------------------------

;=====================================================================
;※ACT분류와 그 파생에 대해
;
;COM에 따라서는 발생하는 ACT분류가 복수에 걸칩니다
;각 COM내를 TFLAG:ACT분류로 나누어 주는 일에 의해, 보다 정확하게 상황을 참조하는 것이 가능합니다
;다만, ACT분류자체도 파생하는 것이 있어, 이 경우, TFLAG:ACT분류에서는 파생 상황을 참조 할 수 있지 못하고, 약간 이상한 일이 됩니다
;
;예를 들면…, ACT55, 다리빨고강제는 본래 ACT분류:봉사계입니다만, 이상계로부터 발생하는 경우가 있어,
;TFLAG:ACT분류의 단락에서는, 상정 그대로의 구상이 표시되지 않는다고 말하는 것이 생각됩니다
;
;거기서 TFLAG:ACT분류는 아니고, @MENUMATCH든지@GET_ACTM로 ACT분류 마다의 분기를 마련해 주면, 파생처를 참조하는 것이 가능하게 되어
;실제로 행해지고 있는 ACT분류로 처리되게 됩니다
;IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "봉사")와 같은 테두리를 마련해 주면, 위에 올린 것 같은 상황에도 대응이 가능합니다
;MENUMATCH의 분류 마다의 정의에 대해서는 이 파일의 28행째를 참조해 주세요
;
;ACT분류파생 일람…
;(각 수치는 ACT 번호)
;회화(기분 0/호의 1/지식 2/농락해라 3/위협한다 4/의상 5/합의 6/처분 7/고함친다 8/비웃는다 9/매도 66)
;애무(손으로 10/가슴을 11/입으로 12/A손 13/A구 14/키스 15/C찰 16/B찰 17/다리로 18/V손 19/로터 20/압도 27)
;도구(로터 20/바이 21/A바이 22/A비 23/C사랑 24/니풀 25/P대 26/관장 68)
;성교(정상 30/후배 31/승마 32/대면 33/배면 34/AS35)
;수치(자위 40/개장 41/보이고 42/대거울 43/야외 44/방뇨 45/코스(미실장)46/방치 73)
;봉사(손으로 50/입으로 51/가슴으로 52/가랑이 53/족지 55/이라 56/승마 32/가슴을 57)
;가학(손바닥 60/회초리 61/바늘 62/줄 63/눈가림 64/0입마개 65/매도 66/목마 67/관장 68/이라 56)
;이상(Fist70/AFi71/양Fi72/방치 73/공중 74/P장 75/비웃는다 9/다리로 18/족지 55/목마 67)
;사역(미실장)
;휴게(휴일 90/복약 91/치료 92)
;성교(봉사) (정상 95/후배 96/승마 103/대면 97/배면 98/AS99)
;
;=====================================================================
[SKIPSTART]
MENUMATCH소분기 견본…

다른 분기와의 우선도에 주의해, 필요에 따라서 각 COM에 추가해 사용해 주세요
	;=================================================
	;ACT분류, 회화
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "회화")
	ENDIF
	;=================================================
	;ACT분류, 애무
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "애무")
	ENDIF
	;=================================================
	;ACT분류, 도구
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "도구")
	ENDIF
	;=================================================
	;ACT분류, 성교
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "성교")
	ENDIF
	;=================================================
	;ACT분류, 수치
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "수치")
	ENDIF
	;=================================================
	;ACT분류, 봉사
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "봉사")
	ENDIF
	;=================================================
	;ACT분류, 가학
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "가학")
	ENDIF
	;=================================================
	;ACT분류, 이상
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "이상")
	ENDIF
	;=================================================
	;ACT분류, 휴게
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "휴게")
	ENDIF
	;=================================================
	;ACT분류, 성적봉사
	;=================================================
	IF 구상 기입 체크(0) && MENUMATCH(TFLAG:ACT, "성적봉사")
	ENDIF
[SKIPEND]
;---------------------------------------------------------------------

;※각 분기에는 대응하는 지문을 제시되어 있습니다만, 최종적으로 어떠한 형태로 할까 아직 뭐라고도 말할 수 없는 곳

;=====================================================================
;회화계 반응 COM0 접수대
;=====================================================================
;·주로 ACT분류:회화계/휴게계 시에 발생한다
;·발생은 트리거 이벤트제. 특정의 ACT가 트리거가 된다
;·ACT분류:회화에 대해서는 REACT로 완결시키는 것이 바람직함
;-------------------------------------------------

;─
;COM0, 침묵한다
;─
;·따랐던 것에는 안 된다
;·ACT1, 지금의기분을얘기한다/자신에게로의기분을얘기한다의 경우는 REACT파생 1, "질문에 답하지 않는다"되어 벌포인트 가산
;·REACT인상:나쁜인상
;·의존도 변화(-1, -1)
;-------------------------------------------------
;·ACT가지금의기분을얘기한다/자신에게로의기분을얘기한다의 경우는 파생 1, 질문에 답하지 않는 취급
;·그 외의 경우는 파생 0, 단순한 침묵
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류 0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류 2:도구
;	ACT:
;ACT분류 4:수치
;	ACT:수치플레이/야외플레이
;ACT분류 6:가학
;	ACT:줄/아이마스크/볼개그/매도/삼각목마
;ACT분류 7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류 9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;--------------------------------------------------
@KOJO_COM_K1_0
;-------------------------------------------------
;·TFLAG:REACT파생(0=단순한 침묵/1=질문에 답하지 않는다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 침묵한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;ACT, 지금의기분을얘기한다/자신에게로의기분을얘기한다의 경우에 파생한다
	;=================================================
	;REACT파생 1, 질문에 답하지 않는다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「어쩔 수 없네요. 너의 몸에 들어요」
		PRINTFORMW 「솔직하게 가르침%RAND_SPLIT("라고 갖고 싶어요/라고 줘/하세요")%」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 무심코 뭔가 이야기를 시작했지만, 부끄러운 내용이었으므로
			;말을 삼켜, 입다물고 있었다…
			PRINTFORMW 「엣치한 일 생각하고 있어?」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 기분이 안좋은 것 같게 침묵을 지킨 채였다…
			PRINTFORMW 「입다물고 있으면, 괴로워져요?」
			CALL FADEOUT(8, "「… 뭐, 좋아요」")
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 아무것도 말하지 않았다…
		PRINTFORML 「필요로 하는 것이 곤란하고 있는거죠」
		PRINTFORMW 「나에게 맡기세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%의 모습을 엿보는 것 같은 기색을 보였지만,
		;아무것도 말하지 못하고 입다물고 있었다…
		PRINTFORMW 「나에게는 솔직해질 수 없어?」
		RETURN 1
	ENDIF
	
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM1, 여리게대답한다
;─
;·REACT분류는 파생에 의해 변화
;·ACT("엄격하게위협한다/벌이라고선언한다/고함친다/밀어넘어뜨린다")로 일정 조건을 채우면 REACT파생 1, 애원에 파생
;·REACT인상:약간좋은인상
;·의존도 변화(0, +1)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다/의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;-------------------------------------------------
@KOJO_COM_K1_1
;-------------------------------------------------
;·TFLAG:REACT파생(0=여리게대답한다/1=애원 한다)
;-------------------------------------------------

;REACT파생로 내용이 다르므로 파생마다 나누어 버립니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 애원 한다(REACT분류:용서를빈다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신이 응석부린 기분을 뿌리쳐,
			;허약하면서, 벌써 그만두고라고 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 기분을 해치지 않게 가능한 한 허약하고,
			;이제(벌써) 이런 일 그만두고라고 말했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 심약구, 이제(벌써) 이런 일 그만두고라고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 심약구 작은 소리로, 기분을 엿보도록(듯이)하면서
		;이제(벌써) 이런 일 그만두고라고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「안 돼요, 그만두어 주지 않는다♪」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 여리게대답한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신의 기분에 희미하게 깨달으면서도,
			;마음이 약하게 거리를 취하는 것 같은 대답을 했다…
			PRINTFORMW 「나에게는 솔직해질 수 없어?」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 기분을 해치지 않게 가능한 한 허약하게 대답했다…
		PRINTFORMDL %CALLNAME:TARGET%는 곤란한 것처럼 머리를 긁고 있다
		PRINTFORMW 「응… 좀 더 솔직해져도 좋아요?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 심약구대답했다…
		PRINTFORML 「응… 그 말씨의 나쁨…」
		PRINTFORMW 「기분이 결정되지 않은 것 같구나?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 심약구 작은 소리로, 기분을 엿보도록(듯이)하면서 대답했다…
		PRINTFORMDL %CALLNAME:TARGET%는 곤란한 것처럼 머리를 긁고 있다
		PRINTFORMW 「응… 곤란했군요…」
		RETURN 1
	ENDIF
	
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM2, 무뚝뚝하게대답한다
;─
;·REACT분류:없음
;·따랐던 것에는 안 된다
;·벌포인트가 가산된다
;·REACT인상:나쁜인상
;·의존도 변화(-2, -2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다/의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K1_2
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 무뚝뚝하게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 무심코 뭔가 이야기를 시작했지만, 부끄러운 내용이었으므로
			;아무것도 아니면 무뚝뚝하게 손질했다…
			CALL FADEOUT(2, "개응")
			PRINTFORMW 츤츤
			PRINTFORMW 말랑말랑
			PRINTFORMDW %CALLNAME:MASTER%의 고집은 당장 무너뜨려질 것 같다
			PRINTFORMDW 필사적으로 입가를 긴축시키고 견딘다%CALLNAME:MASTER%에%CALLNAME:TARGET%는 우쭐거렸는지와 같이 입가를 매달아 올렸다
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 힘껏의 저항을 담아 무뚝뚝하게 응했다…
			PRINTFORMW 「아무것도 아닐 이유 없지요」
			PRINTFORMW 「저기, 나에게는 솔직해질 수 없어?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 무뚝뚝하게\@ IS_NOWACTNAME("밀어넘어뜨린다") ? 응답 # 대답 \@했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 심지의 강한 눈동자로%CALLNAME%를 응시해 무뚝뚝하게 응했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;CASE 0의 확률은 짠다　
		SELECTCASE RAND:5
		;SELECTCASE RAND:2
			CASE 0
			;너는 아니고 %CALLNAME:MASTER%호칭으로 기분을 끄는 시도;빈발하는 것도 무엇인 것으로 저확률
				PRINTFORMW 「저기,%CALLNAME:MASTER%」
				PRINTFORMW 「여기 향해?」
			CASEELSE
				PRINTFORMW 「무정하네요」
		ENDSELECT
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM3, 붙임성좋게대답한다
;─
;·REACT분류:적극적 따른다
;·REACT인상:좋은인상
;·의존도 변화(+3, +1)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K1_3
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 붙임성좋게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는 잘 비위를 맞추려고 만들기 웃는 얼굴로 응했다…
		PRINTFORMW 「표정이 딱딱해요?」
		PRINTFORMDW 에!
		PRINTFORMDW %CALLNAME:TARGET%의 손가락이%CALLNAME:MASTER%의 뺨을 밀어넣는다
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 붙임성 좋게\@ IS_NOWACTNAME("밀어넘어뜨린다") ? 응답 # 대답 \@했다…
		PRINTFORMW 「너무 애교가 있는 것도, 너답지 않을지도…」
		PRINTFORMW 「읏, 사치일까?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 기쁜듯이%CALLNAME%에 미소지어,
		;붙임성 좋게 대답했다…
		PRINTFORMW 「그 얼굴, 나 이외에 보이면%RAND_SPLIT("허락하지 않아요? /안 되는 것이니까/용서하지 않아요")%」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM4, 강하게대답한다
;─
;·REACT분류:없음
;·따랐던 것에는 안 된다
;·ACT, 밀어넘어뜨린다의 경우는 REACT파생 1, "밀어넘어뜨리기에 굽히지 않는다"에 파생
;·REACT인상:나쁜인상
;·의존도 변화(-1, -3)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다/의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K1_4
;-------------------------------------------------
;·TFLAG:REACT파생(0=강하게 응답/1=밀어넘어뜨리기에 굽히지 않는다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 강하게대답한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;ACT, 밀어넘어뜨린다의 경우에게만 파생
	;=================================================
	;REACT파생 1, 밀어넘어뜨리기에 굽히지 않는다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		;%CALLNAME:MASTER%는 이제 그만두라고 강하게 말했다…
		;1회째는 (들)물어 놓칠 것 같은 소리로
		CALL FADEOUT(4, "「그만두지 않아요」")
		CALL FADEIN(20, "「그만두지 않아요」")
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 무심코 뭔가 이야기를 시작했지만, 부끄러운 내용이었으므로
			;%CALLNAME%에는 관계없으면 강하게 말했다…
			PRINTFORMDW %CALLNAME:TARGET%조금 이마를 얻어맞았다
			PRINTFORMW 「%RAND_SPLIT("화내요? /관계없지 않아요/바보 같은 일 말하지 않는거야.")%너의 일인 것이니까, 나의 일이야」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 강한척 해 응했지만, 소리는 조금 떨고 있다…
			PRINTFORMW 「강한척 하는 너도, 사랑스러워요」
			PRINTFORMDW %CALLNAME:TARGET%는,%CALLNAME:MASTER%의 목덜미에 얼굴을 묻어, 목덜미를 빨아 왔다
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 강해 대답했다…
		PRINTFORMW 「아라, 위세가 좋네요?」
		PRINTFORMDW %CALLNAME:TARGET%가 미소짓는다
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%를 노려봐, 강해 응했다…
		PRINTFORMW %CALLNAME:TARGET%는 곤란한 것처럼 머리를 긁고 있다
		RETURN 1
	ENDIF
	
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM5, 빈정거린다
;─
;·ACT, 지금의기분을얘기한다/자신에게로의기분을얘기한다의 경우는 REACT분류:거부
;·그 이외의 경우는 REACT분류:태연하게 따르는이 된다
;·벌포인트 가산
;·REACT인상:나쁜인상
;·의존도 변화(-3, -1)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/합의를요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:줄/아이마스크/볼개그/매도/삼각목마
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K1_5
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 빈정거린다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 솔직한 기분을 말하려고 했지만,
			;왠지 부끄러워져, 야유 섞임에 응했다…
			PRINTFORMW 「이제(벌써) 한숨일까」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 작은 악의를 말에 혼합해 따끔 돌려주었다…
			;말 있고는 했지만, 기분을 해치지 않은가 끊임없이 신경쓰고 있다…
			PRINTFORML 「너답지 않네요. 뭐 좋아요」
			PRINTFORMW 「그 정도라면 허락해 준다」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 빈정거리면서도, 말하는 일에는 솔직하게 따라서 보였다…
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 여유를 보이는 것 같은 태도로 홀연히 따르면서,
		;야유 섞임에 응답했다…
		PRINTFORMW 「네네」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「아직도 여유가 있을 듯 하구나?」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM6, 더하드하게
;─
;·REACT분류:받아들인다
;·조교자의 소질, BASE:초조등에 의해 REACT파생 1, 「이 철면피의 암퇘지놈!」(이)가 된다
;·REACT파생 1의 경우는 벌포인트 가산
;·REACT인상:0에서멀어짐
;·의존도 변화(0, +3)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/상냥하게위로한다
;		벌이라고선언한다
;ACT분류:1:애무
;	ACT:손으로애무/가슴애무/입으로애무/키스/성기를서로켠다/파이즈리한다
;ACT분류:2:도구
;	ACT:C애무도구/니플캡
;ACT분류:3:성교
;	ACT:정상위
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:스팽킹/줄/아이마스크/볼개그/삼각목마
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K1_6
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=이 철면피의 암퇘지놈!)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 「이 철면피의 암퇘지놈!」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌SOURCE가 일정 이상
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && SOURCE:MASTER:쾌C + SOURCE:MASTER:쾌V + SOURCE:MASTER:쾌A + SOURCE:MASTER:쾌B > 10
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 기분을 누르면서, 좀 더 하고라고 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 반 자포자기가 되어, 좀 더 하드하게 하면 좋다고 말했다…
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 좀 더 굉장한 것 여겨지고 싶다고 했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감에 빠져, 좀 더 굉장한 일 하고라고 말했다…
			PRINTFORMW 「나는, 개만들기에 밖에 흥미없어요」
			PRINTFORMDW 활짝 개이는 쾌청의 하늘과 같이 실로 시원한 웃는 얼굴로, 그렇게 돌려주어졌다
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「아프지 않은 것일까… 그렇지 않으면, 아픈 것이 좋은 것일까?」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;쾌SOURCE가 일정 이상의 네스트를 개방하지 않는 경우는, 상황에 관련되지 않고 이 개소가 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 여러가지 기분을 정리하면서 말을 선택해,
			;%CALLNAME%로 된다면 좀 더 하드해도 좋다고 말했다…
			PRINTFORMW 「이상한 신경을 쓰지 마. 나는 소프트인 것이 좋아해」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 자포자기가 되어, 좀 더 하드하게 하면 좋다고 말했다…
			;말 있고는 했지만, 입술을 딱 닫아 어깨를 진동시키고 있다…
			PRINTFORMW 「등지지 말… 아요, 기분전환해?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 흥미 반으로, 더하드하게도 좋다고 말해 보았다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 좀 더 기분이 좋은 일을 해 주었으면 해져,
		;무심코 더하드하게라고 말해 버렸다…
		PRINTFORMW 「나는, 개만들기에 밖에 흥미없어요」
		PRINTFORMDW 활짝 개이는 쾌청의 하늘과 같이 실로 시원한 웃는 얼굴로, 그렇게 돌려주어졌다
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「좀 더 격렬해서, 아프지 않은 것일까? 그렇지 않으면 아픈 것이 좋은 것일까?」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM7, 용서를빈다
;─
;·REACT분류:용서를빈다. 파생했을 경우는 REACT분류:없음
;·조교자의 소질, BASE:이성, 아라이먼트에 의해 REACT파생 1, 「의견을 내다니 좋은 담력이군요!」(이)가 된다
;·턴방침이 이상의 경우는 파생 1이 되기 쉽다
;·파생 1의 경우는 벌포인트 가산
;·REACT인상:0에가까워짐
;·의존도 변화(0, +2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류:1:애무
;	ACT:애널애무/애널빨고/밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:애널바이브/애널비즈/페니스밴드
;ACT분류:3:성교
;	ACT:후배위/배면좌위/애널섹스
;ACT분류:4:수치
;	ACT:자위/비패개장/수치플레이/야외플레이/방뇨
;ACT분류:5:봉사
;	ACT:다리빨고강제
;ACT분류:6:가학
;	ACT:스팽킹/채찍/바늘/삼각목마/관장세트
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착/피스트퍽/애널피스트/양구멍피스트/공기
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:역강간
;
;TEQUIP:볼개그는 튕겨진다
;--------------------------------------------------
@KOJO_COM_K1_7
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=의견을 내다니 좋은 담력이군요!)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;ACT, 공기에서는 파생하지 않는다
	;=================================================
	;REACT파생 1, 의견을 내다니 좋은 담력이군요!
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신이 이상하게 되기 시작하고 있는 것이 무서워져,
			;%CALLNAME%에 이제(벌써) 허락해와 간원 했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 아첨하도록(듯이)%CALLNAME%를 올려보면, 이제(벌써) 허락해와 바랐다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 이제(벌써) 허락하고라고 말하기 시작했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 눈물을 머금어%CALLNAME%에 이제(벌써) 허락해 주세요간원 했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「허락하면 좋아?%RAND_SPLIT("그렇다면 어떻게 하면 좋은가, 아네요? /응후후… 어떻게 하지?")%」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM8, 기분좋게해줘
;─
;·REACT분류:받아들인다
;·조교자의 소질, BASE:이성, 아라이먼트에 의해 REACT파생 1, 「이 철면피의 암퇘지놈!」(이)가 된다
;·파생 1의 경우는 벌포인트 가산
;·REACT인상:0에서멀어짐
;·의존도 변화(0, +2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다/상냥하게위로한다/합의를요구한다
;ACT분류:1:애무
;	ACT:애널애무/애널빨고/밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:페니스밴드
;ACT분류:3:성교
;	ACT:
;ACT분류:4:수치
;	ACT:자위과시하고/수치플레이/야외플레이/방뇨
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;
;TEQUIP:볼개그는 튕겨진다
;-------------------------------------------------
@KOJO_COM_K1_8
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=이 철면피의 암퇘지놈!)
;-------------------------------------------------

;REACT파생 0는 사실상 COM 명칭 그대로 해석 해 줄 수 있으면 좋을까
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 이 철면피의 암퇘지놈!
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;쾌SOURCE가 일정 이상
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && SOURCE:MASTER:쾌C + SOURCE:MASTER:쾌V + SOURCE:MASTER:쾌A + SOURCE:MASTER:쾌B > 10
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 기분을 누르면서, 좀 더 하고라고 말했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 몹시 꾸짖을 수 있는 것보다는 좋다고 생각해, 이대로 하고라고 말했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 좀 더기분좋게해줘계속 이라고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감에 빠져, 좀 더 자주(잘) 해와 졸랐다…
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;쾌SOURCE가 일정 이상의 네스트를 개방하지 않는 경우는, 상황에 관련되지 않고 이 개소가 보완합니다
;본체측의 처리를 나누어지고 있으므로, 텐프레측도 일단 나누어 보았습니다만
;현시점에서는 내용에 그다지 차이가 없기 때문에, 이 개소만이라도 좋은 생각은 듭니다
;다만 이 바리안트, 지문이 단번에 증가하거나 바뀌거나 하므로 뭐라고도 말할 수 없는 곳
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 기분을 누르면서, 욕망에 정직하게
			;좀 더 기분 좋은 일을 되고 싶다고 했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 몹시 꾸짖을 수 있는 것보다는 좋다고 생각해,
			;어차피라면 기분 좋아지고 싶다고 했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는기분좋게해줘라고 말하기 시작했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 정욕의 포로가 된 것처럼, 좀 더 기분 좋은 일 해라고 호소했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「어떻게 하지? 응후후…」
			CASEELSE
				PRINTFORMW 「솔직하게 말해 주는 것은, 기뻐요」
		ENDSELECT
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;쾌락계 반응
;=====================================================================
;·쾌SOURCE나 특정의 PALAM가 트리거가 되어 발생한다
;·COM에 따라서는 ACT분류가 트리거가 된다
;-------------------------------------------------

;─
;COM10, 부끄러워한다
;─
;·REACT분류:소극적 따른다
;·벌포인트 가산
;·쾌SOURCE가 일정 이상의 경우는 파생 1, 쾌감에부끄러워한다가 된다
;·의존도 변화(+1, 0)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:성에대한이야기를한다/의상의변경을요구한다
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:수치
;	ACT:자위/비패개장/자위과시하고/수치플레이/야외플레이/방뇨
;ACT분류:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/가슴애무강제
;ACT분류:이상
;	ACT:방치플레이
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;※SOURCE:쾌C/쾌V/쾌A/쾌B, PALAM:치정이 트리거가 된다
;-------------------------------------------------
@KOJO_COM_K1_10
;-------------------------------------------------
;·TFLAG:REACT파생(0=상황에부끄러워한다/1=쾌감에부끄러워한다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 쾌감에 대해서부끄러워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 좀 더 기분 좋아지고 싶다고 생각하고 있는 자신을 의식해 버려,
			;뺨을 물들여 부끄러워했다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 쾌감이 주어지는 일에 강한 수치를 느껴
			;몸을 움츠러들게 해 부끄러워했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 복받치는 쾌감을 숨기지 못하고, 부끄러워해 얼굴을 돌렸다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감으로 소리를 흘려, 깜짝 놀란 것처럼 숨을 삼키면
		;시선을 피해 부끄러워했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;REACT파생 1, 쾌감에부끄러워한다를 썼을 경우는 REACT파생 0, 상황에부끄러워한다취급
;파생 1을 쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;이하에 나타내는 지문은 REACT파생 0의 것
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 부끄러운 모습을 볼 수 있는 것으로 이상한 기분이 되어 버려,
			;더욱 더 뺨을 물들여 부끄러워했다…
			;PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 자신을 욕보이는 시선에 견딜 수 없게 되어,
			;몸을 안도록(듯이)해 부끄러워했다…
			;PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(50) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 욕보여지고 있는 상황에 견딜 수 있지 못하고, 수치의 너무 얼굴을 돌렸다…
		PRINTFORMW 「여기 향해?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(50) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%가 빠는 것 같은 시선을 느껴
		;무심코 시선을 피해 부끄러워했다…
		PRINTFORMW 「초심인 너도 사랑스러워요」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「부끄러워한다너도 나쁘지 않아요」
		SIF IS_CONDITION(TARGET, "지루함")
			PRINTFORMW 「그렇지만 그렇구나. 좀 더 솔직해져 주면, 나는 기뻐요」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM11, 싫어한다
;─
;·REACT분류:소극적 따른다
;·REACT파생 1는 ACT 방해가 되어 REACT분류:날뛴다취급
;·의존도 변화(-1, -1)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:의상의변경을요구한다
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/풋잡
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;
;※SOURCE:쾌C/쾌V/쾌A/쾌B, PALAM:치정이 트리거가 된다
;-------------------------------------------------
@KOJO_COM_K1_11
;-------------------------------------------------
;·TFLAG:REACT파생(0=싫어한다/1=싫어해 ACT를 방해)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 싫어해 방해한다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 순간에 싫어하는 손을 대어 버렸지만,
			;%CALLNAME%에 미안한 것 같은 표정을 보였다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 힘껏의 기력을 쥐어짜, 싫어해%CALLNAME%로부터 피했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 싫어해 몸을 비틀어,%CALLNAME%의 방해를 했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 싫어해%CALLNAME%를 밀쳤다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;파생 1을 썼을 경우는 REACT파생 0, 싫어한다가 ACT는 방해하지 않고 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 내심에서는 그렇게 그렇지만도 않다고 생각하면서도,
			;부끄럽기 때문에 싫어해 보였다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 힘껏의 기력을 쥐어짜, 떨리는 소리로 싫다고 말했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 고개를 저어 싫어했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 그런 것 싫다고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「싫다고 해도 그만두어 줄 수 없어요」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM12, 서투르다고매도한다
;─
;·REACT분류:태연하게 따른다
;·REACT파생 1(강한척 한다)는 REACT분류:소극적 따르는 취급
;·벌포인트 가산
;·파생 0, 새겨 내린다(REACT인상:약간나쁜인상)
;·파생 1, 강한척 한다(REACT인상:나쁜인상)
;·의존도 변화(-2, -3)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:성교
;	ACT:정상위/후배위/대면좌위/배면좌위
;
;※TFLAG:ACT분류(1=애무/3=성교)가 트리거
;-------------------------------------------------
@KOJO_COM_K1_12
;-------------------------------------------------
;·TFLAG:REACT파생(0=새겨 내린다/1=강한척 한다)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 강한척 해(쾌감을 느끼면서)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는 허덕이는 소리 섞임의 뻔히 보임의 허세로,%CALLNAME%에 서투르다고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;착실하게 쾌감을 느끼면서도 %CALLNAME:MASTER%는 서투르다고 우겼다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감에 얼굴을 비뚤어지게 하면서도, 이런 것은 느끼지 않는다고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;파생 1을 썼을 경우는 REACT파생 0, 새겨 내리는(실제로 서투른 경우) 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는 힘껏의 허풍으로,%CALLNAME%에 서투르다고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:TARGET%의 미숙한 테크닉에, %CALLNAME:MASTER%는 서투르다고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 이것이라면 자신이 능숙하다고 말했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「서투른 일도 그 안에, 그리워지는 날이 오는 것」
				PRINTFORMW 「지금은, 지금의 나를 즐겨 줘」
				;쓰고 있어 눈치챈 부메랑. 눈치챈 %CALLNAME:TARGET%씨. 스스로 자신을 웃는다
				CALL FADEOUT(4, "키득")
				PRINTFORMW 「나도 그렇게 해요」
			CASEELSE
				PRINTFORMW 「지금부터 능숙해지기 때문에 좋은거야」
		ENDSELECT
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM13, 겁없게웃는다
;─
;·REACT분류:태연하게 따른다
;·벌포인트 가산
;·REACT인상:나쁜인상
;·의존도 변화(-2, -3)
;-------------------------------------------------
;·트리거가 되는 행동, 소스, 파라미터
;·SOURCE:쾌C/쾌V/쾌A/쾌B, SOURCE:아픔/구속, ACT분류:가학
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그/매도/삼각목마/관장세트
;-------------------------------------------------
@KOJO_COM_K1_13
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM13, 겁없게웃는다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;PRINTFORML %CALLNAME:MASTER%는 자신은 아직 제정신이라고 확인하도록(듯이) 웃어 보였다…
			;뉘앙스를 잘못 잡고 있는 예감이 하므로 comment out
			PRINTFORMW 「웃을 수 밖에 없을까?」
			PRINTFORMW 「그런 때는 좀 더, 생긋 웃으세요」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 들러붙은 것 같은 미소를 돌려주었지만, 눈이 헤엄치고 있다…
			PRINTFORMW 「여기 보세요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 일부러 %CALLNAME:TARGET%를 도발하도록(듯이) 웃었다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%가 조교에 고심하고 있는 것을 조소했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;아라이먼트와 플레이어의 상상에 의지해, 필요이상으로 묘사하지 않는 것이 좋은 것 같다
		PRINTFORMDW %CALLNAME:TARGET%는 미소지어 돌려주었다
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM14, 쾌감을참는다
;─
;·REACT분류:소극적 따른다
;·일정 조건으로 REACT파생 1 「애원(REACT분류:용서를빈다)」취급
;·파생 1의 경우 REACT인상는 0에가까워짐
;·의존도 변화(+1, -1)
;-------------------------------------------------
;※트리거가 되는 행동, 소스, 파라미터
;·SOURCE:쾌C/쾌V/쾌A/쾌B
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/가랑이
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트
;-------------------------------------------------
@KOJO_COM_K1_14
;-------------------------------------------------
;·TFLAG:REACT파생(0=내구/1=애원)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 애원 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	;고소비 개소에 하는대로가 되어 버리는 것 같은 언동을 배치하는 것은 어떨까…
	IF 구상기입체크(50) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 쾌감으로 제정신을 잃는 일에 무서워해, 벌써 허락하고라고 말했다…
			PRINTFORMW 「무리하고 있어요―」
			PRINTFORMW 「이상하게 되어 버려도 좋아요. 내가 받아 들여 주어요」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 쾌감을 받아들이려고 하지 않고, 그만두어와 간원 했다
			PRINTFORMW 「그만두었으면 좋은거야?」
			CALL FADEIN(4, "「나,」")
			CALL FADEOUT(4, "「나는」")
			CALL FADEIN(4, "「그만두고 싶지 않아요」")
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(50) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 강한 쾌감을 참으면서, 이제(벌써) 이런 일 그만두어와 간원 했다…
		PRINTFORMW 「싫어요, 그만두어는 주지 않아요 원」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(50) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 쾌감을 참으면서 허가를 바라고 있다…
		PRINTFORMW 「자연히(에) 참아져 버리는 것도, 뭔가 화가 나요…」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;파생 1을 썼을 경우는 REACT파생 0, 쾌감을 인내 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(50) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 욕망을 뿌리쳐, 쾌감을 견디려고 했다…
			PRINTFORMW 「꽉 눈을 감아%RAND_SPLIT("괴롭혀 신경쓰고 있다/하아하아 하고 있다")%너 꽤 그림이 되어 있어요」
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 쾌감을 받아들이려고 하지 않고 참고 있다…
			PRINTFORMW 「이래서야 싫어?」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 필사적으로 쾌감을 참았다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 분명한 쾌감을 느끼면서도, 그것을 참으려고 하고 있다…
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「참아도 쓸데없어요? 너가%RAND_SPLIT("항복해 준다/이제(벌써) 안된다는 것은 된다")%까지 계속하는 것」
		PRINTFORMW 「이변은 내가%RAND_SPLIT("해결한다/이긴다")%까지 계속되는거야」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM15, 쾌감을받아들인다
;─
;·COM16, 기분좋다！와 거의 같음. 차이는 의존도의 변화만
;·조교 대상이 의존하기 쉽다
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도 변화(+1, +5)
;-------------------------------------------------
;※트리거가 되는 행동, 소스, 파라미터
;·SOURCE:쾌C/쾌V/쾌A/쾌B
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/가랑이
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;-------------------------------------------------
@KOJO_COM_K1_15
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;쾌감에 방심해 버리면, %CALLNAME:MASTER%의 마음은 순식간에 녹기 시작했다
		PRINTFORMW 「무리하고 있네요. 알아요 너의 일인걸」
		PRINTFORMW 「거기까지 하지 않아도 괜찮은 것이지만, 뭐 좋아요」
		;전회 ACT 추가 묘사
		IF 구상기입체크(100) && IS_NOWACTNAME("정상위시킨다/대면좌위시킨다")
			CALL FADEOUT(4, "좋아 좋아")
			PRINTFORMW 「나를 위해서(때문에) 무리했어요」
			CALL FADEOUT(4, "팡팡")
			PRINTFORMW 「노력했군요」
			PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 등을 어루만져 주었다
		ENDIF
		IF 구상기입체크(100) && IS_NOWACTNAME("키스")
		;바라지 않는 행복감에 마음이 채워지는 것을 느껴 눈에 눈물을 머금었다
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 달콤한 교성을 주면서 쾌감을 받아들였다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;본심은 저확률로 누설한다
		SIF !RAND:3
			PRINTFORMW 「분명하게, 나의 일도 생각하세요?」
		PRINTFORMW 「너만 기분 좋아지는 것은, 안 되는 것이니까」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「좋은 것 같은 소리 내 버려♪군침 나와 있어요? 이제(벌써)…%RAND_SPLIT("어쩔 수 없네요/한심하네요/안되구나")%」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM16, 기분좋다！
;─
;·COM15, 쾌감을받아들인다와 거의 같음. 차이는 의존도의 변화만
;·주인님의 의존을 권하기 쉽다
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도 변화(+4, +1)
;-------------------------------------------------
;※트리거가 되는 행동, 소스, 파라미터
;·SOURCE:쾌C/쾌V/쾌A/쾌B
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스/성기를서로켠다
;		파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:수치
;	ACT:자위/비패개장
;ACT분류:봉사
;	ACT:파이즈리/가랑이
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;-------------------------------------------------
@KOJO_COM_K1_16
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;쌍방 정욕 성교 봉사
	;=================================================
	IF 구상기입체크(50) && V_SEX(MASTER) && IS_CONDITION(MASTER, "정욕") && IS_CONDITION(TARGET, "정욕")
		PRINTFORM 「도 취할 수 있고─의 보지로 자지 쓱쓱 해, 좀 더 기분 좋아져
		CALL HEARTBW
		PRINTFORMDW %CALLNAME:TARGET%는 군침을 흘리면서 필사적으로 허리를 흔들고 있다
		RETURN 1
	ENDIF
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는 아첨하도록(듯이) 기분 좋은 것 같은 소리로 허덕이고 있다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 쾌락을 얻을 수 있는 행복을 감사하는것 같이 소리에 냈다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 달콤한 소리로 쾌감과 기쁨을%CALLNAME%에게 전했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		[SKIPSTART]
		;묘사 가필에 대해 comment out
		PRINTFORMW 「응… 계속해요?」
		[SKIPEND]
		SELECTCASE RAND:3
			CASE 0
				CALL FADEOUT(4, "「응…」")
			CASE 1
				CALL FADEOUT(8, "(기분이 좋다…)")
			CASEELSE
				CALL FADEOUT(4, "「에헤헤…」")
		ENDSELECT
		PRINTFORMW 「계속해요?」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;성교계 반응 COM20 접수대
;=====================================================================
;·ACT분류:성교/성적봉사, ACT:파이즈리한다/성기를서로켠다등이 트리거가 되어 발생
;-------------------------------------------------

;─
;COM20, 허리를흔드는
;─
;·주도권(SEX_INITIATIVE)에 의해 내용이 다른 COM
;-------------------------------------------------
;·ACT분류가 성교 봉사, ACT:승마위의 경우는 주도권은 조교 대상(SEX_INITIATIVE(MASTER))
;·REACT분류:적극적 따른다
;·의존도 변화(+5, +3)
;-------------------------------------------------
;·ACT분류가 성교, ACT:역강간/성기를서로켠다/파이즈리한다의 경우는 주도권은 조교자
;·※성기를서로켠다/파이즈리한다는 SEX_INITIATIVE(TARGET)에서는 참조 할 수 없다
;·REACT분류:받아들인다
;·의존도 변화(+4, +8)
;-------------------------------------------------
;·REACT인상:좋은인상
;-------------------------------------------------
;·초조하게해중은 실행 불가
;·ACT:파이즈리한다의 경우, 조교자가 빈유라고 튕겨진다
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:성기를서로켠다/파이즈리한다
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:성적봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다/역강간
;-------------------------------------------------
@KOJO_COM_K1_20
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;주도권은 조교 대상(ACT분류가 성교 봉사 /ACT:승마위의 경우)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && SEX_INITIATIVE(MASTER)
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME%의 환심을 얻기 위해, %CALLNAME:MASTER%는 힘껏 허리를 사용했다…
		PRINTFORMW 「노력해 봉사해 주는 것은 기뻐요」
		PRINTFORMW 「그렇지만 말야? 너도 기분 좋아져 주지 않으면…」
		CALL FADEIN(16, "「안 되는 것이니까」")
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동/자연스러운 행동과 통합
	;=================================================
	;자연스러운 행동과 통합
	;IF 구상 기입 체크(100) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는%CALLNAME%를 기쁘게 하려고 허리를 사용하고 있다…
	IF 구상기입체크(100) && GROUPMATCH(GET_COM_RANK(), "자연스러운 행동", "무리가 없는 행동")
		;=================================================
		;쌍방 정욕 기분이 좋다! 레이무씨
		;=================================================
		IF IS_CONDITION(MASTER, "정욕") && IS_CONDITION(TARGET, "정욕")
			PRINTFORM 「개─기분 좋다… 개─기분 있어라…
			CALL HEARTBW
			;상황은 삽입중에 대해 「여기」는 부적절. 레이무가 간역의 경우도 여기가 되는 모양.
			;PRINTFORM 「-의 여기… 좀 더%RAND_SPLIT("쑤컹쑤컹/동글동글/쑤컹쑤컹/철퍽철퍽")%해…
			PRINTFORM 「-의 저기. 좀 더,%RAND_SPLIT("쑤컹쑤컹/동글동글/쑤컹쑤컹/철퍽철퍽")%해
			CALL HEARTBW
		;=================================================
		;당신이 정욕 강하게 대답하는 레이무씨
		;=================================================
		ELSEIF IS_CONDITION(MASTER, "정욕")
			PRINTFORMW 「%RAND_SPLIT("이! /우쭐해져/번창해 버려")%… 좀 더 상냥하고… 하세요…」
			PRINTFORMW 「(듣)묻지 않네요…%RAND_SPLIT("아 이제(벌써)/이 바보")%」
			PRINTFORMW 「너로 되는 대로라고 하는 것도, 무엇인가%RAND_SPLIT("적이군요/마음에 들지 않아요/나답지 않을까요")%…」
		;=================================================
		;%CALLNAME:TARGET%정욕 레이무씨 받고 기분 좋게 해
		;=================================================
		ELSEIF IS_CONDITION(TARGET, "정욕")
					PRINTFORM 「응. 너의 좋아하게 움직여도 좋으니까
					CALL HEARTBW
					PRINTFORMW 「%RAND_SPLIT("/%CALLNAME:TARGET%의 보지")%%RAND_SPLIT("/자지로")%기분 좋게 해」
		;=================================================
		;그 이외
		;=================================================
		ELSE
			SELECTCASE RAND:2
				;%CALLNAME:TARGET%씨가 기분 좋게 해
				[SKIPSTART]
				;기입 개소 미스의 예감. comment out
				CASE 0
					PRINTFORM 「응… 너의 좋아하게 움직여도 좋으니까」
					PRINTFORMW 「기분 좋게 해
					CALL HEARTBW
				[SKIPEND]
				CASE 1
					[SKIPSTART]
					;오른쪽 클릭에서는 표시되고 있을 뿐이 되므로 기대가 빗나간다. 역. comment out
					;오른쪽 클릭 해 2행째를 놓친다. 오른쪽 클릭 해 본다. 두 번 즐긴다.
					;템포의 조정
					;CALL FADEIN(20, " 「좀 더, 좀 더!」")
					;CALL FADEOUT(20, @" 「%CALLNAME:TARGET%로! 기분 좋아져!」")
					[SKIPEND]
					CALL FADEOUT(8, "「좀 더, 좀 더 움직여!」")
					CALL FADEIN(8, @"「%CALLNAME:TARGET%로! 기분 좋아져!」")
				;빈정거린다레이무씨
				CASEELSE
					PRINTFORMW 「완전히 번창해 버리며…♪」
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 허리사용으로 상대를 느끼게 하는 일에 기쁨을 찾아내고 있다…
		PRINTFORMW 「필사적으로 허리 흔들어 버려… 후후」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;SEX_INITIATIVE(MASTER)를 쓰지 않았던 경우는 여기서 보완됩니다만 범위가 넓기 때문에
;최저한, TEQUIP:성교중/성교봉사중, IS_COMGRONAME/IS_NOWACTNAME든지로 나누어 쓰지 않으면 정합성은 무너지는 일이 될까하고
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우(ACT분류가 성교 /ACT:역강간/성기를서로켠다/파이즈리한다의 경우)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%가 허리를 아무리 털어도, 자신의 쾌감이 강해질 뿐이었다…
		PRINTFORM 「응후후… 이봐요 힘내라 힘내라
		CALL HEARTBW
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME%의 신체에 빠지도록(듯이), %CALLNAME:MASTER%의 허리는 마음대로 움직여 쾌감을 탐낸다…
		PRINTFORMW 「이제(벌써)%RAND_SPLIT("조금/조금")%%RAND_SPLIT("상냥하고/천천히")%」
		PRINTFORMW 「허리, 멈추지 않아? 완전히… 어쩔 수 없기 때문에」
		PRINTFORMW 「…1회 말해 버리면 침착할까?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 자신이 기분이 좋게 여차저차허리를 흔들고 있다…
		PRINTFORMW 「열중해 버려」
		PRINTFORM 「%RAND_SPLIT("후후/네에에/… 후후")%
		CALL HEARTBW
		PRINTFORMW 「나의 일도 기분 좋게 하세요?」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「함께 기분 좋아집시다
		CALL HEARTBW
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM21, 안에내！
;─
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·조교 대상이 남자의 경우는 튕겨진다
;·의존도 변화(+10, +10)
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;-------------------------------------------------
@KOJO_COM_K1_21
;-------------------------------------------------
;·TFLAG:REACT파생(0=V에 내/1=A에 내)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM21, 안에내！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 범해져 쾌락으로 마음을 어지럽혀지면서
		;자포자기가 된 것처럼, 이제(벌써) 안으로 좋은, 안에 내고라고 말했다…
		PRINTFORMW 「좋으면 사양은 하지 않아요」
		PRINTFORMW 「응… 욕구를 말하면 좀 더,%RAND_SPLIT("마음속으로부터/기쁜 듯한")%의 것을 (듣)묻고 싶어요」
		RETURN 1
	ENDIF
	
	;정상위, 대면좌위의 경우는 지문이 변화
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는 행동"
		;정상위/대면좌위의 경우	%CALLNAME:MASTER%는 %CALLNAME:TARGET%의 허리를 양 다리로 단단히안아, 안에 내와 속삭였다…
		;그 외의 경우			%CALLNAME:MASTER%는 관철해지는 쾌감에 빠져, 안에내！라고 외쳤다…
		PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%를 껴안으면%RAND_SPLIT("허리의 움직임을 빨리 했다/%CALLNAME:MASTER%의 자궁에 첨단을 강압했다/기쁜 듯한 표정을 했다")%」
		;리턴 없음으로 그 외·조건 없음 개소에 연결한다
		;RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 범해지는 쾌감과 정욕에 지배되어
		;황홀한 기분의 표정으로 어떻게 되어도 괜찮은, 안에 갖고 싶다고 외쳤다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RAND:3
			CASE 0
				PRINTFORM 「나의 아기. 갖고 싶은거야? 응후후
				CALL HEARTBW
			CASE 1
				PRINTFORM 「
				CALL HEARTBW
				;PRINTFORMW」
			CASEELSE
				PRINTFORMW 「아 이제(벌써)! 너무 좋아!」
		ENDSELECT
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM22, 안에내지마！
;─
;·REACT분류:용서를빈다
;·REACT파생 1(안에 낸다)의 경우는 벌포인트 추가. REACT분류:없음
;·REACT인상:나쁜인상
;·의존도 변화(0, -10)
;·인내 플래그는 소멸한다
;·조교 대상이 남자의 경우는 튕겨진다
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;-------------------------------------------------
@KOJO_COM_K1_22
;-------------------------------------------------
;·TFLAG:REACT파생(0=안에 내지 않는다/1=안에 낸다)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM22, 안에내지마！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;v1. 010 현재, 파생해도 지문은 공통
	;=================================================
	;REACT파생 1, 안에 낸다
	;=================================================
	IF 구상기입체크(50) && TFLAG:REACT파생 == 1
		PRINTFORMDW %CALLNAME:TARGET%는 최안쪽까지 돌진하면서 허리를 멈추었다
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 수치도 세상소문도 없고%CALLNAME%를 요구하고 싶은 감정을 열심히 뿌리쳐,
			;중만은 안돼, 밖에 내라고 외쳤다…
			PRINTFORMW 「이상한 곳에서 참아 버려… 안심해」
			PRINTFORM 「책임을 져 준다
			CALL HEARTBW
		ELSE
			;%CALLNAME:MASTER%는%CALLNAME%의 분노를 사는 것을 무서워하면서도,
			;교배되는 것을 무서워해, 안은 그만두어, 내서는 안돼와 비통하게 외쳤다…
			SELECTCASE RAND:3
				CASE 0
					PRINTFORM 「%RAND_SPLIT("당황해 버려/허둥지둥 해 버려")%, 사랑스럽기 때문에
					CALL HEARTBW
				CASE 1
					;남 %CALLNAME:MASTER%의 엉덩이도 가미해, 질우치오모테기를 피한다
					PRINTFORM 「안에 내지 않으면, 할 수 없잖아
					CALL HEARTBW
				CASEELSE
					PRINTFORM 「좋으니까 임신하세요
					CALL HEARTBW
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 관철해지는 쾌감을 참으면서, 안에내지마！라고 외쳤다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 힘 없고%CALLNAME%에 범해져 축 늘어지면서도
		;중만은 그만두어, 허락해와 애원 했다…
		PRINTFORMW 「바보같구나. 여기서 그만둔다면 이런 일 시작하지 않아요」
		PRINTFORMW 「아무래도 갖고 싶은거야」
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「바보같구나. 여기서 그만둔다면 이런 일 시작하지 않아요」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;고통계 반응
;=====================================================================
;·가학계 행동이나 삽입을 수반하는 각종 행동으로 볼 수 있다
;·v1. 010 현재, 고통계에 의존도 변화의 처리 없음
;-------------------------------------------------

;─
;COM30, 아픔을참는다
;─
;·REACT분류:소극적 따른다
;·REACT인상:약간좋은인상
;·SOURCE:아픔이 없는 경우는 튕겨진다
;-------------------------------------------------
;·트리거가 되는 행동, 소스, 파라미터
;·SOURCE:아픔/구속
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:가슴애무/입으로애무/애널애무/애널빨고/키스/파이즈리한다
;ACT분류:도구
;	ACT:바이브/애널바이브/애널비즈
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트
;-------------------------------------------------
@KOJO_COM_K1_30
;-------------------------------------------------
;·TFLAG:REACT파생(0=아픔을 인내/1=아프지만기분좋다！)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30_1, 파생:아프지만기분좋다！
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 여러가지 감정을 삼켜, 쾌감과 고통을 동시에 참았다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 아픔안에 쾌감을 의식해, 무서워하면서 양쪽 모두를 참았다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;쾌감과 고통이 동시에 덮쳐, %CALLNAME:MASTER%는 흐려진 소리를 흘렸다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 아픔을 감추기 (위해)때문에, 동시에 복받치는 쾌감에 달라붙었다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 1을 개방했을 경우는 파생 0, 아픔을 인내 취급
;------------------------------------------------
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 고통을 견디면서,%CALLNAME%의 행위를 받아들였다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 고민의 신음을 흘리면서 아픔을 참았다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 아픔을 참았다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ACT:이라마치오시에는 지문이 변화. 내용은 이하와 같다
	;%CALLNAME:MASTER%는 목 안까지 후벼파지는 괴로움에 오열을 흘리면서도,%CALLNAME%의 행위를 받아들였다
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;그 외 ACT의 경우의 지문
		;%CALLNAME:MASTER%는 이빨을 이를 악물어 고통을 참았다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「좋으니까 조복 되어 버리세요」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM31, 비명을올린다
;─
;·REACT분류:소극적 따른다
;·REACT인상:0에서멀어짐
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:회화
;	ACT:엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류:애무
;	ACT:가슴애무/입으로애무/애널애무/애널빨고/키스/파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:바이브/애널바이브/애널비즈
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그/매도/삼각목마/관장세트
;ACT분류:이상
;	ACT:피스트퍽/애널피스트/양구멍피스트/공기
;-------------------------------------------------
@KOJO_COM_K1_31
;-------------------------------------------------
;·TFLAG:REACT파생(0=아픔에 의한 비명/1=공포에 의한 비명)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 공포에 의한 비명
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 1
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 아첨하는 것 같은 비명을 올렸다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 무서워한 것처럼 비명을 올려 보였다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 공포를 느껴 비명을 올렸다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 이상한 공포를 느껴 비명을 올렸다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;REACT파생 1를 썼을 경우는 REACT파생 0, 아픔에 의한 비명 취급
;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;그 외의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는%CALLNAME%가 좋아하는 고통의 비명을 자연히(에) 소리에 냈다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 비명을 올려 고통에 몸부림쳐 보였다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 아픔을 견딜 수 있지 못하고 비명을 올렸다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 아픔의 너무 비통한 외침을 올렸다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「아 이제(벌써), 시끄럽네요」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM32, 날뛴다
;─
;·파생에 의해 날뛰고 성공(REACT분류:날뛴다), 날뛰고 실패(REACT분류:소극적 따른다)로 나누어진다
;·날뛰고가 통과하면 도구계/성교계/성교 봉사계 커맨드를 방해한 취급이 된다
;·벌포인트 추가
;·REACT인상:약간나쁜인상
;-------------------------------------------------
;선택 가능한 조건…
;ACT분류:애무
;	ACT:손으로애무/가슴애무/입으로애무/애널애무/애널빨고/키스
;		성기를서로켠다/파이즈리한다/풋잡한다
;ACT분류:도구
;	ACT:로터/바이브/애널바이브/애널비즈 /C애무도구/니플캡
;ACT분류:성교
;	ACT:정상위/후배위/승마위/대면좌위/배면좌위/애널섹스
;ACT분류:봉사
;	ACT:이라마치오
;ACT분류:가학
;	ACT:스팽킹/채찍/바늘/줄/아이마스크/볼개그
;ACT분류:휴게
;	ACT:치료한다
;ACT분류:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_32
;-------------------------------------------------
;·TFLAG:REACT파생(0=날뛰고 성공/1=줄구속에 의한 날뛰고 실패/2=제지에 의한 날뛰고 실패)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;REACT파생 2, 제지에 의한 날뛰고 실패(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 2
		;%CALLNAME:MASTER%는 날뛰어 저항하려고 했지만, %CALLNAME:TARGET%에 억제 당했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT파생 1, 줄구속에 의한 날뛰고 실패(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		;속박된 %CALLNAME:MASTER%는 날뛰어 저항하려고 했지만, 줄이 한층 더 먹혀들어 다만 아픈 것뿐이다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1으로 2를 썼을 경우는 파생 0, 날뛰고 성공(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 파생에 관련되지 않고 이 개소를 보완합니다
	;※파생 마다의 내용의 차이가 비교적 크기 때문에, 나누어 쓰는 것이 무난하다라고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		;%CALLNAME:MASTER%는 날뛰어 %CALLNAME:TARGET%의 행동을 방해했다. %CALLNAME:TARGET%는 꽤 화나 있는 것 같다…
		PRINTFORMW 「얌전하게 하지 않으면, 얌전하게 시켜요?」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM33, 무서워한다
;─
;·REACT분류:용서를빈다
;·일정 조건으로 분노를 사, 벌포인트 추가
;-------------------------------------------------
;·트리거 이벤트제
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다
;		엄격하게위협한다/벌이라고선언한다/고함친다/기분나쁘게웃는다
;ACT분류:1:애무
;	ACT:풋잡한다/밀어넘어뜨린다
;ACT분류:2:도구
;	ACT:애널바이브/애널비즈
;ACT분류:3:성교
;	ACT
;ACT분류:4:수치
;	ACT:수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:
;ACT분류:6:가학
;	ACT:줄/아이마스크/볼개그/매도/관장세트
;ACT분류:7:이상
;	ACT:방치플레이/공기 플레이/페니스밴드장착
;ACT분류:9:휴게
;	ACT:쉬게한다/영양제를먹인다/치료한다
;ACT분류:10:성교 봉사
;	ACT:
;-------------------------------------------------
@KOJO_COM_K1_33
;-------------------------------------------------
;·TFLAG:REACT파생(0=-응/1=두려움에 대한 초조)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 무서워한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;ACT, 공기에서는 파생하지 않는다
	;=================================================
	;파생:두려움에 대한 초조
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는%CALLNAME%보다 오히려 이상하게 될 것 같은 자신에게 무서워하고 있는 것 같다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 분한 생각을 삼켜, 얌전하게 공포의 시선을 향했다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 공포를 느끼고 무서워하고 있다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 작은 동물과 같이 무서워하고 있다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「무서워하지 않아도 괜찮아요」
		PRINTFORMW 「무서워해도… 하는 것은 함께인 것이니까」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;봉사계 반응 COM40 접수대
;=====================================================================
;·조교자에게로의 애무를 포함한 행동으로 볼 수 있다
;·ACT분류:봉사+트리거 이벤트제
;-------------------------------------------------

;─
;COM40, 거부한다
;─
;·REACT분류:거부
;·REACT인상:약간나쁜인상
;·벌포인트 추가
;·의존도 변화(-2, -4)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:의상의변경을요구한다/합의를요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:영양제를먹인다
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/배면좌위시킨다/애널섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_40
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM40, 거부한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 미혹을 보이면서도 요구에는 응하지 않았다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 압도되면서도 요구는 삼키지 않았다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 %CALLNAME:TARGET%의 요구를 거부했다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 강한 의지로 요구를 퇴짜놓았다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;이변 해결시의 강경세 김
		PRINTFORMW 「싫다고 말해지고 있을 것 같습니까와 물러나는 만큼, 단정하지 않아요」
		PRINTFORMW 「무리해서라도 해요!」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM41, 소극적한다
;─
;·REACT분류:소극적 따른다
;·REACT파생 1는 "소극적인의가 마음에 들지 않는다"되어 벌포인트 추가
;·REACT인상:약간좋은인상
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:의상의변경을요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/애널섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_41
;-------------------------------------------------
;·TFLAG:REACT파생 == 0, 소극적이지만 좋은가
;·의존도 변화(-3, +2)
;-------------------------------------------------
;·TFLAG:REACT파생 == 1, 소극적인의가 마음에 들지 않는다
;·의존도 변화(-5, 0)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;파생 1, 소극적인의가 마음에 들지 않는다(벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		;%CALLNAME:MASTER%의 소극적인 자세에,%CALLNAME%는 불만인 것 같다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자신으로부터 행동하는 용기가 없는 것 같다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 분한 듯이 하면서도 거역하려고는 하지 않는다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 어떻게든%CALLNAME%가 말하는 대로 하고 있다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는%CALLNAME%에 역네 없다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW 「손, 떨고 있어요?」
			CASEELSE
				PRINTFORMW 「응… 좀 더 빠졌으면 좋네요」
		ENDSELECT
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM42, 적극적한다
;─
;·REACT분류:적극적 따른다
;·REACT인상:좋은인상
;·인내 플래그 해소
;·의존도 변화(+4, +2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:의상의변경을요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/애널섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_42
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 자기보다%CALLNAME%를 우선하려고 하고 있다…
			PRINTFORMW 
		;실은 싫어
		ELSE
			;%CALLNAME:MASTER%는 의식해 기특하게 행동하려고 하고 있는 것 같다…
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 진행되어%CALLNAME%에 힘쓰고 있다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%는 어쨌든%CALLNAME%를 기쁘게 하고 싶은 것 같다…
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「그럴 기분이 들어 준 것 같구나」
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM43, 애정을담아준다
;─
;·일정 이상의 호감도, 조교레벨이 필요
;·REACT분류:적극적 따른다
;·REACT인상:좋은인상
;·인내 플래그 해소
;·반발 각인이 있으면 튕겨진다
;·의존도 변화(+6, +10)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/애널섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_43
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을담아준다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는 평정을 가장하고 있지만, 기쁜 듯한 표정을 숨길 수 없다…
		PRINTFORMW 「지금의 너, 재미있는 얼굴 하고 있어요? 응후후♪」
		;손가락이라도 볼에서도 자지에서도, 통합성이 무너지지 않도록
		PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 뺨을 동글동글 밀어넣어 왔다
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(0) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 넋을 잃은 얼굴로 정중하게 명령을 구사할 수 있었다…
		;소녀 퇴고중
		;PRINTFORM 「응후…
		;CALL HEARTBW
		CALL FADEOUT(8, "「저기?」")
		CALL FADEIN(8, "「손」")
		CALL FADEIN(8, "「잡아」")
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%의 태도는 사랑스러운 사람에 힘쓸 때의 그것 그 자체였다…
		PRINTFORMW 「%RAND_SPLIT("응후후…♪/기분이 좋다/따뜻함")%」
		PRINTFORMDW %CALLNAME:TARGET%는 기분을 느슨한,%CALLNAME:MASTER%에 몸을 맡기고 있다
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM44, 태연하게한다
;─
;·REACT분류:태연하게 따른다
;·REACT인상:약간좋은인상
;·의존도 변화(+2, -2)
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:의상의변경을요구한다
;ACT분류:1:애무
;	ACT:
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:승마위
;ACT분류:4:수치
;	ACT:자위/방뇨
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/파이즈리/가랑이/다리빨고강제/이라마치오/가슴애무강제
;ACT분류:6:가학
;	ACT:
;ACT분류:7:이상
;	ACT:
;ACT분류:9:휴게
;	ACT:
;ACT분류:10:성교 봉사
;	ACT:정상위시킨다/후배위시킨다/대면좌위시킨다/후배위시킨다/애널섹스시킨다
;-------------------------------------------------
@KOJO_COM_K1_44
;-------------------------------------------------
;·REACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:REACT파생
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;실은 그렇지만도 않다
		IF COMCOR_POSI()
			;%CALLNAME:MASTER%는 평상심을 유지하려 하고 있다…
			PRINTFORMW 「너의 열심인 모습도 나쁘지 않지만…」
			PRINTFORMW 「응… 좀 더 솔직해져도 좋아요?」
		;실은 싫어
		;ELSE
			;%CALLNAME:MASTER%는 무리를 하면서도 평정을 가장하고 있다…
		;	PRINTFORMW 
			RETURN 0
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는%CALLNAME%를 기쁘게 해 환심을 권하려고 하고 있다…
		PRINTFORMW 「좀 더 걸근거려도 좋아요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%에는%CALLNAME%의 모습을 살필 뿐(만큼)의 여유가 있는 것 같다…
		PRINTFORMW 「담담하게 되는 것도, 뭔가 적이군요…」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	;IF 구상 기입 체크(0)
	;	PRINTFORMW 
	;	RETURN 1
	;ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;=====================================================================
;그 외의 반응 COM50 접수대
;=====================================================================
;·특정의 장면에서 발생하는 이상한 행동
;-------------------------------------------------

;─
;COM50, 자위하기시작한다
;─
;·REACT분류:받아들인다
;·파생 1, 마음대로 자위 한데의 경우는 벌포인트 추가
;·REACT인상:나쁜인상
;·인내 플래그 해소
;-------------------------------------------------
;·트리거 이벤트제
;-------------------------------------------------
;·줄로 속박되고 있으면 안돼
;·벌써 자위하고 있다, 성교중, 성교봉사중은 튕겨진다
;-------------------------------------------------
;트리거하는 행동이나 상황…
;ACT분류:0:회화
;	ACT:지금의기분을얘기한다/자신에게로의기분을얘기한다/성에대한이야기를한다
;ACT분류:1:애무
;	ACT:키스
;ACT분류:2:도구
;	ACT:
;ACT분류:3:성교
;	ACT:
;ACT분류:4:수치
;	ACT:자위과시하고/수치플레이/야외플레이
;ACT분류:5:봉사
;	ACT:손으로애무를강제/입으로애무를강제/다리빨고강제/가슴애무강제
;ACT분류:6:가학
;	ACT:매도
;ACT분류:7:이상
;	ACT:방치플레이
;ACT분류:9:휴게
;	ACT:쉬게한다
;ACT분류:10:성교 봉사
;	ACT:
;-------------------------------------------------
@KOJO_COM_K1_50
;-------------------------------------------------
;TFLAG:REACT파생 == 0, 마음대로 자위 하고 있지만 좋은가, 의존도 변화(-1, 0)
;TFLAG:REACT파생 == 1, 마음대로 자위 하지마, 의존도 변화(-3, +2)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;본체 측에 전용의 처리 없음
	
	;=================================================
	;반찬 분기
	;=================================================
	IF 구상기입체크(0) && TEQUIP:자위중
		PRINTFORM 
	ENDIF
	;=================================================
	;REACT파생 1, 마음대로 자위 하지마
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「무엇, 마음대로 즉시 시작하고 있는거야」
		;PRINTFORMW 「… 내면%RAND_SPLIT("인내/해 알아/허야")%없어요」
		IF TALENT:새디즘
			PRINTFORMDW %CALLNAME:TARGET%는 비난하는 것 같은 시선을 향하여 왔다
		ELSE
			CALL FADEOUT(8, "「좋지만」")
			PRINTFORMW 「해도 된다고 말하고 있는거야.」
			PRINTFORMW 「해도 되지만, 내면%RAND_SPLIT("인내해/알아/허야")%없어요」
		ENDIF
		RETURN 1
	ENDIF
	;=================================================
	;이후 마음대로 자위 하고 있지만 좋은가
	;=================================================
	;=================================================
	;REACT부하 2, 무리를 한 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리를 한 행동"
		;%CALLNAME:MASTER%는 자위를 하는 것으로 쾌감에 빠져 버리려고 하고 있다…
		PRINTFORMW 「무리하게 그런 일, 하지 않아도 괜찮아요」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 1, 무리가 없는 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "무리가 없는 행동"
		;%CALLNAME:MASTER%는 참지 못하고 마음대로 자신을 위로하고 있다…
		PRINTFORMW 「무엇 만지작거리고 있는거야」
		PRINTFORMW 「사람의 일, 마음대로 반찬으로 해… 나쁘다\@ TALENT:MASTER:남자 ? 자지 # 자 \@군요」
		PRINTFORMW 「부끄럽지만, 멈출 수 없어?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT부하 0, 자연스러운 행동
	;=================================================
	IF 구상기입체크(100) && GET_COM_RANK() == "자연스러운 행동"
		;%CALLNAME:MASTER%의 손은 무의식 중에 자신의 성기를 만지작거리고 있는 것 같다…
		PRINTFORMW 「무엇 만지작거리고 있는거야」
		PRINTFORMW 「말해져 깨달아도 얼굴이군요…」
		RETURN 1
	ENDIF
	
	;상층의 소분기를 모두 썼을 경우는 불필요한 개소입니다. 그 때는 삭제해 버리는 것을 추천
	;파생을 쓰지 않는, 혹은 일부를 방치했을 경우는 이 개소가 보완합니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	;IF 구상 기입 체크(0)
	;	RETURN 1
	;ENDIF
;조건에 히트 하지 않으면 통괄 범용 리액션에
RETURN 0
ENDIF

;─
;COM51, 도구를제외한다
;─
;·REACT분류:날뛴다
;·TEQUIP:(바이브/애널바이브/애널비즈/관장기＋플러그/클리캡/오나홀/니플캡/아이마스크/볼개그/페니스밴드)를 장착하고 있는 것이 조건
;·조교 대상이 자위중인 경우는 중단이 된다
;·REACT인상:약간나쁜인상
;·의존도 변화(-3, -10)
;-------------------------------------------------
;·제외하는 도구는 한 번에 1개까지
;※우선도는 이하와 같다
;(아이마스크>관장기＋플러그>애널바이브>바이브> 오나홀>클리캡>볼개그>니플캡> 애널비즈)
;-------------------------------------------------
;선택 가능한 조건…
;·TEQUIP:(바이브/애널바이브/애널비즈/관장기＋플러그/클리캡/오나홀/니플캡/아이마스크/볼개그/페니스밴드)를 장착하고 있는 것이 조건
;·TEQUIP:줄/성교중은 실행 불가
;-------------------------------------------------
@KOJO_COM_K1_51
;-------------------------------------------------
;·제외한 도구의 TEQUIP 번호가 TFLAG:REACT파생에 들어간다
;-------------------------------------------------

;지문은 범용 처리가 있을 뿐
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM51, 도구를제외한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;REACT파생분기를 추가하는 경우는 이 개소의 상층에서
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;%CALLNAME:MASTER%는 장착된 도구를 마음대로 제외해, %CALLNAME:TARGET%는 매우 화나 있는 것 같다…
		;무언이 제일 참는다
		PRINTFORMDW 
		RETURN 1
	ENDIF
ENDIF

;─
;COM52, 하는대로
;─
;·효과 없음. 실행할 수 있는 반응이 한 개도 없을 때의 구제
;·조교자에게 좋을대로 희롱해지고 한편, 하라는 대로 되는 목표 상황
;·REACT파생 마다 REACT분류, 의존도 변화가 바뀐다
;-------------------------------------------------
@KOJO_COM_K1_52
;-------------------------------------------------
;·TFLAG:REACT파생(0=통상/1=성교계/2=성교 봉사계/3=수치계, 강제계/4=봉사계/5=협박계/6=협박계/7=고통계/8=자위중의 회화/9=쾌감을 받으면서 회화/10=쾌락계/11=회화계/12=한번휴식)
;-------------------------------------------------

;------------------------------------------------
;REACT분류:받아들인다/의존도 변화(+4, +10)
;성교계 ACT·역강간
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 1, 「무저항에 범해진다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	;PRINTFORML %CALLNAME:MASTER%는 힘이 들어가지 않는 신체를 무저항에 범해져 이송되는 쾌락에 허덕이고 있다…
	PRINTFORMW 「무엇인가, 안 되는 것 하고 있는 기분이 되어 와요…」
	RETURN 1
ENDIF

;------------------------------------------------
;REACT분류:받아들인다/의존도 변화(+10, +10)
;성교계 봉사계 ACT·승마위
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 2, 「빠진 것처럼허리를흔드는」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 2
	;%CALLNAME:MASTER%는%CALLNAME%의 신체에 빠진 것처럼, 숨을 어지럽혀 오로지 허리를 흔들고 있다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;ACT(의상 변경/자위/방뇨/개장/이라마치오)
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 3, 「말해지는 대로 천천히 따른다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 3
	;%CALLNAME:MASTER%는 다만%CALLNAME%에 말해지는 대로 따랐다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;봉사계 ACT
;REACT분류:받아들인다/의존도 변화(+8, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 4, 「매료되어진 것처럼 봉사를 반복한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 4
	;마치 매료되어진 것처럼, %CALLNAME:MASTER%는 무심하게%CALLNAME%에 봉사했다…
	;심 여기에 있지 않고라고 하는 모습으로, 그 손놀림은 이상한 것이다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;ACT(위협한다/벌과 선언/고함친다/기분나쁘게웃는다/매도)
;반항적인 소질이 있어, 굴복각인 5 이하의 경우
;REACT분류:소극적 따른다/의존도 변화(0, -2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 5, 「분한 듯이 노려본다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 5
	;%CALLNAME:MASTER%는 분한 듯이 입술을 깨물어,%CALLNAME%를 올려보았다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;ACT(위협한다/벌과 선언/고함친다/기분나쁘게웃는다/매도)
;REACT분류:소극적 따른다/의존도 변화(+2, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 6, 「무서워한 것처럼 응시한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 6
	;%CALLNAME:MASTER%는 무서워한 것처럼%CALLNAME%를 응시했다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;가학계 ACT·고통이 큰 경우
;REACT분류:소극적 따른다/의존도 변화(+8, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 7, 「소리가 되지 않는 비명을 올려 몸부림쳐 뒹군다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 7
	;%CALLNAME:MASTER%는 아픔의 그다지 소리가 되지 않는 비명을 올려 몸부림쳐 뒹굴었다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT 또한 자위중인 경우
;REACT분류:받아들인다/REACT인상:나쁜인상/의존도 변화(-2, +2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 8, 「오나니─에 열중해 듣지 않았다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 8
	;%CALLNAME:MASTER%는 자위에 몰두해 온전히 대답을 할 수 없었다…
	;%CALLNAME%는 불만인 것 같다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT
;REACT분류:받아들인다/의존도 변화(+4, +2)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 9, 「기분 좋아서 선대답」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 9
	;%CALLNAME:MASTER%는 쾌감에 몸부림 하면서 수긍할 수 밖에 없었다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;쾌SOURCE 있어
;회화계 ACT 이외의 경우
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 10, 「머리 진흰색 황홀 상태」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 10
	;%CALLNAME:MASTER%는 주어지는 쾌감에 희롱해져 황홀이 되고 있다…
	;녹초가된 머릿속은 새하얗게 물들어, 아무것도 생각하는 것이 할 수 없다…
	PRINTFORMW 
	RETURN 1
ENDIF

;------------------------------------------------
;회화계 ACT
;REACT분류:받아들인다/의존도 변화(+8, +4)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 11, 「스스로도 모르는 채 부끄러운 것을 말해버린다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 11
	;%CALLNAME:MASTER%는 어딘가 망연히 한 채로 대답 했다…
	;스스로도 잘 모르는 채로 부끄러운 것을 말해버리고 있는 생각이 든다…
	PRINTFORMW 「그런 일은, 좀 더 맹숭맹숭한 얼굴때에 말하세요」
	RETURN 1
ENDIF

;------------------------------------------------
;조교자, 혹은 조교 대상 상태에 의해 한번휴식이 되었을 경우
;혹은 대만족 보너스로 쉬게 해를 선택했을 경우
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;한번휴식의 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:한번휴식
	;%CALLNAME:MASTER%는 안심한 것처럼 몸이 힘을 뺐다…
	;=================================================
	;대만족 보너스로 쉬게 해를 선택했을 경우
	;=================================================
	IF 구상기입체크(100) && IS_POLICY("오늘", "휴게")
		SELECTCASE TIME
			;낮  차 후
			CASE 0
				PRINTFORMW 「좋은 날씨군요」
				;%CALLNAME:MASTER%사망 플래그 %CALLNAME:MASTER%사망 END에 대응하기 때문에(위해) 가필;아무것도 아닌 것을 모두 즐기는 것이 연인
				CALL FADEOUT(20, "응 봐")
				CALL FADEIN(20, "석양")
				CALL FADEIN(20, "예쁘구나")
				PRINTFORMDW %CALLNAME:MASTER%는%CALLNAME:TARGET%와 함께 저녁놀하늘을 바라보았다
				;밤  저녁식사 후
			CASEELSE
				PRINTFORMW 「이런 것, 좋구나…」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;------------------------------------------------
;모든 조건에 해당하지 않는 경우
;REACT분류:받아들인다/의존도 변화(+4, +10)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 0, 그 외
;━━━━━━━━━━━━━━━━━━━━━━━━
[SKIPSTART]
;상부 조건에 해당해도 표시되는;조교 대상의 피로에 의한 중지 시에도 표시되는 모양;상정외이기 때문에 comment out
IF 구상 기입 체크(25)
	;%CALLNAME:MASTER%는 신체를 내던져, 되는 대로 되어 있다…
	PRINTFORMDW 「응. 반응이 둔하네요… 자기 마음대로해 버려요?」
	RETURN 1
ENDIF
[SKIPEND]

;=====================================================================
;특수한 반응 COM70～
;=====================================================================

;─
;COM70, 가게해！
;─
;·REACT분류:받아들인다
;·REACT인상:좋은인상
;·의존도 변화(+10, +6)
;-------------------------------------------------
;선택 가능한 조건…
;·초조하게해중은 아니다
;·TCVAR:MASTER:갈것같다가 서 있다
;·애무계 ACT(키스/밀어넘어뜨리기 이외) 한편 각 부위의 PALAM가 5000이상이다
;※상기 조건에 들어맞는 경우는 가능
;-------------------------------------------------
;·TFLAG:REACT파생(0=들은체 만체 한다/1=가게해 준다/2=가게해 주지 않는다)
;-------------------------------------------------
;ACT에 있어서 TCVAR:MASTER:초조하게해번 == 1(가게 한다)에서 만나도 COM70의 선택으로 TCVAR:MASTER:초조하게해번 > 1(초조하게 한다)(이)가 되는 경우 있어
;현재의 사양에서는 이 손의 변화를 참조할 방법은 없다. 본체측의 대응이 바람직한다!
;-------------------------------------------------
@KOJO_COM_K1_70
;-------------------------------------------------
;·TFLAG:REACT파생(0=들은체 만체 한다/1=가게해 준다/2=가게해 주지 않는다)
;-------------------------------------------------

;%CALLNAME:TARGET%는 심술궂은 미소를 띄우면 %CALLNAME:MASTER%를 초조하게해 시작했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT파생 2, 가게해 주지 않는다
;━━━━━━━━━━━━━━━━━━━━━━━━
;우선 최소한의 분기를 놓아둡니다만
;PENIS(MASTER) 등으로 페니스의 유무를 참조하는 것이 무난할지도 모릅니다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:REACT파생 == 2
	;=================================================
	;ACT분류, 애무
	;=================================================
	IF 구상기입체크(0) && MENUMATCH(TFLAG:ACT, "애무")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(MENUMATCH(TFLAG:ACT, "성적봉사"), TEQUIP:성교봉사중) > 0
		;V성교봉사중
		;IF V_SEX(MASTER)
		;	PRINTFORMW 
		;;A성교봉사중
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		;V성교봉사중
		IF V_SEX(MASTER)
			[SKIPSTART]
			PRINTFORMW 「아직 안돼」
			PRINTFORMW 「그렇구나?」
			PRINTFORMW 「너가, 외양 상관하지 않고 허리를 잡아, 마음대로 허리를 흔들어 질내사정 해 올 때까지, 보류군요」
			[SKIPEND]
			PRINTFORMW 「아직 안 돼요」
			PRINTFORMW 「그렇구나… 너가, 외양 상관하지 않고 허리를 잡아, 마음대로 허리 흔들어, 질내사정 해 올 때까지」
			PRINTFORMW 「보류군요」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「좀 더 만%RAND_SPLIT("분발함/참고 견뎌/참아")%하세요」
		RETURN 1
	ENDIF
ENDIF

;%CALLNAME:TARGET%는 상냥하게 미소지으면서 애무를 계속했다
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 1, 가게해 준다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:REACT파생 == 1
	;=================================================
	;ACT분류, 애무
	;=================================================
	IF 구상기입체크(0) && MENUMATCH(TFLAG:ACT, "애무")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(MENUMATCH(TFLAG:ACT, "성적봉사"), TEQUIP:성교봉사중) > 0
		;;V성교봉사중
		;IF V_SEX(MASTER)
		;	PRINTFORMW 
		;;A성교봉사중
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		;V성교봉사중
		IF V_SEX(MASTER)
			[SKIPSTART]
			;갈것같다도 아니어도 발생할 수 있는 위에 사정 분기와 입기 때문에(위해) 재고가 필요
			PRINTFORMW 「%RAND_SPLIT("이제(벌써) 사정한 듯하다? /좋아요, 가득 내/응, 와")%」
			PRINTFORM 「나는 너%RAND_SPLIT("의 신부/의 것/용")%니까
			CALL HEARTBW
			[SKIPEND]
			IF !RAND:3
				PRINTFORMW 「나의 보지로 가고 싶다? 보지로 자지 기분 좋고--하고 싶다…?」
				PRINTFORMDW %CALLNAME:TARGET%는 기쁜듯이%CALLNAME:MASTER%의 말을%RAND_SPLIT("악물고/반추 해")%라고 있다
			ELSEIF !RAND:2
				PRINTFORMW 「응…♪」
				PRINTFORM 「너의 자지, 의 보지로 가게 해 주어요
				CALL HEARTBW
			ELSE
				PRINTFORM 「나는 너%RAND_SPLIT("의 신부/의 것/용")%니까… 너의 졸라댐은, (들)물어 주지 않으면…
				CALL HEARTBW
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「야무지지 못하기 때문에… 뭐, 좋아요」
		RETURN 1
	ENDIF
ENDIF

;%CALLNAME%는 못된 장난 같은 미소를 띄우면서, %CALLNAME:MASTER%의 요구를 들은체 만체 했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;파생 0, 요구를 들은체 만체 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:REACT파생 == 0
	[SKIPSTART]
	;구문 미스에 대해 comment out
	SELECTCASE RANDCOM(3, 0)
		CASE 0
			;님표기는 살려 두어야 할 것인가가 고민거리
			PRINTFORMW 「%CALLNAME:TARGET%님너무 좋아! 라고 말할 수 있으면, 생각해 주어도 괜찮아요?」
			PRINTFORMW 「%RAND_SPLIT("오─들! /이봐요 이봐요,/어머 어머♪")%체념해 가 버리세요」
		;CASE 1
		;	PRINTFORMW 
		;CASE 2
		;	PRINTFORMW
		CASEELSE
			PRINTFORMW 「흐음… 어떻게 하지?」
	ENDSELECT
	;가게 할 생각 상태로부터가게해！시에 모순
	;들은체 만체 하고 있는 상황으로 상대로 하고 있는 것이 문제
		PRINTFORMW 「흐음… 어떻게 하지?」
		PRINTFORM 「응후후…
		PRINTFORMD %CALLNAME:TARGET%님너무 좋아!
		PRINTFORMW 라고 말할 수 있으면, 생각해 주어도 괜찮아요?」
		PRINTFORMW 「%RAND_SPLIT("오─들! /이봐요 이봐요,/어머 어머♪")%체념해 가 버리세요」
	RETURN 1
	[SKIPEND]
ENDIF
