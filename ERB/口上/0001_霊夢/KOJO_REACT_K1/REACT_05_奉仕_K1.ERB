;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무 구상 봉사계 개별 리액션 구상 파일
;
;	봉사계 리액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;봉사계 REACT50～57
;========================================================================
;· 각 REACT내의 나누는 방법으로서 TFLAG:REACT분류(IS_COMGRONAME)나 SELECTCOM(IS_NOWCOMNAME)가 생각됩니다
;·다만, 범위의 넓은 조건으로 주워 버리는 것은 기본적으로 추천하지 않습니다
;·ACT분류:봉사계의 경우, 봉사계 COM(COM40~COM44)를 중심으로, ACT에 따라서는 애무계 COM가 선택지가 됩니다만
;·상황에 따라서는 그 외의 것이 선택지에 포함될 가능성도 있습니다
;·리액션(REACT)으로 처리되지 않았던 것은 범용 리액션(COM)으로 보완되므로
;·무리하게 모두 묻을 필요는 없습니다
;·필요라고 생각하는 것을 좁혀, COM와의 제휴를 생각해 효율 좋게 구축해야 합니다
;-------------------------------------------------
;· 각 REACT내는,&& IS_NOWCOMNAME든지,&& IS_COMGRONAME등의 조건으로 결정하고 쳐 합시다
;·여기에 조건 없음 분기가 있으면, 모두 주워 버려 범용 리액션에 흐르지 않습니다
;·조교 대상의 반응에 관련되지 않고, 같은 리액션 밖에 하지 않게 되어 버립니다
;·요컨데 이것은, 상정된 시스템의 파괴에 연결됩니다
;-------------------------------------------------
;  REACT의 지문은 ACT_MESSAGE.ERB의 TRAIN_MESSAGE2_{TFLAG:ACT}로 처리됩니다
;  기본적으로 지문은 TFLAG:REACT분류(적극적 따른다/태연하게 따른다/용서를빈다/소극적 따른다)로 분기 해,
;  각 REACT분류내에서 TFLAG:REACT부하(자연스러운 행동/무리가 없는 행동/무리를 한 행동)으로 파생
;  고유의 TRAIN_MESSAGE2가 없는 경우는, REACTION_MESSAGE로 COM마다 범용 처리됩니다
;  ACT_MESSAGE.ERB REACTION_MESSAGE.ERB를 참조하면서의 작업을 추천합니다
;-------------------------------------------------
;각 REACT에서는 상정되는 반응을 망라되어 있습니다. 필요에 따라서 분기를 증감해 사용해 주세요
;COM에 따라서는 REACT파생로 REACT분류가 변화하는 일에 주의
;각 COM의 파생은 기본적으로 REACT파생 1를 조건 결정 우리 소분기로 해, REACT파생 0는 그 외 취급으로 조건 없다고 보고 있습니다
;수고를 걸고 싶지 않은 경우는 그 외만을 써, 파생에 관련되지 않고 해당 개소를 보완할 수가 있습니다
;또 IF 구상 기입 체크(0) && TFLAG:REACT파생 < 1으로 하는 일로 파생했을 경우는 범용 리액션에 흘린다 따위라고 하는 일도 가능합니다
;파생은 장래 증가할 가능성도 있으므로 != 1등으로 하는 것보다는< 1으로 하는 것이 무난하다라고 생각합니다
;-------------------------------------------------

;─
;REACT50, 손으로애무를강제·개별 리액션
;─
;·TFLAG:ACT파생(0=애무 강제/1=자위 강제/2=페니스밴드 자위 강제)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다
;봉사계 반응
;	COM:거부한다/소극적한다/적극적한다/애정을담아준다/태연하게한다
;그 외 반응
;	COM:자위하기시작한다
;-------------------------------------------------
@KOJO_REACT_K1_50
;-------------------------------------------------
;·지문은 REACT분류와 ACT파생로 분기
;·REACT분류:받아들이고는 REACTION_MESSAGE로 처리된다
;·REACT분류가 소극적 따르는/태연하게 따른다/적극적 따른다/받아들인다/용서를빈다인 경우는 수용 취급
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절한 취급이 된다
;-------------------------------------------------
;·텐프레는 ACT파생로 단락짓는 형태로 했습니다만, 단지 IF 구상 기입 체크(0)로 해 정리해 내부에서 분기 시키는 방법도 있습니다
;-------------------------------------------------

;거절 처리는 최상층에서 실시해 ACT파생 마다 소구분하지 않는 사양
;수고를 싫어하지 않는 것이면 이 개소에서 각 ACT파생 마다 나누어 쓰든지, 각 ACT파생로 개별적으로 처리하든지
;그 근처의 판단은 구상 작자에게 맡깁니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 거절했을 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다는 REACT분류:받아들이는 취급이었지만
;0616판으로부터 봉사계 ACT 및 ACT쉬게한다의 경우는 거부 취급이 되어 있는 일에 주의
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("거부/날뛴다/도망친다")
	;=================================================
	;COM가자위하기시작한다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("자위하기시작한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;부끄러워서 명령 대로로 할 수 없었다(치정선행)
	;=================================================
	IF 구상기입체크(0) && PALAM:MASTER:치정 > PALAM:MASTER:반항
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;치정선행의 분기를 썼을 경우에는 싫어해 요구에 응하지 않았던(반항 선행) 취급
	;쓰지 않았던 경우는 PALAM 상태에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		RETURN 1
	ENDIF
ENDIF

;조교자의 클리토리스를 중심으로 애무한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 애무 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;COM10, 부끄러워한다(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
		;-------------------------------------------------
		;쾌감에부끄러워한다
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;%CALLNAME:TARGET%씨자위 하지 않을 것 같다
			;PRINTFORMW 「이래서야, %CALLNAME:MASTER%의 손으로 자위하고 있는 것 같구나…」
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM41, 소극적한다(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("소극적한다")
		;-------------------------------------------------
		;소극적인의가 마음에 들지 않는다(벌포인트 가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(100)
			PRINTFORMW 「좀 더 걸근거려도 괜찮아요?」
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM42, 적극적한다(REACT분류:적극적 따른다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("적극적한다")
		PRINTFORMW 「제대로 풀어 두세요」
		RETURN 1
	ENDIF
	
	;=================================================
	;COM44, 태연하게한다(REACT분류:태연하게 따른다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("태연하게한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;파생 0으로 거의 같은 내용의 것을 형태를 바꾸어 제시합니다
;IS_COMGRONAME내에서 IS_NOWACTNAME로 나누어 쓰거나 TFLAG:REACT부하등으로 나누는 방법도 있습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 자위 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;COM가 하는대로의 경우는 범용 리액션에
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다") && !IS_NOWCOMNAME("하는대로")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;COM를 결정하고 집. REACT분류조건에 해당해도적극적한다/태연하게한다 이외는 범용 리액션에
	;=================================================
	;REACT분류가 적극적 따르는/태연하게 따른다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("적극적 따른다/태연하게 따른다") && IS_NOWCOMNAME("적극적한다/태연하게한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가용서를빈다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("용서를빈다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;COM를 결정하고 집. REACT분류조건에 해당해도부끄러워한다/소극적한다 이외는 범용 리액션에
	;=================================================
	;REACT분류가 소극적 따른다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("소극적 따른다") && IS_NOWCOMNAME("부끄러워한다/소극적한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;파생 0, 파생 1을 참고에 분기를 추가해 사용해 주세요
;IF 구상 기입 체크(0) && GROUPMATCH(TFLAG:ACT파생, 1, 2)등으로 해서 자위계를 모은 다음 내부에서 분기 시키는 방법도 있습니다
;조건 없음의 기술은 멈춥시다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 페니스밴드 자위 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT50, 손으로애무를강제

;─
;REACT51, 입으로애무를강제·개별 리액션
;─
;·TFLAG:ACT파생(0=쿤닐링구스 강제/1=페라 강제/2=안면승마/3=안면승마애널)
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다
;봉사계 반응
;	COM:거부한다/소극적한다/적극적한다/애정을담아준다/태연하게한다
;그 외 반응
;	COM:자위하기시작한다
;-------------------------------------------------
@KOJO_REACT_K1_51
;-------------------------------------------------
;·지문은 REACT분류와 ACT파생로 분기
;·REACT분류:받아들이고는 REACTION_MESSAGE로 처리된다
;·REACT분류가 소극적 따르는/태연하게 따른다/적극적 따른다/받아들인다/용서를빈다인 경우는 수용 취급
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절한 취급이 된다
;-------------------------------------------------
;·텐프레는 ACT파생로 단락짓는 형태로 했습니다만, 단지 IF 구상 기입 체크(0)로 해 정리해 내부에서 분기 시키는 방법도 있습니다
;-------------------------------------------------

;거절 처리는 최상층에서 실시해 ACT파생 마다 소구분하지 않는 사양
;수고를 싫어하지 않는 것이면 이 개소에서 각 ACT파생 마다 나누어 쓰든지, 각 ACT파생로 개별적으로 처리하든지
;그 근처의 판단은 구상 작자에게 맡깁니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 거절했을 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다는 REACT분류:받아들이는 취급이었지만
;0616판으로부터 봉사계 ACT 및 ACT쉬게한다의 경우는 거부 취급이 되어 있는 일에 주의
;-------------------------------------------------
IF 구상기입체크(100) && IS_COMGRONAME("거부/날뛴다/도망친다")
	;=================================================
	;COM가자위하기시작한다
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("자위하기시작한다")
		IF IS_CONDITION(TARGET, "정욕")
			PRINTFORMDL %CALLNAME:TARGET%는 어이를 상실하고 굳어지고 있었지만,
			PRINTFORMDL %CALLNAME:MASTER%의 행위를 이해하면 기학적인 눈으로%CALLNAME:MASTER%를 슬쩍 봐, 입술을 빨아,%RAND_SPLIT("반찬으로 하기 쉽게/잘 보이도록/더욱 크고")%다리를 열었다
		ELSE
			PRINTFORMDW 얼굴을 수치에 붉게 물들이면서도%CALLNAME:TARGET%가 투덜댄다
			PRINTFORMW 「%RAND_SPLIT("무엇 혼자서 즉시 시작하고 있어/마음대로 반찬으로 하고 있어/발정하고 있어/그렇지 않아요")%야. 이 바보」
			PRINTFORMDW 수치에 뺨을 붉게 물들여, 눈을 숙이면서%CALLNAME:TARGET%가 투덜댄다
			PRINTFORMW 「벌이 필요할까…」
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;부끄러워서 명령 대로로 할 수 없었다(치정선행)
	;=================================================
	IF 구상기입체크(0) && PALAM:MASTER:치정 > PALAM:MASTER:반항
		RETURN 1
	ENDIF
	
	;치정선행의 분기를 썼을 경우에는 싫어해 요구에 응하지 않았던(반항 선행) 취급
	;쓰지 않았던 경우는 PALAM 상태에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 쿤닐링구스 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;COM10, 부끄러워한다(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
		;-------------------------------------------------
		;쾌감에부끄러워한다
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM41, 소극적한다(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("소극적한다")
		;-------------------------------------------------
		;소극적인의가 마음에 들지 않는다(벌포인트 가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(100)
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM42, 적극적한다(REACT분류:적극적 따른다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("적극적한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM44, 태연하게한다(REACT분류:태연하게 따른다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM50, 자위하기시작한다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("자위하기시작한다")
		;-------------------------------------------------
		;마음대로 자위 하지마(벌포인트 가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「마음대로 자위 하고 있지만 좋은가」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;파생 0으로 거의 같은 내용의 것을 형태를 바꾸어 제시합니다
;IS_COMGRONAME내에서 IS_NOWACTNAME로 나누어 쓰거나 TFLAG:REACT부하등으로 나누는 방법도 있습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 1, 페라 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;COM50, 자위하기시작한다(REACT분류:받아들인다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("자위하기시작한다")
		;-------------------------------------------------
		;마음대로 자위 하지마(벌포인트 가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「마음대로 자위 하고 있지만 좋은가」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(100)
			IF !TALENT:MASTER:남자
				PRINTFORMW 「무엇, 준비해 있는거야. 위의 입으로 빠세요」
				PRINTFORMW 「… 그렇게, 그쪽에서 물고 싶어?」
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;COM가 하는대로의 경우는 범용 리액션에
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다") && !IS_NOWCOMNAME("하는대로")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;COM를 결정하고 집. REACT분류조건에 해당해도적극적한다/태연하게한다 이외는 범용 리액션에
	;=================================================
	;REACT분류가 적극적 따르는/태연하게 따른다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("적극적 따른다/태연하게 따른다") && IS_NOWCOMNAME("적극적한다/태연하게한다")
		PRINTFORMW 「왠지 기쁜 것 같구나?」
		PRINTFORMW 「부탁해 두어이지만, 싫지 않아?」
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가용서를빈다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("용서를빈다")
		;즉답
		CALL FADEIN(2, "「안돼」")
		CALL FADEIN(6, "「해」")
		RETURN 1
	ENDIF
	
	;COM를 결정하고 집. REACT분류조건에 해당해도부끄러워한다/소극적한다 이외는 범용 리액션에
	;=================================================
	;REACT분류가 소극적 따른다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("소극적 따른다") && IS_NOWCOMNAME("부끄러워한다/소극적한다")
		PRINTFORMW 「좀 더, 좀 더 해. 심술쟁이 하지 않고, 인내 시키지 말아줘」
		PRINTFORMDW %CALLNAME:TARGET%의 호흡은 여유 없게 흐트러져, 안타까운 듯이 허리를 떨리게 하고 있다
		PRINTFORMDW 당장%CALLNAME:MASTER%의 목 안에 쑤셔 넣어 올 것 같다…
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;파생 0, 파생 1을 참고에 분기를 추가해 사용해 주세요
;IF 구상 기입 체크(0) && GROUPMATCH(TFLAG:ACT파생, 2, 3)등으로 해서 안면승마계를 모은 다음 내부에서 분기 시키는 방법도 있습니다
;조건 없음의 기술은 멈춥시다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 안면승마
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;조건에 히트 하지 않으면 범용 리액션에 흘린다
	RETURN 0
ENDIF

;파생 0, 파생 1을 참고에 분기를 추가해 사용해 주세요
;IF 구상 기입 체크(0) && GROUPMATCH(TFLAG:ACT파생, 2, 3)등으로 해서 안면승마계를 모은 다음 내부에서 분기 시키는 방법도 있습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 3, 안면승마애널
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 3
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF
;REACT51, 입으로애무를강제

;─
;REACT52, 파이즈리·개별 리액션
;─
;·ACT파생없음
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;봉사계 반응
;	COM:거부한다/소극적한다/적극적한다/애정을담아준다/태연하게한다
;-------------------------------------------------
;남 %CALLNAME:MASTER%의 가슴이 사용되는 일은 없다
@KOJO_REACT_K1_52
;-------------------------------------------------
;·지문은 REACT분류와 TALENT:MASTER:거유/빈유로 분기
;·REACT분류:받아들이고는 REACTION_MESSAGE로 처리된다
;·REACT분류가 소극적 따르는/태연하게 따른다/적극적 따른다/받아들인다/용서를빈다인 경우는 수용 취급
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절한 취급이 된다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 거절했을 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_COMGRONAME("거부/날뛴다/도망친다")
	;=================================================
	;COM가자위하기시작한다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("자위하기시작한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;부끄러워서 명령 대로로 할 수 없었다(치정선행)
	;=================================================
	IF 구상기입체크(0) && PALAM:MASTER:치정 > PALAM:MASTER:반항
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;치정선행의 분기를 썼을 경우에는 싫어해 요구에 응하지 않았던(반항 선행) 취급
	;쓰지 않았던 경우는 PALAM 상태에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(100) && TFLAG:REACT파생 == 1
		PRINTFORMW 「자지로 푹푹 되어 기분 좋아져 버리다니…」
		PRINTFORMW 「나는 기뻐요」
		PRINTFORM 「함께 기분 좋아집시다
		CALL HEARTBW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「사랑스러워요」
		PRINTFORMW 「사랑스러워요」
		RETURN 1
	ENDIF
ENDIF

;파생 1의 경우는 REACT분류:날뛴다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1의 경우는 REACT분류:용서를빈다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다(소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
	;=================================================
	;애원(REACT분류:용서를빈다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적한다(REACT분류:적극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("적극적한다")
	PRINTFORMW 「그렇게 하면」
	PRINTFORM 「자지 말해 버리는… 가게 되어 버린다…
	CALL HEARTBW
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을담아준다(REACT분류:적극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("애정을담아준다")
	PRINTFORMW 「젖가슴 기분이 좋다」
	PRINTFORMDW %CALLNAME:MASTER%가%CALLNAME:TARGET%를 받아들이고 감싸면,%CALLNAME:TARGET%는%CALLNAME:MASTER%로 사정하기 위해서 허리를 흩뜨렸다
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게한다(REACT분류:태연하게 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게한다")
	PRINTFORMW 
	RETURN 1
ENDIF
;REACT52, 파이즈리

;─
;REACT53, 가랑이·개별 리액션
;─
;·ACT파생없음
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다/싫어한다/쾌감을참는다/쾌감을받아들인다/기분좋다！
;봉사계 반응
;	COM:거부한다/소극적한다/적극적한다/애정을담아준다/태연하게한다
;-------------------------------------------------
@KOJO_REACT_K1_53
;-------------------------------------------------
;·지문은 REACT분류로 분기
;·REACT분류:받아들이고는 REACTION_MESSAGE로 처리된다
;·REACT분류가 소극적 따르는/태연하게 따른다/적극적 따른다/받아들인다/용서를빈다인 경우는 수용 취급
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절한 취급이 된다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 거절했을 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다는 REACT분류:받아들이는 취급이었지만
;0616판으로부터 봉사계 ACT 및 ACT쉬게한다의 경우는 거부 취급이 되어 있는 일에 주의
;-------------------------------------------------
IF 구상기입체크(0) && IS_COMGRONAME("거부/날뛴다/도망친다")
	;=================================================
	;COM가자위하기시작한다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("자위하기시작한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;부끄러워서 명령 대로로 할 수 없었다(치정선행)
	;=================================================
	IF 구상기입체크(0) && PALAM:MASTER:치정 > PALAM:MASTER:반항
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;치정선행의 분기를 썼을 경우에는 싫어해 요구에 응하지 않았던(반항 선행) 취급
	;쓰지 않았던 경우는 PALAM 상태에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 부끄러워한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("부끄러워한다")
	;=================================================
	;쾌감에부끄러워한다
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1의 경우는 REACT분류:날뛴다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM11, 싫어한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("싫어한다")
	;=================================================
	;싫어해 방해했다(REACT분류:날뛴다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「싫어한다가 ACT는 방해하지 않고」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「싫어?」
		PRINTFORMW 「이래서야 아도 치워 있고 원이군요」
		CALL FADEOUT(12, "「우후후」")
		RETURN 1
	ENDIF
ENDIF

;파생 1의 경우는 REACT분류:용서를빈다취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM14, 쾌감을참는다(소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을참는다")
	;=================================================
	;애원(REACT분류:용서를빈다)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「내구」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM15, 쾌감을받아들인다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("쾌감을받아들인다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM16, 기분좋다！(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("기분좋다！")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적한다(REACT분류:적극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("적극적한다")
	PRINTFORMW (이대로 삽입되어져 버릴까?)
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을담아준다(REACT분류:적극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("애정을담아준다")
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게한다(REACT분류:태연하게 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게한다")
	PRINTFORMW 
	RETURN 1
ENDIF
;REACT53, 가랑이

;─
;REACT55, 다리빨고강제·개별 리액션
;─
;·ACT파생없음
;-----------------------------------------------------------
;상정되는 COM…
;회화계 반응
;	COM:용서를빈다
;봉사계 반응
;	COM:거부한다/소극적한다/적극적한다/애정을담아준다/태연하게한다
;그 외 반응
;	COM:자위하기시작한다
;-------------------------------------------------
@KOJO_REACT_K1_55
;-------------------------------------------------
;·지문은 REACT분류로 분기
;·REACT분류:받아들이고는 REACTION_MESSAGE로 처리된다
;·REACT분류가 소극적 따르는/태연하게 따른다/적극적 따른다/받아들인다/용서를빈다인 경우는 수용 취급
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절한 취급이 된다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 거절했을 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다는 REACT분류:받아들이는 취급이었지만
;0616판으로부터 봉사계 ACT 및 ACT쉬게한다의 경우는 거부 취급이 되어 있는 일에 주의
;-------------------------------------------------
IF 구상기입체크(100) && IS_COMGRONAME("거부/날뛴다/도망친다")
	;=================================================
	;COM가자위하기시작한다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("자위하기시작한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;부끄러워서 명령 대로로 할 수 없었다(치정선행)
	;=================================================
	IF 구상기입체크(0) && PALAM:MASTER:치정 > PALAM:MASTER:반항
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;치정선행의 분기를 썼을 경우에는 싫어해 요구에 응하지 않았던(반항 선행) 취급
	;쓰지 않았던 경우는 PALAM 상태에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「아니, 뭐, 그…. 그렇게 물러나지 말아요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 용서를빈다(REACT분류:용서를빈다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("용서를빈다")
	;파생 1의 경우는 REACT분류:없음
	;=================================================
	;의견을 내다니 좋은 담력이구나! (벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「흐음」(조교 대상의 의견을 묵살) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("소극적한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
	PRINTFORMDW %CALLNAME:MASTER%는%CALLNAME:TARGET%의 무릎에 입맞추었다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적한다(REACT분류:적극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("적극적한다")
	PRINTFORMW 「이봐! 허벅지가 아니고, 읏. 이 바보」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을담아준다(REACT분류:적극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("애정을담아준다")
	;소극적한다의 발전형 이 단계가 되면 레이무측도 끝 비친다
	;태연하게한다의 언동을 반영 온순하지만 너무 온순하지 않는 손대중을 더한다
	PRINTFORMDW %CALLNAME:MASTER%는%CALLNAME:TARGET%의 무릎에 입맞추었다
	PRINTFORMW 「… 좀 더」
	PRINTFORMDW %CALLNAME:MASTER%는 능숙하게 졸라댈 수 있었다%CALLNAME:TARGET%의 요구에 따랐다
	;키스 쿤닐링구스 강제 유두 들이마셔 강제에 연결되면 기쁘다
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게한다(REACT분류:태연하게 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_NOWCOMNAME("태연하게한다")
	PRINTFORMW 「… 뭐든지 말하는 대로가 되어 버리는 것도, 왠지 어딘지 부족하네요…」
	RETURN 1
ENDIF
;REACT55, 다리빨고강제

;─
;REACT56, 이라마치오·개별 리액션
;─
;·ACT파생없음
;-----------------------------------------------------------
;상정되는 COM…
;고통계 반응
;	COM:아픔을참는다/비명을올린다/날뛴다
;봉사계 반응
;	COM:소극적한다/적극적한다/애정을담아준다/태연하게한다
;-------------------------------------------------
@KOJO_REACT_K1_56
;-------------------------------------------------
;·지문은 REACT분류(적극적 따른다, 태연하게 따른다/용서를빈다, 소극적 따른다/받아들인다)로 분기
;·다른 봉사계와 달리, 이라마치오의 경우는 거절 할 수 없다
;·다만, COM32, 날뛴다로 날뛰고가 통과할 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM30, 아픔을참는다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("아픔을참는다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 비명을올린다(REACT분류:받아들인다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("비명을올린다")
	;=================================================
	;공포에 의한 비명
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「아픔에 의한 비명」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;파생 1. 파생 2의 경우는 REACT분류:소극적 따르는 취급
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM32, 날뛴다(REACT분류:날뛴다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("날뛴다")
	;=================================================
	;날뛰고 실패(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && GROUPMATCH(TFLAG:REACT파생, 1, 2)
		[SKIPSTART]
		※내용을 나누어 쓴다면 comment out를 해제해 사용해 주세요
		;줄구속에 의한 날뛰고 실패
		IF TFLAG:REACT파생 == 1
			PRINTFORMW 
		;제지에 의한 날뛰고 실패
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
		[SKIPEND]
		
		;수고를 걸고 싶지 않으면 조건 분기 시키지 않고, 정리해 버리면 좋습니다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;파생 1, 2를 썼을 경우는 REACT파생 0, 「날뛰고 성공」(REACT분류:날뛴다) 취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;다만 COM32날뛴다의 경우, REACT파생에 의해 내용이 크게 다르므로, 나누어 쓰는 (분)편이 바람직하다고 생각합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM41, 소극적한다(REACT분류:소극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("소극적한다")
	;=================================================
	;소극적인의가 마음에 들지 않는다(벌포인트 가산)
	;=================================================
	IF 구상기입체크(0) && TFLAG:REACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
	;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM42, 적극적한다(REACT분류:적극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("적극적한다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM43, 애정을담아준다(REACT분류:적극적 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("애정을담아준다")
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM44, 태연하게한다(REACT분류:태연하게 따른다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게한다")
	PRINTFORMW 
	RETURN 1
ENDIF
;REACT56, 이라마치오

;─
;REACT57, 가슴애무강제·개별 리액션
;─
;·TFLAG:ACT파생(0=손으로애무 강제/2=입으로애무 강제)
;·ACT파생 == 1이 숙청된 관계로 번호가 날고 있으므로 주의
;-------------------------------------------------
;상정되는 COM…
;애무계 반응
;	COM:부끄러워한다
;봉사계 반응
;	COM:거부한다/소극적한다/적극적한다/애정을담아준다/태연하게한다
;그 외 반응
;	COM:자위하기시작한다
;-------------------------------------------------
@KOJO_REACT_K1_57
;-------------------------------------------------
;·지문은 REACT분류로 분기
;·REACT분류:받아들이고는 REACTION_MESSAGE로 처리된다
;·REACT분류가 소극적 따르는/태연하게 따른다/적극적 따른다/받아들인다/용서를빈다인 경우는 수용 취급
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절한 취급이 된다
;-------------------------------------------------
;·텐프레는 ACT파생로 단락짓는 형태로 했습니다만, 단지 IF 구상 기입 체크(0)로 해 정리해 내부에서 분기 시키는 방법도 있습니다
;-------------------------------------------------

;거절 처리는 최상층에서 실시해 ACT파생 마다 소구분하지 않는 사양
;수고를 싫어하지 않는 것이면 이 개소에서 각 ACT파생 마다 나누어 쓰든지, 각 ACT파생로 개별적으로 처리하든지
;그 근처의 판단은 구상 작자에게 맡깁니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 거절했을 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 자위하기시작한다는 REACT분류:받아들이는 취급이었지만
;0616판으로부터 봉사계 ACT 및 ACT쉬게한다의 경우는 거부 취급이 되어 있는 일에 주의
;-------------------------------------------------
IF 구상기입체크(100) && IS_COMGRONAME("거부/날뛴다/도망친다")
	;=================================================
	;COM가자위하기시작한다
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("자위하기시작한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;부끄러워서 명령 대로로 할 수 없었다(치정선행)
	;=================================================
	IF 구상기입체크(0) && PALAM:MASTER:치정 > PALAM:MASTER:반항
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;치정선행의 분기를 썼을 경우에는 싫어해 요구에 응하지 않았던(반항 선행) 취급
	;쓰지 않았던 경우는 PALAM 상태에 관련되지 않고, 이 개소를 보완합니다
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「이상하네요… 젖가슴을 좋아하지 않는다 남자는 없다고 들었지만, 너는 다를까」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 0, 손으로 가슴애무강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;COM10, 부끄러워한다(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("부끄러워한다")
		;-------------------------------------------------
		;쾌감에부끄러워한다
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「지금의 상황에부끄러워한다」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(100)
			PRINTFORMW 「좀 더, 걸근거려도 괜찮아요」
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM41, 소극적한다(REACT분류:소극적 따른다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("소극적한다")
		;-------------------------------------------------
		;소극적인의가 마음에 들지 않는다(벌포인트 가산)
		;-------------------------------------------------
		IF 구상기입체크(0) && TFLAG:REACT파생 == 1
			RETURN 1
		ENDIF
		
		;파생 1을 썼을 경우는 REACT파생 0, 「소극적이지만 좋은가」취급
		;쓰지 않았던 경우는 REACT파생에 관련되지 않고, 이 개소를 보완합니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;COM42, 적극적한다(REACT분류:적극적 따른다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("적극적한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;COM43, 애정을담아준다(REACT분류:적극적 따른다)
	;=================================================
	IF 구상기입체크(100) && IS_NOWCOMNAME("애정을담아준다")
		PRINTFORMW 「읏…… 훗…… 응…… 응……」
		PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 손에 몸을 바쳐 완만한 쾌락에 잠겨 있다
		RETURN 1
	ENDIF
	
	;=================================================
	;COM44, 태연하게한다(REACT분류:태연하게 따른다)
	;=================================================
	IF 구상기입체크(0) && IS_NOWCOMNAME("태연하게한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF

;파생 0으로 거의 같은 내용의 것을 형태를 바꾸어 제시합니다
;IS_COMGRONAME내에서 IS_NOWACTNAME로 나누어 쓰거나 TFLAG:REACT부하등으로 나누는 방법도 있습니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT파생 2, 입으로 가슴애무강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;COM가 하는대로의 경우는 범용 리액션에
	;=================================================
	;REACT분류가 받아들인다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("받아들인다") && !IS_NOWCOMNAME("하는대로")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;COM를 결정하고 집. REACT분류조건에 해당해도적극적한다/태연하게한다 이외는 범용 리액션에
	;=================================================
	;REACT분류가 적극적 따르는/태연하게 따른다
	;=================================================
	IF 구상기입체크(100) && IS_COMGRONAME("적극적 따른다/태연하게 따른다") && IS_NOWCOMNAME("적극적한다/애정을담아준다/태연하게한다")
		PRINTFORMDW %CALLNAME:TARGET%입는 쑥 미소지으면서%CALLNAME:MASTER%의 머리를 어루만지고 있다…
		RETURN 1
	ENDIF
	
	;=================================================
	;REACT분류가용서를빈다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("용서를빈다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;COM를 결정하고 집. REACT분류조건에 해당해도부끄러워한다/소극적한다 이외는 범용 리액션에
	;=================================================
	;REACT분류가 소극적 따른다
	;=================================================
	IF 구상기입체크(0) && IS_COMGRONAME("소극적 따른다") && IS_NOWCOMNAME("부끄러워한다/소극적한다")
		PRINTFORMW 
		RETURN 1
	ENDIF
;조건에 히트 하지 않으면 범용 리액션에 흘린다
RETURN 0
ENDIF