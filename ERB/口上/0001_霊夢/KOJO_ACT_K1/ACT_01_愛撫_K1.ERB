;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무 구상 애무계 액션 구상 파일
;
;	애무계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;애무계 ACT10～19 & 27
;========================================================================
;·TFLAG:ACT(10=손으로애무/11=가슴애무/12=입으로애무/13=애널애무/14=애널빨고/15=키스/16=성기를서로켠다/17=파이즈리한다/18=풋잡한다/19=손가락삽입해라/27=밀어넘어뜨린다)
;·TFLAG:ACT분류 == 1에 해당
;·ACT18, 풋잡은 이상계로부터 파생하는 경우 있어
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "애무")로 줍는 것이 가능
;·조교자가 조교 대상을 애무한다. 이 구상의 시점에서 조교자는 벌써 손을 대고 있다
;·미약을 사용하는 케이스가 있다
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;─
;ACT10, 손으로애무(ACT10_0, 애무하는/ACT10_1, 자위 하는/ACT10_2, 페니스밴드 자위)
;─
;·C 중심으로 애무한다
;·조교 대상으로 페니스가 있는 경우, 및 페니스밴드장착 때에는 파생 커맨드가 된다
;·조교 대상이 자위중의 경우는 해제된다
;·조교자의 손에 더러움이 있는 경우 불쾌 SOURCE가 변동한다
;·조교자 기교, 애무, 기묘한손가락소질에 응해 발기도 상승
;-------------------------------------------------
@KOJO_ACT_K1_10
;-------------------------------------------------
;·TFLAG:ACT파생(0=애무/1=자위/2=페니스밴드 자위)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_1, 자위 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·남성기에의 애무를 실시한다, 즉 손 애무
;·다른 파생과 비교해 SOURCE:MASTER:쾌C는+200%
;·조교자 기교, 애무, 기묘한손가락소질에 응해 발기도 상승
;-------------------------------------------------
;필요에 따라서, TEQUIP:오나홀이나 TCVAR:MASTER:발기도
;PANTSCHECK(1) 등으로 분기를 마련하는 일도 할 수 있습니다.
;온전히 하면 수고가 심상치 않아서, 효율 좋게 기술하는 일을 추천합니다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;첫회
	;=================================================
	;시험삼아 조수를 불러 본 곳, 더블 손 애무에서도 이 개소가 발생
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「손대어도 괜찮을까? 우응. 너에게 거부권은 없어요, 손대게 해 주세요!」
		PRINTFORMW 「\@ TEQUIP:MASTER:하반신속옷 ? … 이것일까?  # \@… 로 하고 있어, 딱딱해져 오고 있는… 이상한 감촉이군요」
		PRINTFORML
		;처녀성의 보강. 좀 더 잘 오지 않는다
		;CALL FADEOUT(8, "(우햐아……)")
		PRINTFORMDW 점차 경도를 더해 가는 음경에%CALLNAME:TARGET%는 몹시 놀라고 있다
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	;IF 구상 기입 체크(0) && ONCEACT(0)
	;	;하반신 복장이 있는 경우
	;	IF PANTSCHECK(1)
	;		PRINTFORMW 
	;	;그 외
	;	ELSE
	;		PRINTFORMW 
	;	ENDIF
	;	RETURN 1
	;ENDIF
	
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;-------------------------------------------------
		;전부터 애무
		;-------------------------------------------------
		;IF 구상 기입 체크(0) && TCVAR:MASTER:위치전후 == 1
		;	;초조하게해번에 의해 분기
		;	SELECTCASE TCVAR:MASTER:초조하게해번
		;		;초조하게 해 안
		;		CASE IS > 1
		;		;초조하게 해 종료
		;		CASE 1
		;		;초조하게 해 없음
		;		CASEELSE
		;	ENDSELECT
		;	RETURN 1
		;ENDIF
		
		;-------------------------------------------------
		;뒤로부터 애무
		;-------------------------------------------------
		;IF 구상 기입 체크(0) && TCVAR:MASTER:위치전후 == 2
		;	;초조하게해번에 의해 분기
		;	SELECTCASE TCVAR:MASTER:초조하게해번
		;		;초조하게 해 안
		;		CASE IS > 1
		;		;초조하게 해 종료
		;		CASE 1
		;		;초조하게 해 없음
		;		CASEELSE
		;	ENDSELECT
		;	RETURN 1
		;ENDIF
		
		;위치전후 분기를 묻는다면 필요없음. 묻지 않으면 여기서 보완시킵시다
		;불필요한 분기는 삭제, 혹은 comment out 하는 것으로 부하는 가벼워집니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(100)
			;초조하게해번에 의해 분기
			SELECTCASE TCVAR:MASTER:초조하게해번
				;초조하게 해 안
				CASE IS > 1
					;초조하게 해 그것이 다발하므로 확충 확률 조작
					IF !RAND:3
						PRINTFORMW 「조금 심술쟁이해 버릴까… 후후」
						IF !RAND:3
							PRINTFORM 「
							PRINTFORMD 정말 좋아하는%CALLNAME:TARGET%씨로%RAND_SPLIT("뽑고 싶다/가고 싶다")%입니다
							PRINTFORM 라고 말할 수 있으면, 계속해 주어요
							CALL HEARTBW
							IF !RAND:3
								PRINTFORML
								PRINTFORMW 「내 쪽이 쭉, 초조해 해지고 있던 것이니까…」
								PRINTFORMDW %CALLNAME:TARGET%는 손의 움직임을 느슨한, 등진 것처럼 졸라대 왔다
							ENDIF
						ENDIF
					ELSE
						PRINTFORML 「이대로 손으로 내 버리는 것도, 과분한 생각이 들어 왔어요」
						;쾌감을받아들인다경우 부자연스럽게 되기 십상인 때문 조정 comment out
						;PRINTFORMW 「너는 어떻게 하시고 싶어? 어디서 사정하고 싶을까」
						PRINTFORMW 「너는 어떻게 하시고 싶어? 사정하고 싶어?」
					ENDIF
				;구문의 변경
				;초조하게 해 종료
				;CASE 1
				;초조하게 해 없음
				;CASEELSE
					;RETURN 0
				;초조하게 해 종료 초조하게 해 없음
				CASEELSE
					IF IS_CONDITION(TARGET, "정욕")
						PRINTFORML 「%RAND_SPLIT("슬슬/이제 한계")%일까? 이제 한계군요」
						PRINTFORML 「이봐요, 가 버리세요」
						PRINTFORMW 「가는 곳 보여 줘」
						;아이의 만드는 법은 알고 있어도 사정에 대한 성지식은 없다. 그렇다고 하는 조건은 괴로운 구상용 플래그의 설치를 검토하는 일
						;「가 버려라」 「가 버릴 수 있는 말해 버려라」와 「가 버릴 수 있는 말해 버려라」 「가 버려라」템포에 퇴고를 필요로 하는 3회연속
						PRINTFORMW 「가 버려라」
						PRINTFORMW 「가 버려라」
						PRINTFORMW 「가 버려라」
						RETURN 1
					ENDIF
					IF TALENT:새디즘
						PRINTFORML 「이봐요 이대로 사정해도 좋아요」
						PRINTFORML 「는 가죽안에 정액 내 기분 좋아져 버리세요」
						PRINTFORMDW %CALLNAME:TARGET%는 가죽으로 귀두를 싸면서%CALLNAME:MASTER%를 절정에 이끌었다
					ELSE
						PRINTFORMW 「%RAND_SPLIT("슬슬/이제 한계")%일까?%RAND_SPLIT("말하고 좋아요/자주(잘) 노력했군요")%」
						;애널빨고 자위와의 맞대고 비빔이 아직도
						;PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 귀를 살짝 깨물기 하면서 손의 움직임을 빨리 했다
						
						;PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 목덜미에 입맞추면서 손의 움직임을 빨리 했다
					ENDIF
			ENDSELECT
		ENDIF
		RETURN 1
	ENDIF

	;=================================================
	;연속 실행
	;=================================================
	;IF 구상 기입 체크(0) && TFLAG:ACT == TFLAG:전ACT
	;	;-------------------------------------------------
	;	;안면승마 안
	;	;-------------------------------------------------
	;	IF 구상 기입 체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
	;		;전부터 애무
	;		IF TCVAR:MASTER:위치전후 == 1
	;			PRINTFORMW 
	;		;뒤로부터 애무
	;		ELSE
	;			PRINTFORMW 
	;		ENDIF
	;		RETURN 1
	;	ENDIF
	;	
	;	;-------------------------------------------------
	;	;그 외
	;	;-------------------------------------------------
	;	IF 구상 기입 체크(0)
	;		;전부터 애무
	;		IF TCVAR:MASTER:위치전후 == 1
	;			PRINTFORMW 
	;		;뒤로부터 애무
	;		ELSE
	;			PRINTFORMW 
	;		ENDIF
	;		RETURN 1
	;	ENDIF
	;ENDIF
	;=================================================
	;무조건 안면승마
	;=================================================
	IF 구상기입체크(100) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 「너의 것도 기분좋게해 주어요」
		PRINTFORMW 「쓱쓱 해 주기 때문에, 너는 나의 것을 반찬으로 해 주세요」
		PRINTFORMW 「이렇게 굳건한 신부에게 엉덩이에 깔리다는… 너는 행운아군요」
		RETURN 1
	ENDIF
	
	[SKIPSTART]
	;=================================================
	;굴복각인
	;=================================================
	;하나 더 잘 다룰 수 없기 때문에 comment out
	;【굴복:30】성교
	IF 구상 기입 체크(20) && GETBIT(CFLAG:각인, 30)
		PRINTFORML 「이것이 나의 안에 들어갔어요」
		;빈발시키는 것은 아니기 때문에 언급에 조건의 덧붙여 씀이 필요
		;【굴복:25】질내사정
		IF 구상 기입 체크(50) && GETBIT(CFLAG:각인, 25)
			;후타나리시 모순의 우려
			;자위에도 전용할 수 있을 것 같은 조건
			PRINTFORMW (이런 느낌이었을까?)
			PRINTFORM 「이런 느낌으로%RAND_SPLIT("//강하고")%너 곧 끝나 버려, 확실히 교배해 주고%RAND_SPLIT("/")%원이군요
			CALL HEARTBW
			PRINTFORMDL %CALLNAME:TARGET%의 입가가 심술궂은 것 같게 치켜올라간다
			PRINTFORMW 「또, 하고 싶어?」
		ENDIF
		RETURN 1
	ENDIF
	[SKIPEND]
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SIF 구상기입체크(20) && TALENT:기묘한손가락
			PRINTFORMW 「너가 좋아하는 일. 해 주어요」
		SIF TALENT:새디즘
			PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 입술에 손가락을 쑤셔 넣으면, 타액으로 젖은 손가락으로 음경을 다뤘다…
		;전부터 애무
		IF TCVAR:MASTER:위치전후 == 1
			SELECTCASE RAND:4
				CASE 0
					PRINTFORML 「저기. 기분이 좋을까?%RAND_SPLIT("고집부리지 않아도 좋지 않다/숨겨도 안 돼요? /기분이 좋네요? /응후후…")%」
					PRINTFORML 「너%RAND_SPLIT("의 일이다/라고 알기 쉬운/, 얼굴에 나온다")%것… 알아요♪」
				CASE 1
					PRINTFORML 「자지, 가득%RAND_SPLIT("종류종류/쫄깃쫄깃/쓱쓱")%해 주어요」
					PRINTFORMW 「소중한 곳인걸. 제대로 닦아 주지 않으면」
					IF !EQUIP:하반신속옷
						PRINTFORML 「너%RAND_SPLIT("는, 이런 것/이런 것,")%를 좋아하겠지?」
						;PRINTFORMDW %CALLNAME:TARGET%는 드로워즈를 씌우면서 열심히 %CALLNAME:MASTER%의 음경을 닦고 있다
						PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 음경을 속옷으로 싸 왔다
						PRINTFORMW 「건강하게 되어있는♪속옷, 찢지 말아요」
					ENDIF
				CASE 2
					PRINTFORML 「………」
					CALL FADEOUT(8, "    글자")
					CALL FADEIN(8, "    키득")
					PRINTFORMDW %CALLNAME:TARGET%는 무언으로%CALLNAME:MASTER%의 표정에게 물으면서 음경을 다루고 있다
				CASEELSE
					PRINTFORMW 「%RAND_SPLIT("후후/쿠스")%. 기분 좋은 것 같구나\@ AFFECTION("몹시 사랑함") ? ♪ # ?  \@」
					PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 표정을 차분히 관찰하면서 정중하게 음경을 다루고 있다
			ENDSELECT
		;comment out 해 둡니다@/L
		;ELSE
		;뒤로부터
		ELSE
			SELECTCASE RAND:5
				CASE 0
					PRINTFORMDW %CALLNAME:TARGET%는 뺨을 붙이면서,%CALLNAME:MASTER%에의 고문을 계속하고 있다
					PRINTFORMW 「이렇게 해 너의 얼굴을 가까이서 보면서 하는 것도, 좋은 거네」
				CASE 1
					PRINTFORML 「어때?\@ EXP:MASTER:자위경험 ? 스스로 하는 것보다 #  \@기분이 좋을까?」
					PRINTFORM 「
					PRINTFORMD %CALLNAME:TARGET%의 라고라고 기분이 좋다
					PRINTFORML 라고 말할 수 있으면, 참고로 해 주어도 괜찮아요」
				CASE 2
					PRINTFORMDW 등에%CALLNAME:TARGET%의 가슴의 감촉이 전해져 온다
					;새디즘 성분의 보강 안보여도 %CALLNAME:TARGET%의 거동을 이해하고 있는 %CALLNAME:MASTER%
					IF TALENT:새디즘
						PRINTFORMD %CALLNAME:TARGET%의 입가는 비뚤어져,
						PRINTFORM 맞히고 있는거야
						PRINTFORMDW 라고 할듯이 힐쭉거리고 있다
					ENDIF
				CASE 3
					;목덜미 고문+독점욕을 낸다
					;여기에 물소리를 넣어 시야 불량을 보충하고 싶다
					PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 목덜미를 들이마셔 세우면서 봉사를 계속하고 있다
				CASEELSE
					PRINTFORMDW %CALLNAME:TARGET%는 한 손으로 음경을 잡아당기면서, 이제(벌써) 다른 한쪽의 손으로%CALLNAME:MASTER%의 손을 잡아 왔다
					PRINTFORMW 「이렇게 하면, 너가 느끼고 있는 것이 잘 전해져 와요♪」
				ENDSELECT
			ENDIF
			;comment out 해 둡니다@/L
			;처치 감사합니다
			;지문에 의지하는 것이 좋을지도 모르기 때문에 생략
			;PRINTFORMDW %CALLNAME:TARGET%는 뒤로부터 껴안도록(듯이) 팔을 돌려 %CALLNAME:MASTER%의 사타구니를 만지작거려 왔다
			;PRINTFORMDW 등에 %CALLNAME:TARGET%의 가슴의 감촉이 전해져 온다
		RETURN 1
	ENDIF
ENDIF
;ACT10_1, 자위 한다

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_2, 페니스밴드 자위
;━━━━━━━━━━━━━━━━━━━━━━━━
;·조교 대상이 페니스밴드장착 때에 발생
;·쾌CSOURCE적으로는 TFLAG:ACT파생 == 1인가 아닌가로 밖에 보지 않은 같기 때문에, 사실상 자위는 아니고 애무와 동등?
;·쾌V도 보지 않는 것 같은 느낌. 그리고 부위의 더러움도 참조 할 수 없는 것이 아닐까와
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(1)
		;하반신 복장이 있는 경우
		IF PANTSCHECK(1)
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마 안
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
			;전부터 애무
			IF TCVAR:MASTER:위치전후 == 1
				PRINTFORMW 
			;뒤로부터 애무
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;전부터 애무
			IF TCVAR:MASTER:위치전후 == 1
				PRINTFORMW 
			;뒤로부터 애무
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;전부터 애무
		IF TCVAR:MASTER:위치전후 == 1
			PRINTFORMW 
		;뒤로부터 애무
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;ACT10_2, 페니스밴드 자위

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_0, 애무한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·여성기에의 애무를 실시한다
;·SOURCE적으로는 쾌B와 쾌C는 동등
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(2)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(2)
		;하반신 복장이 있는 경우
		IF PANTSCHECK(1)
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:갈것같다
		;-------------------------------------------------
		;전부터 애무
		;-------------------------------------------------
		IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 1
			;초조하게해번에 의해 분기
			SELECTCASE TCVAR:MASTER:초조하게해번
				;초조하게 해 안
				CASE IS > 1
					PRINTFORMW 
				;초조하게 해 종료
				CASE 1
					PRINTFORMW 
				;초조하게 해 없음
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;뒤로부터 애무
		;-------------------------------------------------
		IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 2
			;초조하게해번에 의해 분기
			SELECTCASE TCVAR:MASTER:초조하게해번
				;초조하게 해 안
				CASE IS > 1
					PRINTFORMW 
				;초조하게 해 종료
				CASE 1
					PRINTFORMW 
				;초조하게 해 없음
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
		
		;위치전후 분기를 묻는다면 필요없음. 묻지 않으면 여기서 보완시킵시다
		;불필요한 분기는 삭제, 혹은 comment out 하는 것으로 부하는 가벼워집니다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;초조하게해번에 의해 분기
			SELECTCASE TCVAR:MASTER:초조하게해번
				;초조하게 해 안
				CASE IS > 1
					PRINTFORMW 
				;초조하게 해 종료
				CASE 1
					PRINTFORMW 
				;초조하게 해 없음
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마 안
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
			;전부터 애무
			IF TCVAR:MASTER:위치전후 == 1
				PRINTFORMW 
			;뒤로부터 애무
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			;전부터 애무
			IF TCVAR:MASTER:위치전후 == 1
				PRINTFORMW 
			;뒤로부터 애무
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;전부터 애무
		IF TCVAR:MASTER:위치전후 == 1
			PRINTFORMW 
		;뒤로부터 애무
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;ACT10_0, 애무한다

;─
;ACT11, 가슴애무
;─
;·가슴애무/유두 들이마셔/젖의 몸싸움/유두 고문
;·특정의 조건으로 「유두 들이마셔」 「젖의 몸싸움」 「유두 고문」에 파생한다
;·조교 대상이 남자의 경우는 반드시 「유두 고문」(이)가 되어, 그 밖에 파생하는 일은 없다
;-------------------------------------------------
@KOJO_ACT_K1_11
;-------------------------------------------------
;·TFLAG:ACT파생(0=가슴애무/1=유두 들이마셔/2=몸싸움/3=유두 고문)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_0, 가슴애무
;━━━━━━━━━━━━━━━━━━━━━━━━
;·모든 파생 조건으로부터 빠지면 통상의 가슴애무가 된다
;·조교자의 욕망+B감각이 3이상의 경우 1/2의 확률로 몸싸움에 파생
;·그리고 새었을 경우에게만, 통상의 가슴애무된다
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;전부터 애무
	IF TCVAR:MASTER:위치전후 == 1
		PRINTFORMW 
	;뒤로부터 애무
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_1, 유두 들이마셔
;━━━━━━━━━━━━━━━━━━━━━━━━
;·전회 ACT가 가슴애무의 경우 유두계가 된다
;·그 때, 조교자에게 유치한가 혀사용이 있으면 3/4, 없으면 1/4으로 유두흡실 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;상반신 복장이 요점 밀치고			옷을
	IF BRACHECK(1, 1)
		PRINTFORMW
	;그 외
	ELSE
		PRINTFORMW
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_2, 젖의 몸싸움
;━━━━━━━━━━━━━━━━━━━━━━━━
;·가슴의 몸싸움을 요구한다. 이 시점에서의 조교 대상의 반응은 미정
;·유두계의 조건으로부터 빠졌을 경우, 조교자의 욕망+B감각 3이상이라면,1/2그리고 가슴의 몸싸움이 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;상반신 복장이 방해			옷 위로부터
	;=================================================
	IF 구상기입체크(0) && BRACHECK(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;상반신윗도리가 요점 밀치고		윗도리를 걸어 인상
	;=================================================
	IF 구상기입체크(0) && UPPERJACKET(1) == 1
		;속옷도 걸었다
		IF UPPERUNDER(1) == 1
			PRINTFORMW 
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;상반신윗도리가 요점 밀치고		속옷의 옷자락으로부터 손을 잠입시킴
	;=================================================
	IF 구상기입체크(0) && UPPERUNDER(1) == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_3, 유두 고문
;━━━━━━━━━━━━━━━━━━━━━━━━
;·전회 ACT가 가슴애무의 경우 유두계가 된다
;·그 때, 조교자에게 유치한가 혀사용이 있으면 1/4, 없으면 3/4으로 유두 고문이 된다
;·조교 대상이 남자의 경우는 유두 꾸짖어 밖에 되지 않는다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 3
	;=================================================
	;조교 대상이 남자
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:남자
		;전부터 애무
		IF TCVAR:MASTER:위치전후 == 1
			PRINTFORMW 
		;뒤로부터 애무
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;전부터 애무
		IF TCVAR:MASTER:위치전후 == 1
			PRINTFORMW 
		;뒤로부터 애무
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT12, 입으로애무
;─
;·쿤닐링구스한다/펠라치오 한다
;·하반신의 의상이 남아 있어도 발생하는 일이 있다. 조교자에게 TALENT:벽통과가 없는 경우는 비켜 놓아 실행한다
;·턴방침이 "이상"이라면 발생하지 않는다
;·TEQUIP(바이브/오나홀/페니스밴드/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교 대상으로 페니스가 있으면, 반드시 페라가 된다
;-------------------------------------------------
@KOJO_ACT_K1_12
;-------------------------------------------------
;·TFLAG:ACT파생(0=쿤닐링구스/1=페라)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT12_0, 쿤닐링구스한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;안면승마, 아라이먼트가 부의 경우, TALENT:새디즘등으로 지문이 분기 하는 경우 있어
	;필요에 따라서 확장하면 좋을 것입니다
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:갈것같다
		;초조하게해번 파생 처리
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게 해 안
			CASE IS > 1 ;초조하게 해 안
				PRINTFORMW 
			;초조하게 해 종료
			CASE 1
				PRINTFORMW 
			;초조하게 해 없음
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마 안
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;연속 실행의 네스트를 개방한다면 여기는 반드시 써 둡시다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;여기를 최초로 씁시다
	;LOWERJACKET(1) == 1 「」LOWERUNDER(1) == 1 「비켜 놓아」등으로 확장하는 것도 좋을 것입니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT12_1, 펠라치오 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;안면승마, 아라이먼트가 부의 경우, TALENT:새디즘등으로 지문이 분기 하는 경우 있어
	;필요에 따라서 확장하면 좋을 것입니다
	;-------------------------------------------------
	;첫회
	;-------------------------------------------------
	IF 구상기입체크(100) && FIRSTACT(0)
		;이 근처는 어느 정도 진행하지 않으면 뵐 수 없다
		[SKIPSTART]
		PRINTFORMW 「입으로 해 주기 때문에, 내세요」
		;미주중;아는 사람의 이름을 내 보았지만, 장황할지도 모른다
		PRINTFORMW 「부부에서는 입으로 하는 것은 당연하라도 요우무가 말한거야」
		;요우무는 전후리 「유유코님은 나루토」에 연결한다
		PRINTFORMDW 와 되면 유유코님으로부터의 전해 들음인가
		PRINTFORML 
		[SKIPEND]
		;자지를 가까이 관찰
		CALL FADEIN(6, "(우햐아…)")
		PRINTFORMW 「이것을, 입으로… 하는거네요…」
		CALL FADEOUT(6, "매우…")
		CALL FADEIN(4, "「해… 해요?」")
		CALL FADEOUT(6, "「아뭇!」")
		PRINTFORMW 「읏, 읏읏… 쥬웃… 쥿……」
		;최선을 다함을 내기 위해서(때문에) 연장. 요령을 잡아 가고 템포를 정돈하고 소리가 규칙적으로. … 를 짧게 해 나간다
		PRINTFORMW 「응쥿…… 쥬웃…… 읏읏… 읏읏읏읏…」
		;휴식이 서투름
		CALL FADEOUT(6, "「… 푸앗」")
		RETURN 1
	ENDIF
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;초조하게해번 파생 처리
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게 해 안
			CASE IS > 1
				PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 표정을 보자, 봉사를 멈추어 버렸다
				PRINTFORMDW %CALLNAME:TARGET%의 눈은 심술궂은 것 같게 웃고 있다…
			;초조하게 해 종료
			CASE 1
				IF IS_CONDITION(TARGET, "정욕")
					CALL FADEOUT(8, "(내, 내)")
					[SKIPSTART]
					;말하게 하지 않는 것이 좋은 것 같다
					PRINTFORM 「키─해 먹여
					CALL HEARTBW
					[SKIPEND]
				ELSE
					CALL FADEOUT(12, "총… 츄파… 쥿… 쥿…")
					PRINTFORMW 「마셔 주기 때문에, 사정해 버리세요」
					CALL FADEOUT(6, "「아뭇!」")
					PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 음경을 깊게 물면 조금 강하게 들이마셔 세웠다…
					IF !RAND:3
						PRINTFORMDW 가지고 갈 곳이 곤란한다%CALLNAME:MASTER%의 시선은%CALLNAME:TARGET%의 가는 어깨에 빨려 들여갔다
						PRINTFORMDW 그 가는 선은%CALLNAME:MASTER%안의수컷에게%CALLNAME:TARGET%의 암컷을 의식시켰다…
					ELSEIF !RAND:2
						PRINTFORMDW %CALLNAME:TARGET%의 요염한 흑발이%CALLNAME:MASTER%의 시선을 붙잡는다…
					ELSE
						PRINTFORMDW 일심 불란에 육봉을 들이마셔 세운다%CALLNAME:TARGET%의 표정은 긴 머리카락에 덮여 안보인다…
					ENDIF
				ENDIF
			;초조하게 해 없음
			CASEELSE
				PRINTFORMDW %CALLNAME:TARGET%는 리본을 흔들면서%CALLNAME:MASTER%의 음경을 목 안까지 사용해 봉사하고 있다…
		ENDSELECT
		RETURN 1
	ENDIF
	
	;행그렇게는 상류에 있는+3회 연속은 우선 나오지 않기 때문에, 다른 플레이로 이행해 사정시키는 일을 의식하는 것이 좋은 것인지도 모른다
	;가게 할 생각이 아닌 형태도 성격이 잘 맞을지도 모른다
	;=================================================
	;페라 연속 실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		;-------------------------------------------------
		;안면승마 안
		;-------------------------------------------------
		IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;연속 실행 페라 %CALLNAME:MASTER%정욕
		;-------------------------------------------------
		IF 구상기입체크(50) && IS_CONDITION(MASTER, "정욕")
			CALL FADEOUT(8, "(이상한 소리 내 버려)")
			CALL FADEIN(8, "(사랑스럽기 때문에)")
			RETURN 1
		ENDIF
		;-------------------------------------------------
		;연속 실행 페라 %CALLNAME:TARGET%정욕
		;-------------------------------------------------
		IF 구상기입체크(50) && IS_CONDITION(TARGET, "정욕")
			[SKIPSTART]
			;병콜라가 존재하므로 구형 패키지라고 강변 하면 캇빠 새우 선도 환상향에 존재할 수 있다. 플레이어씨가 곤란하므로 없음
			PRINTFORMW (아─… 그만두고들 응 없어요 이것…)
			PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 캇빠 새우 선사이즈의 페니스에 달라붙고 있다…
			;비명과의 제휴를 생각하면 없이는 없지만, 아프기 때문에 없음
			PRINTFORMW (그다지 맛있지 않네요… 씹으면 육즙 나올까…?)
			[SKIPEND]
			PRINTFORMDW %CALLNAME:TARGET%는 맛있을 것 같게%CALLNAME:MASTER%의 음경을 맛보고 있다
			RETURN 1
		ENDIF
		
		;연속 실행의 네스트를 개방한다면 여기는 반드시 써 둡시다
		;-------------------------------------------------
		;그 외
		;-------------------------------------------------
		IF 구상기입체크(100)
			PRINTFORML 「계속해요? 아뭇…」
			PRINTFORMDW %CALLNAME:TARGET%는 헌신적으로%CALLNAME:MASTER%의 일물을 입으로 봉사하고 있다…
			RETURN 1
		ENDIF
	ENDIF
	
	;-------------------------------------------------
	;안면승마 안
	;-------------------------------------------------
	IF 구상기입체크(100) && TRUECHECK(TEQUIP:안면승마) > 0
		PRINTFORMW 「나의%RAND_SPLIT("/보지/소중한 곳/저기")%로 자지 이렇게 해 버려…」
		CALL FADEOUT(4, "「응후후」")
		CALL FADEIN(12, "「어쩔 수 없네요」")
		PRINTFORMW 「나로 이렇게 한 것이니까… 책임과 간장은 배달시켜 주지 않으면…%RAND_SPLIT("/안 돼요")%군요」
		;앵글적으로 %CALLNAME:MASTER%에는 안보이기 때문에 없음
		;PRINTFORMDW %CALLNAME:TARGET%는 기쁜듯이 %CALLNAME:MASTER%의 음경을 물었다
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;LOWERJACKET(1) == 1 「」LOWERUNDER(1) == 1 「비켜 놓아」등으로 확장하는 것도 좋을 것입니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;지문
		;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 페니스를 잡아 천천히 상하시키고 있다…
		;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 사타구니에 얼굴을 묻어 터질듯이 노장 한 남성기를 입으로애무하기 시작했다…
		;구상 여기
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMDW %CALLNAME:TARGET%가%CALLNAME:MASTER%의 가랑이에 얼굴을 묻으면, 늘어지는 흑발이%CALLNAME:TARGET%의 표정을 숨긴다
				PRINTFORMDW 안보이는 앵글이, 오히려%CALLNAME:MASTER%를 어지럽혔다
			CASEELSE
				PRINTFORMDW %CALLNAME:TARGET%가%CALLNAME:MASTER%를 높이려고 열심히 머리를 움직일 때마다, 리본이 팔랑팔랑춤추었다…
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;─
;ACT13, 애널애무
;─
;·애널애무/전립선자극/애널바이브 출납
;·애널바이브 장비중은, 애널바이브 출납에 파생
;·연속 실행의 경우, 전립선자극에 파생
;·행그렇게 or쾌C가 5000이상의 경우, 조교자의 애무 스킬에 응해 전립선자극에 파생
;·발기도가 낮은 경우, 조교자의 애무 스킬에 응해 전립선자극에 파생
;·요점강제발기의 경우, 전립선자극에 파생
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드/애널비즈) 장착중에는 발생하지 않는다
;·성교봉사중에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K1_13
;-------------------------------------------------
;·TFLAG:ACT파생(0=애널애무/1=전립선자극/2=애널바이브 출납)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_0, 애널애무
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORML 「응… 아마 여기서 맞고 있네요」
		PRINTFORMDL %CALLNAME:TARGET%는 되어가는 대로 하게%CALLNAME:MASTER%의 국문을 꾸짖어 왔다
		RETURN 1
	ENDIF

	;=================================================
	;탄 떨어짐 또한 고추 서 있지 않다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 <= 1000 && BASE:MASTER:사정 == 0
		PRINTFORML 「총알 떨어짐 하면 허락해 받을 수 있다고 생각했을까?」
		PRINTFORML 「%RAND_SPLIT("나는 아직 만족하지 않아요/좀 더 교제하세요/허락하지 않아요/허없·어·요♪")%」
		RETURN 1
	ENDIF

	;=================================================
	;안면승마 안
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;하반신 복장이 요점 밀치고
	;=================================================
	IF 구상기입체크(0) && PANTSCHECK(1, 1)
		;하반신윗도리를 걷고
		IF LOWERJACKET(1) == 1
			PRINTFORMW 
		;속옷 너머로
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		SELECTCASE ABL:MASTER:A감각
			CASE 0 TO 2
				PRINTFORML 「아파? 괜찮아, 곧 좋아져요」
				CALL FADEOUT(4, "「… 아마」")
			CASEELSE
				PRINTFORMW 「엉덩이로 기분 좋아져 버리다니 너는%RAND_SPLIT("/사실")%」
				PRINTFORMW 「어쩔 수 없다%RAND_SPLIT("변태/와")%군요?」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_1, 전립선자극(애널애무)
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상이 남자의 경우에만 파생
;후타나리의 전립선은 요도, 즉 질 위에 있다고 해, G스팟 자극에
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「아 여기군요? 대개 알아 왔어요」
		RETURN 1
	ENDIF
	;=================================================
	;발기 강제
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:발기도 <= 1000
		PRINTFORMW 「이봐요! 자지 서게 해 주세요!」
		PRINTFORMW 「…%RAND_SPLIT("너의 엉덩이는 편리하구나/너의 자지는 편리하구나/편리하구나 이것")%」
		RETURN 1
	ENDIF
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「응, 편리라고 할까 단순이라고 할까… 이것은 이것인 의미, 남자다운 것일까요?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_2, 애널바이브 출납
;━━━━━━━━━━━━━━━━━━━━━━━━
;·애널바이브 장비중은, 애널바이브 출납이 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;조교 대상이 남자
	IF TALENT:MASTER:남자
		PRINTFORMW 「엉덩이라고%RAND_SPLIT("는 히는 히 말한다/응 몸부림친다")%의 것은 좋지만…」
		PRINTFORMW 「헛된 공격 하면, 벌 해요」
		RETURN 1
	;그 외
	ELSE
		;PRINTFORMW 
		RETURN 0
	ENDIF
	;RETURN 1
ENDIF

;─
;ACT14, 애널빨고
;─
;·애널빨고
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "소프트", "이상"이라면 발생하지 않는다
;·TEQUIP(애널바이브/애널비즈/관장기＋플러그/페니스밴드/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·TCVAR:MASTER:초조하게해번이 실로 되어 있는 경우는 발생하지 않는다
;·혀에 의한 전립선자극에의 파생은 「그런 곳까지 혀가 닿을 이유 없지 않은가www」라고 하는 일로 숙청되고 있습니다
;·현시점에서 ACT가 파생하는 일은 없지만, ACT 개변의 가능성에 대비해, TFLAG:ACT파생 == 0으로 지정
;-------------------------------------------------
@KOJO_ACT_K1_14
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT14_0, 애널빨고
;━━━━━━━━━━━━━━━━━━━━━━━━
;구 음은 물소리에 두어 그다지 말하게 하지 않는다
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 1)
		;조교자가 페니스밴드 몸에 걸치고 있다
		IF TEQUIP:조교자페니스밴드
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마 안
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 자위중
	;=================================================
	IF 구상기입체크(0) && GETBIT(TEQUIP:자위중, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;하반신 복장이 요점 밀치고
	;=================================================
	IF 구상기입체크(0) && PANTSCHECK(1, 1)
		;하반신윗도리를 걷고
		IF LOWERJACKET(1) == 1
			PRINTFORMW 
		;속옷 너머로
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT15, 키스
;─
;·키스/미약키스
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·조교자에게 TALENT:벽통과가 없는 경우, TEQUIP:볼개그 장착중은 튕겨진다
;-------------------------------------------------
@KOJO_ACT_K1_15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15, 키스 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && FIRSTACT(0)
	;=================================================
	;서로 처음
	;=================================================
	IF 구상기입체크(0) && EXP:MASTER:키스경험 == 0 && EXP:TARGET:키스경험 == 0
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 처음
	;=================================================
	IF 구상기입체크(0) && EXP:MASTER:키스경험 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 처음
	;=================================================
	IF 구상기입체크(100) && EXP:TARGET:키스경험 == 0
		PRINTFORMW 「츄…」
		;입맞춤은 히라가나가 분위기가 나온다
		PRINTFORMDW 횡포 마지막 없는 행위와는 너무 다른, 접하는 것 같은 상냥한 입맞춤
		PRINTFORMW 「이것으로 일선, 넘어 버렸군요」
		PRINTFORMDL 뺨을 물들이고 입가를 누르는 행동이 기특하다…
		RETURN 1
	ENDIF

	;"첫회"의 네스트를 개방해, 각 조건의 분기를 방치한다면 여기서 보완합시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15_0, 키스
;━━━━━━━━━━━━━━━━━━━━━━━━
;! GETBIT(TFLAG:미약등사용, 1) 등의 조건을 삭제해! TFLAG:ACT파생로 정리해 버려도 좋다고 생각합니다
;무리하게 모든 개소를 묻는 일은 없기 때문에, 필요에 따라서 구분하여 사용해 주세요
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생 && !GETBIT(TFLAG:미약등사용, 1)
	;=================================================
	;성교중(조교자 삽입)
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위를 파생 처리
		SELECTCASE TEQUIP:성교중
			;정상위					두를 잡아 정열적인 키스를 했다…
			CASE 1
				PRINTFORMW 
			;후배위					뒤돌아 보게 한 입술을 빼앗았다…
			CASE 2
				PRINTFORMW 
			;승마위					하로부터 밀어올리면서 정열적으로 입맞춤했다…
			CASE 3
				PRINTFORMW 
			;대면좌위				꽉 껴안으면 그대로 입술을 빼앗았다…
			CASE 4
				PRINTFORMW 
			;배면좌위				뒤돌아 보게 한 그대로 입술을 빼앗았다…
			CASE 5
				PRINTFORMW 
			;애널섹스			아누스를 범하면서 키스 해 왔다…
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;성교봉사중(조교 대상 삽입)
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		;초조하게해번에 의한 파생			TCVAR:MASTER:갈 것 같아 둘러싸지는 않기 때문에, CASEELSE는 통상 상태를 의미한다
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게 해 안
			CASE IS > 1
				;PRINTFORMW 
				RETURN 0
			;초조하게 해 종료
			CASE 1
				IF IS_CONDITION(TARGET, "정욕")
					PRINTFORMW 「읏!」
					PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 입술에 스스로의 입술을 맞추자, 허리를 격렬하게 움직이기 시작했다
					PRINTFORMDW %CALLNAME:MASTER%가 누설한 교성은,%CALLNAME:TARGET%의 입안에 삼켜져 대체에 타액을 따라졌다
				ELSE
					PRINTFORMW 「참을 수 있었던 포상, 해 주어요」
					PRINTFORMW 「에에. 내가 하고 싶은거야♪」
					PRINTFORMDW %CALLNAME:TARGET%는 기분 좋은 점에 삑삑 리본을 날개를 펼치게 하면, 입술에 입술을 거듭해 왔다
				ENDIF
			;초조하게 해 없음
			CASEELSE
				PRINTFORMDW %CALLNAME:TARGET%의 혀를 들이마시면,%CALLNAME:TARGET%의%RAND_SPLIT("리본이 거절하는 와 떨렸다/질은 기쁜듯이 꾸욱 음경을 단단히 조여 왔다")%…
				CALL FADEOUT(4, "(… 이것)")
				CALL FADEIN(8, "(기분이 좋다…)")
		ENDSELECT
		RETURN 1
	ENDIF
		
	;=================================================
	;상사상애
	;=================================================
	IF 구상기입체크(0) && AFFECTION("상사상애")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;뒤로부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 2
		RETURN 1
	ENDIF
	
	;=================================================
	;전부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 1
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		[SKIPSTART]
		;지문
		;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 얼굴에 가까워져,
		;입술에 입술을 거듭했다…
		;구상 여기
		;공행
		;한층 더 추가행동
		
		;지문 부끄러워했을 경우
		;%CALLNAME:TARGET%는 억지로 %CALLNAME:MASTER%의 입술을 빼앗으면
		;%CALLNAME:MASTER%의 입안을 혀로 유린했다…
		;공행
		;%CALLNAME:MASTER%는 복받치는 쾌감을 숨기지 못하고, 부끄러워해 얼굴을 돌렸다…
		;리액션 구상은 여기
		[SKIPEND]
		
		[SKIPSTART]
		;딥키스에 단정해 버리는 것은 아까운 버드 키스는 리액션 측에 이전
		PRINTFORMW 「%RAND_SPLIT("군요? 눈, 감아/이봐요, -해 주세요/키스, 해요")%」
		PRINTFORMW 「읏!」
		PRINTFORM 「읏
		CALL HEARTBW
		PRINTFORMW 「읏읏」
		PRINTFORM 「응─
		CALL HEARTBW
		
		;키스 절정 도중 중단 이 시점에서는 리액션내에서의 키스 묘사가 있으므로 연구 필요
		PRINTFORMD 키스를 끝내고 눈동자를 열면, %CALLNAME:TARGET%의 리본은
		PRINTFORM 구라고
		PRINTFORMDW 와 힘 없게 처지고 있었다
		
		;추가행동의 사정으로부터 손을 사용할 수 없기 때문에 없음
		PRINTFORMDW %CALLNAME:MASTER%의 뺨에 %CALLNAME:TARGET%의 손이 더해진다

		;추가행동의 사정으로부터 손을 사용할 수 없기 때문에 없음
		PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 목에 손을 써
		
		IF TALENT:TARGET:새디즘
			IF ! RAND:5
				;어디까지나 당신 수동의 키스
				;PRINTFORMW 「너로부터도, 해」
				;PRINTFORMDW %CALLNAME:TARGET%는 한 바탕 %CALLNAME:MASTER%의 입술을 맛보면 목에 팔을 돌려 한층 더 입맞춤을 요구해 왔다
			ELSE
				PRINTFORMW 「해 버렸다」
				PRINTFORMDW %CALLNAME:TARGET%는 싱글벙글 하면서 %CALLNAME:MASTER%의 입술을 손가락으로 훑었다
			ENDIF
		ELSE
		ENDIF
		[SKIPEND]
		IF IS_CONDITION(TARGET, "지루함")
			PRINTFORMW 「싫증해 북이나들 -하세요」
			PRINTFORMW 「이봐요」
		ELSE
			PRINTFORMW 「%RAND_SPLIT("군요? 눈, 감아/이봐요, -해 주세요/해요")%」
			PRINTFORM 「읏
			CALL HEARTBW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15_1, 미약키스
;━━━━━━━━━━━━━━━━━━━━━━━━
;·조교 대상으로 입으로 옮김으로 미약을 먹인다
;-------------------------------------------------
;RESULT치를 2로서@KOJO_USE_KX의 미약등사용구상을 호출하지 않는 설정을 디폴트로 해둡니다
;필요에 따라서 반환값을 설정해 주세요
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생 && GETBIT(TFLAG:미약등사용, 1)
	;미약사용 구상 처리필 체크(미약사용의 지문 컷 할 때는 TFLAG:미약등사용지문스킵의 코멘트를 해제)
	CLEARBIT TFLAG:미약등사용구상, 1
	;CLEARBIT TFLAG:미약등사용지문스킵, 1
	;=================================================
	;성교중(조교자 삽입)
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		;체위를 파생 처리
		SELECTCASE TEQUIP:성교중
			;정상위					두를 잡아 정열적인 키스를 했다…
			CASE 1
				PRINTFORMW 
			;후배위					뒤돌아 보게 한 입술을 빼앗았다…
			CASE 2
				PRINTFORMW 
			;승마위					하로부터 밀어올리면서 정열적으로 입맞춤했다…
			CASE 3
				PRINTFORMW 
			;대면좌위				꽉 껴안으면 그대로 입술을 빼앗았다…
			CASE 4
				PRINTFORMW 
			;배면좌위				뒤돌아 보게 한 그대로 입술을 빼앗았다…
			CASE 5
				PRINTFORMW 
			;애널섹스			아누스를 범하면서 키스 해 왔다…
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 2
	ENDIF
	
	;=================================================
	;성교봉사중(조교 대상 삽입)
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		;초조하게해번에 의한 파생			TCVAR:MASTER:갈 것 같아 둘러싸지는 않기 때문에, CASEELSE는 통상 상태를 의미한다
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게 해 안
			CASE IS > 1
				PRINTFORMW 
			;초조하게 해 종료
			CASE 1
				PRINTFORMW 
			;초조하게 해 없음
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 2
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		[SKIPSTART]
		;지문
		;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 얼굴에 가까워져,
		;입술에 입술을 거듭했다…
		;구상 여기
		;%CALLNAME:TARGET%는 스스로 미약을 마셔, 키스 하면서 그것을 입으로 옮김으로 %CALLNAME:MASTER%에 먹였다…
		[SKIPEND]
		;표현에 과제가 남는다
		PRINTFORML 「입 열어? 진한 것 하기 때문에」
		PRINTFORM 「나의 타액, 마셔
		CALL HEARTBW
		RETURN 2
	ENDIF
ENDIF

;─
;ACT16, 성기를서로켠다
;─
;·짝 맞추기 놀이/가랑이 한다
;·조교 대상으로 페니스가 있는 경우는 ACT파생 == 1 「가랑이 한다」에 파생한다
;·조교 대상이 페니스밴드장착 안은, ACT파생 == 2 「페니스밴드 가랑이」가 된다
;·턴방침이 "하드", "이상하다면 발생하지 않는다
;·조교 대상이 여자 아이로 바이브 장착중이라면 발생하지 않는다
;·TEQUIP(오나홀/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교자에게 페니스가 있으면 발생하지 않는다(봉사계의 가랑이 강제가 된다)
;-------------------------------------------------
@KOJO_ACT_K1_16
;-------------------------------------------------
;·TFLAG:ACT파생(=0, 조개 우려라/=1, 가랑이/=2, 페니스밴드 가랑이)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_0, 짝 맞추기 놀이
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:갈것같다
		;초조하게해번 파생
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게 해 안
			CASE IS > 1
				PRINTFORMW 
			;초조하게 해 종료
			CASE 1
				PRINTFORMW 
			;초조하게 해 없음
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		;위로돌리고
		IF TFLAG:위로돌리고
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;지문은 조교 대상의 하반신 복장 마다의 분기 있어. 필요에 따라서 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;위로돌리고
		IF TFLAG:위로돌리고
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_1, 가랑이 한다
;━━━━━━━━━━━━━━━━━━━━━━━━
;·여성기와 넓적다리로 남성기를 사이에 두어 애무한다
;·조교 대상으로 페니스가 있으면, 반드시 가랑이 하는게 된다
;-------------------------------------------------
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		;초조하게해번 파생
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게 해 안
			CASE IS > 1
				PRINTFORMW 「아직 사정해서는 안 돼요?」
				PRINTFORMW 「쿠스. 노력할 수 있을 뿐(만큼) 노력하세요」
			;초조하게 해 종료
			CASE 1
				PRINTFORMW 「초조하게 하는 것은, 이 정도로 해 둘까」
				PRINTFORMW 「너의 끝 쪽이, 나의 입구에 해당되고 있는 것, 알까?」
				PRINTFORMW 「참지 않아도 괜찮아요?」
			;초조하게 해 없음
			CASEELSE
				PRINTFORMW 「너가 나의 아래에서 사정해 버리는 곳. 보고 있어 주어요♪」
				PRINTFORMW %CALLNAME:TARGET%는 발정해 버린 눈으로%CALLNAME:MASTER%의 페니스의 첨단을 먹어 들어가도록(듯이) 응시하고 있다
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		;위로돌리고
		IF TFLAG:위로돌리고
			PRINTFORMW 「이대로, 칠하고 있으면 삽입해 버릴 것 같구나…」
			PRINTFORMW 「삽입되어지고 싶어?」
			PRINTFORMW 「%RAND_SPLIT("나는 삽입되어지고 싶어요/끝 쪽만, 삽입되어져 버릴까…")%」
			PRINTFORMDW %CALLNAME:TARGET%는 질 구멍으로%CALLNAME:MASTER%의 귀두를 찌부러뜨리면서 허리를 상하에 흔들고 있다
		;그 외
		ELSE
			PRINTFORMW 「너, 허벅지를 좋아하는 걸? 전에 무릎 베개 해 주었을 때, 정신나가고 있었던 것♪」
			PRINTFORMW 「너의 성벽에 교제해 주기 때문에, 감사하세요」
		ENDIF
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;지문은 조교 대상의 하반신 복장 마다의 분기 있어. 필요에 따라서 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;위로돌리고
		IF TFLAG:위로돌리고
			PRINTFORMW 「너의%RAND_SPLIT("/자지/자지")%는, 타지면 건강하게 되는거네요」
			PRINTFORMW 「우후훗♪찌부러뜨려져 자지 기쁜 것 같구나」
		;그 외
		ELSE
			PRINTFORMW 「허벅지로 사이에 두어 주기 때문에, 너는 얌전하게 해 주세요」
			PRINTFORMW 「무엇 밀어넣으려고 하고 있는거야. 마음대로 쑤셔 넣으면 화내요」
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_2, 페니스밴드 가랑이
;━━━━━━━━━━━━━━━━━━━━━━━━
;·조교 대상이 페니스밴드장착 안은, 페니스밴드 가랑이가 된다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;지문은 조교 대상의 하반신 복장 마다의 분기 있어. 필요에 따라서 확장해 주세요
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		;위로돌리고
		IF TFLAG:위로돌리고
			PRINTFORMW
		;그 외
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT17, 파이즈리한다
;─
;·TFLAG:벌플래그가 끊고 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"의 경우는 발생하지 않는다
;·TEQUIP(오나홀/삼각목마) 장착중에는 발생하지 않는다
;·조교 대상으로 페니스가 없는 경우는 발생하지 않는다
;·조교자가 남자의 경우는 발생하지 않는다
;·현재 ACT파생는 존재하지 않지만 장래의 개변에 대비해, ACT파생 == 0으로 한다
;-------------------------------------------------
@KOJO_ACT_K1_17
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT17_0, 파이즈리한다
;━━━━━━━━━━━━━━━━━━━━━━━━
	;로션의 사용을 수반한다
IF 구상기입체크(100) && !TFLAG:ACT파생
	;지문은 조교자의 가슴의 사이즈로 분기 합니다.
	;소질을 변경하는 것이 가능한 것으로, 필요에 따라서 확장하는 것도 좋을 것입니다
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORML 「이런 것은 어떨까?」
		PRINTFORMW 「언제나 나의 가슴, 의식하고 있었던 것이군요?」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교내 첫회
	;=================================================
	IF 구상기입체크(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	;=================================================
	;거유 초조하게 해 종료시 파이즈리페라에 파생 입을 사용할 수 없는 상황에서의 언동에 주의
	;=================================================
	;지문
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 페니스로부터 손을 떼어 놓았다…
	;
	;%CALLNAME:TARGET%는 풍만한 가슴으로부터 간신히 얼굴을 내비친 %CALLNAME:MASTER%의 귀두에
	;입맞춤해, 사정의 순간을 기다렸다…
	;구상 여기
	;
	;%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 몸에 로션을 발랐다…
	;%CALLNAME:MASTER%의 몸이라고 하는 것보다는 스스로의 가슴에생각이 든다
	
	IF 구상기입체크(100) && TALENT:거유 && TCVAR:MASTER:초조하게해번 == 1
		;효과음에 대해 「」() 없음
		CALL FADEOUT(2, "짠다")
		CALL FADEIN(4, "(이고라고 끝내라)")
		RETURN 1
	ENDIF
		
	;=================================================
	;조교 대상이 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:갈것같다
		;초조하게해번에 의해 분기
		SELECTCASE TCVAR:MASTER:초조하게해번
			;초조하게 해 안
			CASE IS > 1
				PRINTFORMW 
			;초조하게 해 종료
			CASE 1
				PRINTFORMW 
			;초조하게 해 없음
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 「어머 어머♪젖가슴으로 사이에 두는 것, 마음에 들었을까?」
		PRINTFORM 「%RAND_SPLIT("히히말하면서 견뎌도/필사적으로 참아도")%… 젖가슴에는 이길 수 없어요? 응후후
		CALL HEARTBW
		RETURN 1
	ENDIF
		[SKIPSTART]
		;---------------------------------------------------
		;지문에는 착의 상태에 의한 분기가 존재합니다만, 나누어 쓰고도 귀찮은 것으로 필요에 따라서 comment out를 제외하든지
		;바라는 형태에 개변하든지 해 사용해 주세요
		;---------------------------------------------------
		;간섭하는 의복이 존재할 때
		IF TRUECHECK(TEQUIP:상반신속옷, TEQUIP:상반신윗도리, TEQUIP:전신윗도리) > 0 || (TEQUIP:MASTER:하반신속옷 && ! TALENT:벽통과) || CFLAG:의상 == 3
			;노출 상황에 의해 분기
			SELECTCASE TEQUIP:착의노출 & 6
				;간섭 개소전노출시
				CASE 6
					PRINTFORMW 
				;조교 대상 성기 노출시
				CASE 4
					PRINTFORMW 
				;조교자 가슴 노출시
				CASE 2
					PRINTFORMW 
				;간섭 개소비노출시
				CASEELSE
					PRINTFORMW 
			ENDSELECT
		;그 외
		ELSE
			PRINTFORMW 
		ENDIF
		[SKIPEND]
		;그 밖에 분기를 마련하지 않는다면, 여기는 써 둡시다
		;분기를 마련한다면 여기의 PRINT는 공행을 낳으므로 삭제합시다
		
	;=================================================
	;여기에 착의 파이즈리를 떠날 수 있자;빈유표백함 분기도 필요하게 된다
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		IF IS_CONDITION(TARGET, "정욕")
			PRINTFORM 「젖가슴으로 찌부러뜨려 주어요
			CALL HEARTBW
			PRINTFORMW 「어머 어머,%CALLNAME:TARGET%의 젖가슴 보지\@ TCVAR:MASTER:갈것같다 ? 로 말해 버리세요 # 기분이 좋아?  \@」
		ELSE
			IF !RAND:2
				PRINTFORMW 「젖가슴의 시간이야」
				PRINTFORMW 「응후후… 어머 어머. 자지 행복하게%RAND_SPLIT("되어 버리세요/해 주어요")%」
			ELSE
				PRINTFORMW 「젖가슴으로 찌부러뜨려 주어요」
				PRINTFORM 「기분이 좋아? 사이에 두고 있어 주기 때문에, 움직여도 괜찮아요
				CALL HEARTBW
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT18, 풋잡한다
;─
;·풋잡한다
;·조교 대상으로 페니스가 없는 경우는 발생하지 않는다
;·이상계로부터 파생하는 경우도 있다
;·TFLAG:벌플래그가 끊고 있으면 발생하지 않는다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·TEQUIP(오나홀/삼각목마) 장착중은 발생하지 않는다
;·성교중/성교봉사중은 발생하지 않는다
;·조교자에게 다리가 없으면 발생하지 않는다
;·조교 대상이 여자 아이라면 발생하지 않는다
;·현재 ACT파생는 존재하지 않지만 장래의 개변에 대비해, ACT파생 == 0으로 한다
;-------------------------------------------------
@KOJO_ACT_K1_18
;-------------------------------------------------
;·ACT파생없음
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT18, 풋잡한다
;━━━━━━━━━━━━━━━━━━━━━━━━	
	;풋잡은 작자의 취미는 아닌 것 같아, 지문은 비교적 부실이었거나
	;반대로 말하면, 다양하게 해도 지문과의 정합성이 무너질 걱정은 그다지 없기 때문에, 좋을대로 하면 좋을 것입니다
;=================================================
;첫회
;=================================================
IF 구상기입체크(100) && FIRSTACT(0)
	PRINTFORMW 「다리가 좋은거네? 너는 밟히고 기뻐하는 변태도, 나의 감이 말하고 있는거야」
	PRINTFORMW 「한심한 소리 올려 버려… 어쩔 수 없기 때문에♪」
	;첫회에 한정할 것도 없는 것 같은 언동
	PRINTFORMW 「너의 한심한 자지, 내가 돌봐 주어요♪」
	RETURN 1
ENDIF
	
	;지문에 TCVAR:MASTER:초조하게해번 등의 분기는 존재하지 않는 개소
	;=================================================
	;조교 대상이 가 그렇게 양 다리로 %CALLNAME:MASTER%의 페니스를 몹시 거칠게 훑어 왔다…
	;=================================================
IF 구상기입체크(100) && !TFLAG:ACT파생
	IF 구상기입체크(100) && TCVAR:MASTER:갈것같다
		IF POLICY("오늘") == "자 만들기"
			PRINTFORMW 「이봐요 이봐요… 나의 일%RAND_SPLIT("임신시키고/임신하게 했다")%싶지요? 보지에 질내사정하고 싶지요?」
			PRINTFORMW 「그렇다면 다리로는… 가서는 안 돼요? 응후후…」
			PRINTFORM 「노력하세요
			CALL HEARTBW
			RETURN 1
		ENDIF
	ENDIF

;=================================================
	;연속 실행 유쾌한 것 같게 %CALLNAME:MASTER%를 내려다 보면서 페니스를 동글동글 짓밟고 있다…
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT == TFLAG:전ACT
		;안면승마 안 이외
		IF !TEQUIP:안면승마 || !TEQUIP:안면승마애널
			IF TEQUIP:하반신윗도리
				PRINTFORML
				PRINTFORMDW %CALLNAME:TARGET%는 스커트의 옷자락을 집어올렸다
				PRINTFORMW 「눈이 추잡해요?」
				PRINTFORMW 「너는%RAND_SPLIT("사실,/정말,/, 우후후…")%%RAND_SPLIT("호색가/호색가/엣치")%니까」
				PRINTFORMW 「… 너에게%RAND_SPLIT("녀로서/그러한 눈으로/추잡한 눈으로")%볼 수 있는 것은,%RAND_SPLIT("나쁜 생각은 들어/싫지/그다지라도")%없어요」
				RETURN 1
			ENDIF
		ENDIF
	ENDIF
	
	;지문에 파생은 마련해 있지 않습니다만, 이것은 비교적 필요라고 생각되므로
	;=================================================
	;양말 신은 채로 풋잡(디폴트로 양말 신지 않은 캐릭터는 표시되지 않는다)
	;=================================================
	IF 구상기입체크(0) && TEQUIP:양말
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;조교자에게 TALENT:벽통과가 없는 경우, 옷감 너머로 된다
	;=================================================
	;하반신 복장이 방해
	;=================================================
	IF 구상기입체크(0) && PANTSCHECK(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		;서늘한 시선이 부족함
		SELECTCASE RAND:3
			CASE 0
				PRINTFORMW 「무엇 밟혀 기뻐하고 있는거야, 이%RAND_SPLIT("안돼/바보")%자지」
			CASE 1
				PRINTFORMW 「응후후…%RAND_SPLIT("에잇에잇/동글동글/어머 어머")%♪… 응후후♪」
			CASEELSE
				PRINTFORMW 「우후후♪너가 좋아하는 일, 가득 해 주어요」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;─
;ACT19, 손가락삽입해라(ACT19_0, 손가락삽입해라/ACT19_1, G스팟 자극/ACT19_2, 바이브 출납)
;─
;·조교 대상이 후타나리 혹은 여자의 경우에 발생
;·조교 대상이 처녀의 경우 G스팟 자극등에 파생하지 않는＆쾌V에 마이나스 보정
;·바이브 장비중은 바이브 출납에 파생
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·TEQUIP(삼각목마/페니스밴드) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교 대상이 남자라면 발생하지 않는다
;·조교 대상이 후타나리의 경우, 초조하게 해 안에는 발생하지 않는다
;-------------------------------------------------
;※조교 대상이 처녀는 아니다
;※연속 실행의 경우
;※행그렇게 or쾌C가 5000이상의 경우, 조교자의 애무 스킬에 응해
;※발기도가 낮은 경우, 조교자의 애무 스킬에 응해
;※요점강제발기의 경우
;·상기 조건의 경우 G스팟 자극에 파생한다
;-------------------------------------------------
@KOJO_ACT_K1_19
;-------------------------------------------------
;·TFLAG:ACT파생(0=손가락삽입해라/1=G스팟 자극/2=바이브 출납)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_0, 손가락삽입해라 질구에 손가락끝을 주어, 살그머니 안쪽에 밀어넣었다…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;안면승마 안
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TEQUIP:안면승마, TEQUIP:안면승마애널) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;위치전후 분기는 필요에 따라서
	;어디선가 통합해 버리는 것도 좋다고 생각합니다
	;=================================================
	;뒤로부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_1, G스팟 자극 손가락을 진입시키고 G스팟을 직접 자극했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;행그렇게 or쾌C가 5000이상의 경우, 조교자의 애무 스킬에 응해 G스팟 자극
;G스팟 고문에 파생하면 전립선자극 취급이 된다
;-------------------------------------------------
;조교 대상이 후타나리의 경우에서도 발생하므로, 필요에 따라서 PENIS(MASTER) 되어! PENIS(MASTER)로 분기를 마련하면 좋다고 생각합니다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;뒤로부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;전부터
	;=================================================
	IF 구상기입체크(0) && TCVAR:MASTER:위치전후 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_2, 바이브 출납 G스팟을 후벼파도록(듯이) 격렬하게 움직였다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;지문 자체에 파생은 없습니다만, @TARGET_DESCRIPTION에서의 상황 묘사가 들어갈 가능성이 있습니다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	PRINTFORMW 
	RETURN 1
ENDIF

;─
;ACT27, 밀어넘어뜨린다
;─
;·밀어넘어뜨린다/밀어넘어뜨리기 종료
;·이미 밀어넘어뜨리기라고 있는 경우, 해방이 된다
;·TFLAG:벌플래그가 끊고 있으면 발생하지 않는다
;·오늘의방침가 "러브러브"라면 발생하지 않는다
;·TEQUIP:페니스밴드장착 안에는 발생하지 않는다
;·초조하게 해 안에는 발생하지 않는다
;-------------------------------------------------
@KOJO_ACT_K1_27
;-------------------------------------------------
;TFLAG:ACT파생(=0, 밀어넘어뜨린다/=1, 밀어넘어뜨리기 종료)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT27_0, 밀어넘어뜨린다 %CALLNAME:MASTER%를 밀어넘어뜨리기…
;━━━━━━━━━━━━━━━━━━━━━━━━
;지문에 파생은 없습니다만, @TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성이 있습니다
;-------------------------------------------------
;성교 봉사는 발생하지 않지만 파이즈리는 발생한다
;밀어넘어뜨리기 착정은 발생하지 않는 너를 가게 하는 것이 많은 대개 자위가 온다
;-------------------------------------------------
IF 구상기입체크(100) && !TFLAG:ACT파생
	;-------------------------------------------------
	;상태 분기
	;-------------------------------------------------
	IF 구상기입체크(100) && IS_CONDITION(TARGET, "정욕")
		PRINTFORMDW !
		;묘사와 호흡을 떼어내지 않게 공행의 comment out
		;PRINTFORML
		PRINTFORMW 「%RAND_SPLIT("… 훗…/하… 하… 핫…")%」
		PRINTFORML
		PRINTFORMDW %CALLNAME:TARGET%의 한숨은 뜨겁게 정욕에 물들고 있다
		RETURN 1
	ENDIF
	IF 구상기입체크(100) && IS_CONDITION(TARGET, "분노")
		PRINTFORMDL %CALLNAME:TARGET%는 노기를 임신한 눈을 하고 있다
		RETURN 1
	ENDIF
	IF 구상기입체크(100) && IS_CONDITION(TARGET, "지루함")
		PRINTFORMW 「졸려져 왔어요… 너이불이 되세요」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;조건 없음
	;-------------------------------------------------
	IF 구상기입체크(100)
		SELECTCASE RAND:4
			CASE 0
				PRINTFORML 「지금부터 어떻게 하셔 버리는지, 아네요?」
				PRINTFORML 「응후후, 귀여워해 주기 때문에 각오 하세요」
			CASE 1
				;새끼 고양이를 잡는것 같이. 후는 4개
				PRINTFORML 「응후후후후…」
				PRINTFORMDL %CALLNAME:TARGET%는 의미있는 웃음을 하면서%CALLNAME:MASTER%에 덮어씌워 왔다
				;뭐야 저항하는 것 이 자식!
				PRINTFORML 「응─?」
				;새끼 고양이를 잡았는지와 같이. 후는 5개
				PRINTFORML 「응후후후후후♪」
			CASE 2
				CALL FADEIN(12, "「저항하지 않으면, 난폭한 흉내는…」")
				CALL FADEOUT(8, "「아니 뭐 그…」")
				PRINTFORMW 「어쨌든, 체념하세요」
			CASE 3
				PRINTFORMW 「얌전하게 하세요」
				PRINTFORML 「얌전하게 해도, 난폭한 흉내는 하지만…」
				CALL FADEOUT(12, "「거기는, 응…」")
				CALL FADEIN(4, "「체념하세요」")
			;죽어 분기
			CASEELSE
				PRINTFORML 「지금부터 어떻게 하셔 버리는지, 아네요? 응후후후후…」
				PRINTFORMW 「모른다고 시치미를 떼어도, 허락해 주지 않아요」
				;%CALLNAME:TARGET%씨에게 철저 능욕 되는 에로 동인 주세요
				PRINTFORMW 「♪하는거야. 에로 동인같이」
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT27_1, 밀어넘어뜨리기 종료		%CALLNAME:MASTER%를 자유롭게 했다…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 1
	PRINTFORMW 「네. 끝」
	RETURN 1
ENDIF