;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무 구상 봉사계 액션 구상 파일
;
;	봉사계 액션 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;봉사계 ACT50～57
;========================================================================
;·TFLAG:ACT(50=손으로애무를강제/51=입으로애무를강제/52=파이즈리/53=가랑이/55=다리빨고강제/56=이라마치오/57=가슴애무강제)
;·TFLAG:ACT분류 == 5에 해당
;·범용 리액션에 대해서는 MENUMATCH(TFLAG:ACT, "봉사")로 줍는 것이 가능
;·조교 대상으로, 조교자의 신체를 애무하도록(듯이) 명령한다
;·기본적으로는 명령인 것으로, 이 단계에서는 따를지 어떨지 확정하고 있지 않다(ACT56, 이라마치오는 거부 할 수 없다)
;-------------------------------------------------
;·ACT54, 풋잡은 12/04/08판으로 숙청
;-------------------------------------------------
;·반환값(RESULT)에 대해
;·시스템측에서는 구상측의 RESULT(종단의 RETURN 1 등)에 의해 구상이 표시되었는지 아닌지,
;·또 개소 마다의 특유의 거동(미약등 구상을 스킵 따위)을 제어하고 있습니다
;·@구상 기입 체크의 경우 대입 처리를 생략 할 수 있습니다만, RESULT치는 구상측에서 결정하고 쳐 할 필요가 있습니다
;·특별한 이유가 없는 한, 텐프레로서는 RETURN 1을 기술해 둡니다
;-------------------------------------------------

;─
;ACT50, 손으로애무를강제
;─
;·애무 강제/자위 강제/페니스밴드 자위 강제
;·조교 대상(MASTER)으로, 손에 의한 애무를 강제한다
;·조교자에게 페니스가 있으면 반드시 자위 강제가 된다(페니스가 있으면 통상의 애무 강제는 일어나지 않는다)
;·조교자가 페니스밴드장착 안은 페니스밴드 자위 강제가 된다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "하드", "이상"이라면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(줄/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교 대상이 따르는지 아닌지는 이 시점에서는 미정
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_ACT_K1_50
;-------------------------------------------------
;·TFLAG:ACT파생(0=애무 강제/1=자위 강제/2=페니스밴드 자위 강제)
;-------------------------------------------------
;·지문은 ACT파생 마다 분기. 각 ACT파생내에서는 분기 없음
;·ACT파생 0, 애무때만 쾌B/쾌ASOURCE 증가
;·그 외의 경우는 쾌C한정
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50, 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
;이 구조로 첫회 분기에 떨어지면 파생에 관련되지 않고 한 번 뿐인 표시
;파생마다 첫회 분기를 마련하고 싶으면 각 ACT파생내에서 처리하는 일을 추천합니다
;FIRSTACT(0)/FIRSTACT(1)/FIRSTACT(2)와 같이 나누어 주면 파생마다 첫회를 참조할 수 있습니다
;-------------------------------------------------
IF 구상기입체크(100) && FIRSTACT(0)
	;=================================================
	;ACT파생 1, 자위 강제
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT파생 2, 페니스밴드 자위 강제
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 2
		PRINTFORMW
		RETURN 1
	ENDIF
	
	;상층의 분기를 묻었을 경우는 TFLAG:ACT파생 == 0으로 동의가 된다
	;여기에서만 끝마친다면, 범용적으로 사용할 수 있도록(듯이) 유의해야 할 개소
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「이봐요, 너가%RAND_SPLIT("해/해? /해 줘")%」
		PRINTFORML 「분명하게 기분 좋게 하세요? 너이니까 손대게 하기 때문에」
		SIF IS_CONDITION(TARGET, "정욕")
			PRINTFORML 「%RAND_SPLIT("이봐요, 빨리/빨리, 빨리 해/부탁")%」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_0, 애무 강제 「자신의 성기를 손으로애무 하라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「이봐요, 너의 차례야」
		PRINTFORML 「상냥하게 하세요? 확실히 해 주지 않으면, 다음에 아프기 때문에」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_1, 자위 강제 「자신의 남성기를 손으로 잡아당기라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_2, 페니스밴드 자위 강제 「페니스밴드를 손으로 잡아 움직이라고 명령한다」
;━━━━━━━━━━━━━━━━━━━━━━━━
;페니스밴드에의 애무는 쾌CSOURCE가 감소해, 쾌VSOURCE가 증가한다
;장착하고 있는 시점에서 대상이 처녀일 가능성은 없다
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;여기를 최초로 씁시다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;─
;ACT51, 입으로애무를강제
;─
;·쿤닐링구스/펠라치오/안면승마/안면승마애널/
;·조교 대상으로, 입에 의한 애무를 강제한다
;·조교자가 후타나리의 경우, 조교 대상이 위로돌리고라면 1/2으로 안면승마, 그렇지 않으면 페라 강제가 된다
;·조교자에게 페니스가 없고, 조교 대상이 위로돌리고라면 안면승마가 된다
;·안면승마가 선택되고 있었을 때, 안면승마애널에 변화하는 일이 있다
;·모든 조건으로부터 빠졌을 경우는 통상의 쿤닐링구스가 된다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각목마) 장착중은 발생하지 않는다
;·조교자가 페니스밴드를 장착하고 있는 경우는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_ACT_K1_51
;-------------------------------------------------
;·TFLAG:ACT파생(0=쿤닐링구스/1=페라/2=안면승마/3=안면승마애널)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51, 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
;우선 FIRSTACT(0)로 최상층에 첫회를 배치했습니다만, 각 ACT파생내에서 처리하는 것도 좋을 것입니다
;FIRSTACT의 이벤트번호를 입지 않도록 하면, 파생마다 첫회 판정을 할 수 있습니다
;-------------------------------------------------
IF 구상기입체크(100) && FIRSTACT(0)
	;=================================================
	;ACT파생가 페라 강제
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT파생가 안면승마 69
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		PRINTFORMW 「… 푸핫」
		;말해질 때까지 하지 않는 소극적인Я당신
		PRINTFORML 「저기? 너도 해」
		PRINTFORMW 「아뭇」
		RETURN 1
	ENDIF
	
	;=================================================
	;ACT파생가 안면승마애널
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;상층의 분기를 모두 묻었을 경우는 쿤닐링구스 강제 취급이 된다
	;파생마다 나누어 쓰지 않는 것이면, 이 개소를 범용적으로 사용할 수 있는 내용으로 한 다음 불필요한 분기를 삭제하면 좋을 것입니다
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		[SKIPSTART]
		;IF TALENT:TARGET:처녀
		;	PRINTFORMW 
		;ELSE
		;	PRINTFORMW 
		;ENDIF
		[SKIPEND]
		CALL FADEIN(4, "쿠빠아")
		PRINTFORMW 「이봐요, 그… 알겠죠?」
		CALL FADEOUT(4, "「그…」")
		CALL FADEIN(8, "「처음이니까」")
		PRINTFORMW 「제대로 풀어요」
		;페니반 처녀상실에 대응하는 내용으로 하지 않으면 안 된다. 쌍두 페니반 금지한 (분)편을 락 할 수 있을 것 같다
		PRINTFORMDW %CALLNAME:TARGET%는 남자를 모르는 비밀스런 장소를%CALLNAME:MASTER%의 전에 드러냈다
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상을 C를 꾸짖어지고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상의 C를 꾸짖으면서 안면승마가 되는 시추에이션
;안면승마가 되는 경우는 조교자의 페니스, 페니스밴드등을 고려하는 필요없음
;필요에 따라서
;IF TEQUIP:페니스밴드
;ELSEIF PENIS(MASTER)
;ELSE
;등의 분기를 마련해 사용해 주세요
;TEQUIP:조교대상C사용 == 1은 손으로애무중
;TEQUIP:조교대상C사용 == 2는 입으로애무중이 됩니다
;일단 입과 손 각각의 분기를 마련했습니다만
;조교자의 소질에 따라서는 입으로애무가 대부분 나오지 않았다거나 하므로, 그 근처를 지켜본 다음 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(100) && GROUPMATCH(TEQUIP:조교대상C사용, 1, 2) > 0
	;=================================================
	;질내사정 후 3 턴 이내		질구로부터는 조금 전 충분히 짜낸 정액이 걸죽 넘쳐 나오고 있다…
	;=================================================
	IF 구상기입체크(0) && INRANGE(TCVAR:MASTER:질내사정후경과턴, 1, 2)
		;입으로애무중
		IF TEQUIP:조교대상C사용 == 2
			PRINTFORMW 
		;손으로애무중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;안면승마애널 파생			눈앞에 실룩거리는 국문이 드러내지고 있다……
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 3
		;입으로애무중
		IF TEQUIP:조교대상C사용 == 2
			PRINTFORMW 
		;손으로애무중
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;통상의 안면승마를 상정@VAGINA_DESCRIPTION는 묘사계 함수
	;=================================================
	;그 외·조건 없음			무방비인%VAGINA_DESCRIPTION(TARGET)%가 드러내지고 있다……
	;=================================================
	IF 구상기입체크(100)
		;입으로애무중
		IF TEQUIP:조교대상C사용 == 2
			PRINTFORMW (자지 빨아 젖어 와 버리고 있는 것, 알아 버릴까?)
		;손으로애무중
		ELSE
			IF !RAND:3
				PRINTFORMW 「여기는 맡겨져 주기 때문에, 너도 해 줘」
		PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%로부터의\@ AFFECTION("상사상애") ? 애정 # 자극 \@를 요구하는 것처럼 허벅지와 허벅지를 머뭇머뭇 구불거리게 되어지고 있다…
		PRINTFORML 
			ELSEIF !RAND:2
				;레이무씨의기분좋게해줘!
				PRINTFORMW 「이봐요…%CALLNAME:TARGET%의 여기도, 기분좋게해줘…」
				PRINTFORMDW %CALLNAME:TARGET%의 비밀스런 장소는 접하는 전부터 실룩거려,%CALLNAME:MASTER%의 봉사를 애타게 기다리고 있다…
			ELSE
				PRINTFORMW 「반찬이 있는 것이 좋네요」
				PRINTFORMW 「%CALLNAME:TARGET%의 보지 반찬으로 해, 기분 좋아져 버리세요」
			ENDIF
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_0, 쿤닐링구스 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「… 빨리 하세요」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_1, 펠라치오 주인님에 빨아 된다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_2, 안면승마
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 2
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;질내사정 후 3 턴 이내
	;=================================================
	IF 구상기입체크(0) && INRANGE(TCVAR:MASTER:질내사정후경과턴, 1, 2)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「\@ ALI(0) ? 이봐요,  # 날뛰지 말고,  \@확실히 날름날름 하세요」
		PRINTFORMW 「너는 나의 엉덩이에 깔리고 있는 것이 어울려요」
		[SKIPSTART]
		;분기 조건이 필요
		CALL FADEOUT(2, " 「훗…」")
		CALL FADEIN(4, " 「응읏…」")
		;레이무의 자지이기 때문이다(키릭)라고 말했으면 좋은 후타나리 소녀의 아가씨의 마음의 보강
		PRINTFORMW 「\@ TALENT:MASTER:남자? 남자의 주제에 #  \@자지 빨게 되어져,%RAND_SPLIT("어째서 그렇게 기쁜 듯한 것이야/무엇 기쁜듯이 하고 있는거야")%」
		[SKIPEND]
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_3, 안면승마애널
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && TFLAG:ACT파생 == 3
	;쾌C 이외는 TCVAR:갈것같다에서는 주울 수 없기 때문에 PALAM로
	;=================================================
	;조교자의 쾌A가 일정 이상 「갈것같다」
	;=================================================
	IF 구상기입체크(0) && PALAM:쾌A >= 8500
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;연속 실행
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT == TFLAG:전ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW (그쪽이 아니에요. 바보!)
		PRINTFORMDL %CALLNAME:TARGET%는%CALLNAME:MASTER%의 준비를 갖추면서, 안타까운 듯이 허리를 상하시켰다
		RETURN 1
	ENDIF
ENDIF

;─
;ACT52, 파이즈리
;─
;·조교 대상으로 파이즈리에서의 봉사를 강제한다
;·조교 대상이 남자의 경우는 발생하지 않는다
;·조교자에게 페니스가 없는 경우는 발생하지 않는다
;·턴방침이 "이상"의 경우는 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(줄/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절 취급이 된다
;-------------------------------------------------
@KOJO_ACT_K1_52
;-------------------------------------------------
;·ACT파생없음. 지문에 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT52, 파이즈리 당신이 젖가슴혈역
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;조교 대상의 B절정이 가깝다고 하는 시추에이션
	;=================================================
	;조교 대상의 쾌B가 일정 이상
	;=================================================
	IF 구상기입체크(0) && PALAM:MASTER:쾌B >= 8500
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 거유
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:거유
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 빈유
	;=================================================
	IF 구상기입체크(0) && TALENT:MASTER:빈유
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외·조건 없음
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「이렇게 크기 때문에 사용하지 않으면, 아까워요」
		RETURN 1
	ENDIF
ENDIF

;─
;ACT53, 가랑이
;─
;·조교 대상으로 페니스밴드, 혹은 페니스를 가랑이에 끼워 스치고라고 명령한다
;·조교자에게 봉이 없다(페니스/페니스밴드) 경우는 발생하지 않는다
;·조교 대상으로 페니스가 있는 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"의 경우는 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:삼각목마 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교 대상으로 다리가 없으면 발생하지 않는다(현재 상태로서는 토지코가 조교 대상인 경우만이라고 생각된다)
;·REACT분류가 거부/날뛴다/도망치는인 경우는 거절 취급
;-------------------------------------------------
@KOJO_ACT_K1_53
;-------------------------------------------------
;·TFLAG:ACT파생(0=가랑이/1=페니스밴드 가랑이)
;-------------------------------------------------
;·안면승마계, 자위계는 해제된다
;·지문은 ACT파생로 분기. 파생내에서는 분기 없음@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
;우선 최상층에 배치했습니다만, ACT파생내에서 처리해도 된다고 생각합니다
;FIRSTACT의 인수를 가지런히 하는지, FIRSTACT(0), FIRSTACT(1)로서 개별적으로 첫회 판정을 주울까는 쓰는 사람 나름
;-------------------------------------------------
IF 구상기입체크(0) && FIRSTACT(0)
	;=================================================
	;페니스밴드 가랑이 강제
	;=================================================
	IF 구상기입체크(0) && TFLAG:ACT파생 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;TFLAG:ACT파생 == 1의 분기를 썼을 경우는 ACT파생 == 0(가랑이 강제) 취급이 된다
	;상층의 분기를 방치해 여기에만 한다면, 범용적으로 사용할 수 있도록(듯이) 유의해야 할 개소
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_0, 가랑이 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자에게 페니스가 있는 경우
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	[SKIPSTART]
	;이 분기는 바리안트에 존재하지 않습니다
	;첫회 또한 당신이 남자의 경우
	PRINTFORMW 「어? 미안해요. 조금 기다려」
	PRINTFORML 동글동글
	PRINTFORMW 동글동글
	PRINTFORML 「들어가지 않네요…」
	PRINTFORMDL %CALLNAME:TARGET%는 야오이구멍에 삽입하려고 귀두를 %CALLNAME:MASTER%의 좁은 산길에 강압하면서 이상한 것 같게 고개를 갸웃하고 있는…	
	[SKIPEND]
	;=================================================
	;갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TCVAR:TARGET:갈것같다, TCVAR:MASTER:갈것같다) > 0
		;조교자(TARGET)가갈것같다
		IF TCVAR:TARGET:갈것같다
			PRINTFORMW 
		;조교 대상(MASTER)이갈것같다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		;윤활 충분히
		IF PALAM:MASTER:윤활 >= 500
			PRINTFORMW 
		;윤활 부족
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_1, 페니스밴드 가랑이 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교자가 페니스밴드장착 안의 경우
;-------------------------------------------------
IF 구상기입체크(0) && TFLAG:ACT파생 == 1
	;=================================================
	;갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TRUECHECK(TCVAR:TARGET:갈것같다, TCVAR:MASTER:갈것같다) > 0
		;조교자(TARGET)가갈것같다
		IF TCVAR:TARGET:갈것같다
			PRINTFORMW 
		;조교 대상(MASTER)이갈것같다
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		;윤활 충분히
		IF PALAM:MASTER:윤활 >= 500
			PRINTFORMW 
		;윤활 부족
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;─
;ACT55, 다리빨고강제
;─
;·조교자가 다리를 빨라고 명령해 온다
;·이상계로부터 발생하는 경우 있어
;·턴방침이 "소프트"라면 발생하지 않는다
;·오늘의 방침이 "러브러브"라면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교자에게 다리가 없는 경우는 발생하지 않는다
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절 취급
;-------------------------------------------------
@KOJO_ACT_K1_55
;-------------------------------------------------
;·지문에 분기 없음@TARGET_DESCRIPTION로 상황 묘사가 들어갈 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT55, 다리빨고강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「이봐요, 너의 취미에 교제해 주어요」
		PRINTFORMW 「어차피라면 겨드랑이를 빨고 싶어? 차버려요♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;전회 COM가 허가를 청했다
	;=================================================
	IF 구상기입체크(0) && IS_PREVCOMNAME("용서를빈다")
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;오늘의방침가 벌의 경우는 벌플래그가 선 채로 있으므로 확률 표시, 혹은 ONCEACT를 사용하는 것을 추천
	;=================================================
	;벌모드
	;=================================================
	IF 구상기입체크(0) && TFLAG:벌플래그 ;&& ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 위로돌리고
	;=================================================
	IF 구상기입체크(0) && TFLAG:위로돌리고
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 양말을 신고 있다
	;(디폴트로 양말 신지 않은 캐릭터는 표시되지 않는다)
	;=================================================
	IF 구상기입체크(0) && TEQUIP:양말
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT56, 이라마치오
;─
;·명령하는 것은 아니고, 억지로 페니스를 입에 돌진한다
;·조교 대상의 반응은 미정이지만, 거절하는 일은 할 수 없다
;·가학계로부터 발생하는 경우 있어
;·턴방침이 "노멀", "소프트"라면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP(볼개그/삼각목마) 장착중에는 발생하지 않는다
;·성교중/성교봉사중에는 발생하지 않는다
;·조교자에게 페니스가 없는 경우는 발생하지 않는다(페니스밴드에 의한 이라마치오는 없다)
;-------------------------------------------------
@KOJO_ACT_K1_56
;-------------------------------------------------
;·자위중이면 해제된다
;·지문에 분기 없음. 지문은↓
;·%CALLNAME:TARGET%는 %CALLNAME:MASTER%의 머리를 잡아, 난폭하게 %CALLNAME:MASTER%의 목까지 찔러 넣었다…
;-------------------------------------------------
;·조교자의 발기도합 있고는 TCVAR:발기로 참조할 수 있다
;·TCVAR:발기(0= 아직 부드럽다/1=서서히 경도를 더하고 있다/2=발기한/2이상=뒤로 젖혔다)적인 느낌으로
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT56, 이라마치오
;━━━━━━━━━━━━━━━━━━━━━━━━
;필요에 따라서 분기를 증감해 주세요
;-------------------------------------------------
IF 구상기입체크(0) && !TFLAG:ACT파생
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && TCVAR:TARGET:갈것같다
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상이 위로돌리고
	;=================================================
	IF 구상기입체크(0) && TFLAG:위로돌리고
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;─
;ACT57, 가슴애무강제
;─
;·조교 대상으로, 조교자의 가슴을 애무하도록(듯이) 강제한다
;·조교자가 남자의 경우는 발생하지 않는다
;·TFLAG:벌플래그가 서 있으면 발생하지 않는다
;·턴방침이 "이상"이라면 발생하지 않는다
;·밀어넘어뜨리기중에는 발생하지 않는다
;·TEQUIP:줄장착중은 입으로 가슴애무강제에 파생한다. 다만 볼개그를 장착중의 경우는 튕겨진다
;·볼개그가 없으면,1/2의 확률로 입으로애무가 된다
;·REACT분류가 거부/날뛴다/도망치는 것 경우는 거절 취급
;·PALAM가 치정선행이나 반항 선행인가로"부끄러워서 봉사 할 수 없었다/싫어해 요구에 응하지 않았다"에 파생한다
;-------------------------------------------------
@KOJO_ACT_K1_57
;-------------------------------------------------
;·TFLAG:ACT파생(0=손으로 가슴애무강제/2=입으로 가슴애무강제)
;-------------------------------------------------
;·지문은 ACT파생로 분기. 파생내에서는 분기 없음
;·@TARGET_DESCRIPTION에 의한 상황 묘사가 들어갈 가능성 있어
;·안면승마는 해제된다
;·REACT에 대해, 위로돌리고가 아니고, ACT파생가 입으로애무가 아닌 경우,1/2그리고 뒤로부터 애무에 파생한다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
;우선 최상층에 배치했습니다만, ACT파생내에서 처리해도 된다고 생각합니다
;FIRSTACT의 인수를 가지런히 하는지, FIRSTACT(0), FIRSTACT(1)로서 개별적으로 첫회 판정을 주울까는 쓰는 사람 나름
;-------------------------------------------------
IF 구상기입체크(100) && FIRSTACT(0)
	;=================================================
	;ACT파생 2, 입으로 가슴애무를 강제
	;=================================================
	IF 구상기입체크(100) && TFLAG:ACT파생 == 2
		PRINTFORMW 「과연 부끄러울까?」
		PRINTFORMW 「나는, 굉장히 부끄러워요」
		RETURN 1
	ENDIF
	
	;TFLAG:ACT파생 == 2 분기를 묻었을 경우는! TFLAG:ACT파생분기로서 기능
	;여기에서만 끝마친다면, 어디라도 사용할 수 있는 내용을
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「가슴, 손대어」
		PRINTFORML 「그렇게 작은 것도 아니지요」
		PRINTFORMW 「지금부터, 너가 크게 해 줘?」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_0, 손으로 가슴애무를 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && !TFLAG:ACT파생
	;이전에는 체위마다야는은 규제되고 있었지만, 현재는 모든 체위로 발생한다
	;경우에 따라서는 엉뚱한 체제가 되지만, 이것은 현재 상태로서는 사양이라고 하는 일로
	;=================================================
	;조교자 삽입중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상 삽입중
	;=================================================
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		;PRINTFORML 「네」
		PRINTFORMDW 에
		PRINTFORMW 「………」
		PRINTFORMW 「해?」
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && PALAM:쾌B > 8500
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「너에게 손대어진다고 생각하면… 두근두근 해, 나쁘지 않은거야」
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_2, 입으로 가슴애무를 강제
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && TFLAG:ACT파생 == 2
	;이전에는 체위마다야는은 규제되고 있었지만, 현재는 모든 체위로 발생한다
	;경우에 따라서는 엉뚱한 체제가 되지만, 이것은 현재 상태로서는 사양이라고 하는 일로
	;=================================================
	;조교자 삽입중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교중
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교 대상 삽입중
	;=================================================
	IF 구상기입체크(0) && TEQUIP:성교봉사중
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;조교자가 갈 것 같다
	;=================================================
	IF 구상기입체크(0) && PALAM:쾌B > 8500
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;그 외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「가슴, 입이라서」
		PRINTFORMW 「잘 할 수 있으면… 머리 어루만져 주어요」
		PRINTFORMW 「가끔 씩은? 응석부리게 해 주어요」
		RETURN 1
	ENDIF
ENDIF