;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무 구상 조교 개시 이벤트 구상 파일
;
;	조교 개시 이벤트 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;EV11, 조교 개시 구상
;========================================================================
;·대체로의 경우, 구상 작자가 우선 생각하는 것은 "주인"으로서의 구상이라고 생각되므로
;·@KOJO_MS_EVENT로서 TALENT:주인님인 경우로 한정해 버립니다
;·이벤트 관련은 구상주의 입장에 의해 정합성이 손상될 가능성이 있으므로 이러한 처치로 했습니다
;·구상내의 모든 개소에서 구상 함수에 의한 같은 배타적 연산은 가능했다거나 합니다
;-------------------------------------------------
;  소환되어 조교를 맡겨지는 경우의 구상을 쓴다면
;  호출을@KOJO_EVENT_KX1_11으로서 배타를 실시하지 않고, TALENT:주인님과 그 외로 분기를 마련한다
;  EVENT1, OP(주인인 경우에 밖에 처리되지 않는다)로 KFLAG에의 대입을 실시해, 분기를 마련한다
;  혹은, @KOJO_AS_EVENT_KX1_11으로서 별파일에 분리해 버리면 좋을 것입니다
;-------------------------------------------------
;우선 분기를 마련해 있습니다만, EVENT11범위내의 분기의 증감은 자유롭게 할 수 있습니다
;COMMON_GETTER_CHARA.ERB내, @AFFECTION에는 여기에 없는 것도 정의되어 있거나 합니다
;다만, 조건의 느슨한 분기를 상층에 가지고 오면 하위의 분기가 모두 공수상이 되므로
;우선도에는 충분히 주의해 주세요
;-------------------------------------------------
@KOJO_MS_EVENT_K1_11(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(0, 통상/1, 조교자의 피로에 의한 중지/2, 조교 대상의 피로에 의한 중지)
;-------------------------------------------------

;────────────────────────────────────
;조교 첫회·10회째
;────────────────────────────────────
;·조교 첫회, 및 10회째에 표시되는 구상
;·CFLAG:조교횟수로 회수는 어떻게라도 참조할 수 있습니다만, 너무 하면 다른 조건을 탕진할 가능성 있어
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 첫회
;━━━━━━━━━━━━━━━━━━━━━━━━
[SKIPSTART]
;하부에 통합
;IF 구상 기입 체크(100) && CFLAG:조교횟수 == 1
;	PRINTFORMW 「… 처음은 손대어 사랑옛 노래등, 길들여 갑시다」
;	PRINTFORMW 「멍청이? 시끄럽네요. 너에게 듣고 싶지 않아요!」
;	PRINTFORML
;	PRINTFORMW 「… 이런 일 한 일 없기 때문에… 상냥하게 하세요?」
;	RETURN 1
;ENDIF
[SKIPEND]

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 10회째
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && CFLAG:조교횟수 == 10 && !ARG
	PRINTFORMW 
	RETURN 1
ENDIF

;────────────────────────────────────
;조교중지 ARG:1 ARG:2
;────────────────────────────────────
;CFLAG:소모 > 5혹은 CFLAG:MASTER:소모 > 5를 조건으로서 불려 가는 개소
;이 조건으로부터 빠졌을 경우는 통상의 조교 개시 구상이 불린다
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;피로로 조교가 중지가 되는 경우
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG > 0
	;=================================================
	;피곤한 것은 구상주(TARGET)
	;=================================================
	IF 구상기입체크(100) && ARG == 1
		PRINTFORMW 「오늘은 휴일이야. 너도 천천히 하세요」
		PRINTFORMDW %CALLNAME:TARGET%는 거실에서 뒹굴뒹굴 하고 있다
		RETURN 1
	ENDIF
	
	;=================================================
	;피곤한 것은 조교 대상(MASTER)
	;=================================================
	IF 구상기입체크(100) && ARG == 2
		PRINTFORMW 「피로가 모여 있는 것 같구나… 오늘은 쉬어도 좋아요」
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;그 외·호감도등에 의한 분기
;────────────────────────────────────
;·현재의 ACTABLE나 COMABLE의 사양적으로, 진전도를 측정하려면 (사실상) 호감도를 기준으로 할 수 밖에 없다고 생각된다
;·의존도는@COM로 대입되고 참조할 수 있을 뿐으로, 본체측의 거동에는 아무런 영향을 주지 않았다
;·향후의 손질 여하에 따라서는 용도도 있을지도? 그렇다고 하는 곳
;-------------------------------------------------
;-------------------------------------------------
;·서로 서로 좋아하고 있다
;-------------------------------------------------
;원래는 ED가 실장되어 있지 않은 시대에, 가짜의 ED 범위로서 사용되는 일이 많았던 개소
;현상에서도 이 단계에 이르려면, 그만한 일수와 진전이 필요하게 된다고 생각됩니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;상사상애(호의 3000이상·호감도 3000이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("상사상애")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(0)
		;과거 %CALLNAME:TARGET%묘사  월, 야, 설, 머플러, 요들꿈, 겨울안의 봄, 벚꽃의 꽃잎
		;PRINTFORMDW 

		PRINTFORMDW %CALLNAME:MASTER%는 침상에 앉으면서%CALLNAME:TARGET%에 동기에 대해 물어 보았다
		PRINTFORMW 「과정에 관련되고 있으면 너, 언제까지나 손을 대어 오지 않는 것 같았던 것. 실력 행사야!」
		PRINTFORMDW %CALLNAME:TARGET%에 다가서지고 번득 노려봐진다
		PRINTFORMDW 그럴지도 모른다…
		PRINTFORMW 「끝이 좋다면 그것으로 좋은거야♪」
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(100)
		PRINTFORMW 「자,%RAND_SPLIT("즐깁시다♪/기분 좋게 해 주어요!/서로 사랑합시다")%」
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·조교 대상이 구상 주로 이제(벌써) 헤롱헤롱
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 완료(호의 2000이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("조교 완료")
	;=================================================
	;첫회
	;=================================================
	;소녀 퇴고중
	IF 구상기입체크(100) && FIRSTEVENT(10)
		;미래에 대해 생각해 버리는 %CALLNAME:MASTER%. 좋아하는 사람의 성씨와 자신의 이름을 합체 시켜 물장구 하는 저것에 가깝다
		;주인님이 있는 미래 밖에 상상 할 수 없는 정도로 조교가 끝난 상태 
		PRINTFORMDW 이 생활이 시작되고 나서 상당히 긴 시간이 지난 것처럼 생각된다…
		PRINTFORMDW … 실제의 곳은 반년에도 차지 않는 짧은 것이지만 진한 매일은 나날을 길게 느끼게 했다
		PRINTFORML 
		PRINTFORMDW 요즈음은 이 생활에 익숙하고 여유가 나온 탓인지, 빈 시간에는 앞의 일을 생각하게 되었다
		PRINTFORMDW 오늘의 이 나날도 또, 어느것은 그리운 날이 될까
		PRINTFORMDW %CALLNAME:MASTER%는 눈시울을 닫아 조금 미래를 상상해 보았다
		PRINTFORML 
		;레이무의 거동  구상색  이 타이밍에는 근처에 있지만 방해는 하지 않는 레이무씨
		PRINTFORMW 보글보글…
		PRINTFORML
		PRINTFORMDW 그립구나
		PRINTFORMW 「그립네요」
		PRINTFORML
		PRINTFORMDW 손자의 얼굴을 보고
		PRINTFORMDW 할아버지가 된 자신과 할머니가 되었다%CALLNAME:TARGET%
		PRINTFORML
		PRINTFORMDW 나날 애를 먹혀지면서도
		PRINTFORMDW 애정이 깃들인 손바닥으로 아가씨를 어루만진다%CALLNAME:TARGET%
		PRINTFORML
		PRINTFORMDW 조금 침착해 어른스러워졌다
		PRINTFORMDW 요염한%CALLNAME:TARGET%
		PRINTFORML
		PRINTFORMDW 자식을 타고나고
		PRINTFORMDW 우리 아이를 잉 보고 임신인%CALLNAME:TARGET%
		PRINTFORML
		;%CALLNAME:TARGET%의 거동  구상색
		PRINTFORMW 보글보글…
		PRINTFORML 
		;흰색자. 당신 망상중. 레이무, 당신이 깨닫는 것을 기다린다
		PRINTFORMDW ………
		PRINTFORML 
		;선수, 레이무
		PRINTFORMW 염주…
		PRINTFORML
		PRINTFORMDW … 몇시부터인가 근처에는 따뜻하게 하고의 차와 조금 전까지 생각하고 있던 여성을 약이 같은 사랑스러운 소녀
		PRINTFORML 
		PRINTFORMDW …
		PRINTFORMW 염주…
		PRINTFORML 
		PRINTFORMDW %CALLNAME:TARGET%
		PRINTFORML 
		PRINTFORMW 「뭐야」
		PRINTFORML 
		PRINTFORMDW 어쩐지 조금 줄어든 것 같은, 사랑스러워졌다
		PRINTFORML 
		PRINTFORMW 「뭐야 그것」
		PRINTFORML 
		PRINTFORMDW %CALLNAME:TARGET%가 부드럽게 미소짓는다
		;여기서 최초 기분을 (듣)묻는 것에 연결되면 기려
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·조교자가 조교 대상에 빠져 버려, 깊기도 하고 도착 한 사랑을 따르고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;몹시 사랑함(호감도 2000이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && AFFECTION("몹시 사랑함")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTEVENT(20)
		RETURN 1
	ENDIF
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·조교 대상이 구상 주로 조교되는 것을 행복하게 느끼고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교가 끝난(호의 1000이상) 태연하게한다 적극적으로 하지만 다발하는 기회
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("조교가 끝난 상태")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(30)
		;PRINTFORMDW 그녀의 행동에 응하는 것도 나쁘지 않을지도 모른다
		;일행에서는 뭔가 외롭기 때문에 가필하지만, 이것은 이것대로 촌스럽다
		
		;PRINTFORMDW 너무나 불합리한 현상은 『%CALLNAME:TARGET%씨이니까 어쩔 수 없다』라고 납득이 갔다
		;PRINTFORMDW 그녀의 행동에 응하는 것도 나쁘지 않을지도 모른다
		
		;PRINTFORML 
		;PRINTFORMDW 염주…
		;무언으로 당신의 근처에 있는 레이무
		;PRINTFORMW  …
		;PRINTFORML 
		
		;일본식인 풍미가 좀 더 갖고 싶다  정적이 부족함
		;PRINTFORMDW 너무나 불합리한 현상도,
		;PRINTFORMDL 가슴의 버팀목도 목의 버팀목도 『%CALLNAME:TARGET%씨이니까, 어쩔 수 없는가』라고
		
		;표현에 조정을 필요로 한다
		;PRINTFORMDW 차가 씻어 납득이 갔다
		;PRINTFORMDW 차가 내장에 스며들면 뱃속으로부터 따뜻해져 갔다…
		
		;PRINTFORMDW 너무나 불합리한 현상도, 목의 버팀목도
		;PRINTFORMDL 가슴의 버팀목도 목의 버팀목도
		;향미가 부족할 정도 좋은 떫은 맛도
		;PRINTFORMDL %CALLNAME:MASTER%의 오장육부에 차의 미지근함이 스며든다
		;PRINTFORMDL 『%CALLNAME:TARGET%씨이니까, 어쩔 수 없는가』
		;PRINTFORMDL 와 %CALLNAME:MASTER%는 이상하게 납득했다
		
		;쓴 웃음인가 시원한 웃음인가  너는 싱글벙글 하고 있을까?
		;＞조교 대상이 구상 주로 조교되는 것을 행복하게 느끼고 있다
		;14. 5세의 아이를 웃어 지켜보는 28.9세의 어른 정도 그런 곳인가
		;
		
		;당신의 긴장이 풀릴 기색  레이무씨 기쁜 것 같다
		;당신의 긴장이 풀리고 조금 안심하는 레이무씨
		
		;1일째보다 전의 이야기를 낸다  성적인 관계를 시작해 경화한 바람이 풀린다
		;아니, 레이무씨 쪽이 부담은 있을 것인 상황
		;레이무씨는 씩씩한가?
		
		;여기는 당신의 레이무씨에게로의 호의의 개소이지만 굳이
		;레이무의 당신에게로의 호감도가 올랐는지와 같이 그려
		;당신의 심경의 변화가 레이무의 웃는 얼굴을 눈치채게 한다.
		;레이무씨는 최초부터 웃는 얼굴이었다. 그렇다고 하는 것은 어때
		;레이무씨는 최초부터 담담하게 무자비하게 즈조조야
		
		RETURN 1
	ENDIF
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·조교자에 대해 혐오감을 안고 있다
;━━━━━━━━━━━━━━━━━━━━━━━━
;혐오(호의-100 미만)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && AFFECTION("혐오")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(40)
		PRINTFORMDW %CALLNAME:TARGET%는 곤란한 것처럼 머리를 긁고 있다
		RETURN 1
	ENDIF
		
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·마음에 상처를 만들어져 주인님의 모습을 보는 것만으로 무서워해 버린다
;·트라우마의 종은 「%ACTSTR(CFLAG:MASTER:8)%」로 표시할 수 있다
;  예) PRINTFORMW 「이제(벌써)%ACTSTR(CFLAG:MASTER:트라우마)%는 싫습니다 라고?」
;━━━━━━━━━━━━━━━━━━━━━━━━
;공포(호의 200 미만·트라우마 Lv3 이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && AFFECTION("공포")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTEVENT(50)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·마음을 열지 않는 노예에게 초조해지는 주인님
;━━━━━━━━━━━━━━━━━━━━━━━━
;반항(호의 0 이하·반항각인 Lv3 이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && AFFECTION("반항")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTEVENT(60)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·조교되는 것도 오히려 좋아하게 되었는지도 모른다…
;━━━━━━━━━━━━━━━━━━━━━━━━
;눈을 뜸(호의 500이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
;간신히 접하는데 익숙해지기 시작했을 무렵에 엉덩이 후비어진다
IF 구상기입체크(0) && AFFECTION("눈을 뜸")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTEVENT(70)
		;현재 종료 측에 이전
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;-------------------------------------------------
;·정중하게 조교를 진행되어, 생각했던 것보다 싫지 않을지도…
;━━━━━━━━━━━━━━━━━━━━━━━━
;순응(호의 100이상·호감도 100이상)
;━━━━━━━━━━━━━━━━━━━━━━━━
;일주일눈으로 간신히 %CALLNAME:TARGET%호감도 100과 같은 정도
IF 구상기입체크(100) && AFFECTION("순응")
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(80)
		;아직 초반. 당분간은 별실. 당신의 아래를 레이무가 다닌다
		PRINTFORMW 「… 내가 너의 곳에 다니다니 뭔가 이상한 느낌이군요?」
		CALL FADEOUT(12, "키득")
		PRINTFORMDW %CALLNAME:TARGET%는 입가를 누르고 낡게 미소지었다
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교횟수
;━━━━━━━━━━━━━━━━━━━━━━━━
SELECTCASE CFLAG:조교횟수
	CASE 1
		PRINTFORMW 「… 처음은 손대어 사랑옛 노래등, 길들여 갑시다」
		PRINTFORMW 「멍청이? 시끄럽네요. 너에게 듣고 싶지 않아요!」
		PRINTFORML
		PRINTFORMW 「… 이런 일 한 일 없기 때문에… 상냥하게 하세요?」
	;CASE 3
	;	PRINTFORMW 「응… 석연치 않다는 얼굴이군요? 좋으니까 나에게 맡겨 두세요!」
	;	PRINTFORMW 「과정은 보증하지 않지만, 나쁘게는 안 돼요」
	;대체로 3일째까지는 조수 소환 코스트는 우선 지불할 수 없다
	CASE 2
		PRINTFORMW 「응… 석연치 않다는 얼굴이군요? 좋으니까 나에게 맡겨 두세요!」
		PRINTFORMW 「과정은 보증하지 않지만, 나쁘게는 안 돼요」
	;처음의 아침
	CASE 3
		PRINTFORMDL %CALLNAME:MASTER%는 된장국의 냄새로 눈을 떴다
		PRINTFORMDW 부엌에%CALLNAME:TARGET%가 서 있다
		
		PRINTFORML 
		;된장국의 맛보기인 것으로 즈조조는 아니다
		PRINTFORMW 염주
		PRINTFORML 
		PRINTFORMW 「아라, 겨우 일어난 거네」
		PRINTFORMW 「안녕」
		;소녀답고 갈팡질팡 한다
		PRINTFORMW 「자주(잘) 잘 수 있었을까? 나는,」
		PRINTFORMW 「그다지 잘 수 없었어요」
		;장면 전환의 개행. 아침의 식탁
		PRINTFORML 
		CALL FADEOUT(12, "「그래그래」")
		PRINTFORMW 
		PRINTFORM 「밥의 뒤는, 어제의 계속
		CALL FADEIN(12, "군요」")
		;맛있다(중요)
		PRINTFORMDL %CALLNAME:MASTER%의 맛있는 된장국을 훌쩍거리는 손은 굳어졌다…
	;CASE 5
	;	PRINTFORMW 
	;4일째아침 7～14에 질리게 하지 않기 위한 궁리가 필요
	;CASE 7
	;대체로 2주간째 선택지가 충실해 오는 정도의 때
	;CASE 15
	;슬슬 호의 호감도 2000 정도의 때
	;CASE 30
	;	PRINTFORMW 
	;기일 이 구상에는 특별한 기일 설정은 없다
	;CASE 60
	;	PRINTFORMW 
	CASEELSE
		PRINTFORMW 「자, 시작해요!」
ENDSELECT
RETURN 1

;-------------------------------------------------
;·요컨데, 다른 조건에 히트 하지 않았던 경우에 떨어지는 개소
;·만일 다른 분기를 모두 방치했을 경우, 이 개소만 써 두면 뭔가는 표시된다든가, 그런 장소입니다
;━━━━━━━━━━━━━━━━━━━━━━━━
;초반(조건 없음)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100)
	;=================================================
	;첫회
	;=================================================
	IF 구상기입체크(0) && FIRSTEVENT(90)
		RETURN 1
	ENDIF
	
	;=================================================
	;첫회 이외
	;=================================================
	IF 구상기입체크(0)
		PRINTFORMW 「」
		RETURN 1
	ENDIF
ENDIF