;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무 구상 조교 이벤트 구상 파일
;
;	조교 턴중 이벤트, 절정 구상을 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;☆잠정 사양@/L

;========================================================================
;EV20, 절정 구상
;========================================================================
;·조교자나 조교 대상이 절정에 이를 때 표시
;· 각 절정은 개별의 호출을 가지지 않고, 모두 파생으로 처리된다
;-------------------------------------------------
;=================================================
;관련 변수…
;
;TFLAG:전회질내사정, (1=Ｖ성교/2=Ｖ성교 봉사/3=Ｖ성교(하늘 쏘아 맞히고)/4=Ｖ성교 봉사(하늘 쏘아 맞히고)/5=Ａ성교/6=Ａ성교 봉사/7=Ａ성교(하늘 쏘아 맞히고)/8=Ａ성교 봉사(하늘 쏘아 맞히고))
;TFLAG:REACT분류, (0=없음/1=소극적으로 한다/2=적극적으로 한다/3=용서를빈다/4=날뛴다/5=요구에 응하지 않는, 거부/6=도망치는, 발버둥 친다)
;NOWEX:사정, (1=드라이사정/2=사정/3=대량사정/4=사정(조루)/5=A책사정)
;TEQUIP:자위중, (bit0=조교 대상/bit1=조교자)
;
;=================================================
;함수류…
;
;MASTER_EX
;인  수:ARG:0… 애무자→1. 조교 대상 자신/2. 조교자/4. 조수 1/8. 조수 2/16. 조수 3
;/64. 도구/128. 변소 이용자  생략 하면 애무자를 묻지 않는다
;              비트화에서의 지정이 가능
;       ARG:1…(생략가능) 절정의 종별→1Ｃ절정/2Ｖ절정/3Ａ절정/4Ｂ절정
;/11. 분유/12. 사정/13. 방뇨
;ARG:0(bit0 =조교 대상 자신/bit1 =조교자/bit2 =조수 1/bit3 =조수 2/bit4 =조수 3/bit6 =도구/bit7 =소리)
;-------------------------------------------------
;TARGET_EX
;인  수:ARG:0… 애무자→1. 조교 대상/2. 조교자 자신/4. 조수 1/8. 조수 2/16. 조수 3
;              비트화에서의 지정이 가능. 생략 하면 애무자를 묻지 않는다
;       ARG:1…(생략가능) 절정의 종별→1. c절정/2. v절정/3. a절정/4. b절정
;/11. 분유/12. 사정
;ARG:0(bit0 =조교 대상/bit1 =조교자 자신/bit2 =조수 1/bit3 =조수 2/bit4 =조수 3)
;-------------------------------------------------
@KOJO_EVENT_K1_20(ARG)
;-------------------------------------------------
;TFLAG:전회질내사정;(1=Ｖ성교/2=Ｖ성교 봉사/3=Ｖ성교(하늘 쏘아 맞히고)/4=Ｖ성교 봉사(하늘 쏘아 맞히고)/5=Ａ성교/6=Ａ성교 봉사/7=Ａ성교(하늘 쏘아 맞히고)/8=Ａ성교 봉사(하늘 쏘아 맞히고))
;-------------------------------------------------

;────────────────────────────────────
;쾌감을참는다사정(MASTER→TARGET) 노력했지만 안되었어
;────────────────────────────────────
IF 구상기입체크(100) && IS_NOWCOMNAME("쾌감을참는다") ;COM의 참조는 IS_NOWCOMNAME 추천@/L
	;레이무씨의 절정이 먼 상태에서의 질내사정
	IF 구상기입체크(50) && AFFECTION("상사상애") && PALAM:쾌Ｖ < 8500 && GROUPMATCH(TFLAG:전회질내사정, 2, 4) > 0
		;%UNICODE%는 엔코드가 SJIS였던 시대의 여운. Unicode 환경의 현재는 필요가 없는 문법입니다@/L
		PRINTFORMW 「응? 함께 가고 싶어? 응후후%UNICODE(0x2661)%」
		PRINTFORMW 「기다리고 있어. 나도 곧 기분 좋아지기 때문에」
	;이하질내외 양용 오히려 간과하기 쉬운 것은 남자 당신의Ｂ절정
	ELSEIF 구상기입체크(50)
		PRINTFORMW 「안돼? 안된…… 에서도, 이제 안돼 그렇구나?」
	ELSE
		PRINTFORMW 「필사적으로 참아 버려%RAND_SPLIT("/, 건방지구나/건방져요")%」
		PRINTFORMDW %CALLNAME:MASTER%의 분발함에 응하는것 같이,%CALLNAME:TARGET%의 고문은 격렬함을 늘려 갔다…
	ENDIF
	RETURN 1
ENDIF
;────────────────────────────────────
;질내사정(MASTER→TARGET)
;────────────────────────────────────
;·Ｖ성교 봉사계로 질내사정 하는 경우에 표시
;·TFLAG:뽑지않고는 뽑지 않는 판정 마다+1된다
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;-------------------------------------------------

;사정 연출은 이 후
IF 구상기입체크(100) && GROUPMATCH(TFLAG:전회질내사정, 2, 4) > 0
	;================================================
	;드라이사정
	;================================================
	IF 구상기입체크(100) && SHOOTING(MASTER, "드라이사정")
		PRINTFORMW 「이제(벌써) 나오지 않는 것일까」
		PRINTFORMW 「%RAND_SPLIT("너에게 쏟아 받을 수 없으면/너가 사정해 주지 않으면")%, 왠지 어딘지 부족해요」
		PRINTFORML 「… 어쩔 수 없네요」
		RETURN 1
	ENDIF
	;=================================================
	;임신중
	;=================================================
	IF 구상기입체크(100) && CFLAG:TARGET:임신
		SELECTCASE !RAND:3
			CASE 0
				PRINTFORMW 「이제(벌써) 사정한 듯하다? 좋아요」
				;정액을 자식으로 변경
				PRINTFORMW 「너의 자식은 나에게, (안)중·이·고♪하기 위해서 만들고 있다 것이군요?」
			CASE 1
				PRINTFORMW 「그대로 사정해도 좋아요」
				PRINTFORMW 「너의 자지는 나의 보지로 뽑지 않으면 들어가지 않는 거네」
			CASEELSE
				[SKIPSTART]
				;개선의 여지유
				PRINTFORM 「질내에서%RAND_SPLIT("사정/-―/두근두근")%하고 있는… 너의 아기, 이미 가능하게 되고 있는데…
				CALL HEARTBW
				PRINTFORMW 「뭐 좋아요… 너의 자식은, 나의 질내에 사정하기 위해서만들고 있다 것이군요?」
				IF IS_CONDITION(TARGET, "정욕")
					;개선의 여지유
					PRINTFORM 「의 보지로%RAND_SPLIT("전부,/, 보지로")%전부 내 버리세요
					CALL HEARTBW
				ENDIF
				[SKIPEND]
				IF IS_CONDITION(TARGET, "정욕")
					;개선의 여지유
					PRINTFORM 「의 보지로%RAND_SPLIT("전부,/, 보지로")%전부 내 버리세요
					CALL HEARTBW
				ENDIF
				PRINTFORMW 「질내에서%RAND_SPLIT("사정/-―/두근두근")%하고 있다…」
				PRINTFORM 「따뜻함
				CALL HEARTBW
		ENDSELECT
		RETURN 1
	ENDIF
	;=================================================
	;처음의 질내사정
	;=================================================
	IF 구상기입체크(100) && FIRSTEVENT(0)
		PRINTFORMW 「소중한 일을 말하지 않았지요」
		PRINTFORM 「나 이제(벌써), 세키밥 와 있기 때문에
		;아라이먼트정
		IF ALI(0)
			CALL HEARTBW
		;아라이먼트부
		ELSE
			PRINTFORML 
		ENDIF
		PRINTFORML 
		;짧은 질내사정에 반응한
		CALL FADEIN(4, "「읏!」")
		;긴 사정에 만족해 여운을 악무는
		CALL FADEOUT(12, "「읏…」")
		PRINTFORM 「(안)중으로 나오고 있다
		CALL HEARTBW
		PRINTFORMDL %CALLNAME:TARGET%는 애정과 정욕이 섞인 미소를 띄웠다
		RETURN 1
	ENDIF
	;=================================================
	;임신했어?
	;=================================================
	IF 구상기입체크(100) && CFLAG:TARGET:질정 > 10000 && !CFLAG:TARGET:임신 && FIRSTEVENT(2)
		IF IS_CONDITION(TARGET, "정욕")
			;개선의 여지유
			PRINTFORM 「가능하게 되는… 가능하게 된다…
			CALL HEARTBW
			PRINTFORMDW %CALLNAME:TARGET%의 자궁입구는 잉 보고 국물을 빨아내려고 음경을 질네 들러 붙고 있다
		ENDIF
		PRINTFORM 「아─… 이것, 피탄 끊어 버려요
		CALL HEARTBW
		RETURN 1
	ENDIF
	;=================================================
	;%CALLNAME:TARGET%정욕
	;================================================
	IF 구상기입체크(100) && IS_CONDITION(TARGET, "정욕")
		PRINTFORMW 「%RAND_SPLIT("막혀 탄막으로/막혀 탄막! 막혀 탄막/에 해 시켜/임신시켜")%!%RAND_SPLIT("-오르지 않아 해 피탄시켜/-의 일 임신하게 해/너의 아기 낳게 해엣")%!!」
		PRINTFORM 「아… 앗… 배 속에서…%RAND_SPLIT("움찔움찔 말하고 있다/자지 날뛰고 있다")%…
		CALL HEARTBW
		RETURN 1
	ENDIF
	;================================================
	;조루
	;================================================
	IF 구상기입체크(100) && SHOOTING(MASTER, "조루사정")
		PRINTFORMW 「아… 뱃속으로, 는 하고 있다」
		PRINTFORMW 「무허가로 질내사정 해 주어 버려」
		PRINTFORM 「심하기 때문에
		CALL HEARTBW
		RETURN 1
	ENDIF
	;================================================
	;%CALLNAME:MASTER%정욕
	;================================================
	IF 구상기입체크(100) && IS_CONDITION(MASTER, "정욕")
		IF !RAND:2
			;응후후의 전에 사정을 감지하는 한호흡의 추가. 질내사정 시킬 생각으로 하고 있기 때문에(위해), 놀라움의! 는 없다
			CALL FADEIN(4, "「읏」")
			PRINTFORM 「응후후♪보지에는 이길 수 없었지요
			CALL HEARTBW
		ELSE
			PRINTFORMW 「앗… 는 하고 있다…」
			PRINTFORMW 「… 응후후♪」
		ENDIF
		RETURN 1
	ENDIF
	;================================================
	;대면좌위 명중 판정이 있는 질내사정
	;================================================
	IF TFLAG:ACT == 95
		PRINTFORML 「%RAND_SPLIT("응. 이것, 좋아/응. 이것, 마음 좋아요/행복/너무 좋아")%」
		PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%에 깊게 껴안으면 뺨을 비벼 왔다
		RETURN 1
	ENDIF
	;================================================
	;통상
	;================================================
	IF 구상기입체크(100)
		IF ALI(0)
			PRINTFORMDL %CALLNAME:MASTER%가 피스톤질을 멈춤허리를 강압하면 절정을 헤아렸다%CALLNAME:TARGET%가%CALLNAME:MASTER%의 손을 잡아 왔다
			PRINTFORML
			;된장국은 빠뜨릴 수 없다
			SELECTCASE RAND:2
				CASE 0
					PRINTFORM 「%RAND_SPLIT("매일 아침 된장국 만들어 주기 때문에/받아 들여 주기 때문에/책임은 이미 취해 있기 때문에/사양은 필요하지 않으니까")%、
					PRINTFORM %RAND_SPLIT(@"%CALLNAME:TARGET%의 보지/가장 안쪽/자궁/질내/너의@%CALLNAME:TARGET%")%에
					PRINTFORM %RAND_SPLIT("정액/자식국물/임신하게 한 국물/성욕 전부")%
					PRINTFORM 、%RAND_SPLIT("/전부/한 방울 남김없이")%사정해 버리세요
					CALL HEARTBW
					;체위 관련으로 분할할까 고민거리
					;이것이라면 이렇게 된다. 그렇다고 하는 것은 바리안트적으로 좋지 않은 것 같으니까 분할하지 않는 것이 좋은 것 같다
				CASEELSE
					;개만들기 날것 엣치하다고는 말하게 하지 않는다.
					;개만들기는 아닌 엣치라고 하는 개념이 없다.
					;레이무씨의 사전에 피임이라고 하는 발상은 없다.
					PRINTFORM 「이봐요,%RAND_SPLIT("개만들기 엣치해/착정 되어/무리하게 정액 짜내져/자지 좁혀져/자지 진동시켜")%
					PRINTFORMW %RAND_SPLIT("있고/방편/질내사정 해")%좋은 차이」
			ENDSELECT
			;대량사정시 추가 묘사
			IF 구상기입체크(100) && SHOOTING(MASTER, "대량사정")
				CALL FADEIN(4, "물러난다!")
				;레이무의 태내에서의 감각과 구두에서의 표현을 비켜 놓는다. 레이무씨는 작가는 아니기 때문에 적확하지 않은 것이 좋다.
				CALL FADEOUT(8, "…………")
				PRINTFORMW 「아… 뱃속으로, 구! 구! 라고…」
				CALL FADEOUT(4, "…… 퓨……")
				PRINTFORM 「%RAND_SPLIT("가능하게 되는… 가능하게 된다…/언제까지 사정하고 있어/자지, 떨리고 있다/가득")%
				CALL HEARTBW
			ENDIF
		ELSE
			PRINTFORML 「싫어해도, 너의 자지는 여자의 아가씨를 임신하게 하고 싶은 것 같구나」
			PRINTFORML 「%RAND_SPLIT("/나, 이제(벌써) 세키밥 와 있는 것이지만, 기분 좋았는지 해들/억지로 배게 해 기분 좋았을까")%
			CALL HEARTBW
		ENDIF
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;애널내 사정(MASTER→TARGET)
;────────────────────────────────────
;·Ａ성교 봉사계로 장내 사정하는 경우에 표시
;·TFLAG:106, 뽑지않고는 뽑지 않는 판정 마다+1된다
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;-------------------------------------------------
IF 구상기입체크(0) && GROUPMATCH(TFLAG:전회질내사정, 6, 8) > 0
ENDIF

;────────────────────────────────────
;질내사정(TARGET→MASTER)
;────────────────────────────────────
;·Ｖ성교계로 질내사정 하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;·현재의 사양에서는 조교자가 드라이사정 하는 것은 매우 희(거의 없어?)
;-------------------------------------------------

	IF TALENT:새드
		
	ELSE
		
	ENDIF
	
IF 구상기입체크(100) && GROUPMATCH(TFLAG:전회질내사정, 1, 3) > 0
	;%CALLNAME:TARGET%에 낳게 했던 적이 있다
	IF TALENT:모성
		PRINTFORM 「너에게도 낳아 세등 없으면, 불공평해요
		CALL HEARTBW
		;리턴 없음으로 다음에 연결한다
		;RETURN 1
	ENDIF
	SELECTCASE RAND:4
		CASE 0
			PRINTFORM 「사정해요…%RAND_SPLIT("임신하세요/임신해/나의 아기 임신하세요/자궁으로 받아 들여")%
			CALL HEARTBW
		CASE 1
			PRINTFORMDW 사정을 끝낼 때까지%CALLNAME:TARGET%새와 들러붙어 놓아 주지 않았다
		CASE 2
			;강간은 스치고 들 성욕에만 아니고
			PRINTFORMDW 성욕을 채워 상,%CALLNAME:TARGET%는%CALLNAME:MASTER%를 해방하지 않는다
		CASEELSE
			PRINTFORMW 「… 무심코 질내에 사정해 버렸지만… 책임은 이미 취하고 있기 때문에, 좋네요?」
	ENDSELECT
	;당신이 울적함
	;어떻게 되어져 버리는 것으로 와 버리는 말하고 있어 이제 되어 있어요 당신측은 할 수 없다
	
	;당신이 유아 퇴행 되지 않는 노인화
	;밖이고는 아직 가능 이제 되어 있겠죠 당신측은 할 수 없다
	
	;보지는 단단히 조여 와요
	;CALL HEARTBW
	RETURN 1
ENDIF

;────────────────────────────────────
;애널내 사정(TARGET→MASTER)
;────────────────────────────────────
;·Ａ성교계로 장내 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;·현재의 사양에서는 조교자가 드라이사정 하는 것은 매우 희(거의 없어?)
;-------------------------------------------------
IF 구상기입체크(0) && GROUPMATCH(TFLAG:전회질내사정, 5, 7) > 0
	IF TALENT:새드
		
	ELSE
		
	ENDIF
ENDIF

[SKIPSTART]
부위별의 절정내에서 처리하지 않는 경우는 여기서 보완@/L
;────────────────────────────────────
;동시절정
;────────────────────────────────────
;·조교자와 조교 대상이 함께 절정 하는 경우에 표시
;-------------------------------------------------
IF 구상 기입 체크(0) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정)
ENDIF
[SKIPEND]

;────────────────────────────────────
;조교 대상 사정
;────────────────────────────────────
;·조교 대상이 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 가감(상태)해 사용하는 것이 바람직함
;-------------------------------------------------
;리액션→이것→사정 처리  결정타의 한 마디에 적절한 개소
IF 구상기입체크(100) && NOWEX:MASTER:사정
	;────────────────────────────────────
	;첫회
	;────────────────────────────────────
	IF 구상기입체크(100) && FIRSTACT(0)
		PRINTFORMW 「움찔움찔 흔들림, 라고 흰 것이 나오고 있는…… 자지는 기분 좋아지면, 이렇게 되는 거네……」
		PRINTFORMW 「흩뿌려탄은 귀찮지만… 맞으러 가면 좋기 때문에, 어떻게든 되네요」
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;개만들기의 히
	;────────────────────────────────────
	IF 구상기입체크(100) && POLICY("오늘") == "자 만들기"
		;행동 적극적일지도 모른다
		PRINTFORMW 「응후후. 질외에 내 버렸군요?」
		SELECTCASE RAND:3
			CASE 0
				;템포의 조정 8에서 2에. 당신의 눈물에 즉반응하는 레이무씨.
				CALL FADEIN(2, "「읏, 울지 말아요!」")
				PRINTFORMW 「%RAND_SPLIT("아 이제(벌써)… 나빴어요/너무 했어요")%…」
				;템포의 조정 12라면 2문자 정도가 적당. 공백행이 남으므로 2련FADEOUT는 멈추어 두는 것이 좋다.
				;대체로 1문자에 대해 2 정도?
				CALL FADEIN(8, "「미안해요」")
				CALL FADEOUT(12, "「좋아 좋아」")
			CASE 1
				PRINTFORMW 「… 그런, 죽을 곳을 잃은 것 같은 얼굴 하지 말아요」
				CALL FADEOUT(12, "「과장되니까」")
			CASEELSE
				PRINTFORMW 「눈물고인 눈이 되어있는 너도 꽤, 사랑스러워요」
		ENDSELECT
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;구강사정
	;────────────────────────────────────
	IF 구상기입체크(100) && GET_ACTNAME(GET_NORMALACTNUM(TFLAG:ACT)) == "페라한다"
		;────────────────────────────────────
		;구강사정 첫회
		;────────────────────────────────────
		IF 구상기입체크(100) && FIRSTEVENT()
			IF 구상기입체크(100) && SHOOTING(MASTER, "대량사정")
				;레이무는 사자옥 가지 않은 계 여자이기 때문에 comment out
				;PRINTFORML (사정은 작은 숟가락 한 잔 정도의 양도 케이네가 말했지만… 상당히 많네요)
				PRINTFORML (사정은 작은 숟가락 한 잔 정도의 양이라도 (듣)묻고 있었지만… 상당히 많네요)
				PRINTFORML (빠질 것 같다)
				PRINTFORMDW %CALLNAME:TARGET%는 눈을 감으면서 차분히 입안의 정액을 저작 해 맛보고 있다
			ELSEIF 구상기입체크(100) && SHOOTING(MASTER, "조루사정")
				PRINTFORML 「응긋!」
				PRINTFORMDW %CALLNAME:TARGET%는 갑자기 기관에 흘러드는 정액에 눈을 좌우 양면 놀라고 있다
			ELSE
				PRINTFORMDW %CALLNAME:TARGET%는 눈을 감으면서 차분히 입안의 정액을 저작 해 맛보고 있다
				PRINTFORML (우선……)
			ENDIF
			RETURN 1
		ENDIF
		;────────────────────────────────────
		;구강사정 첫회 이외
		;────────────────────────────────────
		IF 구상기입체크(100)
			;────────────────────────────────────
			;쾌감을참는다사정(MASTER→TARGET) 노력했지만 안되었어
			;────────────────────────────────────
			;인내를 결궤 시키는 시도
			IF IS_COMGRONAME("소극적으로 한다/용서를빈다/쾌감을참는다")
				PRINTFORMDL %CALLNAME:TARGET%는 절정을 참으려고 잡는다%CALLNAME:MASTER%의 손을 풀어 손가락을 걸어 왔다
				PRINTFORMDW %CALLNAME:TARGET%레이무의 부드럽고 난보람 손바닥에%CALLNAME:MASTER%의 긴장이 느슨해진다
				PRINTFORML 
			;ENDIF
			;추가 묘사 대량사정 시킬 생각 만만해 손을 잡아 오는 레이무씨
			;IF SHOOTING(MASTER, "대량사정")
			ELSEIF SHOOTING(MASTER, "대량사정")
				PRINTFORMDW %CALLNAME:MASTER%가 달할 것 같게 되는이나%CALLNAME:TARGET%는%CALLNAME:MASTER%의 손을 잡아 꽉 잡아 왔다
				PRINTFORML 
			ENDIF
			;정액 보충 완료시의 제일 눌러 짬
			IF BASE:MASTER:사정 == MAXBASE:MASTER:사정
				PRINTFORML 「응”! 응쿳! 읏… 응… 응쿳…」
			ENDIF
			
			PRINTFORMW (입의 안에서 날뛰고 있다…)
			PRINTFORMl (맛있지는 않지만, 이 녀석이 기분 좋아지고 있다고 생각하면…)
			IF !SHOOTING(MASTER, "드라이사정")
				CALL FADEOUT(12, "  응… 읏… 꿀꺽…")
				PRINTFORMDW %CALLNAME:TARGET%는\@ SHOOTING(MASTER, "대량사정") ? 입가를 손가락으로 닦으면서 #  \@기쁜듯이 정액을 삼켰다
			ENDIF
			PRINTFORMW 「또, 해 주고 싶어지는거네요…」
			RETURN 1
		ENDIF
	ENDIF
	;────────────────────────────────────
	;자위 사정
	;────────────────────────────────────
	IF 구상기입체크(100) && GET_ACTNAME(GET_NORMALACTNUM(TFLAG:ACT)) == "자위"
		IF 구상기입체크(100)
			PRINTFORM 「사정할 때는
			PRINTFORMD %CALLNAME:TARGET%씨를 좋아하는 사람―
			PRINTFORML 라고 말하지 않으면」
			PRINTFORMW 「이봐요」
			PRINTFORM 「
			PRINTFORMD %CALLNAME:TARGET%씨를 좋아하는 사람
			PRINTFORML 」
			PRINTFORM 「
			PRINTFORMD 를 좋아하는 사람
			PRINTFORML 」
			PRINTFORM 「
			PRINTFORMD 를 좋아하는 사람
			PRINTFORMW 」
			;여기까지 조롱하는 것 같은 %CALLNAME:MASTER%의 소리 흉내의 흰색. 타이르도록(듯이) 박아 넣는 것처럼. 소프트 조교
			;여기로부터 %CALLNAME:TARGET%의 소의 음색의 복숭아. 도중부터 올라 온 %CALLNAME:TARGET%씨. 자폭 셀프 말고문
			;PRINTFORMW 「좋아」
			;악물도록(듯이) 재인식한다. 신부는 다시 반하는 것.
			CALL FADEOUT(12, "「좋아」")
			PRINTFORMW 「좋아」
			PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 절정이 수습될 때까지 계속 상냥하게 속삭였다
		ENDIF
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;사정 내용 분기
	;────────────────────────────────────
	IF 구상기입체크(100) && SHOOTING(MASTER, "전립선사정")
		PRINTFORMW 「파져 내다니 우물인가 뭔가 같구나?」
		CALL FADEOUT(4, "「키득」")
		PRINTFORMW 「아직 나올까… 좀 더 깊게 파 주어요」
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;드라이사정 분기
	;────────────────────────────────────
	IF 구상기입체크(100) && SHOOTING(MASTER, "드라이사정")
		PRINTFORMW 「너가 사정해 주지 않으면, 왠지 어딘지 부족해요」
		PRINTFORMW 「…%RAND_SPLIT("/이제 사정 없어?")%어쩔 수 없네요」
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;대량사정 분기
	;────────────────────────────────────
	IF 구상기입체크(100) && SHOOTING(MASTER, "대량사정")
		;상황에 의해 모순될 우려. 하트가 너무 많다. 불쾌함이나 반쯤 뜬 눈 히죽히죽 얼굴등, 다른 행동도 갖고 싶어진 때문 조정
		PRINTFORM 「이렇게 사정해 주어 버려…%RAND_SPLIT("/너무 많아요/너무 내고")%
		IF IS_CONDITION(TARGET, "정욕")
			CALL HEARTBW
		ELSE
			PRINTFORMW 」
		ENDIF
		SIF RAND:3
			PRINTFORMW 「%RAND_SPLIT("언제쯤이면 멈추는거야… 이것/가득 나와 있지만")%, 망가지지 않네요?」
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;조루사정 분기
	;────────────────────────────────────
	IF 구상기입체크(100) && SHOOTING(MASTER, "조루")
		PRINTFORMW 「아%RAND_SPLIT("//읏앗")%! 이제(벌써)%RAND_SPLIT("사정해 버렸다/조금 즐기고 싶었는데")%…」
		PRINTFORMW 「%RAND_SPLIT("는 양도 적네요/너의 조루만은 짐작 할 수 있을 것 같지 않아요…/좀 더 참아 주세요이, 바보 자지/아직 낼 수 있네요?/안된 자지군요")%」
		RETURN 1
	ENDIF
	;────────────────────────────────────
	;통상 사정 분기
	;────────────────────────────────────
	IF 구상기입체크(100) && SHOOTING(MASTER, "사정")
		;첫회 사정 관찰
		SELECTCASE RAND:3
			CASE 0
				PRINTFORM 「사정할 때는,
				PRINTFORMD %CALLNAME:TARGET%씨너무 좋아
				PRINTFORMW 라고 말하지 않으면 안 돼요?」
			CASE 1
				PRINTFORMW 「기분 좋았어?」
				PRINTFORMDW %CALLNAME:TARGET%는%RAND_SPLIT("뺨을 느슨해지게 했다/빙글빙글해/입가를 매달아 올리고")%면서%CALLNAME:MASTER%의 눈을 응시하고 있다…
			CASEELSE
				;묘사 부족 개선의 여지 있어  정액 묘사가 전해지지 않는다고 생각된다
				PRINTFORM 「는 낸 뒤로 끝 쪽으로 할 수 있는 구슬이,
				PRINTFORMD 『열심히 사정했어』
				PRINTFORMW 라고 말하고 있는 것 같아, 왠지 사랑스럽네요」
				PRINTFORM 「응후후♪잘 할 수 있었습니다
				CALL HEARTBW
			ENDSELECT
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;조교자 사정
;────────────────────────────────────
;·조교자가 사정하는 경우에 표시
;·사정량에 의한 분기는 필요하게 응하고 추가해 사용하는 것이 바람직함
;·현재의 사양에서는 조교자가 드라이사정 하는 것은 매우 희(거의 없어?)
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:사정
	IF TALENT:새드
		
	ELSE
		
	ENDIF
ENDIF

[SKIPSTART]
;────────────────────────────────────
;조교자 절정
;────────────────────────────────────
;·조교자가 절정 하는 경우에 표시
;-------------------------------------------------
IF 구상 기입 체크(100) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정)
	;성교 봉사 절정 분기가 기능하지 않기 때문에 이전 comment out
	PRINTFORMW 「읏! 응%RAND_SPLIT("응//우웃")%」
	PRINTFORM 「하… 하… 하앗…
	CALL HEARTBW
	SIF ! TEQUIP:아이마스크
		PRINTFORMW 「…%RAND_SPLIT("별로/그렇게")%보지 말아요… 부끄럽기 때문에…」
	RETURN 1
ENDIF
[SKIPEND]

[SKIPSTART]
;부위별로 절정을 나누어 쓰지 않는 경우는 여기서 보완@/L
;────────────────────────────────────
;조교 대상 절정
;────────────────────────────────────
;·조교 대상이 절정 하는 경우에 표시
;-------------------------------------------------
IF 구상 기입 체크(0) && TRUECHECK(NOWEX:MASTER:Ｃ절정, NOWEX:MASTER:Ｖ절정, NOWEX:MASTER:Ａ절정, NOWEX:MASTER:Ｂ절정)
ENDIF
[SKIPEND]
;────────────────────────────────────
;조교 대상Ｃ절정
;────────────────────────────────────
;·조교 대상이Ｃ절정 하는 경우에 표시
;·여기에서는 「여자 아이의Ｃ절정」을 상정하고 있다
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:MASTER:Ｃ절정
ENDIF

;────────────────────────────────────
;조교 대상Ｖ절정
;────────────────────────────────────
;·조교 대상이Ｖ절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:MASTER:Ｖ절정
ENDIF

;────────────────────────────────────
;조교 대상Ａ절정
;────────────────────────────────────
;·조교 대상이Ａ절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:MASTER:Ａ절정
ENDIF

;────────────────────────────────────
;조교 대상Ｂ절정
;────────────────────────────────────
;·조교 대상이Ｂ절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:MASTER:Ｂ절정
ENDIF

;────────────────────────────────────
;조교자Ｃ절정
;────────────────────────────────────
;·조교자가Ｃ절정 하는 경우에 표시
;·여기에서는 「여자 아이의Ｃ절정」을 상정하고 있다
;·사정은 사정 구상의 분기에서 쓰는 것이 바람직함
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:Ｃ절정
ENDIF

;────────────────────────────────────
;조교자Ｖ절정
;────────────────────────────────────
;·조교자가Ｖ절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(100) && NOWEX:Ｖ절정
	;────────────────────────────────────
	;조교자 성교 봉사Ｖ절정
	;────────────────────────────────────
	IF 구상기입체크(100) && TEQUIP:성교봉사중
		;조교자 이성 파생
		SELECTCASE TENSION(TARGET, 5)
			;25% 이하
			CASE "낮다"
				PRINTFORM 「아"-… 아"-…
				CALL HEARTBW
				;절정에서 머리 진자백태
				PRINTFORMW 「………」
				;언어를 손놓아 안  묘사가 필요라고 생각된다
				;PRINTFORMDL 
			;25% 이상
			CASE "일반"
				SELECTCASE RAND:2
					CASE 0
						;
						CALL FADEOUT(8, "「앗! 아!」")
						CALL FADEIN(8, "「아……」")
						[SKIPSTART]
						;말장난을 하려면 좀 더 깊은 곳을 갖고 싶다
						PRINTFORMDL 목을 제어 다 할 수 있지 않은 들뜬 교성은 사랑스럽고
						PRINTFORMDW 짐승과 사람의 경계선상의 %CALLNAME:TARGET%도 또한꼬드김정적으로 %CALLNAME:MASTER%를 매료했다
						;말고문 방향의 표현일지도 모른다
						;PRINTFORMDW %CALLNAME:MASTER%는 청각으로부터도 %CALLNAME:TARGET%에 꾸짖어지고 있다
						[SKIPEND]
					CASEELSE
						;어이없이 한 %CALLNAME:TARGET%다움이 부족 기색
						CALL FADEOUT(8, "「훗! … 훗…")
						;PRINTFORMDW 입가를 누름 교성을 억제하는 행동은 그녀의 소녀성을 두드러지게 했다
				ENDSELECT
			;50% 이상
			CASE "높다"
				CALL FADEIN(8, "「하… 핫…」")
		ENDSELECT
		;약간 긴 12. 절정에서 머리 진자백태로부터 말을 뽑을 수 있을 때까지 정도의 간격을 비운다
		CALL FADEIN(12, "………")
		;아라이먼트정
		IF ALI(0)
			IF TALENT:새드
				;봉사시키는 커맨드가 너무 나오지 않는 레이무씨
				PRINTFORMW 「기분이 좋지만… 너가 말해 준 (분)편이, 즐거워요」
			ELSE
				PRINTFORMW 「나만 기분 좋아지는 것도… 왠지 분해요」
				CALL FADEOUT(12, "웅얼웅얼…")
				;삽입 계속이 참조되어 있지 않기 때문에 comment out
				;PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%가 사정하고 있지 않는 것을 확인하면 다시 허리를 움직이기 시작했다
			ENDIF
		;아라이먼트부
		ELSE
			PRINTFORMW 「무엇 우쭐거린 얼굴 하고 있는거야」
			PRINTFORMDW %CALLNAME:TARGET%가 사랑스럽게 뺨을 부풀린다
		ENDIF
		RETURN 1
	ENDIF
		[SKIPSTART]
			;잘 오지 않는 comment out
			PRINTFORMW 「너의 자지로, 가 버렸다」
			CALL FADEOUT(8, " 「가게 되어 버렸다」")
			;여기서 레이무님 대만족으로 연결될 때 와 플레이어씨가 기쁘다
			;만족 게이지에 맡김 음표나 하트는 없는 것이 좋다

			;(안)중이고 언급이 너무 많기 때문에(위해) 전정.
			CALL FADEIN(12, "………")
			CALL FADEOUT(4, "등")
			;PRINTFORMW 「이것으로 질내에 사정해 주면 말하는 일은 없지만…」
		[SKIPEND]
	
	;────────────────────────────────────
	;조교자Ｖ절정 그 이외
	;────────────────────────────────────
	[SKIPSTART]
	;페니반 자폭을 상정 하나 더
	IF 구상 기입 체크(0) && NOWEX:Ｖ절정
		IF IS_CONDITION(TARGET, "정욕")
			PRINTFORMW 「보지 기분 좋다… 보지 기분 좋다…」
		ELSE
			PRINTFORMW 「… 너가 기분 좋아지지 않는 것은, 왠지 어딘지 부족해요」
		ENDIF
	ENDIF
	RETURN 1
	[SKIPEND]
ENDIF
;────────────────────────────────────
;조교자 절정 그 이외
;────────────────────────────────────
;·조교자가 절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(100) && TRUECHECK(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정)
	;성교 봉사 절정 분기가 기능하지 않기 때문에 이전 comment out
	PRINTFORMW 「읏! 응%RAND_SPLIT("응//우웃")%」
	PRINTFORM 「하… 하… 하앗…
	CALL HEARTBW
	SIF !TEQUIP:아이마스크
		PRINTFORMW 「…%RAND_SPLIT("별로/그렇게")%보지 말아요… 부끄럽기 때문에…」
	RETURN 1
ENDIF
;────────────────────────────────────
;조교자Ａ절정
;────────────────────────────────────
;·조교자가Ａ절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:Ａ절정
ENDIF

;────────────────────────────────────
;조교자Ｂ절정
;────────────────────────────────────
;·조교자가Ｂ절정 하는 경우에 표시
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:Ｂ절정
ENDIF

;────────────────────────────────────
;조교 대상 분수
;────────────────────────────────────
;·조교 대상이 분수의 경우에 표시
;·현상, ACT손가락삽입해라시에Ｇ스팟 고문에 파생했을 경우에게만 일어난다
;·주인님의 분수는 없다
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:MASTER:분수
ENDIF

;────────────────────────────────────
;조교 대상 실금
;────────────────────────────────────
;·조교 대상이 실금 하는 경우에 표시
;·조교 대상으로 소질:소변버릇이 없으면 발생하지 않는다
;·Ｃ절정없이 실금만이라고 하는 것은 드물게 있는 케이스
;-------------------------------------------------
IF 구상기입체크(0) && NOWEX:MASTER:방뇨
ENDIF
