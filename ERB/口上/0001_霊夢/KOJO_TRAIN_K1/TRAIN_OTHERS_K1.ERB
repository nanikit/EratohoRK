;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	레이무 구상 그 외 조교 이벤트 구상 파일
;
;	그 외 조교 턴중 이벤트를 정리한 파일
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;☆잠정 사양@/L

;─
;EV21, 대만족 구상
;─
;·조교자의 대만족 보너스를 취득했을 경우에 표시
;-------------------------------------------------
@KOJO_EVENT_K1_21(ARG)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님대만족
;━━━━━━━━━━━━━━━━━━━━━━━━━
;대요정 구상을 참고에 하도록 해 받았습니다
;가, 우리의 것에 늘어뜨림 있고 씨로는 파워 부족했어…. 자기 캐릭터로서는 이것으로 좋은 것인지도 모른다
IF 구상기입체크(25) && !TEQUIP:성교봉사중
	IF !FLAG:오토모드
		;PRINTFORMDW %CALLNAME:TARGET%님대만족
		PRINTFORML
		PRINTFORML 「다음은 너가 만족하는 차례군요. 해 주었으면 하는 것, 있을까?」
		IF TEQUIP:MASTER:볼개그
			PRINTFORMDL %CALLNAME:TARGET%는%CALLNAME:MASTER%의 입마개를 풀면서 물어 왔다
			TEQUIP:MASTER:볼개그 = 0
		ENDIF
		PRINTFORML
		PRINTFORMDW 아무래도%CALLNAME:TARGET%는%CALLNAME:MASTER%의 요구에 응해 주는 것 같다…
		IF TFLAG:벌플래그 && TEQUIP:MASTER:볼개그
			PRINTDL [0] 응―! 응─!
			PRINTDL [1]―! 무─!
			INPUT
			PRINTFORMW 「미안해요 자주(잘) 도취되어 듣지 않았어요」
			PRINTFORMDW %CALLNAME:TARGET%님의 웃는 얼굴은, 눈부시다.
			RETURN 1
		ENDIF
		;성교 봉사 요망은 함부로 짜넣어야 할 것은 아니다
		PRINTDL [0] 좀 더 만족해 주었으면 한다
		PRINTDL [1] 입맞추고 싶다
		PRINTDL [2] 젖가슴
		PRINTDL [3] 봉사시켜 주세요
		PRINTDL [4] 밟아 주세요 부탁합니다
		PRINTDL [5] 자위를 보고 싶다
		;여기가 기능하고 있지 않다
		SIF EQUIP:악세사리 == 0
			PRINTDL [6] 소매를 입어 주세요
		;첫회 분기나 게임 밸런스에의 영향이 강하기 때문에 조건부여
		;녀 당신의 경우【굴복:22】질내사정 되었다
		;남 당신의 경우【굴복:42】애널에 사정되는 것을 본다
		; || 는 사용해야 할 것은 아니지만 어떻게 한 것인가
		SIF PENIS(TARGET) && (TALENT:MASTER:남자 == 0 && GETBIT(CFLAG:각인, 22)) || (TALENT:MASTER:남자 && GETBIT(CFLAG:각인, 42))
			PRINTDL [21] 안에내！
		PRINTDL [100] 특히 요망은 없다
		INPUT
		IF RESULT == 0
			PRINTFORML (하나 하나 사랑스럽네요 이 녀석)
			PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 가슴에 얼굴을 묻어 부비부비 해 왔다
		ELSEIF  RESULT == 1
			;【굴복:11】키스를 취득이 끝난 상태라면
			IF GETBIT(CFLAG:각인, 11)
				;해제 처리
				IF TEQUIP:삼각목마
					PRINTFORML 「이대로는 닿지 않네요」
					TEQUIP:삼각목마 = 0
				ENDIF
				;해제 처리
				IF TEQUIP:안면승마 || TEQUIP:안면승마애널
					PRINTFORMDL %CALLNAME:TARGET%는%CALLNAME:MASTER%의 얼굴에 향해 치유눈을 감았다
					TEQUIP:안면승마 = 0
					TEQUIP:안면승마애널 = 0
				ELSE
					PRINTFORMDL %CALLNAME:TARGET%는 수긍해 눈을 감았다
				ENDIF
				;에 축 늘어차고당신으로부터는 키스 할 수 없다
				PRINTFORMDW 입술을 제출 입맞춤을 기다린다%CALLNAME:TARGET%에%CALLNAME:MASTER%는 넋을 잃고 보고 굳어져 버린다…
				PRINTFORML 
				PRINTFORMDL ………
				PRINTFORMW ………
				PRINTFORML 
				;묘사 부족. 키스 후라고도 받는 것이 가능하게 된다
				CALL FADEOUT(4, "등")
				PRINTFORMW 「언제까지 굳어지고 있는거야」
				CALL FADEOUT(8, "「아니 뭐, 응」")
				PRINTFORMW 「역시 너는 너네」
				;결국 레이무로부터 키스 한다
				TCVAR:행동예약 = 15
			;첫키스는 연주한다
			ELSE
				PRINTFORML 「바보 아직. 아직, 우리들에게는 빨라요」
				PRINTFORMDW 수줍다%CALLNAME:TARGET%는 사랑스럽지만, 상황은 키스할 경황은 아니다
			ENDIF
		ELSEIF RESULT == 2
			PRINTFORMD %CALLNAME:TARGET%는
			;도구 해제 처리
			IF TEQUIP:상반신윗도리
				PRINTFORMD 윗도리를 벗어
				TEQUIP:상반신윗도리 = 0
				TEQUIP:상반신속옷 = 0
			ENDIF
			;도구 해제 처리
			IF TEQUIP:상반신속옷
				PRINTFORMD 사라시를 풀면,
				TEQUIP:상반신윗도리 = 0
				TEQUIP:상반신속옷 = 0
			ENDIF
			;도구 해제 처리
			IF TEQUIP:MASTER:볼개그
				TEQUIP:MASTER:볼개그 = 0
			ENDIF
				
			PRINTFORMDL %CALLNAME:MASTER%의 눈앞에 아름답게 갖추어진 유방을 드러냈다
			;젖가슴 들이마시게 한다
			TCVAR:행동예약 = 57
			TFLAG:ACT파생 = 2
		ELSEIF RESULT == 3
			PRINTFORML 「좋아요. 너가 기분좋게해」
			;도구 해제 처리
			IF TEQUIP:MASTER:볼개그 || TEQUIP:페니스밴드 || TEQUIP:삼각목마
				TEQUIP:MASTER:볼개그 = 0
				TEQUIP:페니스밴드 = 0
				TEQUIP:삼각목마 = 0
			ENDIF
			;줄은 해제하지 않는 처리
			IF TEQUIP:MASTER:줄
				PRINTFORML 「그대로도 할 수 있네요」
			ENDIF
			;성교 봉사 분기는 모순의 바탕으로든지 그런 것으로 일단 최상부에서 배제 죽어 분기
			IF TEQUIP:성교봉사중
				PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%에 몸을 맡기고 졸라대 왔다
				;성교 봉사 정상위 조교자←조교 대상
				TCVAR:행동예약 = 95
			ELSE
				;초심 사노 보강
				PRINTFORMDW %CALLNAME:TARGET%는 이불에 누워%CALLNAME:MASTER%의 행위를 가슴을 크게 울리게 한 기다리고 있다…
				;봉사 쿤닐링구스 조교자←조교 대상
				TCVAR:행동예약 = 51
			ENDIF
		ELSEIF RESULT == 4
			PRINTFORML 「너의 변태는 죽어도 나을 것 같지도 않네요」
			;애무 풋잡
			TCVAR:행동예약 = 18
		ELSEIF RESULT == 5
			;횡설수설하게 OUT는 튀도록(듯이) 빨리 하고 IN는 짜내도록(듯이) 늦은 비처녀가 되어도 처녀성은 큰 일
			;이 바리안트에 %CALLNAME:MASTER%공격 요소는 불필요한 생각이 들지 않는 것도 아니지만, 레이무씨가 사랑스럽다
			
			;OUT는 연속으로 사용하지 않게 주의. FADE에 FORM 기능은 없는 모양. 조정의 필요성을 실감. OUT와 IN의 퇴고
			PRINTFORML 
			CALL FADEIN(4, "「와…」")
			CALL FADEIN(12, "「알았어요」")
			;주인님이라고는 부르게 하지 않는다. 서방님도 조금 틀리다는 느낌이 든다.
			CALL FADEOUT(4, "「(이)닷!」")
			CALL FADEIN(8, "「남편,」")
			;굉장한 어깨 것
			CALL FADEIN(8, "「」")
			PRINTFORML 
			PRINTFORMDW 딱딱함%CALLNAME:TARGET%의 응대에 상관하지 않고%CALLNAME:MASTER%는 계속을 재촉했다
			PRINTFORML 
			CALL FADEOUT(4, "「내가」")
			CALL FADEIN(8, "「내가 스스로」")
			CALL FADEIN(8, "「하는, 하는 것을…」")
			CALL FADEOUT(4, "「엣」")
			PRINTFORML 
			PRINTFORMDW %CALLNAME:MASTER%는 여기라는 듯이%CALLNAME:TARGET%에 무엇을 하는지 물었다…
			PRINTFORML 
			CALL FADEIN(12, "「무엇 말하게 하는거야…」")
			PRINTFORML 
			CALL FADEIN(8, "「이, 인,」")
			CALL FADEOUT(12, "「에―」")
			CALL FADEIN(8, "「자위! 를 봐! 만족! 해 주세요!」")
			CALL FADEOUT(12, "「아아아아아……」")
			PRINTFORML 
			;하행과의 맞대고 비빔이기 때문에 변경
			;PRINTFORMDW %CALLNAME:TARGET%는 행위 전부터 이미 다 피폐해지고 있다…
			PRINTFORMD %CALLNAME:TARGET%는 행위 전부터 이미 피폐 해
			;%CALLNAME:TARGET%피폐화 녹초가 되면서 그대로응 질척질척 하는 모습은 에로하다고 생각해
			;기능하고 있지 않기 때문에 comment out
			;TCVAR:TARGET:상태변화 = 1
			;PRINTFORMD %CALLNAME:TARGET%는
			;도구 해제 처리
			IF TEQUIP:아이마스크
				PRINTFORMD 아이마스크를 꽉 쥐면서
				TEQUIP:아이마스크 = 0
			ENDIF
			PRINTFORMDW 네발로 엎드림이 되어 고개 숙이고 있다…
			PRINTFORMDW 무방비인 하반신은 성적이면서도 어딘가 흐뭇한 것이 있었다…
			;모처럼인 것으로 수려 앵글에서 자위해 받는다
			TEQUIP:안면승마 = 1
			;조교자가 자위
			TCVAR:행동예약 = 42
		ELSEIF RESULT == 6 && EQUIP:악세사리 == 0
			[SKIPSTART]
			IF 소매 오나홀에 사정 끝난 조건이 판명하는 대로 개방
				PRINTFORMDW %CALLNAME:MASTER%는 얼룩을 할 수 있던 소매를 입도록(듯이) %CALLNAME:TARGET%에 부탁했다
				PRINTFORMDW %CALLNAME:TARGET%는 소매를 집어, 하부에 모인 국물을 확인하고 있다…
				PRINTFORML 
				PRINTFORMW 「위%RAND_SPLIT("//읏")%… 출렁출렁 하고 있어 소매의 모퉁이로부터 가는 체 물러날 수 있었던 정액이, 배어 나오고 있다」
			ELSE
			[SKIPEND]
				PRINTFORML 「이상한 곳에 구애받아요… 뭐 좋아요…」
				PRINTFORMDL 흰 소매가%CALLNAME:TARGET%의 겨드랑이를 마음껏 북돋운다
				EQUIP:악세사리 = 2
			;ENDIF
		;커맨드 출현 조건과 같은 조건을 청구서 박수의 배척
		;녀 %CALLNAME:MASTER%의 경우【굴복:22】질내사정 된 남자 %CALLNAME:MASTER%의 경우【굴복:42】애널에 사정되는 것을 요구
		ELSEIF RESULT == 21 && PENIS(TARGET) && (TALENT:MASTER:남자 == 0 && GETBIT(CFLAG:각인, 22)) || (TALENT:MASTER:남자 && GETBIT(CFLAG:각인, 42))
			;새디즘 추가 묘사
			IF TALENT:새디즘
				CALL FADEOUT(20, "「에에. 제대로 알고 있어요」")
				CALL FADEIN(12, "「난폭하게 되고 싶네요」")
				IF ITEM:줄
					;PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%에 지폐를 붙였다
					;사랑스러움의 보강
					PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%조금 지폐를 붙였다
					TEQUIP:줄 = 1
				ENDIF
			ELSE
				PRINTFORMW 「에에. 알았어요」
			ENDIF
			;자지의 준비가 되어 있지 않은 경우
			IF TCVAR:TARGET:발기도 < 1000
				PRINTFORML 「그 앞에 이것, 크게 해 줘」
				PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%\@ TALENT:새디즘 ? 의 목 안에 음경을 쑤셔 넣어 할 수 있었다# 에 입으로 봉사시키고 사정 준비를 시켰다\@
				EXP:MASTER:구음경험 ++
				;갈것같다인 경우 이대로 입안에서 좋아
				IF TCVAR:TARGET:갈것같다
					PRINTFORML 「아─안돼 이것, 이대로 입안에 사정해요」
					;이라마치오
					TCVAR:행동예약 = 56
					RETURN 1
				ELSE
					PRINTFORML 「슬슬 좋아요. 더 이상 하면 사정해 버릴 것 같다」
					BASE:TARGET:사정 = MAXBASE:TARGET:사정
					TCVAR:TARGET:발기도 = 1500
					TCVAR:TARGET:갈것같다 = 1
				ENDIF
			ENDIF
			;당신에게 그대로응이 있는 경우
			IF TALENT:MASTER:남자 == 0
				;도구 해제 처리
				IF TEQUIP:바이브 || TEQUIP:페니스밴드 || TEQUIP:삼각목마
					TEQUIP:페니스밴드 = 0
					TEQUIP:바이브 = 0
					TEQUIP:삼각목마 = 0
				ENDIF
				;녀 당신 플레이어에는 어딘지 부족하다고 생각합니다만 통상남 당신 메인의 구상인 것으로 이 정도로 용서해 주세요
				IF TALENT:새디즘
					;압박감을 갖게하기 때문에(위해) 하트나 음표는 사용하지 않는다. 단락이 부족하게 되므로 때문 개행
					;PRINTFORML 「그러면, 너의 빼기구멍 사용해 주기 때문에 엉덩이를 여기에 향하여 스스로 넓히세요」
					PRINTFORML 「좋아요. 너의 빼기구멍 사용해 주기 때문에, 엉덩이를 여기에 향하여」
					PRINTFORML 「스스로 넓히세요」
					;성교 후배위 조교자→조교 대상
					TCVAR:행동예약 = 31
				ELSE
					PRINTFORML 「그러면, 합시다」
					;PRINTFORMDW %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 뺨에 입맞추었다
					PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 뺨에 입맞추면 상냥하고 이불에 밀어넘어뜨리기 덮어씌워 왔다
					;성교 정상위 조교자→조교 대상
					TCVAR:행동예약 = 30
				ENDIF
			;당신에게 그대로응이 없는 경우
			ELSE
				IF TEQUIP:MASTER:하반신윗도리 || TEQUIP:MASTER:하반신속옷
					TEQUIP:MASTER:하반신윗도리 = 0
					TEQUIP:MASTER:하반신속옷 = 0
				ENDIF
				;도구 해제 처리
				IF TEQUIP:애널비즈 || TEQUIP:애널바이브 || TEQUIP:관장기＋플러그 || TEQUIP:삼각목마
					TEQUIP:애널비즈 = 0
					TEQUIP:애널바이브 = 0
					TEQUIP:관장기＋플러그 = 0
					TEQUIP:삼각목마 = 0
				ENDIF
				PRINTFORML 「그러면 엉덩이를 여기에 향하세요」
				;애널섹스 조교자→조교 대상
				TCVAR:행동예약 = 35
			ENDIF
		ELSE
			PRINTFORMW 「생각해 떠오르지 않았을까」
		ENDIF
	ENDIF
	;이 뒤로 계속되는 %CALLNAME:TARGET%님대만족을 소거해 상부에 설치 소거 되어 있지 않은 모양
	RETURN 2
ENDIF

;─
;EV22, 상태변화 구상
;─
;·상태 이상이 붙었을 때에 표시
;·TCVAR:상태변화가 조교자, TCVAR:MASTER:상태변화가 조교 대상
;·상태변화가 일어나면 3 턴 지속한다
;-------------------------------------------------
@KOJO_EVENT_K1_22(ARG)
;-------------------------------------------------
;*************************************************
;EV22_0, 조교자 상태변화
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[피폐]화 소모시    피로를 위해 소극적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_CONDITION(TARGET, "피폐")

ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[쇠약]화 강소모시  피로를 위해 소극적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_CONDITION(TARGET, "쇠약")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[무기력]화 무기력시  권태이기 때문에 소극적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_CONDITION(TARGET, "무기력")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[몽롱]화 강절정시  쾌감이기 때문에 향락적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_CONDITION(TARGET, "몽롱")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[정욕]화 고욕정시  욕정을 위해 긍정적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(50) && IS_CONDITION(TARGET, "정욕")
	PRINTFORMDW %CALLNAME:TARGET%는 눈을 하트로 하면서 입맛을 다심 했다
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[분노]화 강반발시  분노이기 때문에 부정적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(50) && IS_CONDITION(TARGET, "분노")
	PRINTFORMW 「의문! 퇴치하고 해 줄까…」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[싫증]화 저자극시  싫증을 위해 부정적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && IS_CONDITION(TARGET, "지루함")
	IF 구상기입체크(10) && AFFECTION("상사상애")
		;우후후 중단과 러브 러브의 양립을 꾀한다
		PRINTFORML 「아─… 남비. 화에 걸치고 있을 뿐이었어요」
	ELSEIF 구상기입체크(25)
		;새로운 플레이에 손을 대는 절결로서의 이유가 강하다고 해석
		PRINTFORMW 「슬슬 기회일까…」
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[울적]화 고억압시  억압을 위해 적극적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_CONDITION(TARGET, "울적")
ENDIF
;조교자의 경우 TALENT:광기가 있는 경우에게만 1/5의 확률로 울적화

;*************************************************
;EV22_1, 조교 대상 상태변화
;*************************************************
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[피폐]화 소모시    피로를 위해 소극적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
;휴게로 나가는→힘을 빼는→구상→%CALLNAME:MASTER%는[피폐]의 흐름.
IF 구상기입체크(20) && IS_CONDITION(MASTER, "피폐")
	PRINTFORMW 「조금만 더…」
	RETURN 1
ENDIF
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[쇠약]화 강소모시  피로를 위해 소극적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(20) && IS_CONDITION(MASTER, "쇠약")
	IF TALENT:모유체질
		PRINTFORML 「너무 했군요…… 괜찮아? 젖가슴 마셔?」
		TCVAR:행동예약 = 57
		TFLAG:ACT파생 = 2
	ELSEIF !RAND:2
		PRINTFORML 「이것은…」
		PRINTFORML 「너무 했군요」
		;아츠무 몽상 서 회풍
		PRINTFORMDL %CALLNAME:TARGET%는 땀마크를 띄우면서 쓴웃음 지었다
	ELSE
		PRINTFORML 「너무 했을까…」
		;령나암풍
		PRINTFORMDL %CALLNAME:TARGET%는 머리에 손을 대면서 곤란해 표정을 하고 있다
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[무기력]화 무기력시  권태이기 때문에 소극적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_CONDITION(MASTER, "무기력")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[몽롱]화 강절정시  쾌감이기 때문에 향락적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && IS_CONDITION(MASTER, "몽롱")
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[정욕]화 고욕정시  욕정을 위해 긍정적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(25) && IS_CONDITION(MASTER, "정욕")
	PRINTFORML 「눈초리가 추잡해요?」
	PRINTFORMDW %CALLNAME:TARGET%는 득의양양에 작게 웃었다
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[분노]화 강반발시  분노이기 때문에 부정적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(20) && IS_CONDITION(MASTER, "분노")
;동시 분노가 되었을 경우, 조교자가 상 %CALLNAME:MASTER%가 아래에 표시된다
	PRINTFORMW 「신선하구나. 너의 화난 얼굴은, 본 일 없었던 것」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[싫증]화 저자극시  싫증을 위해 부정적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(50) && IS_CONDITION(MASTER, "지루함")
	PRINTFORML 「응… 반응이 좀 더」
	PRINTFORMDW %CALLNAME:TARGET%는%CALLNAME:MASTER%의 뺨을 쿡쿡 찔러 반응을 확인해 왔다…
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[울적]화 고억압시  억압을 위해 적극적 행동이 우선
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(50) && IS_CONDITION(MASTER, "울적")
	PRINTFORMW 「가감(상태)가 어렵네요…」
	RETURN 1
ENDIF

;─
;EV23, 조교 대상 능력치 업 구상
;─
;·조교중에 조교 대상의 능력치가 상승했을 때에 표시
;-------------------------------------------------
@KOJO_EVENT_K1_23(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(이번 상승한 ABL 번호/각 ARG 번호는 ABL의 거기에 상당)
;
;ARG:0 순종/1욕망/2기교/3 C감각/4 V감각/5 A감각/6 B감각/7봉사정신/8노출버릇/9레즈끼/10 ＢＬ끼/11마조끼
;12자위중독/13정액중독/14레즈중독/15고통중독/16구속중독
;
;ARG:20 회화/21애무/22도구/23성교/24수치/25봉사/26가학/27이상
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[순종]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(25) && ARG == 0
	PRINTFORMW 「그래그래. 순수가 제일이야」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[욕망]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[기교]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 2
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[C감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[V감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 4
	SELECTCASE ABL:MASTER:V감각
		CASE 1 TO 3
			PRINTFORM 「좋은 상태에 풀려 왔군요
			CALL HEARTBW
		CASEELSE
			PRINTFORMW 「발 이렇게와 큐우큐우 단단히 조여 와… 완전히…」
			PRINTFORM 「어쩔 수 없네요…
			CALL HEARTBW
	ENDSELECT
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[A감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 5
	;굳이 개발 도상에서 1회만 감상
	IF ABL:MASTER:A감각 == 3
		PRINTFORML 「… 완전히… 변태인 것이니까」
	ELSE
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;조교 대상[B감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 6
	;당신에게 그대로응이 있는 경우
	IF TALENT:MASTER:남자 == 0
		
	;당신에게 자지가 있는 경우
	ELSE
		
	ENDIF
	RETURN 1
ENDIF

;─
;EV24, 조교자 능력치 업 구상
;─
;·조교중에 조교자의 능력치가 상승했을 때에 표시
;-------------------------------------------------
@KOJO_EVENT_K1_24(ARG)
;-------------------------------------------------
;인수 내역…
;ARG=(이번 상승한 ABL 번호)
;
;ARG:0 순종/1욕망/2기교/3 C감각/4 V감각/5 A감각/6 B감각/7봉사정신/8노출버릇/9레즈끼/10 ＢＬ끼/11마조끼
;12자위중독/13정액중독/14레즈중독/15고통중독/16구속중독
;
;ARG:20 회화/21애무/22도구/23성교/24수치/25봉사/26가학/27이상
;-------------------------------------------------
;자기 개발의 경우도 있기 때문에(위해), 분기 조건에 주의
;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[욕망]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[기교]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 2
	PRINTFORML 「조금씩 너의 일, 알아 왔어요」
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[C감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 3
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[V감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG == 4
	PRINTFORMDL %CALLNAME:TARGET%의%RAND_SPLIT("밀호/질내/보지")%는, 보다%CALLNAME:MASTER%의 음경에 맞추어 풀려 온 것 같다…
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[A감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 5
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;주인님님[B감각]업
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(0) && ARG == 6
	IF TALENT:모유체질
		PRINTFORM 
	ELSE
		PRINTFORML 
	ENDIF
	RETURN 1
ENDIF

;─
;EV29, 굴복·고통각인에 의한 반항각인 상한 처리
;─
;·반항각인 Lv상한의 처리
;·반항각인은 10-(고통각인+굴복각인)/2를 넘지 않는다
;-------------------------------------------------
@KOJO_EVENT_K1_29(ARG)
;-------------------------------------------------
;%CALLNAME:TARGET%가 주는 쾌락\@ MARK:MASTER:고통각인? 라고 고통 # \@가 %CALLNAME:MASTER%로부터 반항의 기력을 빼앗아 간다…
;반항각인은{MARK:반항각인}이 되었다
;-------------------------------------------------
IF 구상기입체크(0)
ENDIF

;========================================================================
;각인 취득 구상
;========================================================================
;·조교중에 각인을 취득시켰을 경우에 표시. 어느 레벨로 구상 표시할까는 가감(상태) 자유로운 것으로 개개로 조정하는 것이 바람직함
;·기입 체크가=1의 경우, 동시에 복수 취득했을 때는 이 파일로 맨 위의 것만을 표시한다
;·=2의 경우, 취득한 각인의 구상 모든 것을 위로부터 순서에 표시한다
;·각인 취득에 의한 소질 변동전에 표시하기 위해(때문에), 소질 취득/상실할까는 이 시점에서의 TALENT의 유무로 판정할 수 있다
;-------------------------------------------------
@KOJO_MARK_K1(ARG:0, ARG:1, ARG:2, ARG:3, ARG:4)
#DIM 기입체크
#DIM 모두순서에표시
모두순서에표시 = 0	;각인 취득 구상 「모두 순서에 표시」를 위한 초기화 처리
;-------------------------------------------------
;인수 내역…
;ARG:(각인 번호)=(이번 취득한 레벨)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;[트라우마]취득
기입체크 = 0	;0. 비표시/1. 동시에 취득한 각인을 이것 이하 표시하지 않는다/2. 모두 순서에 표시한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 기입체크 && ARG:4
	SETBIT 모두순서에표시, 4
	SELECTCASE ARG:4
		;트라우마 Lv1
		CASE 1
		;트라우마 Lv10…[겁쟁이]가 없는 경우, 그것을 얻는다
		;              이미[겁쟁이]가 있어[광기]가 없는 경우,[유치]가 있는 경우는 100%, 없는 경우는 1/3의 확률로[광기]를 얻는다
		;              [광기]의 취득 판정에 새어, 한편[유치]를 가지지 않는 경우,[유치]를 얻는다(유아 퇴행 한다)
		;              현상,[광기]와[유치]는 어느 쪽으로 구를까 먼저 판정할 방법은 없다(요망이 있으면…). 우선 머리가 이상해진다고 해석해 주세요
		CASE 10
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 4
	ENDSELECT
	SIF GETBIT(모두순서에표시, 4) && 기입체크 == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[고통각인]취득
기입체크 = 0	;0. 비표시/1. 동시에 취득한 각인을 이것 이하 표시하지 않는다/2. 모두 순서에 표시한다
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 기입체크 && ARG:0
	SETBIT 모두순서에표시, 0
	SELECTCASE ARG:0
		;고통각인 Lv2
		CASE 2
		;고통각인 Lv9…[기장]이 있는 경우, 그것을 잃는다
		CASE 9
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 0
	ENDSELECT
	SIF GETBIT(모두순서에표시, 0) && 기입체크 == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[반항각인]취득
기입체크 = 0	;(0, 비표시, 1, 동시에 취득한 각인을 이것 이하 표시하지 않는, 2, 모두 순서에 표시한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 구상기입체크(100) && ARG:3
	SETBIT 모두순서에표시, 3
	SELECTCASE ARG:3
		;반항각인 Lv2
		CASE 2
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 3
	ENDSELECT
	SIF GETBIT(모두순서에표시, 3) && 기입체크 == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[굴복각인]취득
기입체크 = 0	;(0, 비표시, 1, 동시에 취득한 각인을 이것 이하 표시하지 않는, 2, 모두 순서에 표시한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 기입체크 && ARG:2
	SETBIT 모두순서에표시, 2
	SELECTCASE ARG:2
		;굴복각인 Lv2
		CASE 2
			PRINTFORMW 「슬슬, 따라줄까?」
		;성교에 이르기까지는 7에 이르지 않을 가능성이 높다
		;굴복각인 Lv7…[반항적]이 있는 경우, 그것을 잃는다
		CASE 7
			;이 표현에서는 오히려 부추겨 버리기 때문에(위해) comment out
			;PRINTFORMW 「이제(벌써), 항복일까?」
			PRINTFORMW 「너는, 충분히 가져 견뎠어요」
		;굴복각인 Lv10…[프라이드높음]이 있는 경우, 그것을 잃는다
		CASE 10
			;항복시키는 것 이기면 강신이 내리지만 혼동하기 쉽기 때문에 조정 comment out
			;참마를 갈면 귀신이 모퉁이와 착각 해 도망친다
			;PRINTFORMW 「귀신일거라고 신이어도 끌어 내려 보여요. 물론 너도」
			;7에 이행. 조금 개변
			;PRINTFORMW 「너는, 충분히 가져 견뎠다고 생각해요?」
			
			;이것으로 어때. 프라이드높음 당신과 어깨를 나란히 하는 레이무.
			;지금까지 비합의였어요? 그렇다고 하는 의문이 남는다
			;레이무씨는 항상적으로 비합의 플레이를 걸고 있었다고 하는 일로 어떻게든
			;레이무씨의 감성 합의근처 안정도＞합의 섹스＞비합의 섹스＞비합의근처 안정도
			;나쁘지 않다. 이제 와서 감이 좋은 맛을 내 준다.
			PRINTFORMW 「근처. 좋, 네요」
			;PRINTFORMDW %CALLNAME:TARGET%는 굳이 %CALLNAME:MASTER%에 허가를 요구해 왔다
			PRINTFORMDW %CALLNAME:TARGET%는 굳이 허가를 요구해,
			;남 당신의 신장＞작은 소녀인 레이무씨의 신장
			PRINTFORMDW %CALLNAME:MASTER%의 근처에 툭 앉아 왔다
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 2
	ENDSELECT
	SIF GETBIT(모두순서에표시, 2) && 기입체크 == 1
		RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;[쾌락각인]취득
기입체크 = 0	;(0, 비표시, 1, 동시에 취득한 각인을 이것 이하 표시하지 않는, 2, 모두 순서에 표시한다)
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 기입체크 && ARG:1
	SETBIT 모두순서에표시, 1
	SELECTCASE ARG:1
		;쾌락각인 Lv2
		CASE 2
		;쾌락각인 Lv5…[쾌감을부정]이 있는 경우, 그것을 잃는다
		CASE 5
		;쾌락각인 Lv7…[억압]이 있는 경우, 그것을 잃는다
		CASE 7
		;쾌락각인 Lv10…[쾌감에순수]가 없는 경우, 그것을 얻는다
		CASE 10
		;표시하지 않는다
		CASEELSE
			CLEARBIT 모두순서에표시, 1
	ENDSELECT
	SIF GETBIT(모두순서에표시, 1) && 기입체크 == 1
		RETURN 1
ENDIF

;각인 취득 구상 「모두 순서에 표시」를 위한 종단 처리
RETURN 모두순서에표시 != 0